# Deepline — Complete Tool & Integration Reference

*Verbatim reference compiled from deepline.com documentation (scraped 2026-06-25). Contains every provider integration, every endpoint, its execution code (spreadsheet step + direct CLI), required fields, and Deepline credit pricing — plus core CLI/API/SDK/database concepts, all plays, and the full master pricing schedule. Marketing, blog, comparison, and SEO-directory content has been removed.*

---

## How to read this file

Every provider endpoint below shows up to four things:

- **Spreadsheet step** — the `deepline enrich --with 'result=<tool_id>:{...}'` command to run the tool as a column over a CSV. Use `{{column_name}}` placeholders to map payload values to your CSV columns.
- **Required fields** — the payload schema (name, type, required, default, description).
- **Direct CLI** — the `deepline tools execute <tool_id> --payload '{...}'` command for single-payload debugging.
- **Cost** — pricing model, estimated Deepline credits per pricing unit, and billing mode. (BYOK users pay $0 on Deepline for all of these; managed-credit users pay the listed credits.)

### Common CLI flags (apply to every `deepline tools execute` and `deepline enrich` call)

| Flag | Description |
| --- | --- |
| `--json` | Print machine-readable output. |
| `--wait` | Wait for terminal provider status when supported. |
| `--debug` | Enable wait mode with additional status/log output. |
| `--wait-timeout SECONDS` | Max seconds to wait in wait mode. |
| `--poll-interval SECONDS` | Polling interval in seconds during wait mode. |
| `--timeout SECONDS` | Request timeout in seconds. |
| `--connect-timeout SECONDS` | Connection timeout in seconds. |

### Billing-mode glossary

- `post_deduct` — credits deducted after the call completes.
- `no_bill` — no Deepline credit billing (provider-native pricing may still apply; common for sequencer/CRM ops and free lookups).
- `pre_deduct` — credits reserved before the call.


---

# PART 1 — Core Concepts (CLI, API, Database, SDK, Playground)


---

## Quickstart

# Find a verified work email in under a minute

Deepline is a CLI and API for GTM data enrichment. It runs waterfall searches across 79 providers (Apollo, Hunter, Prospeo, etc.) so you get more results than any single tool, and only pay for the provider that finds a hit.

## Watch the setup

Getting Started with Deepline: Go-to-Market Data in Claude Code - YouTube

Tap to unmute

[Getting Started with Deepline: Go-to-Market Data in Claude Code](https://www.youtube.com/watch?v=vdTBllkeZoA) [Deepline](https://www.youtube.com/channel/UCGQz99LS042omx7Sf3yPUrQ)

Deepline134 subscribers

[Watch on](https://www.youtube.com/watch?v=vdTBllkeZoA)

## 1\. Install

- Claude Code

- Claude Cowork

### Prerequisites

- Node.js 20+
- Python 3.10+
- npm
- [Claude Code](https://claude.ai/download) or any AI coding agent

### Install CLI

This downloads the `deepline` binary and adds it to your PATH:

macOS/Linux

```
curl -s "https://code.deepline.com/api/v2/cli/install" | bash
```

The install script handles authentication automatically — no extra steps needed.

Install Deepline as a Claude Cowork plugin instead of running the shell installer.

1. Open Claude Desktop and switch to **Cowork**.
2. Open **Customize** in the left sidebar.
3. Click the **+** next to **Personal plugins**.
4. Open **Create plugin**, then choose **Add marketplace**.5. Add the Deepline plugin marketplace:

```
https://github.com/getaero-io/deepline-plugins
```

6. Install the `deepline` plugin.
7. Enable internet access for **all domains** in the Cowork session settings.
8. Start a Cowork session and run:

```
/deepline-quickstart
```

The first Deepline command may install the CLI inside the Cowork sandbox. That is expected.

## 2\. Use the skill

The Deepline skill auto-syncs when you install the CLI. In Claude Code, type:

```
/deepline-gtm Find a verified work email for Jane Smith at Stripe
```

The `/deepline-gtm` prefix tells Claude Code to use the Deepline skill, which knows all 79 providers, waterfall logic, and cost optimization. Describe what you want in plain English after the prefix.

## 3\. Run your first recipe

The install script will automatically start a guided wizard to pick and run your first recipe. You can also come back to it any time with `deepline quickstart`.Or type directly in Claude Code:

```
/deepline-gtm Find the work email for a Chief Marketing Officer in NYC
```

That’s it. Deepline runs a waterfall across multiple providers and returns a verified work email.

CLI command (advanced)

```
deepline tools execute company_to_contact_by_role_waterfall \
  --payload '{"company_name":"Acme","domain":"acme.com","roles":["Chief Marketing Officer"]}'
```

The waterfall tries multiple providers and stops at the first valid hit. You only pay for the one that finds the email. This gets **20-40% more emails** than any single provider ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)).

## What just happened

1. Deepline searched for a CMO in NYC across prospecting providers
2. It ran a **waterfall** across email providers
3. The first provider to return a valid, verified email wins
4. You paid for one successful lookup — typically 0.3-0.4 credits

## Scale it up

Got a list? Just ask:

```
/deepline-gtm I have a CSV with company names and domains. Find work emails for the VP of Sales at each one.
```

Deepline reads your file, picks the right waterfall, and enriches every row.**Pilot mode:** Add “just test on the first 2 rows” to any prompt to review before processing the full file.

## Next steps

[**CLI Concepts** \\
\\
How tools, payloads, and waterfall execution work under the hood.](https://deepline.com/docs/cli-concepts)

[**Playground** \\
\\
Batch-enrich CSVs locally with row-level provider routing.](https://deepline.com/docs/playground-concepts)

[**Email Waterfall Recipe** \\
\\
Step-by-step: find verified emails for a list of contacts.](https://deepline.com/docs/plays/email-waterfall)

[**Provider Integrations** \\
\\
Per-provider guides, schemas, and sample outputs.](https://deepline.com/docs/providers/apollo/guide)

[**I Have X, I Want Y** \\
\\
Pick the right workflow based on what data you already have.](https://deepline.com/docs/plays/decision-tree)

[**Example Skills** \\
\\
Open-source GTM workflow templates on GitHub.](https://github.com/getaero-io/gtm-eng-skills)

* * *

Reach us: `/deepline-feedback` in Claude Code, or [team@deepline.com](mailto:team@deepline.com).


---

## CLI Concepts

## Overview

Deepline CLI turns raw lead/account CSVs into action-ready outputs with repeatable, scriptable workflows.

This CLI is not built for humans - its built for agents. It gives Claude Code and other Agents a sophisticated toolset to work with.

### Why teams use it

- Run deterministic enrichment pipelines over full spreadsheets, not one-off requests.
- Chain provider lookups and AI steps into the same workflow.
- Keep outputs in CSV/JSON so RevOps and outbound systems can consume them fast.

## Command Signatures

```
deepline tools execute <tool_id> --payload JSON [--json] [--wait] [--debug] [--wait-timeout SECONDS] [--poll-interval SECONDS] [--timeout SECONDS] [--connect-timeout SECONDS]
deepline enrich --input /path/to/file.csv --output /path/to/output.csv --with 'summary=run_javascript:{"code":"return row;"}' --json
```

## Command Families

| Family | Signatures |
| --- | --- |
| `enrich` | deepline enrich —input /path/to/file.csv —output /path/to/output.csv —with ‘summary=run\_javascript:{“code”:“return row;”}’ —json |
| `tools` | deepline tools execute <tool\_id> —payload JSON \[—json\] \[—wait\] \[—debug\] \[—wait-timeout SECONDS\] \[—poll-interval SECONDS\] \[—timeout SECONDS\] \[—connect-timeout SECONDS\] |

## Global Flags

No options discovered from CLI source.

## Enrich Pipeline Concepts

```
deepline enrich --input /path/to/file.csv --output /path/to/output.csv --with 'summary=run_javascript:{"code":"return row;"}' --json
```

—with spec format: `<column>=<tool_ref>:{json_payload}`

No options discovered from CLI source.

## Waterfall Enrichment

Use waterfalls when you want fallback providers for the same output column, in priority order.
Each waterfall block groups multiple `--with` steps into one target output. Deepline keeps the first successful result.

### Company enrichment waterfall

```
deepline enrich \
  --input accounts.csv \
  --output accounts.enriched.csv \
  --with-waterfall company \
  --with 'company=crustdata_companydb_search:{"filters":[{"filter_type":"company_website_domain","type":"=","value":"{{domain}}"}],"limit":1} => extract(["data.companies.0","data.0"])' \
  --with 'company=peopledatalabs_company_search:{"query":{"term":{"website":"{{domain}}"}}} => extract(["data.companies.0"])' \
  --end-waterfall \
  --json
```

### Contact enrichment waterfall

```
deepline enrich \
  --input contacts.csv \
  --output contacts.enriched.csv \
  --with-waterfall person \
  --with 'person=dropleads_search_people:{"filters":{"workEmails":["{{email}}"]},"pagination":{"page":1,"limit":1}} => extract(["data.people.0","people.0","results.0"])' \
  --with 'person=peopledatalabs_person_search:{"query":{"bool":{"must":[{"term":{"email":"{{email}}"}}]}}} => extract(["data.records.0"])' \
  --end-waterfall \
  --json
```


---

## Playground Concepts

## Overview

Use Playground commands to run row-range and column-block workflows on CSV files.
Use Playground for row-range and column-block execution on CSV workflows.

## Command Signatures

```
deepline csv render --csv /path/to/file.csv
```

## Execution Flow

1. Start a CSV renderer session with `deepline csv render --csv ...`.
2. Read row windows with `deepline csv show --csv /path/to/file.csv --rows START:END`.
3. Execute cells with `deepline csv --execute_cells --csv /path/to/file.csv --rows START:END --cols START:END --wait --json`.
4. Stop sessions with `deepline backend stop` or individual columns with `deepline csv --stop-column`.


---

## REST API Reference

Use the REST API when Deepline is called from a backend service, serverless
function, notebook, Airflow job, warehouse task, or other non-CLI runtime.

## Authentication

All public REST endpoints accept a workspace API key as a bearer token:

```
Authorization: Bearer <DEEPLINE_API_KEY>
```

No request-signing scheme is required for direct server-side calls to tool or
play endpoints. Store the API key only in trusted server-side environments. Do
not expose it in browser code.

## Execute a tool

Tool execution requests use the tool ID in the URL:

```
POST /api/v2/integrations/{toolId}/execute
```

The request body wraps the tool input in `payload`:

```
{
  "payload": {
    "domain": "stripe.com"
  }
}
```

Example:

```
curl -X POST "https://code.deepline.com/api/v2/integrations/test_company_search/execute" \
  -H "Authorization: Bearer $DEEPLINE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "payload": {
      "domain": "stripe.com"
    }
  }'
```

`POST /api/v2/integrations/execute` is the legacy endpoint and should not be
used for new callers.

## Deno / Supabase Edge Functions

```
const response = await fetch(
  'https://code.deepline.com/api/v2/integrations/test_company_search/execute',
  {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${Deno.env.get('DEEPLINE_API_KEY')}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      payload: {
        domain: 'stripe.com',
      },
    }),
  },
);

if (!response.ok) {
  throw new Error(
    `Deepline returned ${response.status}: ${await response.text()}`,
  );
}

const result = await response.json();
```

## Execute a saved or prebuilt play

For multi-step workflows, start a play run:

```
curl -X POST "https://code.deepline.com/api/v2/plays/run" \
  -H "Authorization: Bearer $DEEPLINE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "person-linkedin-to-email",
    "input": {
      "linkedin_url": "https://www.linkedin.com/in/example-person/",
      "first_name": "Jane",
      "last_name": "Smith",
      "company_name": "ExampleCo",
      "domain": "example.com"
    }
  }'
```

Then poll the returned `workflowId`:

```
curl "https://code.deepline.com/api/v2/plays/run/$WORKFLOW_ID" \
  -H "Authorization: Bearer $DEEPLINE_API_KEY"
```

## Environment variables

- `DEEPLINE_API_KEY` (workspace-scoped)
- Per-provider keys are documented in each provider guide (for BYOK providers)

For payload fields on each action, open the provider page under `Integrations` and pick the operation.


---

## Customer Data Access (PostgreSQL)

# Customer Data Access

This guide is for building product features on your workspace data quickly. It
covers two access paths:

- `query_customer_db`, the agent-callable tool for bounded SQL reads and
`storage`-scoped writes.
- Direct PostgreSQL connection URIs for apps, BI tools, and admin workflows.

Start with the tool path when an agent is working inside Deepline. Use direct
connection URIs when your own application needs a Postgres connection string.

## What You Get

- One dedicated database per workspace.
- Ready-to-query views for people and companies.
- An agent-writable `storage` schema for `query_customer_db`.
- Direct connection roles for application and reporting access.

## Agent Tool: `query_customer_db`

Use `query_customer_db` when Claude, Codex, or another agent needs to inspect
workspace data through Deepline APIs.

```
deepline tools execute query_customer_db --payload '{
  "sql": "SELECT domain, company_name FROM enrichments.companies ORDER BY updated_at DESC LIMIT 20"
}'
```

`query_customer_db` runs one SQL statement at a time. It supports:

- `SELECT`, `EXPLAIN`, and read-only `WITH` queries.
- Aggregate queries such as `SELECT count(*) ...`.
- `INSERT`, `UPDATE`, `DELETE`, `CREATE TABLE`, `CREATE INDEX`,
`ALTER TABLE`, `DROP TABLE`, and `TRUNCATE` only for schema-qualified
`storage.<table>` targets.

The `storage` schema is the safe write path for agent-created tables and
scratch state:

```
deepline tools execute query_customer_db --payload '{
  "sql": "CREATE TABLE IF NOT EXISTS storage.agent_notes (id text PRIMARY KEY, note text NOT NULL, created_at timestamptz NOT NULL DEFAULT now())"
}'
```

Then query or update that table through the same tool:

```
deepline tools execute query_customer_db --payload '{
  "sql": "INSERT INTO storage.agent_notes (id, note) VALUES ('\''run-123'\'', '\''ready for review'\'') ON CONFLICT (id) DO UPDATE SET note = EXCLUDED.note"
}'
```

```
deepline tools execute query_customer_db --payload '{
  "sql": "SELECT id, note, created_at FROM storage.agent_notes ORDER BY created_at DESC LIMIT 20"
}'
```

Do not use `query_customer_db` for arbitrary DDL or migrations. These examples
are blocked:

```
CREATE TABLE agent_notes (...);
CREATE TABLE tenant_custom.agent_notes (...);
DELETE FROM enrichments.companies WHERE domain = 'example.com';
```

If you see `Only read statements or schema-qualified storage tables are supported.` or `Customer DB writes are only allowed for schema-qualified storage tables.`, rewrite the statement to target `storage.<table>` exactly, or make the
query read-only.

## Quick Start: Get Access in Minutes

Use this flow when you need a direct PostgreSQL connection string.

### 1) Check workspace database status

Use:

```
GET /api/v2/ingestion/status
```

### 2) Provision if needed

If not provisioned, run:

```
POST /api/v2/ingestion/provision
```

You are ready when `provisioned` is `true` and plane status is `active`.

### 3) Reveal a connection URI

Use:

```
POST /api/v2/ingestion/connection-uris/reveal
{
  "kind": "customer_db",
  "pooled": true
}
```

The revealed customer DB URI uses the workspace-scoped customer database role.
It can read permitted workspace schemas and can write to the `storage` schema.
Use the same `storage.<table>` convention for app-created tables unless you are
working through a separate admin/runtime migration path.

### 4) Connect from your app

```
import { Client } from 'pg';

const client = new Client({ connectionString: process.env.DB_URI });
await client.connect();

const { rows } = await client.query(
  'SELECT identity_key, display_name FROM dl_resolved.people ORDER BY updated_at DESC LIMIT 20',
);
```

## Build Features Fast (Vibe-Coding Patterns)

### Read customer records for app screens

Use:

- `dl_resolved.people`
- `dl_resolved.companies`

These views already merge base records and customer overrides.

### Store app-specific objects through the agent tool

Create agent-owned tables in `storage`:

```
CREATE TABLE IF NOT EXISTS storage.account_segments (
  id text PRIMARY KEY,
  segment text NOT NULL,
  created_at timestamptz NOT NULL DEFAULT now()
);
```

## API Endpoints You Will Use

- `GET /api/v2/ingestion/status`
  - Check whether your workspace database is provisioned and active (API key or session auth).
- `POST /api/v2/ingestion/provision`
  - Create database resources for the current workspace (dashboard/admin session).
- `GET /api/v2/ingestion/connection-uris/metadata`
  - Return project/database/role metadata for active workspaces (dashboard/admin session).
- `POST /api/v2/ingestion/connection-uris/reveal`
  - Reveal a pooled URI for `customer_db` (dashboard/admin session).
- `POST /api/v2/ingestion/connection-uris/rotate`
  - Rotate the customer DB role password (dashboard/admin session).

## Deep Reference: Schema Contract

### Managed schemas (platform-owned)

- `dl_cache`
  - `person_enrichments`
  - `company_enrichments`
- `dl_override`
  - `person_overrides`
  - `company_overrides`
- `dl_resolved`
  - `people` (view)
  - `companies` (view)
- `dl_meta`
  - `schema_migrations`
  - `tenant_settings`

### Agent storage schema

- `storage`
  - DDL and DML allowed for `query_customer_db` and the customer DB role.
  - Always schema-qualify writes as `storage.<table>`.

## Deep Reference: Role Access

- Customer DB role
  - Read access to permitted workspace schemas.
  - DDL and DML on `storage.*`.
- Runtime role
  - Internal app/runtime operations across managed schemas.

## Operational Notes

- Database access is workspace-scoped, not user-scoped.
- If the backing project was deleted manually, run `POST /api/v2/ingestion/provision` again.
- Branch lifecycle is platform-managed for this tenant model.
- `query_customer_db` accepts a single SQL statement. Do not prepend
`SET search_path`; schema-qualify table names instead.


---

## Claude Cowork

# Claude Cowork

Deepline supports Claude Cowork through a plugin. Install it from the Deepline plugin marketplace in Claude Desktop.

[**Install the Claude Cowork plugin** \\
\\
Follow the Cowork tab in the main Quick Start install instructions.](https://deepline.com/docs/quickstart#claude-cowork-install)

Deepline needs Cowork network egress for live API calls. In Claude Desktop, open **Settings** -\> **Capabilities**, turn on **Allow network egress**, then set **Domain allowlist** to **All domains**.


---

## Free Company Search Database

# Free company search database

Use `free_simple_company_search` when you need a free, SQL-readable company corpus for exact company resolution, bounded list building, and quick segment checks.It is a Deepline Native tool backed by a shared Snowflake `companies` table. It is not a live provider API call. The current corpus is loaded from a raw People Data Labs company CSV snapshot, transformed into Deepline’s normalized schema, quality-checked, and then exposed as the read-only `companies` table.

Deepline does not charge credits for `free_simple_company_search`. Broad
queries can still time out, so treat it as a free database with guardrails,
not an unlimited scanner.

## When to use it

Use it for:

- Exact domain lookup when you have websites or normalized domains.
- Small exact batches by domain, LinkedIn company URL, or company name.
- Prefix candidate generation, such as `company_name ILIKE 'acme%'`.
- Bounded ICP pulls where industry, location, size, or founding year are enough.
- Quick exploratory counts before deciding whether to use paid company search.

Use a provider-native company search instead when you need live web coverage, advanced facets, funding or investor filters, hiring signals, strict market sizing totals, or semantic discovery by what a company does.

## Source

The production loader expects the raw PDL company CSV header:

```
country,founded,id,industry,linkedin_url,locality,name,region,size,website
```

Deepline transforms that source into the query table:

| Source field | Deepline column |
| --- | --- |
| `id` | `source_record_id` |
| `website` | `domain`, `normalized_domain` |
| `name` | `company_name` |
| `industry` | `industry` |
| `locality`, `region`, `country` | `location` |
| `linkedin_url` | `linkedin_url` |
| `size` | `employee_count` |
| `founded` | `year_founded` |

`updated_at` is set during Deepline’s table load. It should be read as import/update time for this corpus, not as proof that PDL recently observed the company.

## Schema

Query the table as `companies`. `public.companies` also works for compatibility.

| Column | Type | Notes |
| --- | --- | --- |
| `source_record_id` | text | Source row id from the snapshot. |
| `normalized_domain` | text | Lowercase root domain with protocol, `www.`, path, and trailing dot removed. Fastest lookup key when you have a domain. |
| `domain` | text | Original website/domain from the source. |
| `company_name` | text | Company name. Required in the loaded table. |
| `industry` | text | Source industry label, for example `Computer Software`. |
| `location` | text | Comma-separated locality, region, country. |
| `linkedin_url` | text | LinkedIn company URL or slug from the source. |
| `employee_count` | integer | Upper bound of the source size bucket: `10`, `50`, `200`, `500`, `1000`, `5000`, `10000`, or `10001` for `10000+`. |
| `year_founded` | integer | Source founding year when present. |
| `updated_at` | timestamp | Deepline load/update timestamp. |

## Query from the CLI

Inspect the live tool schema first:

```
deepline tools get free_simple_company_search
```

Exact domain lookup:

```
PAYLOAD=$(cat <<'JSON'
{
  "sql": "SELECT normalized_domain, domain, company_name, industry, location, employee_count, year_founded FROM companies WHERE normalized_domain = 'openai.com' LIMIT 5"
}
JSON
)

deepline tools execute free_simple_company_search --payload "$PAYLOAD"
```

Use the same payload shape for other SQL queries.Batch domain lookup SQL:

```
SELECT normalized_domain, company_name, linkedin_url
FROM companies
WHERE normalized_domain IN ('openai.com', 'anthropic.com', 'vercel.com')
LIMIT 10
```

LinkedIn company URL lookup SQL:

```
SELECT company_name, normalized_domain, linkedin_url
FROM companies
WHERE linkedin_url IN ('linkedin.com/company/openai')
LIMIT 5
```

Prefix fallback SQL when exact lookup misses:

```
SELECT company_name, normalized_domain, location, employee_count
FROM companies
WHERE company_name ILIKE 'acme%'
LIMIT 25
```

Bounded segment count SQL:

```
SELECT COUNT(*) AS company_count
FROM companies
WHERE industry ILIKE '%software%'
  AND employee_count >= 50
LIMIT 1
```

Bounded company pull SQL:

```
SELECT normalized_domain, company_name, industry, location, employee_count, year_founded
FROM companies
WHERE location ILIKE 'san francisco%'
  AND year_founded >= 2020
ORDER BY employee_count DESC
LIMIT 100
```

## Query from the API

Use the normal tool execution endpoint:

```
curl -X POST "https://code.deepline.com/api/v2/integrations/free_simple_company_search/execute" \
  -H "Authorization: Bearer $DEEPLINE_API_KEY" \
  -H "Content-Type: application/json" \
  --data-binary @- <<'JSON'
{
    "payload": {
      "sql": "SELECT normalized_domain, company_name FROM companies WHERE normalized_domain = 'openai.com' LIMIT 5"
    }
}
JSON
```

The response wraps the SQL result in `data`:

```
{
  "data": {
    "schema": "public.companies",
    "command": "SELECT",
    "row_count_returned": 1,
    "truncated": false,
    "columns": [\
      { "name": "company_name", "table_id": null, "data_type_id": null }\
    ],
    "sql": "SELECT company_name FROM companies WHERE normalized_domain = 'openai.com' LIMIT 5",
    "executed_sql": "SELECT company_name FROM COMPANIES WHERE normalized_domain = 'openai.com' LIMIT 5",
    "limit": 5,
    "rows": [{ "company_name": "OpenAI" }]
  }
}
```

## SQL rules and limits

- SQL must be a single statement.
- Only `SELECT`, `WITH`, or `EXPLAIN` statements are allowed.
- The statement must read from `companies` or `public.companies`.
- A top-level `LIMIT` is required and must be `100000` or less.
- Queries run with a Snowflake timeout. Broad `ILIKE '%keyword%'`, long `OR` chains, large country-wide scans, and expensive `GROUP BY` shapes can time out.
- Workspaces are currently rate-limited to 5 requests per second.
- Result limits above `5000` return CSV data in `rows_csv`; `rows` is empty for that path.

`free_simple_company_search` is for direct CLI/API use. It is not supported inside workflow executor runs.

## Save matching companies to your workspace database

If you want to turn a free-company query into rows in your workspace customer database, use `materialize_free_customer_companies`. It runs a free-company SQL query and upserts row-shaped results into `enrichments.companies`.The query must return only these company columns:

```
domain, company_name, industry, location, linkedin_url, employee_count, year_founded
```

Example:

```
PAYLOAD=$(cat <<'JSON'
{
  "sql": "SELECT normalized_domain AS domain, company_name, industry, location, linkedin_url, employee_count, year_founded FROM companies WHERE industry ILIKE '%software%' AND employee_count >= 50 LIMIT 1000"
}
JSON
)

deepline tools execute materialize_free_customer_companies --payload "$PAYLOAD"
```

Then query the saved rows with `query_customer_db`:

```
deepline tools execute query_customer_db --payload '{
  "sql": "SELECT domain, company_name, industry, employee_count FROM enrichments.companies ORDER BY updated_at DESC LIMIT 50"
}'
```

## TAM builder guidance

For TAM work, start here when the ICP can be expressed with exact domains, company names, LinkedIn company URLs, industry, location, employee count, or founding year. Escalate to Dropleads, Apollo, Crustdata, Exa, or known-source extraction when the needed signal is not in this schema.


---

## Migrating from Clay

Clay tables are column-based — each column is a separately configured enrichment or AI step. Deepline is outcome-centric: describe what you’re trying to achieve and Claude selects the right tools to get there.

The video walks through a real Clay table with email waterfalls, Claygent research, and ICP scoring — the full flow from table → phase 1 doc → scripts → output CSV.

* * *

## What you need

## A Clay table

The table you want to migrate.

## Claude Code

Install from [claude.ai/code](https://claude.ai/code) and log in.

## Deepline CLI

Runs the enrichment scripts Claude Code generates. Install below.

You can use this skill just to **document** a Clay table without running
anything. If that’s your goal, skip to [Documentation-Only\\
Path](https://deepline.com/docs/migrate-from-clay#documentation-only-path) at the bottom.

* * *

## Install Deepline

Deepline is what runs the enrichment. It wraps providers like LeadMagic, Exa, Instantly, and Claude into a single command.

```
npm install -g deepline
deepline auth register
```

Verify it’s working:

```
deepline tools list
```

You don’t need Deepline if you just want a Phase 1 doc (table summary +
dependency graph + pass plan). You only need it when you want to actually run
the output scripts.

* * *

## The workflow

Four steps. The first two are in Claude Code. The last two are in your terminal.

### Easy path: the Clay Extract bookmarklet

Instead of capturing network logs by hand, use the **Clay Extract bookmarklet**. It runs in your already-logged-in Clay tab, pulls the full table config, schema, sample records, and all cell data, then downloads a single `clay_extract_<table>.json` you can hand straight to `/clay-to-deepline`. Your session cookie is never read or stored, only your browser uses it.

Clay Extract, drag me to your bookmarks bar

1

Add the bookmarklet

**Drag the purple button above to your bookmarks bar.** Or click it to copy the code, create a new bookmark, and paste it as the URL.

2

Open your Clay table

Go to the table view you want, e.g. `app.clay.com/workspaces/.../tables/t_xxx/views/gv_xxx`.

3

Click the bookmarklet

A progress toast appears bottom-right, then `clay_extract_<table>.json` downloads automatically.

4

Hand it to Claude Code

Run `/clay-to-deepline` and attach the downloaded JSON. No cURL, no DevTools.

View the bookmarklet source (raw)

This is exactly what runs. It only calls `api.clay.com` v3 read endpoints with your existing browser session and downloads the result locally. Nothing is sent anywhere else.

```
/*
 * Clay Table Extractor — Bookmarklet (source)
 *
 * Run this on any Clay table view URL, e.g.
 *   https://app.clay.com/workspaces/1114258/tables/t_xxx/views/gv_xxx
 *
 * It uses the already-authenticated browser session (credentials: 'include',
 * so the claysession cookie is sent automatically) to pull, from Clay's
 * internal v3 API:
 *   - table config        GET  /v3/tables/{TABLE_ID}            (fields, typeSettings, prompts, action bindings)
 *   - schema + samples     GET  /v3/tables/{TABLE_ID}/views/{VIEW_ID}/table-schema-v2
 *   - all record ids       GET  /v3/tables/{TABLE_ID}/views/{VIEW_ID}/records/ids
 *   - full cell data       POST /v3/tables/{TABLE_ID}/bulk-fetch-records  (batched)
 *
 * Output: downloads a single JSON file shaped to match the clay-extract.py
 * extract the deepline clay-to-deepline skill expects:
 *   { _meta, table, fields, tableSchema, exampleRecords, recordIds, bulkFetchRecords }
 *
 * The cookie is NEVER read, logged, or written — only the browser uses it.
 */
(async () => {
  const API = 'https://api.clay.com';
  const MAX_RECORDS = 500; // cap full pull; raise if you need every row
  const BATCH = 50; // bulk-fetch page size

  const path = location.pathname;
  const tableId = (path.match(/\/tables\/(t_[A-Za-z0-9]+)/) || [])[1];
  let viewId = (path.match(/\/views\/(gv_[A-Za-z0-9]+)/) || [])[1];

  if (!tableId) {
    alert('Clay extract: no table id in URL. Open a /tables/t_... page first.');
    return;
  }

  const toast = (msg) => {
    let el = document.getElementById('__clay_extract_toast');
    if (!el) {
      el = document.createElement('div');
      el.id = '__clay_extract_toast';
      el.style.cssText =
        'position:fixed;z-index:2147483647;bottom:20px;right:20px;background:#111;color:#fff;' +
        'font:13px/1.4 -apple-system,system-ui,sans-serif;padding:10px 14px;border-radius:8px;' +
        'box-shadow:0 4px 16px rgba(0,0,0,.3);max-width:320px';
      document.body.appendChild(el);
    }
    el.textContent = 'Clay extract: ' + msg;
    return el;
  };

  const getJSON = async (url, opts) => {
    const r = await fetch(url, {
      credentials: 'include',
      headers: { accept: 'application/json, text/plain, */*', ...(opts && opts.headers) },
      ...opts,
    });
    if (!r.ok) throw new Error(url.replace(API, '') + ' → ' + r.status);
    return r.json();
  };

  try {
    toast('fetching table config…');
    const table = await getJSON(API + '/v3/tables/' + tableId);

    // Resolve view id from the table if it wasn't in the URL.
    viewId = viewId || table?.table?.firstViewId || table?.firstViewId;
    if (!viewId) throw new Error('could not resolve a view id (firstViewId missing)');

    // table-schema-v2 returns { tableSchema: { f_xxx: {...} }, exampleRecords: [ { f_xxx: <rendered value> } ] }
    // exampleRecords here carry RENDERED formula/action cell values (richest source) — capped by Clay (~2-66 rows).
    toast('fetching schema + sample records…');
    let schemaTree = null;
    let exampleRecords = [];
    try {
      const sv2 = await getJSON(
        API + '/v3/tables/' + tableId + '/views/' + viewId + '/table-schema-v2',
      );
      schemaTree = sv2?.tableSchema || sv2 || null;
      exampleRecords = sv2?.exampleRecords || sv2?.records || sv2?.sampleRecords || [];
    } catch (e) {
      console.warn('[clay-extract] table-schema-v2 failed:', e.message);
    }

    // True record count (no pagination on records/ids, but this confirms the cap).
    let totalRecordCount = null;
    try {
      const cnt = await getJSON(API + '/v3/tables/' + tableId + '/count');
      totalRecordCount = cnt?.tableTotalRecordsCount ?? cnt?.count ?? null;
    } catch (e) {
      console.warn('[clay-extract] count failed:', e.message);
    }

    toast('fetching record ids…');
    let recordIds = [];
    try {
      const ids = await getJSON(
        API + '/v3/tables/' + tableId + '/views/' + viewId + '/records/ids',
      );
      // Clay returns { results: [r_xxx, ...] }; tolerate older shapes too.
      recordIds = ids?.results || ids?.recordIds || ids?.ids || (Array.isArray(ids) ? ids : []);
    } catch (e) {
      console.warn('[clay-extract] records/ids failed:', e.message);
    }

    const truncated = recordIds.length > MAX_RECORDS;
    const idsToFetch = recordIds.slice(0, MAX_RECORDS);
    const bulkFetchRecords = [];
    for (let i = 0; i < idsToFetch.length; i += BATCH) {
      const chunk = idsToFetch.slice(i, i + BATCH);
      toast('records ' + (i + chunk.length) + '/' + idsToFetch.length + '…');
      try {
        const res = await getJSON(API + '/v3/tables/' + tableId + '/bulk-fetch-records', {
          method: 'POST',
          headers: { 'content-type': 'application/json' },
          body: JSON.stringify({ recordIds: chunk, includeExternalContentFieldIds: [] }),
        });
        if (res?.results) bulkFetchRecords.push(...res.results);
      } catch (e) {
        console.warn('[clay-extract] bulk-fetch batch failed:', e.message);
      }
    }

    if (truncated) {
      console.warn(
        '[clay-extract] record pull capped at MAX_RECORDS=' +
          MAX_RECORDS +
          ' of ' +
          recordIds.length +
          ' total. Raise MAX_RECORDS to pull all.',
      );
    }

    const extract = {
      _meta: {
        extractedAt: new Date().toISOString(),
        method: 'bookmarklet',
        tableId,
        viewId,
        url: location.href,
        totalRecordCount, // true table size from /count
        idCount: recordIds.length, // ids returned
        fetchedRecordCount: bulkFetchRecords.length, // rows pulled via bulk-fetch (may be sparse/unrun)
        exampleRecordCount: exampleRecords.length, // rendered sample rows from table-schema-v2 (richest)
        truncated, // true if MAX_RECORDS capped the pull
      },
      table: table?.table || table,
      fields: table?.fields || table?.table?.fields || [],
      // Schema tree keyed by field id (f_xxx → { type, name, children, ... }).
      tableSchema: schemaTree,
      // Rendered sample rows: flat { f_xxx: value } maps with formula/action outputs. Prefer these for prompts/samples.
      exampleRecords,
      recordIds,
      // Raw per-cell data for ALL records (cells are sparse — only populated fields appear).
      bulkFetchRecords,
    };

    const name =
      (extract.table?.name || tableId).replace(/[^A-Za-z0-9_-]+/g, '_').slice(0, 60);
    const blob = new Blob([JSON.stringify(extract, null, 2)], { type: 'application/json' });
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = 'clay_extract_' + name + '.json';
    document.body.appendChild(a);
    a.click();
    a.remove();
    setTimeout(() => URL.revokeObjectURL(a.href), 4000);

    window.__clayExtract = extract; // also left on window for console inspection
    toast(
      'done: ' +
        (extract.fields?.length || 0) +
        ' fields, ' +
        exampleRecords.length +
        ' sample rows, ' +
        bulkFetchRecords.length +
        '/' +
        (totalRecordCount ?? recordIds.length) +
        ' records' +
        (truncated ? ' (CAPPED)' : '') +
        '. Downloaded JSON.',
    );
    setTimeout(() => document.getElementById('__clay_extract_toast')?.remove(), 6000);
  } catch (err) {
    console.error('[clay-extract]', err);
    toast('ERROR: ' + err.message + ' (see console)');
  }
})();
```

Prefer doing it by hand, or the bookmarklet didn’t work? The manual network-log path still works, step by step below.

### Step 1 — Capture your Clay table’s network logs

1. Open **app.clay.com** in Chrome and log in
2. Open DevTools: `Cmd+Option+I` on Mac, or `F12` on Windows
3. Click the **Network** tab
4. Reload the page (or click around in your Clay table to trigger requests)
5. In the filter box, type `api.clay.com` to show only Clay API calls
6. Click any request in the list
7. Right-click → **Copy** → **Copy as cURL**

On Mac it’s “Copy as cURL (bash)”. On Windows it’s “Copy as cURL (cmd)” —
either works, the skill strips what it needs.

The copied command will look like:

```
curl 'https://api.clay.com/v3/tables/t_abc123/...' \
  -H 'accept: application/json' \
  -b 'claysession=eyJhb...; ajs_user_id=abc; ...' \
  ...
```

The part you need is everything between the `-b '...'` quotes — that’s your session cookie.

### Step 2 — Run /clay-to-deepline in Claude Code

Open Claude Code and type:

```
/clay-to-deepline [paste URLs]
```

Then give it your Clay table. You can paste:

- A Clay table URL (e.g. `app.clay.com/workbooks/xxx/tables/yyy`)
- A JSON export from the table
- Network logs or HAR file captured from Chrome DevTools

Network logs contain session cookies. The cookies are **not** shared with
Deepline, but don’t share them externally.

Claude Code reads the table structure and the actual AI prompt templates embedded in each column. The richer your input, the more accurate the output.**Review the Phase 1 doc** — Claude Code produces three things before writing any scripts:

- **Table summary** — every column, what Clay action it runs, what model, what the output looks like
- **Dependency graph** — which columns feed into which (execution order at a glance)
- **Pass plan** — the ordered list of Deepline tool calls that replicate each Clay action

Check that:

- Prompt templates are recovered verbatim (approximated ones are flagged with ⚠️)
- The pass plan matches what your table actually does
- ICP scoring criteria are correct (the skill can’t invent these)

Once you’re happy, say “looks good” and Claude Code writes the scripts.

### Step 3 — Run the scripts

Claude Code generates two scripts in a `scripts/` folder:

```
./scripts/fetch_people.sh           # → seed_people.csv
./scripts/enrich_people.sh          # → output_people.csv
```

Start with a pilot run (3 rows) before doing the full table:

```
./scripts/fetch_people.sh seed_people.csv pilot
./scripts/enrich_people.sh
```

For paid tools (Exa, LeadMagic, email waterfalls), the skill adds a pilot gate
automatically. You’ll see a preview before any money is spent.

### Step 4 — Get your output

Run the normalizer to produce clean CSVs:

```
python3 scripts/normalize.py \
  --contacts output_people.csv \
  --out-contacts output/contacts.csv
```

You get:

- `output/contacts.csv` — every person with enriched fields (email, job function, AI-generated research, ICP score)
- `output/accounts.csv` — every company with verified name, domain, social links, HQ address, CEO

Or push directly to a campaign:

```
deepline tools execute instantly_add_to_campaign \
  --payload '{"campaign_id":"<id>","contacts":[...]}'
```

* * *

## What you get

The migration produces a self-contained project folder:

```
my-table/
├── .env.deepline          # your Clay cookie (gitignored)
├── prompts/
│   ├── job_function.txt   # recovered verbatim from Clay, or marked ⚠️ APPROXIMATED
│   └── qualify_person.txt
├── scripts/
│   ├── fetch_people.sh
│   └── enrich_people.sh
└── output/
    ├── contacts.csv
    └── accounts.csv
```

The scripts are plain bash. You can read them, edit them, schedule them with cron, or run them in CI.

* * *

## How Clay actions map to Deepline

The skill handles these automatically — this is just so you know what’s happening:

| Clay Action | Deepline Equivalent |
| --- | --- |
| Claygent web research | 3 parallel Exa searches + Claude Sonnet synthesis |
| chat-gpt-schema-mapper | `call_ai haiku` — same output, much cheaper |
| octave-qualify-person | `call_ai sonnet + json_mode` ICP scorer |
| add-lead-to-campaign (Instantly) | `instantly_add_to_campaign` |
| add-lead-to-campaign (Smartlead) | `smartlead_api_request` |

* * *

## Accuracy

26-row production test against Clay ground truth (March 2026). Small sample size — results are directional, not statistically definitive:

| Metric | Match Rate |
| --- | --- |
| Email | ≥95% match vs Clay |
| Job function classification | ≥95% exact match |
| Claygent research (strategy, initiatives) | ~85% specific, actionable content |

The ~15% research gap is mostly name collisions — e.g. `onit.com` resolving to NASDAQ:ONIT instead of the legal-ops software company.

The skill’s “confidence” label on research rows doesn’t mean what you’d
expect. “Low” confidence often has great content — it just means the LLM found
blog posts instead of 10-K filings. Judge by whether the content is specific
to the company, not by the confidence score.

* * *

## Cost comparison

Same 26-row test (March 2026). Clay cost uses Argon model (3 data credits/row for 10-step research). Credit prices from [Clay pricing](https://university.clay.com/docs/plans-and-billing).

|  | Clay (Growth, ~$0.05/cr) | Clay (Launch, ~$0.07/cr) | Deepline |
| --- | --- | --- | --- |
| Cost per row | ~$0.15 | ~$0.22 | ~$0.03 |
| Method | Claygent Argon, 10 search steps | Claygent Argon, 10 search steps | 3 parallel Exa searches + Claude Sonnet synthesis |
| Time per row | ~120-140 seconds | ~120-140 seconds | ~15 seconds |

Content quality is equivalent. For large public companies, Deepline can be more current - Exa indexes recent blog and PR content faster than Google indexes 10-Ks.

* * *

## Documentation-only path

If you just want to document a Clay table — understand what it does, map its dependencies, build a reference for the team — you don’t need to run anything.

1. [Install ClayMate Lite from GitHub](https://github.com/GTM-Base/claymate-lite)
2. Open your Clay table in Chrome
3. Click the extension icon → “Export JSON”
4. Drag the downloaded `*.json` file into Claude Code
5. Run `/clay-to-deepline` and ask for Phase 1 docs only

You’ll get a full table summary, dependency graph, and pass plan — without running a single script or spending anything.

ClayMate Lite exports include the table schema + sample rows. This is the
richest input short of a full HAR file. For most tables it’s enough to recover
the actual AI prompt templates from cell values.

* * *

## Feedback or issues?

- **Email**: [team@deepline.com](mailto:team@deepline.com)
- **Slack**: [Join Terminal Velocity GTM](https://join.slack.com/t/terminal-velocity-gtm/shared_invite/zt-3tbnkutuq-n~a7fdFcwVmRld8tgebxCw) — this is where most of the live migration discussion happens
- [Book time to chat or debug](https://calendly.com/jptoor/30min)


---

# PART 2 — Platform Features


---

# Waterfall Enrichment

Waterfall enrichment tries multiple data providers in sequence, cheapest first, and stops the moment one returns a valid result. Deepline ships 6 waterfall plays that orchestrate 29 production providers into cost-ordered chains. Single-provider lookups typically find 40-60% of emails. Waterfall enrichment pushes that to 70-85% by cross-referencing multiple sources in a single request. You pay only for the provider that delivers. **BYOK mode:** misses cost nothing through Deepline. **Managed mode:** misses pass through provider per-call costs (typically 0.3-1.2 credits).

## Primitives

These are the hard numbers. Everything below is derived from them.

| Attribute | Value |
| --- | --- |
| Waterfall plays available | 6 |
| Default email waterfall providers | 6 (Dropleads, Hunter, LeadMagic, Deepline Native, Crustdata, PeopleDataLabs) |
| Cost-aware waterfall steps | 8 (3 pattern-guess + 5 finder providers) |
| Production providers in platform | 29 |
| Cost per email find | 0.2-0.6 credits ($0.016-$0.048) |
| Cost for misses | BYOK: 0 credits. Managed: provider per-call costs (typically 0.3-1.2 credits across the chain) |
| Single-provider email coverage | 40-60% typical (based on internal testing across US B2B contacts) |
| Waterfall email coverage | 70-85% typical (based on internal testing across US B2B contacts) |
| Waterfall logic | Sequential, cost-ordered, stop-on-first-valid-hit |
| BYOK mode | Supported — bring your own API keys, pay providers directly at their rates |

## What waterfall enrichment means for your GTM workflow

Not every workflow needs waterfall enrichment. Here is where it helps and where it does not.

- **Maximizes email coverage.** A single email provider typically resolves 40-60% of lookups in our internal testing on US B2B contacts. B2B contact data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), so any single source goes stale fast. The 6-provider waterfall cross-references Dropleads, Hunter, LeadMagic, Deepline Native, Crustdata, and PeopleDataLabs in real time, pushing coverage to 70-85% in the same tests. That gap translates directly to more contacts reached per campaign. According to industry benchmarks, waterfall enrichment can improve email coverage by 20-40% compared to any single provider ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)).
- **Cheapest path to a valid result.** Waterfall logic tries the cheapest provider first (Dropleads at 0.3 credits) and stops the moment a valid email is found. Most lookups resolve on the first or second provider at 0.2-0.6 credits ($0.016-$0.048). Providers that do not fire are not billed. The cost-aware variant goes further: it tries 3 deterministic email patterns via validation (near-zero cost) before calling any finder provider. On lists where common patterns like `first.last@domain.com` are prevalent, this saves 50-70% compared to starting with a paid finder.
- **Runs from a single prompt or CLI command.** Every waterfall play is callable with a single CLI command or a natural language prompt in Claude Code or Codex. No spreadsheet UI, no column wiring, no template configuration. The agent reads your CSV headers, selects the right waterfall variant, and enriches every row. The entire loop — from input analysis to provider selection to result extraction — runs inside your IDE or terminal. This makes waterfall enrichment composable with other automation steps: scraping, scoring, CRM sync, outreach sequencing.
- **Skip it for single-provider queries.** If you need data from one specific provider (for example, only Apollo people search results, or only Crustdata company data), use a direct tool call instead of a waterfall. Waterfalls are designed for outcome-based queries (“find this person’s email”) where the source does not matter — only the result. For provider-specific queries, Deepline exposes all 29 providers as individual tools with their own endpoints and schemas.

## How it works

The waterfall execution model is the same across all 6 plays. Here is the step-by-step sequence for a typical email lookup.

1. **Analyze input data.** Deepline inspects your payload fields or CSV column headers to determine which waterfall variant fits. If you provide `first_name`, `last_name`, and `domain`, it selects the `name_and_domain_to_email_waterfall`. If you provide a `linkedin_url`, it selects the LinkedIn-optimized `person-linkedin-to-email` play.
2. **Compile the provider chain.** The selected play compiles into an ordered list of provider steps. Each step specifies the tool ID, the payload template (with `{{variable}}` interpolation from your input), and an extraction function that pulls the target field from the provider response. The chain is ordered by cost: cheapest providers run first.
3. **Execute the first provider.** Deepline sends the request to the lowest-cost provider in the chain (for example, Dropleads at 0.3 credits for the default email waterfall). The provider returns either a result or a miss. Execution time per provider is typically 1-3 seconds.
4. **Validate the result.** If the provider returns data, Deepline runs the extraction function to pull the target field (such as `email`). For email waterfalls, this includes checking that the returned address is not a catch-all, not a bounce, and not syntactically invalid. The cost-aware variant uses `leadmagic_email_validation` to verify pattern-guessed addresses against the mail server before accepting them.
5. **Fall through or stop.** If validation fails or the provider returned no data, Deepline moves to the next provider in the chain. If validation passes, the waterfall stops immediately. You are billed only for providers that actually executed — providers below the stopping point never fire. In the default 6-provider chain, most lookups stop at provider 1 or 2.
6. **Return structured output.** The final result includes the found value (email, person context, or contact list depending on the play), the provider that resolved it, the number of providers attempted, and the total credits consumed. This output feeds directly into downstream enrichment steps, CSV columns, or CRM updates.

## Waterfall plays available

Deepline ships 6 waterfall plays. Each play is optimized for a different input shape and output goal.

### 1\. Name + Domain to Email (`name_and_domain_to_email_waterfall`)

The most common waterfall, with built-in cost-aware pattern guessing. Input: `first_name`, `last_name`, `domain`. Optional: `company_name`, `linkedin_url`. Steps: 3 deterministic pattern guesses validated via `leadmagic_email_validation` (patterns: `first.last@domain`, `firstlast@domain`, `first_last@domain`) followed by 5 finder providers (Dropleads, Hunter, LeadMagic, Deepline Native, PeopleDataLabs). If the correct pattern matches, the lookup resolves for 0.1-0.2 credits. Typical cost: 0.1-0.4 credits.

### 2\. LinkedIn to Email (`person-linkedin-to-email`)

Input: `linkedin_url`. Provider chain: Dropleads, Hunter, LeadMagic, Deepline Native, Crustdata, PeopleDataLabs (6 providers). All providers receive the LinkedIn URL, improving match rates for LinkedIn-sourced leads. Typical cost: 0.3-0.4 credits.

### 3\. Personal Email (`personal_email_waterfall`)

Find consumer (non-work) email addresses for ad targeting and direct outreach. Input: `linkedin_url` or `first_name` \+ `last_name` \+ `domain`. Provider chain: Dropleads, LeadMagic, PeopleDataLabs. Use for LinkedIn Matched Audiences, Facebook Custom Audiences, or compliant direct outreach. Typical cost: 0.3-0.5 credits.

### 4\. Name to LinkedIn URL (`name_to_linkedin_url_waterfall`)

Resolve LinkedIn profile URLs from name and company information. Input: `first_name`, `last_name`, `company_name` or `domain`. Combines web search, provider databases, and identity validation. Useful when you need LinkedIn URLs as input to email or phone waterfalls.

### 5\. Contact to Phone (`contact_to_phone_waterfall`)

Find mobile phone numbers for a known contact. Input: name + LinkedIn URL or work email. Provider chain spans LeadMagic, Dropleads, and other phone-capable providers with region-aware routing.

### 6\. Company + Role to Contacts (`company_to_contact_by_role_waterfall`)

Prospecting waterfall. Input: `company_name`, `domain`, `roles` (job titles), `seniority` (level filter). Provider chain: Dropleads, Apollo, Icypeas, Prospeo, Crustdata (5 providers). Returns a list of matching contacts (not a single email). Use when you need to find decision makers at target accounts by title and seniority. Typical cost: 0.2-0.6 credits per provider step. Supports a `limit` parameter (1-100) to control how many contacts each provider returns.

## Example CLI commands

### Single email lookup

```
deepline enrich --with '{"alias":"email","tool":"name_and_domain_to_email_waterfall","payload":{"first_name":"Sarah","last_name":"Chen","domain":"stripe.com"}}'
```

### Bulk CSV enrichment

```
deepline enrich --input leads.csv --output enriched.csv \
  --with '{"alias":"email","tool":"name_and_domain_to_email_waterfall","payload":{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"{{domain}}"}}'
```

### LinkedIn URL to email

```
deepline enrich --with '{"alias":"email","tool":"person-linkedin-to-email","payload":{"linkedin_url":"https://linkedin.com/in/sarahchen"}}'
```

### Find decision makers at a company

```
deepline enrich --with '{"alias":"contacts","tool":"company_to_contact_by_role_waterfall","payload":{"company_name":"Stripe","domain":"stripe.com","roles":["VP Engineering","CTO"],"seniority":["VP","C-Level"]}}'
```

### Personal email lookup

```
deepline enrich --with '{"alias":"personal","tool":"personal_email_waterfall","payload":{"first_name":"Sarah","last_name":"Chen","linkedin_url":"https://linkedin.com/in/sarahchen"}}'
```

### Natural language (Claude Code or Codex)

You do not need to memorize tool IDs. Ask in plain language:

```
Find work emails for everyone in leads.csv
```

```
I have LinkedIn URLs in contacts.csv -- find their work emails using waterfall enrichment
```

```
Find the VP of Engineering at Ramp (ramp.com) and get their email
```

### Pilot mode (test before full run)

Always test on a small subset before running a full list:

```
Run waterfall email enrichment on leads.csv -- just the first 3 rows
```

Or with the CLI: add `--rows 0:2` to the `deepline enrich` command.

## Frequently Asked Questions

How much does waterfall enrichment cost per email?

Most lookups resolve on the first or second provider at 0.2-0.6 credits
($0.016-$0.048). The `name_and_domain_to_email_waterfall` tries 3 free email
pattern guesses first and can resolve for as little as 0.1 credits ($0.008).
Worst case if all providers fire: up to ~5.9 credits for
`name_and_domain_to_email_waterfall` or ~7.2 credits for
`person-linkedin-to-email`, but this is rare — it means every provider missed
or returned invalid data. In BYOK mode (bring your own API keys), waterfall
lookups are free through Deepline; you pay providers directly at their rates.

How does Deepline choose which waterfall variant to use?

Deepline inspects your input fields (payload keys or CSV column headers) and
selects the variant that matches your available data. If you have
`first_name`, `last_name`, and `domain`, it uses
`name_and_domain_to_email_waterfall` (which tries free patterns first). If you
have `linkedin_url`, it prefers `person-linkedin-to-email`. You can also
specify the exact waterfall by tool ID in the CLI if you want to override
automatic selection.

What happens when no provider finds an email?

The waterfall tries every provider in the chain. If none returns a valid
result, the output is null for that row. **BYOK mode:** misses cost nothing
through Deepline. **Managed mode:** misses pass through provider per-call
costs (typically 0.3-1.2 credits across the chain). Deepline does not
fabricate results. A null result means the contact could not be resolved from
any of the 29 production providers in the platform.

Can I customize the provider order or exclude providers?

The built-in waterfall plays use a fixed cost-ordered provider chain that is
optimized for price-performance. If you need a custom chain, you can break the
waterfall into individual tool calls and orchestrate them yourself using
`deepline tools execute` or `deepline enrich` with multiple `--with` steps.
You can also use the plays API (`/api/v2/plays/compile`) to inspect the
compiled provider chain for any play and modify it programmatically.

* * *

**Related pages:** [Email Waterfall](https://deepline.com/docs/plays/email-waterfall) \| [Find Work Email](https://deepline.com/docs/plays/task-find-work-email) \| [Plays Overview](https://deepline.com/docs/plays/plays-overview) \| [Find Decision Makers](https://deepline.com/docs/plays/task-find-decision-makers) \| [Quick Start](https://deepline.com/docs/quickstart)


---

# Bring Your Own Keys (BYOK)

Deepline lets you plug in API keys you already own and use them directly through the CLI, API, and agent skills. There is no monthly platform fee, no per-call markup, and no middleman billing. You pay your providers at their published rates. Deepline handles orchestration, waterfall logic, and data normalization on top of whatever keys you connect.

| Attribute | Value |
| --- | --- |
| BYOK cost | $0/month, 0% markup |
| Providers with API key support | 26 |
| Providers with OAuth support | 3 (Attio, HubSpot, Salesforce) |
| Alternative (no keys) | Managed credits at $0.10/credit, with 10% off on 1,000+ credits |
| Key storage | AES-256 encrypted, per-workspace, never shared across workspaces |
| Mix and match | Yes — BYOK for some providers, managed credits for others |
| Setup time | Under 2 minutes per provider |

* * *

## What BYOK means for your GTM budget

Not every team should use BYOK. Here is a straight breakdown of who benefits most and who should skip it.**Best for teams with existing provider contracts.** If you already pay Apollo, Crustdata, Hunter, or People Data Labs directly, BYOK lets you route those same keys through Deepline without double-paying. You keep your negotiated rates, your existing usage dashboards, and your billing relationship. Deepline adds waterfall logic and unified output schemas on top — nothing changes on the provider side.**Best for cost-conscious operators.** Clay charges $185/month on their Explorer plan and $485/month on Pro before you buy any enrichment credits. ZoomInfo starts at $15,000/year. With BYOK, your Deepline platform cost is $0/month. Your only spend is whatever you pay providers directly at their published per-call rates. For a team running 5,000 enrichments per month across 3 providers, the difference can be $2,000-$4,000/year in platform fees alone.**Best for testing multiple providers side-by-side.** When you are evaluating whether Crustdata or People Data Labs returns better company data for your ICP, BYOK lets you run both through the same waterfall and compare hit rates without committing to either vendor’s annual contract. Connect free-tier keys from each, run a sample batch, and measure coverage before you buy.**Not ideal if you have zero provider accounts.** If you do not have any existing API keys and want to start enriching immediately, use managed credits instead. Managed credits cost $0.10/credit, with a 10% volume discount on purchases of 1,000+ credits. They require no provider signups and work across all 95+ integrations. You can switch individual providers to BYOK later as you negotiate direct contracts.

* * *

## Supported providers

API-key providers listed below accept BYOK connections. Connect any combination. Apollo also accepts customer-owned keys, but it must be connected in the Deepline dashboard and is not available through platform-managed env fallback.

### Enrichment and prospecting

| Provider | Env var name | Auth type | Category |
| --- | --- | --- | --- |
| Adyntel | `ADYNTEL_API_KEY` | API key | Ad intelligence |
| AI Ark | `AI_ARK_API_KEY` | API key | People search, email finder |
| Apollo | Dashboard connection | API key | People + company enrichment |
| ContactOut | `CONTACTOUT_API_KEY` | API key | Email + phone finder |
| Crustdata | `CRUSTDATA_API_KEY` | API key | Company + people enrichment |
| Dropleads | `DROPLEADS_API_KEY` | API key | Lead generation |
| Exa | `EXA_API_KEY` | API key | AI-powered search |
| Forager | `FORAGER_API_KEY` | API key | Data storage + enrichment |
| Hunter | `HUNTER_API_KEY` | API key | Email finder + verification |
| Icypeas | `ICYPEAS_API_KEY` | Custom | Email finder + verification |
| LeadMagic | `LEADMAGIC_API_KEY` | API key | Email + company enrichment |
| Parallel | `PARALLEL_API_KEY` | API key | AI enrichment |
| People Data Labs | `PEOPLEDATALABS_API_KEY` | API key | People + company data |
| Prospeo | `PROSPEO_API_KEY` | API key | Email finder |
| RocketReach | `ROCKETREACH_API_KEY` | API key | Contact lookup |
| ZeroBounce | `ZEROBOUNCE_API_KEY` | API key | Email verification |

### Scraping and search

| Provider | Env var name | Auth type | Category |
| --- | --- | --- | --- |
| Apify | `APIFY_API_KEY` | Bearer | Web scraping platform |
| BuiltWith | `BUILTWITH_API_KEY` | API key | Technographic data |
| Cloudflare | `CLOUDFLARE_API_TOKEN` | Bearer | Web crawling |
| Firecrawl | `FIRECRAWL_API_KEY` | Bearer | Web scraping + extraction |
| Google Search | `GOOGLE_SEARCH_API_KEY` | API key | Custom search API |

### Outbound sequencing

| Provider | Env var name | Auth type | Category |
| --- | --- | --- | --- |
| HeyReach | `HEYREACH_API_KEY` | API key | LinkedIn outreach |
| Instantly | `INSTANTLY_API_KEY` | Bearer | Email sequencing |
| Lemlist | `LEMLIST_API_KEY` | Basic | Email sequencing |
| SmartLead | `SMARTLEAD_API_KEY` | API key | Email sequencing |

### CRM (OAuth)

These 3 providers use OAuth instead of API keys. Connect them through the Deepline dashboard.

| Provider | Auth type | Connection method |
| --- | --- | --- |
| Attio | OAuth | Dashboard one-click |
| HubSpot | OAuth | Dashboard one-click |
| Salesforce | OAuth | Dashboard one-click |

### Internal

| Provider | Env var name | Auth type | Category |
| --- | --- | --- | --- |
| Deepline Waterfall | `WATERFALL_API_KEY` | API key | Multi-provider waterfall |

* * *

## How to connect a key

### Option 1: Dashboard (recommended)

```
# BYOK keys are managed in the dashboard at https://code.deepline.com/dashboard/keys
```

Open the dashboard, pick the provider, paste your API key, and save. The key is encrypted at rest and scoped to your workspace. Repeat for each provider you want to connect. After saving, enrichment calls to that provider use your key automatically.

### Option 2: API

Set a key programmatically via the REST API:

```
curl -X POST https://code.deepline.com/api/v2/keys \
  -H "Authorization: Bearer $DEEPLINE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "provider": "apollo",
    "apiKey": "your-apollo-api-key-here"
  }'
```

### Option 3: Environment variable

For local development or self-hosted deployments, set supported provider env vars directly. Apollo is excluded from env fallback; connect Apollo in the Deepline dashboard instead.

```
export HUNTER_API_KEY="your-hunter-api-key-here"
```

The env var names match the table above exactly. When Deepline detects a provider env var, it uses that key automatically — no additional configuration needed.

### Connecting multiple providers

Connect as many providers as you need. Each key is stored independently. Add each provider in the dashboard at `https://code.deepline.com/dashboard/keys`:

- Apollo
- Hunter
- LeadMagic
- ZeroBounce

All 4 keys are now available for waterfall enrichment. When you run a `name_and_domain_to_email_waterfall`, Deepline queries each connected provider in sequence and returns the first verified hit.

* * *

## Mix and match: BYOK + managed credits

You do not have to choose one model for all providers. Deepline supports mixing BYOK and managed credits at the provider level within the same workspace.**Example setup:**

| Provider | Mode | Why |
| --- | --- | --- |
| Apollo | BYOK | Already on an annual contract |
| Crustdata | BYOK | Using their free tier for testing |
| Hunter | Managed credits | No existing account, low volume |
| People Data Labs | Managed credits | Evaluating before committing |
| LeadMagic | BYOK | Negotiated bulk rate |
| ZeroBounce | Managed credits | Only need occasional verification |

This means a single waterfall enrichment can use your Apollo key for step 1, managed credits for Hunter in step 2, and your LeadMagic key for step 3. Deepline handles the routing transparently. You see which mode was used for each step in the enrichment response.**Switching modes per provider is instant.** Connect a BYOK key and that provider switches from managed credits to your key on the next API call. For providers with platform-managed access, removing the key can fall back to managed credits. Apollo is customer-key-only, so removing the Apollo key disables Apollo calls until a personal key is reconnected.

### Cost comparison for a typical workflow

Consider a monthly workflow of 2,000 email enrichments using a 3-provider waterfall:

| Model | Platform fee | Enrichment cost | Total |
| --- | --- | --- | --- |
| Full BYOK (3 keys) | $0/mo | Provider rates only (~$80-$200) | ~$80-$200/mo |
| Full managed credits | $0/mo | ~$180-$360 at $0.09-$0.18/credit | ~$180-$360/mo |
| Mixed (2 BYOK + 1 managed) | $0/mo | ~$100-$240 | ~$100-$240/mo |
| Clay Explorer (comparable) | $185/mo | \+ Clay credits | $185+/mo |

The Deepline platform fee is $0/month regardless of which mode you use. The difference between BYOK and managed credits is purely the per-enrichment cost — and whether you want the operational overhead of managing provider accounts directly.

* * *

## Key storage and security

Every API key stored through Deepline is:

- **Encrypted at rest** using AES-256 encryption
- **Scoped to your workspace** — keys are never shared across workspaces or tenants
- **Never logged** — keys are redacted from all application logs and error traces
- **Deletable on demand** — remove a key instantly via the dashboard at `https://code.deepline.com/dashboard/keys` or via API

Keys are stored in your workspace’s dedicated database. They do not pass through shared infrastructure or caching layers. When Deepline makes a call to a provider using your key, the request goes directly from the Deepline execution engine to the provider API — there is no proxy that stores or inspects the key beyond the initial decryption.

* * *

## Frequently asked questions

What happens if my BYOK key hits a rate limit?

Deepline respects provider rate limits. If your Apollo key is rate-limited, the waterfall moves to the next provider in the chain (e.g., Hunter or LeadMagic). If all BYOK providers are exhausted, the enrichment returns a partial result with a clear status code. Deepline does not retry rate-limited requests automatically — it moves forward to avoid wasting your quota.

Can I use BYOK and managed credits for the same provider?

No. For a given provider, your workspace uses either your BYOK key or managed
credits — not both simultaneously. If you connect a BYOK key for Apollo, all
Apollo calls use your key. Removing the Apollo key disables Apollo calls until
a personal key is reconnected; Deepline cannot provide platform-managed Apollo
access. For other supported providers, removing your key can fall back to
managed credits when platform-managed access is available. This prevents
ambiguous billing where you cannot tell which key was charged.

Do I need BYOK keys for waterfall enrichment to work?

No. Waterfall enrichment works with managed credits, BYOK keys, or any
combination. The waterfall engine is agnostic to how each provider is
authenticated. If you have zero keys connected, managed credits handle
everything. If you have 3 BYOK keys and 2 managed-credit providers, the
waterfall uses all 5 in the configured priority order.

How do I know which providers my key was used for in an enrichment?

Every enrichment response includes a `provider_chain` array showing which providers were called, in what order, and which one returned the final result. For BYOK providers, the response shows `auth_mode: "byok"`. For managed credits, it shows `auth_mode: "managed"`. You can audit exactly where every credit or API call went.

* * *

## Related pages

[**Quick Start** \\
\\
Install Deepline and run your first enrichment in 60 seconds.](https://deepline.com/docs/quickstart)

[**Pricing Schedule** \\
\\
Full per-endpoint pricing for managed credits across all providers.](https://deepline.com/docs/_generated/pricing-schedule)

[**API Reference** \\
\\
REST API documentation for keys, enrichment, and waterfall endpoints.](https://deepline.com/docs/api-reference)

[**Waterfall Enrichment** \\
\\
How multi-provider waterfall logic works under the hood.](https://deepline.com/docs/features/waterfall-enrichment)


---

# Provider Health & Automatic Failover

Deepline continuously monitors 19 enrichment providers so your waterfalls never stall on a dead endpoint. When a provider goes down, waterfall execution skips it automatically. When it recovers, traffic resumes. You do not configure anything — it works out of the box on every enrichment request.

## Primitives

| Primitive | Value |
| --- | --- |
| Providers monitored | **19** (13 API probes + 5 DNS probes + 1 mixed) |
| API probe timeout | 10 seconds |
| DNS probe timeout | 5 seconds |
| Degraded threshold | Response time exceeds 5 seconds |
| State storage | Vercel KV |
| Transition alerts | Slack (automatic on every status change) |
| Check frequency | Cron-driven (runs on schedule, not per-request) |
| Failover behavior | Automatic skip in waterfall when provider status is `down` |

## What provider health monitoring means for your enrichment reliability

Not every team needs to think about provider health. Here is how to decide whether this matters for your use case:

- **Production-critical pipelines.** If your enrichment runs feed CRM syncs, outbound sequences, or lead scoring models, a single provider outage cascades into stale data and failed automations. Automatic rerouting keeps your pipeline producing results even when a provider is down. No retry logic to build, no status pages to monitor.
- **Multi-provider waterfalls.** The typical email waterfall hits 4-6 providers in sequence. A provider returning HTTP 500s gets marked `down` within one health check cycle, and every subsequent waterfall skips it until recovery. No wasted credits on providers that are going to fail anyway.
- **Teams without dedicated DevOps.** Most enrichment teams are 2-3 people. Nobody has time to poll 19 status pages and toggle provider configs when something breaks. Deepline handles monitoring, state management, alerting, and failover as one system. You get Slack notifications on status changes, and traffic reroutes without intervention.
- **Not relevant for single-provider setups.** If your pipeline only calls Apollo for people search, health monitoring still runs — but failover has nowhere to reroute. You still get Slack alerts when Apollo goes down, but automatic rerouting only matters when your waterfall has multiple providers.

## How health monitoring works

The health monitoring system has four layers: probes that test each provider, state management that tracks status over time, alerts that notify your team, and failover logic that acts on that state during waterfall execution.

### Layer 1: Probes

Every health check cycle runs 19 probes concurrently using `Promise.allSettled`. Each probe targets a lightweight endpoint on the provider’s API — something that authenticates and responds without consuming credits or triggering rate limits.There are two probe types:**API probes** (13 providers) make an authenticated HTTP request to a real API endpoint. The request includes the provider’s API key, uses the correct auth mechanism (header, query parameter, or request body depending on the provider), and expects a successful HTTP response. The timeout is **10 seconds**. If the response comes back with a non-2xx status code, the provider is marked `down`. If it succeeds but takes longer than **5 seconds**, it is marked `degraded`.Providers using API probes: Apollo, Apify, Dropleads, HeyReach, Instantly, LeadMagic, Lemlist, Forager, ZeroBounce, Prospeo, People Data Labs, Deepline Native, Crustdata, and Hunter.**DNS probes** (5 providers) send an unauthenticated `HEAD` request to the provider’s API base URL. These are used for providers that do not expose a free, zero-side-effect health endpoint. The timeout is **5 seconds**. A DNS probe tests that the hostname resolves and the server accepts TCP connections. Any HTTP response — even a 403 or 404 — counts as `up`, because the probe is testing reachability, not API correctness. Providers using DNS probes: Adyntel, Parallel, Exa, Google Search, and Icypeas.**One provider (Icypeas) uses a mixed approach**: it has an API key configured but uses a DNS probe because the API does not expose a free health-check endpoint.

### Layer 2: State management

After all 19 probes complete, the system compares the current results against the previous results stored in **Vercel KV** under the key `provider-health:latest`.The comparison logic walks through every provider and checks whether its status changed since the last run. Transitions involving the `skipped` status are ignored — if a provider’s API key is not configured, it stays `skipped` and does not generate noise.When a transition is detected (for example, `up` to `down`, or `degraded` to `up`), the system records it with the provider name, previous status, new status, current latency, and any error message.The current probe results are then written back to Vercel KV, replacing the previous snapshot. This means KV always holds exactly one record: the most recent health state of all 19 providers.

### Layer 3: Alerts

Every detected transition triggers a Slack notification. The alert includes the provider name, the direction of the change (e.g., “Apollo: up -> down”), the measured latency, and the error message if applicable.Alerts fire on every transition, in both directions. You get notified when a provider goes down, and you get notified when it comes back up. This gives your team a complete audit trail of provider reliability without checking dashboards.

### Layer 4: Failover

During waterfall execution, Deepline reads the current provider health state from Vercel KV before building the execution plan. Any provider with a status of `down` is excluded from the waterfall chain for that request.This means:

1. The waterfall does not waste time or credits calling a provider that is known to be unresponsive.
2. The next healthy provider in the chain takes over, maintaining your enrichment hit rate.
3. When the downed provider recovers (detected on the next health check cycle), it is automatically re-included in future waterfall executions.

The failover is transparent. You do not see different API responses or need to handle it in your code. The waterfall result comes back the same way it always does — you just get results faster because dead providers are skipped.

## Status definitions

Each provider is assigned exactly one status after every health check cycle:

| Status | Meaning | Waterfall behavior |
| --- | --- | --- |
| `up` | Probe succeeded. Response time under 5 seconds. | Provider is included in waterfall chains normally. |
| `degraded` | Probe succeeded, but response time exceeded 5 seconds. | Provider is still included in waterfall chains. Results may be slower than usual. |
| `down` | Probe failed. HTTP error, timeout, or DNS resolution failure. | Provider is **skipped** in waterfall chains until next successful health check. |
| `skipped` | No API key configured for this provider. | Provider is excluded from health checks entirely. Not available for waterfall use. |

The `degraded` status is a signal, not a circuit breaker. Providers running slow are still usable — they just take longer. The 5-second threshold was chosen because most enrichment API calls complete in 1-3 seconds under normal conditions. A response time above 5 seconds indicates the provider is under load or experiencing partial degradation, but still returning valid data.The `down` status is the circuit breaker. When a provider is `down`, waterfall execution treats it as if it does not exist for that request cycle. This is the key mechanism that prevents your pipeline from stalling on an unresponsive provider.

## Frequently asked questions

How often do health checks run?

Health checks are cron-driven. The check runs all 19 probes concurrently on each invocation. The exact schedule is configured in `vercel.json` as a Vercel Cron Job. Each cycle completes in under 15 seconds because all probes run in parallel with individual timeouts (10 seconds for API probes, 5 seconds for DNS probes).

Do health checks consume my provider credits?

No. API probes are designed to hit free, zero-side-effect endpoints: credit
balance checks, campaign list endpoints, autocomplete queries with minimal
input, and similar read-only calls. None of these endpoints consume enrichment
credits on any provider we have tested.

What happens if Vercel KV is unavailable?

If the KV read fails (for example, during a Vercel KV outage), the health
check treats the situation as a first run — no previous state, so no
transitions are detected and no alerts fire. The current results are still
computed and an attempt is made to write them back to KV. Waterfall execution
falls back to treating all providers as available, which is the safe default.
You lose the failover optimization temporarily, but you do not lose enrichment
capability.

Can I see the current health status of all providers?

Yes. The health check endpoint returns a full JSON report including every
provider’s status, latency, probe type, and any error messages. The response
also includes a summary object with counts of providers in each status (`up`,
`degraded`, `down`, `skipped`) and a list of any transitions detected during
that check cycle.

What if a provider flaps between up and down?

Every transition generates a Slack alert. If a provider is flapping, you will see a series of alternating alerts (“Apollo: up -> down”, “Apollo: down -> up”) which makes the instability visible. The waterfall respects the most recent health check result, so a provider that was `down` at the last check will be skipped until the next check finds it `up` again. There is no debounce or cooldown period — the system tracks the provider’s actual state, not a smoothed version of it.

Does degraded status affect my enrichment results?

No. A `degraded` provider still returns valid data — it just takes longer.
Waterfall execution includes `degraded` providers normally. The status is
informational: it tells you that a provider is slower than usual, which might
indicate an emerging issue. If latency continues to increase past the timeout
threshold, the next health check will mark the provider as `down` and failover
will engage.

Can I manually mark a provider as down?

Not currently. The system is fully automated. If you know a provider is having
issues before the next health check detects it, the impact is limited to one
check cycle. In practice, provider outages are detected within minutes because
the cron runs on a regular schedule.

Which providers are monitored?

All 19 providers in the probe list: Apollo, Apify, Dropleads, HeyReach, Instantly, LeadMagic, Lemlist, Forager, ZeroBounce, Prospeo, People Data Labs, Deepline Native, Crustdata, Hunter, Icypeas, Adyntel, Parallel, Exa, and Google Search. If a provider’s API key is not configured in your environment, its status will be `skipped` and it will not participate in health checks or waterfall execution.

## Related

- [Email Waterfall](https://deepline.com/docs/plays/email-waterfall) — how waterfalls select and sequence providers
- [API Reference](https://deepline.com/docs/api-reference) — enrichment execution endpoints
- [Install](https://deepline.com/docs/quickstart) — get set up with the Deepline CLI


---

# PostgreSQL Database

Every Deepline workspace gets its own dedicated Neon PostgreSQL project. Not a shared multi-tenant database. Not a proprietary data store you need a vendor SDK to query. A real PostgreSQL database with your own schemas, your own roles, and direct SQL access from any client that speaks the Postgres wire protocol.Every GTM team eventually asks: “Where does my data actually live, and can I get it out?”Yes. It lives in PostgreSQL. You can get it out with `psql`, DBeaver, Metabase, your ORM, a `pg` driver, or a `COPY TO` command. There is no export queue, no CSV-only download button, no 10,000-row limit.

| Attribute | Value |
| --- | --- |
| Database engine | PostgreSQL (Neon serverless) |
| Isolation | One dedicated Neon project per tenant |
| Schemas | Managed schemas + agent-writable `storage` schema |
| Database roles | Workspace-scoped customer DB and internal runtime roles |
| Schema migrations | Rolling migrations supported |
| Direct SQL access | Any PostgreSQL client (psql, DBeaver, Metabase, pg driver) |
| Data export | Unrestricted — `pg_dump`, `COPY TO`, direct query |
| Data sovereignty | Your data stays in your schema |

* * *

## What an included database means for your GTM data

Most enrichment tools store your data in their cloud and give you a UI to look at it. If you want to do anything real with that data — join it to your CRM, run custom reports, build internal tools, deduplicate across providers — you are either exporting CSVs or paying for an API that rate-limits you against your own records.Deepline takes a different approach. Your enrichment results, identity graph, manual overrides, and resolved views all live in a PostgreSQL database that you can connect to directly. Here is when that matters and when it does not.

## Data sovereignty

Your enrichment results live in your schema, not in a vendor’s multi-tenant
database. Query, export, back up, or delete your data at any time with
standard PostgreSQL tools. No vendor lock-in. No export fees. No “please
contact sales to download your records.” If you leave Deepline, your data is
a `pg_dump` away.

## Custom reporting

Connect Metabase, Looker, Mode, Grafana, or any BI tool directly to your
database. Write SQL to answer questions the vendor UI never anticipated:
“Which companies in our ICP had job postings in the last 30 days but no
enriched contact?” “What is our email verification rate by provider over
time?” Full `SELECT` access to resolved views — no API pagination, no row
limits.

## Identity resolution

Enrich the same person through Apollo, Hunter, and People Data Labs and you
get three separate responses with overlapping but inconsistent data. The
identity graph (`dl_graph`) links enrichment events to resolved entities,
deduplicates across providers, and produces a single coalesced record in
`dl_resolved`. Query the resolved view and get the best available data
without writing merge logic.

## Not relevant for ephemeral lookups

If you run a single enrichment and pipe the result straight into a CRM or
spreadsheet, you may never touch the database directly. The CLI and API
return results inline. The database matters when you build on top of
enrichment data over time — running waterfalls, deduplicating contacts,
tracking history, or building internal tools.

* * *

## Agent access with `query_customer_db`

Agents normally access the workspace database through `query_customer_db`, not
by holding an owner connection string. The tool accepts one SQL statement and
returns structured rows.Use it for reads:

```
deepline tools execute query_customer_db --payload '{
  "sql": "SELECT domain, company_name FROM enrichments.companies ORDER BY updated_at DESC LIMIT 20"
}'
```

Use the `storage` schema for agent-created tables and scratch state:

```
deepline tools execute query_customer_db --payload '{
  "sql": "CREATE TABLE IF NOT EXISTS storage.agent_notes (id text PRIMARY KEY, note text NOT NULL, created_at timestamptz NOT NULL DEFAULT now())"
}'
```

`query_customer_db` can run `SELECT`, `EXPLAIN`, read-only `WITH`, and
storage-scoped writes. Write statements must name the target as
`storage.<table>` or `"storage"."<table>"`. Unqualified DDL such as
`CREATE TABLE agent_notes (...)` is blocked.

## Schema architecture

Each tenant database contains managed schemas plus an agent-writable `storage`
schema. The schemas are designed around a clear data flow: raw enrichment
results land in managed storage, identity resolution links records to resolved
entities, coalesced views surface read models, and platform metadata tracks
schema versioning and tenant configuration.

### The 5 managed schemas

dl\_cache — Enrichment cache

Stores raw enrichment events from every provider call. Each row represents a single enrichment response keyed by a unique source identifier (provider + operation + input hash). The `doc` column holds the full JSON response from the upstream provider. The `extracted_potential_identifier_keys` column contains parsed identifiers (emails, LinkedIn URLs, domains) used by the identity graph for entity linking.**Key table:**`dl_cache.enrichment_event`This schema is write-only from the application perspective. The platform runtime role writes enrichment results here; tenant roles have no direct write access. You can read cached enrichment data through the `dl_resolved` views, which merge cache and override data into a single coalesced record.**Why it matters:** Every enrichment call you have ever made is stored here. You never re-pay for data you already have. The cache also powers waterfall logic — if Provider A already returned an email for this person, Provider B is only called if the cached result is stale or incomplete.

dl\_override — Manual overrides

Stores human-entered corrections and custom enrichment events. When a user corrects an email, updates a job title, or tombstones a stale record, the change lands here. Override records take precedence over cached enrichment data when the `dl_resolved` views are queried.**Key table:**`dl_override.custom_enrichment_event`This schema is platform-managed. Do not write to it through
`query_customer_db`; use the supported product/API workflow for corrections
when one is available.**Why it matters:** Enrichment data is never perfect. Overrides give you a clean separation between “what the provider said” and “what we know is actually correct,” without mutating the original cached data.

dl\_graph — Identity graph

The core of cross-provider deduplication. This schema contains three interconnected tables:

- **`dl_graph.entities`** — Resolved person and company entities. Each entity has a type (`person` or `company`) and an optional parent link (person → company).
- **`dl_graph.adoptions`** — Links enrichment event rows (from `dl_cache` or `dl_override`) to their resolved entity. Tracks confidence scores and the reasoning behind each adoption.
- **`dl_graph.identifier_memberships`** — Maps parsed identifiers (email addresses, LinkedIn URLs, company domains, phone numbers) to entities. This is the join table that enables “find all enrichment results for this person across all providers.”

**Why it matters:** When you enrich the same VP of Sales through three different providers, you get three separate JSON blobs. The identity graph resolves them into a single entity, links all three enrichment events to that entity, and makes the merged result available through `dl_resolved`. Without the graph, you would be writing custom deduplication logic for every provider combination.

dl\_resolved — Coalesced views

Read-only views that merge `dl_cache` enrichment events, `dl_override` corrections, and `dl_graph` entity resolution into a single queryable surface. This is the schema you query 90% of the time.**Key views:**

- `dl_resolved.people` — One row per resolved person entity, with the best available data from all linked enrichment events and any override patches applied.
- `dl_resolved.companies` — Same for companies.
- `dl_resolved.coalesced_enrichment_event` — Lower-level view showing individual enrichment events with override patches applied.

**Why it matters:** You never have to write merge logic. Query `dl_resolved.people` and you get a single record per person with the best email, most recent job title, and all linked identifiers — regardless of which provider originally returned each field.

dl\_meta — Migrations and settings

Tracks schema version and tenant-specific configuration. The platform uses this to manage rolling migrations across tenant databases.**Key tables:**

- `dl_meta.schema_migrations` — Ordered list of applied migrations with timestamps. Used by the migration runner to determine which migrations need to run on each tenant.
- `dl_meta.tenant_settings` — Key-value store for tenant-specific configuration (e.g., which schema object names are active after a migration).

**Why it matters:** Rolling migrations mean your database schema evolves without downtime. The migration runner checks `dl_meta.schema_migrations`, applies any pending migrations in order, and updates `dl_meta.tenant_settings` with the new configuration. You do not need to manage migrations yourself — this is platform-managed.

### The agent-writable schema

In addition to managed schemas, every tenant database includes a `storage`
schema. The customer DB role can create, alter, insert, update, delete, and drop
objects in this schema when statements schema-qualify the target. Use it for
agent-created tables, scratch state, workflow notes, and small application
objects that live alongside enrichment data.

```
CREATE TABLE IF NOT EXISTS storage.account_segments (
  id text PRIMARY KEY,
  segment text NOT NULL,
  score numeric,
  created_at timestamptz NOT NULL DEFAULT now()
);
```

* * *

## Database roles

Each tenant database has carefully scoped permissions. You never connect as the
owner role; it is used only during provisioning and migrations.

| Role | Purpose | Permissions |
| --- | --- | --- |
| **Owner** | Provisioning and DDL | Full access to all schemas. Used by the platform during bootstrap and migrations. Never exposed to users. |
| **Customer DB** | Agent and app SQL | Read access to permitted workspace schemas, plus DDL and DML on schema-qualified `storage.*` tables. |
| **Runtime** (`dl_platform_runtime`) | Platform operations | Internal role for enrichment writes, identity graph updates, and managed runtime work. |

**Which path should I use?** Start with `query_customer_db` for agent-driven
reads and `storage`-scoped writes. Use direct connection URIs only when an app
or BI tool needs a PostgreSQL connection string.

* * *

## How to access your database

### 1\. Check workspace status

```
curl -s https://code.deepline.com/api/v2/ingestion/status \
  -H "Authorization: Bearer $DEEPLINE_API_KEY" | jq .
```

If not provisioned, provision first from the dashboard Database Access flow (workspace admin session required):

```
POST /api/v2/ingestion/provision
```

### 2\. Get a connection URI

```
POST /api/v2/ingestion/connection-uris/reveal
{
  "kind": "customer_db",
  "pooled": true
}
```

The customer DB URI can read permitted workspace schemas and write to
schema-qualified `storage.*` tables.

`GET /api/v2/ingestion/status` supports API key auth. Provisioning and
credential reveal/rotation flows are currently intended for the
dashboard/admin session.

### 3\. Connect with any PostgreSQL client

psql

Query resolved people

Python (psycopg2)

TypeScript (pg)

DBeaver Looker

```
psql "postgresql://dl_ingest_customer_db:****@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require"
```

### 4\. Export your data

Full database export:

```
pg_dump "postgresql://dl_ingest_customer_db:****@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require" \
  --schema=dl_resolved \
  --no-owner \
  -f deepline-export.sql
```

Export to CSV:

```
psql "$DEEPLINE_DB_URI" -c "\copy (SELECT * FROM dl_resolved.people) TO 'people.csv' WITH CSV HEADER"
```

* * *

## Rolling migrations

Deepline uses rolling migrations to evolve your database schema without downtime. Each migration is versioned, ordered, and idempotent. The migration runner:

1. Reads `dl_meta.schema_migrations` to determine the current schema version
2. Applies any pending migrations in order
3. Updates `dl_meta.tenant_settings` with new configuration (e.g., renamed tables/views)
4. Verifies the migration with SQL checks

You do not need to run migrations yourself. The platform manages migration rollout across all tenant databases. Migrations are designed to be backward-compatible during the rollout window — your queries against `dl_resolved` continue to work while migrations are in progress.

* * *

## Frequently asked questions

Can I connect a BI tool like Metabase or Looker directly?

Yes. Request a `customer_db` connection URI and configure your BI tool with the host, port, database, username, and password from the URI. SSL is required. Use resolved or enrichment views as the primary query surface for reporting.

Is my data shared with other tenants?

No. Each workspace gets a dedicated Neon project. Your data is physically
isolated — separate compute, separate storage, separate connection endpoints.
There is no row-level security or shared-schema multi-tenancy.

Can I run my own DDL (CREATE TABLE, ALTER TABLE)?

Yes, through `query_customer_db` or the customer DB role when every write
target is schema-qualified under `storage`. For example:
`CREATE TABLE IF NOT EXISTS storage.agent_notes (...)`. Unqualified DDL such
as `CREATE TABLE agent_notes (...)` is blocked, as are writes to managed
schemas.

How do I rotate database credentials?

Call the credential rotation endpoint:

```
POST /api/v2/ingestion/connection-uris/rotate
{
  "kind": "customer_db"
}
```

This generates a new password for the specified role. Existing connections using the old password will be terminated.

Can I use pg\_dump to back up my database?

Yes. Connect with the customer DB role and run `pg_dump` against schemas that
role can read. For a full export of resolved data, dump the read-model schema
available in your workspace. For agent-created state, dump `storage`.

What PostgreSQL version is this?

Neon runs PostgreSQL 16. All standard PostgreSQL 16 features, extensions
(pgcrypto is enabled by default), and wire protocol compatibility apply.

Is there a row limit or storage cap?

Neon’s serverless architecture scales storage automatically. There is no artificial row limit imposed by Deepline. Storage costs are part of the Neon project provisioned for your workspace — see the pricing page for details.

* * *

## Related

[**Database Access (Developer Guide)** \\
\\
API endpoints, code patterns, and the full schema contract reference for\\
building on your database.](https://deepline.com/docs/database-access)

[**Quick Start** \\
\\
Install Deepline and run your first enrichment in 60 seconds.](https://deepline.com/docs/quickstart)

[**CLI Concepts** \\
\\
Command patterns, payloads, and execution flow.](https://deepline.com/docs/cli-concepts)

[**Pricing** \\
\\
BYOK free tier, managed credits, and what is included with your database.](https://deepline.com/docs/pricing)


---

# Email Sequencer Integration

Deepline connects to four email sequencers — Instantly (8 operations), Lemlist (22 operations), HeyReach (3 operations), and Smartlead (33+ operations) — so you can enrich a lead and push it to a campaign in the same command. No CSV exports. No manual imports. No tab switching.The entire enrich-to-outbound pipeline collapses into a single workflow: find email, validate it, deduplicate against your existing campaign leads, and push. That workflow runs from the CLI, from Claude Code, or from the API. Every sequencer operation is a first-class Deepline tool, which means Claude Code can create campaigns, add leads, check stats, and manage sequences without you opening a browser.

* * *

## Primitives at a glance

| Sequencer | Operations | Key capabilities |
| --- | --- | --- |
| **Instantly** | 8 | Add to campaign, list campaigns, campaign stats, create campaign, activate/pause campaign, get campaign, list leads |
| **Lemlist** | 22 | Add to campaign, list campaigns, campaign stats, create campaign, pause/resume leads, manage sequences, webhooks, inbox threads, export leads, send email, send LinkedIn message, unsubscribe management |
| **HeyReach** | 3 | Add to campaign, list campaigns, campaign stats |
| **Smartlead** | 33+ | Add to campaign, list campaigns, campaign stats, create campaign, push to campaign, pause/resume leads, manage sequences, webhooks, email account management, warmup stats, lead categories, analytics by date, client management, domain block lists |

All four share a common trio of operations — `add-to-campaign`, `list-campaigns`, and `campaign-stats` — which means switching sequencers does not require rewriting your workflow logic. You change the provider name and the rest works.

* * *

## What sequencer integration means for your outbound

Here is where the integration helps and where it does not.

- **Closes the gap between enrichment and outbound.** Deepline’s waterfall finds the email, validates it with ZeroBounce or Leadmagic, deduplicates against existing campaign leads, and pushes — all in one command. No intermediate CSV. No stale data sitting in a download folder. The lead goes from unknown to sequenced in seconds.
- **One interface for multiple sequencers.** If you run Smartlead for high-volume sends and HeyReach for LinkedIn outreach, Deepline wraps both in the same CLI and API shape. A/B test sequencers by changing one parameter, not rebuilding an integration.
- **AI-driven campaign management.** Every sequencer operation is a Deepline tool, so Claude Code can “create a 3-step sequence in Smartlead for this ICP, add the 50 leads I just enriched, and set a webhook to ping me on replies.” The agent plans the steps, calls the tools, and reports back.
- **Skip it if you only use one sequencer and prefer its UI.** If you live inside Instantly’s dashboard and never touch the API, this integration adds a layer you do not need. The value shows up when you are automating, scaling, or coordinating across tools.

* * *

## Per-sequencer breakdown

### Instantly (8 operations)

Instantly is a cold email platform built for deliverability at scale. Deepline covers the full campaign lifecycle:

| Operation | What it does |
| --- | --- |
| `add-to-campaign` | Push one or more leads into an existing campaign |
| `list-campaigns` | Retrieve all campaigns in your Instantly workspace |
| `campaign-stats` | Get open rates, reply rates, and bounce rates for a campaign |
| `instantly-create-campaign` | Create a new campaign with name and settings |
| `instantly-activate-campaign` | Start a paused or draft campaign |
| `instantly-pause-campaign` | Pause an active campaign |
| `instantly-get-campaign` | Fetch full details for a specific campaign |
| `instantly-list-leads` | List all leads in a given campaign |

Instantly’s strength is simplicity. Eight operations cover what most outbound teams need: create, populate, activate, monitor. If you are running straightforward cold email at volume, Instantly’s integration is the fastest path from enrichment to sending.

### Lemlist (22 operations)

Lemlist is a multi-channel outreach platform with email, LinkedIn, and manual task sequences. Deepline’s 22 operations cover the full surface:**Campaign management:** create campaigns, list campaigns, update campaigns, pause campaigns, get campaign sequences, export campaign leads, campaign stats.**Lead management:** add to campaign, pause leads, resume leads, mark leads as interested.**Sequence authoring:** add sequence steps, update sequence steps, delete sequence steps.**Messaging:** send email, send LinkedIn message.**Webhooks:** add webhook, get webhooks, delete webhook.**Compliance:** add unsubscribe, get unsubscribes, export unsubscribes, delete unsubscribe.**Inbox:** list inbox, get inbox thread.**Billing:** check credits.Lemlist’s depth makes it the right choice when you need programmatic control over multi-channel sequences. Claude Code can build a sequence (email step 1, LinkedIn connection request step 2, follow-up email step 3), add leads, and monitor replies — all through Deepline tools.

### HeyReach (3 operations)

HeyReach is a LinkedIn automation platform. Deepline’s integration covers the essentials:

| Operation | What it does |
| --- | --- |
| `add-to-campaign` | Push leads into a HeyReach LinkedIn outreach campaign |
| `list-campaigns` | Retrieve all campaigns |
| `campaign-stats` | Get connection request acceptance rates and reply rates |

HeyReach is deliberately narrow. You use it alongside an email sequencer: enrich with Deepline, push email leads to Instantly or Lemlist, push LinkedIn leads to HeyReach. Same enrichment data, two channels, one CLI.

### Smartlead (33+ operations)

Smartlead is a high-volume cold email infrastructure platform with deep API coverage. Deepline exposes 33+ operations spanning every major subsystem:**Campaign lifecycle:** create campaign, get campaign, list campaigns, delete campaign, update campaign status, update campaign settings, update campaign schedule, campaign stats, get campaign analytics, get campaign analytics by date.**Lead management:** push to campaign, list campaign leads, export campaign leads, fetch lead by email, fetch campaigns by lead, delete lead by campaign, pause lead by campaign, resume lead by campaign, update lead by campaign, update lead category by campaign, unsubscribe lead by campaign, unsubscribe lead, list lead categories, reply to lead thread, fetch lead message history.**Email accounts:** list email accounts, fetch email account, upsert email account, add campaign email account, remove campaign email account, list campaign email accounts, reconnect failed email accounts, fetch email account warmup stats, update email account, update email account warmup.**Sequences:** fetch campaign sequences, save campaign sequences.**Webhooks:** fetch campaign webhooks, upsert campaign webhook, delete campaign webhook.**Infrastructure:** create client, fetch clients, add domain block list, generic API request.Smartlead’s breadth makes it the power-user choice. If you need to manage email account rotation, warmup scheduling, lead-level campaign analytics, or client-level multi-tenant setups, Smartlead’s 33+ operations give you full programmatic control.

* * *

## Example workflows

### Enrich and push to a campaign (single command)

The most common workflow: find an email, validate it, and push to a sequencer. One prompt to Claude Code handles all three steps.

```
# In Claude Code:
# /deepline-gtm Find the work email for Sarah Chen at Ramp,
# validate it, and add her to my Instantly campaign 'Q1 Outbound'
```

Under the hood, Deepline runs:

```
# Step 1: Find email via waterfall
deepline tools execute name_and_domain_to_email_waterfall \
  --payload '{"first_name":"Sarah","last_name":"Chen","domain":"ramp.com"}'

# Step 2: Validate the email
deepline tools execute leadmagic_email_validation \
  --payload '{"email":"sarah.chen@ramp.com"}'

# Step 3: Push to Instantly campaign
deepline tools execute instantly_add_to_campaign \
  --payload '{"campaign_id":"abc123","email":"sarah.chen@ramp.com","first_name":"Sarah","last_name":"Chen","company_name":"Ramp"}'
```

### Bulk enrich a CSV and push to Lemlist

```
# In Claude Code:
# /deepline-gtm I have leads.csv with columns: first_name, last_name, company, domain.
# Find and validate work emails for each row.
# Deduplicate against my Lemlist campaign 'Enterprise Q1'.
# Push new leads only.
```

Claude Code reads the CSV, runs the waterfall for each row, validates results, pulls existing leads from the Lemlist campaign to deduplicate, and pushes only net-new leads. The dedup step prevents embarrassing double-sends and wasted sequencer credits.

### Create a Smartlead campaign from scratch

```
# In Claude Code:
# /deepline-gtm Create a new Smartlead campaign called 'Series A Founders'.
# Add a 3-step email sequence: intro, case study follow-up, break-up.
# Set sending schedule to weekdays 9am-5pm EST.
# Add my 25 enriched leads from the last run.
```

Claude Code calls `smartlead_create_campaign`, then `smartlead_save_campaign_sequences` with three steps, then `smartlead_update_campaign_schedule`, then `smartlead_push_to_campaign` with the leads. Four API calls, one natural-language prompt.

### A/B test sequencers

```
# In Claude Code:
# /deepline-gtm Take my 100 enriched leads and split them 50/50.
# Push half to my Instantly campaign 'Test A'.
# Push the other half to my Lemlist campaign 'Test B'.
# After 7 days, pull campaign stats from both and compare.
```

Same enrichment data, two sequencers, one report. What used to take an hour of CSV juggling becomes a single conversation.

### LinkedIn + email multi-channel

```
# In Claude Code:
# /deepline-gtm For each lead in prospects.csv:
# - Find their work email and push to Instantly campaign 'Cold Email Q1'
# - Find their LinkedIn URL and push to HeyReach campaign 'LinkedIn Q1'
```

Deepline enriches both data points from the same waterfall run. Leads get pushed to both channels without duplicate enrichment costs.

* * *

## Lead deduplication before push

Every push workflow should deduplicate. Sending the same lead twice wastes sequencer credits, damages deliverability, and annoys prospects. Deepline handles this in two ways:

1. **Campaign-level dedup.** Before pushing, pull existing leads from the target campaign (`list-campaigns` \+ lead listing operations) and filter out duplicates by email address. Claude Code does this automatically when you say “push new leads only.”
2. **Cross-campaign dedup.** For Smartlead, use `fetch-campaigns-by-lead` to check if a lead exists in any campaign before adding them. Lemlist’s `export-campaign-leads` serves the same purpose.

Dedup is not a feature you enable — it is a pattern the agent follows. When you include “deduplicate” or “new leads only” in your prompt, Claude Code adds the check step to its plan.

* * *

## FAQ

Do I need API keys for each sequencer?

Yes. Each sequencer requires its own API key. Add them in the dashboard at `https://code.deepline.com/dashboard/keys` (see [Bring Your Own Keys](https://deepline.com/docs/features/bring-your-own-keys)). Deepline stores keys encrypted at rest, scoped to your workspace; calls to the sequencer go directly from the Deepline execution engine to the sequencer’s API. You can also use Deepline managed keys if your plan includes sequencer credits.

Can I use multiple sequencers at the same time?

Yes. There is no limit on how many sequencers you connect. A common pattern is
Instantly or Smartlead for email and HeyReach for LinkedIn. You can push the
same enriched lead to multiple sequencers in a single workflow. The CLI and
API treat each sequencer as an independent provider.

How does dedup work across sequencers?

Deepline deduplicates per-campaign within a single sequencer by pulling
existing leads before pushing. Cross-sequencer dedup (e.g., “don’t add to
Lemlist if already in Instantly”) requires checking both campaigns. Claude
Code can do this when prompted — it pulls leads from both campaigns, computes
the set difference, and pushes only unique leads to each.

What happens if a sequencer API rate-limits me?

Deepline surfaces the rate limit error in the CLI output. It does not automatically retry — you control the retry logic. Claude Code will typically wait and retry when it sees a 429 response. For high-volume pushes (500+ leads), consider batching: push in groups of 50-100 with a short pause between batches.

* * *

## What’s next

[**Instantly Provider Guide** \\
\\
Full reference for all 8 Instantly operations.](https://deepline.com/docs/providers/instantly/guide)

[**Lemlist Provider Guide** \\
\\
Full reference for all 22 Lemlist operations.](https://deepline.com/docs/providers/lemlist/guide)

[**Smartlead Provider Guide** \\
\\
Browse all 33+ Smartlead operations.](https://deepline.com/docs/providers/smartlead/list-campaigns)

[**Email Waterfall** \\
\\
How Deepline finds and validates emails before pushing to sequencers.](https://deepline.com/docs/plays/task-email-waterfall-with-validation)


---

# Claude Code Skills

Deepline ships 4 open-source skills that give Claude Code structured GTM knowledge. Instead of prompting from scratch every time, you install a skill once and invoke it as a slash command. The skill tells Claude Code exactly which providers to call, in what order, with validated parameters — so you get reliable results on the first run instead of the fifth.Skills are not required. The Deepline CLI works without them. But if you run the same enrichment workflows repeatedly — building TAM lists, running email waterfalls, detecting job changes — skills eliminate the setup time and make every run consistent.

| Attribute | Value |
| --- | --- |
| Skills available | 4 user-facing skills |
| Install command | `/skill install deepline` (inside Claude Code) |
| Recipes in deepline-gtm | 7 step-by-step playbooks |
| Agent compatibility | Claude Code, Codex, Cursor |
| Source | Open-source on GitHub |
| Required to use Deepline | No — CLI works standalone |

* * *

## What skills mean for your GTM workflow

Skills solve a specific problem: AI agents are powerful but inconsistent. The same prompt can produce different provider choices, parameter formats, and execution orders on every run. Skills fix that by encoding proven workflows into reusable documents that the agent reads before executing.

- **Natural language GTM automation.** Type `/deepline-gtm` followed by what you want in plain English — e.g. `/deepline-gtm find work emails for these 200 contacts` — and the skill handles provider selection, waterfall ordering, validation, and output formatting. You describe the outcome; the skill handles the how.
- **Repeatable playbooks.** TAM building, waterfall enrichment, job change detection, portfolio prospecting — these follow the same steps every time. Skills encode those steps as slash commands so every run follows the same proven path, whether you execute it or a teammate does.
- **Team standardization.** Skills are files. Share them via Git, and everyone uses the same provider priorities, cost thresholds, and approval gates. No more “it works differently when I run it.”
- **Not required.** Every Deepline operation works through `deepline tools execute` and `deepline enrich` without any skill installed. Skills add a knowledge layer that makes the agent smarter about sequencing, provider selection, and error handling — but they are optional.

* * *

## Available skills

Deepline ships 4 user-facing skills. Each one is a collection of markdown documents that Claude Code reads before executing your request.

### deepline-gtm

The primary skill. Covers the full GTM workflow from prospecting through enrichment to outreach. This is the skill most teams install first and use daily.**What it handles:**

- Finding companies and contacts (Apollo, Crustdata, PDL, and more)
- Enriching and researching (waterfalls, signal extraction, verification)
- Writing outreach (personalization, qualification, email drafting)
- Provider-specific playbooks with validated parameters and known pitfalls
- 7 recipes for common GTM tasks (detailed below)

**Includes sub-documents for:**

- Finding companies and contacts — provider selection, role-based search, parallel execution
- Enriching and researching — waterfall patterns, email/phone/LinkedIn enrichment, play tools
- Writing outreach — qualification frameworks, personalization, email copy
- Actor contracts — Apify actor execution patterns
- Provider playbooks — per-provider quirks, cost notes, fallback behavior

### clay-to-deepline

Migrates Clay table configurations into local Deepline scripts. Use this when you have a Clay table JSON export, a Clay API response, or a Clay workbook URL and want to replicate the workflow without Clay.**What it handles:**

- Converting Clay AI action columns (Claygent, use-ai, chat-gpt-schema-mapper) into `deepline enrich` with `call_ai`, `exa_search`, and `run_javascript`
- Replicating Clay record fetching with Deepline providers
- Producing standalone bash + Deepline scripts that run locally

**Useful when:**

- “Convert my Clay table into a Deepline script”
- “I have a Clay JSON export, make it work with Deepline”
- “Replicate this Clay workflow without the platform fee”

### deepline-feedback

Sends structured feedback or bug reports to the Deepline team. Automatically captures your session transcript, environment info, and reproduction steps.**What it handles:**

- Collecting feedback text from the user
- Packaging session context (transcript, environment, CLI version)
- Submitting to the Deepline team

**Useful when:**

- “Something broke during my enrichment run”
- “I want to request a new provider integration”
- “This waterfall is returning bad results, can you look into it?“

### niche-signal-discovery

Discovers differential signals between Closed Won and Closed Lost accounts. Extracts website content, job listings, and tech stack data, then computes lift scores to identify what distinguishes buyers from non-buyers.**What it handles:**

- Multi-page website content extraction for won vs lost accounts
- Job listing analysis for hiring signals
- Laplace-smoothed lift scoring for signal ranking
- Building account scoring models and prospecting criteria from real data

**Useful when:**

- “What signals differentiate my best customers from lost deals?”
- “Build me an ICP scoring model from my CRM data”
- “Find niche signals I can use to prioritize accounts”

* * *

## deepline-gtm recipes

The deepline-gtm includes 7 recipes — step-by-step playbooks for specific GTM tasks. Each recipe tells Claude Code exactly what to read, which providers to use, and in what order.

### build-tam

Build a Total Addressable Market list by sourcing accounts and contacts from providers like Apollo, Crustdata, and PDL. Start with company discovery criteria (industry, headcount, location, funding), find matching accounts, then find contacts at each one.

```
/deepline-gtm Build a TAM list of Series B+ fintech companies in the US with 50-500 employees
```

### portfolio-prospecting

Find companies backed by a specific investor or accelerator (YC, a16z, Sequoia), then find contacts and build personalized outbound. The recipe’s core insight: VC portfolio data is public. It fetches portfolio pages directly instead of wasting credits on provider searches.

```
/deepline-gtm Find all YC W24 companies and get me the CEO email for each one
```

### waterfall-enrichment

Run multiple enrichment providers in sequence, stopping at the first valid result. Maximizes coverage while minimizing cost — you only pay for the provider that finds the answer. Works for email, phone, LinkedIn, and company data.

```
/deepline-gtm Enrich my CSV with emails using the waterfall pattern
```

### job-change-detector

Detect CRM contacts who changed jobs in the last 6 months, find their new work emails, update the CRM, and add them to re-engagement campaigns. Integrates with HubSpot via MCP tools.

```
/deepline-gtm Find contacts in HubSpot who changed jobs and update their records
```

### contact-to-email

Find and verify email addresses from whatever starting data you have: name + company, LinkedIn URL, or name + domain. Routes to the right play tool based on your input columns.

```
/deepline-gtm I have names and companies in this CSV, find their work emails
```

### get-leads-at-company

From a company name, find the right GTM contacts (by role/title), enrich their contact info, and optionally draft personalized outreach. Chains company lookup, contact discovery, email waterfall, and outreach generation.

```
/deepline-gtm Get me the VP of Sales at each company in accounts.csv and draft a cold email
```

### linkedin-url-lookup

Resolve LinkedIn profile URLs from name + company with strict identity validation to avoid false positives. Uses multiple providers with cross-validation to ensure the profile matches the right person.

```
/deepline-gtm Find LinkedIn URLs for the contacts in my CSV and verify they match
```

* * *

## How to install

### Inside Claude Code

Run the slash command:

```
/skill install deepline
```

This installs all 4 user-facing skills. After installation, skills are available as slash commands in your Claude Code session.

### Verify installation

After installing, confirm the skills loaded:

```
ls ~/.claude/skills/
```

You should see directories for `deepline-gtm`, `clay-to-deepline`, `deepline-feedback`, and `niche-signal-discovery`.

### Using with Codex and Cursor

Skills also work with Codex and Cursor. The skill files are standard markdown documents that any AI coding agent can read. Install using the same command, or point your agent at the skill markdown files directly.

### Updating skills

Skills are versioned with the Deepline CLI. When you update the CLI, skills update too:

```
npm install -g deepline && deepline auth register
```

Then reinstall skills to pick up changes:

```
/skill install deepline
```

* * *

## FAQ

Do I need skills to use Deepline?

No. Every Deepline operation is available through `deepline tools execute` and `deepline enrich` without any skill installed. Skills add a knowledge layer that makes your AI agent smarter about provider selection, sequencing, and error handling. They make things faster and more consistent, but they are not required.

What is the difference between skills and the CLI?

The CLI is the execution layer — it calls providers, runs waterfalls,
processes CSVs. Skills are the knowledge layer — they tell your AI agent which
CLI commands to run, in what order, with what parameters. Skills do not
replace the CLI; they make it easier for agents to use the CLI correctly.

Can I edit or customize skills?

Yes. Skills are open-source markdown files. Fork them, edit them, add your own
provider preferences or approval gates, and share them with your team via Git.
The skill files live in `~/.claude/skills/` after installation.

Do skills cost credits?

No. Skills themselves are free. They are documentation files that guide your
agent. The underlying provider calls (email lookups, enrichments, scraping)
cost credits as usual, but the skill layer adds zero cost.

How are skills different from plays and recipes in the docs?

Plays and recipes in the docs are reference material for humans. Skills are
the same knowledge packaged for AI agents — structured so Claude Code, Codex,
or Cursor can read them and execute the workflow automatically. The content
overlaps, but skills include execution-specific details like parameter
schemas, provider fallback orders, and error handling that agents need.

Can I create my own skills?

Yes. A skill is a directory with a `SKILL.md` entrypoint and optional
sub-documents. Follow the same structure as the built-in skills: a frontmatter
block with name and description, then markdown content with execution steps.
See the Deepline GitHub repo for the full skill specification.

Which skill should I install first?

Start with `deepline-gtm`. It covers the full GTM workflow and includes 7 recipes for the most common tasks. The other skills are specialized — install them when you need Clay migration, feedback reporting, or ICP signal analysis.

* * *

## What’s next

[**Quick Start** \\
\\
Install Deepline and find your first email in 60 seconds.](https://deepline.com/docs/quickstart)

[**CLI Concepts** \\
\\
Full CLI reference — commands, flags, waterfall syntax.](https://deepline.com/docs/cli-concepts)

[**Plays & Recipes** \\
\\
Human-readable workflow guides for common GTM tasks.](https://deepline.com/docs/plays/decision-tree)

[**Pricing** \\
\\
BYOK is free. Managed credits are $0.10 each, with 10% off on 1,000+\\
credits.](https://deepline.com/docs/pricing)


---

# PART 3 — TypeScript SDK v2


---

## SDK: overview

# Deepline SDK

Beta: the SDK docs are published and usable, but the surface is still
evolving.

Write GTM workflows that just work: start from a search, CSV, webhook, or schedule; enrich the right records; route each customer through the right path; push the result to Salesforce, a sequencer, a warehouse, or a CSV; then visualize what changed.

## 1\. Pick the job

Keep the use case short. The workflow can hold the detail.

| Job | Example |
| --- | --- |
| Weekly account search | Find new companies, enrich them, send qualified accounts to Salesforce |
| Custom waterfall | Try providers in your order, stop at the first verified email |
| Segment routing | Run one path for directors, another for VPs |
| Inbound routing | Enrich demo requests and send the right context to the owner |
| Data review | Export a CSV and chart segments, signals, and gaps |

## 2\. Pilot one record

Start with a prebuilt workflow before writing your own.

```
deepline plays describe prebuilt/person-linkedin-to-email --json

deepline plays run prebuilt/person-linkedin-to-email \
  --input '{"linkedin_url":"https://www.linkedin.com/in/real-person/"}' \
  --watch
```

Use a real lead you are allowed to enrich. A fake LinkedIn URL can return nothing.

## 3\. Pilot a small list

```
head -n 4 leads.csv > /tmp/leads-pilot.csv

deepline plays run lead-email-waterfall.play.ts \
  --csv /tmp/leads-pilot.csv \
  --watch

deepline runs export <run-id> --out /tmp/leads-pilot-enriched.csv
```

Inspect the output. If the rows look right, run the full file.

## 4\. Add your business logic

This is where the SDK matters.

```
import { definePlay } from 'deepline';

type Lead = {
  lead_id: string;
  seniority: 'VP' | 'Director';
  company_domain: string;
};

export default definePlay(
  'route-leads-by-seniority',
  async (ctx, input: { leads: Lead[] }) => {
    const rows = await ctx
      .dataset('routed_leads', input.leads)
      .withColumn('research', async (lead, rowCtx) => {
        if (lead.seniority === 'VP') {
          return await rowCtx.tools.execute({
            id: 'vp_research',
            tool: 'test_rate_limit',
            input: { key: lead.company_domain },
            description: 'Research this enterprise lead.',
          });
        }

        return await rowCtx.tools.execute({
          id: 'director_research',
          tool: 'test_rate_limit',
          input: { key: lead.company_domain },
          description: 'Research this midmarket lead.',
        });
      })
      .run({
        key: 'lead_id',
        description: 'Route each lead to the right research workflow.',
      });

    return { rows };
  },
);
```

Use this for custom waterfalls, segment-specific paths, weekly searches, and background jobs.

## 5\. Ship the output

```
deepline plays run weekly-new-accounts.play.ts \
  --input '{"query":"B2B SaaS companies hiring RevOps"}' \
  --watch

deepline runs export <run-id> --out output/new-accounts.csv
```

Then push to Salesforce, send to a sequencer, write to your warehouse, or chart the result.

## Examples

[**Weekly company search to Salesforce** \\
\\
Search once a week, enrich new accounts, and create Salesforce tasks.](https://deepline.com/docs/sdk-v2/examples#weekly-company-search)

[**Custom email waterfall** \\
\\
Try your preferred providers and stop at the first verified email.](https://deepline.com/docs/sdk-v2/examples#lead-email-waterfall)

[**Director vs VP routing** \\
\\
Run different research paths based on buyer type.](https://deepline.com/docs/sdk-v2/examples#segment-specific-routing)

[**Inbound webhook enrichment** \\
\\
Enrich form fills and route them to the right owner.](https://deepline.com/docs/sdk-v2/examples#inbound-lead-webhook)

[**Nightly account refresh** \\
\\
Refresh account signals on a schedule with a run cap.](https://deepline.com/docs/sdk-v2/examples#nightly-account-refresh)

[**Python caller** \\
\\
Trigger a saved workflow from Airflow, notebooks, or backend jobs.](https://deepline.com/docs/sdk-v2/examples#python-caller)

## Reference

[**SDK Reference** \\
\\
CLI commands, workflow authoring, config, triggers, and org safety.](https://deepline.com/docs/sdk-v2/sdk-reference)

[**API Reference** \\
\\
HTTP request shapes for starting and checking workflow runs.](https://deepline.com/docs/sdk-v2/api-reference)


---

## SDK: examples

# Examples

Use these as starting points. Each one follows the same rhythm: inspect, pilot, review, then scale.

## Weekly company search

Job: find new companies on a schedule, enrich the new matches, and send qualified accounts to Salesforce.

```
deepline plays check weekly-new-accounts.play.ts
deepline plays run weekly-new-accounts.play.ts \
  --input '{"query":"B2B SaaS companies hiring RevOps","salesforce_owner":"alex@acme.com"}' \
  --watch

deepline runs export <run-id> --out output/new-accounts.csv
```

## Lead email waterfall

Job: take a lead list and return verified work emails.If you are outside the repo, create a starter CSV first:

```
cat > leads.csv <<'CSV'
lead_id,first_name,last_name,linkedin_url,company_name,company_domain
lead-001,Jane,Smith,https://www.linkedin.com/in/example-person/,ExampleCo,example.com
lead-002,Sam,Lee,https://www.linkedin.com/in/example-two/,Acme,acme.com
CSV
```

```
deepline plays check lead-email-waterfall.play.ts

head -n 4 leads.csv > /tmp/leads-pilot.csv

deepline plays run lead-email-waterfall.play.ts \
  --csv /tmp/leads-pilot.csv \
  --watch

deepline runs export <run-id> --out /tmp/leads-pilot-enriched.csv
```

Inspect `/tmp/leads-pilot-enriched.csv`. Then run the full file:

```
deepline plays run lead-email-waterfall.play.ts \
  --csv leads.csv \
  --watch

deepline runs export <run-id> --out leads-enriched.csv
```

## Inbound lead webhook

Job: enrich a new form fill before routing it.

```
deepline plays check inbound-lead-webhook.play.ts
deepline plays publish inbound-lead-webhook.play.ts
```

## Nightly account refresh

Job: refresh account signals every morning with a run cap.

```
deepline plays check nightly-account-refresh.play.ts
deepline plays publish nightly-account-refresh.play.ts
deepline plays run docs-nightly-account-refresh --input '{"accounts":[]}' --watch
```

## Segment-specific routing

Job: run a different workflow depending on who the buyer is.

```
import { definePlay } from 'deepline';

type Lead = {
  lead_id: string;
  seniority: 'VP' | 'Director';
  company_domain: string;
};

export default definePlay('segment-specific-routing', async (ctx, input: { leads: Lead[] }) => {
  const rows = await ctx
    .dataset('routed_leads', input.leads)
    .withColumn('next_step', async (lead, rowCtx) => {
      if (lead.seniority === 'VP') {
        return await rowCtx.tools.execute({
          id: 'vp_research',
          tool: 'test_rate_limit',
          input: { key: lead.company_domain },
          description: 'Research this enterprise lead.',
        });
      }

      return await rowCtx.tools.execute({
        id: 'director_research',
        tool: 'test_rate_limit',
        input: { key: lead.company_domain },
        description: 'Research this midmarket lead.',
      });
    })
    .run({ key: 'lead_id' });

  return { rows };
});
```

Use this when VPs need account-level context and directors need more tactical pain-point research.

## Python caller

Job: trigger a prebuilt play from a Python process.

```
python3 -m pip install requests
cat >.env.deepline <<'ENV'
DEEPLINE_HOST_URL=https://code.deepline.com
DEEPLINE_API_KEY=dl_workspace_key
ENV
python3 docs-examples/sdk-v2/http-python/run_prebuilt.py
```

The script reads `.env.deepline` directly. Use this from Airflow, a notebook,
or a backend worker.


---

## SDK: api-reference

# Runtime API Reference

Generated from the SDK route registry and public SDK types by `scripts/generate-play-sdk-reference.ts`. Do not edit this file manually.

Generated from source comments and type declarations by `scripts/generate-play-sdk-reference.ts`. Do not edit this file manually.

## Version And Coverage

| Field | Value |
| --- | --- |
| SDK version | `0.1.109` |
| API contract | `2026-06-dataset-column-cell-stale-hard-cutover` |
| Latest supported SDK | `0.1.109` |
| Minimum supported SDK | `0.1.53` |
| Deprecated below | `0.1.53` |
| Generated sources | `src/lib/sdk/api-routes.ts`<br>`sdk/src/types.ts`<br>`sdk/src/client.ts`<br>`sdk/src/release.ts` |
| Coverage | HTTP and SDK client surface for runtime calls: health, tool/provider discovery and execution, customer data queries, play runs, play definitions, play artifacts, files, and run inspection. |
| Not covered | Provider-specific schemas, dashboard-only UI routes, billing/auth setup guides, and tutorial prose. Provider-specific schemas are returned by the generated tool describe routes. |

## Best Current Pattern

Strong runtime API references lead with base URL, auth, version/contract metadata, language examples, and exact generated route tables. Deepline follows that shape here: use the quick call flows first, then the generated route and type tables below for contract details.

## Quick Call Flow

1. `POST /api/v2/plays/run` with a saved/prebuilt `name` and JSON `input`.
2. Read `workflowId` from the response. Treat it as the public run id.
3. Poll `GET /api/v2/runs/:runId` or stream `GET /api/v2/runs/:runId/tail`.
4. Stop when `status` is `completed`, `failed`, or `cancelled`.
5. Read final user output from `result` or the compact `package.outputs` object.

Use the CLI or TypeScript SDK for local file compilation and artifact upload. Raw HTTP is best for backend services, Python jobs, schedulers, notebooks, and warehouses that invoke an already-saved or prebuilt play.

## Tool And Provider Call Flow

1. `GET /api/v2/tools/search?q=...` to discover ranked provider/tool candidates.
2. `GET /api/v2/integrations/:toolId/get` to inspect input schema, pricing, extractors, and examples.
3. `POST /api/v2/integrations/:toolId/execute` with `payload` to execute the provider-backed tool.
4. Read normalized data from `toolResponse.raw`, `extractedValues`, and `extractedLists`. Do not expose provider spend; customer-visible billing is Deepline credits/USD only.

Inside a play, prefer `ctx.tools.execute(...)` so calls are durable, idempotent, and recorded in run progress. From a regular SDK process, use `Deepline.connect().tools.execute(...)` or `client.executeTool(...)`.

## Authentication

Use the Deepline host plus a workspace API key from a trusted backend environment.

```
export DEEPLINE_HOST_URL="${DEEPLINE_HOST_URL:-https://code.deepline.com}"
export DEEPLINE_API_KEY="dl_workspace_key"
```

Every request uses bearer auth:

```
Authorization: Bearer <DEEPLINE_API_KEY>
```

## Start A Named Or Prebuilt Play

```
curl -X POST "$DEEPLINE_HOST_URL/api/v2/plays/run" \
  -H "Authorization: Bearer $DEEPLINE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "prebuilt/person-linkedin-to-email",
    "input": {
      "linkedin_url": "https://www.linkedin.com/in/example-person/"
    }
  }'
```

Response:

```
{
  "workflowId": "play_run_...",
  "apiVersion": 2,
  "status": "running",
  "dashboardUrl": "https://code.deepline.com/dashboard/plays/..."
}
```

## Poll Status

```
curl "$DEEPLINE_HOST_URL/api/v2/runs/$WORKFLOW_ID?full=true" \
  -H "Authorization: Bearer $DEEPLINE_API_KEY"
```

Terminal statuses are `completed`, `failed`, and `cancelled`. `queued`, `running`, and `waiting` are non-terminal.

## Stream Events

```
curl -N "$DEEPLINE_HOST_URL/api/v2/runs/$WORKFLOW_ID/tail?mode=cli" \
  -H "Authorization: Bearer $DEEPLINE_API_KEY" \
  -H "Accept: text/event-stream"
```

The stream emits a canonical run snapshot first, then incremental play events until the connection closes or the run reaches terminal state.

## Stop A Run

```
curl -X POST "$DEEPLINE_HOST_URL/api/v2/runs/$WORKFLOW_ID/stop" \
  -H "Authorization: Bearer $DEEPLINE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"reason":"caller cancelled"}'
```

## Python Caller

This example is copied from `docs-examples/sdk-v2/http-python/run_prebuilt.py` and compiled by `bun run docs:sdk-v2:check`.Source: `docs-examples/sdk-v2/http-python/run_prebuilt.py`

```
import os
import time
import json
import requests

def load_deepline_env(path=".env.deepline"):
    values = {}
    if not os.path.exists(path):
        return values
    with open(path) as env_file:
        for line in env_file:
            stripped = line.strip()
            if not stripped or stripped.startswith("#") or "=" not in stripped:
                continue
            key, value = stripped.split("=", 1)
            values[key.strip()] = value.strip().strip('"').strip("'")
    return values

deepline_env = load_deepline_env()
BASE_URL = os.environ.get(
    "DEEPLINE_HOST_URL",
    deepline_env.get("DEEPLINE_HOST_URL", "https://code.deepline.com"),
)
API_KEY = os.environ.get("DEEPLINE_API_KEY", deepline_env.get("DEEPLINE_API_KEY"))
if not API_KEY:
    raise RuntimeError("Missing DEEPLINE_API_KEY in .env.deepline")

headers = {
    "Authorization": f"Bearer {API_KEY}",
    "Content-Type": "application/json",
}

start = requests.post(
    f"{BASE_URL}/api/v2/plays/run",
    headers=headers,
    json={
        "name": "prebuilt/person-linkedin-to-email",
        "input": {
            "linkedin_url": "https://www.linkedin.com/in/example-person/",
        },
    },
    timeout=30,
)
start.raise_for_status()
workflow_id = start.json()["workflowId"]

while True:
    status = requests.get(
        f"{BASE_URL}/api/v2/runs/{workflow_id}",
        headers=headers,
        timeout=30,
    )
    status.raise_for_status()
    body = status.json()
    if body.get("status") in {"completed", "failed", "cancelled"}:
        with open("person-email-result.json", "w") as f:
            json.dump(body, f, indent=2)
        print(body)
        break
    time.sleep(2)
```

## Generated Route Tables

### Runtime Health

| Method | Path | SDK/client surface | Purpose | Source |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v2/health` | `health` | Check API availability and SDK target health. | `src/app/api/v2/health/route.ts` |

### Tool And Provider Calls

| Method | Path | SDK/client surface | Purpose | Source |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v2/integrations/:toolId` | `getTool` | Describe one provider-backed tool by integration id. | `src/app/api/v2/integrations/[toolId]/route.ts` |
| `POST` | `/api/v2/integrations/:toolId/execute` | `executeTool`<br>`executeToolRaw` | Execute one provider-backed tool call through Deepline. | `src/app/api/v2/integrations/execute/route.ts` |
| `GET` | `/api/v2/integrations/:toolId/get` | `getTool` | Describe one provider-backed tool, including schema, pricing, guidance, and extractors. | `src/app/api/v2/integrations/get/route.ts` |
| `GET` | `/api/v2/integrations/list` | `searchTools` | Compatibility discovery route for integration/tool listing. | `src/app/api/v2/integrations/list/route.ts` |
| `GET` | `/api/v2/tools` | `listTools` | List callable provider/tool definitions. | `src/app/api/v2/tools/route.ts` |
| `GET` | `/api/v2/tools/search` | `searchTools` | Search callable provider/tool definitions with ranked metadata search. | `src/app/api/v2/tools/search/route.ts` |

### Customer Data

| Method | Path | SDK/client surface | Purpose | Source |
| --- | --- | --- | --- | --- |
| `POST` | `/api/v2/db/query` | `queryCustomerDb` | Run a bounded query against the customer data plane. | `src/app/api/v2/db/query/route.ts` |

### Play Runs

| Method | Path | SDK/client surface | Purpose | Source |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v2/plays/:name/runs` | `listPlayRuns` | List recent runs for one play. | `src/app/api/v2/plays/[name]/runs/route.ts` |
| `GET` | `/api/v2/plays/:name/sheet` | `runs.exportDatasetRows`<br>`getPlaySheetRows` | Read/export runtime sheet rows for a run dataset. | `src/app/api/v2/plays/[name]/sheet/route.ts` |
| `POST` | `/api/v2/plays/run` | `startPlayRun`<br>`startPlayRunFromBundle`<br>`runPlay` | Start a saved, prebuilt, or artifact-backed play run. | `src/app/api/v2/plays/run/route.ts` |
| `GET` | `/api/v2/runs` | `runs.list`<br>`listRuns` | List runs with filters such as play name and status. | `src/app/api/v2/runs/route.ts` |
| `GET` | `/api/v2/runs/:runId` | `runs.get`<br>`getRunStatus`<br>`getPlayStatus` | Read canonical status, result, outputs, and run package. | `src/app/api/v2/runs/[runId]/route.ts` |
| `GET` | `/api/v2/runs/:runId/logs` | `runs.logs`<br>`getRunLogs` | SDK-facing route. | `src/app/api/v2/runs/[runId]/logs/route.ts` |
| `POST` | `/api/v2/runs/:runId/observe-grant` | `runs.tail`<br>`tailRun`<br>`runPlay` | SDK-facing route. | `src/app/api/v2/runs/[runId]/observe-grant/route.ts` |
| `POST` | `/api/v2/runs/:runId/stop` | `runs.stop`<br>`stopRun`<br>`cancelPlay`<br>`stopPlay` | Stop a running or waiting play run. | `src/app/api/v2/runs/[runId]/stop/route.ts` |
| `GET` | `/api/v2/runs/:runId/tail` | `runs.tail`<br>`tailRun` | Stream canonical run events over SSE. | `src/app/api/v2/runs/[runId]/tail/route.ts` |

### Play Definitions

| Method | Path | SDK/client surface | Purpose | Source |
| --- | --- | --- | --- | --- |
| `GET` | `/api/v2/plays` | `listPlays`<br>`searchPlays` | List or search callable plays. | `src/app/api/v2/plays/route.ts` |
| `DELETE` | `/api/v2/plays/:name` | `deletePlay` | Delete a saved org-owned play. | `src/app/api/v2/plays/[name]/route.ts` |
| `GET` | `/api/v2/plays/:name` | `getPlay`<br>`describePlay` | Describe a saved, shared, or prebuilt play. | `src/app/api/v2/plays/[name]/route.ts` |
| `POST` | `/api/v2/plays/:name/history/clear` | `clearPlayHistory` | SDK-facing route. | `src/app/api/v2/plays/[name]/history/clear/route.ts` |
| `POST` | `/api/v2/plays/:name/live` | `publishPlayVersion` | Promote a revision as the live named play. | `src/app/api/v2/plays/[name]/live/route.ts` |
| `GET` | `/api/v2/plays/:name/versions` | `listPlayVersions` | List saved play revisions. | `src/app/api/v2/plays/[name]/versions/route.ts` |

### Play Artifacts

| Method | Path | SDK/client surface | Purpose | Source |
| --- | --- | --- | --- | --- |
| `POST` | `/api/v2/enrich/compile` | `compileEnrichPlan` | Compile legacy enrich command input into a runtime plan. | `src/app/api/v2/enrich/compile/route.ts` |
| `POST` | `/api/v2/plays/artifacts` | `registerPlayArtifact` | Register a bundled play artifact for ad hoc runs. | `src/app/api/v2/plays/artifacts/route.ts` |
| `POST` | `/api/v2/plays/check` | `checkPlayArtifact` | Validate a play bundle before storing or running it. | `src/app/api/v2/plays/check/route.ts` |
| `POST` | `/api/v2/plays/files/stage` | `stagePlayFiles`<br>`resolveStagedPlayFiles` | Stage CSV or packaged files used by play runs. | `src/app/api/v2/plays/files/stage/route.ts` |

### Management And CLI

| Method | Path | SDK/client surface | Purpose | Source |
| --- | --- | --- | --- | --- |
| `POST` | `/api/v2/auth/cli/org-create` | `org create` | SDK-facing route. | `src/app/api/v2/auth/cli/org-create/route.ts` |
| `POST` | `/api/v2/auth/cli/organizations` | `org list` | SDK-facing route. | `src/app/api/v2/auth/cli/organizations/route.ts` |
| `POST` | `/api/v2/auth/cli/register` | `auth register` | SDK-facing route. | `src/app/api/v2/auth/cli/register/route.ts` |
| `POST` | `/api/v2/auth/cli/status` | `auth status` | SDK-facing route. | `src/app/api/v2/auth/cli/status/route.ts` |
| `POST` | `/api/v2/auth/cli/switch` | `org switch` | SDK-facing route. | `src/app/api/v2/auth/cli/switch/route.ts` |
| `GET` | `/api/v2/billing/balance` | `billing balance` | SDK-facing route. | `src/app/api/v2/billing/balance/route.ts` |
| `GET` | `/api/v2/billing/catalog/current` | `billing.plans`<br>`getBillingPlans`<br>`billing plans` | SDK-facing route. | `src/app/api/v2/billing/catalog/current/route.ts` |
| `POST` | `/api/v2/billing/checkout` | `billing checkout` | SDK-facing route. | `src/app/api/v2/billing/checkout/route.ts` |
| `POST` | `/api/v2/billing/checkout/verify` | `billing redeem` | SDK-facing route. | `src/app/api/v2/billing/checkout/verify/route.ts` |
| `GET` | `/api/v2/billing/invoices` | `billing.invoices.list`<br>`listBillingInvoices`<br>`billing invoices` | SDK-facing route. | `src/app/api/v2/billing/invoices/route.ts` |
| `GET` | `/api/v2/billing/ledger` | `billing history` | SDK-facing route. | `src/app/api/v2/billing/ledger/route.ts` |
| `DELETE` | `/api/v2/billing/limit` | `billing limit off` | SDK-facing route. | `src/app/api/v2/billing/limit/route.ts` |
| `GET` | `/api/v2/billing/limit` | `billing limit` | SDK-facing route. | `src/app/api/v2/billing/limit/route.ts` |
| `POST` | `/api/v2/billing/limit` | `billing limit set` | SDK-facing route. | `src/app/api/v2/billing/limit/route.ts` |
| `POST` | `/api/v2/billing/subscription/cancel` | `billing.subscription.cancel`<br>`cancelBillingSubscription`<br>`billing subscription cancel` | SDK-facing route. | `src/app/api/v2/billing/subscription/cancel/route.ts` |
| `POST` | `/api/v2/billing/subscription/checkout` | `billing subscribe` | SDK-facing route. | `src/app/api/v2/billing/subscription/checkout/route.ts` |
| `GET` | `/api/v2/billing/subscription/status` | `billing.subscription.status`<br>`getBillingSubscriptionStatus`<br>`billing subscription status` | SDK-facing route. | `src/app/api/v2/billing/subscription/status/route.ts` |
| `GET` | `/api/v2/billing/usage` | `billing usage` | SDK-facing route. | `src/app/api/v2/billing/usage/route.ts` |
| `POST` | `/api/v2/cli/feedback` | `feedback` | SDK-facing route. | `src/app/api/v2/cli/feedback/route.ts` |
| `POST` | `/api/v2/cli/send-session` | `sessions send` | SDK-facing route. | `src/app/api/v2/cli/send-session/route.ts` |
| `POST` | `/api/v2/cli/send-session/chunk` | `sessions send` | SDK-facing route. | `src/app/api/v2/cli/send-session/chunk/route.ts` |
| `POST` | `/api/v2/cli/send-session/finalize` | `sessions send` | SDK-facing route. | `src/app/api/v2/cli/send-session/finalize/route.ts` |
| `DELETE` | `/api/v2/plays/:name/share` | `unpublishSharePage` | SDK-facing route. | `src/app/api/v2/plays/[name]/share/route.ts` |
| `GET` | `/api/v2/plays/:name/share` | `getSharePage` | SDK-facing route. | `src/app/api/v2/plays/[name]/share/route.ts` |
| `PATCH` | `/api/v2/plays/:name/share` | `updateSharePage` | SDK-facing route. | `src/app/api/v2/plays/[name]/share/route.ts` |
| `POST` | `/api/v2/plays/:name/share` | `publishSharePage` | SDK-facing route. | `src/app/api/v2/plays/[name]/share/route.ts` |
| `POST` | `/api/v2/plays/:name/share/regenerate` | `regenerateSharePage` | SDK-facing route. | `src/app/api/v2/plays/[name]/share/regenerate/route.ts` |
| `GET` | `/api/v2/sdk/compat` | `compat check` | SDK-facing route. | `src/app/api/v2/sdk/compat/route.ts` |
| `GET` | `/api/v2/secrets` | `secrets list`<br>`secrets check`<br>`listSecrets` | SDK-facing route. | `src/app/api/v2/secrets/route.ts` |
| `POST` | `/api/v2/secrets` | `secrets set` | SDK-facing route. | `src/app/api/v2/secrets/route.ts` |
| `DELETE` | `/api/v2/secrets/:id` | `secrets delete` | SDK-facing route. | `src/app/api/v2/secrets/[id]/route.ts` |
| `POST` | `/api/v2/secrets/:id/test` | `secrets test` | SDK-facing route. | `src/app/api/v2/secrets/[id]/test/route.ts` |
| `GET` | `/api/v2/workflows` | `listWorkflows` | SDK-facing route. | `src/app/api/v2/workflows/route.ts` |
| `DELETE` | `/api/v2/workflows/:id` | `deleteWorkflow` | SDK-facing route. | `src/app/api/v2/workflows/[id]/route.ts` |
| `GET` | `/api/v2/workflows/:id` | `getWorkflow` | SDK-facing route. | `src/app/api/v2/workflows/[id]/route.ts` |
| `POST` | `/api/v2/workflows/:id/disable` | `disableWorkflow` | SDK-facing route. | `src/app/api/v2/workflows/[id]/disable/route.ts` |
| `POST` | `/api/v2/workflows/:id/enable` | `enableWorkflow` | SDK-facing route. | `src/app/api/v2/workflows/[id]/enable/route.ts` |
| `GET` | `/api/v2/workflows/:id/runs` | `listWorkflowRuns` | SDK-facing route. | `src/app/api/v2/workflows/[id]/runs/route.ts` |
| `GET` | `/api/v2/workflows/:id/runs/:runId` | `getWorkflowRun` | SDK-facing route. | `src/app/api/v2/workflows/[id]/runs/[runId]/route.ts` |
| `POST` | `/api/v2/workflows/:id/runs/:runId/cancel` | `cancelWorkflowRun` | SDK-facing route. | `src/app/api/v2/workflows/[id]/runs/[runId]/cancel/route.ts` |
| `POST` | `/api/v2/workflows/apply` | `applyWorkflow` | SDK-facing route. | `src/app/api/v2/workflows/apply/route.ts` |
| `POST` | `/api/v2/workflows/call` | `callWorkflow` | SDK-facing route. | `src/app/api/v2/workflows/call/route.ts` |
| `POST` | `/api/v2/workflows/lint` | `lintWorkflow` | SDK-facing route. | `src/app/api/v2/workflows/lint/route.ts` |
| `GET` | `/api/v2/workflows/schema` | `getWorkflowSchema` | SDK-facing route. | `src/app/api/v2/workflows/schema/route.ts` |

## Recent Compatible API Changes

These entries come from `COMPATIBLE_SDK_API_CHANGES` and explain additive changes that did not require an SDK API-contract bump. The full ledger lives in `src/lib/sdk/api-routes.ts`.

| Change | Reason |
| --- | --- |
| `2026-06-play-live-dashboard-first-paint-optimization` | Optimizes GET /api/v2/plays/:name/live for dashboard first paint by allowing lightweight one-run discovery, preserving the existing full recent-runs refresh path, and parallelizing optional enrichment/pipeline work. Route path, method, a… |
| `2026-06-sdk-thin-package-runtime-deps` | Trims the published `deepline` npm package by removing Convex/ws/acorn runtime dependencies and replacing the broad packaged repo snapshot with a minimal local-play bundling source payload. This is packaging, local CLI bundling, and SDK… |
| `2026-06-sdk-installer-quickstart-auth-copy` | Fixes SDK CLI installer handoff behavior and auth success parity without changing the SDK/API contract: installer-launched `deepline quickstart` now honors DEEPLINE\_INSTALLER\_MODE/—no-launch while still using a terminal for browser sele… |
| `2026-06-play-live-run-backed-canvas-definition` | Makes GET /api/v2/plays/:name/live?mode=canvas return an existing run-backed definition payload when an org/generated play has persisted runs but its definition document is not yet visible, instead of returning a transient 404. The respo… |
| `2026-06-sdk-cli-scalar-output-rendering` | Improves SDK CLI and internal Slack rendering for scalar play-run outputs: completed run packages with top-level value outputs now show those values in `plays run --watch` text output, and SDK CLI activity alerts label scalar results as… |
| `2026-06-sdk-enrich-prebuilt-alias-resolution` | Fixes SDK CLI/enrich prebuilt play naming and dashboard live-state handling without changing installed-client contracts: legacy native waterfall ids such as person\_linkedin\_to\_email\_waterfall now resolve to their existing canonical V2 pr… |
| `2026-06-synthetic-play-run-active-cap` | Raises the internal active-run limit only for non-prod synthetic POST /api/v2/plays/run requests used by preview/runtime test harnesses. Customer requests keep the same workspace active-run cap, route path, method, auth semantics, reques… |
| `2026-06-sdk-enrich-waterfall-provider-fallback` | Updates SDK CLI `deepline enrich --with-waterfall` generated play source so recoverable provider/upstream 5xx failures inside a waterfall child are treated as misses and later waterfall providers can run. This is CLI-side generated-sourc… |

## Public Types

### `ToolDefinition`

Summary definition of a callable provider-backed tool.Returned by `DeeplineClient.listTools` and ranked tool search. Use
`getTool(toolId)` or the matching HTTP describe route for provider-specific
schema, examples, pricing, and extraction guidance before executing.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `toolId` | `string` | Yes | Unique tool identifier used in API calls (e.g. `"dropleads_search_people"`). |
| `provider` | `string` | Yes | Provider that backs this tool (e.g. `"hunter"`, `"dropleads"`, `"test"`). |
| `displayName` | `string` | Yes | Human-readable name for display. |
| `description` | `string` | Yes | What this tool does — suitable for LLM tool descriptions. |
| `categories` | `DeeplineToolCategory[]` | Yes | Categorization tags (e.g. `["people", "enrichment"]`). |
| `operation` | `string` | No | Operation slug within the provider. |
| `operationId` | `string` | No | Normalized operation identifier. |
| `operationAliases` | `string[]` | No | Alternative names that resolve to this tool. |
| `playReference` | `prebuilt/${string}` | No | Explicit globally runnable play reference for play-backed catalog entries. |
| `hasInputSchema` | `boolean` | No | Whether detailed input schema is available from `tools describe`. |
| `hasOutputSchema` | `boolean` | No | Whether detailed output schema is available from `tools describe`. |
| `inputSchema` | `Record<string, unknown>` | No | JSON Schema describing the tool’s input parameters. |
| `outputSchema` | `Record<string, unknown>` | No | JSON Schema describing the tool’s output shape. |
| `pricing` | `ToolPricingSummary | null` | No | User-facing pricing summary. Internal provider/settlement costs are intentionally omitted. |
| `usageGuidance` | `{ execute?: string; prefer?: string[]; access?: { extractedLists?: { expression?: string; meaning?: string; }; extractedValues?: { expression?: string; meaning?: string; }; rawToolResponse?: { expression?: string; meaning?: string; }; invalidGetterHint?: string; }; toolExecutionResult?: { type?: 'ToolExecutionResult'; toolResponse?: { raw?: string; meta?: string; }; meta?: string; extractedLists?: | Array<{ name: string; expression: string; details?: { strategy?: string; rawToolOutputPaths?: string[]; candidatePaths?: string[]; }; }> | Record< string, { expression: string; details?: { strategy?: string; rawToolOutputPaths?: string[]; candidatePaths?: string[]; }; } >; extractedValues?: | Array<{ name: string; expression: string; details?: { strategy?: string; rawToolOutputPaths?: string[]; candidatePaths?: string[]; }; }> | Record< string, { expression: string; details?: { strategy?: string; rawToolOutputPaths?: string[]; candidatePaths?: string[]; }; } >; [key: string]: unknown; }; }` | No | Copyable play-runtime guidance for V2 tool execution results. |
| `search_score` | `number` | No | Search relevance score returned by ranked tool search. |
| `search_matches` | `Array<{ field: string; value: string; term?: string; }>` | No | Search match snippets returned by ranked tool search. |

### `ToolSearchOptions`

Query options for ranked tool/provider discovery.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `query` | `string` | No | Free-text search query. |
| `categories` | `string` | No | Comma-separated category filter such as `company_search` or `email_finder`. |
| `searchTerms` | `string` | No | Optional explicit search terms used by agent/CLI callers. |
| `searchMode` | `'v1' | 'v2'` | No | Search algorithm/version. Defaults to the current ranked mode. |
| `includeSearchDebug` | `boolean` | No | Include backend debug metadata in the search response. |

### `ToolSearchResult`

Ranked tool/provider discovery response.Includes matching tools plus render/action hints used by the CLI and agents.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `tools` | `ToolDefinition[]` | Yes | Ranked matching tools. |
| `count` | `number` | No | Count included in this response when available. |
| `total` | `number` | No | Total available count when the backend reports it. |
| `truncated` | `boolean` | No | Whether results were truncated by server-side limits. |
| `query` | `string` | No | Echoed query. |
| `categories` | `string[]` | No | Parsed category filters. |
| `search_terms` | `string[]` | No | Parsed search terms. |
| `search_mode` | `'v1' | 'v2'` | No | Search mode used. |
| `search_fallback_to_category` | `boolean` | No | Whether search fell back to category matching. |
| `omitted_plays_hint` | `string` | No | Hint explaining omitted play results when searching tools only. |
| `commandTemplates` | `{ describe?: string; execute?: string; }` | No | Copyable CLI command templates for follow-up discovery/execution. |
| `render` | `{ sections?: Array<{ title: string; lines: string[]; }>; actions?: Array<{ label: string; command: string; }>; }` | No | Pre-rendered sections and actions for CLI/agent display. |

### `ToolExecution`

Standard provider/tool execution envelope returned by low-level SDK calls.`toolResponse.raw` contains the provider result. `extractedValues` and
`extractedLists` contain Deepline-normalized getters when the tool exposes
them. Billing fields are Deepline-facing and must not expose provider spend.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `status` | `string` | Yes |  |
| `job_id` | `string` | No |  |
| `meta` | `Record<string, unknown>` | No |  |
| `toolResponse` | `{ raw: TData; meta?: TMeta; }` | Yes |  |
| `extractedLists` | `Record<string, unknown>` | No |  |
| `extractedValues` | `Record<string, unknown>` | No |  |
| `billing` | `Record<string, unknown>` | No |  |

### `StartPlayRunRequest`

Request body for starting a play run via `DeeplineClient.startPlayRun`.Internal/advanced request shape for low-level submission primitives.
Most callers should prefer `deepline plays run`, `DeeplineClient.runPlay`,
or `Deepline.connect`.Either `name` (for live plays) or `artifactStorageKey` (for packaged ad hoc runs) is required.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | `string` | No | Play name for registered revisions. |
| `revisionId` | `string` | No | Explicit revision ID when the caller wants a specific saved version. |
| `artifactStorageKey` | `string` | No | R2 artifact key for ad hoc artifact-backed runs. |
| `sourceCode` | `string` | No | Source snapshot already validated while registering this artifact. |
| `sourceFiles` | `Record<string, string>` | No | Source graph snapshots for local helper files included in cloud preflight. |
| `staticPipeline` | `unknown` | No | Static pipeline already produced while registering this artifact. |
| `artifactHash` | `string` | No | Artifact content hash already validated while registering this artifact. |
| `graphHash` | `string` | No | Static graph hash already validated while registering this artifact. |
| `runtimeArtifact` | `Record<string, unknown>` | No | Optional preloaded artifact snapshot for immediate ad hoc execution. |
| `compilerManifest` | `PlayCompilerManifest` | No | Compiler manifest for ad hoc graph runs, including imported play dependencies. |
| `inputFileUpload` | `unknown` | No | Primary input file bytes for one-shot server-side staging. |
| `packagedFileUploads` | `unknown[]` | No | Packaged file bytes for one-shot server-side staging. |
| `input` | `Record<string, unknown>` | No | Runtime input passed to the play function as its second argument. |
| `inputFile` | `unknown` | No | Staged file reference for the primary input file (e.g. CSV). |
| `packagedFiles` | `unknown[]` | No | Additional staged file references (dependencies, data files). |
| `force` | `boolean` | No | Compatibility flag; active sibling runs are allowed. |
| `waitForCompletionMs` | `number` | No | Optionally let the start request wait briefly and return a terminal result. |
| `profile` | `string` | No | Per-run execution profile override. The server defaults to workers\_edge;<br>tests and runtime probes can pass a different profile here. Most callers<br>should leave this unset. |

### `PlayRunStart`

Response from starting a play run.Internal/advanced payload returned by low-level play submission primitives.
Most callers should prefer `deepline plays run`, `DeeplineClient.runPlay`,
or `PlayJob.get`.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `workflowId` | `string` | Yes | Public Deepline play-run id for tracking this execution. |
| `apiVersion` | `number` | No | Public Deepline play-run API version. |
| `name` | `string` | No | Play name (echoed back from the request). |
| `status` | `string` | No | Initial status (typically `'RUNNING'`). |
| `runtimeBackend` | `string` | No | Resolved runtime backend used for this run. |
| `contract` | `Record<string, unknown> | null` | No | Canonical run contract compatibility metadata. |
| `dashboardUrl` | `string` | No | Dashboard URL for the named play. |
| `finalStatus` | `unknown` | No | Terminal status returned when the start request used a short completion wait. |
| `package` | `PlayRunPackage` | No | Canonical compact run package returned by current SDK/API responses. |

### `PlayStatus`

Current status of a play execution, returned by `DeeplineClient.getPlayStatus`.Poll this until `status` reaches a terminal state:
`'completed'` \| `'failed'` \| `'cancelled'`.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `runId` | `string` | Yes | Public play-run identifier. |
| `apiVersion` | `number` | No | Public Deepline play-run API version. |
| `name` | `string` | No | Saved play name for this run, when available. |
| `playName` | `string` | No | Alias for `name` used by run/result APIs. |
| `dashboardUrl` | `string` | No | Dashboard URL for inspecting the play and its run output in the app. |
| `status` | `| 'queued' | 'running' | 'waiting' | 'completed' | 'failed' | 'cancelled'` | Yes | Product-level play-run state. |
| `progress` | `PlayProgressStatus` | No | Execution progress with logs and error details. |
| `result` | `unknown` | No | Partial or final result. Available once the play returns. |
| `package` | `PlayRunPackage` | No | Compact typed run package returned by current run status endpoints. |
| `outputs` | `PlayRunPackage['outputs']` | No | Compact typed output summaries, mirrored from the run package when present. |
| `run` | `{ id?: string; startTime?: string | null; closeTime?: string | null; [key: string]: unknown; } | null` | No | Scheduler-backed run metadata when returned by the status endpoint. |
| `resultView` | `unknown` | No | Server-rendered result view metadata for CLI/UI summaries. |
| `contract` | `Record<string, unknown> | null` | No | Canonical run contract snapshot metadata, when available. |
| `wait` | `{ kind: 'integration_event' | 'sleep'; boundaryId?: string; eventKey?: string; until?: number; } | null` | No | If the run is blocked on a durable boundary, expose the public wait state. |
| `next` | `PlayRunPackage['next'] | Record<string, unknown>` | No | Structured follow-up actions for inspect/query/export. |

### `PlayRunPackage`

Compact canonical package for an inspected play run.This object is designed for SDK/CLI/API consumers that need stable run
metadata, output handles, and follow-up actions without reading dashboard
internals.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `schemaVersion` | `1` | Yes | Package schema version. |
| `kind` | `'play_run'` | Yes | Package discriminator. |
| `run` | `{ id: string; playName: string; status: string; dashboardUrl?: string; updatedAt?: number | null; startedAt?: number | null; finishedAt?: number | null; durationMs?: number | null; error?: string; }` | Yes | Run identity, status, timing, and dashboard metadata. |
| `steps` | `Array<Record<string, unknown>>` | Yes | Step-level summaries emitted by the runtime. |
| `outputs` | `Record<string, Record<string, unknown>>` | Yes | Named output summaries, including dataset handles and scalar outputs. |
| `next` | `{ inspect?: PlayRunActionPackage; export?: PlayRunActionPackage; query?: PlayRunActionPackage; }` | No | Follow-up actions a caller can perform against the run. |

### `PlayRunListItem`

Summary of a single play run, returned by `DeeplineClient.listPlayRuns`.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `workflowId` | `string` | Yes | Public Deepline play-run id. |
| `runId` | `string` | Yes | Backend run attempt id, when exposed. |
| `type` | `string` | Yes | Workflow type (typically `'Workflow'`). |
| `status` | `string` | Yes | Human-readable status (e.g. `'Completed'`, `'Failed'`). |
| `startTime` | `string | null` | Yes | ISO 8601 timestamp when the run started. |
| `closeTime` | `string | null` | Yes | ISO 8601 timestamp when the run finished. |
| `executionTime` | `string | null` | Yes | Duration string (e.g. `'2.5s'`). |
| `billingTotalCredits` | `number` | No | Total Deepline credits charged for the run, when available. |
| `billingMaxCreditsPerRun` | `number | null` | No | Configured per-run Deepline credit cap, when available. |
| `memo` | `{ orgId: string; playName: string; userId: string | null; }` | Yes | Metadata attached to the workflow. |

### `StopPlayRunResult`

Result returned by `DeeplineClient.stopPlay`.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `runId` | `string` | Yes | Public play-run identifier the stop request targeted. |
| `stopped` | `boolean` | Yes | Whether the server confirmed the run was stopped. |
| `hitlCancelledCount` | `number` | Yes | Number of open HITL interactions marked cancelled. |
| `staleSchedulerState` | `boolean` | No | True when the scheduler state for the run was stale and the stop could<br>not be confirmed. Absent on older servers (treated as confirmed). |
| `error` | `string` | No | Server-side error detail when the stop was not confirmed. |

### `RunsNamespace`

Public runs namespace exposed as `client.runs`.This namespace mirrors the canonical `/api/v2/runs` resource family and is
the preferred low-level surface for polling, streaming, stopping, reading
logs, and exporting durable dataset rows.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `get` | `(runId: string, options?: { full?: boolean }) => Promise<PlayStatus>` | Yes | Get current run status by public run id. |
| `list` | `(options: RunsListOptions) => Promise<PlayRunListItem[]>` | Yes | List runs for one play, optionally filtered by status. |
| `tail` | `(runId: string, options?: RunsTailOptions) => Promise<PlayStatus>` | Yes | Stream run events and return the latest/terminal run status. |
| `logs` | `(runId: string, options?: RunsLogsOptions) => Promise<RunsLogsResult>` | Yes | Fetch persisted log lines for a run. |
| `exportDatasetRows` | `(input: { playName: string; tableNamespace: string; runId?: string; limit?: number; offset?: number; }) => Promise<PlaySheetRowsResult>` | Yes | Export persisted rows for a runtime-sheet dataset/table namespace. |
| `stop` | `( runId: string, options?: { reason?: string }, ) => Promise<StopPlayRunResult>` | Yes | Stop a running/waiting run. |

### `CustomerDbQueryResult`

Result returned by `DeeplineClient.queryCustomerDb`.Rows are intentionally untyped because the schema depends on the caller’s SQL
query and selected customer tables.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `command` | `string` | Yes | Database command executed by the query endpoint. |
| `row_count` | `number | null` | Yes | Total affected row count when reported by the database. |
| `row_count_returned` | `number` | Yes | Number of rows included in this response. |
| `truncated` | `boolean` | Yes | Whether server-side row limits truncated the result. |
| `columns` | `CustomerDbColumn[]` | Yes | Column metadata for the returned rows. |
| `rows` | `unknown[]` | Yes | Result rows. |


---

## SDK: sdk-reference

# SDK Reference

Generated from SDK source comments by `scripts/generate-play-sdk-reference.ts`. Do not edit this file manually.

## Runtime Model

The Deepline SDK is a runtime SDK. Your TypeScript defines durable play code and typed run contracts; Deepline executes that code in the cloud runtime, records provider/tool calls, persists dataset rows, and exposes run state through SDK handles and HTTP APIs.Use `definePlay(...)` for code that runs inside a Deepline play. Inside that function, `ctx.*` is the runtime boundary: `ctx.tools.execute` calls managed providers, `ctx.dataset` records row-level work, `ctx.step` checkpoints scalar work, `ctx.fetch` records external HTTP, and `ctx.runPlay` composes registered or prebuilt plays.Use `Deepline.connect()` and `DeeplineClient` from regular Node/TypeScript services, scripts, schedulers, or tests. Those APIs discover tools and plays, start runs, stream/poll status, stop runs, and inspect durable output without requiring a local play file.

## Reference Map

| Area | Primary surface | Use when |
| --- | --- | --- |
| Runtime entrypoint | `Deepline.connect()` / `DeeplineContext` | A script or service needs to call tools, run plays, or inspect runs. |
| Play authoring | `definePlay(...)` / `ctx.*` | Code should run durably inside Deepline with persisted steps, datasets, tools, and child plays. |
| Tool/provider calls | `ctx.tools.execute(...)`, `deepline.tools.execute(...)`, `client.executeTool(...)` | You need provider-backed enrichment/search with Deepline auth, billing, extraction metadata, and retries. |
| Remote plays/runs | `ctx.play(name)`, `ctx.runPlay(...)`, `PlayJob`, `client.runs` | You need to run, poll, stream, stop, export, publish, or inspect plays. |
| Raw HTTP | `references/api-reference.md` | A backend, notebook, scheduler, or non-TypeScript caller invokes Deepline over REST. |

## Detail Policy

| Material | Rendered as |
| --- | --- |
| Tested examples | Full runnable code blocks. |
| Classes | One member table with purpose, parameters, and returns. |
| Interfaces and object types | Field tables; no duplicate declaration dump. |
| Fieldless aliases or overloads | Compact signature line plus parameter/return tables. |
| Full HTTP routes | Generated in `references/api-reference.md`. |

## Tested Examples

These examples are copied from `docs-examples/sdk-v2` and validated by `bun run docs:sdk-v2:check`. Keep examples there first, then regenerate this reference.

### Run A Prebuilt From TypeScript

Source: `docs-examples/sdk-v2/run-prebuilt.ts`

```
import { Deepline } from 'deepline';

const ctx = await Deepline.connect();

const job = await ctx.play('prebuilt/person-linkedin-to-email').run({
  linkedin_url: 'https://www.linkedin.com/in/example-person/',
});

const result = await job.get();
console.log(JSON.stringify(result, null, 2));
```

### Define A Play With `ctx.tools.execute`

Source: `docs-examples/sdk-v2/company-lookup.play.ts`

```
import { definePlay } from 'deepline';

type Input = {
  domain: string;
};

type Output = {
  domain: string;
  lookupStatus: string;
};

export default definePlay(
  'docs-company-lookup',
  async (ctx, input: Input): Promise<Output> => {
    const result = await ctx.tools.execute({
      id: 'company_lookup',
      tool: 'test_rate_limit',
      input: {
        key: input.domain,
      },
      description:
        'Check that the company lookup path can run for this domain.',
    });

    return {
      domain: input.domain,
      lookupStatus: result.status,
    };
  },
);
```

### Schedule A Dataset Refresh

Source: `docs-examples/sdk-v2/nightly-account-refresh.play.ts`

```
import { definePlay } from 'deepline';

type Account = {
  domain: string;
  owner: string;
};

export default definePlay(
  'docs-nightly-account-refresh',
  async (ctx, input: { accounts: Account[] }) => {
    const rows = await ctx
      .dataset('account_refresh', input.accounts)
      .withColumn(
        'company_signal',
        (account, rowCtx) =>
          rowCtx.tools.execute({
            id: 'company_signal',
            tool: 'test_rate_limit',
            input: {
              key: account.domain,
            },
            description: 'Refresh one account signal for the owner.',
          }),
        { staleAfterSeconds: 86_400 },
      )
      .run({
        key: 'domain',
        description: 'Refresh target account signals once per day.',
      });

    return { rows };
  },
  {
    cron: {
      schedule: '0 9 * * *',
      timezone: 'America/New_York',
    },
    billing: {
      maxCreditsPerRun: 25,
    },
  },
);
```

### Verify A Webhook With HMAC

Source: `docs-examples/sdk-v2/inbound-lead-webhook.play.ts`

```
import { definePlay } from 'deepline';

type InboundLead = {
  email: string;
  company_domain?: string;
  source?: string;
};

export default definePlay(
  'docs-inbound-lead-webhook',
  async (ctx, input: InboundLead) => {
    const domain = input.company_domain ?? input.email.split('@')[1] ?? '';
    const company = await ctx.tools.execute({
      id: 'company_context',
      tool: 'test_rate_limit',
      input: {
        key: domain,
      },
      description: 'Add company context before routing the inbound lead.',
    });

    return {
      email: input.email,
      domain,
      source: input.source ?? 'webhook',
      company_status: company.status,
    };
  },
  {
    webhook: {
      hmac: {
        secretEnv: 'HUBSPOT_WEBHOOK_SECRET',
        header: 'x-hubspot-signature-v3',
        algorithm: 'sha256',
      },
    },
  },
);
```

Generated from source comments and type declarations by `scripts/generate-play-sdk-reference.ts`. Do not edit this file manually.

## Version And Coverage

| Field | Value |
| --- | --- |
| SDK version | `0.1.109` |
| API contract | `2026-06-dataset-column-cell-stale-hard-cutover` |
| Latest supported SDK | `0.1.109` |
| Minimum supported SDK | `0.1.53` |
| Deprecated below | `0.1.53` |
| Generated sources | `sdk/src/client.ts`<br>`sdk/src/play.ts`<br>`shared_libs/play-runtime/cell-staleness.ts`<br>`shared_libs/play-runtime/tool-result-types.ts`<br>`shared_libs/plays/dataset.ts` |
| Coverage | Runtime SDK surface: `Deepline.connect`, `DeeplineContext`, `DeeplineClient`, play authoring, in-play `ctx.*` primitives, provider/tool calls, named play handles, run handles, datasets, and tool result accessors. |
| Not covered | Full CLI command help, provider-specific input/output schemas, dashboard-only routes, and marketing/tutorial guides. Use `references/api-reference.md` for generated HTTP route contracts. |

## Runtime Entrypoints

### `Deepline`

Static entry point for the Deepline SDK.Signature: `class Deepline`

#### Members

| Member | Kind | Purpose | Parameters | Returns / type |
| --- | --- | --- | --- | --- |
| `connect` | method | Create a connected SDK context.<br>Resolves configuration from options, environment variables, and CLI config<br>files. See `resolveConfig` for the resolution order. | `options?: DeeplineClientOptions` \- Optional overrides for API key, base URL, etc. | `Promise<DeeplineContext>` |

### `DeeplineContext`

High-level SDK context with tool shortcuts and play handles.Created by `Deepline.connect`. Wraps a `DeeplineClient` with
a friendlier API for common operations.Signature: `class DeeplineContext`

#### Members

| Member | Kind | Purpose | Parameters | Returns / type |
| --- | --- | --- | --- | --- |
| `constructor` | constructor | Create a high-level SDK context.<br>Most callers should use `Deepline.connect`; direct construction is<br>equivalent when you already have explicit client options. | `options?: DeeplineClientOptions` \- Optional SDK client configuration. |  |
| `tools` | getter | Tool operations namespace. |  | `DeeplineToolsNamespace` |
| `plays` | getter | Play discovery and named-play handles.<br>Use `plays.list()` for discovery and `plays.get(name)` when you prefer a<br>namespace spelling over `ctx.play(name)`. |  | `DeeplinePlaysNamespace` |
| `prebuilt` | getter | Convenience references for Deepline-managed prebuilt plays.<br>Known prebuilts are exposed by camel-cased aliases. Any other property is<br>converted into `prebuilt/<property>` so callers can pass the reference to<br>`ctx.runPlay(...)`. |  | `Record<string, PrebuiltPlayRef>` |
| `play` | method | Get a named play handle for remote lifecycle operations. | `name: string` \- Play name (as registered on the server) | `DeeplineNamedPlay<TInput, TOutput>` |
| `runPlay` | method | Run a named or prebuilt play and wait for its output.<br>This is the high-level SDK equivalent of `ctx.play(name).runSync(input)`.<br>Inside a play runtime, prefer the in-play `ctx.runPlay(key, playRef, input,<br />options)` form so the child run is checkpointed under a stable key. | `playOrRef: string | PlayReferenceLike` \- Play name or prebuilt/reference object.<br>`input: TInput` \- JSON input passed to the play. | `Promise<TOutput>` |

## Play Authoring And In-Play Runtime

### `definePlay`

Define a play — a composable TypeScript workflow for the Deepline platform.The returned value is both:

1. **A callable function** — invoked by the Temporal worker with a runtime context
2. **A named play handle** — with `.run()`, `.versions()`, `.get()`, `.publish()`, etc. for remote lifecycle management

Plays are the primary abstraction for building repeatable data pipelines.
They run on Temporal for durable execution with automatic retries and timeouts.Signature: `export function definePlay<TInput, TOutput extends PlayReturnObject>( config: DefinePlayConfig<TInput, TOutput>, ): DefinedPlay<TInput, TOutput>; export function definePlay<TInput, TOutput extends PlayReturnObject>( name: string, fn: (ctx: DeeplinePlayRuntimeContext, input: TInput) => Promise<TOutput>, bindings?: PlayBindings, ): DefinedPlay<TInput, TOutput>;`

#### Overload 1

#### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `config` | `DefinePlayConfig<TInput, TOutput>` | Yes | Object-form play config. |

#### Returns

`DefinedPlay<TInput, TOutput>`

#### Overload 2

#### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | `string` | Yes | Play name. |
| `fn` | `(ctx: DeeplinePlayRuntimeContext, input: TInput) => Promise<TOutput>` | Yes | Play function. |
| `bindings` | `PlayBindings` | No | Trigger bindings. |

#### Returns

`DefinedPlay<TInput, TOutput>`

### `DefinePlayConfig`

Object-form play definition accepted by `definePlay(config)`.Use this form when the input contract should be explicit at definition time
through `defineInput<T>(schema)`, or when configuration reads clearer as one
object. The shorthand `definePlay(name, fn, bindings?)` is equivalent for
simple file-backed plays.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | `string` | Yes | Play id/name. |
| `input` | `PlayInputContract<TInput>` | Yes | Input schema. |
| `run` | `(ctx: DeeplinePlayRuntimeContext, input: TInput) => Promise<TOutput>` | Yes | Play function. |
| `bindings` | `PlayBindings` | No | Trigger bindings. |
| `billing` | `PlayBindings['billing']` | No | Billing options. |

### `PlayBindings`

Optional trigger bindings for a play.Plays can be triggered by webhooks (with HMAC signature verification)
or cron schedules. Bindings are declared as the third argument to
[definePlay](https://deepline.com/docs/sdk-v2/sdk-reference#defineplay).

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `billing` | `{ maxCreditsPerRun?: number; }` | No | Optional per-run billing controls enforced by the runtime. |
| `webhook` | `{ hmac?: { algorithm?: 'sha256'; header?: string; secretEnv: string; }; }` | No | Webhook trigger with optional HMAC signature verification. |
| `cron` | `{ schedule: string; timezone?: string; }` | No | Cron schedule trigger. |
| `sqlListeners` | `SqlListenerDeclaration[]` | No | Customer DB row-change listeners that wake this play when published. |
| `secrets` | `readonly string[]` | No | Customer-authored play secrets this play is allowed to use at runtime.<br>Values are never bundled or exposed by the SDK; access them with<br>`ctx.secrets.get("NAME")` and approved helpers such as<br>`ctx.secrets.bearer(handle)`. |

### `ctx.csv(path, options)`

Load a staged CSV file as a durable dataset handle.Use this when a play receives a CSV path from the CLI or API and row work
should continue through [DeeplinePlayRuntimeContext.dataset](https://deepline.com/docs/sdk-v2/sdk-reference#runtime-primitives). The path is
normally an input field such as `input.csv`, populated by
`deepline plays run my.play.ts --csv rows.csv`.Each CSV row becomes an object keyed by canonical column names. Use
`options.columns` / `options.rename` to map user headers such as
`"Company Domain"` to stable code fields such as `domain`.Signature: `csv<T = Record<string, unknown>>( path: string, options?: CsvOptions, ): Promise<PlayDataset<T>>;`

#### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `path` | `string` | Yes | Staged CSV path. |
| `options` | `CsvOptions` | No | CSV load options. |

#### Returns

`Promise<PlayDataset<T>>`

### `CsvOptions`

Options for loading a staged CSV with `ctx.csv(...)`.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `description` | `string` | No | Human-readable description for runtime logs and inspection. |
| `columns` | `CsvRenameMap` | No | Canonical field-to-header aliases, e.g. `{ domain: ['domain', 'Company Domain'] }`. |
| `rename` | `CsvRenameMap` | No | Header rename map; use `columns` for new code. |
| `required` | `readonly string[]` | No | Canonical fields that must be present after header normalization. |

### `ctx.dataset(key, items)`

Create a persisted row dataset/table from input rows.`ctx.dataset` is Deepline’s row-work primitive. It records row identity,
progress, retries, table output, and idempotency for a collection of rows.
Use `.withColumn(name, resolver)` on the returned builder to define output
columns, then `.run(...)` to execute the row program.The `key` identifies the logical dataset/table. Renaming it is a persistence
migration: existing rows may no longer be reused. Row identity is derived
automatically from input row content unless `.run({ key: ... })` overrides
it with stable business fields such as `domain`, `email`, or `linkedin_url`.By default, `ctx.dataset` is row-preserving: one input row produces one output
row, with original fields merged with the columns produced by
`.withColumn(...)`. If one input entity must become many output rows, use the
documented expand/flatten recipe instead of assuming `ctx.dataset` changes
row cardinality.Signature: `dataset<TSource extends PlayDatasetInput<object>>( key: string, items: TSource, ): DatasetBuilder< PlayDatasetRow<TSource> & object, PlayDatasetRow<TSource> & object >;`

#### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `key` | `string` | Yes | Dataset/table name. |
| `items` | `TSource` | Yes | Input rows. |

#### Returns

`DatasetBuilder< PlayDatasetRow<TSource> & object, PlayDatasetRow<TSource> & object >`

### `.dataset(...).withColumn(name, resolver).run(options)`

Define one output column for every row in this dataset.The `name` becomes a field on each output row. For example,
`.withColumn('contact', ...)` creates `row.contact` in later column resolvers; it does
not spread returned object fields such as `contact.email` into `row.email`.
Add a later column resolver when you want a top-level export field:
`.withColumn('email', row => row.contact?.email ?? null)`.

```
withColumn<Name extends string, Value>(
    name: Name,
    resolver: ColumnResolver<OutputRow, Value>,
  ): DatasetBuilder<InputRow, OutputRow & Record<Name, Value>>;

withColumn<Name extends string, Value>(
    name: Name,
    definition: DatasetColumnDefinition<OutputRow, Value> & {
      readonly runIf: (
        row: OutputRow,
        index: number,
      ) => boolean | Promise<boolean>;
    },
  ): DatasetBuilder<InputRow, OutputRow & Record<Name, Value | null>>;

withColumn<Name extends string, Value>(
    name: Name,
    definition: DatasetColumnDefinition<OutputRow, Value>,
  ): DatasetBuilder<InputRow, OutputRow & Record<Name, Value>>;

withColumn<Name extends string, Value>(
    name: Name,
    resolver:
      | StepResolver<OutputRow, Value>
      | StepProgramResolver<OutputRow, Value>,
    options: StepOptions<OutputRow, Value>,
  ): DatasetBuilder<InputRow, OutputRow & Record<Name, Value | null>>;

run(options?: {
    description?: string;
    key?:
      | (keyof InputRow & string)
      | readonly (keyof InputRow & string)[]
      | ((
          row: InputRow,
          index: number,
        ) => string | number | readonly unknown[]);
  }): Promise<PlayDataset<OutputRow>>;
```

#### Column Overload 1 Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | `Name` | Yes | Output column name. |
| `resolver` | `ColumnResolver<OutputRow, Value>` | Yes | Computes the value for one row. |

#### Column Overload 1 Returns

`DatasetBuilder<InputRow, OutputRow & Record<Name, Value>>`

#### Column Overload 2 Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | `Name` | Yes | Output column name. |
| `definition` | `DatasetColumnDefinition<OutputRow, Value> & { readonly runIf: ( row: OutputRow, index: number, ) => boolean | Promise<boolean>; }` | Yes | Object-column definition with required `runIf`. |

#### Column Overload 2 Returns

`DatasetBuilder<InputRow, OutputRow & Record<Name, Value | null>>`

#### Column Overload 3 Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | `Name` | Yes | Output column name. |
| `definition` | `DatasetColumnDefinition<OutputRow, Value>` | Yes | Object-column definition. |

#### Column Overload 3 Returns

`DatasetBuilder<InputRow, OutputRow & Record<Name, Value>>`

#### Column Overload 4 Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | `Name` | Yes | Output column name. |
| `resolver` | `| StepResolver<OutputRow, Value> | StepProgramResolver<OutputRow, Value>` | Yes | Computes the value for one row. |
| `options` | `StepOptions<OutputRow, Value>` | Yes | Row gate and freshness options. |

#### Column Overload 4 Returns

`DatasetBuilder<InputRow, OutputRow & Record<Name, Value | null>>`

#### Run Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `options` | `{ description?: string; key?: | (keyof InputRow & string) | readonly (keyof InputRow & string)[] | (( row: InputRow, index: number, ) => string | number | readonly unknown[]); }` | No | Run options. |

#### Run Returns

`Promise<PlayDataset<OutputRow>>`Execute the row-column program and return a durable dataset handle.The returned [PlayDataset](https://deepline.com/docs/sdk-v2/sdk-reference#playdataset) preserves one output row per input row,
with original fields merged with the columns produced by `.withColumn(...)`.

### `StaleAfterSeconds`

Freshness policy for dataset cells and step-program columns.Use a positive whole number of seconds for a fixed TTL. Use a function when
the next expiry depends on the value that was just produced. The function
receives the completed cell value; return a positive whole number of seconds
to set the next expiry, or `null` to keep that value indefinitely.Result-based policies are evaluated only for dataset/step-program cells. The
scalar `ctx.step`, `ctx.fetch`, `ctx.runPlay`, and `ctx.tools.execute` APIs
accept numeric TTLs.Signature: `export type StaleAfterSeconds<Value = unknown> = | number | ((value: Value) => number | null);`

### `DatasetColumnRunInput`

Input object passed to an object-column `run` resolver.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `row` | `Row` | Yes | Current row, including previously computed columns. |
| `ctx` | `DeeplinePlayRuntimeContext` | Yes | Runtime context for tool/play/fetch/log calls. |
| `index` | `number` | Yes | Zero-based row index for this dataset run. |
| `previousCell` | `PreviousCell<Value>` | No | The prior stored value for this exact row+column when the runtime has<br>decided the cell is due to run again. `previousCell.value` is the same type<br>this column returns; metadata such as `completedAt` and `staleAt` lives<br>beside it and is not mixed into the value. |

### `DatasetColumnDefinition`

Object-column form for `.withColumn(...)`.Use this when a column needs `runIf`, typed `previousCell`, or a
result-based `staleAfterSeconds(value)` policy.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `run` | `(input: DatasetColumnRunInput<Row, Value>) => Value | Promise<Value>` | Yes | Compute one cell value. Receives the previous stored value when rerunning. |
| `runIf` | `(row: Row, index: number) => boolean | Promise<boolean>` | No | Optional row-level gate. Skipped rows produce `null` for this column. |
| `recompute` | `boolean` | No | Recompute this cell on each run instead of reusing its durable value. |
| `recomputeOnError` | `boolean` | No | Recompute this cell when its durable value is an error-shaped object. |
| `staleAfterSeconds` | `StaleAfterSeconds<Value>` | No | Fixed or value-dependent freshness policy for this cell. |

### `StepOptions`

Options for row-level `.withColumn(...)` and `steps().step(...)` entries.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `runIf` | `(row: Row, index: number) => boolean | Promise<boolean>` | No | Optional row-level gate. Skipped rows produce `null` for this column. |
| `recompute` | `boolean` | No | Recompute this cell on each run instead of reusing its durable value. |
| `recomputeOnError` | `boolean` | No | Recompute this cell when its durable value is an error-shaped object. |
| `staleAfterSeconds` | `StaleAfterSeconds<Value>` | No | Fixed or value-dependent freshness policy for this cell. |

### `PreviousCell`

Previous durable cell value passed to object-column resolvers.The runtime supplies this when a row+column is being recomputed after a
previous value existed. `value` has the same type that the column returns;
freshness metadata lives beside it.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `value` | `Value` | Yes | Previous completed value for this row+column. |
| `completedAt` | `number` | No | Millisecond timestamp when the previous value completed. |
| `staleAt` | `number | null` | No | Millisecond timestamp when the previous value becomes stale; `null` means no expiry. |
| `staleAfterSeconds` | `number` | No | Resolved numeric TTL in seconds for the previous value, when present. |

### `ctx.step(id, fn)`

Create one scalar checkpoint for the whole play run.Use `ctx.step` when a value is nondeterministic, expensive, external, or
useful to inspect as a named boundary. The first execution stores the
JSON-serializable output under `id`; replay and retries return the stored
value instead of running `run` again.Plain deterministic assignment does not need `ctx.step`. Use
`ctx.dataset(...).withColumn(...)`, not `ctx.step`, when the value should become a
field on each exported row.Signature: `step<T>( id: string, run: () => T | Promise<T>, options?: { staleAfterSeconds?: number }, ): Promise<T>;`

#### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | `string` | Yes | Checkpoint id. |
| `run` | `() => T | Promise<T>` | Yes | Computes the value once. |
| `options` | `{ staleAfterSeconds?: number }` | No | Checkpoint options. |

#### Returns

`Promise<T>`

### `ctx.runPlay(key, playRef, input, options)`

Invoke another registered or file-backed play as a child workflow.Use this for real composition boundaries, especially when a fitting
scalar prebuilt play already encodes provider order, fallbacks,
normalization, and no-result behavior. Do not invoke plays through
`ctx.tools.execute`; tools and plays are separate namespaces.`key` is the stable child-call identity for idempotency and traceability.Signature: `runPlay<TOutput = unknown>( key: string, playRef: string | PlayReferenceLike, input: Record<string, unknown>, options: { description: string; staleAfterSeconds?: number }, ): Promise<TOutput>;`

#### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `key` | `string` | Yes | Stable child-call key. |
| `playRef` | `string | PlayReferenceLike` | Yes | Registered play name, play handle, or file-backed play reference. |
| `input` | `Record<string, unknown>` | Yes | Input object passed to the child play. |
| `options` | `{ description: string; staleAfterSeconds?: number }` | Yes | Child play options. |

#### Returns

`Promise<TOutput>`

### `ctx.tools.execute(request)`

Execute a single tool with a keyword-style request object.Signature: `execute<TOutput = LoosePlayObject>( request: ToolExecutionRequest, ): Promise<ToolExecuteResult<TOutput>>;`

#### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `request` | `ToolExecutionRequest` | Yes | Tool call request. |

#### Returns

`Promise<ToolExecuteResult<TOutput>>`

### `ToolExecutionRequest`

Keyword-style request object for `ctx.tools.execute(...)`.The `tool` value comes from live tool discovery. The `id` is the stable
logical call name inside this play and participates in replay/idempotency.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | `string` | Yes | Stable logical id for this tool call within the play. |
| `tool` | `string` | Yes | Current tool id from `deepline tools search` / `deepline tools describe`. |
| `input` | `Record<string, unknown>` | Yes | JSON-serializable provider/tool input object. |
| `description` | `string` | No | Human-readable description for logs and run inspection. |
| `force` | `boolean` | No | Recompute this tool call instead of reusing a durable receipt/checkpoint. |
| `staleAfterSeconds` | `number` | No | Numeric TTL in seconds for this tool checkpoint. |

### `ctx.fetch(key, url, init)`

Durable HTTP fetch.Use this for non-provider HTTP calls that must replay safely. The response
is recorded under `key` so workflow replay sees the same value. Prefer
`ctx.tools.execute(...)` for Deepline-managed provider APIs because tools
handle auth, retries, rate limits, extraction metadata, and spend tracking.Signature: `fetch( key: string, url: string | URL, init?: SecretAwareRequestInit, options?: { staleAfterSeconds?: number }, ): Promise<{ ok: boolean; status: number; statusText: string; url: string; headers: Record<string, string>; bodyText: string; json: unknown | null; }>;`

#### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `key` | `string` | Yes | Checkpoint id. |
| `url` | `string | URL` | Yes | URL to fetch. |
| `init` | `SecretAwareRequestInit` | No | Fetch options. |
| `options` | `{ staleAfterSeconds?: number }` | No |  |

#### Returns

`Promise<{ ok: boolean; status: number; statusText: string; url: string; headers: Record<string, string>; bodyText: string; json: unknown | null; }>`

### `ctx.runSteps(program, input, options)`

Run a reusable step program against one scalar input object.`steps().step(...)` is a composable mini-pipeline. Use `ctx.runSteps(...)`
when that mini-pipeline should execute outside a row dataset. Inside a
`ctx.dataset` column resolver, pass the step program directly to
`.withColumn(name, program)` instead.Signature: `runSteps<TInput extends Record<string, unknown>, TOutput>( program: RunnableStepProgram<TInput, TOutput>, input: TInput, options?: { description?: string }, ): Promise<TOutput>;`

#### Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `program` | `RunnableStepProgram<TInput, TOutput>` | Yes | Step program. |
| `input` | `TInput` | Yes | Program input. |
| `options` | `{ description?: string }` | No | Run options. |

#### Returns

`Promise<TOutput>`

### `PlayDataset`

Durable handle for rows produced by `ctx.csv(...)` or `ctx.dataset(...).run()`.A `PlayDataset` is not a normal in-memory array. It points at runtime-managed
rows, usually backed by persisted sheet storage, and carries metadata such as
dataset kind, dataset id, table namespace, count, and preview rows.Pass dataset handles directly into later `ctx.dataset(...)` stages by default so
Deepline keeps row progress, retries, memory use, and table output under
runtime control. Use `count()` and `peek()` for bounded inspection. Use
`materialize(limit)` or async iteration only when the dataset is intentionally
small and bounded. `PlayDataset` intentionally does not expose `.rows`,
`.toArray()`, or other array aliases; those hide the runtime cost of loading
persisted rows into memory.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `datasetKind` | `PlayDatasetKind` | Yes | Dataset kind. |
| `datasetId` | `string` | Yes | Dataset id. |
| `backing` | `PlayDatasetBacking` | No | Backing store info. |
| `sourceLabel` | `string | null` | No | Display label. |
| `tableNamespace` | `string | null` | No | Runtime table name. |

### `ToolExecuteResult`

Canonical result returned by Deepline tool execution.The top-level object is Deepline-owned execution metadata and semantic
extraction state. Raw tool/provider data lives under `toolResponse.raw`;
response metadata lives under `toolResponse.meta`. Semantic single-value
getters live under `extractedValues.<name>.get()`, and list getters live
under `extractedLists.<name>.get()`.Use extractors first when a tool contract exposes them. Drop to
`toolResponse.raw` when you need provider-specific fields or when debugging
from persisted run rows.Signature: `export type ToolExecuteResult< TResult = unknown, TMeta = Record<string, unknown>, TExtracted extends Record<string, unknown> = Partial<DeeplineGetterValueMap>, TLists extends Record<string, Record<string, unknown>> = Record< string, Record<string, unknown> >, > = ToolExecuteResultBase<TResult, TMeta> & ToolExecuteResultAccessors<TExtracted, TLists>;`

## Tool And Provider Calls

### `DeeplineContext.tools`

Tool/provider operations available from a connected `DeeplineContext`.This namespace is for regular SDK callers outside a play runtime. Inside a
`definePlay(...)` body, use `ctx.tools.execute({ id, tool, input, ... })`
so provider calls become durable runtime checkpoints.Signature: `export type DeeplineToolsNamespace = { list(): Promise<ToolDefinition[]>; get(toolId: string): Promise<ToolMetadata>; execute( toolId: string, input: Record<string, unknown>, ): Promise<ToolExecuteResult>; };`

## Remote Plays And Runs

### `DeeplineContext.plays`

Named-play discovery and handle operations from a connected `DeeplineContext`.Signature: `export type DeeplinePlaysNamespace = { list(): Promise<PlayListItem[]>; get<TInput = Record<string, unknown>, TOutput = unknown>( name: string, ): DeeplineNamedPlay<TInput, TOutput>; };`

### `DeeplineNamedPlay`

Handle to a named play for remote lifecycle operations.Returned by `DeeplineContext.play` and attached to `DefinedPlay`.
Provides methods to run, inspect, list runs, and publish a play by name.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | `string` | Yes | The play’s name. |

### `PlayJob`

Handle to a running play execution.Provides methods to check status, stream logs, wait for completion,
or cancel the execution.This handle is the SDK-context equivalent of `deepline plays run --watch` and
`POST /api/v2/plays/run`: every surface returns a run id first, then exposes
the completed user output through `PlayJob.get()` or the status endpoint’s
`result` field. Runtime logs are available from `status().progress.logs` and
are intentionally separate from the returned output object.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | `string` | Yes | Temporal workflow ID for this execution. |

## Low-Level Client

### `DeeplineClient`

Low-level client for the Deepline REST API.Provides typed methods for every API endpoint: tools, plays, auth, and health.
Handles authentication, retries, and localhost failover automatically.Signature: `class DeeplineClient`

#### Members

| Member | Kind | Purpose | Parameters | Returns / type |
| --- | --- | --- | --- | --- |
| `constructor` | constructor | Create a low-level SDK client.<br>Most callers can omit options and let the SDK resolve auth/config from<br>environment variables and CLI-managed credentials. | `options?: DeeplineClientOptions` \- Optional overrides for API key, base URL, timeout, and retries. |  |
| `runs` | property | Canonical run lifecycle namespace backed by `/api/v2/runs`. |  | `RunsNamespace` |
| `billing` | property | Billing namespace: subscription status/cancel and invoice history. |  | `BillingNamespace` |
| `baseUrl` | getter | The resolved base URL this client is targeting (e.g. `"http://localhost:3000"`). |  | `string` |
| `listSecrets` | method | List secret metadata visible to the current workspace. |  | `Promise<PlaySecretMetadata[]>` |
| `checkSecret` | method | Check whether a named secret exists, is active, and has a stored value. | `name: string` \- Secret name. It is normalized to uppercase before lookup. | `Promise<PlaySecretMetadata | null>` |
| `listTools` | method | List all available tools.<br>Returns tool definitions including ID, provider, description, input/output schemas,<br>and list extractor paths for automatic CSV conversion. | `options?: { categories?: string; grep?: string; grepMode?: 'all' | 'any' | 'phrase'; compact?: boolean; }` | `Promise<ToolDefinition[]>` |
| `searchTools` | method | Search available tools using Deepline’s ranked backend search.<br>This is the same discovery surface used by the CLI: it ranks across<br>tool metadata, categories, agent guidance, and input schema fields. | `options?: ToolSearchOptions` | `Promise<ToolSearchResult>` |
| `getTool` | method | Get detailed metadata for a single tool.<br>Returns everything from `ToolDefinition` plus pricing info, sample<br>inputs/outputs, failure modes, and cost estimates. | `toolId: string` \- Tool identifier (e.g. `"dropleads_search_people"`) | `Promise<ToolMetadata>` |
| `executeTool` | method | Execute a tool and return the standard execution envelope.<br>The `toolResponse.raw` field contains the raw tool response.<br>`toolResponse.meta` contains tool/provider metadata.<br>Top-level fields such as `status`, `job_id`, and `billing` describe the<br>Deepline execution envelope. | `toolId: string`<br>`input: Record<string, unknown>`<br>`options?: ExecuteToolRawOptions` | `Promise<ToolExecution<TData, TMeta>>` |
| `executeToolRaw` | method | Back-compatible alias for `executeTool`.<br>Retained for callers that still use the older raw naming while the response<br>envelope remains the same. | `toolId: string`<br>`input: Record<string, unknown>`<br>`options?: ExecuteToolRawOptions` | `Promise<ToolExecution<TData, TMeta>>` |
| `queryCustomerDb` | method | Run a bounded SQL query against the customer data plane.<br>Use this from trusted backend or agent contexts only. The API enforces<br>workspace scoping and row limits. | `input: { sql: string; maxRows?: number; }` | `Promise<CustomerDbQueryResult>` |
| `startPlayRun` | method | Start a play run.<br>Internal/advanced primitive. For normal callers, prefer the public<br>entrypoints: the CLI, `Deepline.connect`, `submitPlay`,<br>or `runPlay`.<br>Supported invocation surfaces intentionally share this same run contract:<br>`deepline plays run`, repo scripts such as `bun run deepline -- plays run`,<br>SDK context calls like `Deepline.connect().play(name).run()`, and direct<br>`POST /api/v2/plays/run` calls all return a workflow/run id. The completed<br>output is always retrievable from `getPlayStatus(runId).result` (or from<br>`PlayJob.get()` for SDK context calls). Execution logs live under<br>`progress.logs`; they are not part of the user output object. | `request: StartPlayRunRequest` \- Play run configuration (name, code, input, etc.) | `Promise<PlayRunStart>` |
| `startPlayRunStream` | method | Start a play run and stream live runtime events from the same request.<br>Use this when a caller wants low-level event handling instead of submitting<br>first and then connecting to `streamPlayRunEvents(runId)`. | `request: StartPlayRunRequest` \- Play run configuration.<br>`options?: { signal?: AbortSignal }` \- Optional streaming options. | `AsyncGenerator<PlayLiveEvent>` |
| `registerPlayArtifact` | method | Register a bundled play artifact.<br>Internal/advanced primitive used by packaging flows. Public callers should<br>prefer the CLI, `submitPlay`, or `runPlay`. | `input: { name: string; sourceCode: string; sourceFiles?: Record<string, string>; artifact: Record<string, unknown>; compilerManifest?: PlayCompilerManifest; publish?: boolean; ownerType?: 'org' | 'deepline'; scope?: 'org' | 'system'; userId?: string; }` | `Promise<{ success?: boolean; name?: string; artifactStorageKey: string; artifactMetadata?: Record<string, unknown> | null; staticPipeline?: unknown; definitionId?: string | null; revisionId?: string | null; version?: number | null; liveVersion?: number | null; triggerMetadata?: unknown; triggerBindings?: unknown; }>` |
| `registerPlayArtifacts` | method | Register multiple bundled play artifacts in one request.<br>Used by packaging and prebuilt publication flows. Each artifact is compiled<br>first when a compiler manifest is not already supplied. | `artifacts: Array<{ name: string; sourceCode: string; sourceFiles?: Record<string, string>; artifact: Record<string, unknown>; compilerManifest?: PlayCompilerManifest; publish?: boolean; ownerType?: 'org' | 'deepline'; scope?: 'org' | 'system'; userId?: string; }>` | `Promise<{ success: boolean; artifacts: Array<{ success?: boolean; name?: string; artifactStorageKey: string; artifactMetadata?: Record<string, unknown> | null; staticPipeline?: unknown; definitionId?: string | null; revisionId?: string | null; version?: number | null; liveVersion?: number | null; triggerMetadata?: unknown; triggerBindings?: unknown; }>; }>` |
| `compilePlayManifest` | method | Compile a bundled play artifact into the server-side compiler manifest.<br>The manifest records imports, trigger bindings, static pipeline shape, and<br>runtime metadata needed before a play artifact can be checked, registered,<br>or run. | `input: { name: string; sourceCode: string; sourceFiles?: Record<string, string>; artifact: Record<string, unknown>; importedPlayDependencies?: PlayCompilerManifest[]; }` | `Promise<PlayCompilerManifest>` |
| `checkPlayArtifact` | method | Check a bundled play artifact against the server’s current play compiler.<br>Unlike `registerPlayArtifact`, this does not store the artifact,<br>publish a revision, or start a run. It is the authoritative cloud validation<br>path used by `deepline plays check`. | `input: { name?: string; sourceCode: string; sourceFiles?: Record<string, string>; artifact: Record<string, unknown>; }` | `Promise<PlayCheckResult>` |
| `compileEnrichPlan` | method | Compile legacy enrich command arguments into a runtime plan.<br>This is primarily used by CLI compatibility paths that translate older<br>enrichment commands onto the play runtime. | `input: { plan_args?: string[]; config?: unknown; native_play_materialization?: 'macro' | 'inline_prebuilt'; }` | `Promise<{ config: EnrichCompiledConfig }>` |
| `startPlayRunFromBundle` | method | Register an already-bundled play artifact and start a run from it.<br>This is the low-level file-backed run path used by SDK/CLI packaging<br>wrappers after local bundling has produced the runtime artifact. | `input: { name: string; sourceCode: string; sourceFiles?: Record<string, string>; artifact: Record<string, unknown>; compilerManifest?: PlayCompilerManifest; input?: Record<string, unknown>; inputFile?: PlayStagedFileRef | null; packagedFiles?: PlayStagedFileRef[]; force?: boolean; }` | `Promise<PlayRunStart>` |
| `submitPlay` | method | Register a bundled play artifact and start a run from the live revision.<br>Convenience wrapper around `registerPlayArtifact` plus<br>`startPlayRun`. This is the canonical file-backed path used by wrappers.<br>The returned id can be passed to `getPlayStatus` to retrieve the same<br>durable `{ result }` object that the CLI prints after `--watch` completes. | `code: string` \- Source string fallback; the bundled artifact should be passed in `options.artifact`<br>`csvPath: string | null` \- Path to input CSV file, or `null`<br>`name?: string` \- Play name (extracted from source if omitted)<br>`options?: { sourceCode?: string; sourceFiles?: Record<string, string>; artifact?: Record<string, unknown>; compilerManifest?: PlayCompilerManifest; input?: Record<string, unknown>; inputFile?: PlayStagedFileRef | null; packagedFiles?: PlayStagedFileRef[]; force?: boolean; }` \- Additional submission options | `Promise<PlayRunStart>` |
| `stagePlayFiles` | method | Upload files to the staging area for use in play runs.<br>Internal/advanced primitive used by packaging flows. Public callers should<br>prefer the CLI, `submitPlay`, or `runPlay`.<br>Staged files are referenced by their returned `PlayStagedFileRef`<br>in subsequent `startPlayRun` calls via `inputFile` or `packagedFiles`. | `files: Array<{ logicalPath: string; contentBase64: string; contentHash: string; contentType: string; bytes: number; }>` \- Array of files to stage (base64-encoded content) | `Promise<PlayStagedFileRef[]>` |
| `resolveStagedPlayFiles` | method | Resolve staged play files by content hash without uploading bytes.<br>Missing files are returned so callers can upload only the files the server<br>does not already have. | `files: Array<{ logicalPath: string; contentHash: string; contentType: string; bytes: number; }>` | `Promise<{ files: PlayStagedFileRef[]; missing: Array<{ logicalPath: string; contentHash: string }>; }>` |
| `getPlayStatus` | method | Get the current status of a play execution.<br>Internal/advanced primitive. Public callers should usually prefer<br>`runPlay`, `PlayJob.get`, or `deepline plays run --watch`. | `workflowId: string` \- Play-run id from `startPlayRun`<br>`options?: { billing?: boolean; full?: boolean }` | `Promise<PlayStatus>` |
| `streamPlayRunEvents` | method | Stream semantic play-run events using the same SSE feed as the dashboard.<br>The server emits a canonical `play.run.snapshot` event first for every<br>connection, then incremental live events until terminal state or reconnect. | `workflowId: string`<br>`options?: { signal?: AbortSignal; lastEventId?: string; mode?: 'cli' | 'ui'; }` | `AsyncGenerator<PlayLiveEvent>` |
| `cancelPlay` | method | Cancel a running play execution.<br>Sends a stop request for the run. | `workflowId: string` \- Public Deepline play-run id to cancel | `Promise<void>` |
| `stopPlay` | method | Stop a running play execution, including open HITL waits. | `workflowId: string` \- Public Deepline play-run id to stop<br>`options?: { reason?: string }` | `Promise<StopPlayRunResult>` |
| `listPlayRuns` | method | List recent runs for a named play.<br>Returns runs sorted by start time (newest first), including workflow IDs,<br>status, timestamps, and metadata. | `playName: string` \- The play name to query | `Promise<PlayRunListItem[]>` |
| `listWorkflows` | method | List the org’s workflows. `GET /api/v2/workflows`. | `options?: { limit?: number }` | `Promise<{ workflows: Array<{ id: string; name: string; status: string; current_published_version: number | null; }>; }>` |
| `getWorkflow` | method | Fetch a single workflow (including its published-revision config — the<br>input to `compileWorkflowConfigToPlay`). `GET /api/v2/workflows/:id`. | `id: string` | `Promise<{ workflow: { id: string; name: string; status: string; current_published_version: number | null; current_published_revision: { version: number; config: unknown; } | null; } | null; validation?: unknown; }>` |
| `deleteWorkflow` | method | Delete a workflow. `DELETE /api/v2/workflows/:id`. | `id: string` | `Promise<unknown>` |
| `disableWorkflow` | method | Turn a workflow off. `POST /api/v2/workflows/:id/disable`. | `id: string` | `Promise<unknown>` |
| `enableWorkflow` | method | Turn a workflow back on. `POST /api/v2/workflows/:id/enable`. | `id: string` | `Promise<unknown>` |
| `applyWorkflow` | method | Create/update a workflow from config. `POST /api/v2/workflows/apply`. | `body: Record<string, unknown>` | `Promise<unknown>` |
| `lintWorkflow` | method | Validate a workflow config without saving. `POST /api/v2/workflows/lint`. | `body: Record<string, unknown>` | `Promise<unknown>` |
| `getWorkflowSchema` | method | Fetch live workflow request schemas. `GET /api/v2/workflows/schema`. | `subject?: string` | `Promise<unknown>` |
| `callWorkflow` | method | Queue a workflow run. `POST /api/v2/workflows/call`. | `body: Record<string, unknown>` | `Promise<unknown>` |
| `listWorkflowRuns` | method | List a workflow’s runs. `GET /api/v2/workflows/:id/runs`. | `id: string`<br>`options?: { limit?: number }` | `Promise<unknown>` |
| `getWorkflowRun` | method | Fetch one workflow run. `GET /api/v2/workflows/:id/runs/:runId`. | `id: string`<br>`runId: string` | `Promise<unknown>` |
| `cancelWorkflowRun` | method | Cancel a workflow run. `POST /api/v2/workflows/:id/runs/:runId/cancel`. | `id: string`<br>`runId: string` | `Promise<unknown>` |
| `getRunStatus` | method | Get a run by id using the public runs resource model.<br>This is the SDK equivalent of:<br>`bash<br />deepline runs get <run-id> --json<br />` | `runId: string`<br>`options?: { full?: boolean }` | `Promise<PlayStatus>` |
| `listRuns` | method | List play runs using the public runs resource model.<br>This is the SDK equivalent of:<br>`bash<br />deepline runs list --play <play-name> --status failed --json<br />` | `options: RunsListOptions` | `Promise<PlayRunListItem[]>` |
| `observeRunEvents` | method | Observe one run’s live events. Uses the Convex Run Snapshot subscription<br>transport first (ADR-0008), then falls back to the canonical SSE stream<br>when the subscription transport or its optional client modules are not<br>available. Pass `fallback: 'none'` to receive<br>`RunObserveTransportUnavailableError` instead. | `runId: string`<br>`options?: { signal?: AbortSignal; onNotice?: (message: string) => void; fallback?: 'sse' | 'none'; }` | `AsyncGenerator<PlayLiveEvent>` |
| `tailRun` | method | Read the canonical run stream until a terminal run status is observed.<br>Tries the Convex Run Snapshot subscription transport first (ADR-0008);<br>when the server cannot serve it (grant endpoint missing/unconfigured or<br>Convex unreachable) it falls back — with one `onNotice` message — to the<br>support-window SSE stream below.<br>Server stream windows are finite: they end cleanly at the function<br>ceiling even while the run keeps executing. A window that ends (cleanly<br>or via transient network error) without a terminal event triggers one<br>durable-status re-check followed by a backed-off reconnect, so long runs<br>tail to completion. Abort via `options.signal` to stop waiting. | `runId: string`<br>`options?: RunsTailOptions` | `Promise<PlayStatus>` |
| `getRunLogs` | method | Fetch persisted logs for a run using the public runs resource model.<br>This is the SDK equivalent of:<br>`bash<br />deepline runs logs <run-id> --limit 200 --json<br />` | `runId: string`<br>`options?: RunsLogsOptions` | `Promise<RunsLogsResult>` |
| `getPlaySheetRows` | method | Export persisted runtime-sheet rows for a play dataset/table namespace.<br>This is the SDK form of exporting `ctx.dataset(...).run()` output for a<br>specific play and optional run id. | `input: { playName: string; tableNamespace: string; runId?: string; limit?: number; offset?: number; }` | `Promise<PlaySheetRowsResult>` |
| `stopRun` | method | Stop a run by id using the public runs resource model.<br>This is the SDK equivalent of:<br>`bash<br />deepline runs stop <run-id> --reason "stale lock" --json<br />` | `runId: string`<br>`options?: { reason?: string }` | `Promise<StopPlayRunResult>` |
| `listPlays` | method | List callable plays visible to the workspace.<br>Pass `origin: "prebuilt"` for Deepline-managed prebuilts or<br>`origin: "owned"` for org-owned plays. | `options?: { origin?: 'prebuilt' | 'owned'; grep?: string; grepMode?: 'all' | 'any' | 'phrase'; }` | `Promise<PlayListItem[]>` |
| `searchPlays` | method | Search callable plays and return compact play descriptions.<br>Prebuilt plays are preferred by default because they have maintained<br>contracts and stable run behavior. | `options: { query: string; compact?: boolean; scope?: 'prebuilt' | 'owned' | 'all'; }` | `Promise<PlayDescription[]>` |
| `getPlay` | method | Get the full definition and state of a named play.<br>Returns the play’s revision state (draft, live), recent runs,<br>sheet processing summary, and database URL. | `name: string` \- Play name | `Promise<PlayDetail>` |
| `describePlay` | method | Get a normalized play description suitable for agents and CLIs.<br>The description includes runnable examples, input/output summaries, clone<br>guidance, revision state, and latest run metadata when available. | `name: string`<br>`options?: { compact?: boolean }` | `Promise<PlayDescription>` |
| `clearPlayHistory` | method | Clear run history and durable sheet/result data for a play without deleting<br>the play definition or revisions. | `name: string`<br>`request?: ClearPlayHistoryRequest` | `Promise<ClearPlayHistoryResult>` |
| `listPlayVersions` | method | List saved versions for a named play.<br>Returns immutable revision snapshots newest-first, including the revision<br>id needed for exact-version runs and live-version switching. | `name: string` \- Play name<br>`options?: { full?: boolean }` | `Promise<PlayRevisionSummary[]>` |
| `publishPlayVersion` | method | Make a play revision live.<br>When `revisionId` is omitted, the current working revision becomes live.<br>The live version is what executes when the play is run by name without<br>specifying an explicit revision. | `name: string` \- Play name<br>`request?: PublishPlayVersionRequest` \- Optional explicit revision to make live | `Promise<PublishPlayVersionResult>` |
| `deletePlay` | method | Delete an org-owned play definition, including its revisions, trigger<br>bindings, and local run records. Deepline prebuilt plays are read-only. | `name: string` | `Promise<DeletePlayResult>` |
| `getSharePage` | method | Current share status for a play: the public page (if any), the published<br>copy, and the revision picker. Read-only. | `name: string` | `Promise<SharePageStatus>` |
| `publishSharePage` | method | Publish (or repoint) the play’s public share page to a revision. Requires<br>`acknowledgedUnlisted: true` — the page is publicly viewable. Org-admin only. | `name: string`<br>`request: PublishSharePageRequest` | `Promise<SharePageStatus>` |
| `updateSharePage` | method | Update share-page settings (SEO indexing, credit-cost / latency display)<br>without moving the published pointer. Org-admin only. | `name: string`<br>`request: UpdateSharePageRequest` | `Promise<SharePageStatus>` |
| `unpublishSharePage` | method | Unshare: hard-delete the play’s public page and its cards. Returns the<br>fresh status (now `share: null`). Org-admin only. Idempotent — a no-op when<br>the play was never published. | `name: string` | `Promise<SharePageStatus>` |
| `regenerateSharePage` | method | Regenerate the LLM landing-page copy for a revision (defaults to the<br>published one). Org-admin only. | `name: string`<br>`request?: { revisionId?: string }` | `Promise<SharePageStatus>` |
| `runPlay` | method | Run a play end-to-end: submit, stream until terminal, return result.<br>This is the highest-level play execution method. It submits the play,<br>reads the canonical run stream for status updates, and returns a structured<br>result with logs and timing. Supports cancellation via `AbortSignal`. | `code: string` \- Source string fallback; pass the bundled artifact in `options.artifact`<br>`csvPath: string | null` \- Input CSV path, or `null`<br>`name?: string` \- Play name<br>`options?: { onProgress?: (status: PlayStatus) => void; signal?: AbortSignal; input?: Record<string, unknown>; sourceCode?: string; artifact?: Record<string, unknown>; compilerManifest?: PlayCompilerManifest; inputFile?: PlayStagedFileRef | null; packagedFiles?: PlayStagedFileRef[]; force?: boolean; }` \- Execution options | `Promise<PlayRunResult>` |
| `getBillingPlans` | method | Published plans plus the caller’s active plan: prices, monthly grant<br>credits, rollover policy, and which plans are open for subscription.<br>Prefer `client.billing.plans()`. |  | `Promise<BillingPlansResult>` |
| `getBillingSubscriptionStatus` | method | Subscription state for the active workspace: active plan, whether a<br>Stripe subscription backs it, renewal/cancellation facts, and remaining<br>Deepline credit pools. Prefer `client.billing.subscription.status()`. |  | `Promise<BillingSubscriptionStatus>` |
| `cancelBillingSubscription` | method | Schedule subscription cancellation at period end, or reverse a pending<br>cancellation with `{ undo: true }`. The customer keeps the cycle they<br>paid for and every remaining credit — cancellation never claws back<br>credits. Prefer `client.billing.subscription.cancel(...)`. | `options?: { undo?: boolean; }` | `Promise<BillingSubscriptionCancelResult>` |
| `listBillingInvoices` | method | Customer-facing billing history: subscription invoices plus one-time<br>credit purchase receipts, newest first, with Stripe-hosted links.<br>Prefer `client.billing.invoices.list(...)`. | `options?: { limit?: number; }` | `Promise<BillingInvoicesResult>` |
| `health` | method | Check API connectivity and server health. |  | `Promise<{ status: string; version?: string }>` |

### `client.runs`

Public runs namespace exposed as `client.runs`.This namespace mirrors the canonical `/api/v2/runs` resource family and is
the preferred low-level surface for polling, streaming, stopping, reading
logs, and exporting durable dataset rows.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `get` | `(runId: string, options?: { full?: boolean }) => Promise<PlayStatus>` | Yes | Get current run status by public run id. |
| `list` | `(options: RunsListOptions) => Promise<PlayRunListItem[]>` | Yes | List runs for one play, optionally filtered by status. |
| `tail` | `(runId: string, options?: RunsTailOptions) => Promise<PlayStatus>` | Yes | Stream run events and return the latest/terminal run status. |
| `logs` | `(runId: string, options?: RunsLogsOptions) => Promise<RunsLogsResult>` | Yes | Fetch persisted log lines for a run. |
| `exportDatasetRows` | `(input: { playName: string; tableNamespace: string; runId?: string; limit?: number; offset?: number; }) => Promise<PlaySheetRowsResult>` | Yes | Export persisted rows for a runtime-sheet dataset/table namespace. |
| `stop` | `( runId: string, options?: { reason?: string }, ) => Promise<StopPlayRunResult>` | Yes | Stop a running/waiting run. |

### `client.billing`

Public billing namespace exposed as `client.billing`.Carries the durable Deepline billing product model — plans, subscription
state, period-end cancellation, and invoice/receipt history — so CLI
commands and programmatic callers share the same surface.

#### Fields

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `plans` | `() => Promise<BillingPlansResult>` | Yes | Published plans plus the plan you are on (“what plans exist and what am I on”). |
| `subscription` | `{ status: () => Promise<BillingSubscriptionStatus>; cancel: (options?: { undo?: boolean; }) => Promise<BillingSubscriptionCancelResult>; }` | Yes |  |
| `invoices` | `{ list: (options?: { limit?: number }) => Promise<BillingInvoicesResult>; }` | Yes |  |


---

# PART 4 — Provider Integrations & Endpoints

*Every integrated provider, its agent playbook/guidance, and all endpoints with execution code, required fields, and pricing. Providers are alphabetical.*


---

## Adyntel

*Provider folder: `adyntel` · 9 endpoints*


### Adyntel — Agent Guidance

## Playbook

Use Adyntel for paid-media intelligence (creative examples, channel presence, and ad strategy signals).

- Deepline injects required Adyntel account identity fields automatically.
- Normalize domains to bare host format (`company.com`, no protocol or `www`).
- Use channel-native tools (`adyntel_facebook`, `adyntel_google`, `adyntel_linkedin`, `adyntel_tiktok_search`) before cross-channel synthesis.
- Prefer `adyntel_google_shopping_sync` to launch and poll Google Shopping in one call.
- Keep `adyntel_google_shopping_status` for manual follow-up polling when you already have an `id`.
- Use `adyntel_tiktok_ad_details` only after collecting IDs from `adyntel_tiktok_search`; pass `id` as a string.
- Billing is request-based for paid endpoints, so pre-filter targets before broad sweeps.

```
deepline tools execute adyntel_google --payload '{"company_domain":"hubspot.com"}'
```

```
deepline tools execute adyntel_google_shopping_sync --payload '{"company_domain":"allbirds.com"}'
```


### Adyntel — Endpoints


#### `adyntel-domain-keywords`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=adyntel_domain_keywords:{"company_domain":"{{company_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_domain` | `string` | Yes |  | Company domain in normalized form (for example company.com). Do not include https:// or [www](http://www/). |

**Direct CLI:**

```
deepline tools execute adyntel_domain_keywords --payload '{
  "company_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


#### `adyntel-facebook`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=adyntel_facebook:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute adyntel_facebook --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


#### `adyntel-facebook-ad-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=adyntel_facebook_ad_search:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | Keyword to search in Meta ad library. |

**Direct CLI:**

```
deepline tools execute adyntel_facebook_ad_search --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


#### `adyntel-google`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=adyntel_google:{"company_domain":"{{company_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_domain` | `string` | Yes |  | Company domain in normalized form (for example company.com). Do not include https:// or [www](http://www/). |

**Direct CLI:**

```
deepline tools execute adyntel_google --payload '{
  "company_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


#### `adyntel-google-shopping-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=adyntel_google_shopping_status:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | ID returned by adyntel\_google\_shopping. |

**Direct CLI:**

```
deepline tools execute adyntel_google_shopping_status --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `adyntel-google-shopping-sync`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=adyntel_google_shopping_sync:{"company_domain":"{{company_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_domain` | `string` | Yes |  | Company domain in normalized form (for example company.com). Do not include https:// or [www](http://www/). |

**Direct CLI:**

```
deepline tools execute adyntel_google_shopping_sync --payload '{
  "company_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


#### `adyntel-linkedin`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=adyntel_linkedin:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute adyntel_linkedin --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


#### `adyntel-tiktok-ad-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=adyntel_tiktok_ad_details:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Ad ID returned by adyntel\_tiktok\_search. Pass as string. |

**Direct CLI:**

```
deepline tools execute adyntel_tiktok_ad_details --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


#### `adyntel-tiktok-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=adyntel_tiktok_search:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | Keyword to search on TikTok ads. |

**Direct CLI:**

```
deepline tools execute adyntel_tiktok_search --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


---

## AI Ark

*Provider folder: `ai_ark` · 11 endpoints*


### AI Ark — Endpoints


#### `ai-ark-company-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ai_ark_company_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute ai_ark_company_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `ai-ark-email-finder-results`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ai_ark_email_finder_results:{"trackId":"{{trackId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.trackId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute ai_ark_email_finder_results --payload '{
  "trackId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- The finder/dispatcher step itself is free. The underlying enrichment step is charged at its listed rate.
- Billing mode: `no_bill`.


#### `ai-ark-email-finder-statistics`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ai_ark_email_finder_statistics:{"trackId":"{{trackId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.trackId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute ai_ark_email_finder_statistics --payload '{
  "trackId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- The finder/dispatcher step itself is free. The underlying enrichment step is charged at its listed rate.
- Billing mode: `no_bill`.


#### `ai-ark-export-people`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ai_ark_export_people:{"webhook":"{{webhook}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.webhook` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute ai_ark_export_people --payload '{
  "webhook": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `ai-ark-export-results`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ai_ark_export_results:{"trackId":"{{trackId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.trackId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute ai_ark_export_results --payload '{
  "trackId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `ai-ark-export-statistics`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ai_ark_export_statistics:{"trackId":"{{trackId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.trackId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute ai_ark_export_statistics --payload '{
  "trackId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `ai-ark-find-emails`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ai_ark_find_emails:{"trackId":"{{trackId}}","webhook":"{{webhook}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.trackId` | `string` | Yes |  |  |
| `payload.webhook` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute ai_ark_find_emails --payload '{
  "trackId": "string",
  "webhook": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `ai-ark-mobile-phone-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ai_ark_mobile_phone_finder:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute ai_ark_mobile_phone_finder --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.7` per pricing unit.
- Billing mode: `post_deduct`.


#### `ai-ark-people-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ai_ark_people_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute ai_ark_people_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `ai-ark-personality-analysis`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ai_ark_personality_analysis:{"url":"{{url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.url` | `string` | Yes | `"https://www.linkedin.com/in/mahyar-mohsenpour"` | The URL for the linkedin profile. |

**Direct CLI:**

```
deepline tools execute ai_ark_personality_analysis --payload '{
  "url": "https://www.linkedin.com/in/mahyar-mohsenpour"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `ai-ark-reverse-lookup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ai_ark_reverse_lookup:{"search":"{{search}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.search` | `string` | Yes | `"bnorris@cnyric.org"` | The contact information to search for (email address, phone number, etc.) |

**Direct CLI:**

```
deepline tools execute ai_ark_reverse_lookup --payload '{
  "search": "bnorris@cnyric.org"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


---

## Allegrow

*Provider folder: `allegrow` · 6 endpoints*


### Allegrow — Agent Guidance

## Playbook

# Allegrow Workflow Guidance

Allegrow specialises in B2B catch-all domain resolution and primary-email identification. Use it as a final-layer validator after cheaper/faster verifiers (ZeroBounce, Findymail, etc.) have already run and returned `catch_all` or when you need to distinguish an executive’s primary address from their secondary ones.

## Status Interpretation

| `result.status` | Meaning | Action |
| --- | --- | --- |
| `safe` | Valid, safe to send | **Send** |
| `do_not_mail_abuse` | Valid address but high spam-report risk | Skip email; use LinkedIn/phone instead |
| `some_risk` | Inconclusive, risk factors identified | Hold 30 days; re-validate before next send |
| `block_bounce_risk` | Invalid; will hard-bounce | Remove from list |
| `dead_email` | Invalid account on a catch-all host (won’t bounce) | Use other channels; remove from active send list |
| `spamtrap` | Spamtrap or trap-like mailbox | Remove from list |
| `more_time_required` | Validation still processing | Poll later |
| `missing_email` | Request did not include an email | Fix input |

`result.subStatus`:

- `primary`: Allegrow identified this as the contact’s **main email**. Especially useful for execs and senior decision-makers who have multiple valid addresses.
- `null`: No additional context.

## Usage Patterns

- **After ZeroBounce returns `catch_all`**: Run Allegrow on those rows to resolve dead vs safe. Allegrow’s B2B specialisation makes it materially better than generic verifiers on corporate domains.
- **Executive lists**: Use `subStatus == primary` to prefer the primary address when multiple candidates exist.
- **Do not use**`allegrow_validate` as a bulk first-pass verifier. It costs per call and is slower than SMTP-based verifiers. Reserve it for catch-all resolution and high-value contacts.
- **Polling**: The sync endpoint has a 30-second timeout. A 202 response includes `pollUrl` and `retryAfter`. If your run context supports polling, retry after `retryAfter` seconds. In `deepline enrich` this is handled automatically.
- **Async validation**: Use `allegrow_validate_async` only when a configured webhook or later polling via `allegrow_validate_status` fits the workflow.
- **Bulk CSV**: Use the CSV job endpoints when the user explicitly needs Allegrow’s bulk CSV workflow. `allegrow_create_csv_validation_job` only creates the job and upload URL; the CSV upload itself is a direct PUT to the returned presigned URL.

## Known Limitations

- No balance/credit endpoint exposed in the API; quota resets are managed with your account manager.
- owner.com and similar corporate domains return `dead_email` even for likely-valid addresses. Allegrow is conservative on fully catch-all domains with no MX activity evidence. Cross-reference against CRM data before removing these.
- The exact purchased validation USD rate is not in the docs. Keep `ALLEGROW_PROVIDER_CURRENCY_TO_USD_RATE` blocked at 0 until the contract rate is supplied.


### Allegrow — Endpoints


#### `allegrow-create-csv-validation-job`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=allegrow_create_csv_validation_job:{"emailColumn":"{{emailColumn}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.emailColumn` | `number` | Yes |  | Zero-indexed column number containing email addresses. |

**Direct CLI:**

```
deepline tools execute allegrow_create_csv_validation_job --payload '{
  "emailColumn": 0
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `allegrow-download-csv-job-results`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=allegrow_download_csv_job_results:{"jobId":"{{jobId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.jobId` | `string` | Yes |  | UUID returned by allegrow\_create\_csv\_validation\_job. |

**Direct CLI:**

```
deepline tools execute allegrow_download_csv_job_results --payload '{
  "jobId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `allegrow-get-csv-job-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=allegrow_get_csv_job_status:{"jobId":"{{jobId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.jobId` | `string` | Yes |  | UUID returned by allegrow\_create\_csv\_validation\_job. |

**Direct CLI:**

```
deepline tools execute allegrow_get_csv_job_status --payload '{
  "jobId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `allegrow-validate`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=allegrow_validate:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address to validate. |

**Direct CLI:**

```
deepline tools execute allegrow_validate --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.12` per pricing unit.
- Billing mode: `post_deduct`.


#### `allegrow-validate-async`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=allegrow_validate_async:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address to validate. |

**Direct CLI:**

```
deepline tools execute allegrow_validate_async --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.12` per pricing unit.
- Billing mode: `post_deduct`.


#### `allegrow-validate-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=allegrow_validate_status:{"requestId":"{{requestId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.requestId` | `string` | Yes |  | UUID returned by sync or async validation. |

**Direct CLI:**

```
deepline tools execute allegrow_validate_status --payload '{
  "requestId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Apify

*Provider folder: `apify` · 10 endpoints*


### Apify — Agent Guidance

## Playbook

Use Apify when you need controlled web automation/scraping workflows.

- Use `apify_list_store_actors` first when you do not know the actor id yet.
- **Results are ranked by quality score by default.** The top result is the most reliable actor based on rating, review count, total runs, and 30-day success rate. Pick the #1 result unless you have a specific reason not to.
- Each actor in the response includes `_qualityScore` (higher is better), `_baseQualityScore`, and `_successRate30d` (percentage). Prefer actors with `_deeplineVetted: true`, high usage/rating, and `_successRate30d >= 95%`.
- For generic LinkedIn post scraping, prefer `supreme_coder/linkedin-post`. For LinkedIn post engagers/reactions, prefer `harvestapi/linkedin-post-reactions`. Avoid actors returned with `_deeplineDownranked: true` unless the user explicitly asked for that actor.
- Build `actorId` as `username/name` from store results.
- Use `apify_get_actor_input_schema` to inspect required/optional fields before running.
- Wrapper-level fields (`actorId`, `input`, `params`, `timeoutMs`) and runtime validation behavior can differ from actor-page docs.
- Prefer `apify_run_actor_sync` as the default execution path when you want results in one call.
- Use `apify_run_actor` only when you need non-blocking execution, then poll run status before fetching outputs.
- Validate payload shape with a tiny run before scaling row counts.

## Quality ranking

Actors are ranked by:

```
score = rating * log2(reviews + 1) * log10(runs + 1) / 5
```

Actors with less than 80% 30-day success rate are penalized. Actors with 0 reviews but high usage get a reduced fallback score.To bypass quality ranking and use Apify’s native sort, pass `rankBy: "relevance"`.

## Examples

```
# Search for actors, ranked by quality (default)
deepline tools execute apify_list_store_actors --payload '{"search":"google play reviews","limit":5}'
```

```
# Search with Apify's native relevance sort
deepline tools execute apify_list_store_actors --payload '{"search":"google play reviews","sortBy":"relevance","rankBy":"relevance","limit":5}'
```

```
# Inspect the actor's input schema page before execution
deepline tools execute apify_get_actor_input_schema --payload '{"actorId":"neatrat/google-play-store-reviews-scraper"}'
```

```
# Run an actor synchronously
deepline tools execute apify_run_actor_sync --payload '{"actorId":"neatrat/google-play-store-reviews-scraper","input":{"appIdOrUrl":"com.airbnb.android","sortBy":"newest","maxReviews":10},"timeoutMs":120000}'
```

```
deepline tools execute apify_get_dataset_items --payload '{"datasetId":"EU1bcB5F9gY3J1Zq2","limit":10,"offset":0}'
```


### Apify — Endpoints


#### `apify-list-actor-runs`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apify_list_actor_runs:{"actorId":"{{actorId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.actorId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute apify_list_actor_runs --payload '{
  "actorId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-actor`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apify_get_actor:{"actorId":"{{actorId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.actorId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute apify_get_actor --payload '{
  "actorId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-actor-input-schema`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apify_get_actor_input_schema:{"actorId":"{{actorId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.actorId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute apify_get_actor_input_schema --payload '{
  "actorId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-actor-run`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apify_get_actor_run:{"runId":"{{runId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.runId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute apify_get_actor_run --payload '{
  "runId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-dataset-items`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apify_get_dataset_items:{"datasetId":"{{datasetId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.datasetId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute apify_get_dataset_items --payload '{
  "datasetId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-kv-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apify_get_kv_record:{"storeId":"{{storeId}}","recordKey":"{{recordKey}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.storeId` | `string` | Yes |  |  |
| `payload.recordKey` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute apify_get_kv_record --payload '{
  "storeId": "string",
  "recordKey": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-log`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apify_get_log:{"buildOrRunId":"{{buildOrRunId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.buildOrRunId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute apify_get_log --payload '{
  "buildOrRunId": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `list-store-actors`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apify_list_store_actors:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute apify_list_store_actors --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `run-actor`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apify_run_actor:{"actorId":"{{actorId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.actorId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute apify_run_actor --payload '{
  "actorId": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `run-actor-sync`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apify_run_actor_sync:{"actorId":"{{actorId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.actorId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute apify_run_actor_sync --payload '{
  "actorId": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


---

## Apollo

*Provider folder: `apollo` · 15 endpoints*


### Apollo — Agent Guidance

## Playbook

Apollo is customer-credential-only in Deepline.Do not route default plays, quickstarts, GTM skills, or fallback recipes to
Apollo. Deepline cannot provide platform-managed Apollo access. A caller must
first connect their own Apollo API key in the dashboard before any Apollo tool
can execute.Use Apollo only when the user explicitly asks for Apollo and confirms their own
Apollo key is connected. For general company, contact, and email workflows,
prefer the non-Apollo routes documented in the GTM and Plays skills.


### Apollo — Endpoints


#### `apollo-bulk-organization-enrichment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_bulk_organization_enrichment:{"domains":"{{domains}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domains` | `array` | Yes |  | Company domains to enrich (1-10 items). |

**Direct CLI:**

```
deepline tools execute apollo_bulk_organization_enrichment --payload '{
  "domains": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


#### `apollo-company-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_company_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute apollo_company_search --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


#### `apollo-enrich-person`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_enrich_person:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute apollo_enrich_person --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


#### `apollo-get-complete-organization-info`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_get_complete_organization_info:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Apollo organization ID. |

**Direct CLI:**

```
deepline tools execute apollo_get_complete_organization_info --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `apollo-news-articles-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_news_articles_search:{"organization_ids":"{{organization_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.organization_ids` | `array` | Yes |  | Apollo organization IDs to fetch news for (required). |

**Direct CLI:**

```
deepline tools execute apollo_news_articles_search --payload '{
  "organization_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `apollo-organization-enrich`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_organization_enrich:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Company domain (e.g., ‘apollo.io’). |

**Direct CLI:**

```
deepline tools execute apollo_organization_enrich --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


#### `apollo-organization-job-postings`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_organization_job_postings:{"organization_id":"{{organization_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.organization_id` | `string` | Yes |  | Apollo organization ID. |

**Direct CLI:**

```
deepline tools execute apollo_organization_job_postings --payload '{
  "organization_id": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


#### `apollo-people-match`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_people_match:{"id":"person_123","first_name":"Ada","last_name":"Lovelace"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute apollo_people_match --payload '{
  "id": "person_123",
  "first_name": "Ada",
  "last_name": "Lovelace"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


#### `apollo-people-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_people_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute apollo_people_search --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


#### `apollo-reveal-person`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_reveal_person:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute apollo_reveal_person --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


#### `apollo-search-people`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_search_people:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute apollo_search_people --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `bulk-people-enrichment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_bulk_people_enrichment:{"details":[{"first_name":"Jane","last_name":"Doe","email":"jane.doe@example.com","domain":"example.com"}]}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.details` | `array` | Yes |  | People detail objects to enrich (1-10 items). |

**Direct CLI:**

```
deepline tools execute apollo_bulk_people_enrichment --payload '{
  "details": [\
    {\
      "first_name": "Jane",\
      "last_name": "Doe",\
      "email": "jane.doe@example.com",\
      "domain": "example.com"\
    }\
  ]
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


#### `enrich-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_enrich_company:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Company domain (e.g., ‘apollo.io’). |

**Direct CLI:**

```
deepline tools execute apollo_enrich_company --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


#### `people-search-paid`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_people_search_paid:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute apollo_people_search_paid --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


#### `search-people-with-match`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=apollo_search_people_with_match:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute apollo_search_people_with_match --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.17` per pricing unit.
- Billing mode: `post_deduct`.


---

## Attio

*Provider folder: `attio` · 104 endpoints*


### Attio — Agent Guidance

## Playbook

# Attio CRM — Agent Guidance

## Quick Reference

| Goal | Operation | Notes |
| --- | --- | --- |
| Upsert person by email | `attio_assert_record` (object: `people`, matching\_attribute: `email_addresses`) | Preferred over `create_record` — no conflict errors. |
| Upsert company by domain | `attio_assert_record` (object: `companies`, matching\_attribute: `domains`) | Attio auto-enriches when domain is provided. |
| Search by name/keyword | `attio_search_records` | Fuzzy, max 25 results. Not real-time. |
| Filter records precisely | `attio_query_records` | Structured filters, pagination, sorting. Use this for production queries. |
| Add to pipeline | `attio_create_entry` or `attio_assert_entry` | Use assert for upsert behavior. |
| Query pipeline entries | `attio_query_entries` | Supports same filter syntax as record queries. |
| Log activity | `attio_create_note` | Supports markdown format. |
| Assign follow-up | `attio_create_task` | Link to records, set deadline, assign workspace member. |
| Discover schema | `attio_list_attributes` | Always check available attributes before writing unfamiliar values. |
| Verify API key | `attio_identify` | Free. Returns workspace info and scopes. |

## Playbooks

### Playbook 1: Enrichment Roundtrip

The most common workflow. Attio auto-enriches records when email (people) or domain (companies) is provided.

```
1. attio_assert_record  -> upsert person/company by email/domain
2. Attio auto-enriches  -> job titles, social profiles, company data
3. attio_query_records  -> poll or re-query updated enrichment fields before downstream sync
```

### Playbook 2: Pipeline Qualification

```
1. attio_query_records  -> filter by enrichment criteria (employee_count > 50, funding stage)
2. attio_create_entry   -> add qualified records to "Sales Pipeline" list
3. attio_create_task    -> assign follow-up to rep (round-robin)
4. attio_create_note    -> log qualification reasoning
```

### Playbook 3: Meeting Follow-Up

```
1. attio_assert_record  -> upsert contact by email
2. attio_assert_record  -> upsert company by domain
3. attio_create_note    -> meeting summary (markdown format)
4. attio_create_task    -> action items with deadline
5. attio_assert_entry   -> create/update deal in pipeline
```

### Playbook 4: Batch Import

```
1. Loop: attio_assert_record -> upsert each record (respect 25 writes/sec)
2. Loop: attio_create_entry  -> add to pipeline (respect 25 writes/sec)
3. attio_query_records       -> verify enrichment state before routing follow-up tasks
```

## Common Mistakes

- **Use `assert_record` (PUT) not `create_record` (POST) for upserts.** POST fails with 409 on unique attribute conflicts. Assert always succeeds — creates if missing, updates if found.
- **Multiselect append behavior on assert:** When the matching attribute is multiselect, new values are appended (existing preserved). Non-matching multiselect attributes are fully replaced. Plan accordingly.
- **`search_records` is fuzzy and eventual.** It caps at 25 results and is not real-time. Use `query_records` with structured filters for precise, paginated queries.
- **No bulk API.** Each record is created or updated individually. Stay within rate limits.
- **Note event gotcha:**`note.updated` only fires for title changes. Use `note-content.updated` to track body edits.
- **Enrichment is automatic.** Attio enriches records when email (people) or domain (companies) is provided. There is no explicit enrichment API call.
- **Always check schema first.** Before writing to an unfamiliar object, call `attio_list_attributes` to discover available attribute slugs and types.

## Rate Limits

| Type | Limit |
| --- | --- |
| Read requests | 100/sec |
| Write requests | 25/sec |
| Webhook delivery | 25/sec per target URL |

All actions handle `429` responses by respecting the `Retry-After` header. For batch imports, pace writes to stay under the 25/sec write limit.


### Attio — Endpoints


#### `attio-assert-company-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_assert_company_record:{"matching_attribute":"{{matching_attribute}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.matching_attribute` | `string` | Yes |  | Unique attribute slug or ID used to upsert the company record. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_assert_company_record --payload '{
  "matching_attribute": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-assert-deal-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_assert_deal_record:{"matching_attribute":"{{matching_attribute}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.matching_attribute` | `string` | Yes |  | Unique attribute slug or ID used to upsert the deal record. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_assert_deal_record --payload '{
  "matching_attribute": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-assert-entry`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_assert_entry:{"list":"{{list}}","parent_record_id":"{{parent_record_id}}","parent_object":"{{parent_object}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list` | `string` | Yes |  | List ID or slug. |
| `payload.parent_record_id` | `string` | Yes |  | Record ID of the parent object entry. |
| `payload.parent_object` | `string` | Yes |  | API slug of the parent object. |

**Direct CLI:**

```
deepline tools execute attio_assert_entry --payload '{
  "list": "string",
  "parent_record_id": "string",
  "parent_object": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-assert-person-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_assert_person_record:{"matching_attribute":"{{matching_attribute}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.matching_attribute` | `string` | Yes |  | Unique attribute slug or ID used to upsert the person record. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_assert_person_record --payload '{
  "matching_attribute": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-assert-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_assert_record:{"object":"{{object}}","matching_attribute":"{{matching_attribute}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | API slug or ID of the object. |
| `payload.matching_attribute` | `string` | Yes |  | Attribute slug or ID used for upsert matching. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_assert_record --payload '{
  "object": "string",
  "matching_attribute": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-assert-user-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_assert_user_record:{"matching_attribute":"{{matching_attribute}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.matching_attribute` | `string` | Yes |  | Unique attribute slug or ID used to upsert the user record. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_assert_user_record --payload '{
  "matching_attribute": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-assert-workspace-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_assert_workspace_record:{"matching_attribute":"{{matching_attribute}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.matching_attribute` | `string` | Yes |  | Unique attribute slug or ID used to upsert the workspace record. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_assert_workspace_record --payload '{
  "matching_attribute": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-attribute`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_attribute:{"target":"objects","identifier":"{{identifier}}","title":"{{title}}","api_slug":"{{api_slug}}","type":"text"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `"objects" | "lists"` | Yes |  | Whether the resource belongs to an object or a list. |
| `payload.identifier` | `string` | Yes |  | Object or list slug/ID. |
| `payload.title` | `string` | Yes |  | Human-readable attribute title. |
| `payload.api_slug` | `string` | Yes |  | Unique snake\_case API slug for the attribute. |
| `payload.type` | `"text" | "number" | "checkbox" | "currency" | "date" | "timestamp" | "rating" | "status" | "select" | "record-reference" | "actor-reference" | "location" | "domain" | "email-address" | "phone-number" | "interaction" | "personal-name"` | Yes |  | Attribute data type. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.target` | `objects`, `lists` |
| `payload.type` | `text`, `number`, `checkbox`, `currency`, `date`, `timestamp`, `rating`, `status`, `select`, `record-reference`, `actor-reference`, `location`, `domain`, `email-address`, `phone-number`, `interaction`, `personal-name` |

**Direct CLI:**

```
deepline tools execute attio_create_attribute --payload '{
  "target": "objects",
  "identifier": "string",
  "title": "string",
  "api_slug": "string",
  "type": "text"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-comment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_comment:{"format":"plaintext","content":"{{content}}","author":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.format` | `"plaintext"` | Yes |  | Attio comments currently accept “plaintext” only. |
| `payload.content` | `string` | Yes |  | Comment body content. |
| `payload.author` | `object` | Yes |  | Comment author. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.format` | `plaintext` |

**Direct CLI:**

```
deepline tools execute attio_create_comment --payload '{
  "format": "plaintext",
  "content": "string",
  "author": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-company-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_company_record:{"values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_create_company_record --payload '{
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-deal-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_deal_record:{"values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_create_deal_record --payload '{
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-entry`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_entry:{"list":"{{list}}","parent_record_id":"{{parent_record_id}}","parent_object":"{{parent_object}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list` | `string` | Yes |  | List ID or slug. |
| `payload.parent_record_id` | `string` | Yes |  | Record ID of the parent object entry. |
| `payload.parent_object` | `string` | Yes |  | API slug of the parent object. |

**Direct CLI:**

```
deepline tools execute attio_create_entry --payload '{
  "list": "string",
  "parent_record_id": "string",
  "parent_object": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-file`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_file:{"object":"{{object}}","record_id":"{{record_id}}","file_type":"folder"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | Object slug or ID. |
| `payload.record_id` | `string` | Yes |  | Record ID. |
| `payload.file_type` | `"folder" | "connected-folder" | "connected-file"` | Yes |  |  |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.file_type` | `folder`, `connected-folder`, `connected-file` |

**Direct CLI:**

```
deepline tools execute attio_create_file --payload '{
  "object": "string",
  "record_id": "string",
  "file_type": "folder"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_list:{"name":"{{name}}","parent_object":"{{parent_object}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | Display name for the new list. |
| `payload.parent_object` | `string` | Yes |  | API slug of the parent object (e.g. “companies”). |

**Direct CLI:**

```
deepline tools execute attio_create_list --payload '{
  "name": "string",
  "parent_object": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-note`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_note:{"parent_object":"{{parent_object}}","parent_record_id":"{{parent_record_id}}","title":"{{title}}","content":"{{content}}","format":"plaintext"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.parent_object` | `string` | Yes |  | API slug of the parent object (e.g. “companies”). |
| `payload.parent_record_id` | `string` | Yes |  | Record ID to attach the note to. |
| `payload.title` | `string` | Yes |  | Note title. |
| `payload.content` | `string` | Yes |  | Note body content. |
| `payload.format` | `"plaintext" | "markdown"` | Yes |  | Content format. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.format` | `plaintext`, `markdown` |

**Direct CLI:**

```
deepline tools execute attio_create_note --payload '{
  "parent_object": "string",
  "parent_record_id": "string",
  "title": "string",
  "content": "string",
  "format": "plaintext"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-object`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_object:{"api_slug":"{{api_slug}}","singular_noun":"{{singular_noun}}","plural_noun":"{{plural_noun}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.api_slug` | `string` | Yes |  | Unique API slug for the object. |
| `payload.singular_noun` | `string` | Yes |  | Singular display name. |
| `payload.plural_noun` | `string` | Yes |  | Plural display name. |

**Direct CLI:**

```
deepline tools execute attio_create_object --payload '{
  "api_slug": "string",
  "singular_noun": "string",
  "plural_noun": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-person-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_person_record:{"values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_create_person_record --payload '{
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_record:{"object":"{{object}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | API slug or ID of the object (e.g. “people”, “companies”). |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_create_record --payload '{
  "object": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-select-option`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_select_option:{"target":"objects","identifier":"{{identifier}}","attribute":"{{attribute}}","title":"{{title}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `"objects" | "lists"` | Yes |  | Whether the resource belongs to an object or a list. |
| `payload.identifier` | `string` | Yes |  | Object or list slug/ID. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |
| `payload.title` | `string` | Yes |  | Select option title. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.target` | `objects`, `lists` |

**Direct CLI:**

```
deepline tools execute attio_create_select_option --payload '{
  "target": "objects",
  "identifier": "string",
  "attribute": "string",
  "title": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_status:{"target":"objects","identifier":"{{identifier}}","attribute":"{{attribute}}","title":"{{title}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `"objects" | "lists"` | Yes |  | Whether the resource belongs to an object or a list. |
| `payload.identifier` | `string` | Yes |  | Object or list slug/ID. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |
| `payload.title` | `string` | Yes |  | Status title. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.target` | `objects`, `lists` |

**Direct CLI:**

```
deepline tools execute attio_create_status --payload '{
  "target": "objects",
  "identifier": "string",
  "attribute": "string",
  "title": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-task`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_task:{"content":"{{content}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.content` | `string` | Yes |  | Task description text. |

**Direct CLI:**

```
deepline tools execute attio_create_task --payload '{
  "content": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-user-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_user_record:{"values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_create_user_record --payload '{
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-create-workspace-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_create_workspace_record:{"values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_create_workspace_record --payload '{
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-delete-comment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_delete_comment:{"comment_id":"{{comment_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.comment_id` | `string` | Yes |  | Comment ID to delete. |

**Direct CLI:**

```
deepline tools execute attio_delete_comment --payload '{
  "comment_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-delete-company-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_delete_company_record:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | company record ID to delete. |

**Direct CLI:**

```
deepline tools execute attio_delete_company_record --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-delete-deal-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_delete_deal_record:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | deal record ID to delete. |

**Direct CLI:**

```
deepline tools execute attio_delete_deal_record --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-delete-entry`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_delete_entry:{"list":"{{list}}","entry_id":"{{entry_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list` | `string` | Yes |  | List ID or slug. |
| `payload.entry_id` | `string` | Yes |  | Entry ID to delete. |

**Direct CLI:**

```
deepline tools execute attio_delete_entry --payload '{
  "list": "string",
  "entry_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-delete-file`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_delete_file:{"file_id":"{{file_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.file_id` | `string` | Yes |  | File ID to delete. |

**Direct CLI:**

```
deepline tools execute attio_delete_file --payload '{
  "file_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-delete-note`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_delete_note:{"note_id":"{{note_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.note_id` | `string` | Yes |  | Note ID to delete. |

**Direct CLI:**

```
deepline tools execute attio_delete_note --payload '{
  "note_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-delete-person-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_delete_person_record:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | person record ID to delete. |

**Direct CLI:**

```
deepline tools execute attio_delete_person_record --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-delete-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_delete_record:{"object":"{{object}}","record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | API slug or ID of the object. |
| `payload.record_id` | `string` | Yes |  | Record ID to delete. |

**Direct CLI:**

```
deepline tools execute attio_delete_record --payload '{
  "object": "string",
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-delete-task`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_delete_task:{"task_id":"{{task_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.task_id` | `string` | Yes |  | Task ID to delete. |

**Direct CLI:**

```
deepline tools execute attio_delete_task --payload '{
  "task_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-delete-user-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_delete_user_record:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | user record ID to delete. |

**Direct CLI:**

```
deepline tools execute attio_delete_user_record --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-delete-workspace-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_delete_workspace_record:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | workspace record ID to delete. |

**Direct CLI:**

```
deepline tools execute attio_delete_workspace_record --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-download-file`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_download_file:{"file_id":"{{file_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.file_id` | `string` | Yes |  | File ID to download. |

**Direct CLI:**

```
deepline tools execute attio_download_file --payload '{
  "file_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-attribute`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_attribute:{"target":"objects","identifier":"{{identifier}}","attribute":"{{attribute}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `"objects" | "lists"` | Yes |  | Whether the resource belongs to an object or a list. |
| `payload.identifier` | `string` | Yes |  | Object or list slug/ID. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.target` | `objects`, `lists` |

**Direct CLI:**

```
deepline tools execute attio_get_attribute --payload '{
  "target": "objects",
  "identifier": "string",
  "attribute": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-call-recording`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_call_recording:{"meeting_id":"{{meeting_id}}","call_recording_id":"{{call_recording_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.meeting_id` | `string` | Yes |  | Meeting ID. |
| `payload.call_recording_id` | `string` | Yes |  | Call recording ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_call_recording --payload '{
  "meeting_id": "string",
  "call_recording_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-call-transcript`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_call_transcript:{"meeting_id":"{{meeting_id}}","call_recording_id":"{{call_recording_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.meeting_id` | `string` | Yes |  | Meeting ID. |
| `payload.call_recording_id` | `string` | Yes |  | Call recording ID. |

**Direct CLI:**

```
deepline tools execute attio_get_call_transcript --payload '{
  "meeting_id": "string",
  "call_recording_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-comment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_comment:{"comment_id":"{{comment_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.comment_id` | `string` | Yes |  | Comment ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_comment --payload '{
  "comment_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-company-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_company_record:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | company record ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_company_record --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-deal-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_deal_record:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | deal record ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_deal_record --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-entry`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_entry:{"list":"{{list}}","entry_id":"{{entry_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list` | `string` | Yes |  | List ID or slug. |
| `payload.entry_id` | `string` | Yes |  | Entry ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_entry --payload '{
  "list": "string",
  "entry_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-file`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_file:{"file_id":"{{file_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.file_id` | `string` | Yes |  | File ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_file --payload '{
  "file_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_list:{"list":"{{list}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list` | `string` | Yes |  | List ID or slug. |

**Direct CLI:**

```
deepline tools execute attio_get_list --payload '{
  "list": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-meeting`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_meeting:{"meeting_id":"{{meeting_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.meeting_id` | `string` | Yes |  | Meeting ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_meeting --payload '{
  "meeting_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-note`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_note:{"note_id":"{{note_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.note_id` | `string` | Yes |  | Note ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_note --payload '{
  "note_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-object`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_object:{"object":"{{object}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | Object slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_get_object --payload '{
  "object": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-person-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_person_record:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | person record ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_person_record --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_record:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | Record ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_record --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-task`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_task:{"task_id":"{{task_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.task_id` | `string` | Yes |  | Task ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_task --payload '{
  "task_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-thread`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_thread:{"thread_id":"{{thread_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.thread_id` | `string` | Yes |  | Thread ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_thread --payload '{
  "thread_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-user-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_user_record:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | user record ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_user_record --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-workspace-member`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_workspace_member:{"workspace_member_id":"{{workspace_member_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.workspace_member_id` | `string` | Yes |  | Workspace member ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_workspace_member --payload '{
  "workspace_member_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-get-workspace-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_get_workspace_record:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | workspace record ID to retrieve. |

**Direct CLI:**

```
deepline tools execute attio_get_workspace_record --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-identify`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_identify:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_identify --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-attributes`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_attributes:{"target":"objects","identifier":"{{identifier}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `"objects" | "lists"` | Yes |  | Whether the resource belongs to an object or a list. |
| `payload.identifier` | `string` | Yes |  | Object or list slug/ID. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.target` | `objects`, `lists` |

**Direct CLI:**

```
deepline tools execute attio_list_attributes --payload '{
  "target": "objects",
  "identifier": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-call-recordings`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_call_recordings:{"meeting_id":"{{meeting_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.meeting_id` | `string` | Yes |  | Meeting ID whose call recordings to list. |

**Direct CLI:**

```
deepline tools execute attio_list_call_recordings --payload '{
  "meeting_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-company-record-attribute-values`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_company_record_attribute_values:{"record_id":"{{record_id}}","attribute":"{{attribute}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | company record ID to inspect. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_list_company_record_attribute_values --payload '{
  "record_id": "string",
  "attribute": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-company-record-entries`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_company_record_entries:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | company record ID whose list entries to enumerate. |

**Direct CLI:**

```
deepline tools execute attio_list_company_record_entries --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-deal-record-attribute-values`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_deal_record_attribute_values:{"record_id":"{{record_id}}","attribute":"{{attribute}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | deal record ID to inspect. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_list_deal_record_attribute_values --payload '{
  "record_id": "string",
  "attribute": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-deal-record-entries`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_deal_record_entries:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | deal record ID whose list entries to enumerate. |

**Direct CLI:**

```
deepline tools execute attio_list_deal_record_entries --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-entry-attribute-values`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_entry_attribute_values:{"list":"{{list}}","entry_id":"{{entry_id}}","attribute":"{{attribute}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list` | `string` | Yes |  | List ID or slug. |
| `payload.entry_id` | `string` | Yes |  | Entry ID to inspect. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_list_entry_attribute_values --payload '{
  "list": "string",
  "entry_id": "string",
  "attribute": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-files`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_files:{"object":"{{object}}","record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | Object slug or ID. |
| `payload.record_id` | `string` | Yes |  | Record ID. |

**Direct CLI:**

```
deepline tools execute attio_list_files --payload '{
  "object": "string",
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-lists`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_lists:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_list_lists --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-meetings`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_meetings:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_list_meetings --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-notes`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_notes:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_list_notes --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-objects`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_objects:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_list_objects --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-person-record-attribute-values`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_person_record_attribute_values:{"record_id":"{{record_id}}","attribute":"{{attribute}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | person record ID to inspect. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_list_person_record_attribute_values --payload '{
  "record_id": "string",
  "attribute": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-person-record-entries`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_person_record_entries:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | person record ID whose list entries to enumerate. |

**Direct CLI:**

```
deepline tools execute attio_list_person_record_entries --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-record-attribute-values`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_record_attribute_values:{"object":"{{object}}","record_id":"{{record_id}}","attribute":"{{attribute}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | API slug or ID of the object. |
| `payload.record_id` | `string` | Yes |  | Record ID to inspect. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_list_record_attribute_values --payload '{
  "object": "string",
  "record_id": "string",
  "attribute": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-record-entries`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_record_entries:{"object":"{{object}}","record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | API slug or ID of the object. |
| `payload.record_id` | `string` | Yes |  | Record ID whose list entries to enumerate. |

**Direct CLI:**

```
deepline tools execute attio_list_record_entries --payload '{
  "object": "string",
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-select-options`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_select_options:{"target":"objects","identifier":"{{identifier}}","attribute":"{{attribute}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `"objects" | "lists"` | Yes |  | Whether the resource belongs to an object or a list. |
| `payload.identifier` | `string` | Yes |  | Object or list slug/ID. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.target` | `objects`, `lists` |

**Direct CLI:**

```
deepline tools execute attio_list_select_options --payload '{
  "target": "objects",
  "identifier": "string",
  "attribute": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-statuses`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_statuses:{"target":"objects","identifier":"{{identifier}}","attribute":"{{attribute}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `"objects" | "lists"` | Yes |  | Whether the resource belongs to an object or a list. |
| `payload.identifier` | `string` | Yes |  | Object or list slug/ID. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.target` | `objects`, `lists` |

**Direct CLI:**

```
deepline tools execute attio_list_statuses --payload '{
  "target": "objects",
  "identifier": "string",
  "attribute": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-tasks`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_tasks:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_list_tasks --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-threads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_threads:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_list_threads --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-user-record-attribute-values`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_user_record_attribute_values:{"record_id":"{{record_id}}","attribute":"{{attribute}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | user record ID to inspect. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_list_user_record_attribute_values --payload '{
  "record_id": "string",
  "attribute": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-user-record-entries`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_user_record_entries:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | user record ID whose list entries to enumerate. |

**Direct CLI:**

```
deepline tools execute attio_list_user_record_entries --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-workspace-members`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_workspace_members:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_list_workspace_members --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-workspace-record-attribute-values`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_workspace_record_attribute_values:{"record_id":"{{record_id}}","attribute":"{{attribute}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | workspace record ID to inspect. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_list_workspace_record_attribute_values --payload '{
  "record_id": "string",
  "attribute": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-list-workspace-record-entries`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_list_workspace_record_entries:{"record_id":"{{record_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | workspace record ID whose list entries to enumerate. |

**Direct CLI:**

```
deepline tools execute attio_list_workspace_record_entries --payload '{
  "record_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-query-company-records`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_query_company_records:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_query_company_records --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-query-deal-records`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_query_deal_records:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_query_deal_records --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-query-entries`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_query_entries:{"list":"{{list}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list` | `string` | Yes |  | List ID or slug. |

**Direct CLI:**

```
deepline tools execute attio_query_entries --payload '{
  "list": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-query-person-records`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_query_person_records:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_query_person_records --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-query-records`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_query_records:{"object":"{{object}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | API slug or ID of the object to query. |

**Direct CLI:**

```
deepline tools execute attio_query_records --payload '{
  "object": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-query-user-records`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_query_user_records:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_query_user_records --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-query-workspace-records`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_query_workspace_records:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute attio_query_workspace_records --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-search-records`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_search_records:{"query":"{{query}}","objects":"{{objects}}","request_as":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | Free-text search query. Empty string returns a default result set. |
| `payload.objects` | `array` | Yes |  | Restrict search to specific object slugs. |
| `payload.request_as` | `object` | Yes |  | Search context to run as. |

**Direct CLI:**

```
deepline tools execute attio_search_records --payload '{
  "query": "string",
  "objects": "array",
  "request_as": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-attribute`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_attribute:{"target":"objects","identifier":"{{identifier}}","attribute":"{{attribute}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `"objects" | "lists"` | Yes |  | Whether the resource belongs to an object or a list. |
| `payload.identifier` | `string` | Yes |  | Object or list slug/ID. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.target` | `objects`, `lists` |

**Direct CLI:**

```
deepline tools execute attio_update_attribute --payload '{
  "target": "objects",
  "identifier": "string",
  "attribute": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-company-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_company_record:{"record_id":"{{record_id}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | company record ID to update. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_update_company_record --payload '{
  "record_id": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-deal-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_deal_record:{"record_id":"{{record_id}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | deal record ID to update. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_update_deal_record --payload '{
  "record_id": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-entry`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_entry:{"list":"{{list}}","entry_id":"{{entry_id}}","entry_values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list` | `string` | Yes |  | List ID or slug. |
| `payload.entry_id` | `string` | Yes |  | Entry ID to update. |
| `payload.entry_values` | `record` | Yes |  | Attribute values to update. |

**Direct CLI:**

```
deepline tools execute attio_update_entry --payload '{
  "list": "string",
  "entry_id": "string",
  "entry_values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-entry-overwrite`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_entry_overwrite:{"list":"{{list}}","entry_id":"{{entry_id}}","entry_values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list` | `string` | Yes |  | List ID or slug. |
| `payload.entry_id` | `string` | Yes |  | Entry ID to update. |
| `payload.entry_values` | `record` | Yes |  | Attribute values to overwrite. |

**Direct CLI:**

```
deepline tools execute attio_update_entry_overwrite --payload '{
  "list": "string",
  "entry_id": "string",
  "entry_values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_list:{"list":"{{list}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list` | `string` | Yes |  | List ID or slug. |

**Direct CLI:**

```
deepline tools execute attio_update_list --payload '{
  "list": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-object`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_object:{"object":"{{object}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | Object slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_update_object --payload '{
  "object": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-person-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_person_record:{"record_id":"{{record_id}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | person record ID to update. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_update_person_record --payload '{
  "record_id": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_record:{"object":"{{object}}","record_id":"{{record_id}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | API slug or ID of the object. |
| `payload.record_id` | `string` | Yes |  | Record ID to update. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_update_record --payload '{
  "object": "string",
  "record_id": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-record-overwrite`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_record_overwrite:{"object":"{{object}}","record_id":"{{record_id}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | API slug or ID of the object. |
| `payload.record_id` | `string` | Yes |  | Record ID to update. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_update_record_overwrite --payload '{
  "object": "string",
  "record_id": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-select-option`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_select_option:{"target":"objects","identifier":"{{identifier}}","attribute":"{{attribute}}","option":"{{option}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `"objects" | "lists"` | Yes |  | Whether the resource belongs to an object or a list. |
| `payload.identifier` | `string` | Yes |  | Object or list slug/ID. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |
| `payload.option` | `string` | Yes |  | Select option slug or ID. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.target` | `objects`, `lists` |

**Direct CLI:**

```
deepline tools execute attio_update_select_option --payload '{
  "target": "objects",
  "identifier": "string",
  "attribute": "string",
  "option": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_status:{"target":"objects","identifier":"{{identifier}}","attribute":"{{attribute}}","status":"{{status}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `"objects" | "lists"` | Yes |  | Whether the resource belongs to an object or a list. |
| `payload.identifier` | `string` | Yes |  | Object or list slug/ID. |
| `payload.attribute` | `string` | Yes |  | Attribute slug or ID. |
| `payload.status` | `string` | Yes |  | Status slug or ID. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.target` | `objects`, `lists` |

**Direct CLI:**

```
deepline tools execute attio_update_status --payload '{
  "target": "objects",
  "identifier": "string",
  "attribute": "string",
  "status": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-task`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_task:{"task_id":"{{task_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.task_id` | `string` | Yes |  | Task ID to update. |

**Direct CLI:**

```
deepline tools execute attio_update_task --payload '{
  "task_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-user-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_user_record:{"record_id":"{{record_id}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | user record ID to update. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_update_user_record --payload '{
  "record_id": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-update-workspace-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_update_workspace_record:{"record_id":"{{record_id}}","values":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.record_id` | `string` | Yes |  | workspace record ID to update. |
| `payload.values` | `record` | Yes |  | Attribute values keyed by attribute slug or ID. |

**Direct CLI:**

```
deepline tools execute attio_update_workspace_record --payload '{
  "record_id": "string",
  "values": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `attio-upload-file`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=attio_upload_file:{"object":"{{object}}","record_id":"{{record_id}}","content":"{{content}}","name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | Object slug or ID. |
| `payload.record_id` | `string` | Yes |  | Record ID. |
| `payload.content` | `string` | Yes |  | Raw file bytes as text or base64 string. |
| `payload.name` | `string` | Yes |  | Filename to upload. |

**Direct CLI:**

```
deepline tools execute attio_upload_file --payload '{
  "object": "string",
  "record_id": "string",
  "content": "string",
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Aviato

*Provider folder: `aviato` · 27 endpoints*


### Aviato — Agent Guidance

## Playbook

# Aviato

Use `aviato_get_balance` first when validating credentials or checking account state.Use preview enrichment (`preview: true`) when you only need free identity resolution fields. Non-preview person/company enrichment and bulk enrichment can consume Aviato credits.Do not use disabled Aviato operations until Aviato supplies the complete endpoint credit ladder and the purchased-credit USD rate. Disabled operations are present only to keep OpenAPI coverage explicit.


### Aviato — Endpoints


#### `aviato-company-bulk-enrich`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_company_bulk_enrich:{"lookups":"{{lookups}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lookups` | `array` | Yes |  | List of company lookups to perform (max 100) |

**Direct CLI:**

```
deepline tools execute aviato_company_bulk_enrich --payload '{
  "lookups": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-company-enrich`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_company_enrich:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute aviato_company_enrich --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-company-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_company_search:{"dsl":"{{dsl}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.dsl` | `unknown` | Yes |  | The DSL query object. Must contain a `filters` array. |

**Direct CLI:**

```
deepline tools execute aviato_company_search --payload '{
  "dsl": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.04` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-generate-map`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_generate_map:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute aviato_generate_map --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `2.1` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-geocoder-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_geocoder_search:{"text":"{{text}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.text` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute aviato_geocoder_search --payload '{
  "text": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-acquisitions`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_acquisitions:{"perPage":"{{perPage}}","page":"{{page}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.perPage` | `integer` | Yes |  | Number of items per page. |
| `payload.page` | `integer` | Yes |  | Page number, starts from 0. |

**Direct CLI:**

```
deepline tools execute aviato_get_acquisitions --payload '{
  "perPage": "integer",
  "page": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-company-founders`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_company_founders:{"perPage":"{{perPage}}","page":"{{page}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.perPage` | `integer` | Yes |  | Number of items per page. |
| `payload.page` | `integer` | Yes |  | Page number, starts from 0. |

**Direct CLI:**

```
deepline tools execute aviato_get_company_founders --payload '{
  "perPage": "integer",
  "page": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-company-fund-investments`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_company_fund_investments:{"perPage":"{{perPage}}","page":"{{page}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.perPage` | `integer` | Yes |  | Number of items per page. |
| `payload.page` | `integer` | Yes |  | Page number, starts from 0. |

**Direct CLI:**

```
deepline tools execute aviato_get_company_fund_investments --payload '{
  "perPage": "integer",
  "page": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-company-funding-rounds`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_company_funding_rounds:{"perPage":"{{perPage}}","page":"{{page}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.perPage` | `integer` | Yes |  | Number of items per page. |
| `payload.page` | `integer` | Yes |  | Page number, starts from 0. |

**Direct CLI:**

```
deepline tools execute aviato_get_company_funding_rounds --payload '{
  "perPage": "integer",
  "page": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-company-funds`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_company_funds:{"perPage":"{{perPage}}","page":"{{page}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.perPage` | `integer` | Yes |  | Number of items per page. |
| `payload.page` | `integer` | Yes |  | Page number, starts from 0. |

**Direct CLI:**

```
deepline tools execute aviato_get_company_funds --payload '{
  "perPage": "integer",
  "page": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-company-outbound-investments`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_company_outbound_investments:{"perPage":"{{perPage}}","page":"{{page}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.perPage` | `integer` | Yes |  | Number of items per page. |
| `payload.page` | `integer` | Yes |  | Page number, starts from 0. |

**Direct CLI:**

```
deepline tools execute aviato_get_company_outbound_investments --payload '{
  "perPage": "integer",
  "page": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-company-posts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_company_posts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute aviato_get_company_posts --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.7` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-employees`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_employees:{"perPage":"{{perPage}}","page":"{{page}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.perPage` | `integer` | Yes |  | Number of items per page. |
| `payload.page` | `integer` | Yes |  | Page number, starts from 0. |

**Direct CLI:**

```
deepline tools execute aviato_get_employees --payload '{
  "perPage": "integer",
  "page": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-founded-companies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_founded_companies:{"perPage":"{{perPage}}","page":"{{page}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.perPage` | `integer` | Yes |  | Number of items per page. |
| `payload.page` | `integer` | Yes |  | Page number, starts from 0. |

**Direct CLI:**

```
deepline tools execute aviato_get_founded_companies --payload '{
  "perPage": "integer",
  "page": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-fund-investments`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_fund_investments:{"perPage":"{{perPage}}","page":"{{page}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.perPage` | `integer` | Yes |  | Number of items per page. |
| `payload.page` | `integer` | Yes |  | Page number, starts from 0. |

**Direct CLI:**

```
deepline tools execute aviato_get_fund_investments --payload '{
  "perPage": "integer",
  "page": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-person-posts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_person_posts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute aviato_get_person_posts --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.7` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-post-comments`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_post_comments:{"postUrn":"{{postUrn}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.postUrn` | `string` | Yes |  | LinkedIn post URN |

**Direct CLI:**

```
deepline tools execute aviato_get_post_comments --payload '{
  "postUrn": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-post-reactions`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_post_reactions:{"postUrn":"{{postUrn}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.postUrn` | `string` | Yes |  | LinkedIn post URN |

**Direct CLI:**

```
deepline tools execute aviato_get_post_reactions --payload '{
  "postUrn": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-post-reshares`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_post_reshares:{"postUrn":"{{postUrn}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.postUrn` | `string` | Yes |  | LinkedIn post URN |

**Direct CLI:**

```
deepline tools execute aviato_get_post_reshares --payload '{
  "postUrn": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-get-vestings`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_get_vestings:{"perPage":"{{perPage}}","page":"{{page}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.perPage` | `integer` | Yes |  | Number of items per page. |
| `payload.page` | `integer` | Yes |  | Page number, starts from 0. |

**Direct CLI:**

```
deepline tools execute aviato_get_vestings --payload '{
  "perPage": "integer",
  "page": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `1.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-person-bulk-contact-info`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_person_bulk_contact_info:{"lookups":"{{lookups}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lookups` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute aviato_person_bulk_contact_info --payload '{
  "lookups": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-person-bulk-enrich`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_person_bulk_enrich:{"lookups":"{{lookups}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lookups` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute aviato_person_bulk_enrich --payload '{
  "lookups": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-person-enrich`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_person_enrich:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute aviato_person_enrich --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-person-get-company-investments`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_person_get_company_investments:{"perPage":"{{perPage}}","page":"{{page}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.perPage` | `integer` | Yes |  | Number of items per page. |
| `payload.page` | `integer` | Yes |  | Page number, starts from 0. |

**Direct CLI:**

```
deepline tools execute aviato_person_get_company_investments --payload '{
  "perPage": "integer",
  "page": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-person-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_person_search:{"dsl":"{{dsl}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.dsl` | `unknown` | Yes |  | The DSL query object. Must contain a `filters` array. |

**Direct CLI:**

```
deepline tools execute aviato_person_search --payload '{
  "dsl": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.04` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-pull-contact-info`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_pull_contact_info:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute aviato_pull_contact_info --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `1.12` per pricing unit.
- Billing mode: `post_deduct`.


#### `aviato-pull-email-hash`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=aviato_pull_email_hash:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute aviato_pull_email_hash --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `1.12` per pricing unit.
- Billing mode: `post_deduct`.


---

## BetterContact

*Provider folder: `bettercontact` · 3 endpoints*


### BetterContact — Agent Guidance

## Playbook

# BetterContact Agent Guidance

## Key patterns

- **Enrichment is async.**`bettercontact_enrich` and `bettercontact_bulk_enrich` launch BetterContact jobs and return a request id immediately. Use `bettercontact_get_result` to fetch terminal results.
- **Email status hierarchy:** deliverable > catch\_all\_safe > catch\_all\_not\_safe > undeliverable. Only trust deliverable and catch\_all\_safe for outreach.
- **Batch up to 100 contacts** per enrichment request using `bettercontact_bulk_enrich`.
- Use the launcher response `id` as the `request_id` for `bettercontact_get_result`.
- **Rate limit:** 60 requests per minute per API key, shared across all endpoints.

## Pricing

- 1 successful email = 1 provider credit, 1 successful mobile phone = +10 provider credits
- `bettercontact_get_result` returns `credits_consumed`; Deepline bills from that terminal provider usage
- **Phone enrichment is expensive** — only enable `enrich_phone_number: true` when explicitly needed

## When to use

- Use BetterContact when you need waterfall email enrichment with multi-provider verification.
- Good fallback when single-provider finders (LeadMagic, Prospeo) miss.
- Includes triple email verification, phone verification, contact & company enrichment.

## When NOT to use

- Don’t use for email validation only — use a dedicated validator.
- Don’t use for company/org enrichment — BetterContact is contact-focused.


### BetterContact — Endpoints


#### `bettercontact-bulk-enrich`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=bettercontact_bulk_enrich:{"contacts":"{{contacts}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.contacts` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute bettercontact_bulk_enrich --payload '{
  "contacts": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.69` per pricing unit.
- Billing mode: `post_deduct`.


#### `bettercontact-enrich`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=bettercontact_enrich:{"first_name":"{{first_name}}","last_name":"{{last_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name. |
| `payload.last_name` | `string` | Yes |  | Contact last name. |

**Direct CLI:**

```
deepline tools execute bettercontact_enrich --payload '{
  "first_name": "string",
  "last_name": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.69` per pricing unit.
- Billing mode: `post_deduct`.


#### `bettercontact-get-result`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=bettercontact_get_result:{"request_id":"{{request_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.request_id` | `string` | Yes |  | The BetterContact request id returned by the create endpoint. |

**Direct CLI:**

```
deepline tools execute bettercontact_get_result --payload '{
  "request_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Bloomberry

*Provider folder: `bloomberry` · 5 endpoints*


### Bloomberry — Agent Guidance

## Playbook

# Bloomberry Agent Guidance

Use Bloomberry when the user needs account-level B2B technographic signals or hiring signals.Prefer:

- `bloomberry_get_company_tech_stack` when you already know a company domain and need the vendors it currently uses.
- `bloomberry_get_tech_stack_changes` when the user needs recent adoption, churn, or usage signals. Provide exactly one of `category` or `vendor_name`.
- `bloomberry_get_current_customers` when building a list of companies currently using a vendor or category. Provide exactly one of `category` or `vendor_name`.
- `bloomberry_list_vendors` before vendor-filtered searches when the exact vendor/category name is uncertain.
- `bloomberry_search_job_postings` for hiring intent. Provide at least one of `keyword`, `normalized_job_titles`, or `domain`.

Use `limit` conservatively on result-list endpoints because Bloomberry charges credits per returned signal, customer, or job posting.


### Bloomberry — Endpoints


#### `bloomberry-get-company-tech-stack`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=bloomberry_get_company_tech_stack:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | The domain of the company to get current vendors they’re subscribed to. For example, ‘ford.com’ to get all active vendors that Ford is subscribed to. |

**Direct CLI:**

```
deepline tools execute bloomberry_get_company_tech_stack --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.43` per pricing unit.
- Billing mode: `post_deduct`.


#### `bloomberry-get-current-customers`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=bloomberry_get_current_customers:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute bloomberry_get_current_customers --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `2.11` per pricing unit.
- Billing mode: `post_deduct`.


#### `bloomberry-get-tech-stack-changes`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=bloomberry_get_tech_stack_changes:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute bloomberry_get_tech_stack_changes --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `4.21` per pricing unit.
- Billing mode: `post_deduct`.


#### `bloomberry-list-vendors`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=bloomberry_list_vendors:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute bloomberry_list_vendors --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `bloomberry-search-job-postings`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=bloomberry_search_job_postings:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute bloomberry_search_job_postings --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.09` per pricing unit.
- Billing mode: `post_deduct`.


---

## BuiltWith

*Provider folder: `builtwith` · 16 endpoints*


### BuiltWith — Agent Guidance

## Playbook

# BuiltWith Guidance

- Use `builtwith_domain_lookup` when you already know the domain and need live/current technographics. The handler defaults `live_only` to true; set `live_only=false` only when historical detections matter.
- Use `builtwith_vector_search` to discover the exact BuiltWith technology label before `builtwith_lists` or `builtwith_trends`. Free-text tech guesses often miss if the BuiltWith canonical name differs.
- Use `builtwith_bulk_domain_lookup` for row-heavy domain work. It auto-polls queued jobs by default and normalizes both sync and async paths to the same `results[]` shape.
- The batch compiler only coalesces `builtwith_domain_lookup` calls when the request can be losslessly represented by the native bulk API. Requests using `trust`, `no_attr`, or date-range filters stay single-call.
- `builtwith_lists` is best for account sourcing by technology; `builtwith_relationships`, `builtwith_redirects`, and `builtwith_tag_lookup` are better for niche infrastructure signals and related-domain expansion.


### BuiltWith — Endpoints


#### `builtwith-bulk-domain-job-result`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_bulk_domain_job_result:{"job_id":"{{job_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.job_id` | `string` | Yes |  | BuiltWith bulk domain job ID. |

**Direct CLI:**

```
deepline tools execute builtwith_bulk_domain_job_result --payload '{
  "job_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `builtwith-bulk-domain-job-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_bulk_domain_job_status:{"job_id":"{{job_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.job_id` | `string` | Yes |  | BuiltWith bulk domain job ID. |

**Direct CLI:**

```
deepline tools execute builtwith_bulk_domain_job_status --payload '{
  "job_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `builtwith-bulk-domain-lookup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_bulk_domain_lookup:{"domains":"{{domains}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domains` | `array` | Yes |  | Domains to submit to the native BuiltWith bulk domain API. |

**Direct CLI:**

```
deepline tools execute builtwith_bulk_domain_lookup --payload '{
  "domains": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `builtwith-company-to-url`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_company_to_url:{"company":"{{company}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company` | `string` | Yes |  | Company name to resolve to candidate domains. |

**Direct CLI:**

```
deepline tools execute builtwith_company_to_url --payload '{
  "company": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `builtwith-domain-lookup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_domain_lookup:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute builtwith_domain_lookup --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `builtwith-free-lookup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_free_lookup:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Root domain or subdomain to inspect (for example stripe.com). |

**Direct CLI:**

```
deepline tools execute builtwith_free_lookup --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `builtwith-keywords`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_keywords:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Root domain or subdomain to inspect (for example stripe.com). |

**Direct CLI:**

```
deepline tools execute builtwith_keywords --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `builtwith-lists`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_lists:{"tech":"{{tech}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tech` | `string` | Yes |  | Technology name to search for (for example Shopify). |

**Direct CLI:**

```
deepline tools execute builtwith_lists --payload '{
  "tech": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `builtwith-product-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_product_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | Product search query (for example headphones or Adidas Yeezy). |

**Direct CLI:**

```
deepline tools execute builtwith_product_search --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `builtwith-recommendations`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_recommendations:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Root domain or subdomain to inspect (for example stripe.com). |

**Direct CLI:**

```
deepline tools execute builtwith_recommendations --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `builtwith-redirects`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_redirects:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Root domain or subdomain to inspect (for example stripe.com). |

**Direct CLI:**

```
deepline tools execute builtwith_redirects --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `builtwith-relationships`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_relationships:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Root domain or subdomain to inspect (for example stripe.com). |

**Direct CLI:**

```
deepline tools execute builtwith_relationships --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `builtwith-tag-lookup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_tag_lookup:{"lookup":"{{lookup}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lookup` | `string` | Yes |  | IP or BuiltWith tag lookup string (for example IP-1.2.3.4). |

**Direct CLI:**

```
deepline tools execute builtwith_tag_lookup --payload '{
  "lookup": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `builtwith-trends`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_trends:{"tech":"{{tech}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tech` | `string` | Yes |  | Technology name to inspect in BuiltWith Trends. |

**Direct CLI:**

```
deepline tools execute builtwith_trends --payload '{
  "tech": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `builtwith-trust-lookup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_trust_lookup:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Root domain or subdomain to inspect (for example stripe.com). |

**Direct CLI:**

```
deepline tools execute builtwith_trust_lookup --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `builtwith-vector-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=builtwith_vector_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | Semantic technology or category search query. |

**Direct CLI:**

```
deepline tools execute builtwith_vector_search --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


---

## Cloudflare

*Provider folder: `cloudflare` · 1 endpoints*


### Cloudflare — Agent Guidance

## Playbook

# Cloudflare Browser Rendering — Agent Guidance

## When to use

- You need to crawl a website and extract structured content (markdown, HTML, or JSON).
- You need browser-rendered pages (JavaScript-heavy SPAs, dynamic content).
- You want a managed crawl that follows links up to a configurable depth/limit.

## Key parameters

- **`url`** (required): The starting URL.
- **`limit`**: Max pages to crawl. Default 10. Keep low unless the user needs broad coverage.
- **`depth`**: Max link depth from the starting URL. Default 100,000.
- **`source`**: URL discovery method — `"all"` (default), `"sitemaps"`, or `"links"`.
- **`formats`**: Array of `"html"`, `"markdown"`, `"json"`. Default `["markdown"]`. Markdown is best for LLM consumption. JSON uses Workers AI for extraction and requires `jsonOptions`.
- **`render`**: Browser rendering toggle. Default `true`. Set `false` for simple static pages to save browser seconds.
- **`options.includePatterns`** / **`excludePatterns`**: Wildcard patterns to filter which URLs get crawled. Exclude takes priority.
- **`timeoutMs`**: How long to poll before returning partial results. Default 5 minutes.

## Timeout behavior

- On timeout, the action returns whatever partial results are available with `timedOut: true`.
- The `jobId` is included so you can construct a follow-up poll if needed.
- The action does NOT throw on timeout.

## Job statuses

- `running` — crawl in progress (non-terminal)
- `completed` — all pages crawled successfully
- `errored` — unrecoverable error
- `cancelled_due_to_timeout` — exceeded 7-day max runtime
- `cancelled_due_to_limits` — hit account browser time limits
- `cancelled_by_user` — manually cancelled

## Cost awareness

- Priced at **$0.20 per 1,000 browser-seconds** (render=true). Billed post-deduct based on `browserSecondsUsed` in the response.
- `render: false` crawls use 0 browser-seconds (free during beta).
- Keep `limit` reasonable — large crawls consume significant browser time.
- Crawl jobs expire after 7 days; results available 14 days post-completion.

## Robots.txt

- Cloudflare respects `robots.txt` by default, including `crawl-delay`.
- Blocked URLs appear in results with `"status": "disallowed"`.


### Cloudflare — Endpoints


#### `crawl`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=cloudflare_crawl:{"url":"{{url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.url` | `string` | Yes |  | Starting URL to crawl |

**Direct CLI:**

```
deepline tools execute cloudflare_crawl --payload '{
  "url": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


---

## ContactOut

*Provider folder: `contactout` · 7 endpoints*


### ContactOut — Agent Guidance

## Playbook

# ContactOut — Agent Guidance

## When to use

ContactOut for LinkedIn → email/phone enrichment when you have a LinkedIn URL. High accuracy for active LinkedIn users. Strong for US + global. Falls after dropleads in cost-ordered waterfalls.**Key differentiator**: Free pre-check APIs (`contactout_check_email_status`, `contactout_check_work_email`, `contactout_check_personal_email`, `contactout_check_phone`) tell you which contact channels are present before you spend credits on enrichment. Use these first when enriching at scale.

## Provider characteristics

- **Input required**: LinkedIn URL (best), email, or name+company
- **Geographic coverage**: Global, strongest in US + Europe
- **Credit cost**: ~$0.10 per email credit; separate phone + search credits
- **LinkedIn URL requirement**: Must contain “linkedin.com/in/” or “linkedin.com/pub/”. Sales Navigator URLs not supported.

## Key operations

### contactout\_check\_email\_status (FREE convenience helper)

Checks work-email and personal-email availability together for one LinkedIn profile.

```
{
  "profile": "https://www.linkedin.com/in/johndoe"
}
```

Returns:

- `contactout_check_email_status` → `{ "has_personal_email": false, "has_work_email": true, "status": "verified" }`

### contactout\_check\_work\_email / contactout\_check\_personal\_email / contactout\_check\_phone (FREE — use first at scale)

Check whether a LinkedIn profile has work email, personal email, or phone coverage. Zero credits consumed. Use these to filter out profiles with no coverage before running enrichment.

```
{
  "profile": "https://www.linkedin.com/in/johndoe"
}
```

Returns one channel-specific payload per tool:

- `contactout_check_work_email` → `{ "has_work_email": true, "status": "verified" }`
- `contactout_check_personal_email` → `{ "has_personal_email": false }`
- `contactout_check_phone` → `{ "has_phone": true }`

### contactout\_enrich\_person

Enriches a person by LinkedIn URL (preferred), email, or name+company. Returns email array at `email`, `work_email`, `personal_email`.

```
{
  "linkedin_url": "https://www.linkedin.com/in/johndoe",
  "include": ["work_email"]
}
```

```
{
  "first_name": "John",
  "last_name": "Doe",
  "company_domain": "acme.com"
}
```

### contactout\_search\_people

Search people by title, company, location, seniority. Use `reveal_info: false` (default) for count/discovery. Set `reveal_info: true` to retrieve emails (costs search + email credits).

```
{
  "job_title": "(VP OR Head) Sales",
  "company_size": "201-500",
  "location": "United States",
  "reveal_info": false
}
```

Boolean logic supported: `"(Sales AND CRM) NOT Manager"`

### contactout\_enrich\_domain

Enriches company data (size, industry, funding, HQ) from a domain name.

```
{
  "domain": "salesforce.com"
}
```

## Output shape

`contactout_enrich_person` returns a flat profile object. Email at `email[0]`, `work_email[0]`, or `personal_email[0]`. No nested envelope.`contactout_search_people` returns `{ profiles: [...], metadata: { total_results: N } }`.

## Anti-patterns

- Don’t use Sales Navigator or Recruiter URLs — they’ll return 400
- Don’t skip the free checker tools when enriching a large list — they filter out empty profiles before you spend credits
- Don’t include “http://” or “ [www](http://www/).” in domain values for `enrich_domain`
- Don’t set `reveal_info: true` on search without knowing the count first — use `reveal_info: false` to size the audience


### ContactOut — Endpoints


#### `contactout-check-email-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=contactout_check_email_status:{"profile":"{{profile}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.profile` | `string` | Yes |  | LinkedIn profile URL (linkedin.com/in/ or linkedin.com/pub/). |

**Direct CLI:**

```
deepline tools execute contactout_check_email_status --payload '{
  "profile": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `contactout-check-personal-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=contactout_check_personal_email:{"profile":"{{profile}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.profile` | `string` | Yes |  | Fully formed LinkedIn profile URL. |

**Direct CLI:**

```
deepline tools execute contactout_check_personal_email --payload '{
  "profile": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `contactout-check-phone`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=contactout_check_phone:{"profile":"{{profile}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.profile` | `string` | Yes |  | Fully formed LinkedIn profile URL. |

**Direct CLI:**

```
deepline tools execute contactout_check_phone --payload '{
  "profile": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `contactout-check-work-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=contactout_check_work_email:{"profile":"{{profile}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.profile` | `string` | Yes |  | Fully formed LinkedIn profile URL. |

**Direct CLI:**

```
deepline tools execute contactout_check_work_email --payload '{
  "profile": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `contactout-enrich-domain`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=contactout_enrich_domain:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute contactout_enrich_domain --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `contactout-enrich-person`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=contactout_enrich_person:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute contactout_enrich_person --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `contactout-search-people`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=contactout_search_people:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute contactout_search_people --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.4` per pricing unit.
- Billing mode: `post_deduct`.


---

## Crustdata

*Provider folder: `crustdata` · 11 endpoints*


### Crustdata — Agent Guidance

## Playbook

Use CrustData for structured discovery and enrichment with recall-first filtering.

- Start with free autocomplete (`companydb_autocomplete`, `persondb_autocomplete`) to discover canonical values.
- Default to fuzzy contains operator `(.)`; use strict `[.]` only when explicitly requested.
- Use `crustdata_companydb_search` as the resolver step; for `crustdata_enrich_company`, prefer domain-based inputs instead of name-only payloads.
- For job data, use `crustdata_v2_job_search` for indexed listings or `crustdata_v2_live_job_search` when freshness is required for a known CrustData company id. Legacy job-listing compatibility actions are hidden from discovery and should not be selected for new workflows.
- In changed-company email recovery, use Crust as the second step after LeadMagic and before PDL.
- Keep filters composable and inspect a small sample before adding expensive enrichments.
- If Crust misses in the first 10 rows for a batch, move it later for the rest of that batch.

### Filter format

`filters` accepts an array of condition objects (AND-combined automatically). Each condition: `{"filter_type":"<field>","type":"<operator>","value":"<val>"}` or `{"filter_name":"<field>","type":"<operator>","value":"<val>"}`. `filter_name` is syntactic sugar for `filter_type`; human-friendly aliases (e.g. `company_investors` → `crunchbase_investors`, `company_funding_stage` → `last_funding_round_type`) are auto-mapped. A single condition object (not in array) also works.**Company filter\_type values:**`company_name`, `company_website_domain`, `linkedin_industries`, `hq_country`, `hq_location`, `region`, `year_founded`, `employee_metrics.latest_count`, `employee_count_range`, `employee_metrics.growth_6m_percent`, `employee_metrics.growth_12m_percent`, `employee_metrics.growth_12m`, `follower_metrics.latest_count`, `follower_metrics.growth_6m_percent`, `crunchbase_investors`, `tracxn_investors`, `crunchbase_categories`, `crunchbase_total_investment_usd`, `last_funding_date`, `last_funding_round_type`, `estimated_revenue_lower_bound_usd`, `estimated_revenue_higher_bound_usd`, `linkedin_id`, `linkedin_profile_url`, `company_type`, `acquisition_status`, `ipo_date`, `largest_headcount_country`, `markets`, `competitor_ids`, `competitor_websites`.**Person filter\_type values:**`current_employers.company_website_domain`, `current_employers.title`, `current_employers.seniority_level`, `headline`, `region`, `num_of_connections`, `years_of_experience_raw`.**Operators:**`(.)` = fuzzy contains (default), `[.]` = substring, `=`, `!=`, `in`, `not_in`, `>`, `<`, `=>`, `=<`. Person search also supports `geo_distance` for `region`.**Range filtering:** There is NO range operator like `[100..500]` or `between`. To filter a numeric range, use TWO separate filter conditions with `>` and `<` (or `=>` and `=<`):

```
[\
  {\
    "filter_type": "employee_metrics.latest_count",\
    "type": ">",\
    "value": "100"\
  },\
  {\
    "filter_type": "employee_metrics.latest_count",\
    "type": "<",\
    "value": "500"\
  }\
]
```

**Headcount filtering:** For headcount, prefer `employee_count_range` (string enum like `"51-200"`, `"201-500"`) with the `in` operator when exact buckets work. Use `employee_metrics.latest_count` with `>` / `<` only when you need precise numeric boundaries. Note: `employee_metrics.latest_count` is valid for `sorts` and `filters`, but `employee_count_range` uses string enum values.

### Examples

```
deepline tools execute crustdata_companydb_autocomplete --payload '{"field":"linkedin_industries","query":"software","limit":5}'
```

```
deepline tools execute crustdata_companydb_search --payload '{"filters":[{"filter_type":"linkedin_industries","type":"(.)","value":"software"},{"filter_type":"hq_country","type":"=","value":"USA"}],"limit":5}'
```

```
deepline enrich --input accounts.csv --output accounts.csv.out.csv --with '{"alias":"company_lookup","tool":"crustdata_companydb_autocomplete","payload":{"field":"company_name","query":"{{Company}}","limit":1}}'
```


### Crustdata — Endpoints


#### `company-enrichment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata_company_enrichment:{"companyDomain":"example.com"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute crustdata_company_enrichment --payload '{
  "companyDomain": "example.com"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `companydb-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata_companydb_autocomplete:{"field":"acquisition_status","query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.field` | `"acquisition_status" | "company_name" | "company_type" | "company_website_domain" | "competitor_ids" | "competitor_websites" | "crunchbase_categories" | "crunchbase_investors" | "crunchbase_total_investment_usd" | "employee_count_range" | "employee_metrics.growth_12m" | "employee_metrics.growth_12m_percent" | "employee_metrics.growth_6m_percent" | "employee_metrics.latest_count" | "estimated_revenue_higher_bound_usd" | "estimated_revenue_lower_bound_usd" | "follower_metrics.growth_6m_percent" | "follower_metrics.latest_count" | "hq_country" | "hq_location" | "ipo_date" | "largest_headcount_country" | "last_funding_date" | "last_funding_round_type" | "linkedin_id" | "linkedin_industries" | "linkedin_profile_url" | "markets" | "region" | "tracxn_investors" | "year_founded"` | Yes |  | Field name to autocomplete. Must be one of: acquisition\_status, company\_name, company\_type, company\_website\_domain, competitor\_ids, competitor\_websites, crunchbase\_categories, crunchbase\_investors, crunchbase\_total\_investment\_usd, employee\_count\_range, employee\_metrics.growth\_12m, employee\_metrics.growth\_12m\_percent, employee\_metrics.growth\_6m\_percent, employee\_metrics.latest\_count, estimated\_revenue\_higher\_bound\_usd, estimated\_revenue\_lower\_bound\_usd, follower\_metrics.growth\_6m\_percent, follower\_metrics.latest\_count, hq\_country, hq\_location, ipo\_date, largest\_headcount\_country, last\_funding\_date, last\_funding\_round\_type, linkedin\_id, linkedin\_industries, linkedin\_profile\_url, markets, region, tracxn\_investors, year\_founded. Key mappings: funding stage/round → last\_funding\_round\_type, headcount/size → employee\_count\_range, industry → linkedin\_industries, location → hq\_country (ISO alpha-3) or hq\_location. Note: hq\_country uses 3-letter ISO codes (USA, GBR, CAN), not country names. |
| `payload.query` | `string` | Yes |  | Partial text to match. For hq\_country, use ISO code patterns (e.g. “US”, “USA”, “GBR”) rather than country names. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.field` | `acquisition_status`, `company_name`, `company_type`, `company_website_domain`, `competitor_ids`, `competitor_websites`, `crunchbase_categories`, `crunchbase_investors`, `crunchbase_total_investment_usd`, `employee_count_range`, `employee_metrics.growth_12m`, `employee_metrics.growth_12m_percent`, `employee_metrics.growth_6m_percent`, `employee_metrics.latest_count`, `estimated_revenue_higher_bound_usd`, `estimated_revenue_lower_bound_usd`, `follower_metrics.growth_6m_percent`, `follower_metrics.latest_count`, `hq_country`, `hq_location`, `ipo_date`, `largest_headcount_country`, `last_funding_date`, `last_funding_round_type`, `linkedin_id`, `linkedin_industries`, `linkedin_profile_url`, `markets`, `region`, `tracxn_investors`, `year_founded` |

**Direct CLI:**

```
deepline tools execute crustdata_companydb_autocomplete --payload '{
  "field": "acquisition_status",
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `companydb-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata_companydb_search:{"filters":[{"filter_type":"hq_country","type":"=","value":"USA"}]}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.filters` | `object | array` | Yes |  | Array of filter conditions (AND-combined). Each: {filter\_type, type, value} or {filter\_name, type, value}. filter\_name is syntactic sugar for filter\_type (e.g. company\_investors → crunchbase\_investors, company\_funding\_stage → last\_funding\_round\_type). Single object is accepted. Nested {op,conditions} groups supported for OR logic. Current Crust CompanyDB dot-path field names are accepted and passed through. |

**Direct CLI:**

```
deepline tools execute crustdata_companydb_search --payload '{
  "filters": [\
    {\
      "filter_type": "hq_country",\
      "type": "=",\
      "value": "USA"\
    }\
  ]
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `enrich-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata_enrich_company:{"domain":"example.com"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute crustdata_enrich_company --payload '{
  "domain": "example.com"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `enrich-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata_enrich_contact:{"email":"jane.doe@example.com"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute crustdata_enrich_contact --payload '{
  "email": "jane.doe@example.com"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.2` per pricing unit.
- Billing mode: `post_deduct`.


#### `linkedin-posts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata_linkedin_posts:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | Keyword to search for. |

**Direct CLI:**

```
deepline tools execute crustdata_linkedin_posts --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `people-enrich`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata_people_enrich:{"linkedinProfileUrl":"https://www.linkedin.com/in/example"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute crustdata_people_enrich --payload '{
  "linkedinProfileUrl": "https://www.linkedin.com/in/example"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.2` per pricing unit.
- Billing mode: `post_deduct`.


#### `people-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata_people_search:{"companyDomain":"{{companyDomain}}","titleKeywords":"{{titleKeywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.companyDomain` | `string` | Yes |  | Company website domain to match. |
| `payload.titleKeywords` | `string | array` | Yes |  | Title keyword(s) to match. |

**Direct CLI:**

```
deepline tools execute crustdata_people_search --payload '{
  "companyDomain": "string",
  "titleKeywords": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `person-enrichment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata_person_enrichment:{"linkedinProfileUrl":"https://www.linkedin.com/in/example"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute crustdata_person_enrichment --payload '{
  "linkedinProfileUrl": "https://www.linkedin.com/in/example"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.2` per pricing unit.
- Billing mode: `post_deduct`.


#### `persondb-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata_persondb_autocomplete:{"field":"current_employers.company_website_domain","query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.field` | `"current_employers.company_website_domain" | "current_employers.seniority_level" | "current_employers.title" | "headline" | "num_of_connections" | "region" | "years_of_experience_raw"` | Yes |  | Field name to autocomplete. Must be one of: current\_employers.company\_website\_domain, current\_employers.seniority\_level, current\_employers.title, headline, num\_of\_connections, region, years\_of\_experience\_raw. Key mappings: job title → current\_employers.title, seniority → current\_employers.seniority\_level, location → region, company/employer → current\_employers.company\_website\_domain, experience → years\_of\_experience\_raw, bio/summary → headline. |
| `payload.query` | `string` | Yes |  | Partial text to match. Examples: “san franci” for region, “Eng” for job titles. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.field` | `current_employers.company_website_domain`, `current_employers.seniority_level`, `current_employers.title`, `headline`, `num_of_connections`, `region`, `years_of_experience_raw` |

**Direct CLI:**

```
deepline tools execute crustdata_persondb_autocomplete --payload '{
  "field": "current_employers.company_website_domain",
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `persondb-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata_persondb_search:{"filters":[{"filter_type":"region","type":"(.)","value":"United States"}]}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.filters` | `object | array` | Yes |  | Array of filter conditions (AND-combined). Each: {filter\_type, type, value} or {filter\_name, type, value}. filter\_name is syntactic sugar for filter\_type. Single object is accepted. Nested {op,conditions} groups supported for OR logic. |

**Direct CLI:**

```
deepline tools execute crustdata_persondb_search --payload '{
  "filters": [\
    {\
      "filter_type": "region",\
      "type": "(.)",\
      "value": "United States"\
    }\
  ]
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


---

## Crustdata v2

*Provider folder: `crustdata-v2` · 14 endpoints*


### Crustdata v2 — Agent Guidance

## Playbook

# CrustData V2 Guidance

Use autocomplete before structured CompanyDB or PersonDB searches when a field expects canonical values. Prefer in-DB search for broad list building and reserve realtime search/enrichment for freshness-sensitive cases.For job data, use `crustdata_v2_job_search` for indexed job-listing discovery and `crustdata_v2_live_job_search` when the user explicitly needs current professional-network jobs for one known CrustData company id. Legacy dataset-table compatibility actions are hidden from discovery and should not be selected for new workflows.Do not expose provider credit ladders to customers; Deepline tool pricing should remain Deepline-facing.


### Crustdata v2 — Endpoints


#### `crustdata-v2-company-search-realtime`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_company_search_realtime:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_company_search_realtime --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `crustdata-v2-companydb-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_companydb_autocomplete:{"field":"{{field}}","query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.field` | `string` | Yes |  | The field name to get suggestions for (e.g., “hq\_country”, “linkedin\_industries”) |
| `payload.query` | `string` | Yes |  | Search text to get autocomplete suggestions |

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_companydb_autocomplete --payload '{
  "field": "string",
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `crustdata-v2-companydb-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_companydb_search:{"filters":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.filters` | `object` | Yes |  | Filter conditions for searching companies. Can be a simple condition or complex nested conditions with AND/OR logic. |

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_companydb_search --payload '{
  "filters": {}
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `crustdata-v2-enrich-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_enrich_company:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_enrich_company --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `crustdata-v2-enrich-person`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_enrich_person:{"fields":"{{fields}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.fields` | `string` | Yes | `"linkedin_profile_url,linkedin_flagship_url,name,location,email,title,last_updated,headline,summary,num_of_connections,skills,profile_picture_url,profile_picture_permalink,twitter_handle,languages,all_employers,past_employers,current_employers,education_background,all_employers_company_id,all_titles,all_schools,all_degrees"` | Comma-separated list of fields to return. When omitted, the endpoint returns the default profile fields listed under `default` below. Several fields are opt-in and must be requested explicitly; some incur additional credits. **Opt-in fields (request via `fields=`):** \- `business_email`: business emails at the current employer. +2 credits per profile. - `personal_contact_info.personal_emails`: personal email addresses. +2 credits per profile. Access-gated. - `personal_contact_info.phone_numbers`: phone numbers. +2 credits per profile. Access-gated. - `github_profiles`: ranked GitHub profiles. - `linkedin_open_to_cards`: LinkedIn “Open to work / hiring / pro bono” cards. - `linkedin_joined_date`: date the LinkedIn profile was created. - `linkedin_verifications`: LinkedIn verification badges. - `certifications`, `honors`: self-reported certifications and honors. - `num_of_followers`: follower count. **Always returned regardless of `fields=`:**`enriched_realtime`, `query_linkedin_profile_urn_or_slug`, `person_id`. If you request only an opt-in field (e.g. `fields=business_email`), the response will contain only that field plus the always-returned ones. To get the default profile fields alongside an opt-in field, list both in the `fields=` value. |

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_enrich_person --payload '{
  "fields": "linkedin_profile_url,linkedin_flagship_url,name,location,email,title,last_updated,headline,summary,num_of_connections,skills,profile_picture_url,profile_picture_permalink,twitter_handle,languages,all_employers,past_employers,current_employers,education_background,all_employers_company_id,all_titles,all_schools,all_degrees"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `crustdata-v2-filters-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_filters_autocomplete:{"filter_type":"region","query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.filter_type` | `"region" | "industry"` | Yes |  | Type of filter to get suggestions for |
| `payload.query` | `string` | Yes |  | Search query for autocomplete suggestions |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.filter_type` | `region`, `industry` |

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_filters_autocomplete --payload '{
  "filter_type": "region",
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `crustdata-v2-identify-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_identify_company:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_identify_company --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `crustdata-v2-job-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_job_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_job_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `crustdata-v2-linkedin-posts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_linkedin_posts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_linkedin_posts --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `crustdata-v2-linkedin-posts-keyword-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_linkedin_posts_keyword_search:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | The keyword or phrase to search for in LinkedIn posts |

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_linkedin_posts_keyword_search --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `crustdata-v2-live-job-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_live_job_search:{"crustdata_company_id":"{{crustdata_company_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.crustdata_company_id` | `integer` | Yes |  | CrustData company id to fetch live open jobs for. |

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_live_job_search --payload '{
  "crustdata_company_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `crustdata-v2-people-search-realtime`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_people_search_realtime:{"filters":"{{filters}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.filters` | `array` | Yes |  | Each item contains filter\_type, type, value. For details, see [How to Build Filters](https://docs.crustdata.com/docs/discover/people-apis/how-to-build-people-filters). |

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_people_search_realtime --payload '{
  "filters": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `crustdata-v2-persondb-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_persondb_autocomplete:{"field":"{{field}}","query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.field` | `string` | Yes |  | The field name to get suggestions for (e.g., “region”, “current\_employers.name”) |
| `payload.query` | `string` | Yes |  | Search text to get autocomplete suggestions |

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_persondb_autocomplete --payload '{
  "field": "string",
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `crustdata-v2-persondb-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=crustdata-v2_crustdata_v2_persondb_search:{"filters":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.filters` | `object` | Yes |  | Filter conditions for searching people. Can be a simple condition or complex nested conditions with AND/OR logic. |

**Direct CLI:**

```
deepline tools execute crustdata-v2_crustdata_v2_persondb_search --payload '{
  "filters": {}
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.4` per pricing unit.
- Billing mode: `post_deduct`.


---

## Customer DB

*Provider folder: `customer_db` · 4 endpoints*


### Customer DB — Endpoints


#### `get-customer-db-file`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=customer_db_get_customer_db_file:{"path":"{{path}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.path` | `string` | Yes |  | Workspace file path, for example lead\_scoring/model.json. Paths are scoped to the authenticated workspace. |

**Direct CLI:**

```
deepline tools execute customer_db_get_customer_db_file --payload '{
  "path": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `materialize-free-customer-companies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=customer_db_materialize_free_customer_companies:{"sql":"{{sql}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sql` | `string` | Yes |  | Single read-only SQL statement to execute against the shared free company corpus before upserting rows into enrichments.companies. SQL must include LIMIT, LIMIT must be <= 100000, and the result columns must be limited to: domain, company\_name, industry, location, linkedin\_url, employee\_count, year\_founded. Aggregate-only shapes like count(\*) are rejected. |

**Direct CLI:**

```
deepline tools execute customer_db_materialize_free_customer_companies --payload '{
  "sql": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `query-customer-db`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=customer_db_query_customer_db:{"sql":"{{sql}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sql` | `string` | Yes |  | One SQL statement for the workspace customer DB. Use SELECT/EXPLAIN/read-only WITH to inspect permitted schemas. Any write or DDL must target a schema-qualified storage table, for example CREATE TABLE storage.agent\_notes (…); unqualified CREATE TABLE blocked (…) is rejected. |

**Direct CLI:**

```
deepline tools execute customer_db_query_customer_db --payload '{
  "sql": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `upsert-customer-db-file`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=customer_db_upsert_customer_db_file:{"path":"{{path}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.path` | `string` | Yes |  | Workspace file path, for example lead\_scoring/model.json. Paths are scoped to the authenticated workspace. |

**Direct CLI:**

```
deepline tools execute customer_db_upsert_customer_db_file --payload '{
  "path": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## DataForSEO

*Provider folder: `dataforseo` · 99 endpoints*


### DataForSEO — Agent Guidance

## Playbook

# DataForSEO

Use DataForSEO when you need broad SEO, SERP, content analysis, backlink, or keyword datasets directly from the provider API.

## Auth

- DataForSEO upstream uses HTTP Basic auth.
- Store `DATA_FOR_SEO_API_KEY` as either a raw `login:password`, a pre-encoded base64 token, or a full `Basic ...` header value.

## Execution notes

- Generated POST tools send the payload as a single-task array because DataForSEO task/live endpoints are array-based.
- Generated GET tools pass top-level fields as query parameters.
- Pricing metadata is scraped from DataForSEO pricing pages and may be formula-based rather than fixed per call.

## Generated surface

- Most tool docs, fixtures, sample outputs, mocks, and catalog metadata are generated by `src/lib/integrations/dataforseo/dev/generate-dataforseo-provider.ts`.
- If the docs or schemas drift, update the scraper/generator and regenerate instead of manually editing generated artifacts.


### DataForSEO — Endpoints


#### `dataforseo-ai-optimization-ai-keyword-data-keywords-search-volume-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_ai_optimization_ai_keyword_data_keywords_search_volume_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | keywords required field UTF-8 encoding The maximum number of keywords you can specify: 1000; The maximum number of characters in a single keyword: 250; The keywords will be converted to lowercase format; learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_ai_optimization_ai_keyword_data_keywords_search_volume_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-ai-optimization-chat-gpt-llm-scraper-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_ai_optimization_chat_gpt_llm_scraper_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 2000 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_ai_optimization_chat_gpt_llm_scraper_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `per_page` (per page).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-ai-optimization-chat-gpt-llm-scraper-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_ai_optimization_chat_gpt_llm_scraper_live_html:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 2000 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_ai_optimization_chat_gpt_llm_scraper_live_html --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `per_page` (per page).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-ai-optimization-gemini-llm-scraper-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_ai_optimization_gemini_llm_scraper_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 2000 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_ai_optimization_gemini_llm_scraper_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `per_page` (per page).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-ai-optimization-gemini-llm-scraper-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_ai_optimization_gemini_llm_scraper_live_html:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 2000 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_ai_optimization_gemini_llm_scraper_live_html --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `per_page` (per page).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-app-data-apple-app-listings-search-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_app_data_apple_app_listings_search_live:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_app_data_apple_app_listings_search_live --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-app-data-google-app-listings-search-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_app_data_google_app_listings_search_live:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_app_data_google_app_listings_search_live --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-business-data-business-listings-categories-aggregation-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_business_data_business_listings_categories_aggregation_live:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_business_data_business_listings_categories_aggregation_live --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-business-data-business-listings-search-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_business_data_business_listings_search_live:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_business_data_business_listings_search_live --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-content-analysis-category-trends-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_content_analysis_category_trends_live:{"category_code":"{{category_code}}","date_from":"{{date_from}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.category_code` | `integer` | Yes |  | target category code required field to obtain a full list of available categories, refer to the Categories endpoint |
| `payload.date_from` | `string` | Yes |  | starting date of the time range required field minimum value: 2022-10-31 date format: “yyyy-mm-dd” example: “2019-01-15” |

**Direct CLI:**

```
deepline tools execute dataforseo_content_analysis_category_trends_live --payload '{
  "category_code": "integer",
  "date_from": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-content-analysis-phrase-trends-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_content_analysis_phrase_trends_live:{"keyword":"{{keyword}}","date_from":"{{date_from}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | target keyword required field UTF-8 encoding the keywords will be converted to a lowercase format; Note : to match an exact phrase instead of a stand-alone keyword, use double quotes and backslashes; example: “keyword”: ""tesla palo alto"" learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |
| `payload.date_from` | `string` | Yes |  | starting date of the time range required field date format: “yyyy-mm-dd” example: “2019-01-15” |

**Direct CLI:**

```
deepline tools execute dataforseo_content_analysis_phrase_trends_live --payload '{
  "keyword": "string",
  "date_from": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-content-analysis-rating-distribution-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_content_analysis_rating_distribution_live:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | target keyword required field UTF-8 encoding the keywords will be converted to a lowercase format; Note : to match an exact phrase instead of a stand-alone keyword, use double quotes and backslashes; example: “keyword”: ""tesla palo alto"" learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_content_analysis_rating_distribution_live --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-content-analysis-search-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_content_analysis_search_live:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | target keyword required field UTF-8 encoding the keywords will be converted to a lowercase format; Note : to match an exact phrase instead of a stand-alone keyword, use double quotes and backslashes; example: “keyword”: ""tesla palo alto"" learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_content_analysis_search_live --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-content-analysis-sentiment-analysis-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_content_analysis_sentiment_analysis_live:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | target keyword required field UTF-8 encoding the keywords will be converted to a lowercase format; Note : to match an exact phrase instead of a stand-alone keyword, use double quotes and backslashes; example: “keyword”: ""tesla palo alto"" learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_content_analysis_sentiment_analysis_live --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-content-analysis-summary-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_content_analysis_summary_live:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | target keyword required field UTF-8 encoding the keywords will be converted to a lowercase format; Note : to match an exact phrase instead of a stand-alone keyword, use double quotes and backslashes; example: “keyword”: ""tesla palo alto"" learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_content_analysis_summary_live --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-amazon-bulk-search-volume-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_amazon_bulk_search_volume_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000; the keywords will be converted to lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_amazon_bulk_search_volume_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-amazon-product-competitors-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_amazon_product_competitors_live:{"asin":"{{asin}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.asin` | `string` | Yes |  | product ID required field unique product identifier (ASIN) on Amazon; you can receive the asin parameter by making a separate request to the Amazon Products endpoint |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_amazon_product_competitors_live --payload '{
  "asin": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-amazon-product-keyword-intersections-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_amazon_product_keyword_intersections_live:{"asins":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.asins` | `object` | Yes |  | asins of target products required field product IDs of the products for which you need to find keyword intersections; specify the ASINs as in the following example: “asins”: { “1”: “019005476X”, “2”: “0190074442” } the maximum number of ASINs you can specify in this object is 20; learn more about the parameter on this help center page |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_amazon_product_keyword_intersections_live --payload '{
  "asins": {}
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-amazon-product-rank-overview-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_amazon_product_rank_overview_live:{"asins":"{{asins}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.asins` | `array` | Yes |  | product IDs to compare required field product IDs to receive ranking data for; the maximum number of ASINs you can specify in this array is 1000; you can receive the asin parameter by making a separate request to the Amazon Products endpoint Note: all letters in ASIN code must be specified in uppercase format; example: B01LW2SL7R |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_amazon_product_rank_overview_live --payload '{
  "asins": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-amazon-ranked-keywords-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_amazon_ranked_keywords_live:{"asin":"{{asin}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.asin` | `string` | Yes |  | product ID required field unique product identifier (ASIN) on Amazon; you can receive the asin parameter by making a separate request to the Amazon Products endpoint |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_amazon_ranked_keywords_live --payload '{
  "asin": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-amazon-related-keywords-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_amazon_related_keywords_live:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field UTF-8 encoding the keywords should be specified in the lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_amazon_related_keywords_live --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-apple-app-competitors-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_apple_app_competitors_live:{"app_id":"{{app_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.app_id` | `string` | Yes |  | id of the app required field ID of the mobile application on App Store; you can find the ID in the URL of every app listed on App Store; example: in the URL [https://apps.apple.com/us/app/id835599320](https://apps.apple.com/us/app/id835599320) the id is 835599320 |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_apple_app_competitors_live --payload '{
  "app_id": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-apple-app-intersection-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_apple_app_intersection_live:{"app_ids":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.app_ids` | `object` | Yes |  | ids of the target apps required field IDs of the target mobile applications on App Store; you can find the ID in the URL of every app listed on App Store; example: in the URL [https://apps.apple.com/us/app/id835599320](https://apps.apple.com/us/app/id835599320) the id is 835599320 ; the ids should be specified the following way: “app\_ids”: { “1”: “686449807”, “2”: “382617920” } if you specify a single ID here, the API will return results only for one application; the maximum number of app IDs you can specify in this object is 20 |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_apple_app_intersection_live --payload '{
  "app_ids": {}
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-apple-bulk-app-metrics-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_apple_bulk_app_metrics_live:{"app_ids":"{{app_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.app_ids` | `array` | Yes |  | ids of the apps required field IDs of mobile applications on App Store; you can find the ID in the URL of every app listed on App Store; example: in the URL [https://apps.apple.com/us/app/id835599320](https://apps.apple.com/us/app/id835599320) the id is 835599320 ; the maximum number of IDs you can specify in this field is 1000 |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_apple_bulk_app_metrics_live --payload '{
  "app_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-apple-keywords-for-app-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_apple_keywords_for_app_live:{"app_id":"{{app_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.app_id` | `string` | Yes |  | id of the app required field ID of the mobile application on App Store; you can find the ID in the URL of every app listed on App Store; example: in the URL [https://apps.apple.com/us/app/id835599320](https://apps.apple.com/us/app/id835599320) the id is 835599320 |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_apple_keywords_for_app_live --payload '{
  "app_id": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-app-competitors-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_app_competitors_live:{"app_id":"{{app_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.app_id` | `string` | Yes |  | id of the app required field ID of the mobile application on Google Play; you can find the ID in the URL of every app listed on Google Play; example: in the URL [https://play.google.com/store/apps/details?id=org.telegram.messenger](https://play.google.com/store/apps/details?id=org.telegram.messenger) the id is org.telegram.messenger |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_app_competitors_live --payload '{
  "app_id": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-app-intersection-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_app_intersection_live:{"app_ids":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.app_ids` | `object` | Yes |  | ids of the target apps required field IDs of the target mobile applications on Google Play; you can find the ID in the URL of every app listed on Google Play; example: in the URL [https://play.google.com/store/apps/details?id=org.telegram.messenger](https://play.google.com/store/apps/details?id=org.telegram.messenger) the id is org.telegram.messenger ;; the ids should be specified the following way: “app\_ids”: { “1”: “org.telegram.messenger”, “2”: “com.zhiliaoapp.musically” } if you specify a single ID here, the API will return results only for one application; the maximum number of app IDs you can specify in this object is 20 |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_app_intersection_live --payload '{
  "app_ids": {}
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-bulk-app-metrics-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_bulk_app_metrics_live:{"app_ids":"{{app_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.app_ids` | `array` | Yes |  | ids of the app required field IDs of the mobile applications on Google Play; you can find the ID in the URL of every app listed on Google Play; example: in the URL [https://play.google.com/store/apps/details?id=org.telegram.messenger](https://play.google.com/store/apps/details?id=org.telegram.messenger) the id is org.telegram.messenger ; the maximum number of IDs you can specify in this field is 1000 |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_bulk_app_metrics_live --payload '{
  "app_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-bulk-keyword-difficulty-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_bulk_keyword_difficulty_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000 the keywords will be converted to lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_bulk_keyword_difficulty_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-bulk-traffic-estimation-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_bulk_traffic_estimation_live:{"targets":"{{targets}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.targets` | `array` | Yes |  | target domains, subdomains, and webpages required field you can specify domains, subdomains, and webpages in this field; domains and subdomains should be specified without https:// and [www](http://www/). ; pages should be specified with absolute URL, including https:// and [www](http://www/). ; you can set up to 1000 domains, subdomains or webpages |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_bulk_traffic_estimation_live --payload '{
  "targets": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-categories-for-domain-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_categories_for_domain_live:{"target":"{{target}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `string` | Yes |  | domain or subdomain required field the domain or subdomain name of the target website the domain or subdomain should be specified without https:// and [www](http://www/). |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_categories_for_domain_live --payload '{
  "target": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-categories-for-keywords-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_categories_for_keywords_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000 the keywords will be converted to lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_categories_for_keywords_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-domain-intersection-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_domain_intersection_live:{"target1":"{{target1}}","target2":"{{target2}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target1` | `string` | Yes |  | domain required field the domain name of the first target website the domain should be specified without https:// and [www](http://www/). |
| `payload.target2` | `string` | Yes |  | domain required field the domain name of the second target website the domain should be specified without https:// and [www](http://www/). |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_domain_intersection_live --payload '{
  "target1": "string",
  "target2": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-domain-metrics-by-categories-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_domain_metrics_by_categories_live:{"category_codes":"{{category_codes}}","first_date":"{{first_date}}","second_date":"{{second_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.category_codes` | `array` | Yes |  | product and service categories required field The maximum number of categories you can specify: 5 you can download the full list of possible categories |
| `payload.first_date` | `string` | Yes |  | first date of comparison period required field first date for which domain metrics will be provided; date format: “yyyy-mm-dd” ; example: “2021-06-01” ; the list available dates is available through the available history endpoint ; Note: first\_date cannot be greater than today’s date; Also note: the dates specified in first\_date and second\_date cannot point to the same month of the same year; you can specify the dates in any order: first\_date can be greater than second\_date and vice versa; minimum date: “2020-10-01” |
| `payload.second_date` | `string` | Yes |  | second date of comparison period required field second date for which domain metrics will be provided; date format: “yyyy-mm-dd” ; example: “2021-10-01” ; the list available dates is available through the available history endpoint ; Note: second\_date cannot be greater than today’s date; Also note: the dates specified in first\_date and second\_date cannot point to the same month of the same year; you can specify the dates in any order: second\_date can be greater than first\_date and vice versa; minimum date: “2020-10-01” |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_domain_metrics_by_categories_live --payload '{
  "category_codes": "array",
  "first_date": "string",
  "second_date": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-domain-rank-overview-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_domain_rank_overview_live:{"target":"{{target}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `string` | Yes |  | domain required field the domain name of the target website the domain should be specified without https:// and [www](http://www/). |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_domain_rank_overview_live --payload '{
  "target": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-historical-bulk-traffic-estimation-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_historical_bulk_traffic_estimation_live:{"targets":"{{targets}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.targets` | `array` | Yes |  | target domains and subdomains required field you can specify domains and subdomains in this field; domains and subdomains should be specified without https:// and [www](http://www/). ; you can set up to 1000 domains or subdomains |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_historical_bulk_traffic_estimation_live --payload '{
  "targets": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-historical-keyword-data-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_historical_keyword_data_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | keywords required field The maximum number of keywords you can specify: 700 The maximum number of characters for each keyword: 80 The maximum number of words for each keyword phrase: 10 the specified keywords will be converted to lowercase format, data will be provided in a separate array note that if some of the keywords specified in this array are omitted in the results you receive, then our database doesn’t contain such keywords and cannot return data on them you will not be charged for the keywords omitted in the results learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_historical_keyword_data_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-historical-rank-overview-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_historical_rank_overview_live:{"target":"{{target}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `string` | Yes |  | domain required field the domain name of the target website the domain should be specified without https:// and [www](http://www/). |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_historical_rank_overview_live --payload '{
  "target": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-historical-serps-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_historical_serps_live:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field; all %## will be decoded (plus character ‘+’ will be decoded to a space character); if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_historical_serps_live --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-keyword-ideas-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_keyword_ideas_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | keywords required field UTF-8 encoding The maximum number of keywords you can specify: 200. The keywords will be converted to lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_keyword_ideas_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-keyword-overview-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_keyword_overview_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | keywords required field The maximum number of keywords you can specify: 700 The maximum number of characters for each keyword: 80 The maximum number of words for each keyword phrase: 10 the specified keywords will be converted to lowercase format, data will be provided in a separate array note that if some of the keywords specified in this array are omitted in the results you receive, then our database doesn’t contain such keywords and cannot return data on them you will not be charged for the keywords omitted in the results learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_keyword_overview_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-keyword-suggestions-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_keyword_suggestions_live:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field UTF-8 encoding the keywords will be converted to lowercase format; learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_keyword_suggestions_live --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-keywords-for-app-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_keywords_for_app_live:{"app_id":"{{app_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.app_id` | `string` | Yes |  | id of the apps required field ID of the mobile application on Google Play; you can find the ID in the URL of every app listed on Google Play; example: in the URL [https://play.google.com/store/apps/details?id=org.telegram.messenger](https://play.google.com/store/apps/details?id=org.telegram.messenger) the id is org.telegram.messenger |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_keywords_for_app_live --payload '{
  "app_id": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-keywords-for-categories-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_keywords_for_categories_live:{"category_codes":"{{category_codes}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.category_codes` | `array` | Yes |  | product and service categories required field The maximum number of categories you can specify: 20 you can download the full list of possible categories |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_keywords_for_categories_live --payload '{
  "category_codes": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-keywords-for-site-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_keywords_for_site_live:{"target":"{{target}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `string` | Yes |  | target domain required field the domain name of the target website the domain should be specified without https:// |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_keywords_for_site_live --payload '{
  "target": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-page-intersection-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_page_intersection_live:{"pages":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.pages` | `object` | Yes |  | target URLs of pages required field you can set up to 20 pages in this object the pages should be specified with absolute URLs (including http:// or https://) example: “pages”: { “1”:“ [https://www.apple.com/mac/](https://www.apple.com/mac/) _”, “2”:“ [https://dataforseo.com/](https://dataforseo.com/)_”, “3”:“ [https://support.microsoft.com/](https://support.microsoft.com/)” } if you specify a single page here, we will return results only for this page; you can also use a wildcard (' _') character to specify the search pattern example: “example.com” search for the exact URL “example.com/eng/_” search for the example.com page and all its related URLs which start with ‘/eng/’ , such as “example.com/eng/index.html” and “example.com/eng/help/” , etc. note: a wilcard should be placed after the slash (’/’) character in the end of the URL, it is not possible to place it after the domain in the following way: [https://dataforseo.com](https://dataforseo.com/) \\* use [https://dataforseo.com](https://dataforseo.com/) /\\* instead Note: this endpoint will not provide results if the number of intersecting keywords exceeds 10 million |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_page_intersection_live --payload '{
  "pages": {}
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-ranked-keywords-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_ranked_keywords_live:{"target":"{{target}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `string` | Yes |  | domain name or page url required field the domain name of the target website or URL of the target webpage; the domain name must be specified without https:// or [www](http://www/). ; the webpage URL must be specified with https:// or [www](http://www/). Note: if you specify the webpage URL without https:// or [www](http://www/). , the result will be returned for the entire domain rather than the specific page |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_ranked_keywords_live --payload '{
  "target": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-related-keywords-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_related_keywords_live:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field UTF-8 encoding the keywords will be converted to lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_related_keywords_live --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-relevant-pages-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_relevant_pages_live:{"target":"{{target}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `string` | Yes |  | domain required field the domain name of the target website the domain should be specified without https:// and [www](http://www/). |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_relevant_pages_live --payload '{
  "target": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-search-intent-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_search_intent_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000; the keywords will be converted to lowercase format learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_search_intent_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-serp-competitors-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_serp_competitors_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | keywords array required field the results will be based on the keywords you specify in this array UTF-8 encoding; the keywords will be converted to lowercase format; you can specify the maximum of 200 keywords learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_serp_competitors_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-subdomains-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_subdomains_live:{"target":"{{target}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `string` | Yes |  | domain required field the domain name of the target website the domain should be specified without https:// and [www](http://www/). |

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_subdomains_live --payload '{
  "target": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-dataforseo-labs-google-top-searches-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_dataforseo_labs_google_top_searches_live:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_dataforseo_labs_google_top_searches_live --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-domain-analytics-technologies-aggregation-technologies-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_domain_analytics_technologies_aggregation_technologies_live:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_domain_analytics_technologies_aggregation_technologies_live --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-domain-analytics-technologies-domain-technologies-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_domain_analytics_technologies_domain_technologies_live:{"target":"{{target}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.target` | `string` | Yes |  | target domain required field domain name of the website to analyze Note: results will be returned for the specified domain only |

**Direct CLI:**

```
deepline tools execute dataforseo_domain_analytics_technologies_domain_technologies_live --payload '{
  "target": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-domain-analytics-technologies-domains-by-html-terms-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_domain_analytics_technologies_domains_by_html_terms_live:{"search_terms":"{{search_terms}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.search_terms` | `array` | Yes |  | target search terms required field specify target HTML elements, tags, attributes, their content or all of the above if you specify more than one search term, you will receive only the domains containing all of the specified terms in the HTML code of their homepage maximum number of search terms you can specify: 10 example: \[“data-attrid”\] |

**Direct CLI:**

```
deepline tools execute dataforseo_domain_analytics_technologies_domains_by_html_terms_live --payload '{
  "search_terms": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-domain-analytics-technologies-domains-by-technology-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_domain_analytics_technologies_domains_by_technology_live:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_domain_analytics_technologies_domains_by_technology_live --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-domain-analytics-technologies-technologies-summary-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_domain_analytics_technologies_technologies_summary_live:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_domain_analytics_technologies_technologies_summary_live --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-domain-analytics-technologies-technology-stats-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_domain_analytics_technologies_technology_stats_live:{"technology":"{{technology}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.technology` | `string` | Yes |  | target technology required field you can find the full list of technologies you can specify here on this page example: “Salesforce” |

**Direct CLI:**

```
deepline tools execute dataforseo_domain_analytics_technologies_technology_stats_live --payload '{
  "technology": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-domain-analytics-whois-overview-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_domain_analytics_whois_overview_live:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_domain_analytics_whois_overview_live --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-keywords-data-clickstream-data-bulk-search-volume-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_keywords_data_clickstream_data_bulk_search_volume_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000; each keyword should be at least 3 characters long; the keywords will be converted to lowercase format; Note: certain symbols and characters (e.g., UTF symbols, emojis) are not allowed to learn more about which symbols and characters can be used, please refer to this article learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_keywords_data_clickstream_data_bulk_search_volume_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-keywords-data-clickstream-data-dataforseo-search-volume-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_keywords_data_clickstream_data_dataforseo_search_volume_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000 the keywords will be converted to lowercase format Note: certain symbols and characters (e.g., UTF symbols, emojis) are not allowed to learn more about which symbols and characters can be used, please refer to this article learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_keywords_data_clickstream_data_dataforseo_search_volume_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-keywords-data-clickstream-data-global-search-volume-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_keywords_data_clickstream_data_global_search_volume_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | target keywords required field UTF-8 encoding maximum number of keywords you can specify in this array: 1000; each keyword should be at least 3 characters long; the keywords will be converted to lowercase format; Note: certain symbols and characters (e.g., UTF symbols, emojis) are not allowed to learn more about which symbols and characters can be used, please refer to this article learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_keywords_data_clickstream_data_global_search_volume_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-keywords-data-dataforseo-trends-demography-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_keywords_data_dataforseo_trends_demography_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | keywords required field the maximum number of keywords you can specify: 5 avoid symbols and special characters (e.g., UTF symbols, emojis); specifying non-Latin characters, you’ll get data for the countries where they are used learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_keywords_data_dataforseo_trends_demography_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-keywords-data-dataforseo-trends-explore-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_keywords_data_dataforseo_trends_explore_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | keywords required field the maximum number of keywords you can specify: 5 learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_keywords_data_dataforseo_trends_explore_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-keywords-data-dataforseo-trends-merged-data-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_keywords_data_dataforseo_trends_merged_data_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | keywords required field the maximum number of keywords you can specify: 5 avoid symbols and special characters (e.g., UTF symbols, emojis); specifying non-Latin characters, you’ll get data for the countries where they are used learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_keywords_data_dataforseo_trends_merged_data_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-keywords-data-dataforseo-trends-subregion-interests-live`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_keywords_data_dataforseo_trends_subregion_interests_live:{"keywords":"{{keywords}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keywords` | `array` | Yes |  | keywords required field the maximum number of keywords you can specify: 5 avoid symbols and special characters (e.g., UTF symbols, emojis); specifying non-Latin characters, you’ll get data for the countries where they are used learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_keywords_data_dataforseo_trends_subregion_interests_live --payload '{
  "keywords": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.01` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-on-page-lighthouse-live-json`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_on_page_lighthouse_live_json:{"url":"{{url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.url` | `string` | Yes |  | target URL required field target page should be specified with its absolute URL (including http:// or https://) example: [https://dataforseo.com/](https://dataforseo.com/) |

**Direct CLI:**

```
deepline tools execute dataforseo_on_page_lighthouse_live_json --payload '{
  "url": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-bing-organic-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_bing_organic_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_bing_organic_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-bing-organic-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_bing_organic_live_html:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_bing_organic_live_html --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-bing-organic-live-regular`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_bing_organic_live_regular:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_bing_organic_live_regular --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-ads-advertisers-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_ads_advertisers_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_ads_advertisers_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-ads-search-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_ads_search_live_advanced:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_ads_search_live_advanced --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-ai-mode-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_ai_mode_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_ai_mode_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-ai-mode-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_ai_mode_live_html:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_ai_mode_live_html --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-autocomplete-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_autocomplete_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_autocomplete_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-dataset-info-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_dataset_info_live_advanced:{"dataset_id":"{{dataset_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.dataset_id` | `string` | Yes |  | ID of the dataset required field you can find dataset ID in the dataset URL or dataset item of Google Dataset Search result example: L2cvMTFqbl85ZHN6MQ== |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_dataset_info_live_advanced --payload '{
  "dataset_id": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-dataset-search-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_dataset_search_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_dataset_search_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-events-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_events_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_events_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-finance-explore-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_finance_explore_live_advanced:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_finance_explore_live_advanced --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-finance-explore-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_finance_explore_live_html:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_finance_explore_live_html --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-finance-markets-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_finance_markets_live_advanced:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_finance_markets_live_advanced --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-finance-markets-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_finance_markets_live_html:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_finance_markets_live_html --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-finance-quote-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_finance_quote_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | ticker or stock symbol required field in this field you can pass the ticker symbol of publicly traded shares of a particular stock or security on a particular stock exchange; you can specify up to 700 characters in the keyword field; all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_finance_quote_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-finance-quote-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_finance_quote_live_html:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | ticker or stock symbol required field in this field you can pass the ticker symbol of publicly traded shares of a particular stock or security on a particular stock exchange; you can specify up to 700 characters in the keyword field; all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_finance_quote_live_html --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-finance-ticker-search-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_finance_ticker_search_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | company or financial instrument name required field in this field, you can enter the name of a company or financial instrument to search for relevant tickers; you can specify up to 700 characters in the keyword field; all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_finance_ticker_search_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-images-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_images_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; if this field contains such parameters as ‘allinanchor:’, ‘allintext:’, ‘allintitle:’, ‘allinurl:’, ‘define:’, ‘filetype:’, ‘id:’, ‘inanchor:’, ‘info:’, ‘intext:’, ‘intitle:’, ‘inurl:’, ‘link:’, ‘related:’, ‘site:’ , the charge per task will be multiplied by 5 Note: queries containing the ‘cache:’ parameter are not supported and will return a validation error learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_images_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-images-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_images_live_html:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; if this field contains such parameters as ‘allinanchor:’, ‘allintext:’, ‘allintitle:’, ‘allinurl:’, ‘define:’, ‘filetype:’, ‘id:’, ‘inanchor:’, ‘info:’, ‘intext:’, ‘intitle:’, ‘inurl:’, ‘link:’, ‘related:’, ‘site:’ , the charge per task will be multiplied by 5 Note: queries containing the ‘cache:’ parameter are not supported and will return a validation error learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_images_live_html --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-local-finder-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_local_finder_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_local_finder_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-local-finder-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_local_finder_live_html:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_local_finder_live_html --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-maps-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_maps_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; if this field contains such parameters as ‘allinanchor:’, ‘allintext:’, ‘allintitle:’, ‘allinurl:’, ‘define:’, ‘filetype:’, ‘id:’, ‘inanchor:’, ‘info:’, ‘intext:’, ‘intitle:’, ‘inurl:’, ‘link:’, ‘related:’, ‘site:’ , the charge per task will be multiplied by 5 Note: queries containing the ‘cache:’ parameter are not supported and will return a validation error learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_maps_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-news-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_news_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; if this field contains such parameters as ‘allinanchor:’, ‘allintext:’, ‘allintitle:’, ‘allinurl:’, ‘define:’, ‘filetype:’, ‘id:’, ‘inanchor:’, ‘info:’, ‘intext:’, ‘intitle:’, ‘inurl:’, ‘link:’, ‘related:’, ‘site:’ , the charge per task will be multiplied by 5 Note: queries containing the ‘cache:’ parameter are not supported and will return a validation error learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_news_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-news-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_news_live_html:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; if this field contains such parameters as ‘allinanchor:’, ‘allintext:’, ‘allintitle:’, ‘allinurl:’, ‘define:’, ‘filetype:’, ‘id:’, ‘inanchor:’, ‘info:’, ‘intext:’, ‘intitle:’, ‘inurl:’, ‘link:’, ‘related:’, ‘site:’ , the charge per task will be multiplied by 5 Note: queries containing the ‘cache:’ parameter are not supported and will return a validation error learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_news_live_html --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-organic-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_organic_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ‘+’ will be decoded to a space character) if you need to use the “%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; if this field contains such parameters as ‘allinanchor:’, ‘allintext:’, ‘allintitle:’, ‘allinurl:’, ‘define:’, ‘definition:’, ‘filetype:’, ‘id:’, ‘inanchor:’, ‘info:’, ‘intext:’, ‘intitle:’, ‘inurl:’, ‘link:’, ‘site:’ , the charge per task will be multiplied by 5 Note: queries containing the ‘cache:’ parameter are not supported and will return a validation errorlearn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_organic_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-organic-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_organic_live_html:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; if this field contains such parameters as ‘allinanchor:’, ‘allintext:’, ‘allintitle:’, ‘allinurl:’, ‘define:’, ‘filetype:’, ‘id:’, ‘inanchor:’, ‘info:’, ‘intext:’, ‘intitle:’, ‘inurl:’, ‘link:’, ‘related:’, ‘site:’ , the charge per task will be multiplied by 5 Note: queries containing the ‘cache:’ parameter are not supported and will return a validation error |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_organic_live_html --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-google-organic-live-regular`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_google_organic_live_regular:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B”; if this field contains such parameters as ‘allinanchor:’, ‘allintext:’, ‘allintitle:’, ‘allinurl:’, ‘define:’, ‘filetype:’, ‘id:’, ‘inanchor:’, ‘info:’, ‘intext:’, ‘intitle:’, ‘inurl:’, ‘link:’, ‘site:’ , the charge per task will be multiplied by 5 Note: queries containing the ‘cache:’ parameter are not supported and will return a validation error |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_google_organic_live_regular --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-yahoo-organic-live-advanced`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_yahoo_organic_live_advanced:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_yahoo_organic_live_advanced --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-yahoo-organic-live-html`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_yahoo_organic_live_html:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_yahoo_organic_live_html --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `dataforseo-serp-yahoo-organic-live-regular`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dataforseo_serp_yahoo_organic_live_regular:{"keyword":"{{keyword}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.keyword` | `string` | Yes |  | keyword required field you can specify up to 700 characters in the keyword field all %## will be decoded (plus character ’+’ will be decoded to a space character) if you need to use the ”%” character for your keyword , please specify it as “%25”; if you need to use the “+” character for your keyword , please specify it as “%2B” learn more about rules and limitations of keyword and keywords fields in DataForSEO APIs in this Help Center article |

**Direct CLI:**

```
deepline tools execute dataforseo_serp_yahoo_organic_live_regular --payload '{
  "keyword": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


---

## Datagma

*Provider folder: `datagma` · 7 endpoints*


### Datagma — Agent Guidance

## Playbook

# Datagma Workflow Guidance

Datagma is strongest when you need real-time enrichment rather than a static
contact database. It is especially useful for direct mobile numbers, international
coverage, and job-change validation.

## When to use Datagma

- Use `datagma_full_enrichment` when you have a strong identifier such as a
LinkedIn URL, a professional email, or a domain-backed full name.
- `datagma_enrich_person` and `datagma_enrich_company` remain valid compatibility aliases when older workflows expect the flat legacy Datagma response shape.
- Use `datagma_find_email` when you only need a verified work email and want a
narrower, cheaper workflow than full enrichment.
- Use `datagma_search_phone_numbers` when you already have an email or social URL
and want direct mobile numbers.
- Use `datagma_job_change_detection` before outreach refreshes when you need to
confirm whether a contact is still at the same company.
- Use `datagma_find_people` to source up to 10 people by title inside a target company.

## Input strategy

1. LinkedIn URL or company domain
2. Professional email
3. Full name plus company context
4. Company-name-only lookups only when nothing stronger is available

Datagma’s own docs emphasize that LinkedIn URL and domain-backed inputs are the
most reliable. Prefer those over plain company-name searches.

## Billing behavior to remember

- Public pricing currently states `1 credit = 1 verified email`.
- Public pricing currently states `30 credits = 1 mobile phone number`.
- `datagma_find_people` is explicitly documented as 10 credits on success and
1 credit on a no-result response.
- `datagma_full_enrichment`, `datagma_job_change_detection`, and
`datagma_search_phone_numbers` expose `creditBurn` in the response. Deepline
uses that vendor-reported value rather than guessing.
- Catch-all email results are free on the public pricing page.

## Endpoint guidance

### `datagma_find_email`

Use for:

- verified work-email lookup from name + company context

Best inputs:

- `firstName` \+ `lastName` \+ `companyDomain`
- or `fullName` \+ `companyDomain`
- optionally `linkedInSlug` when you have the company LinkedIn slug

Canonical Deepline snake\_case column names map straight through, so CSV columns
named `first_name` / `last_name` / `full_name` / `company_name` /
`company_domain` / `domain` are accepted as input aliases for their camelCase
equivalents (the bare `domain` column is treated as the company web domain).

### `datagma_full_enrichment`

Use for:

- person or company enrichment
- firmographics plus person details in one pass
- real-time phone/email/company expansion

Best inputs:

- `data` set to a LinkedIn URL or professional email
- `fullName` or `firstName` \+ `lastName` only when paired with `data`

Important:

- `phoneFull=true` should be reserved for cases where you do not already have
a social profile or email, matching Datagma’s docs guidance.

### `datagma_job_change_detection`

Use for:

- validating whether a contact is still at the same company

Best inputs:

- `fullName` \+ `companyName`
- add `jobTitle` when the contact name may be ambiguous

### `datagma_find_people`

Use for:

- prospecting inside one company by role title

Best inputs:

- `currentJobTitle`
- plus one of `linkedinId`, `domain`, or `currentCompanies`

### `datagma_search_phone_numbers`

Use for:

- direct mobile-number search from an email or profile URL

Best inputs:

- both `email` and `username` when you have them
- Datagma explicitly recommends passing both together when possible

## Internal-only endpoints

Datagma also exposes reverse-email, reverse-phone, and Twitter lookup endpoints.
They remain internal in this repo because the current public docs do not disclose
standalone pricing for them. Do not move them to the public tool surface until
pricing is verified against live credentials or direct vendor confirmation.


### Datagma — Endpoints


#### `datagma-enrich-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=datagma_enrich_company:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute datagma_enrich_company --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.27` per pricing unit.
- Billing mode: `post_deduct`.


#### `datagma-enrich-person`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=datagma_enrich_person:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute datagma_enrich_person --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.27` per pricing unit.
- Billing mode: `post_deduct`.


#### `datagma-find-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=datagma_find_email:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute datagma_find_email --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `datagma-find-people`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=datagma_find_people:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute datagma_find_people --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.31` per pricing unit.
- Billing mode: `post_deduct`.


#### `datagma-full-enrichment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=datagma_full_enrichment:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute datagma_full_enrichment --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `datagma-job-change-detection`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=datagma_job_change_detection:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute datagma_job_change_detection --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `datagma-search-phone-numbers`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=datagma_search_phone_numbers:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute datagma_search_phone_numbers --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


---

## Deepline Agent

*Provider folder: `deeplineagent` · 2 endpoints*


### Deepline Agent — Agent Guidance

## Playbook

Use `ai_inference` for plain text or structured-output model calls with no tool use.Use `deeplineagent` when the task benefits from streaming output and tool use across the current whitelist: `serper_google_search`, `exa_search`, `firecrawl_scrape`, `firecrawl_map`, `firecrawl_crawl`, and `bash`.For research tasks, prefer an adaptive loop: cheap Serper search first, synthesize, then only run targeted Exa follow-up searches if key gaps remain.When using `exa_search`, prefer `type: "auto"` with `contents.highlights` and no `contents.summary`; highlights are token-efficient snippets, while summaries add an extra AI pass.Use Firecrawl only after you know the target site or section. Prefer `firecrawl_scrape` for one known URL, `firecrawl_map` for URL inventory before crawling, and keep `firecrawl_crawl` tightly scoped with explicit low `limit`, `includePaths`/`excludePaths`, or `maxDiscoveryDepth`. Firecrawl work scales with discovered pages, crawled pages, and requested modifiers, so do not run broad speculative crawls.When `bash` is enabled, `/refs/prompts.json` is available as a lazily loaded reference file for GTM prompt-template lookup.Prefer Deepline-native tools over freeform bash when structured provider actions already exist.


### Deepline Agent — Endpoints


#### `ai_inference`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deeplineagent_ai_inference:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute deeplineagent_ai_inference --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `deeplineagent`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deeplineagent_deeplineagent:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute deeplineagent_deeplineagent --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


---

## Deepline Native

*Provider folder: `deepline_native` · 52 endpoints*


### Deepline Native — Endpoints


#### `company-domain-to-individual`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_domain_to_individual:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Company domain. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_domain_to_individual --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-domain-to-individual-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_domain_to_individual_email:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Company domain. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_domain_to_individual_email --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-domain-to-individual-first-last`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_domain_to_individual_first_last:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Company domain. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_domain_to_individual_first_last --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-domain-to-individual-linkedin-url`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_domain_to_individual_linkedin_url:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Company domain. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_domain_to_individual_linkedin_url --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-linkedin-url-to-individual`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_linkedin_url_to_individual:{"linkedin_company_url":"{{linkedin_company_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_company_url` | `string` | Yes |  | LinkedIn company URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_linkedin_url_to_individual --payload '{
  "linkedin_company_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-linkedin-url-to-individual-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_linkedin_url_to_individual_email:{"linkedin_company_url":"{{linkedin_company_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_company_url` | `string` | Yes |  | LinkedIn company URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_linkedin_url_to_individual_email --payload '{
  "linkedin_company_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-linkedin-url-to-individual-first-last`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_linkedin_url_to_individual_first_last:{"linkedin_company_url":"{{linkedin_company_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_company_url` | `string` | Yes |  | LinkedIn company URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_linkedin_url_to_individual_first_last --payload '{
  "linkedin_company_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-linkedin-url-to-individual-linkedin-url`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_linkedin_url_to_individual_linkedin_url:{"linkedin_company_url":"{{linkedin_company_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_company_url` | `string` | Yes |  | LinkedIn company URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_linkedin_url_to_individual_linkedin_url --payload '{
  "linkedin_company_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-name-to-individual`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_name_to_individual:{"company_name":"{{company_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_name` | `string` | Yes |  | Company name. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_name_to_individual --payload '{
  "company_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-name-to-individual-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_name_to_individual_email:{"company_name":"{{company_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_name` | `string` | Yes |  | Company name. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_name_to_individual_email --payload '{
  "company_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-name-to-individual-first-last`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_name_to_individual_first_last:{"company_name":"{{company_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_name` | `string` | Yes |  | Company name. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_name_to_individual_first_last --payload '{
  "company_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-name-to-individual-linkedin-url`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_name_to_individual_linkedin_url:{"company_name":"{{company_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_name` | `string` | Yes |  | Company name. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_name_to_individual_linkedin_url --payload '{
  "company_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-titles`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_titles:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute deepline_native_company_titles --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-to-contact-by-role-waterfall`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_company_to_contact_by_role_waterfall:{"roles":"{{roles}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.roles` | `array | string` | Yes |  | Primary persona intent for contact lookup. Accepts one or more role strings as an array or comma-separated string. Prefer exact, real-world title tokens when you know them. |

**Direct CLI:**

```
deepline tools execute deepline_native_company_to_contact_by_role_waterfall --payload '{
  "roles": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Waterfall orchestration is free. Individual provider lookups within the waterfall are charged at their listed rates (see provider pricing above).
- Typical cost: 0.3-1.0 credits for email waterfalls, 1-5 credits for phone waterfalls.
- Billing mode: `no_bill`.


#### `engagers-to-icp-qualification`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_engagers_to_icp_qualification:{"first_name":"{{first_name}}","last_name":"{{last_name}}","position":"{{position}}","icp_description":"{{icp_description}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name |
| `payload.last_name` | `string` | Yes |  | Contact last name |
| `payload.position` | `string` | Yes |  | Current position/headline |
| `payload.icp_description` | `string` | Yes |  | ICP criteria for tier1/tier2/tier3 classification |

**Direct CLI:**

```
deepline tools execute deepline_native_engagers_to_icp_qualification --payload '{
  "first_name": "string",
  "last_name": "string",
  "position": "string",
  "icp_description": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `enrich-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_enrich_company:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute deepline_native_enrich_company --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.98` per pricing unit.
- Billing mode: `pre_reserve`.


#### `enrich-company-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_enrich_company_finder:{"job_id":"{{job_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.job_id` | `string` | Yes |  | The Waterfall job id used by finder endpoints. |

**Direct CLI:**

```
deepline tools execute deepline_native_enrich_company_finder --payload '{
  "job_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- The finder/dispatcher step itself is free. The underlying enrichment step is charged at its listed rate.
- Billing mode: `no_bill`.


#### `enrich-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_enrich_contact:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute deepline_native_enrich_contact --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.98` per pricing unit.
- Billing mode: `post_deduct`.


#### `enrich-contact-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_enrich_contact_finder:{"job_id":"{{job_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.job_id` | `string` | Yes |  | The Waterfall job id used by finder endpoints. |

**Direct CLI:**

```
deepline tools execute deepline_native_enrich_contact_finder --payload '{
  "job_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- The finder/dispatcher step itself is free. The underlying enrichment step is charged at its listed rate.
- Billing mode: `no_bill`.


#### `enrich-phone`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_enrich_phone:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute deepline_native_enrich_phone --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `4.9` per pricing unit.
- Billing mode: `post_deduct`.


#### `enrich-phone-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_enrich_phone_finder:{"job_id":"{{job_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.job_id` | `string` | Yes |  | The Waterfall job id used by finder endpoints. |

**Direct CLI:**

```
deepline tools execute deepline_native_enrich_phone_finder --payload '{
  "job_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- The finder/dispatcher step itself is free. The underlying enrichment step is charged at its listed rate.
- Billing mode: `no_bill`.


#### `free-simple-company-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_free_simple_company_search:{"sql":"{{sql}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sql` | `string` | Yes |  | Single read-only SQL statement against the Snowflake-backed companies table (~35M rows). Columns: normalized\_domain, domain, company\_name, industry, location, linkedin\_url, employee\_count (integer), year\_founded (integer), updated\_at. Prefer exact normalized\_domain equality or IN lookups when you have domains. Use linkedin\_url and company\_name equality/IN for small lookup batches. For fallback candidates, prefer anchored prefixes like company\_name ILIKE ‘acme%’. Broad unanchored ILIKE ‘%keyword%’ scans are legal but can scan the full corpus; use dedicated company search providers for broad keyword discovery or strict market sizing. Supports SELECT, aggregates, GROUP BY, CTEs, EXPLAIN. SQL must include LIMIT <= 100000. Example: SELECT \* FROM companies WHERE normalized\_domain IN (‘openai.com’,‘anthropic.com’) LIMIT 10 |

**Direct CLI:**

```
deepline tools execute deepline_native_free_simple_company_search --payload '{
  "sql": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `job-change`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_job_change:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute deepline_native_job_change --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.96` per pricing unit.
- Billing mode: `post_deduct`.


#### `linkedin-post-to-engagers`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_linkedin_post_to_engagers:{"post_url":"{{post_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.post_url` | `string` | Yes |  | LinkedIn post URL |

**Direct CLI:**

```
deepline tools execute deepline_native_linkedin_post_to_engagers --payload '{
  "post_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `name-and-domain-to-email-waterfall`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_name_and_domain_to_email_waterfall:{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name. |
| `payload.last_name` | `string` | Yes |  | Contact last name. |
| `payload.domain` | `string` | Yes |  | Company domain (for example acme.com). |

**Direct CLI:**

```
deepline tools execute deepline_native_name_and_domain_to_email_waterfall --payload '{
  "first_name": "string",
  "last_name": "string",
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Waterfall orchestration is free. Individual provider lookups within the waterfall are charged at their listed rates (see provider pricing above).
- Typical cost: 0.3-1.0 credits for email waterfalls, 1-5 credits for phone waterfalls.
- Billing mode: `no_bill`.


#### `person-linkedin-sales-nav-url-to-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_sales_nav_url_to_email:{"linkedin_sales_nav_url":"{{linkedin_sales_nav_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_sales_nav_url` | `string` | Yes |  | LinkedIn Sales Navigator person URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_sales_nav_url_to_email --payload '{
  "linkedin_sales_nav_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-sales-nav-url-to-job-history`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_sales_nav_url_to_job_history:{"linkedin_sales_nav_url":"{{linkedin_sales_nav_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_sales_nav_url` | `string` | Yes |  | LinkedIn Sales Navigator person URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_sales_nav_url_to_job_history --payload '{
  "linkedin_sales_nav_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-sales-nav-url-to-personal-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_sales_nav_url_to_personal_email:{"linkedin_sales_nav_url":"{{linkedin_sales_nav_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_sales_nav_url` | `string` | Yes |  | LinkedIn Sales Navigator person URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_sales_nav_url_to_personal_email --payload '{
  "linkedin_sales_nav_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-sales-nav-url-to-phone`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_sales_nav_url_to_phone:{"linkedin_sales_nav_url":"{{linkedin_sales_nav_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_sales_nav_url` | `string` | Yes |  | LinkedIn Sales Navigator person URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_sales_nav_url_to_phone --payload '{
  "linkedin_sales_nav_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-sales-nav-url-to-work-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_sales_nav_url_to_work_email:{"linkedin_sales_nav_url":"{{linkedin_sales_nav_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_sales_nav_url` | `string` | Yes |  | LinkedIn Sales Navigator person URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_sales_nav_url_to_work_email --payload '{
  "linkedin_sales_nav_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-to-email-waterfall`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_to_email_waterfall:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | Standard LinkedIn profile URL in /in/ format. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_to_email_waterfall --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Waterfall orchestration is free. Individual provider lookups within the waterfall are charged at their listed rates (see provider pricing above).
- Typical cost: 0.3-1.0 credits for email waterfalls, 1-5 credits for phone waterfalls.
- Billing mode: `no_bill`.


#### `person-linkedin-url-context-to-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_url_context_to_email:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | LinkedIn person profile URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_url_context_to_email --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-url-context-to-job-history`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_url_context_to_job_history:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | LinkedIn person profile URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_url_context_to_job_history --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-url-context-to-personal-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_url_context_to_personal_email:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | LinkedIn person profile URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_url_context_to_personal_email --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-url-context-to-phone`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_url_context_to_phone:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | LinkedIn person profile URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_url_context_to_phone --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-url-context-to-work-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_url_context_to_work_email:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | LinkedIn person profile URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_url_context_to_work_email --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-url-to-job-history`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_url_to_job_history:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | LinkedIn person profile URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_url_to_job_history --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-url-to-personal-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_url_to_personal_email:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | LinkedIn person profile URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_url_to_personal_email --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-linkedin-url-to-phone`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_linkedin_url_to_phone:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | LinkedIn person profile URL. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_linkedin_url_to_phone --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-name-company-to-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_name_company_to_email:{"first_name":"{{first_name}}","last_name":"{{last_name}}","company_name":"{{company_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name. |
| `payload.last_name` | `string` | Yes |  | Contact last name. |
| `payload.company_name` | `string` | Yes |  | Company name. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_name_company_to_email --payload '{
  "first_name": "string",
  "last_name": "string",
  "company_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-name-company-to-job-history`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_name_company_to_job_history:{"first_name":"{{first_name}}","last_name":"{{last_name}}","company_name":"{{company_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name. |
| `payload.last_name` | `string` | Yes |  | Contact last name. |
| `payload.company_name` | `string` | Yes |  | Company name. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_name_company_to_job_history --payload '{
  "first_name": "string",
  "last_name": "string",
  "company_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-name-company-to-linkedin-url`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_name_company_to_linkedin_url:{"first_name":"{{first_name}}","last_name":"{{last_name}}","company_name":"{{company_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name. |
| `payload.last_name` | `string` | Yes |  | Contact last name. |
| `payload.company_name` | `string` | Yes |  | Company name. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_name_company_to_linkedin_url --payload '{
  "first_name": "string",
  "last_name": "string",
  "company_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-name-company-to-personal-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_name_company_to_personal_email:{"first_name":"{{first_name}}","last_name":"{{last_name}}","company_name":"{{company_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name. |
| `payload.last_name` | `string` | Yes |  | Contact last name. |
| `payload.company_name` | `string` | Yes |  | Company name. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_name_company_to_personal_email --payload '{
  "first_name": "string",
  "last_name": "string",
  "company_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-name-company-to-work-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_name_company_to_work_email:{"first_name":"{{first_name}}","last_name":"{{last_name}}","company_name":"{{company_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name. |
| `payload.last_name` | `string` | Yes |  | Contact last name. |
| `payload.company_name` | `string` | Yes |  | Company name. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_name_company_to_work_email --payload '{
  "first_name": "string",
  "last_name": "string",
  "company_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-name-domain-to-job-history`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_name_domain_to_job_history:{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name. |
| `payload.last_name` | `string` | Yes |  | Contact last name. |
| `payload.domain` | `string` | Yes |  | Company domain. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_name_domain_to_job_history --payload '{
  "first_name": "string",
  "last_name": "string",
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-name-domain-to-linkedin-url`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_name_domain_to_linkedin_url:{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name. |
| `payload.last_name` | `string` | Yes |  | Contact last name. |
| `payload.domain` | `string` | Yes |  | Company domain. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_name_domain_to_linkedin_url --payload '{
  "first_name": "string",
  "last_name": "string",
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-name-domain-to-personal-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_name_domain_to_personal_email:{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name. |
| `payload.last_name` | `string` | Yes |  | Contact last name. |
| `payload.domain` | `string` | Yes |  | Company domain. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_name_domain_to_personal_email --payload '{
  "first_name": "string",
  "last_name": "string",
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-name-domain-to-phone`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_person_name_domain_to_phone:{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Contact first name. |
| `payload.last_name` | `string` | Yes |  | Contact last name. |
| `payload.domain` | `string` | Yes |  | Company domain. |

**Direct CLI:**

```
deepline tools execute deepline_native_person_name_domain_to_phone --payload '{
  "first_name": "string",
  "last_name": "string",
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `personal-email-to-linkedin-waterfall`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_personal_email_to_linkedin_waterfall:{"personal_email":"{{personal_email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.personal_email` | `string` | Yes |  | Personal email (Gmail/GitHub signup address) to reverse-resolve into a LinkedIn profile. |

**Direct CLI:**

```
deepline tools execute deepline_native_personal_email_to_linkedin_waterfall --payload '{
  "personal_email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Waterfall orchestration is free. Individual provider lookups within the waterfall are charged at their listed rates (see provider pricing above).
- Typical cost: 0.3-1.0 credits for email waterfalls, 1-5 credits for phone waterfalls.
- Billing mode: `no_bill`.


#### `prospector`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_prospector:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute deepline_native_prospector --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `prospector-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_prospector_finder:{"job_id":"{{job_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.job_id` | `string` | Yes |  | The Waterfall job id used by finder endpoints. |

**Direct CLI:**

```
deepline tools execute deepline_native_prospector_finder --payload '{
  "job_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- The finder/dispatcher step itself is free. The underlying enrichment step is charged at its listed rate.
- Billing mode: `no_bill`.


#### `search-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=deepline_native_search_contact:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute deepline_native_search_contact --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


---

## DiscoLike

*Provider folder: `discolike` · 12 endpoints*


### DiscoLike — Agent Guidance

## Playbook

# DiscoLike Integration Guide

Use DiscoLike for website-first company discovery and domain intelligence.

## Best entry points

1. `discolike_count` when you want a fast volume estimate for supported search filters like phrase, category, geography, technology, or business model before paying to retrieve a large result set.
2. `discolike_discover` when you have seed domains or a natural-language ICP.
3. `discolike_bizdata` when you already know the domain and need a firmographic profile.
4. `discolike_match` when you start from a company name and need the best matching domain.
5. `discolike_search_indexed_contacts` when you want indexed contacts from DiscoLike’s contact dataset at known company domains or matching contact/company filters.
6. `discolike_generate_candidate_contacts` when you want ContaGen-style open-web research to generate candidate contacts at known company domains using DiscoLike-configured BYOK model and search-provider integrations.
7. `discolike_run_company_research` when you want Claygent-style arbitrary company research: run one prompt per domain against company context and optional web search.
8. `discolike_vendors`, `discolike_publiclink`, `discolike_subsidiaries`, and `discolike_redirects` for relationship mapping.

## Segment behavior

`discolike_segment` is public and synchronous from the agent perspective: it submits DiscoLike’s async segment job, polls the provider status endpoint, and returns the completed segment rows when available.If the sync wait times out, call `discolike_get_segment_status` with the returned `task_id` to retrieve the provider result later. This status call is a free read-only recovery step for tasks created by the same organization; final customer billing is reconciled by Deepline’s async provider cron against the original `discolike_segment` billing row.

## Contact generation behavior

`discolike_search_indexed_contacts` searches DiscoLike’s indexed contact/persona data.`discolike_generate_candidate_contacts` is the ContaGen path: it submits `POST /contacts/discover/generate`, then polls DiscoGen status until terminal results are available or the caller’s wait budget expires. Use it only when candidate open-web contact discovery is acceptable. Treat generated `email`, `linkedin_url`, phone, and identity fields as candidates until validated.`discolike_run_company_research` is the Claygent-like path: it submits `POST /discogen/process`, runs an arbitrary prompt against each company domain, and polls DiscoGen status for final results.DiscoGen/ContaGen are BYOK from DiscoLike’s perspective: they use the DiscoLike-configured LLM and search-provider integrations and may return a provider-side `estimated_cost` for model/search usage. For Deepline-managed DiscoLike execution, those configured model/search integrations are Deepline-managed upstream costs, so returned `estimated_cost` is included in Deepline billing. If the caller omits `integration_id`, Deepline uses the cheapest verified managed LLM config, OpenAI `gpt-5-nano`; if the caller omits `search_context_size`, Deepline uses `low`. Caller-selected model/search overrides, high search context, and `web_search: true` are blocked on the managed shared-key path. Deepline charges 0.01fordomain−modegenerated−contactorcompany−researchtasklaunches,plusreturned‘estimatedcost‘whenpresent.Website/profilecontextmodesalsoaddDeeplineDiscoLikeProusagepricingforcompanycontext:0.01 for domain-mode generated-contact or company-research task launches, plus returned \`estimated\_cost\` when present. Website/profile context modes also add Deepline DiscoLike Pro usage pricing for company context: 0.01fordomain−modegenerated−contactorcompany−researchtasklaunches,plusreturned‘estimatedc​ost‘whenpresent.Website/profilecontextmodesalsoaddDeeplineDiscoLikeProusagepricingforcompanycontext:0.21/query plus $4.20 per 1,000 submitted domains.

## Pricing caveat

DiscoLike bills directly in USD and the exact query / record rate depends on the account plan. Deepline’s shared Pro-plan model prices customer-facing paid DiscoLike usage at 40% above the provider rate: 0.21perqueryplus0.21 per query plus 0.21perqueryplus4.20 per 1,000 net-new records. Records cached within the provider account for 90 days are free on repeat retrieval upstream, but Deepline still uses stable customer-facing modeled pricing.


### DiscoLike — Endpoints


#### `discolike-bizdata`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_bizdata:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute discolike_bizdata --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `2.94` per pricing unit.
- Billing mode: `post_deduct`.


#### `discolike-count`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_count:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute discolike_count --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `discolike-discover`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_discover:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute discolike_discover --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `2.94` per pricing unit.
- Billing mode: `post_deduct`.


#### `discolike-extract`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_extract:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute discolike_extract --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `2.94` per pricing unit.
- Billing mode: `post_deduct`.


#### `discolike-generate-candidate-contacts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_generate_candidate_contacts:{"icp_text":"{{icp_text}}","domains":"{{domains}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.icp_text` | `string` | Yes |  |  |
| `payload.domains` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute discolike_generate_candidate_contacts --payload '{
  "icp_text": "string",
  "domains": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.1` per pricing unit.
- Billing mode: `post_deduct`.


#### `discolike-get-discogen-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_get_discogen_status:{"task_id":"{{task_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.task_id` | `string` | Yes |  | Task identifier returned by a DiscoGen or Contacts Discover Generate initial response. |

**Direct CLI:**

```
deepline tools execute discolike_get_discogen_status --payload '{
  "task_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `discolike-get-segment-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_get_segment_status:{"task_id":"{{task_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.task_id` | `string` | Yes |  | Task identifier returned by the Segment API initial response. |

**Direct CLI:**

```
deepline tools execute discolike_get_segment_status --payload '{
  "task_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `discolike-growth`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_growth:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute discolike_growth --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `2.94` per pricing unit.
- Billing mode: `post_deduct`.


#### `discolike-run-company-research`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_run_company_research:{"query":"{{query}}","domains":"{{domains}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  |  |
| `payload.domains` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute discolike_run_company_research --payload '{
  "query": "string",
  "domains": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.1` per pricing unit.
- Billing mode: `post_deduct`.


#### `discolike-score`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_score:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute discolike_score --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `2.94` per pricing unit.
- Billing mode: `post_deduct`.


#### `discolike-search-indexed-contacts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_search_indexed_contacts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute discolike_search_indexed_contacts --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `2.94` per pricing unit.
- Billing mode: `post_deduct`.


#### `discolike-segment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=discolike_segment:{"domains":"{{domains}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domains` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute discolike_segment --payload '{
  "domains": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


---

## DropLeads

*Provider folder: `dropleads` · 7 endpoints*


### DropLeads — Agent Guidance

## Playbook

# Dropleads Playbook

Use Dropleads as a two-phase flow: low-cost contact discovery first, paid enrichment second. Do not use Dropleads people search as the first step for account discovery.

## 1) Start with low-cost discovery

- Use `dropleads_get_lead_count` to size the audience before any paid call.
- Use `dropleads_search_people` to inspect masked contacts and validate ICP filters (free).
- Use `dropleads_search_people` after you already have target account domains, by passing `filters.companyDomains`. It is a contact search primitive, not a dependable way to discover target accounts.
- Do not build joins or account-discovery flows that depend on every returned lead having `companyDomain`. Treat returned `companyDomain` as optional; if you need account domains as the source of truth, use a company-native search/enrichment tool first.
- Tighten filters until sample rows clearly match role, industry, and geo expectations.
- Key filter fields: `filters.jobTitles`, `filters.seniority` (VP/Director/Manager/Senior/Entry/Intern), `filters.industries`, `filters.departments`, `filters.companyDomains`, `filters.employeeRanges`, `filters.personalCountries.include` (for geo), `pagination.page`, `pagination.limit`. Use title terms like `CEO`, `CTO`, or `Founder` for C-level searches; do not pass `C-Level` as a Dropleads seniority value.

### Filter best practices

All Dropleads filters nest under the `filters` object. Pagination nests under `pagination`. The canonical payload shape:

```
{
  "filters": {
    "companyDomains": ["microsoft.com"],
    "jobTitles": ["CTO", "VP Engineering"],
    "seniority": ["VP", "Director"],
    "personalCountries": { "include": ["United States"] }
  },
  "pagination": { "page": 1, "limit": 25 }
}
```

**Quick reference — correct filter keys:**

| Filter | Correct key | Why |
| --- | --- | --- |
| Company | `filters.companyDomains` | Exact domain match for known accounts. Prefer this when you already have domains; do not rely on people-search results to discover a complete account-domain list. `companyNames` does fuzzy substring matching — “Microsoft” pulls in unrelated businesses. |
| Country | `filters.personalCountries.include` | Array inside a nested object. |
| Seniority | `filters.seniority` | Exact values only: `VP`, `Director`, `Manager`, `Senior`, `Entry`, `Intern`. Use `jobTitles` terms like `CEO`, `CTO`, or `Founder` for C-level searches. |
| Industry | `filters.industries` | Exact strings from Dropleads. Pilot with a broad search first when unsure. |

### Geo filters are best-effort, not verified

Dropleads geo filters (`personalCountries` / `personalStates` / `personalCities` and the `organization*` equivalents) match against **self-reported, LinkedIn-sourced location text** — they are not verified against the contact’s actual location. Treat them accordingly:

- **City-level is the loosest match and leaks.**`personalCities` can return contacts whose stated city loosely matches even when their real location differs, and non-US contacts can appear under a US-city filter (e.g. a Bulgarian contact surfacing under `personalCities: San Francisco` \+ `personalCountries: United States`). Country/state are more reliable.
- **Person vs. company location are different fields.**`personal*` filters the contact’s own location; `organization*` filters the company HQ. Don’t conflate them — filtering a remote employee by company HQ city (or vice versa) drops or leaks legitimate matches.
- **Verify geo when precision matters.** Combine `personalCountries`/`personalStates` with `personalCities`, then post-filter the returned leads on their `country`/`state`/`city` (and exclude obvious mismatches) before trusting the result or spending on enrichment. Do not assume the filter alone guarantees the geo.

## 2) Escalate paid calls only for shortlisted targets

- Run `dropleads_email_finder` for contacts that passed the discovery pass.
- Run `dropleads_mobile_finder` only when phone is required for the workflow.
- Keep pilots small first, then scale after quality checks pass.

## 3) Gate outbound with verifier status

- Treat `invalid`, `catch_all`, and `unknown` as non-send by default.
- Treat `valid` as the only status that passes automatic send gates.
- Respect `credits_charged` in responses for post-execution billing accuracy.

## 4) Practical sequencing

1. Count segment (`dropleads_get_lead_count`).
2. Sample segment (`dropleads_search_people`).
3. Pre-score titles with `run_javascript` if looking for a specific profile (e.g. founders, GTM engineers).
4. Scrape LinkedIn profiles with `apify_run_actor_sync` for work history/signals (preferred over `call_ai` — structured data, faster, cheaper).
5. Extract signals with `run_javascript` from Apify output (e.g. founder detection, hiring signals).
6. Enrich emails via waterfall (`dropleads_email_finder` first, then other providers).
7. Verify candidate emails (`dropleads_email_verifier` or `leadmagic_email_validation`).
8. Expand only after pilot quality is confirmed.

## 5) Account discovery boundary

For account-based pipelines, start with a company-native source that returns account domains as first-class results. Feed those domains into Dropleads via `filters.companyDomains` to find contacts at known accounts. Dropleads may include `companyDomain` on returned people, but it is not guaranteed enough to be the join key that creates the account universe.


### DropLeads — Endpoints


#### `email-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dropleads_email_finder:{"first_name":"{{first_name}}","last_name":"{{last_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Person’s first name. |
| `payload.last_name` | `string` | Yes |  | Person’s last name. |

**Direct CLI:**

```
deepline tools execute dropleads_email_finder --payload '{
  "first_name": "string",
  "last_name": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.26` per pricing unit.
- Billing mode: `post_deduct`.


#### `email-verifier`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dropleads_email_verifier:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address to verify. |

**Direct CLI:**

```
deepline tools execute dropleads_email_verifier --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


#### `enrich-single-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dropleads_enrich_single_lead:{"leadId":"{{leadId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.leadId` | `string` | Yes |  | Lead identifier from Prime-DB search results. |

**Direct CLI:**

```
deepline tools execute dropleads_enrich_single_lead --payload '{
  "leadId": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.26` per pricing unit.
- Billing mode: `post_deduct`.


#### `get-lead-count`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dropleads_get_lead_count:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dropleads_get_lead_count --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `mobile-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dropleads_mobile_finder:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | Person’s LinkedIn profile URL. |

**Direct CLI:**

```
deepline tools execute dropleads_mobile_finder --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.76` per pricing unit.
- Billing mode: `post_deduct`.


#### `search-people`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dropleads_search_people:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dropleads_search_people --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `single-person-enrichment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=dropleads_single_person_enrichment:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute dropleads_single_person_enrichment --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.26` per pricing unit.
- Billing mode: `post_deduct`.


---

## EmailBison

*Provider folder: `emailbison` · 162 endpoints*


### EmailBison — Agent Guidance

## Playbook

Use EmailBison read endpoints to inspect campaigns, sender email accounts, replies, lead state, tags, and warmup state before making writes.Prefer targeted campaign/lead operations over workspace/admin operations. Mutating workspace, token, sender account, webhook, and delete operations require the user’s own credential and should be used only when the requested workflow explicitly needs them.Do not expose EmailBison team credit balances as customer-facing provider spend. Account details and balance fields stay internal, and there is no shared Deepline balance monitor for EmailBison because API keys are customer-specific.


### EmailBison — Endpoints


#### `emailbison-accept-workspace-invitation`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_accept_workspace_invitation:{"team_invitation_id":"{{team_invitation_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.team_invitation_id` | `integer` | Yes |  | The ID of the team invitation. |

**Direct CLI:**

```
deepline tools execute emailbison_accept_workspace_invitation --payload '{
  "team_invitation_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-accept-workspace-invitation-api-workspaces-v1-1-accept-team-invitation-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_accept_workspace_invitation_api_workspaces_v1_1_accept_team_invitation_id:{"team_invitation_id":"{{team_invitation_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.team_invitation_id` | `integer` | Yes |  | The ID of the team invitation. |

**Direct CLI:**

```
deepline tools execute emailbison_accept_workspace_invitation_api_workspaces_v1_1_accept_team_invitation_id --payload '{
  "team_invitation_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-account-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_account_details:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_account_details --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-activate-or-deactivate-asequence-step-variant`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_activate_or_deactivate_asequence_step_variant:{"sequence_step_id":"{{sequence_step_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sequence_step_id` | `integer` | Yes |  | The ID of the sequence step. |

**Direct CLI:**

```
deepline tools execute emailbison_activate_or_deactivate_asequence_step_variant --payload '{
  "sequence_step_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-add-lead-to-blacklist`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_add_lead_to_blacklist:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `integer` | Yes |  | The ID of the contact |

**Direct CLI:**

```
deepline tools execute emailbison_add_lead_to_blacklist --payload '{
  "lead_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-archive-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_archive_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_archive_campaign --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-attach-scheduled-email-to-reply`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_attach_scheduled_email_to_reply:{"reply_id":"{{reply_id}}","scheduled_email_id":"{{scheduled_email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_id` | `integer` | Yes |  | The ID of the reply. |
| `payload.scheduled_email_id` | `integer` | Yes |  | The ID of the scheduled email. |

**Direct CLI:**

```
deepline tools execute emailbison_attach_scheduled_email_to_reply --payload '{
  "reply_id": "integer",
  "scheduled_email_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-attach-tags-to-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_attach_tags_to_campaigns:{"tag_ids":"{{tag_ids}}","campaign_ids":"{{campaign_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tag_ids` | `array` | Yes |  | An array of tag IDs to be attached. |
| `payload.campaign_ids` | `array` | Yes |  | An array of campaign IDs to which the tags will be attached. |

**Direct CLI:**

```
deepline tools execute emailbison_attach_tags_to_campaigns --payload '{
  "tag_ids": "array",
  "campaign_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-attach-tags-to-email-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_attach_tags_to_email_accounts:{"tag_ids":"{{tag_ids}}","sender_email_ids":"{{sender_email_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tag_ids` | `array` | Yes |  | An array of tag IDs to be attached. |
| `payload.sender_email_ids` | `array` | Yes |  | An array of email account IDs to which the tags will be attached. |

**Direct CLI:**

```
deepline tools execute emailbison_attach_tags_to_email_accounts --payload '{
  "tag_ids": "array",
  "sender_email_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-attach-tags-to-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_attach_tags_to_leads:{"tag_ids":"{{tag_ids}}","lead_ids":"{{lead_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tag_ids` | `array` | Yes |  | An array of tag IDs to be attached. |
| `payload.lead_ids` | `array` | Yes |  | An array of lead IDs to which the tags will be attached. |

**Direct CLI:**

```
deepline tools execute emailbison_attach_tags_to_leads --payload '{
  "tag_ids": "array",
  "lead_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-breakdown-of-events-by-date`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_breakdown_of_events_by_date:{"start_date":"{{start_date}}","end_date":"{{end_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.start_date` | `string` | Yes |  | The start date to fetch stats. |
| `payload.end_date` | `string` | Yes |  | The end date to fetch stats. |

**Direct CLI:**

```
deepline tools execute emailbison_breakdown_of_events_by_date --payload '{
  "start_date": "string",
  "end_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-bulk-add-sender-emails`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_bulk_add_sender_emails:{"csv":"{{csv}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.csv` | `string` | Yes |  | The CSV file containing the contacts. |

**Direct CLI:**

```
deepline tools execute emailbison_bulk_add_sender_emails --payload '{
  "csv": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-bulk-check-missing-mxrecords`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_bulk_check_missing_mxrecords:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_bulk_check_missing_mxrecords --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-bulk-create-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_bulk_create_leads:{"leads":"{{leads}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.leads` | `array` | Yes |  | An array of lead objects. |

**Direct CLI:**

```
deepline tools execute emailbison_bulk_create_leads --payload '{
  "leads": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-bulk-create-leads-using-csv`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_bulk_create_leads_using_csv:{"name":"{{name}}","csv":"{{csv}}","columnsToMap":"{{columnsToMap}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | The name of the contact list |
| `payload.csv` | `string` | Yes |  | The CSV file containing the contacts. |
| `payload.columnsToMap` | `array` | Yes |  | The array with the header fields from the csv. |

**Direct CLI:**

```
deepline tools execute emailbison_bulk_create_leads_using_csv --payload '{
  "name": "string",
  "csv": "string",
  "columnsToMap": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-bulk-delete-campaigns-by-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_bulk_delete_campaigns_by_id:{"campaign_ids":"{{campaign_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_ids` | `array` | Yes |  | Array of campaign IDs |

**Direct CLI:**

```
deepline tools execute emailbison_bulk_delete_campaigns_by_id --payload '{
  "campaign_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-bulk-delete-leads-by-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_bulk_delete_leads_by_id:{"lead_ids":"{{lead_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_ids` | `array` | Yes |  | The id of an existing record in the leads table. |

**Direct CLI:**

```
deepline tools execute emailbison_bulk_delete_leads_by_id --payload '{
  "lead_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-bulk-update-email-daily-limits`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_bulk_update_email_daily_limits:{"sender_email_ids":"{{sender_email_ids}}","daily_limit":"{{daily_limit}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sender_email_ids` | `array` | Yes |  | An array of sender email IDs to update daily limits for. |
| `payload.daily_limit` | `integer` | Yes |  | The daily sending limit to set. |

**Direct CLI:**

```
deepline tools execute emailbison_bulk_update_email_daily_limits --payload '{
  "sender_email_ids": "array",
  "daily_limit": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-bulk-update-email-signatures`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_bulk_update_email_signatures:{"sender_email_ids":"{{sender_email_ids}}","email_signature":"{{email_signature}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sender_email_ids` | `array` | Yes |  | An array of sender email IDs to update signatures for. |
| `payload.email_signature` | `string` | Yes |  | The HTML signature to use. |

**Direct CLI:**

```
deepline tools execute emailbison_bulk_update_email_signatures --payload '{
  "sender_email_ids": "array",
  "email_signature": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-bulk-update-lead-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_bulk_update_lead_status:{"lead_ids":"{{lead_ids}}","status":"verified"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_ids` | `array` | Yes |  | The array of lead IDs. |
| `payload.status` | `"verified" | "unverified" | "unknown" | "unsubscribed" | "risky" | "inactive"` | Yes |  | The status to apply to the lead. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.status` | `verified`, `unverified`, `unknown`, `unsubscribed`, `risky`, `inactive` |

**Direct CLI:**

```
deepline tools execute emailbison_bulk_update_lead_status --payload '{
  "lead_ids": "array",
  "status": "verified"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-campaign-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_campaign_details:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_campaign_details --payload '{
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-check-mxrecords`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_check_mxrecords:{"senderEmailId":"{{senderEmailId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.senderEmailId` | `integer` | Yes |  | The ID (or email address) of the email account. |

**Direct CLI:**

```
deepline tools execute emailbison_check_mxrecords --payload '{
  "senderEmailId": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-compose-new-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_compose_new_email:{"sender_email_id":"{{sender_email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sender_email_id` | `integer` | Yes |  | The id of an existing record in the sender\_emails table. |

**Direct CLI:**

```
deepline tools execute emailbison_compose_new_email --payload '{
  "sender_email_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-acampaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_acampaign:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | The name of the new campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_create_acampaign --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-anew-custom-variable`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_anew_custom_variable:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | The name of the custom variable |

**Direct CLI:**

```
deepline tools execute emailbison_create_anew_custom_variable --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-anew-webhook`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_anew_webhook:{"name":"{{name}}","url":"{{url}}","events":"{{events}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | The name of the webhook. |
| `payload.url` | `string` | Yes |  | The URL to send data to. |
| `payload.events` | `array` | Yes |  | The events to subscribe to. |

**Direct CLI:**

```
deepline tools execute emailbison_create_anew_webhook --payload '{
  "name": "string",
  "url": "string",
  "events": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-apitoken-for-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_apitoken_for_workspace:{"team_id":"{{team_id}}","name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.team_id` | `integer` | Yes |  | The ID of the team. |
| `payload.name` | `string` | Yes |  | The name of the user |

**Direct CLI:**

```
deepline tools execute emailbison_create_apitoken_for_workspace --payload '{
  "team_id": "integer",
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-areply-template`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_areply_template:{"name":"{{name}}","body":"{{body}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | The name of the new reply template. |
| `payload.body` | `string` | Yes |  | The full contents of the reply template message. |

**Direct CLI:**

```
deepline tools execute emailbison_create_areply_template --payload '{
  "name": "string",
  "body": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-blacklisted-domain`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_blacklisted_domain:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | The domain to be blacklisted. |

**Direct CLI:**

```
deepline tools execute emailbison_create_blacklisted_domain --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-blacklisted-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_blacklisted_email:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | The email address to be blacklisted. |

**Direct CLI:**

```
deepline tools execute emailbison_create_blacklisted_email --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-campaign-schedule`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_campaign_schedule:{"campaign_id":"{{campaign_id}}","monday":"{{monday}}","tuesday":"{{tuesday}}","wednesday":"{{wednesday}}","thursday":"{{thursday}}","friday":"{{friday}}","saturday":"{{saturday}}","sunday":"{{sunday}}","start_time":"{{start_time}}","end_time":"{{end_time}}","timezone":"{{timezone}}","save_as_template":"{{save_as_template}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.monday` | `boolean` | Yes |  | Whether the schedule includes Monday. |
| `payload.tuesday` | `boolean` | Yes |  | Whether the schedule includes Tuesday. |
| `payload.wednesday` | `boolean` | Yes |  | Whether the schedule includes Wednesday. |
| `payload.thursday` | `boolean` | Yes |  | Whether the schedule includes Thursday. |
| `payload.friday` | `boolean` | Yes |  | Whether the schedule includes Friday. |
| `payload.saturday` | `boolean` | Yes |  | Whether the schedule includes Saturday. |
| `payload.sunday` | `boolean` | Yes |  | Whether the schedule includes Sunday. |
| `payload.start_time` | `string` | Yes |  | The start time in HH:MM format. |
| `payload.end_time` | `string` | Yes |  | The end time in HH:MM format. |
| `payload.timezone` | `string` | Yes |  | The timezone of the schedule. |
| `payload.save_as_template` | `boolean` | Yes |  | Wheter the created schedule should be saved as template. |

**Direct CLI:**

```
deepline tools execute emailbison_create_campaign_schedule --payload '{
  "campaign_id": "integer",
  "monday": false,
  "tuesday": false,
  "wednesday": false,
  "thursday": false,
  "friday": false,
  "saturday": false,
  "sunday": false,
  "start_time": "string",
  "end_time": "string",
  "timezone": "string",
  "save_as_template": false
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-campaign-schedule-from-template`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_campaign_schedule_from_template:{"campaign_id":"{{campaign_id}}","schedule_id":"{{schedule_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.schedule_id` | `integer` | Yes |  | The ID of the schedule template. |

**Direct CLI:**

```
deepline tools execute emailbison_create_campaign_schedule_from_template --payload '{
  "campaign_id": "integer",
  "schedule_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-custom-tracking-domain`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_custom_tracking_domain:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | The domain name to be added. |

**Direct CLI:**

```
deepline tools execute emailbison_create_custom_tracking_domain --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-ignore-phrase`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_ignore_phrase:{"phrase":"{{phrase}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.phrase` | `string` | Yes |  | The ignore phrase to add. |

**Direct CLI:**

```
deepline tools execute emailbison_create_ignore_phrase --payload '{
  "phrase": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-imapsmtpemail-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_imapsmtpemail_account:{"name":"{{name}}","email":"{{email}}","password":"{{password}}","imap_server":"{{imap_server}}","imap_port":"{{imap_port}}","smtp_server":"{{smtp_server}}","smtp_port":"{{smtp_port}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | The name associated with the sender email. |
| `payload.email` | `string` | Yes |  | The email address of the sender. Must be unique and in a valid email format. |
| `payload.password` | `string` | Yes |  | The password for the sender email. |
| `payload.imap_server` | `string` | Yes |  | The IMAP server address for the sender email. Must be a valid domain name. |
| `payload.imap_port` | `integer` | Yes |  | The IMAP server port for the sender email. |
| `payload.smtp_server` | `string` | Yes |  | The SMTP server address for the sender email. Must be a valid domain name. |
| `payload.smtp_port` | `integer` | Yes |  | The SMTP server port for the sender email. |

**Direct CLI:**

```
deepline tools execute emailbison_create_imapsmtpemail_account --payload '{
  "name": "string",
  "email": "string",
  "password": "string",
  "imap_server": "string",
  "imap_port": "integer",
  "smtp_server": "string",
  "smtp_port": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_lead:{"first_name":"{{first_name}}","email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | The first name of the contact. |
| `payload.email` | `string` | Yes |  | The email address of the contact. Must be unique and in a valid email format. |

**Direct CLI:**

```
deepline tools execute emailbison_create_lead --payload '{
  "first_name": "string",
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-new-reply`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_new_reply:{"reply_id":"{{reply_id}}","to_emails":"{{to_emails}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_id` | `integer` | Yes |  | The ID of the parent reply. |
| `payload.to_emails` | `array` | Yes |  | Array of people to send this email to. Not required if `reply_all` is set to true. If `reply_all` is set to true and this parameter is passed, the recipients will be appended. Duplicate recipients are ignored. |

**Direct CLI:**

```
deepline tools execute emailbison_create_new_reply --payload '{
  "reply_id": "integer",
  "to_emails": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-sequence-stepsdeprecated`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_sequence_stepsdeprecated:{"campaign_id":"{{campaign_id}}","title":"{{title}}","sequence_steps":"{{sequence_steps}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.title` | `string` | Yes |  | The title for the sequence. |
| `payload.sequence_steps` | `array` | Yes |  | The array containing the sequence steps |

**Direct CLI:**

```
deepline tools execute emailbison_create_sequence_stepsdeprecated --payload '{
  "campaign_id": "integer",
  "title": "string",
  "sequence_steps": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-sequence-stepsv11`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_sequence_stepsv11:{"campaign_id":"{{campaign_id}}","title":"{{title}}","sequence_steps":"{{sequence_steps}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.title` | `string` | Yes |  | The title for the sequence. |
| `payload.sequence_steps` | `array` | Yes |  | The array containing the sequence steps |

**Direct CLI:**

```
deepline tools execute emailbison_create_sequence_stepsv11 --payload '{
  "campaign_id": "integer",
  "title": "string",
  "sequence_steps": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-tag`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_tag:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | The name of the tag. |

**Direct CLI:**

```
deepline tools execute emailbison_create_tag --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-userand-add-to-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_userand_add_to_workspace:{"name":"{{name}}","password":"{{password}}","email":"{{email}}","role":"admin"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | The name of the user |
| `payload.password` | `string` | Yes |  | The password of the user. |
| `payload.email` | `string` | Yes |  | The email of the user. |
| `payload.role` | `"admin" | "editor" | "client" | "reseller"` | Yes |  | The role of the new team member. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.role` | `admin`, `editor`, `client`, `reseller` |

**Direct CLI:**

```
deepline tools execute emailbison_create_userand_add_to_workspace --payload '{
  "name": "string",
  "password": "string",
  "email": "string",
  "role": "admin"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_workspace:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | The new workspace name. |

**Direct CLI:**

```
deepline tools execute emailbison_create_workspace --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-create-workspace-api-workspaces-v1-1`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_create_workspace_api_workspaces_v1_1:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | The new workspace name. |

**Direct CLI:**

```
deepline tools execute emailbison_create_workspace_api_workspaces_v1_1 --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-delete-acampaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_delete_acampaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_delete_acampaign --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-delete-alead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_delete_alead:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  | Optional parameter. int\|string The ID or email of the lead |

**Direct CLI:**

```
deepline tools execute emailbison_delete_alead --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-delete-awebhook`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_delete_awebhook:{"webhook_url_id":"{{webhook_url_id}}","id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.webhook_url_id` | `integer` | Yes |  | The ID of the webhook url. |
| `payload.id` | `integer` | Yes |  | The ID of the webhook url. |

**Direct CLI:**

```
deepline tools execute emailbison_delete_awebhook --payload '{
  "webhook_url_id": "integer",
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-delete-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_delete_email_account:{"senderEmailId":"{{senderEmailId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.senderEmailId` | `integer` | Yes |  | The ID (or email address) of the email account. |

**Direct CLI:**

```
deepline tools execute emailbison_delete_email_account --payload '{
  "senderEmailId": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-delete-reply`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_delete_reply:{"reply_id":"{{reply_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_id` | `integer` | Yes |  | The ID of the reply. |

**Direct CLI:**

```
deepline tools execute emailbison_delete_reply --payload '{
  "reply_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-delete-reply-template`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_delete_reply_template:{"reply_template_id":"{{reply_template_id}}","id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_template_id` | `integer` | Yes |  | The ID of the reply template. |
| `payload.id` | `integer` | Yes |  | The ID of the reply template. |

**Direct CLI:**

```
deepline tools execute emailbison_delete_reply_template --payload '{
  "reply_template_id": "integer",
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-delete-sequence-step`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_delete_sequence_step:{"sequence_step_id":"{{sequence_step_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sequence_step_id` | `integer` | Yes |  | The ID of the sequence step. You can get a list of all campaign sequence steps by calling the `/campaigns/ /sequence-steps` endpoint. |

**Direct CLI:**

```
deepline tools execute emailbison_delete_sequence_step --payload '{
  "sequence_step_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-delete-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_delete_workspace:{"team_id":"{{team_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.team_id` | `integer` | Yes |  | The ID of the team (workspace) to delete. |

**Direct CLI:**

```
deepline tools execute emailbison_delete_workspace --payload '{
  "team_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-delete-workspace-member`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_delete_workspace_member:{"user_id":"{{user_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.user_id` | `integer` | Yes |  | The ID of the team member. |

**Direct CLI:**

```
deepline tools execute emailbison_delete_workspace_member --payload '{
  "user_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-delete-workspace-member-api-workspaces-v1-1-members-user-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_delete_workspace_member_api_workspaces_v1_1_members_user_id:{"user_id":"{{user_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.user_id` | `integer` | Yes |  | The ID of the team member. |

**Direct CLI:**

```
deepline tools execute emailbison_delete_workspace_member_api_workspaces_v1_1_members_user_id --payload '{
  "user_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-disable-warmup-for-email-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_disable_warmup_for_email_accounts:{"sender_email_ids":"{{sender_email_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sender_email_ids` | `array` | Yes |  | Array of sender email IDs. |

**Direct CLI:**

```
deepline tools execute emailbison_disable_warmup_for_email_accounts --payload '{
  "sender_email_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-duplicate-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_duplicate_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_duplicate_campaign --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-enable-warmup-for-email-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_enable_warmup_for_email_accounts:{"sender_email_ids":"{{sender_email_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sender_email_ids` | `array` | Yes |  | Array of sender email IDs. |

**Direct CLI:**

```
deepline tools execute emailbison_enable_warmup_for_email_accounts --payload '{
  "sender_email_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-forward-reply`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_forward_reply:{"reply_id":"{{reply_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_id` | `integer` | Yes |  | The ID of the parent reply. |

**Direct CLI:**

```
deepline tools execute emailbison_forward_reply --payload '{
  "reply_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-generate-headless-uitokenbeta`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_generate_headless_uitokenbeta:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_generate_headless_uitokenbeta --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-acustom-tracking-domains`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_acustom_tracking_domains:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The custom tracking domain ID. |

**Direct CLI:**

```
deepline tools execute emailbison_get_acustom_tracking_domains --payload '{
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-blacklisted-domains`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_blacklisted_domains:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_blacklisted_domains --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-blacklisted-emails`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_blacklisted_emails:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_blacklisted_emails --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-campaign-sender-emails`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_campaign_sender_emails:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_get_all_campaign_sender_emails --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-custom-tracking-domains`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_custom_tracking_domains:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_custom_tracking_domains --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-custom-variables`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_custom_variables:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_custom_variables --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-ignore-phrases`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_ignore_phrases:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_ignore_phrases --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_leads:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_leads --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-leads-for-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_leads_for_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_get_all_leads_for_campaign --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-replies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_replies:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_replies --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-replies-for-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_replies_for_lead:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `integer` | Yes |  | the ID or email of the lead |

**Direct CLI:**

```
deepline tools execute emailbison_get_all_replies_for_lead --payload '{
  "lead_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-reply-templates`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_reply_templates:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_reply_templates --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-scheduled-emails`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_scheduled_emails:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `integer` | Yes |  | the ID or email of the lead |

**Direct CLI:**

```
deepline tools execute emailbison_get_all_scheduled_emails --payload '{
  "lead_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-scheduled-emails-api-scheduled-emails`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_scheduled_emails_api_scheduled_emails:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_scheduled_emails_api_scheduled_emails --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-scheduled-emails-for-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_scheduled_emails_for_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_get_all_scheduled_emails_for_campaign --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-sent-emails-for-alead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_sent_emails_for_alead:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  | The ID or the email of the lead. |

**Direct CLI:**

```
deepline tools execute emailbison_get_all_sent_emails_for_alead --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-tags-for-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_tags_for_workspace:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_tags_for_workspace --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-webhook-event-types`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_webhook_event_types:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_webhook_event_types --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-all-webhooks`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_all_webhooks:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_all_webhooks --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-asingle-webhook`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_asingle_webhook:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The ID of the webhook. |

**Direct CLI:**

```
deepline tools execute emailbison_get_asingle_webhook --payload '{
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-blacklisted-domain`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_blacklisted_domain:{"blacklisted_domain_id":"{{blacklisted_domain_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.blacklisted_domain_id` | `string` | Yes |  | The domain or ID of the blacklisted domain. |

**Direct CLI:**

```
deepline tools execute emailbison_get_blacklisted_domain --payload '{
  "blacklisted_domain_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-blacklisted-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_blacklisted_email:{"blacklisted_email_id":"{{blacklisted_email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.blacklisted_email_id` | `string` | Yes |  | The email or ID of the blacklisted email. |

**Direct CLI:**

```
deepline tools execute emailbison_get_blacklisted_email --payload '{
  "blacklisted_email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-campaign-replies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_campaign_replies:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_get_campaign_replies --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-campaign-statssummary`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_campaign_statssummary:{"campaign_id":"{{campaign_id}}","start_date":"{{start_date}}","end_date":"{{end_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.start_date` | `string` | Yes |  | The start date to fetch stats. |
| `payload.end_date` | `string` | Yes |  | The end date to fetch stats. |

**Direct CLI:**

```
deepline tools execute emailbison_get_campaign_statssummary --payload '{
  "campaign_id": "integer",
  "start_date": "string",
  "end_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-email-account-oauth-access-token`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_email_account_oauth_access_token:{"senderEmailId":"{{senderEmailId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.senderEmailId` | `integer` | Yes |  | The ID (or email address) of the email account. |

**Direct CLI:**

```
deepline tools execute emailbison_get_email_account_oauth_access_token --payload '{
  "senderEmailId": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-email-account-replies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_email_account_replies:{"senderEmailId":"{{senderEmailId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.senderEmailId` | `integer` | Yes |  | The ID (or email address) of the email account. |

**Direct CLI:**

```
deepline tools execute emailbison_get_email_account_replies --payload '{
  "senderEmailId": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-full-normalized-stats-by-date`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_full_normalized_stats_by_date:{"campaign_id":"{{campaign_id}}","start_date":"{{start_date}}","end_date":"{{end_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.start_date` | `string` | Yes |  | The start date to fetch stats. |
| `payload.end_date` | `string` | Yes |  | The end date to fetch stats. |

**Direct CLI:**

```
deepline tools execute emailbison_get_full_normalized_stats_by_date --payload '{
  "campaign_id": "integer",
  "start_date": "string",
  "end_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-full-normalized-stats-by-date-api-workspaces-v1-1-line-area-chart-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_full_normalized_stats_by_date_api_workspaces_v1_1_line_area_chart_stats:{"start_date":"{{start_date}}","end_date":"{{end_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.start_date` | `string` | Yes |  | The start date to fetch stats. |
| `payload.end_date` | `string` | Yes |  | The end date to fetch stats. |

**Direct CLI:**

```
deepline tools execute emailbison_get_full_normalized_stats_by_date_api_workspaces_v1_1_line_area_chart_stats --payload '{
  "start_date": "string",
  "end_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-master-inbox-settings`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_master_inbox_settings:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_get_master_inbox_settings --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-reply`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_reply:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The ID of the reply. |

**Direct CLI:**

```
deepline tools execute emailbison_get_reply --payload '{
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-reply-conversation-thread`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_reply_conversation_thread:{"reply_id":"{{reply_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_id` | `integer` | Yes |  | The ID of the reply. |

**Direct CLI:**

```
deepline tools execute emailbison_get_reply_conversation_thread --payload '{
  "reply_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-sample-webhook-event-payload`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_sample_webhook_event_payload:{"event_type":"email_sent"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.event_type` | `"email_sent" | "lead_first_contacted" | "lead_replied" | "lead_interested" | "email_opened" | "email_bounced" | "lead_unsubscribed" | "email_account_added" | "email_account_removed" | "email_account_disconnected" | "email_account_reconnected" | "manual_email_sent" | "untracked_reply_received"` | Yes |  | The event type. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.event_type` | `email_sent`, `lead_first_contacted`, `lead_replied`, `lead_interested`, `email_opened`, `email_bounced`, `lead_unsubscribed`, `email_account_added`, `email_account_removed`, `email_account_disconnected`, `email_account_reconnected`, `manual_email_sent`, `untracked_reply_received` |

**Direct CLI:**

```
deepline tools execute emailbison_get_sample_webhook_event_payload --payload '{
  "event_type": "email_sent"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-scheduled-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_scheduled_email:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The ID of the scheduled email. |

**Direct CLI:**

```
deepline tools execute emailbison_get_scheduled_email --payload '{
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-single-ignore-phrase`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_single_ignore_phrase:{"ignore_phrase_id":"{{ignore_phrase_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.ignore_phrase_id` | `string` | Yes |  | Optional parameter. int\|string The ID or name of the ignore phrase |

**Direct CLI:**

```
deepline tools execute emailbison_get_single_ignore_phrase --payload '{
  "ignore_phrase_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-single-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_single_lead:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  | Optional parameter. int\|string The ID or email of the lead |

**Direct CLI:**

```
deepline tools execute emailbison_get_single_lead --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-get-workspace-statssummary`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_get_workspace_statssummary:{"start_date":"{{start_date}}","end_date":"{{end_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.start_date` | `string` | Yes |  | The start date to fetch stats. |
| `payload.end_date` | `string` | Yes |  | The end date to fetch stats. |

**Direct CLI:**

```
deepline tools execute emailbison_get_workspace_statssummary --payload '{
  "start_date": "string",
  "end_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-import-leads-by-ids`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_import_leads_by_ids:{"campaign_id":"{{campaign_id}}","lead_ids":"{{lead_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.lead_ids` | `array` | Yes |  | An array of lead IDs to import. |

**Direct CLI:**

```
deepline tools execute emailbison_import_leads_by_ids --payload '{
  "campaign_id": "integer",
  "lead_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-import-leads-from-existing-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_import_leads_from_existing_list:{"campaign_id":"{{campaign_id}}","lead_list_id":"{{lead_list_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.lead_list_id` | `integer` | Yes |  | The ID of the lead list to import. |

**Direct CLI:**

```
deepline tools execute emailbison_import_leads_from_existing_list --payload '{
  "campaign_id": "integer",
  "lead_list_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-import-sender-emails-by-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_import_sender_emails_by_id:{"campaign_id":"{{campaign_id}}","sender_email_ids":"{{sender_email_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.sender_email_ids` | `array` | Yes |  | An array of sender emails IDs to attach. |

**Direct CLI:**

```
deepline tools execute emailbison_import_sender_emails_by_id --payload '{
  "campaign_id": "integer",
  "sender_email_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-invite-team-member`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_invite_team_member:{"email":"{{email}}","role":"admin"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | The email of the new team member. |
| `payload.role` | `"admin" | "editor" | "client" | "reseller"` | Yes |  | The role of the new team member. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.role` | `admin`, `editor`, `client`, `reseller` |

**Direct CLI:**

```
deepline tools execute emailbison_invite_team_member --payload '{
  "email": "string",
  "role": "admin"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-invite-team-member-api-workspaces-v1-1-invite-members`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_invite_team_member_api_workspaces_v1_1_invite_members:{"email":"{{email}}","role":"admin"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | The email of the new team member. |
| `payload.role` | `"admin" | "editor" | "client" | "reseller"` | Yes |  | The role of the new team member. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.role` | `admin`, `editor`, `client`, `reseller` |

**Direct CLI:**

```
deepline tools execute emailbison_invite_team_member_api_workspaces_v1_1_invite_members --payload '{
  "email": "string",
  "role": "admin"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-list-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_list_campaigns:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_list_campaigns --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-list-email-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_list_email_accounts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_list_email_accounts --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-list-email-accounts-with-warmup-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_list_email_accounts_with_warmup_stats:{"start_date":"{{start_date}}","end_date":"{{end_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.start_date` | `string` | Yes |  | The start date to fetch stats (defaults to 10 days ago). |
| `payload.end_date` | `string` | Yes |  | The end date to fetch stats (defaults to today). |

**Direct CLI:**

```
deepline tools execute emailbison_list_email_accounts_with_warmup_stats --payload '{
  "start_date": "string",
  "end_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-list-workspaces`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_list_workspaces:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_list_workspaces --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-list-workspaces-api-workspaces-v1-1`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_list_workspaces_api_workspaces_v1_1:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_list_workspaces_api_workspaces_v1_1 --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-mark-as-automated-or-not-automated`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_mark_as_automated_or_not_automated:{"reply_id":"{{reply_id}}","automated":"{{automated}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_id` | `integer` | Yes |  | The ID of the reply. |
| `payload.automated` | `boolean` | Yes |  | Whether to mark the reply as automated or not automated. |

**Direct CLI:**

```
deepline tools execute emailbison_mark_as_automated_or_not_automated --payload '{
  "reply_id": "integer",
  "automated": false
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-mark-as-interested`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_mark_as_interested:{"reply_id":"{{reply_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_id` | `integer` | Yes |  | The ID of the reply. |

**Direct CLI:**

```
deepline tools execute emailbison_mark_as_interested --payload '{
  "reply_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-mark-as-not-interested`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_mark_as_not_interested:{"reply_id":"{{reply_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_id` | `integer` | Yes |  | The ID of the reply. |

**Direct CLI:**

```
deepline tools execute emailbison_mark_as_not_interested --payload '{
  "reply_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-mark-as-read-or-unread`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_mark_as_read_or_unread:{"reply_id":"{{reply_id}}","read":"{{read}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_id` | `integer` | Yes |  | The ID of the reply. |
| `payload.read` | `boolean` | Yes |  | Whether to set mark the reply as read or unread. |

**Direct CLI:**

```
deepline tools execute emailbison_mark_as_read_or_unread --payload '{
  "reply_id": "integer",
  "read": false
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-move-leads-to-another-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_move_leads_to_another_campaign:{"campaign_id":"{{campaign_id}}","target_campaign_id":"{{target_campaign_id}}","lead_ids":"{{lead_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the original campaign. |
| `payload.target_campaign_id` | `integer` | Yes |  | The ID of the campaign that leads will be moved to. |
| `payload.lead_ids` | `array` | Yes |  | The array of lead IDs. |

**Direct CLI:**

```
deepline tools execute emailbison_move_leads_to_another_campaign --payload '{
  "campaign_id": "integer",
  "target_campaign_id": "integer",
  "lead_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-pause-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_pause_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_pause_campaign --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-push-replyand-lead-toreply-followup-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_push_replyand_lead_toreply_followup_campaign:{"reply_id":"{{reply_id}}","campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_id` | `integer` | Yes |  | The ID of the reply |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the followup campaign |

**Direct CLI:**

```
deepline tools execute emailbison_push_replyand_lead_toreply_followup_campaign --payload '{
  "reply_id": "integer",
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-remove-blacklisted-domain`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_remove_blacklisted_domain:{"blacklisted_domain_id":"{{blacklisted_domain_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.blacklisted_domain_id` | `string` | Yes |  | The domain or ID of the blacklisted domain. |

**Direct CLI:**

```
deepline tools execute emailbison_remove_blacklisted_domain --payload '{
  "blacklisted_domain_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-remove-blacklisted-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_remove_blacklisted_email:{"blacklisted_email_id":"{{blacklisted_email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.blacklisted_email_id` | `string` | Yes |  | The email or ID of the blacklisted email. |

**Direct CLI:**

```
deepline tools execute emailbison_remove_blacklisted_email --payload '{
  "blacklisted_email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-remove-custom-tracking-domain`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_remove_custom_tracking_domain:{"custom_tracking_domain_id":"{{custom_tracking_domain_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.custom_tracking_domain_id` | `integer` | Yes |  | The custom tracking domain ID |

**Direct CLI:**

```
deepline tools execute emailbison_remove_custom_tracking_domain --payload '{
  "custom_tracking_domain_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-remove-ignore-phrase`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_remove_ignore_phrase:{"ignore_phrase_id":"{{ignore_phrase_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.ignore_phrase_id` | `string` | Yes |  | Optional parameter. int\|string The ID or name of the ignore phrase |

**Direct CLI:**

```
deepline tools execute emailbison_remove_ignore_phrase --payload '{
  "ignore_phrase_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-remove-leads-from-acampaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_remove_leads_from_acampaign:{"campaign_id":"{{campaign_id}}","lead_ids":"{{lead_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.lead_ids` | `array` | Yes |  | An array of lead IDs to remove. |

**Direct CLI:**

```
deepline tools execute emailbison_remove_leads_from_acampaign --payload '{
  "campaign_id": "integer",
  "lead_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-remove-sender-emails-by-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_remove_sender_emails_by_id:{"campaign_id":"{{campaign_id}}","sender_email_ids":"{{sender_email_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.sender_email_ids` | `array` | Yes |  | An array of sender emails IDs to attach. |

**Direct CLI:**

```
deepline tools execute emailbison_remove_sender_emails_by_id --payload '{
  "campaign_id": "integer",
  "sender_email_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-remove-tag`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_remove_tag:{"tag_id":"{{tag_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tag_id` | `integer` | Yes |  | The ID of the tag |

**Direct CLI:**

```
deepline tools execute emailbison_remove_tag --payload '{
  "tag_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-remove-tags-from-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_remove_tags_from_campaigns:{"tag_ids":"{{tag_ids}}","campaign_ids":"{{campaign_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tag_ids` | `array` | Yes |  | An array of tag IDs to be detached. |
| `payload.campaign_ids` | `array` | Yes |  | An array of campaign IDs from which the tags will be detached. |

**Direct CLI:**

```
deepline tools execute emailbison_remove_tags_from_campaigns --payload '{
  "tag_ids": "array",
  "campaign_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-remove-tags-from-email-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_remove_tags_from_email_accounts:{"tag_ids":"{{tag_ids}}","sender_email_ids":"{{sender_email_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tag_ids` | `array` | Yes |  | An array of tag IDs to be detached. |
| `payload.sender_email_ids` | `array` | Yes |  | An array of email account IDs from which the tags will be detached. |

**Direct CLI:**

```
deepline tools execute emailbison_remove_tags_from_email_accounts --payload '{
  "tag_ids": "array",
  "sender_email_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-remove-tags-from-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_remove_tags_from_leads:{"tag_ids":"{{tag_ids}}","lead_ids":"{{lead_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tag_ids` | `array` | Yes |  | An array of tag IDs to be detached. |
| `payload.lead_ids` | `array` | Yes |  | An array of lead IDs from which the tags will be detached. |

**Direct CLI:**

```
deepline tools execute emailbison_remove_tags_from_leads --payload '{
  "tag_ids": "array",
  "lead_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-reply-template-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_reply_template_details:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The ID of the reply template. |

**Direct CLI:**

```
deepline tools execute emailbison_reply_template_details --payload '{
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-resume-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_resume_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_resume_campaign --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-send-atest-webhook-event`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_send_atest_webhook_event:{"event_type":"email_sent","url":"{{url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.event_type` | `"email_sent" | "lead_first_contacted" | "lead_replied" | "lead_interested" | "email_opened" | "email_bounced" | "lead_unsubscribed" | "email_account_added" | "email_account_removed" | "email_account_disconnected" | "email_account_reconnected" | "manual_email_sent" | "untracked_reply_received"` | Yes |  | The event type. |
| `payload.url` | `string` | Yes |  | The URL to send the webhook to. This must be an active URL that can receive requests. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.event_type` | `email_sent`, `lead_first_contacted`, `lead_replied`, `lead_interested`, `email_opened`, `email_bounced`, `lead_unsubscribed`, `email_account_added`, `email_account_removed`, `email_account_disconnected`, `email_account_reconnected`, `manual_email_sent`, `untracked_reply_received` |

**Direct CLI:**

```
deepline tools execute emailbison_send_atest_webhook_event --payload '{
  "event_type": "email_sent",
  "url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-send-sequence-step-test-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_send_sequence_step_test_email:{"sequence_step_id":"{{sequence_step_id}}","sender_email_id":"{{sender_email_id}}","to_email":"{{to_email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sequence_step_id` | `integer` | Yes |  | The ID of the sequence step. You can get a list of all campaign sequence steps by calling the `/campaigns/ /sequence-steps` endpoint. |
| `payload.sender_email_id` | `integer` | Yes |  | The ID of the sender email to send from. |
| `payload.to_email` | `string` | Yes |  | The email address to send the sequence step to. |

**Direct CLI:**

```
deepline tools execute emailbison_send_sequence_step_test_email --payload '{
  "sequence_step_id": "integer",
  "sender_email_id": "integer",
  "to_email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-show-email-account-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_show_email_account_campaigns:{"senderEmailId":"{{senderEmailId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.senderEmailId` | `integer` | Yes |  | The ID (or email address) of the email account. |

**Direct CLI:**

```
deepline tools execute emailbison_show_email_account_campaigns --payload '{
  "senderEmailId": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-show-email-account-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_show_email_account_details:{"senderEmailId":"{{senderEmailId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.senderEmailId` | `integer` | Yes |  | The ID (or email address) of the email account. |

**Direct CLI:**

```
deepline tools execute emailbison_show_email_account_details --payload '{
  "senderEmailId": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-show-sending-schedule-for-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_show_sending_schedule_for_campaign:{"campaign_id":"{{campaign_id}}","day":"today"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.day` | `"today" | "tomorrow" | "day_after_tomorrow"` | Yes |  | The day of the schedule. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.day` | `today`, `tomorrow`, `day_after_tomorrow` |

**Direct CLI:**

```
deepline tools execute emailbison_show_sending_schedule_for_campaign --payload '{
  "campaign_id": "integer",
  "day": "today"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-show-sending-schedules-for-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_show_sending_schedules_for_campaigns:{"day":"today"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.day` | `"today" | "tomorrow" | "day_after_tomorrow"` | Yes |  | The day of the schedule. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.day` | `today`, `tomorrow`, `day_after_tomorrow` |

**Direct CLI:**

```
deepline tools execute emailbison_show_sending_schedules_for_campaigns --payload '{
  "day": "today"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-show-single-email-account-with-warmup-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_show_single_email_account_with_warmup_details:{"senderEmailId":"{{senderEmailId}}","start_date":"{{start_date}}","end_date":"{{end_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.senderEmailId` | `integer` | Yes |  | The ID (or email address) of the email account. |
| `payload.start_date` | `string` | Yes |  | The start date to fetch stats. |
| `payload.end_date` | `string` | Yes |  | The end date to fetch stats. |

**Direct CLI:**

```
deepline tools execute emailbison_show_single_email_account_with_warmup_details --payload '{
  "senderEmailId": "integer",
  "start_date": "string",
  "end_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-stop-future-emails-for-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_stop_future_emails_for_leads:{"campaign_id":"{{campaign_id}}","lead_ids":"{{lead_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.lead_ids` | `array` | Yes |  | An array of lead IDs to stop future emails for. |

**Direct CLI:**

```
deepline tools execute emailbison_stop_future_emails_for_leads --payload '{
  "campaign_id": "integer",
  "lead_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-switch-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_switch_workspace:{"team_id":"{{team_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.team_id` | `integer` | Yes |  | The ID of the team to switch. |

**Direct CLI:**

```
deepline tools execute emailbison_switch_workspace --payload '{
  "team_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-switch-workspace-api-workspaces-v1-1-switch-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_switch_workspace_api_workspaces_v1_1_switch_workspace:{"team_id":"{{team_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.team_id` | `integer` | Yes |  | The ID of the team (workspace) to switch. |

**Direct CLI:**

```
deepline tools execute emailbison_switch_workspace_api_workspaces_v1_1_switch_workspace --payload '{
  "team_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-unsubscribe-contact-that-replied`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_unsubscribe_contact_that_replied:{"reply_id":"{{reply_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.reply_id` | `integer` | Yes |  | The ID of the reply. |

**Direct CLI:**

```
deepline tools execute emailbison_unsubscribe_contact_that_replied --payload '{
  "reply_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-unsubscribe-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_unsubscribe_lead:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `integer` | Yes |  | The ID of the contact |

**Direct CLI:**

```
deepline tools execute emailbison_unsubscribe_lead --payload '{
  "lead_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-areply-template`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_areply_template:{"id":"{{id}}","name":"{{name}}","body":"{{body}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The ID of the reply template. |
| `payload.name` | `string` | Yes |  | The name of the new reply template. |
| `payload.body` | `string` | Yes |  | The full contents of the reply template message. |

**Direct CLI:**

```
deepline tools execute emailbison_update_areply_template --payload '{
  "id": "integer",
  "name": "string",
  "body": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-awebhook`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_awebhook:{"id":"{{id}}","name":"{{name}}","url":"{{url}}","events":"{{events}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The ID of the webhook. |
| `payload.name` | `string` | Yes |  | The name of the webhook. |
| `payload.url` | `string` | Yes |  | The URL to send data to. |
| `payload.events` | `array` | Yes |  | The events to subscribe to. |

**Direct CLI:**

```
deepline tools execute emailbison_update_awebhook --payload '{
  "id": "integer",
  "name": "string",
  "url": "string",
  "events": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-campaign-schedule`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_campaign_schedule:{"campaign_id":"{{campaign_id}}","monday":"{{monday}}","tuesday":"{{tuesday}}","wednesday":"{{wednesday}}","thursday":"{{thursday}}","friday":"{{friday}}","saturday":"{{saturday}}","sunday":"{{sunday}}","start_time":"{{start_time}}","end_time":"{{end_time}}","timezone":"{{timezone}}","save_as_template":"{{save_as_template}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |
| `payload.monday` | `boolean` | Yes |  | Whether the schedule includes Monday. |
| `payload.tuesday` | `boolean` | Yes |  | Whether the schedule includes Tuesday. |
| `payload.wednesday` | `boolean` | Yes |  | Whether the schedule includes Wednesday. |
| `payload.thursday` | `boolean` | Yes |  | Whether the schedule includes Thursday. |
| `payload.friday` | `boolean` | Yes |  | Whether the schedule includes Friday. |
| `payload.saturday` | `boolean` | Yes |  | Whether the schedule includes Saturday. |
| `payload.sunday` | `boolean` | Yes |  | Whether the schedule includes Sunday. |
| `payload.start_time` | `string` | Yes |  | The start time in HH:MM format. |
| `payload.end_time` | `string` | Yes |  | The end time in HH:MM format. |
| `payload.timezone` | `string` | Yes |  | The timezone of the schedule. |
| `payload.save_as_template` | `boolean` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute emailbison_update_campaign_schedule --payload '{
  "campaign_id": "integer",
  "monday": false,
  "tuesday": false,
  "wednesday": false,
  "thursday": false,
  "friday": false,
  "saturday": false,
  "sunday": false,
  "start_time": "string",
  "end_time": "string",
  "timezone": "string",
  "save_as_template": false
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-campaign-settings`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_campaign_settings:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_update_campaign_settings --payload '{
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-daily-warmup-limits-for-email-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_daily_warmup_limits_for_email_accounts:{"sender_email_ids":"{{sender_email_ids}}","daily_limit":"{{daily_limit}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sender_email_ids` | `array` | Yes |  | Array of sender email IDs. |
| `payload.daily_limit` | `integer` | Yes |  | The daily limit of warmup emails to send |

**Direct CLI:**

```
deepline tools execute emailbison_update_daily_warmup_limits_for_email_accounts --payload '{
  "sender_email_ids": "array",
  "daily_limit": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_lead:{"lead_id":"{{lead_id}}","first_name":"{{first_name}}","email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  | Optional parameter. int\|string The ID or email of the lead |
| `payload.first_name` | `string` | Yes |  | The first name of the contact. |
| `payload.email` | `string` | Yes |  | The email address of the contact. Must be in a valid email format. |

**Direct CLI:**

```
deepline tools execute emailbison_update_lead --payload '{
  "lead_id": "string",
  "first_name": "string",
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-lead-api-leads-lead-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_lead_api_leads_lead_id:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  | Optional parameter. int\|string The ID or email of the lead |

**Direct CLI:**

```
deepline tools execute emailbison_update_lead_api_leads_lead_id --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-lead-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_lead_status:{"lead_id":"{{lead_id}}","status":"verified"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  | The ID or email of the lead. |
| `payload.status` | `"verified" | "unverified" | "unknown" | "unsubscribed" | "risky" | "inactive"` | Yes |  | The status to apply to the lead. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.status` | `verified`, `unverified`, `unknown`, `unsubscribed`, `risky`, `inactive` |

**Direct CLI:**

```
deepline tools execute emailbison_update_lead_status --payload '{
  "lead_id": "string",
  "status": "verified"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-master-inbox-settings`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_master_inbox_settings:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_update_master_inbox_settings --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-or-create-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_or_create_lead:{"lead_id":"{{lead_id}}","first_name":"{{first_name}}","email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  | Optional parameter. int\|string\|null optional The ID or email of the lead |
| `payload.first_name` | `string` | Yes |  | The first name of the contact. |
| `payload.email` | `string` | Yes |  | The email address of the contact. Must be in a valid email format. |

**Direct CLI:**

```
deepline tools execute emailbison_update_or_create_lead --payload '{
  "lead_id": "string",
  "first_name": "string",
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-or-create-multiple-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_or_create_multiple_leads:{"leads":"{{leads}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.leads` | `array` | Yes |  | An array of lead objects. |

**Direct CLI:**

```
deepline tools execute emailbison_update_or_create_multiple_leads --payload '{
  "leads": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-password`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_password:{"current_password":"{{current_password}}","password":"{{password}}","password_confirmation":"{{password_confirmation}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.current_password` | `string` | Yes |  | Your current password. |
| `payload.password` | `string` | Yes |  | Your new password. |
| `payload.password_confirmation` | `string` | Yes |  | Your new password repeated. |

**Direct CLI:**

```
deepline tools execute emailbison_update_password --payload '{
  "current_password": "string",
  "password": "string",
  "password_confirmation": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-profile-picture`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_profile_picture:{"photo":"{{photo}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.photo` | `string` | Yes |  | The new profile picture to update. Must be a valid image file (jpg, jpeg or png) and no larger than 1MB. |

**Direct CLI:**

```
deepline tools execute emailbison_update_profile_picture --payload '{
  "photo": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-sender-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_sender_email:{"senderEmailId":"{{senderEmailId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.senderEmailId` | `integer` | Yes |  | The ID (or email address) of the email account. |

**Direct CLI:**

```
deepline tools execute emailbison_update_sender_email --payload '{
  "senderEmailId": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-sequence-stepsdeprecated`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_sequence_stepsdeprecated:{"sequence_id":"{{sequence_id}}","title":"{{title}}","sequence_steps":"{{sequence_steps}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sequence_id` | `integer` | Yes |  | The ID of the sequence. You can get this on the Campaign object. |
| `payload.title` | `string` | Yes |  | The title for the sequence. |
| `payload.sequence_steps` | `array` | Yes |  | The array containing the sequence steps |

**Direct CLI:**

```
deepline tools execute emailbison_update_sequence_stepsdeprecated --payload '{
  "sequence_id": "integer",
  "title": "string",
  "sequence_steps": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-sequence-stepsv11`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_sequence_stepsv11:{"sequence_id":"{{sequence_id}}","title":"{{title}}","sequence_steps":"{{sequence_steps}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sequence_id` | `integer` | Yes |  | The ID of the sequence. You can get this on the Campaign object. |
| `payload.title` | `string` | Yes |  | The title for the sequence. |
| `payload.sequence_steps` | `array` | Yes |  | The array containing the sequence steps |

**Direct CLI:**

```
deepline tools execute emailbison_update_sequence_stepsv11 --payload '{
  "sequence_id": "integer",
  "title": "string",
  "sequence_steps": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_workspace:{"team_id":"{{team_id}}","name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.team_id` | `integer` | Yes |  | The ID of the workspace. |
| `payload.name` | `string` | Yes |  | The new workspace name. |

**Direct CLI:**

```
deepline tools execute emailbison_update_workspace --payload '{
  "team_id": "integer",
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-workspace-api-workspaces-v1-1-team-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_workspace_api_workspaces_v1_1_team_id:{"team_id":"{{team_id}}","name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.team_id` | `integer` | Yes |  | The ID of the team (workspace) to delete. |
| `payload.name` | `string` | Yes |  | The new workspace name. |

**Direct CLI:**

```
deepline tools execute emailbison_update_workspace_api_workspaces_v1_1_team_id --payload '{
  "team_id": "integer",
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-workspace-member`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_workspace_member:{"user_id":"{{user_id}}","role":"admin"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.user_id` | `integer` | Yes |  | The ID of the team member. |
| `payload.role` | `"admin" | "editor" | "client" | "reseller"` | Yes |  | The new role of the team member. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.role` | `admin`, `editor`, `client`, `reseller` |

**Direct CLI:**

```
deepline tools execute emailbison_update_workspace_member --payload '{
  "user_id": "integer",
  "role": "admin"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-update-workspace-member-role`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_update_workspace_member_role:{"user_id":"{{user_id}}","role":"admin"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.user_id` | `integer` | Yes |  | The ID of the team member. |
| `payload.role` | `"admin" | "editor" | "client" | "reseller"` | Yes |  | The new role of the team member. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.role` | `admin`, `editor`, `client`, `reseller` |

**Direct CLI:**

```
deepline tools execute emailbison_update_workspace_member_role --payload '{
  "user_id": "integer",
  "role": "admin"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-view-all-available-schedule-timezones`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_view_all_available_schedule_timezones:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_view_all_available_schedule_timezones --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-view-all-schedule-templates`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_view_all_schedule_templates:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute emailbison_view_all_schedule_templates --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-view-campaign-schedule`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_view_campaign_schedule:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_view_campaign_schedule --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-view-campaign-sequence-stepsdeprecated`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_view_campaign_sequence_stepsdeprecated:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_view_campaign_sequence_stepsdeprecated --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-view-campaign-sequence-stepsv11`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_view_campaign_sequence_stepsv11:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer` | Yes |  | The ID of the campaign. |

**Direct CLI:**

```
deepline tools execute emailbison_view_campaign_sequence_stepsv11 --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-workspace-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_workspace_details:{"team_id":"{{team_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.team_id` | `integer` | Yes |  | The ID of the workspace. |

**Direct CLI:**

```
deepline tools execute emailbison_workspace_details --payload '{
  "team_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `emailbison-workspace-details-api-workspaces-v1-1-team-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=emailbison_workspace_details_api_workspaces_v1_1_team_id:{"team_id":"{{team_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.team_id` | `integer` | Yes |  | The ID of the team (workspace) to delete. |

**Direct CLI:**

```
deepline tools execute emailbison_workspace_details_api_workspaces_v1_1_team_id --payload '{
  "team_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Enformion

*Provider folder: `enformion` · 4 endpoints*


### Enformion — Agent Guidance

## Playbook

# EnformionGO Integration Guide

Consumer skip-trace API (Galaxy) for finding an individual’s **personal contact info** — personal emails and mobile phones — from name + city/state.Tool quick-pick:

- **Personal email** → `enformion_person_search` (Person database; returns `emailAddresses[]` flagged `nonBusiness:1`). Do NOT use `enformion_workplace_search` for personal email — it returns other-employer work emails.
- **Personal mobile (phone-first)** → `enformion_contact_enrich` (skip-trace, ~53% mobile hit rate).
- **Officers of a business** → `enformion_business_search` → officer `tahoeId` → `enformion_person_search` by tahoeId.

## When to use

Use `enformion_contact_enrich` when you need to find the personal mobile of a small business owner (restaurant, retail, local services) who has:

- No LinkedIn profile
- No company domain email
- No B2B database presence

This is the right tool after you’ve already extracted the owner’s name from a public source (Google search, Yelp, SOS officer records, OpenMart staff).

## Critical gotchas — read before calling

### 1\. Strip middle initials from last\_name (automatic)

The integration automatically strips middle initials. You do NOT need to pre-process — just pass the raw last name. But be aware:

- `"K Tarzai"` → bare last = `"Tarzai"` ✓
- `"De La Cruz Castillo"` → bare last = `"Castillo"` ✓
- `"Smith Jr."` → bare last = `"Smith"` ✓

If you want to override this, pass only the bare last word yourself.

### 2\. FL SOS officer names (use reverseFlOfficerName helper)

Florida SOS returns officer names as `"LASTNAME, FIRSTNAME MIDDLE"`. You must reverse these before calling:

- `"SMITH, JOHN EDWARD"` → `"John Smith"` ✓
- `"DE LA CRUZ, MARIA"` → `"Maria De La Cruz"` ✓

The helper `reverseFlOfficerName()` is exported from `actions/enformion-shared.ts`.

### 3\. Rate limits

No documented rate limit, but runs degrade with >200 concurrent requests. The Deepline rate limiter caps at 10 req/s which is safe for all batch sizes.

### 4\. No-result interpretation

- `identityScore: 0` = no match found (not billed)
- `identityScore: 75–100` = high confidence match
- `person.phones[]` empty = found the person but no phone on record

## Response structure

```
{
  "person": {
    "name": { "firstName": "Gary", "lastName": "Lincoln" },
    "age": "45",
    "addresses": [...],
    "phones": [\
      {\
        "number": "(856) 725-5922",\
        "type": "mobile",\
        "isConnected": true,\
        "firstReportedDate": "...",\
        "lastReportedDate": "..."\
      }\
    ],
    "emails": [...]
  },
  "identityScore": 100,
  "isError": false
}
```

Extract the mobile: `person.phones.find(p => p.type === "mobile" && p.isConnected === true)?.number`

## Validation

After finding a phone via EnformionGO, validate it with `trestle_phone_validation`:

- `line_type: "Mobile"` \+ `activity_score >= 60` = confirmed owner mobile
- `line_type: "Landline"` or `line_type: "FixedVOIP"` = store/office phone — discard

## Credentials

Requires two custom headers set from stored credentials:

- `galaxy-ap-name`: your API key name
- `galaxy-ap-password`: your API key password

Set via Deepline dashboard → Integrations → EnformionGO, or via `ENFORMION_KEY_NAME` \+ `ENFORMION_KEY_PASSWORD` environment variables.API endpoint: `https://devapi.enformion.com/Contact/Enrich`

* * *

## enformion\_person\_search — PERSONAL emails (use this, not workplace\_search)

### When to use

Use `enformion_person_search` whenever you need a person’s **personal email** (and/or phone). This is the Galaxy “Person Search” database (`galaxy-search-type: Person`). Unlike Workplace Search — which returns the owner’s _other-employer_ work emails 97% of the time — Person Search returns the individual’s personal addresses (gmail/yahoo/comcast), each flagged `nonBusiness: 1`.Two lookup modes:

- **By name + city/state** — the common path. `enformion_person_search({ first_name, last_name, city_state })`.
- **By tahoe\_id** — exact-person lookup. Pass `{ tahoe_id }` to pin one identity (e.g. an officer tahoeId returned by `enformion_business_search`). When `tahoe_id` is set, the name/location fields are ignored.

Middle initials and compound surname prefixes are stripped automatically (same silent-zero-result bug as Contact/Enrich).

### Recommended flow for business officers (vendor-recommended)

To get the personal contact info of a business’s officers/agents:

1. `enformion_business_search({ name, city_state })` → business record with `officers[]` / agents, each carrying a `tahoeId`.
2. For each officer, `enformion_person_search({ tahoe_id })` → that person’s `emailAddresses[]` \+ `phoneNumbers[]`.

**Entitlement note:** Business Search is a _separately entitled_ Galaxy product. If the access profile is not provisioned for it, the call returns `isError: true` (“Access Profile does not permit client to call Business Search.”). When that happens, **fall back to person-search-by-name** using the officer name you already resolved (from Google/Yelp/SOS) — Person Search is entitled and returns the same personal contact data. (As of 2026-06-09 the `aeroailabs` profile has Person Search but not Business Search.)

### Response structure (Person Search)

```
{
  "persons": [\
    {\
      "tahoeId": "G-2492258155993150029",\
      "fullName": "Maria Delcarmen Castillo",\
      "name": { "firstName": "Maria", "lastName": "Castillo" },\
      "age": 49,\
      "emailAddresses": [\
        { "emailAddress": "mcastillo12692@gmail.com", "emailOrdinal": 1, "isPremium": true, "nonBusiness": 1 }\
      ],\
      "phoneNumbers": [\
        { "phoneNumber": "(305) 555-0142", "phoneType": "Wireless", "isConnected": true }\
      ],\
      "addresses": [ { "fullAddress": "...", "city": "Miami", "state": "FL" } ]\
    }\
  ],
  "isError": false
}
```

Extract the best personal email: `persons[0].emailAddresses[0].emailAddress` (prefer entries with `isPremium: true`). No result: `persons` is an empty array.

### Billing

Billed per match (`post_deduct`, $0.35) only when the top match has a personal email or a connected phone. No charge on empty / no-match.API endpoint: `https://devapi.enformion.com/PersonSearch` (`galaxy-search-type: Person`).
Business Search endpoint: `https://api.galaxysearchapi.com/BusinessV2Search` (`galaxy-search-type: BusinessV2`, body uses camelCase `businessName` \+ `addressLine2`). Note the different host from the other endpoints — Business Search is served from `api.galaxysearchapi.com`, not `devapi.enformion.com`.

* * *

## enformion\_workplace\_search

### When to use

Use `enformion_workplace_search` as a **fallback** when `enformion_contact_enrich` returns `identityScore: 0` or an empty phones array. It queries a separate B2B employment profile database and has a different rate-limit bucket, so it does not compete with Contact/Enrich capacity.`city_state` is optional here (unlike Contact/Enrich where it is required). Omitting it broadens the match at the cost of potentially more ambiguous results.

### Critical warning: emailAddresses\[\]

`emailAddresses[]` in Workplace Search results are almost always the owner’s OTHER employer (a corporate day job, a franchise group, etc.) and NOT the restaurant’s email. In practice:

- 97% of returned email addresses belong to a different employer.
- Only use an email address if the domain matches the target restaurant’s domain (e.g., `gary@lincolnautogroup.com` is useless if you want `gary@lincolndiner.com`).

### Rate-limit bucket

Workplace Search uses a **separate** rate-limit bucket from Contact/Enrich. Both can run concurrently without reducing each other’s throughput. The Deepline rate limiter treats them as independent pools.

### Response structure

```
{
  "workplaceRecords": [\
    {\
      "fullName": "Gary Lincoln",\
      "firstName": "Gary",\
      "lastName": "Lincoln",\
      "professionalTitles": "Owner",\
      "phoneNumbers": ["+18567255922"],\
      "emailAddresses": ["gary.lincoln@someemployer.com"],\
      "currentEmployment": [\
        {\
          "employer": "Lincoln Auto Group",\
          "jobTitle": "Owner",\
          "level": "Owner",\
          "department": "Management"\
        }\
      ]\
    }\
  ]
}
```

Extract phone: `workplaceRecords[0]?.phoneNumbers?.[0]`No result: `workplaceRecords` will be an empty array `[]`.

### Billing

Billed per match (`post_deduct`, $0.50) only when a workplace record with a phone or email is returned. No charge on empty / no-match.


### Enformion — Endpoints


#### `enformion-business-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=enformion_business_search:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | Business / entity name (e.g. ‘Castaways River Tiki Bar’). |

**Direct CLI:**

```
deepline tools execute enformion_business_search --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `4.9` per pricing unit.
- Billing mode: `post_deduct`.


#### `enformion-contact-enrich`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=enformion_contact_enrich:{"first_name":"{{first_name}}","last_name":"{{last_name}}","city_state":"{{city_state}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Owner first name. Do not include middle name or initial. |
| `payload.last_name` | `string` | Yes |  | Owner last name. The integration automatically strips middle initials and compound surname prefixes (e.g. ‘De La Cruz’ → ‘Cruz’) because EnformionGO returns zero results for multi-word last names. |
| `payload.city_state` | `string` | Yes |  | City and state as “City, ST” (e.g. “Cleveland, OH”). Used as Address.addressLine2 in the EnformionGO request. |

**Direct CLI:**

```
deepline tools execute enformion_contact_enrich --payload '{
  "first_name": "string",
  "last_name": "string",
  "city_state": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.68` per pricing unit.
- Billing mode: `post_deduct`.


#### `enformion-person-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=enformion_person_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute enformion_person_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `3.5` per pricing unit.
- Billing mode: `post_deduct`.


#### `enformion-workplace-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=enformion_workplace_search:{"first_name":"{{first_name}}","last_name":"{{last_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | Owner first name. |
| `payload.last_name` | `string` | Yes |  | Owner last name. Middle initials are stripped automatically. |

**Direct CLI:**

```
deepline tools execute enformion_workplace_search --payload '{
  "first_name": "string",
  "last_name": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `7` per pricing unit.
- Billing mode: `post_deduct`.


---

## Exa

*Provider folder: `exa` · 9 endpoints*


### Exa — Agent Guidance

## Playbook

Use Exa for web-grounded retrieval, then synthesis.**`type` vs `category`:**`type` controls the search strategy (auto/fast/neural/deep). `category` filters result type (company/people/news/financial report). These are independent params. `"type":"news"` is a 422 error — use `"category":"news"` for news results.

- In AI-column workflows, instruct the model prompt to use Exa retrieval explicitly for website-derived tech stack and on-site signals.
- Use direct Exa tool calls when you need tighter provider controls or auditable step-by-step retrieval outside AI-column orchestration.
- For auditable outputs, run `exa_search`/`exa_contents` first and synthesize after inspecting citations.
- Use focused queries and small `numResults` during pilots, then widen only if coverage is low.
- Treat `exa_answer` as the summarization layer, not the first retrieval step, when precision matters.

```
deepline tools execute exa_search --payload '{"query":"series b devtools companies united states","numResults":5,"type":"fast"}'
```

```
deepline tools execute exa_contents --payload '{"urls":["https://example.com"],"text":true}'
```

```
deepline tools execute exa_answer --payload '{"query":"Summarize the top GTM signals from these results","text":true}'
```


### Exa — Endpoints


#### `answer`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=exa_answer:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | Question to answer using web results. |

**Direct CLI:**

```
deepline tools execute exa_answer --payload '{
  "query": "latest developments in AI agents"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `contents`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=exa_contents:{"urls":["https://example.com"]}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute exa_contents --payload '{
  "urls": [\
    "https://example.com"\
  ]
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `exa-agent`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=exa_agent:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | Natural-language question or instructions for Exa Agent. |

**Direct CLI:**

```
deepline tools execute exa_agent --payload '{
  "query": "latest developments in AI agents"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `exa-company-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=exa_company_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute exa_company_search --payload '{
  "query": "latest developments in AI agents"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `exa-get-agent-run`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=exa_get_agent_run:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute exa_get_agent_run --payload '{
  "id": "doc_123"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `exa-get-research-result`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=exa_get_research_result:{"researchId":"{{researchId}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.researchId` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute exa_get_research_result --payload '{
  "researchId": "doc_123"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `exa-people-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=exa_people_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute exa_people_search --payload '{
  "query": "latest developments in AI agents"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `exa-research`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=exa_research:{"instructions":"{{instructions}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.instructions` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute exa_research --payload '{
  "instructions": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=exa_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | The search query string. |

**Direct CLI:**

```
deepline tools execute exa_search --payload '{
  "query": "latest developments in AI agents"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


---

## Findymail

*Provider folder: `findymail` · 29 endpoints*


### Findymail — Agent Guidance

## Playbook

# Findymail

Use Findymail for verified B2B email lookup, email verification, phone lookup, company enrichment, similar-company discovery, technology lookup, and Intellimatch lead-list workflows.

- Prefer `findymail_find_from_name` when you have a person name plus company domain.
- Prefer `findymail_find_from_business_profile` or `findymail_reverse_email_lookup` when the input is a LinkedIn/profile URL or an existing email.
- Use `findymail_search_technologies` before `findymail_lookup_technologies_by_domain` when the caller wants to filter by a technology name.
- Intellimatch is asynchronous: call `findymail_search_leads`, poll `findymail_get_export_status`, then retrieve rows with `findymail_get_results`.
- Mutating list, exclusion-list, and signal-monitor operations require the caller’s own Findymail credential so agents do not mutate the shared provider account.


### Findymail — Endpoints


#### `findymail-add-excluded-domains`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_add_excluded_domains:{"domains":"{{domains}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domains` | `array` | Yes |  | Array of domain names to exclude (max 10000). |

**Direct CLI:**

```
deepline tools execute findymail_add_excluded_domains --payload '{
  "domains": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-create-exclusion-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_create_exclusion_list:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | The name of the list (must be unique per user). |

**Direct CLI:**

```
deepline tools execute findymail_create_exclusion_list --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-create-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_create_list:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | Name of the list |

**Direct CLI:**

```
deepline tools execute findymail_create_list --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-delete-exclusion-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_delete_exclusion_list:{"excludedDomainList_id":"{{excludedDomainList_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.excludedDomainList_id` | `integer` | Yes |  | The ID of the excludedDomainList. |

**Direct CLI:**

```
deepline tools execute findymail_delete_exclusion_list --payload '{
  "excludedDomainList_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-delete-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_delete_list:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The ID of the list. |

**Direct CLI:**

```
deepline tools execute findymail_delete_list --payload '{
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-find-employees`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_find_employees:{"website":"{{website}}","job_titles":"{{job_titles}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.website` | `string` | Yes |  | company website |
| `payload.job_titles` | `array` | Yes |  | target job titles (max 10) |

**Direct CLI:**

```
deepline tools execute findymail_find_employees --payload '{
  "website": "string",
  "job_titles": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `findymail-find-from-business-profile`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_find_from_business_profile:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | Person’s business profile URL. (can be full URL “ [https://linkedin.com/in/johndoe](https://linkedin.com/in/johndoe)” or username only “johndoe”) |

**Direct CLI:**

```
deepline tools execute findymail_find_from_business_profile --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `findymail-find-from-domain`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_find_from_domain:{"domain":"{{domain}}","roles":"{{roles}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Email domain |
| `payload.roles` | `array` | Yes |  | Target roles related to the given domain (max 3 roles) |

**Direct CLI:**

```
deepline tools execute findymail_find_from_domain --payload '{
  "domain": "string",
  "roles": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `findymail-find-from-name`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_find_from_name:{"name":"{{name}}","domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | Person’s full name |
| `payload.domain` | `string` | Yes |  | Company domain (best) or company name |

**Direct CLI:**

```
deepline tools execute findymail_find_from_name --payload '{
  "name": "string",
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `findymail-find-phone`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_find_phone:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | Person’s business profile URL. (can be full URL “ [https://linkedin.com/in/johndoe](https://linkedin.com/in/johndoe)” or username only “johndoe”) |

**Direct CLI:**

```
deepline tools execute findymail_find_phone --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `2.78` per pricing unit.
- Billing mode: `post_deduct`.


#### `findymail-get-company-information`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_get_company_information:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute findymail_get_company_information --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `findymail-get-excluded-domains`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_get_excluded_domains:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute findymail_get_excluded_domains --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-get-exclusion-list-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_get_exclusion_list_details:{"excludedDomainList_id":"{{excludedDomainList_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.excludedDomainList_id` | `integer` | Yes |  | The ID of the excludedDomainList. |

**Direct CLI:**

```
deepline tools execute findymail_get_exclusion_list_details --payload '{
  "excludedDomainList_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-get-export-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_get_export_status:{"hash":"{{hash}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.hash` | `string` | Yes |  | The export hash returned by the search endpoint. |

**Direct CLI:**

```
deepline tools execute findymail_get_export_status --payload '{
  "hash": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-get-results`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_get_results:{"hash":"{{hash}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.hash` | `string` | Yes |  | The hash returned by the search endpoint. |

**Direct CLI:**

```
deepline tools execute findymail_get_results --payload '{
  "hash": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-get-saved-contacts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_get_saved_contacts:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | Optional parameter. Get contacts from a specified list. If 0, no list used (“All contacts”). |

**Direct CLI:**

```
deepline tools execute findymail_get_saved_contacts --payload '{
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-get-signal`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_get_signal:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The ID of the signal. |

**Direct CLI:**

```
deepline tools execute findymail_get_signal --payload '{
  "id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-list-contact-lists`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_list_contact_lists:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute findymail_list_contact_lists --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-list-exclusion-lists`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_list_exclusion_lists:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute findymail_list_exclusion_lists --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-list-signals`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_list_signals:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute findymail_list_signals --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-lookup-technologies-by-domain`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_lookup_technologies_by_domain:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | The company domain to look up. |

**Direct CLI:**

```
deepline tools execute findymail_lookup_technologies_by_domain --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `findymail-remove-excluded-domains`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_remove_excluded_domains:{"ids":"{{ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.ids` | `array` | Yes |  | Array of domain IDs to delete. |

**Direct CLI:**

```
deepline tools execute findymail_remove_excluded_domains --payload '{
  "ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-reverse-email-lookup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_reverse_email_lookup:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address to lookup |

**Direct CLI:**

```
deepline tools execute findymail_reverse_email_lookup --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `findymail-search-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_search_leads:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | The search query describing target companies. |

**Direct CLI:**

```
deepline tools execute findymail_search_leads --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `findymail-search-lookalike-companies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_search_lookalike_companies:{"seed":"{{seed}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.seed` | `string` | Yes |  | The seed company domain or URL. |

**Direct CLI:**

```
deepline tools execute findymail_search_lookalike_companies --payload '{
  "seed": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `findymail-search-technologies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_search_technologies:{"q":"{{q}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.q` | `string` | Yes |  | Search term (min 2 characters). |

**Direct CLI:**

```
deepline tools execute findymail_search_technologies --payload '{
  "q": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-update-contact-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_update_contact_list:{"id":"{{id}}","name":"{{name}}","isShared":"{{isShared}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  | The ID of the list. |
| `payload.name` | `string` | Yes |  | New name of the list |
| `payload.isShared` | `boolean` | Yes |  | Enable/disable list sharing with the team |

**Direct CLI:**

```
deepline tools execute findymail_update_contact_list --payload '{
  "id": "integer",
  "name": "string",
  "isShared": false
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-update-exclusion-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_update_exclusion_list:{"excludedDomainList_id":"{{excludedDomainList_id}}","name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.excludedDomainList_id` | `integer` | Yes |  | The ID of the excludedDomainList. |
| `payload.name` | `string` | Yes |  | The new name for the list (must be unique per user). |

**Direct CLI:**

```
deepline tools execute findymail_update_exclusion_list --payload '{
  "excludedDomainList_id": "integer",
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `findymail-verify-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=findymail_verify_email:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email to verify |

**Direct CLI:**

```
deepline tools execute findymail_verify_email --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


---

## Firecrawl

*Provider folder: `firecrawl` · 17 endpoints*


### Firecrawl — Agent Guidance

## Playbook

# Firecrawl — Agent Guidance

## Action selection

- **Single page** → `firecrawl_scrape`. Returns markdown by default. Costs 1 Firecrawl credit plus scrape option modifiers.
- **Web search + content** → `firecrawl_search`. Replaces Google search + individual page scraping in one call.
- **Site discovery** → `firecrawl_map` first to enumerate URLs, then `firecrawl_batch_scrape` the ones you need.
- **Full site crawl** → `firecrawl_crawl_params_preview` to estimate cost, then `firecrawl_crawl`.
- **Known URL list** → `firecrawl_batch_scrape`. More efficient than individual scrapes.
- **Structured extraction** → `firecrawl_extract` with a JSON schema or natural language prompt.
- **Complex web tasks** → `firecrawl_agent` with a natural language instruction. The agent navigates pages autonomously.

## Budget awareness

- Credit costs vary by action: scrape/crawl/batch scrape are 1 Firecrawl credit per page plus modifiers; map is 1 credit per discovered page; search is 2 credits per 10 results plus scrape modifiers; agent and extract are dynamic.
- Scrape modifiers stack: PDF parsing +1 credit per PDF page, JSON format +4 credits per page, Enhanced Mode +4 credits per page, and Zero Data Retention +1 credit per page.
- Firecrawl can charge when infrastructure processes a request even if the target returns 403/404. Avoid blind retries of blocked URLs; inspect `metadata.statusCode`.
- Crawl defaults to `limit: 10000` and Firecrawl preflights available credits against that limit. Always pass an explicit lower `limit` unless a 10000-page crawl is intentional.
- `crawl_params_preview` is free and shows estimated credit usage before committing.

## Async operations

- `crawl`, `batch_scrape`, `agent`, and `extract` are async. They return a job ID immediately.
- The action handler polls automatically for up to 5 minutes and returns results when ready.
- For non-blocking usage, use the corresponding status-check action (`get_crawl_status`, etc.) to poll manually.
- Cancel long-running jobs with the cancel actions when results are no longer needed.

## Format recommendations

- Use `markdown` format for LLM consumption (default).
- Use `html` when you need the raw DOM structure.
- Use `links` to extract all hyperlinks from a page.
- Use `screenshot` when visual layout matters.

## Rate limits

- Standard rate limit is 15 requests/second.
- Batch and crawl operations are rate-limited server-side; the API handles queuing.


### Firecrawl — Endpoints


#### `agent`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_agent:{"prompt":"{{prompt}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.prompt` | `string` | Yes |  | Natural language instruction for the AI agent (max 10000 chars) |

**Direct CLI:**

```
deepline tools execute firecrawl_agent --payload '{
  "prompt": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `batch-scrape`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_batch_scrape:{"urls":"{{urls}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.urls` | `array` | Yes |  | URLs to scrape |

**Direct CLI:**

```
deepline tools execute firecrawl_batch_scrape --payload '{
  "urls": "array"
}' --json
```

**Cost:**

- Pricing model: `per_page` (per page).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `cancel-agent`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_cancel_agent:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Agent job ID to cancel |

**Direct CLI:**

```
deepline tools execute firecrawl_cancel_agent --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `cancel-batch-scrape`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_cancel_batch_scrape:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Batch scrape job ID to cancel |

**Direct CLI:**

```
deepline tools execute firecrawl_cancel_batch_scrape --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `cancel-crawl`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_cancel_crawl:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Crawl job ID to cancel |

**Direct CLI:**

```
deepline tools execute firecrawl_cancel_crawl --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `crawl`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_crawl:{"url":"{{url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.url` | `string` | Yes |  | Starting URL to crawl |

**Direct CLI:**

```
deepline tools execute firecrawl_crawl --payload '{
  "url": "string"
}' --json
```

**Cost:**

- Pricing model: `per_page` (per page).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `crawl-params-preview`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_crawl_params_preview:{"url":"{{url}}","prompt":"{{prompt}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.url` | `string` | Yes |  | URL to preview crawl params for |
| `payload.prompt` | `string` | Yes |  | Natural language description of what to crawl |

**Direct CLI:**

```
deepline tools execute firecrawl_crawl_params_preview --payload '{
  "url": "string",
  "prompt": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `extract`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_extract:{"urls":"{{urls}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.urls` | `array` | Yes |  | URLs to extract data from |

**Direct CLI:**

```
deepline tools execute firecrawl_extract --payload '{
  "urls": "array"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `get-agent-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_get_agent_status:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Agent job ID |

**Direct CLI:**

```
deepline tools execute firecrawl_get_agent_status --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-batch-scrape-errors`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_get_batch_scrape_errors:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Batch scrape job ID |

**Direct CLI:**

```
deepline tools execute firecrawl_get_batch_scrape_errors --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-batch-scrape-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_get_batch_scrape_status:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Batch scrape job ID |

**Direct CLI:**

```
deepline tools execute firecrawl_get_batch_scrape_status --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-crawl-errors`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_get_crawl_errors:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Crawl job ID |

**Direct CLI:**

```
deepline tools execute firecrawl_get_crawl_errors --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-crawl-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_get_crawl_status:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Crawl job ID |

**Direct CLI:**

```
deepline tools execute firecrawl_get_crawl_status --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-extract-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_get_extract_status:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Extract job ID |

**Direct CLI:**

```
deepline tools execute firecrawl_get_extract_status --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `map`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_map:{"url":"{{url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.url` | `string` | Yes |  | URL to map |

**Direct CLI:**

```
deepline tools execute firecrawl_map --payload '{
  "url": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `scrape`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_scrape:{"url":"{{url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.url` | `string` | Yes |  | URL to scrape |

**Direct CLI:**

```
deepline tools execute firecrawl_scrape --payload '{
  "url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=firecrawl_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | Search query (max 500 chars) |

**Direct CLI:**

```
deepline tools execute firecrawl_search --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


---

## Forager

*Provider folder: `forager` · 19 endpoints*


### Forager — Agent Guidance

## Playbook

# Forager Workflow Guidance

Forager has 850M+ B2B person records and is especially strong for verified mobile phone numbers (200M+ database). Prefer Forager over other providers when the goal is phone number discovery.

## Search & Discovery

- Always start with totals operations (`forager_person_role_search_totals`, `forager_organization_search_totals`, `forager_job_search_totals`) to estimate result volume at zero credit cost before running paid searches.
- Use `forager_person_role_search` for prospecting by role, skills, and company criteria. Each page costs 1 credit.
- Use `forager_organization_search` for company prospecting by industry, size, technology stack, and revenue filters.
- Use `forager_job_search` for intent-signal analysis: companies hiring for specific roles indicate growth and budget in those areas.

## Boolean Text Search

- Fields like `role_title`, `role_description`, `person_name`, `organization_description` use boolean text search.
- **Multi-word phrases MUST be quoted**: `'"VP Sales"'` not `'VP Sales'`. Unquoted phrases cause a parse error.
- Supports AND, OR, NOT, and parentheses: `("VP Sales" OR "Director of Sales") NOT intern`

## Filter Fields Use Integer IDs

- `person_locations`, `person_skills`, `person_industries`, `organization_locations`, etc. all require **integer IDs**, not name strings.
- Retrieve IDs from the lookup operations first (e.g. `forager_industries`, `forager_locations`, `forager_person_skills`).

## Enrichment & Reveals

Use `forager_person_detail_lookup` to enrich a known person by `person_id` or `linkedin_public_identifier`.
Use `forager_person_detail_reverse_lookup_by_email` when you already know a work/personal email and want to resolve the person.

- Use `forager_person_detail_reverse_lookup_by_phone_number` when you already know a phone number and want to resolve the person.
- Use `forager_website_detail_lookup` for technographic enrichment on a specific domain.
- API docs: [https://docs.forager.ai/openapi/api\_keys](https://docs.forager.ai/openapi/api_keys)

## Cross-Provider Workflow Tips

- When building name-dependent workflows (e.g. email pattern generation), start from a source that returns full names before using Forager for phone/email reveals.
- Forager person search results include `person_id` — save this for subsequent `forager_person_detail_lookup` calls to avoid re-searching.
- Check account balance from Forager before large batch operations.


### Forager — Endpoints


#### `forager-industry-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_industry_autocomplete:{"q":"{{q}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.q` | `string` | Yes |  | Industry name. |

**Direct CLI:**

```
deepline tools execute forager_industry_autocomplete --payload '{
  "q": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `forager-job-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_job_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute forager_job_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_page` (per page).
- Estimated Deepline credits: `0.63` per pricing unit.
- Billing mode: `post_deduct`.


#### `forager-job-search-totals`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_job_search_totals:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute forager_job_search_totals --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `forager-location-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_location_autocomplete:{"q":"{{q}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.q` | `string` | Yes |  | City, State, Country or Continent. |

**Direct CLI:**

```
deepline tools execute forager_location_autocomplete --payload '{
  "q": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `forager-organization-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_organization_autocomplete:{"q":"{{q}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.q` | `string` | Yes |  | Organization name or domain. |

**Direct CLI:**

```
deepline tools execute forager_organization_autocomplete --payload '{
  "q": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `forager-organization-keyword-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_organization_keyword_autocomplete:{"q":"{{q}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.q` | `string` | Yes |  | Organization keyword name. |

**Direct CLI:**

```
deepline tools execute forager_organization_keyword_autocomplete --payload '{
  "q": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `forager-organization-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_organization_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute forager_organization_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_page` (per page).
- Estimated Deepline credits: `0.32` per pricing unit.
- Billing mode: `post_deduct`.


#### `forager-organization-search-totals`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_organization_search_totals:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute forager_organization_search_totals --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `forager-person-contacts-lookup-personal-emails`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_person_contacts_lookup_personal_emails:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute forager_person_contacts_lookup_personal_emails --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `forager-person-contacts-lookup-phone-numbers`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_person_contacts_lookup_phone_numbers:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute forager_person_contacts_lookup_phone_numbers --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `4.67` per pricing unit.
- Billing mode: `post_deduct`.


#### `forager-person-contacts-lookup-work-emails`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_person_contacts_lookup_work_emails:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute forager_person_contacts_lookup_work_emails --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `forager-person-detail-lookup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_person_detail_lookup:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute forager_person_detail_lookup --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.32` per pricing unit.
- Billing mode: `post_deduct`.


#### `forager-person-detail-reverse-lookup-by-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_person_detail_reverse_lookup_by_email:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | The personal email address of the person being searched. |

**Direct CLI:**

```
deepline tools execute forager_person_detail_reverse_lookup_by_email --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `forager-person-detail-reverse-lookup-by-phone-number`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_person_detail_reverse_lookup_by_phone_number:{"phone_number":"{{phone_number}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.phone_number` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute forager_person_detail_reverse_lookup_by_phone_number --payload '{
  "phone_number": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `4.67` per pricing unit.
- Billing mode: `post_deduct`.


#### `forager-person-role-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_person_role_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute forager_person_role_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_page` (per page).
- Estimated Deepline credits: `0.32` per pricing unit.
- Billing mode: `post_deduct`.


#### `forager-person-role-search-totals`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_person_role_search_totals:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute forager_person_role_search_totals --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `forager-person-skill-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_person_skill_autocomplete:{"q":"{{q}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.q` | `string` | Yes |  | Person skill name. |

**Direct CLI:**

```
deepline tools execute forager_person_skill_autocomplete --payload '{
  "q": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `forager-web-technology-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_web_technology_autocomplete:{"q":"{{q}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.q` | `string` | Yes |  | Web technology name. |

**Direct CLI:**

```
deepline tools execute forager_web_technology_autocomplete --payload '{
  "q": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `forager-website-detail-lookup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=forager_website_detail_lookup:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute forager_website_detail_lookup --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.32` per pricing unit.
- Billing mode: `post_deduct`.


---

## FullEnrich

*Provider folder: `fullenrich` · 7 endpoints*


### FullEnrich — Agent Guidance

## Playbook

# FullEnrich Agent Guidance

## Key patterns

- **Async submit + async fetch.**`fullenrich_bulk_enrich` and `fullenrich_reverse_email` start background jobs and return an `enrichment_id`. Poll with `fullenrich_get_result` or `fullenrich_get_reverse_result` for terminal data.
- **Use `enrich_fields`** to control what’s enriched: `contact.emails` (1 credit), `contact.phones` (10 credits), `contact.personal_emails` (3 credits).
- **LinkedIn URL** improves accuracy significantly (5-20% for emails, 10-60% for phones).
- **Email status hierarchy:** DELIVERABLE > HIGH\_PROBABILITY > CATCH\_ALL > INVALID. Use `most_probable_work_email` field for the best result.
- **Phone costs 10x email** — use judiciously and only when explicitly needed.
- **Search is synchronous** — use `fullenrich_people_search` or `fullenrich_company_search` for prospecting.
- Use `fullenrich_get_result` / `fullenrich_get_reverse_result` after every async submit when you need terminal data.
- **`forceResults=true`** query param on get-result returns partial results if enrichment is still running.

## When to use

- Best for high-quality email/phone waterfall enrichment with extensive provider coverage (20+ sources).
- Search API is good for prospecting by job title, company, location, industry.
- Reverse email lookup useful for identifying contacts from email addresses.

## When NOT to use

- Don’t use for email validation only — use a dedicated validator (ZeroBounce, LeadMagic validation).
- Don’t use phone enrichment unless explicitly needed — expensive (10 credits).
- For quick single-provider email lookups, LeadMagic or Prospeo are faster/cheaper.


### FullEnrich — Endpoints


#### `fullenrich-bulk-enrich`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=fullenrich_bulk_enrich:{"name":"{{name}}","data":"{{data}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | Readable enrichment batch name shown in the FullEnrich dashboard. |
| `payload.data` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute fullenrich_bulk_enrich --payload '{
  "name": "string",
  "data": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.84` per pricing unit.
- Billing mode: `post_deduct`.


#### `fullenrich-company-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=fullenrich_company_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute fullenrich_company_search --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.21` per pricing unit.
- Billing mode: `post_deduct`.


#### `fullenrich-get-result`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=fullenrich_get_result:{"enrichment_id":"{{enrichment_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.enrichment_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute fullenrich_get_result --payload '{
  "enrichment_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `fullenrich-get-reverse-result`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=fullenrich_get_reverse_result:{"enrichment_id":"{{enrichment_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.enrichment_id` | `string` | Yes |  | The unique identifier returned when the reverse lookup was started |

**Direct CLI:**

```
deepline tools execute fullenrich_get_reverse_result --payload '{
  "enrichment_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `fullenrich-people-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=fullenrich_people_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute fullenrich_people_search --payload '{}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.21` per pricing unit.
- Billing mode: `post_deduct`.


#### `fullenrich-reverse-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=fullenrich_reverse_email:{"name":"{{name}}","data":"{{data}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | Readable reverse lookup batch name shown in the FullEnrich dashboard. |
| `payload.data` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute fullenrich_reverse_email --payload '{
  "name": "string",
  "data": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.84` per pricing unit.
- Billing mode: `post_deduct`.


#### `fullenrich-verify-key`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=fullenrich_verify_key:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute fullenrich_verify_key --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Generic HTTP

*Provider folder: `generic_http` · 1 endpoints*


### Generic HTTP — Endpoints


#### `request`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=generic_http_request:{"url":"{{url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.url` | `string` | Yes |  | Absolute http/https URL to call. Private and localhost targets are blocked. |

**Direct CLI:**

```
deepline tools execute generic_http_request --payload '{
  "url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Google Ads Audiences

*Provider folder: `google_ads_audiences` · 5 endpoints*


### Google Ads Audiences — Endpoints


#### `create-audience`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=google_ads_audiences_create_audience:{"account_id":"{{account_id}}","name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `string` | Yes |  | Data Manager account ID (e.g. Google Ads customer ID, no formatting). |
| `payload.name` | `string` | Yes |  | displayName for the UserList. |

**Direct CLI:**

```
deepline tools execute google_ads_audiences_create_audience --payload '{
  "account_id": "string",
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `delete-audience`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=google_ads_audiences_delete_audience:{"account_id":"{{account_id}}","audience_id":"{{audience_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `string` | Yes |  | Data Manager account ID (e.g. Google Ads customer ID, no formatting). |
| `payload.audience_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute google_ads_audiences_delete_audience --payload '{
  "account_id": "string",
  "audience_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `get-audience-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=google_ads_audiences_get_audience_status:{"account_id":"{{account_id}}","audience_id":"{{audience_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `string` | Yes |  | Data Manager account ID (e.g. Google Ads customer ID, no formatting). |
| `payload.audience_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute google_ads_audiences_get_audience_status --payload '{
  "account_id": "string",
  "audience_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `list-audiences`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=google_ads_audiences_list_audiences:{"account_id":"{{account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `string` | Yes |  | Data Manager account ID (e.g. Google Ads customer ID, no formatting). |

**Direct CLI:**

```
deepline tools execute google_ads_audiences_list_audiences --payload '{
  "account_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `sync-audience-members`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=google_ads_audiences_sync_audience_members:{"account_id":"{{account_id}}","audience_id":"{{audience_id}}","rows":"{{rows}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `string` | Yes |  | Data Manager account ID (e.g. Google Ads customer ID, no formatting). |
| `payload.audience_id` | `string` | Yes |  |  |
| `payload.rows` | `array` | Yes |  | Audience member rows to upload. Most platforms accept much smaller per-request batches, so Deepline automatically chunks requests. |

**Direct CLI:**

```
deepline tools execute google_ads_audiences_sync_audience_members --payload '{
  "account_id": "string",
  "audience_id": "string",
  "rows": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


---

## HeyReach

*Provider folder: `heyreach` · 45 endpoints*


### HeyReach — Agent Guidance

## Playbook

Use HeyReach for outbound activation after qualification and verification are complete.

- Always list campaigns first and resolve the exact campaign target before inserts.
- HeyReach public API does not expose campaign creation. Do not attempt to create campaigns via API tools; create campaigns in HeyReach UI first, then reference the resulting `campaign_id`.
- Batch writes in small chunks and validate response shape before scaling.
- Pull campaign stats after insert operations to confirm downstream effects.

```
deepline tools execute heyreach_list_campaigns --payload '{}'
```

```
deepline tools execute heyreach_add_to_campaign --payload '{"campaign_id":"12345","contacts":[{"linkedin_url":"https://www.linkedin.com/in/example","first_name":"Ada","last_name":"Lovelace","email":"ada@example.com"}]}'
```


### HeyReach — Endpoints


#### `add-to-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_add_to_campaign:{"campaign_id":"{{campaign_id}}","contacts":"{{contacts}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  | Numeric HeyReach campaign ID. Use list\_campaigns to discover valid IDs. |
| `payload.contacts` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_add_to_campaign --payload '{
  "campaign_id": "string",
  "contacts": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `campaign-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_campaign_stats:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  | Numeric HeyReach campaign ID. |

**Direct CLI:**

```
deepline tools execute heyreach_get_campaign_stats --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-add-lead-tags`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_add_lead_tags:{"tags":"{{tags}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tags` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_add_lead_tags --payload '{
  "tags": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-add-leads-to-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_add_leads_to_list:{"list_id":"{{list_id}}","leads":"{{leads}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |
| `payload.leads` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_add_leads_to_list --payload '{
  "list_id": "integer",
  "leads": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-add-leads-to-list-v2`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_add_leads_to_list_v2:{"list_id":"{{list_id}}","leads":"{{leads}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |
| `payload.leads` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_add_leads_to_list_v2 --payload '{
  "list_id": "integer",
  "leads": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-check-api-key`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_check_api_key:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute heyreach_check_api_key --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-create-empty-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_create_empty_list:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_create_empty_list --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-create-lead-tags`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_create_lead_tags:{"tags":"{{tags}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tags` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_create_lead_tags --payload '{
  "tags": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-create-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_create_workspace:{"workspace_name":"{{workspace_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.workspace_name` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_create_workspace --payload '{
  "workspace_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-create-workspace-api-key`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_create_workspace_api_key:{"workspace_id":"{{workspace_id}}","api_key_type":"PUBLIC"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.workspace_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |
| `payload.api_key_type` | `"PUBLIC" | "N8N" | "MAKE" | "ZAPIER" | "MCP"` | Yes |  |  |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.api_key_type` | `PUBLIC`, `N8N`, `MAKE`, `ZAPIER`, `MCP` |

**Direct CLI:**

```
deepline tools execute heyreach_create_workspace_api_key --payload '{
  "workspace_id": "integer",
  "api_key_type": "PUBLIC"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-delete-leads-from-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_delete_leads_from_list:{"list_id":"{{list_id}}","lead_member_ids":"{{lead_member_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |
| `payload.lead_member_ids` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_delete_leads_from_list --payload '{
  "list_id": "integer",
  "lead_member_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-delete-leads-from-list-by-profile-url`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_delete_leads_from_list_by_profile_url:{"list_id":"{{list_id}}","profile_urls":"{{profile_urls}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |
| `payload.profile_urls` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_delete_leads_from_list_by_profile_url --payload '{
  "list_id": "integer",
  "profile_urls": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-all-users`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_all_users:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute heyreach_get_all_users --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-all-workspaces`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_all_workspaces:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute heyreach_get_all_workspaces --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-campaign-by-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_campaign_by_id:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_get_campaign_by_id --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-campaigns-for-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_campaigns_for_lead:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute heyreach_get_campaigns_for_lead --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-chatroom`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_chatroom:{"account_id":"{{account_id}}","conversation_id":"{{conversation_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |
| `payload.conversation_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_get_chatroom --payload '{
  "account_id": "integer",
  "conversation_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-companies-from-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_companies_from_list:{"list_id":"{{list_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_get_companies_from_list --payload '{
  "list_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-conversations`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_conversations:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute heyreach_get_conversations --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_lead:{"profile_url":"{{profile_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.profile_url` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_get_lead --payload '{
  "profile_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-lead-tags`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_lead_tags:{"profile_url":"{{profile_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.profile_url` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_get_lead_tags --payload '{
  "profile_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-leads-from-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_leads_from_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_get_leads_from_campaign --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-leads-from-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_leads_from_list:{"list_id":"{{list_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_get_leads_from_list --payload '{
  "list_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-linkedin-account-by-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_linkedin_account_by_id:{"account_id":"{{account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_get_linkedin_account_by_id --payload '{
  "account_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-list-by-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_list_by_id:{"list_id":"{{list_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_get_list_by_id --payload '{
  "list_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-lists-for-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_lists_for_lead:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute heyreach_get_lists_for_lead --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-my-network-for-sender`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_my_network_for_sender:{"sender_id":"{{sender_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sender_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_get_my_network_for_sender --payload '{
  "sender_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-overall-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_overall_stats:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute heyreach_get_overall_stats --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-user-by-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_user_by_id:{"user_id":"{{user_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.user_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_get_user_by_id --payload '{
  "user_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-workspace-api-keys`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_workspace_api_keys:{"workspace_id":"{{workspace_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.workspace_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_get_workspace_api_keys --payload '{
  "workspace_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-get-workspace-users`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_get_workspace_users:{"workspace_id":"{{workspace_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.workspace_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_get_workspace_users --payload '{
  "workspace_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-invite-admins`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_invite_admins:{"inviter_email":"{{inviter_email}}","emails":"{{emails}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.inviter_email` | `string` | Yes |  |  |
| `payload.emails` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_invite_admins --payload '{
  "inviter_email": "string",
  "emails": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-invite-managers`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_invite_managers:{"inviter_email":"{{inviter_email}}","emails":"{{emails}}","workspace_ids":"{{workspace_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.inviter_email` | `string` | Yes |  |  |
| `payload.emails` | `array` | Yes |  |  |
| `payload.workspace_ids` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_invite_managers --payload '{
  "inviter_email": "string",
  "emails": "array",
  "workspace_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-invite-members`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_invite_members:{"inviter_email":"{{inviter_email}}","emails":"{{emails}}","workspace_ids":"{{workspace_ids}}","permissions":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.inviter_email` | `string` | Yes |  |  |
| `payload.emails` | `array` | Yes |  |  |
| `payload.workspace_ids` | `array` | Yes |  |  |
| `payload.permissions` | `object` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_invite_members --payload '{
  "inviter_email": "string",
  "emails": "array",
  "workspace_ids": "array",
  "permissions": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-is-connection`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_is_connection:{"sender_account_id":"{{sender_account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sender_account_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_is_connection --payload '{
  "sender_account_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-list-linkedin-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_list_linkedin_accounts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute heyreach_list_linkedin_accounts --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-list-lists`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_list_lists:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute heyreach_list_lists --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-pause-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_pause_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_pause_campaign --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-replace-lead-tags`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_replace_lead_tags:{"tags":"{{tags}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tags` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_replace_lead_tags --payload '{
  "tags": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-resume-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_resume_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_resume_campaign --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-send-message`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_send_message:{"message":"{{message}}","conversation_id":"{{conversation_id}}","linkedin_account_id":"{{linkedin_account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.message` | `string` | Yes |  |  |
| `payload.conversation_id` | `string` | Yes |  |  |
| `payload.linkedin_account_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_send_message --payload '{
  "message": "string",
  "conversation_id": "string",
  "linkedin_account_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-set-seen-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_set_seen_status:{"conversation_id":"{{conversation_id}}","linkedin_account_id":"{{linkedin_account_id}}","seen":"{{seen}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.conversation_id` | `string` | Yes |  |  |
| `payload.linkedin_account_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |
| `payload.seen` | `boolean` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute heyreach_set_seen_status --payload '{
  "conversation_id": "string",
  "linkedin_account_id": "integer",
  "seen": false
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-stop-lead-in-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_stop_lead_in_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_stop_lead_in_campaign --payload '{
  "campaign_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `heyreach-update-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_update_workspace:{"workspace_id":"{{workspace_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.workspace_id` | `integer | string` | Yes |  | Numeric id as integer or numeric string. |

**Direct CLI:**

```
deepline tools execute heyreach_update_workspace --payload '{
  "workspace_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `list-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=heyreach_list_campaigns:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute heyreach_list_campaigns --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## HubSpot

*Provider folder: `hubspot` · 85 endpoints*


### HubSpot — Agent Guidance

## Playbook

# HubSpot CRM - Agent Guidance

## Quick Reference

| Goal | Operation | Notes |
| --- | --- | --- |
| Create a company | `hubspot_create_company` | Use `website_url` when you want HubSpot to infer the domain. |
| Create a contact | `hubspot_create_contact` | Prefer `email` for stable identity matching. |
| Create a deal | `hubspot_create_deal` | Use `deal_stage` and `deal_probability` only when you know the pipeline. |
| Create a note | `hubspot_create_note` | `time_stamp` is required. Add associations to place it on a record timeline. |
| Create a task | `hubspot_create_task` | `task_type` should usually be `TODO`. |
| Update a record | `hubspot_update_*` | Always include `id` and only the fields you want to change. |
| Delete a record | `hubspot_delete_*` | Hard delete only when the target should disappear from HubSpot. |
| Browse records | `hubspot_list_*` | Use for paging and record inspection. |
| Fetch one record | `hubspot_get_object` | Best when you already have the record ID. |
| Search records | `hubspot_search_objects` | Best for fuzzy lookups and filters. |

## Practical Notes

- HubSpot normalizes most writes to CRM property names such as `firstname`, `lastname`, `hubspot_owner_id`, and `dealstage`.
- For object-heavy workflows, prefer `search_objects` over broad listing when you need filters or quick lookup by email/domain.
- The `list_objects` and `get_object` helpers work for standard objects and custom objects when you know the object type.
- When using notes or tasks, add associations up front so the activity lands on the right record timeline.


### HubSpot — Endpoints


#### `hubspot-add-records-to-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_add_records_to_list:{"list_id":"{{list_id}}","record_ids":"{{record_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.list_id` | `string` | Yes |  | HubSpot ILS list ID. |
| `payload.record_ids` | `array` | Yes |  | HubSpot record IDs to add to the list. |

**Direct CLI:**

```
deepline tools execute hubspot_add_records_to_list --payload '{
  "list_id": "string",
  "record_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-attach-campaign-asset`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_attach_campaign_asset:{"campaign_guid":"{{campaign_guid}}","asset_type":"{{asset_type}}","asset_id":"{{asset_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.asset_type` | `string` | Yes |  | Campaign asset type. |
| `payload.asset_id` | `string` | Yes |  | HubSpot asset ID. |

**Direct CLI:**

```
deepline tools execute hubspot_attach_campaign_asset --payload '{
  "campaign_guid": "string",
  "asset_type": "string",
  "asset_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-batch-archive-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_batch_archive_campaigns:{"inputs":"{{inputs}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.inputs` | `array` | Yes |  | Campaigns to archive. HubSpot allows at most 50 items per batch request. |

**Direct CLI:**

```
deepline tools execute hubspot_batch_archive_campaigns --payload '{
  "inputs": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-batch-archive-objects`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_batch_archive_objects:{"object_type":"{{object_type}}","inputs":"{{inputs}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object_type` | `string` | Yes |  | HubSpot CRM object type or alias. |
| `payload.inputs` | `array` | Yes |  | Records to archive in one batch request (max 100). |

**Direct CLI:**

```
deepline tools execute hubspot_batch_archive_objects --payload '{
  "object_type": "string",
  "inputs": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-batch-create-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_batch_create_campaigns:{"inputs":"{{inputs}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.inputs` | `array` | Yes |  | Campaigns to create. HubSpot allows at most 50 items per batch request. |

**Direct CLI:**

```
deepline tools execute hubspot_batch_create_campaigns --payload '{
  "inputs": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-batch-read-associations`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_batch_read_associations:{"from_object_type":"{{from_object_type}}","to_object_type":"{{to_object_type}}","inputs":"{{inputs}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.from_object_type` | `string` | Yes |  | Source object type. |
| `payload.to_object_type` | `string` | Yes |  | Target object type. |
| `payload.inputs` | `array` | Yes |  | Source record IDs to read associations for (max 1000). |

**Direct CLI:**

```
deepline tools execute hubspot_batch_read_associations --payload '{
  "from_object_type": "string",
  "to_object_type": "string",
  "inputs": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-batch-read-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_batch_read_campaigns:{"inputs":"{{inputs}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.inputs` | `array` | Yes |  | Campaign IDs to read. HubSpot allows at most 50 items per batch request. |

**Direct CLI:**

```
deepline tools execute hubspot_batch_read_campaigns --payload '{
  "inputs": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-batch-read-objects`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_batch_read_objects:{"object_type":"{{object_type}}","inputs":"{{inputs}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object_type` | `string` | Yes |  | HubSpot CRM object type or alias. |
| `payload.inputs` | `array` | Yes |  | Records to read in a single batch request (max 100). |

**Direct CLI:**

```
deepline tools execute hubspot_batch_read_objects --payload '{
  "object_type": "string",
  "inputs": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-batch-update-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_batch_update_campaigns:{"inputs":"{{inputs}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.inputs` | `array` | Yes |  | Campaigns to update. HubSpot allows at most 50 items per batch request. |

**Direct CLI:**

```
deepline tools execute hubspot_batch_update_campaigns --payload '{
  "inputs": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-batch-upsert-objects`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_batch_upsert_objects:{"object_type":"{{object_type}}","inputs":"{{inputs}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object_type` | `string` | Yes |  | HubSpot CRM object type or alias. |
| `payload.inputs` | `array` | Yes |  | Records to create or update in one batch request (max 100). |

**Direct CLI:**

```
deepline tools execute hubspot_batch_upsert_objects --payload '{
  "object_type": "string",
  "inputs": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-change-user-role`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_change_user_role:{"id":"{{id}}","superAdmin":"{{superAdmin}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | HubSpot user ID. |
| `payload.superAdmin` | `boolean` | Yes |  | Whether the user should be a super admin. |

**Direct CLI:**

```
deepline tools execute hubspot_change_user_role --payload '{
  "id": "string",
  "superAdmin": false
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-clone-marketing-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_clone_marketing_email:{"email_id":"{{email_id}}","clone_name":"{{clone_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | Source marketing email ID to clone. |
| `payload.clone_name` | `string` | Yes |  | Name to assign to the cloned email. |

**Direct CLI:**

```
deepline tools execute hubspot_clone_marketing_email --payload '{
  "email_id": "string",
  "clone_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-associations`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_associations:{"from_object_type":"{{from_object_type}}","to_object_type":"{{to_object_type}}","inputs":"{{inputs}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.from_object_type` | `string` | Yes |  | Source object type. |
| `payload.to_object_type` | `string` | Yes |  | Target object type. |
| `payload.inputs` | `array` | Yes |  | Association edges to create or delete. |

**Direct CLI:**

```
deepline tools execute hubspot_create_associations --payload '{
  "from_object_type": "string",
  "to_object_type": "string",
  "inputs": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_campaign:{"properties":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.properties` | `object` | Yes |  | Writable HubSpot campaign properties documented by the Campaigns API. |

**Direct CLI:**

```
deepline tools execute hubspot_create_campaign --payload '{
  "properties": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-campaign-budget`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_campaign_budget:{"campaign_guid":"{{campaign_guid}}","amount":"{{amount}}","name":"{{name}}","order":"{{order}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.amount` | `number` | Yes |  | Budget amount for this line item. |
| `payload.name` | `string` | Yes |  | Budget line item name. |
| `payload.order` | `integer` | Yes |  | Display order for the budget line item. |

**Direct CLI:**

```
deepline tools execute hubspot_create_campaign_budget --payload '{
  "campaign_guid": "string",
  "amount": 0,
  "name": "string",
  "order": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-campaign-spend`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_campaign_spend:{"campaign_guid":"{{campaign_guid}}","amount":"{{amount}}","name":"{{name}}","order":"{{order}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.amount` | `number` | Yes |  | Spend amount for this line item. |
| `payload.name` | `string` | Yes |  | Spend line item name. |
| `payload.order` | `integer` | Yes |  | Display order for the spend line item. |

**Direct CLI:**

```
deepline tools execute hubspot_create_campaign_spend --payload '{
  "campaign_guid": "string",
  "amount": 0,
  "name": "string",
  "order": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_company:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_create_company --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_contact:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_create_contact --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-deal`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_deal:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_create_deal --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_list:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | Public HubSpot list/segment name. |

**Direct CLI:**

```
deepline tools execute hubspot_create_list --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-marketing-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_marketing_email:{"name":"{{name}}","subject":"{{subject}}","template_path":"{{template_path}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | Internal HubSpot email name. |
| `payload.subject` | `string` | Yes |  | Email subject line. |
| `payload.template_path` | `string` | Yes |  | HubSpot email template path, for example `@hubspot/email/dnd/plain_text.html`. |

**Direct CLI:**

```
deepline tools execute hubspot_create_marketing_email --payload '{
  "name": "string",
  "subject": "string",
  "template_path": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-marketing-email-ab-variation`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_marketing_email_ab_variation:{"content_id":"{{content_id}}","variant_name":"{{variant_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.content_id` | `string` | Yes |  | Source marketing email ID to use for the new A/B variation. |
| `payload.variant_name` | `string` | Yes |  | Name for the new A/B test variation. |

**Direct CLI:**

```
deepline tools execute hubspot_create_marketing_email_ab_variation --payload '{
  "content_id": "string",
  "variant_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-note`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_note:{"time_stamp":"{{time_stamp}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.time_stamp` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_create_note --payload '{
  "time_stamp": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-property`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_property:{"objectType":"{{objectType}}","data":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.objectType` | `string` | Yes |  |  |
| `payload.data` | `object` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_create_property --payload '{
  "objectType": "string",
  "data": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-task`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_task:{"task_type":"TODO","priority":"LOW","due_date":"{{due_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.task_type` | `"TODO"` | Yes |  |  |
| `payload.priority` | `"LOW" | "MEDIUM" | "HIGH"` | Yes |  |  |
| `payload.due_date` | `string` | Yes |  |  |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.task_type` | `TODO` |
| `payload.priority` | `LOW`, `MEDIUM`, `HIGH` |

**Direct CLI:**

```
deepline tools execute hubspot_create_task --payload '{
  "task_type": "TODO",
  "priority": "LOW",
  "due_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-create-user`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_create_user:{"email":"{{email}}","secondaryTeamIds":"{{secondaryTeamIds}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  |  |
| `payload.secondaryTeamIds` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_create_user --payload '{
  "email": "string",
  "secondaryTeamIds": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-delete-associations`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_delete_associations:{"from_object_type":"{{from_object_type}}","to_object_type":"{{to_object_type}}","inputs":"{{inputs}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.from_object_type` | `string` | Yes |  | Source object type. |
| `payload.to_object_type` | `string` | Yes |  | Target object type. |
| `payload.inputs` | `array` | Yes |  | Association edges to create or delete. |

**Direct CLI:**

```
deepline tools execute hubspot_delete_associations --payload '{
  "from_object_type": "string",
  "to_object_type": "string",
  "inputs": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-delete-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_delete_campaign:{"campaign_guid":"{{campaign_guid}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |

**Direct CLI:**

```
deepline tools execute hubspot_delete_campaign --payload '{
  "campaign_guid": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-delete-campaign-budget`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_delete_campaign_budget:{"campaign_guid":"{{campaign_guid}}","budget_id":"{{budget_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.budget_id` | `string` | Yes |  | Campaign budget item ID. |

**Direct CLI:**

```
deepline tools execute hubspot_delete_campaign_budget --payload '{
  "campaign_guid": "string",
  "budget_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-delete-campaign-spend`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_delete_campaign_spend:{"campaign_guid":"{{campaign_guid}}","spend_id":"{{spend_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.spend_id` | `string` | Yes |  | Campaign spend item ID. |

**Direct CLI:**

```
deepline tools execute hubspot_delete_campaign_spend --payload '{
  "campaign_guid": "string",
  "spend_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-delete-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_delete_company:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_delete_company --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-delete-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_delete_contact:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_delete_contact --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-delete-deal`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_delete_deal:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_delete_deal --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-delete-marketing-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_delete_marketing_email:{"email_id":"{{email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |

**Direct CLI:**

```
deepline tools execute hubspot_delete_marketing_email --payload '{
  "email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-delete-task`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_delete_task:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_delete_task --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-delete-user`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_delete_user:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_delete_user --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-detach-campaign-asset`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_detach_campaign_asset:{"campaign_guid":"{{campaign_guid}}","asset_type":"{{asset_type}}","asset_id":"{{asset_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.asset_type` | `string` | Yes |  | Campaign asset type. |
| `payload.asset_id` | `string` | Yes |  | HubSpot asset ID. |

**Direct CLI:**

```
deepline tools execute hubspot_detach_campaign_asset --payload '{
  "campaign_guid": "string",
  "asset_type": "string",
  "asset_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-enroll-contact-in-sequence`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_enroll_contact_in_sequence:{"sequence_id":"{{sequence_id}}","contact_id":"{{contact_id}}","sender_email":"{{sender_email}}","user_id":"{{user_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sequence_id` | `string` | Yes |  | HubSpot sales sequence ID. |
| `payload.contact_id` | `string` | Yes |  | HubSpot contact ID to enroll. |
| `payload.sender_email` | `string` | Yes |  | Email address of the sender. Must be a connected inbox in HubSpot. |
| `payload.user_id` | `string` | Yes |  | HubSpot user ID of the enrolling user. Use hubspot\_list\_owners to find user IDs. |

**Direct CLI:**

```
deepline tools execute hubspot_enroll_contact_in_sequence --payload '{
  "sequence_id": "string",
  "contact_id": "string",
  "sender_email": "string",
  "user_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-fetch-custom-objects`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_fetch_custom_objects:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_fetch_custom_objects --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-fetch-pipelines`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_fetch_pipelines:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_fetch_pipelines --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-fetch-properties`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_fetch_properties:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_fetch_properties --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-fetch-roles`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_fetch_roles:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_fetch_roles --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_campaign:{"campaign_guid":"{{campaign_guid}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |

**Direct CLI:**

```
deepline tools execute hubspot_get_campaign --payload '{
  "campaign_guid": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-campaign-budget`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_campaign_budget:{"campaign_guid":"{{campaign_guid}}","budget_id":"{{budget_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.budget_id` | `string` | Yes |  | Campaign budget item ID. |

**Direct CLI:**

```
deepline tools execute hubspot_get_campaign_budget --payload '{
  "campaign_guid": "string",
  "budget_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-campaign-budget-totals`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_campaign_budget_totals:{"campaign_guid":"{{campaign_guid}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |

**Direct CLI:**

```
deepline tools execute hubspot_get_campaign_budget_totals --payload '{
  "campaign_guid": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-campaign-contacts-report`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_campaign_contacts_report:{"campaign_guid":"{{campaign_guid}}","contact_type":"{{contact_type}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.contact_type` | `string` | Yes |  | Campaign contact bucket to fetch. |

**Direct CLI:**

```
deepline tools execute hubspot_get_campaign_contacts_report --payload '{
  "campaign_guid": "string",
  "contact_type": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-campaign-metrics`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_campaign_metrics:{"campaign_guid":"{{campaign_guid}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |

**Direct CLI:**

```
deepline tools execute hubspot_get_campaign_metrics --payload '{
  "campaign_guid": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-campaign-revenue-report`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_campaign_revenue_report:{"campaign_guid":"{{campaign_guid}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |

**Direct CLI:**

```
deepline tools execute hubspot_get_campaign_revenue_report --payload '{
  "campaign_guid": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-campaign-spend`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_campaign_spend:{"campaign_guid":"{{campaign_guid}}","spend_id":"{{spend_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.spend_id` | `string` | Yes |  | Campaign spend item ID. |

**Direct CLI:**

```
deepline tools execute hubspot_get_campaign_spend --payload '{
  "campaign_guid": "string",
  "spend_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-contact-sequence-enrollments`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_contact_sequence_enrollments:{"contact_id":"{{contact_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.contact_id` | `string` | Yes |  | HubSpot contact ID. |

**Direct CLI:**

```
deepline tools execute hubspot_get_contact_sequence_enrollments --payload '{
  "contact_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-marketing-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_marketing_email:{"email_id":"{{email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |

**Direct CLI:**

```
deepline tools execute hubspot_get_marketing_email --payload '{
  "email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-marketing-email-ab-variation`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_marketing_email_ab_variation:{"email_id":"{{email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |

**Direct CLI:**

```
deepline tools execute hubspot_get_marketing_email_ab_variation --payload '{
  "email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-marketing-email-draft`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_marketing_email_draft:{"email_id":"{{email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |

**Direct CLI:**

```
deepline tools execute hubspot_get_marketing_email_draft --payload '{
  "email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-marketing-email-revision`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_marketing_email_revision:{"email_id":"{{email_id}}","revision_id":"{{revision_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |
| `payload.revision_id` | `string` | Yes |  | Revision ID. |

**Direct CLI:**

```
deepline tools execute hubspot_get_marketing_email_revision --payload '{
  "email_id": "string",
  "revision_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-marketing-email-statistics-histogram`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_marketing_email_statistics_histogram:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_get_marketing_email_statistics_histogram --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-object`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_object:{"object_type":"{{object_type}}","object_id":"{{object_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object_type` | `string` | Yes |  | HubSpot CRM object type or alias. |
| `payload.object_id` | `string` | Yes |  | Record ID or unique value to retrieve. |

**Direct CLI:**

```
deepline tools execute hubspot_get_object --payload '{
  "object_type": "string",
  "object_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-get-sequence`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_get_sequence:{"sequence_id":"{{sequence_id}}","user_id":"{{user_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sequence_id` | `string` | Yes |  | HubSpot sales sequence ID. |
| `payload.user_id` | `string` | Yes |  | HubSpot user ID to evaluate sequence permissions. |

**Direct CLI:**

```
deepline tools execute hubspot_get_sequence --payload '{
  "sequence_id": "string",
  "user_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-association-types`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_association_types:{"from_object_type":"{{from_object_type}}","to_object_type":"{{to_object_type}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.from_object_type` | `string` | Yes |  | Source object type. |
| `payload.to_object_type` | `string` | Yes |  | Target object type. |

**Direct CLI:**

```
deepline tools execute hubspot_list_association_types --payload '{
  "from_object_type": "string",
  "to_object_type": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-automation-email-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_automation_email_campaigns:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_list_automation_email_campaigns --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-campaign-assets`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_campaign_assets:{"campaign_guid":"{{campaign_guid}}","asset_type":"AD_CAMPAIGN"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.asset_type` | `"AD_CAMPAIGN" | "BLOG_POST" | "SOCIAL_BROADCAST" | "WEB_INTERACTIVE" | "CTA" | "EXTERNAL_WEB_URL" | "FORM" | "LANDING_PAGE" | "MARKETING_EMAIL" | "MARKETING_EVENT" | "OBJECT_LIST" | "SITE_PAGE" | "AUTOMATION_PLATFORM_FLOW" | "MARKETING_SMS"` | Yes |  |  |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.asset_type` | `AD_CAMPAIGN`, `BLOG_POST`, `SOCIAL_BROADCAST`, `WEB_INTERACTIVE`, `CTA`, `EXTERNAL_WEB_URL`, `FORM`, `LANDING_PAGE`, `MARKETING_EMAIL`, `MARKETING_EVENT`, `OBJECT_LIST`, `SITE_PAGE`, `AUTOMATION_PLATFORM_FLOW`, `MARKETING_SMS` |

**Direct CLI:**

```
deepline tools execute hubspot_list_campaign_assets --payload '{
  "campaign_guid": "string",
  "asset_type": "AD_CAMPAIGN"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_campaigns:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_list_campaigns --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-currency-codes`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_currency_codes:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_list_currency_codes --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-marketing-email-revisions`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_marketing_email_revisions:{"email_id":"{{email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |

**Direct CLI:**

```
deepline tools execute hubspot_list_marketing_email_revisions --payload '{
  "email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-marketing-email-statistics`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_marketing_email_statistics:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_list_marketing_email_statistics --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-marketing-emails`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_marketing_emails:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_list_marketing_emails --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-objects`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_objects:{"object_type":"{{object_type}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object_type` | `string` | Yes |  | HubSpot CRM object type or alias (for example `contacts`, `companies`, `notes`, `tickets`, or a custom object fullyQualifiedName). |

**Direct CLI:**

```
deepline tools execute hubspot_list_objects --payload '{
  "object_type": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-owners`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_owners:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_list_owners --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-sequences`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_sequences:{"user_id":"{{user_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.user_id` | `string` | Yes |  | HubSpot user ID whose sequences to list. Use hubspot\_list\_owners to find user IDs. |

**Direct CLI:**

```
deepline tools execute hubspot_list_sequences --payload '{
  "user_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-list-users`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_list_users:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_list_users --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-publish-marketing-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_publish_marketing_email:{"email_id":"{{email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |

**Direct CLI:**

```
deepline tools execute hubspot_publish_marketing_email --payload '{
  "email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-reset-marketing-email-draft`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_reset_marketing_email_draft:{"email_id":"{{email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |

**Direct CLI:**

```
deepline tools execute hubspot_reset_marketing_email_draft --payload '{
  "email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-restore-marketing-email-revision`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_restore_marketing_email_revision:{"email_id":"{{email_id}}","revision_id":"{{revision_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |
| `payload.revision_id` | `string` | Yes |  | Revision ID. |

**Direct CLI:**

```
deepline tools execute hubspot_restore_marketing_email_revision --payload '{
  "email_id": "string",
  "revision_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-restore-marketing-email-revision-to-draft`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_restore_marketing_email_revision_to_draft:{"email_id":"{{email_id}}","revision_id":"{{revision_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |
| `payload.revision_id` | `string` | Yes |  | Revision ID. |

**Direct CLI:**

```
deepline tools execute hubspot_restore_marketing_email_revision_to_draft --payload '{
  "email_id": "string",
  "revision_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-search-lists`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_search_lists:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hubspot_search_lists --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-search-objects`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_search_objects:{"object_type":"{{object_type}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object_type` | `string` | Yes |  | HubSpot CRM object type or alias. |

**Direct CLI:**

```
deepline tools execute hubspot_search_objects --payload '{
  "object_type": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-unpublish-marketing-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_unpublish_marketing_email:{"email_id":"{{email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |

**Direct CLI:**

```
deepline tools execute hubspot_unpublish_marketing_email --payload '{
  "email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-update-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_update_campaign:{"campaign_guid":"{{campaign_guid}}","properties":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.properties` | `object` | Yes |  | Writable HubSpot campaign properties documented by the Campaigns API. |

**Direct CLI:**

```
deepline tools execute hubspot_update_campaign --payload '{
  "campaign_guid": "string",
  "properties": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-update-campaign-budget`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_update_campaign_budget:{"campaign_guid":"{{campaign_guid}}","budget_id":"{{budget_id}}","amount":"{{amount}}","name":"{{name}}","order":"{{order}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.budget_id` | `string` | Yes |  | Campaign budget item ID. |
| `payload.amount` | `number` | Yes |  | Budget amount for this line item. |
| `payload.name` | `string` | Yes |  | Budget line item name. |
| `payload.order` | `integer` | Yes |  | Display order for the budget line item. |

**Direct CLI:**

```
deepline tools execute hubspot_update_campaign_budget --payload '{
  "campaign_guid": "string",
  "budget_id": "string",
  "amount": 0,
  "name": "string",
  "order": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-update-campaign-spend`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_update_campaign_spend:{"campaign_guid":"{{campaign_guid}}","spend_id":"{{spend_id}}","amount":"{{amount}}","name":"{{name}}","order":"{{order}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_guid` | `string` | Yes |  | HubSpot campaign GUID. |
| `payload.spend_id` | `string` | Yes |  | Campaign spend item ID. |
| `payload.amount` | `number` | Yes |  | Spend amount for this line item. |
| `payload.name` | `string` | Yes |  | Spend line item name. |
| `payload.order` | `integer` | Yes |  | Display order for the spend line item. |

**Direct CLI:**

```
deepline tools execute hubspot_update_campaign_spend --payload '{
  "campaign_guid": "string",
  "spend_id": "string",
  "amount": 0,
  "name": "string",
  "order": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-update-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_update_company:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_update_company --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-update-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_update_contact:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_update_contact --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-update-deal`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_update_deal:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_update_deal --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-update-marketing-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_update_marketing_email:{"email_id":"{{email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |

**Direct CLI:**

```
deepline tools execute hubspot_update_marketing_email --payload '{
  "email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-update-marketing-email-draft`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_update_marketing_email_draft:{"email_id":"{{email_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_id` | `string` | Yes |  | HubSpot marketing email ID. |

**Direct CLI:**

```
deepline tools execute hubspot_update_marketing_email_draft --payload '{
  "email_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hubspot-update-task`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hubspot_update_task:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute hubspot_update_task --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Hunter

*Provider folder: `hunter` · 8 endpoints*


### Hunter — Agent Guidance

## Playbook

# Hunter Workflow Guidance

- Start with `hunter_discover` and `hunter_email_count` to shape ICP and estimate reachable volume at zero credit cost.
- Prefer `hunter_domain_search` when you need multiple contacts from one account; add `department`/`seniority` filters to keep recall high but usable.
- Use `hunter_email_finder` for one named person after domain-level search is exhausted or too broad.
- Always run `hunter_email_verifier` immediately before outbound send decisions; treat `invalid`, `accept_all`, `webmail`, and `disposable` as non-send states by default.
- Use `hunter_people_find` and `hunter_companies_find` for enrichment context, not as your first discovery step.
- Use `hunter_combined_find` only when person + company enrichment are both needed in one call and you already have a strong identity seed (email/LinkedIn/domain).


### Hunter — Endpoints


#### `hunter-combined-find`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hunter_combined_find:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hunter_combined_find --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.3` per pricing unit.
- Billing mode: `post_deduct`.


#### `hunter-companies-find`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hunter_companies_find:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Company domain to enrich. |

**Direct CLI:**

```
deepline tools execute hunter_companies_find --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.3` per pricing unit.
- Billing mode: `post_deduct`.


#### `hunter-discover`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hunter_discover:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hunter_discover --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hunter-domain-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hunter_domain_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hunter_domain_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.3` per pricing unit.
- Billing mode: `post_deduct`.


#### `hunter-email-count`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hunter_email_count:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hunter_email_count --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `hunter-email-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hunter_email_finder:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hunter_email_finder --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.3` per pricing unit.
- Billing mode: `post_deduct`.


#### `hunter-email-verifier`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hunter_email_verifier:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address to verify deliverability and risk flags. |

**Direct CLI:**

```
deepline tools execute hunter_email_verifier --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.15` per pricing unit.
- Billing mode: `post_deduct`.


#### `hunter-people-find`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=hunter_people_find:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute hunter_people_find --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.3` per pricing unit.
- Billing mode: `post_deduct`.


---

## Icypeas

*Provider folder: `icypeas` · 13 endpoints*


### Icypeas — Agent Guidance

## Playbook

# Icypeas Workflow Guidance

- **Always start with counts.** Run `icypeas_count_people` or `icypeas_count_companies` before paid find operations. These are free and let you estimate result volume, refine filters, and avoid wasting credits on overly broad queries.
- **Email search is async.**`icypeas_email_search` returns a `SCHEDULED` status immediately. Poll `icypeas_read_results` with the returned `_id` to get the final email. Plan for this delay in workflows.
- **Use bulk search for volume.** When processing more than a handful of records, prefer `icypeas_bulk_search` over individual `icypeas_email_search` calls. Bulk supports up to 5,000 rows per batch. Monitor progress via `icypeas_read_bulk_files`.
- **Verify before sending.** Always run `icypeas_email_verification` on discovered emails before outbound. It costs only 0.1 credits. Treat `NOT_FOUND` and `DEBITED_NOT_FOUND` as non-deliverable.
- **LinkedIn scraping is powerful but costs more.**`icypeas_scrape_profile` (1.5 credits) returns rich contact data including phone numbers and verified emails. `icypeas_scrape_company` (0.5 credits) is cheaper for company-level data.
- **Find-people supports 16 filters.** Use include/exclude arrays for precise targeting: job title, company, location, skills, languages, school, keywords, and more. Start broad with count, then narrow.
- **Pagination uses token-based cursors.** For `icypeas_find_people` and `icypeas_find_companies`, pass the `token` from the previous response into the next request’s `pagination.token`. Page size max is 200.
- **Free operations for planning:**`icypeas_count_people`, `icypeas_count_companies`, `icypeas_read_results`, and `icypeas_read_bulk_files` cost zero credits. Use them liberally.
- **Check account status.** Use your internal usage dashboard before large operations.


### Icypeas — Endpoints


#### `icypeas-bulk-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_bulk_search:{"name":"{{name}}","task":"email-search","data":"{{data}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | Name for this bulk search job. |
| `payload.task` | `"email-search" | "email-verification" | "domain-search"` | Yes |  | Type of search to perform in bulk. |
| `payload.data` | `array` | Yes |  | Rows of data. Format depends on task type (for example \[firstname, lastname, domain\] for email-search). |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.task` | `email-search`, `email-verification`, `domain-search` |

**Direct CLI:**

```
deepline tools execute icypeas_bulk_search --payload '{
  "name": "string",
  "task": "email-search",
  "data": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `pre_reserve`.


#### `icypeas-count-companies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_count_companies:{"query":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `object` | Yes |  | Search filters for company count. |

**Direct CLI:**

```
deepline tools execute icypeas_count_companies --payload '{
  "query": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `icypeas-count-people`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_count_people:{"query":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `object` | Yes |  | Search filters for people count. |

**Direct CLI:**

```
deepline tools execute icypeas_count_people --payload '{
  "query": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `icypeas-domain-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_domain_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute icypeas_domain_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `icypeas-email-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_email_search:{"domainOrCompany":"{{domainOrCompany}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domainOrCompany` | `string` | Yes |  | Company domain or name (for example stripe.com). |

**Direct CLI:**

```
deepline tools execute icypeas_email_search --payload '{
  "domainOrCompany": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `icypeas-email-verification`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_email_verification:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address to verify deliverability. |

**Direct CLI:**

```
deepline tools execute icypeas_email_verification --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `icypeas-find-companies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_find_companies:{"query":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `object` | Yes |  | Search filters for companies. |

**Direct CLI:**

```
deepline tools execute icypeas_find_companies --payload '{
  "query": {}
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `icypeas-find-company-url`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_find_company_url:{"companyOrDomain":"{{companyOrDomain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.companyOrDomain` | `string` | Yes |  | Company name or domain to find the LinkedIn URL for. |

**Direct CLI:**

```
deepline tools execute icypeas_find_company_url --payload '{
  "companyOrDomain": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `icypeas-find-people`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_find_people:{"query":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `object` | Yes |  | Search filters for people. |

**Direct CLI:**

```
deepline tools execute icypeas_find_people --payload '{
  "query": {}
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.14` per pricing unit.
- Billing mode: `post_deduct`.


#### `icypeas-read-bulk-files`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_read_bulk_files:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute icypeas_read_bulk_files --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `icypeas-read-results`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_read_results:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute icypeas_read_results --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `icypeas-scrape-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_scrape_company:{"url":"{{url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.url` | `string` | Yes |  | LinkedIn company page URL (for example [https://www.linkedin.com/company/stripe](https://www.linkedin.com/company/stripe)). |

**Direct CLI:**

```
deepline tools execute icypeas_scrape_company --payload '{
  "url": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `icypeas-scrape-profile`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=icypeas_scrape_profile:{"url":"{{url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.url` | `string` | Yes |  | LinkedIn profile URL (for example [https://www.linkedin.com/in/johndoe](https://www.linkedin.com/in/johndoe)). |

**Direct CLI:**

```
deepline tools execute icypeas_scrape_profile --payload '{
  "url": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.21` per pricing unit.
- Billing mode: `post_deduct`.


---

## Instantly

*Provider folder: `instantly` · 94 endpoints*


### Instantly — Agent Guidance

## Playbook

Use Instantly for campaign activation and lightweight outbound reporting.

- Resolve campaign IDs from `list_campaigns` before any add operation.
- For `instantly_create_campaign`, do **not** brute-force timezone guesses. Reuse a known-good timezone from an existing campaign via `instantly_get_campaign` when possible.
- If the requested timezone is not directly accepted, map it to the closest supported Instantly value before sending. Example: use `America/Detroit` when the user asks for `America/New_York`.
- Treat `UTC` as unsupported by Instantly create-campaign; use a supported UTC-equivalent enum value from existing campaigns instead of sending literal `UTC`.
- For `campaign_schedule.schedules[].days`, use numeric day keys (`"0"`..`"6"`). Named weekdays are normalized by Deepline, but values outside sunday..saturday are rejected.
- `list_leads` accepts both `campaign` and `campaign_id` (alias). It also supports `list_id`, `in_campaign`, `in_list`, and `search` filters. Omit all filters to list leads globally.
- Insert in controlled batches and re-check campaign stats after writes.
- Keep activation behind enrichment/verification gates to reduce low-quality sends.

```
deepline tools execute instantly_list_campaigns --payload '{}'
```

```
deepline tools execute instantly_add_to_campaign --payload '{"campaign_id":"abc-123","leads":[{"email":"ada@example.com","first_name":"Ada","last_name":"Lovelace","company_name":"Babbage Ltd"}]}'
```


### Instantly — Endpoints


#### `account-campaign-mapping`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_campaigns_by_email:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_get_campaigns_by_email --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `add-to-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_add_to_campaign:{"campaign_id":"{{campaign_id}}","leads":"{{leads}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  |  |
| `payload.leads` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_add_to_campaign --payload '{
  "campaign_id": "string",
  "leads": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `audit-log-operations`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_audit_logs:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_audit_logs --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `campaign-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_campaign_stats:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_campaign_stats --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-activate-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_activate_campaign:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_activate_campaign --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-add-campaign-variables`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_add_campaign_variables:{"variables":"{{variables}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.variables` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_add_campaign_variables --payload '{
  "variables": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-assign-or-unassign-tags-to-resources`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_assign_or_unassign_tags_to_resources:{"tag_ids":"{{tag_ids}}","resource_type":"{{resource_type}}","resource_ids":"{{resource_ids}}","assign":"{{assign}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tag_ids` | `array` | Yes |  |  |
| `payload.resource_type` | `1 | 2` | Yes |  |  |
| `payload.resource_ids` | `array` | Yes |  |  |
| `payload.assign` | `boolean` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_assign_or_unassign_tags_to_resources --payload '{
  "tag_ids": "array",
  "resource_type": "1",
  "resource_ids": "array",
  "assign": false
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-bulk-assign-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_bulk_assign_leads:{"organization_user_ids":"{{organization_user_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.organization_user_ids` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_bulk_assign_leads --payload '{
  "organization_user_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-create-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_create_account:{"email":"{{email}}","provider_code":"{{provider_code}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  |  |
| `payload.provider_code` | `integer` | Yes |  | Email provider type (1=Custom IMAP/SMTP, 2=Google, 3=Microsoft, 4=AWS, 8=AirMail). |

**Direct CLI:**

```
deepline tools execute instantly_create_account --payload '{
  "email": "string",
  "provider_code": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-create-api-key`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_create_api_key:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_create_api_key --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-create-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_create_campaign:{"name":"{{name}}","campaign_schedule":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  |  |
| `payload.campaign_schedule` | `object` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_create_campaign --payload '{
  "name": "string",
  "campaign_schedule": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-create-campaign-subsequence`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_create_campaign_subsequence:{"name":"{{name}}","conditions":{},"subsequence_schedule":{},"sequences":"{{sequences}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  |  |
| `payload.conditions` | `record` | Yes |  |  |
| `payload.subsequence_schedule` | `object` | Yes |  |  |
| `payload.sequences` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_create_campaign_subsequence --payload '{
  "name": "string",
  "conditions": {},
  "subsequence_schedule": {},
  "sequences": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-create-custom-tag`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_create_custom_tag:{"label":"{{label}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.label` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_create_custom_tag --payload '{
  "label": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-create-dfy-order`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_create_dfy_order:{"domain_count":"{{domain_count}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain_count` | `integer` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_create_dfy_order --payload '{
  "domain_count": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-create-email-template`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_create_email_template:{"name":"{{name}}","subject":"{{subject}}","body":"{{body}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  |  |
| `payload.subject` | `string` | Yes |  |  |
| `payload.body` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_create_email_template --payload '{
  "name": "string",
  "subject": "string",
  "body": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-create-from-shared`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_create_from_shared:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_create_from_shared --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-create-lead-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_create_lead_list:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_create_lead_list --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-create-workspace-group-member`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_create_workspace_group_member:{"sub_workspace_id":"{{sub_workspace_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sub_workspace_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_create_workspace_group_member --payload '{
  "sub_workspace_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-create-workspace-member`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_create_workspace_member:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_create_workspace_member --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-delete-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_delete_account:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_delete_account --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-delete-api-key`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_delete_api_key:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_delete_api_key --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-delete-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_delete_campaign:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_delete_campaign --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-delete-campaign-subsequence`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_delete_campaign_subsequence:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_delete_campaign_subsequence --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-delete-custom-tag`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_delete_custom_tag:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_delete_custom_tag --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-delete-email-template`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_delete_email_template:{"template_id":"{{template_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.template_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_delete_email_template --payload '{
  "template_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-delete-lead-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_delete_lead_list:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_delete_lead_list --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-delete-workspace-group-member`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_delete_workspace_group_member:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_delete_workspace_group_member --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-delete-workspace-member`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_delete_workspace_member:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_delete_workspace_member --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-disable-warmup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_disable_warmup:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_disable_warmup --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-duplicate-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_duplicate_campaign:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_duplicate_campaign --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-enable-warmup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_enable_warmup:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_enable_warmup --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-export-campaign-json`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_export_campaign_json:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_export_campaign_json --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-forward-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_forward_email:{"eaccount":"{{eaccount}}","reply_to_uuid":"{{reply_to_uuid}}","to_address_email_list":"{{to_address_email_list}}","subject":"{{subject}}","body":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.eaccount` | `string` | Yes |  | The email account used to send the forwarded email. |
| `payload.reply_to_uuid` | `string` | Yes |  |  |
| `payload.to_address_email_list` | `string` | Yes |  | Comma-separated list of recipient email addresses. |
| `payload.subject` | `string` | Yes |  |  |
| `payload.body` | `object` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_forward_email --payload '{
  "eaccount": "string",
  "reply_to_uuid": "string",
  "to_address_email_list": "string",
  "subject": "string",
  "body": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_account:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_account --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-background-job`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_background_job:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_background_job --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_get_campaign --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-campaign-analytics`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_campaign_analytics:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_campaign_analytics --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-campaign-analytics-overview`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_campaign_analytics_overview:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_campaign_analytics_overview --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-campaign-analytics-steps`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_campaign_analytics_steps:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_campaign_analytics_steps --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-campaign-sending-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_campaign_sending_status:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_campaign_sending_status --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-campaign-subsequence`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_campaign_subsequence:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_campaign_subsequence --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-custom-tag`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_custom_tag:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_custom_tag --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-daily-account-analytics`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_daily_account_analytics:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_daily_account_analytics --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-daily-campaign-analytics`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_daily_campaign_analytics:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_daily_campaign_analytics --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_email:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_email --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-email-template`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_email_template:{"template_id":"{{template_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.template_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_get_email_template --payload '{
  "template_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-launched-campaigns-count`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_launched_campaigns_count:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_launched_campaigns_count --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-lead-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_lead_list:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_lead_list --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-lead-list-verification-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_lead_list_verification_stats:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_lead_list_verification_stats --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-pre-warmed-domains`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_pre_warmed_domains:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_pre_warmed_domains --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-prompt-template`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_prompt_template:{"template_id":"{{template_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.template_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_get_prompt_template --payload '{
  "template_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-tracking-domain-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_tracking_domain_status:{"host":"{{host}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.host` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_get_tracking_domain_status --payload '{
  "host": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-warmup-analytics`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_warmup_analytics:{"emails":"{{emails}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.emails` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_get_warmup_analytics --payload '{
  "emails": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_workspace:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_workspace --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-workspace-member`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_workspace_member:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_workspace_member --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-get-workspace-plan-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_get_workspace_plan_details:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_get_workspace_plan_details --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_accounts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_accounts --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-api-keys`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_api_keys:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_api_keys --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-background-jobs`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_background_jobs:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_background_jobs --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-campaign-subsequences`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_campaign_subsequences:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_campaign_subsequences --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-custom-tags`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_custom_tags:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_custom_tags --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-dfy-orders`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_dfy_orders:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_dfy_orders --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-email-templates`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_email_templates:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_email_templates --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-emails`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_emails:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_emails --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-lead-lists`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_lead_lists:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_lead_lists --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_leads:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_leads --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-prompt-templates`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_prompt_templates:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_prompt_templates --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-workspace-group-members`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_workspace_group_members:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_workspace_group_members --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-list-workspace-members`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_workspace_members:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_workspace_members --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-mark-account-fixed`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_mark_account_fixed:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_mark_account_fixed --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-move-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_move_accounts:{"emails":"{{emails}}","source_workspace_id":"{{source_workspace_id}}","destination_workspace_id":"{{destination_workspace_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.emails` | `array` | Yes |  |  |
| `payload.source_workspace_id` | `string` | Yes |  |  |
| `payload.destination_workspace_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_move_accounts --payload '{
  "emails": "array",
  "source_workspace_id": "string",
  "destination_workspace_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-move-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_move_leads:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_move_leads --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-patch-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_patch_account:{"account_id":"{{account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_patch_account --payload '{
  "account_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-patch-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_patch_campaign:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_patch_campaign --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-patch-campaign-subsequence`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_patch_campaign_subsequence:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_patch_campaign_subsequence --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-patch-custom-tag`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_patch_custom_tag:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_patch_custom_tag --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-patch-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_patch_email:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_patch_email --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-patch-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_patch_lead:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  | Instantly lead id. |

**Direct CLI:**

```
deepline tools execute instantly_patch_lead --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-patch-lead-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_patch_lead_list:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_patch_lead_list --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-patch-workspace`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_patch_workspace:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_patch_workspace --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-patch-workspace-member`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_patch_workspace_member:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_patch_workspace_member --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-pause-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_pause_account:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_pause_account --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-pause-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_pause_campaign:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_pause_campaign --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-remove-lead-from-subsequence`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_remove_lead_from_subsequence:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_remove_lead_from_subsequence --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-reply-to-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_reply_to_email:{"eaccount":"{{eaccount}}","reply_to_uuid":"{{reply_to_uuid}}","subject":"{{subject}}","body":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.eaccount` | `string` | Yes |  | The email account used to send the reply. |
| `payload.reply_to_uuid` | `string` | Yes |  |  |
| `payload.subject` | `string` | Yes |  |  |
| `payload.body` | `object` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_reply_to_email --payload '{
  "eaccount": "string",
  "reply_to_uuid": "string",
  "subject": "string",
  "body": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-resume-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_resume_account:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_resume_account --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-search-campaigns-by-lead-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_search_campaigns_by_lead_email:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_search_campaigns_by_lead_email --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-send-test-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_send_test_email:{"eaccount":"{{eaccount}}","to_address_email_list":"{{to_address_email_list}}","subject":"{{subject}}","body":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.eaccount` | `string` | Yes |  | The email account used to send the test email. |
| `payload.to_address_email_list` | `string` | Yes |  | Comma-separated list of recipient email addresses. |
| `payload.subject` | `string` | Yes |  |  |
| `payload.body` | `object` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_send_test_email --payload '{
  "eaccount": "string",
  "to_address_email_list": "string",
  "subject": "string",
  "body": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-share-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_share_campaign:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_share_campaign --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-test-account-vitals`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_test_account_vitals:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_test_account_vitals --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-update-lead-interest-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_update_lead_interest_status:{"lead_email":"{{lead_email}}","interest_value":"{{interest_value}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_email` | `string` | Yes |  |  |
| `payload.interest_value` | `integer` | Yes |  | Lead interest status. Allowed values: 1, 2, 3, 4, 0, -1, -2, -3, -4. Set to null to reset. |

**Direct CLI:**

```
deepline tools execute instantly_update_lead_interest_status --payload '{
  "lead_email": "string",
  "interest_value": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `instantly-update-lead-subsequence`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_update_lead_subsequence:{"subsequence_id":"{{subsequence_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.subsequence_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute instantly_update_lead_subsequence --payload '{
  "subsequence_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `list-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_campaigns:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_campaigns --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `sales-flow-operations`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=instantly_list_sales_flows:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute instantly_list_sales_flows --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## IPQS

*Provider folder: `ipqs` · 4 endpoints*


### IPQS — Agent Guidance

## Playbook

# IPQS — Agent Routing

## Phone validation: use Trestle instead

**For phone validation, use `trestle_phone_validation` first, not IPQS.**Trestle returns `activity_score` (0–100) which tells you whether a line is active or disconnected — IPQS phone validation does not. Activity score is the most useful signal for filtering stale numbers before cold outreach.

| Scenario | Use |
| --- | --- |
| Validate a phone after enrichment | `trestle_phone_validation` |
| Verify phone belongs to a specific person (name match) | `trestle_real_contact` |
| IPQS phone validate | Last resort only — no activity score |

## Email validation: use LeadMagic instead

**Do not use `ipqs_email_verify` or `ipqs_batch_email_verify` for outbound email validation.**IPQS email validation returns a boolean `valid` field with no `catch_all` distinction. The Deepline waterfall stack depends on `catch_all` status to decide whether to fall through to the next provider. Use `leadmagic_email_validation` instead — it returns structured `email_status` (`valid` / `catch_all` / `invalid`) at $0.006/call.Priority for email validation: `leadmagic_email_validation` → `zerobounce_validate` → IPQS (last resort).

## What IPQS is good for

- Fraud scoring: `fraud_score` on email or phone
- Disposable/honeypot detection: `disposable`, `honeypot` fields
- Spam trap detection on email
- DNC status on phone (use `trestle_phone_validation` for line type + activity though)


### IPQS — Endpoints


#### `ipqs-batch-email-verify`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ipqs_batch_email_verify:{"email_batch":"{{email_batch}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_batch` | `array` | Yes |  | Array of email objects to verify in batch (1-100). Deepline runs at most 5 IPQS checks concurrently, so larger batches can be slow. |

**Direct CLI:**

```
deepline tools execute ipqs_batch_email_verify --payload '{
  "email_batch": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `ipqs-batch-phone-validate`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ipqs_batch_phone_validate:{"phone_batch":"{{phone_batch}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.phone_batch` | `array` | Yes |  | Array of phone objects to validate in batch (1-100). Deepline runs at most 5 IPQS checks concurrently, so larger batches can be slow. |

**Direct CLI:**

```
deepline tools execute ipqs_batch_phone_validate --payload '{
  "phone_batch": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `ipqs-email-verify`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ipqs_email_verify:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address to verify. |

**Direct CLI:**

```
deepline tools execute ipqs_email_verify --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `ipqs-phone-validate`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=ipqs_phone_validate:{"phone":"{{phone}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.phone` | `string` | Yes |  | Phone number to validate. |

**Direct CLI:**

```
deepline tools execute ipqs_phone_validate --payload '{
  "phone": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


---

## LeadMagic

*Provider folder: `leadmagic` · 16 endpoints*


### LeadMagic — Agent Guidance

## Playbook

Use LeadMagic as a contact-resolution, verification, and intent layer.

- Start with cheaper gates: `leadmagic_email_validation`, `leadmagic_company_search`, and `leadmagic_jobs_finder`.
- Escalate to premium contact discovery only after the target is worth it: `leadmagic_email_finder`, `leadmagic_mobile_finder`, `leadmagic_profile_search`, `leadmagic_b2b_social_email`, and `leadmagic_email_to_profile`.
- For role-based discovery, use `leadmagic_role_finder` for a single decision-maker and `leadmagic_employee_finder` when you need a wider company roster.
- `leadmagic_email_validation` is the final outbound validity gate for this layer. Default acceptance rule is `email_status == valid`; treat `catch_all` and `unknown` as unresolved unless the user explicitly accepts risk.
- LeadMagic is **conservative on catch-all domains** (many Google Workspace and corporate domains). `email_status: "invalid"` with a populated `mx_record` \+ `mx_provider` usually means “mailbox not provable,” not “does not deliver.” Don’t auto-discard these rows — fall back to `deepline_native` validation or a second provider (`zerobounce`, `bettercontact`) before giving up on the lead.
- If LeadMagic profile or phone quality is noisy on pilot rows, switch to quality-first enrichment (`crustdata_person_enrichment`, `peopledatalabs_enrich_contact`) before scaling.

Operational pattern:

1. Run 1-row pilots for identity + email candidates.
2. Validate with `leadmagic_email_validation` on every candidate.
3. Keep fallback chains explicit in your `--with-waterfall` order.
4. Promote only after pilot success and clear assumptions are set.

```
deepline enrich --input contacts.csv --output contacts.csv.out.csv \
  --with-waterfall "email-verify" \
  --with '{"alias":"verify_primary","tool":"leadmagic_email_validation","payload":{"email":"{{email_1}}"}}' \
  --with '{"alias":"verify_secondary","tool":"leadmagic_email_validation","payload":{"email":"{{email_2}}"}}' \
  --end-waterfall
```

Related docs:

- [leadmagic\_email\_validation reference](https://code.deepline.com/tools/leadmagic_email_validation)
- [leadmagic\_email\_finder reference](https://code.deepline.com/tools/leadmagic_email_finder)


### LeadMagic — Endpoints


#### `leadmagic-b2b-ads-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_b2b_ads_details:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute leadmagic_b2b_ads_details --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.68` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-b2b-ads-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_b2b_ads_search:{"company_domain":"example.com"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute leadmagic_b2b_ads_search --payload '{
  "company_domain": "example.com"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-b2b-social-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_b2b_social_email:{"profile_url":"{{profile_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.profile_url` | `string` | Yes |  | B2B profile URL (for example, a professional social profile) |

**Direct CLI:**

```
deepline tools execute leadmagic_b2b_social_email --payload '{
  "profile_url": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.68` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-company-funding`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_company_funding:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute leadmagic_company_funding --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.35` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-company-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_company_search:{"company_domain":"example.com"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute leadmagic_company_search --payload '{
  "company_domain": "example.com"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.34` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-email-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_email_finder:{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"example.com"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | The person’s first name. |
| `payload.last_name` | `string` | Yes |  | The person’s last name. |

**Direct CLI:**

```
deepline tools execute leadmagic_email_finder --payload '{
  "first_name": "string",
  "last_name": "string",
  "domain": "example.com"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.34` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-email-to-profile`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_email_to_profile:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute leadmagic_email_to_profile --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `3.36` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-email-validation`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_email_validation:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address to validate. |

**Direct CLI:**

```
deepline tools execute leadmagic_email_validation --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.09` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-employee-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_employee_finder:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute leadmagic_employee_finder --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-google-ads-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_google_ads_search:{"company_domain":"example.com"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute leadmagic_google_ads_search --payload '{
  "company_domain": "example.com"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-jobs-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_jobs_finder:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute leadmagic_jobs_finder --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.34` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-meta-ads-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_meta_ads_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute leadmagic_meta_ads_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-mobile-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_mobile_finder:{"profile_url":"https://www.linkedin.com/in/example"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute leadmagic_mobile_finder --payload '{
  "profile_url": "https://www.linkedin.com/in/example"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.68` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-personal-email-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_personal_email_finder:{"profile_url":"{{profile_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.profile_url` | `string` | Yes |  | B2B profile URL (for example, a professional social profile) |

**Direct CLI:**

```
deepline tools execute leadmagic_personal_email_finder --payload '{
  "profile_url": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.68` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-profile-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_profile_search:{"profile_url":"{{profile_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.profile_url` | `string` | Yes |  | Full URL of the B2B profile. |

**Direct CLI:**

```
deepline tools execute leadmagic_profile_search --payload '{
  "profile_url": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.34` per pricing unit.
- Billing mode: `post_deduct`.


#### `leadmagic-role-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=leadmagic_role_finder:{"job_title":"{{job_title}}","company_name":"Example Corp"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.job_title` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute leadmagic_role_finder --payload '{
  "job_title": "string",
  "company_name": "Example Corp"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.68` per pricing unit.
- Billing mode: `post_deduct`.


---

## Lemlist

*Provider folder: `lemlist` · 96 endpoints*


### Lemlist — Agent Guidance

## Playbook

Use Lemlist for multi-channel outbound campaigns (email + LinkedIn). Full campaign lifecycle management is available.

- Keep activation behind enrichment/verification gates — only push contacts that have been validated.
- Resolve campaign IDs from `lemlist_list_campaigns` before any write operation.
- Insert contacts in batches of 10–25 and re-check campaign stats after writes.
- Always review in Lemlist UI before starting campaigns — use the `web_url` returned by create/update operations.

## Workflow

1. **Create or list campaigns** before adding contacts.
2. **Add sequence steps** (email, LinkedIn invite, LinkedIn DM) to define the outreach flow.
3. **Add contacts** in small batches (10–25), then check stats to verify.
4. **Review in Lemlist UI** before starting — use the `web_url` returned by create/update operations.
5. **Monitor** via activities and inbox threads.

## Quick Reference

### Campaigns

```
deepline tools execute lemlist_list_campaigns --payload '{}'
deepline tools execute lemlist_create_campaign --payload '{"name":"My Campaign"}'
deepline tools execute lemlist_pause_campaign --payload '{"campaign_id":"cam_abc123"}'
deepline tools execute lemlist_update_campaign --payload '{"campaign_id":"cam_abc123","name":"New Name"}'
deepline tools execute lemlist_get_campaign_stats --payload '{"campaign_id":"cam_abc123"}'
```

### Sequences

```
deepline tools execute lemlist_get_campaign_sequences --payload '{"campaign_id":"cam_abc123"}'
deepline tools execute lemlist_add_sequence_step --payload '{"sequence_id":"seq_abc","type":"linkedinInvite","message":"Hi!","delay":0}'
deepline tools execute lemlist_update_sequence_step --payload '{"sequence_id":"seq_abc","step_id":"stp_xyz","type":"linkedinSend","delay":2}'
deepline tools execute lemlist_delete_sequence_step --payload '{"sequence_id":"seq_abc","step_id":"stp_xyz"}'
```

### Leads

```
deepline tools execute lemlist_add_to_campaign --payload '{"campaign_id":"cam_abc","contacts":[{"email":"ada@example.com","first_name":"Ada","last_name":"Lovelace"}]}'
deepline tools execute lemlist_export_campaign_leads --payload '{"campaign_id":"cam_abc","state":"interested"}'
deepline tools execute lemlist_pause_lead --payload '{"lead_id":"lea_abc"}'
deepline tools execute lemlist_resume_lead --payload '{"lead_id":"lea_abc"}'
deepline tools execute lemlist_mark_lead_interested --payload '{"lead_id_or_email":"ada@example.com"}'
```

### Activities

```
deepline tools execute lemlist_get_activities --payload '{"campaign_id":"cam_abc","type":"emailsReplied","limit":50}'
```

### Inbox

```
deepline tools execute lemlist_list_inbox --payload '{"user_id":"usr_abc"}'
deepline tools execute lemlist_get_inbox_thread --payload '{"contact_id":"ctc_abc"}'
deepline tools execute lemlist_send_email --payload '{"send_user_id":"usr_abc","send_user_email":"me@co.com","send_user_mailbox_id":"mbx_abc","contact_id":"ctc_abc","lead_id":"lea_abc","subject":"Follow up","message":"<p>Hi!</p>"}'
deepline tools execute lemlist_send_linkedin_message --payload '{"send_user_id":"usr_abc","lead_id":"lea_abc","contact_id":"ctc_abc","message":"Thanks for connecting!"}'
```

### Unsubscribes

```
deepline tools execute lemlist_list_unsubscribed_variables --payload '{"limit":50}'
deepline tools execute lemlist_unsubscribe_variable --payload '{"value":"bounce@example.com"}'
deepline tools execute lemlist_resubscribe_variable --payload '{"value":"bounce@example.com"}'
deepline tools execute lemlist_export_unsubscribed_variables --payload '{}'
deepline tools execute lemlist_get_unsubscribe_by_email --payload '{"email":"bounce@example.com"}'
```

## Response Shape Contract

Deepline wraps all provider payloads in a standard result envelope: `{ data, meta }`.

- `lemlist_list_campaigns` → `result.data` is an array of `{ id, name, status }`.
- `lemlist_get_campaign_stats` → `result.data` contains `{ sent, opened, clicked, replied, bounced }`.
- `lemlist_get_campaign_sequences` → `result.data` is keyed by sequence ID, each with a `steps` array.
- `lemlist_export_campaign_leads` → `result.data` is an array of lead objects with `email`, `firstName`, `lastName`, `state`.
- `lemlist_add_to_campaign` → `result.data` contains `{ pushed, failed, errors }`.
- `lemlist_create_campaign` / `lemlist_update_campaign` → `result.data` includes `web_url` for UI review.

## Key Notes

- **Step types:**`email`, `linkedinInvite`, `linkedinSend`, `linkedinVisit`, `manual`, `phone`, `api`, `whatsappMessage`, `conditional`, `sendToAnotherCampaign`
- **Delays are in days** for both `add_sequence_step` and `update_sequence_step` (0 = immediate, 2 = 2 days). Do not pass seconds or hours.
- **Deep links:** Campaign mutations return `web_url` — always review in Lemlist UI before starting campaigns.
- **Lead states for export:**`all`, `contacted`, `interested`, `notInterested`, `emailsBounced`, `paused`, `emailsSent`, `emailsOpened`, `emailsReplied`
- **Activity types:**`emailsSent`, `emailsOpened`, `emailsClicked`, `emailsReplied`, `emailsBounced`, `emailsUnsubscribed`

## Gotchas

- **Delay unit:** Always days. Passing `172800` thinking “seconds” will result in a `> 1500 days` API error.
- **Inbox operations require user/mailbox IDs:**`send_email` needs `send_user_id`, `send_user_email`, and `send_user_mailbox_id`. List inbox first to discover these values.
- **Sequence writes:** Prefer adding/updating sequence steps while campaigns are still draft/paused to avoid campaign-state edge cases.
- **Lead deduplication:** Validation rejects duplicate emails and duplicate `linkedin_url` values within the same batch. Across batches, Lemlist can reject duplicates (for example 409 conflicts), which surface in `result.data.errors`.


### Lemlist — Endpoints


#### `add-to-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_add_to_campaign:{"campaign_id":"{{campaign_id}}","contacts":"{{contacts}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  |  |
| `payload.contacts` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_add_to_campaign --payload '{
  "campaign_id": "string",
  "contacts": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `campaign-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_campaign_stats:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_get_campaign_stats --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-add-lead-variables`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_add_lead_variables:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_add_lead_variables --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-add-sequence-step`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_add_sequence_step:{"sequence_id":"{{sequence_id}}","type":"email"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sequence_id` | `string` | Yes |  | Sequence ID (e.g. seq\_abc123) |
| `payload.type` | `"email" | "linkedinInvite" | "linkedinSend" | "linkedinVisit" | "manual" | "phone" | "api" | "whatsappMessage" | "conditional" | "sendToAnotherCampaign"` | Yes |  | Step type. email/linkedinSend/linkedinInvite require message; email also requires subject; conditional requires conditionKey + delayType. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.type` | `email`, `linkedinInvite`, `linkedinSend`, `linkedinVisit`, `manual`, `phone`, `api`, `whatsappMessage`, `conditional`, `sendToAnotherCampaign` |

**Direct CLI:**

```
deepline tools execute lemlist_add_sequence_step --payload '{
  "sequence_id": "string",
  "type": "email"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-add-unsubscribe`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_add_unsubscribe:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address or domain to unsubscribe (e.g. [alice@acme.com](mailto:alice@acme.com) or acme.com) |

**Direct CLI:**

```
deepline tools execute lemlist_add_unsubscribe --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-associate-schedule-with-campaign`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-attach-labels-to-conversation`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_attach_labels_to_conversation:{"contact_id":"{{contact_id}}","label_ids":"{{label_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.contact_id` | `string` | Yes |  |  |
| `payload.label_ids` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_attach_labels_to_conversation --payload '{
  "contact_id": "string",
  "label_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-bulk-enrich-data`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_bulk_enrich_data:{"requests":"{{requests}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.requests` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_bulk_enrich_data --payload '{
  "requests": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-bulk-unsubscribe-variables`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_bulk_unsubscribe_variables:{"values":"{{values}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.values` | `array` | Yes |  | Values to unsubscribe |

**Direct CLI:**

```
deepline tools execute lemlist_bulk_unsubscribe_variables --payload '{
  "values": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-connect-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_connect_email_account:{"sender_name":"{{sender_name}}","sender_email":"{{sender_email}}","smtp_host":"{{smtp_host}}","smtp_port":"{{smtp_port}}","smtp_login":"{{smtp_login}}","smtp_password":"{{smtp_password}}","imap_host":"{{imap_host}}","imap_port":"{{imap_port}}","imap_login":"{{imap_login}}","imap_password":"{{imap_password}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.sender_name` | `string` | Yes |  | Display name for the sender |
| `payload.sender_email` | `string` | Yes |  | Sender email address |
| `payload.smtp_host` | `string` | Yes |  | SMTP server hostname |
| `payload.smtp_port` | `integer` | Yes |  | SMTP port (e.g. 587, 465) |
| `payload.smtp_login` | `string` | Yes |  | SMTP login username |
| `payload.smtp_password` | `string` | Yes |  | SMTP login password |
| `payload.imap_host` | `string` | Yes |  | IMAP server hostname |
| `payload.imap_port` | `integer` | Yes |  | IMAP port (e.g. 993) |
| `payload.imap_login` | `string` | Yes |  | IMAP login username |
| `payload.imap_password` | `string` | Yes |  | IMAP login password |

**Direct CLI:**

```
deepline tools execute lemlist_connect_email_account --payload '{
  "sender_name": "string",
  "sender_email": "string",
  "smtp_host": "string",
  "smtp_port": "integer",
  "smtp_login": "string",
  "smtp_password": "string",
  "imap_host": "string",
  "imap_port": "integer",
  "imap_login": "string",
  "imap_password": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-create-campaign`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-create-company-note`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-create-label`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-create-lead-in-campaign`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-create-schedule`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-create-task`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-delete-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_delete_lead:{"campaign_id":"{{campaign_id}}","lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  | Campaign ID |
| `payload.lead_id` | `string` | Yes |  | Lead ID |

**Direct CLI:**

```
deepline tools execute lemlist_delete_lead --payload '{
  "campaign_id": "string",
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-delete-lead-variables`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_delete_lead_variables:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_delete_lead_variables --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-delete-schedule`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-delete-sequence-step`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-delete-unsubscribe`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_delete_unsubscribe:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email to remove from unsubscribe list (e.g. [alice@acme.com](mailto:alice@acme.com) or acme.com) |

**Direct CLI:**

```
deepline tools execute lemlist_delete_unsubscribe --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-disconnect-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_disconnect_email_account:{"email_account_id":"{{email_account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_account_id` | `string` | Yes |  | Email account ID to disconnect |

**Direct CLI:**

```
deepline tools execute lemlist_disconnect_email_account --payload '{
  "email_account_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-duplicate-campaign`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-enrich-data`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_enrich_data:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_enrich_data --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-export-campaign-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_export_campaign_leads:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  | Campaign ID |

**Direct CLI:**

```
deepline tools execute lemlist_export_campaign_leads --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-export-unsubscribed-contacts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_export_unsubscribed_contacts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_export_unsubscribed_contacts --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-export-unsubscribed-variables`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_export_unsubscribed_variables:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_export_unsubscribed_variables --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-export-unsubscribes`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-activities`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_activities:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_get_activities --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-campaign`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-campaign-export-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_campaign_export_status:{"campaign_id":"{{campaign_id}}","export_id":"{{export_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  |  |
| `payload.export_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_get_campaign_export_status --payload '{
  "campaign_id": "string",
  "export_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-campaign-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_campaign_leads:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_get_campaign_leads --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-campaign-reports`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-campaign-schedules`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-campaign-sequences`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-campaign-statutes`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_campaign_statutes:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_get_campaign_statutes --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-company-notes`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-contact-subscription-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_contact_subscription_status:{"contact_id":"{{contact_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.contact_id` | `string` | Yes |  | Contact ID |

**Direct CLI:**

```
deepline tools execute lemlist_get_contact_subscription_status --payload '{
  "contact_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-crm-filters`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_crm_filters:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_get_crm_filters --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-enrichment-result`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_enrichment_result:{"enrich_id":"{{enrich_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.enrich_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_get_enrichment_result --payload '{
  "enrich_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-inbox-thread`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_inbox_thread:{"contact_id":"{{contact_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.contact_id` | `string` | Yes |  | Contact ID (e.g. ctc\_abc123) |

**Direct CLI:**

```
deepline tools execute lemlist_get_inbox_thread --payload '{
  "contact_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-label`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-lead-activity`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_lead_activity:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  | Lemlist lead ID to get activities for |

**Direct CLI:**

```
deepline tools execute lemlist_get_lead_activity --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-lead-by-email`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-lead-by-email-or-id`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-lemwarm-settings`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-many-labels`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-many-schedules`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-many-tasks`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-schedule`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-signals`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_signals:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_get_signals --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-team`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-team-crm-users`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_team_crm_users:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_get_team_crm_users --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-team-senders`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-unsubscribe-by-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_unsubscribe_by_email:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address or domain to look up (e.g. [alice@acme.com](mailto:alice@acme.com) or acme.com) |

**Direct CLI:**

```
deepline tools execute lemlist_get_unsubscribe_by_email --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-unsubscribed-variable`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_unsubscribed_variable:{"value":"{{value}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.value` | `string` | Yes |  | Variable value (email, domain, phone, etc.) |

**Direct CLI:**

```
deepline tools execute lemlist_get_unsubscribed_variable --payload '{
  "value": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-unsubscribes`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_unsubscribes:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_get_unsubscribes --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-get-user`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-get-user-channels`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_get_user_channels:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_get_user_channels --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-ignore-tasks`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-import-leads-from-crm`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_import_leads_from_crm:{"campaign_id":"{{campaign_id}}","crm":"{{crm}}","user_id":"{{user_id}}","filter_id":"{{filter_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  |  |
| `payload.crm` | `string` | Yes |  |  |
| `payload.user_id` | `string` | Yes |  |  |
| `payload.filter_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_import_leads_from_crm --payload '{
  "campaign_id": "string",
  "crm": "string",
  "user_id": "string",
  "filter_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-list-companies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_list_companies:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_list_companies --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-list-fields`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_list_fields:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_list_fields --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-list-inbox`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_list_inbox:{"user_id":"{{user_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.user_id` | `string` | Yes |  | Lemlist user ID |

**Direct CLI:**

```
deepline tools execute lemlist_list_inbox --payload '{
  "user_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-list-unsubscribed-variables`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_list_unsubscribed_variables:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_list_unsubscribed_variables --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-mark-lead-interested`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_mark_lead_interested:{"lead_id_or_email":"{{lead_id_or_email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id_or_email` | `string` | Yes |  | Lead ID or email address |

**Direct CLI:**

```
deepline tools execute lemlist_mark_lead_interested --payload '{
  "lead_id_or_email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-mark-lead-not-interested`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_mark_lead_not_interested:{"lead_id_or_email":"{{lead_id_or_email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id_or_email` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_mark_lead_not_interested --payload '{
  "lead_id_or_email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-mark-lead-not-interested-in-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_mark_lead_not_interested_in_campaign:{"campaign_id":"{{campaign_id}}","lead_id_or_email":"{{lead_id_or_email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  |  |
| `payload.lead_id_or_email` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_mark_lead_not_interested_in_campaign --payload '{
  "campaign_id": "string",
  "lead_id_or_email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-pause-campaign`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-pause-lead`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-pause-lemwarm`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_pause_lemwarm:{"user_mailbox_id":"{{user_mailbox_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.user_mailbox_id` | `string` | Yes |  | User mailbox ID |

**Direct CLI:**

```
deepline tools execute lemlist_pause_lemwarm --payload '{
  "user_mailbox_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-remove-labels-from-conversation`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_remove_labels_from_conversation:{"contact_id":"{{contact_id}}","label_ids":"{{label_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.contact_id` | `string` | Yes |  |  |
| `payload.label_ids` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_remove_labels_from_conversation --payload '{
  "contact_id": "string",
  "label_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-resubscribe-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_resubscribe_contact:{"contact_id":"{{contact_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.contact_id` | `string` | Yes |  | Contact ID to re-subscribe |

**Direct CLI:**

```
deepline tools execute lemlist_resubscribe_contact --payload '{
  "contact_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-resubscribe-variable`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_resubscribe_variable:{"value":"{{value}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.value` | `string` | Yes |  | Value to re-subscribe |

**Direct CLI:**

```
deepline tools execute lemlist_resubscribe_variable --payload '{
  "value": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-resume-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_resume_lead:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  | Lead ID |

**Direct CLI:**

```
deepline tools execute lemlist_resume_lead --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-send-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_send_email:{"send_user_id":"{{send_user_id}}","send_user_email":"{{send_user_email}}","send_user_mailbox_id":"{{send_user_mailbox_id}}","contact_id":"{{contact_id}}","lead_id":"{{lead_id}}","subject":"{{subject}}","message":"{{message}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.send_user_id` | `string` | Yes |  | Sender user ID |
| `payload.send_user_email` | `string` | Yes |  | Sender email address (must be a valid email) |
| `payload.send_user_mailbox_id` | `string` | Yes |  | Sender mailbox ID |
| `payload.contact_id` | `string` | Yes |  | Recipient contact ID |
| `payload.lead_id` | `string` | Yes |  | Lead ID |
| `payload.subject` | `string` | Yes |  | Email subject |
| `payload.message` | `string` | Yes |  | Email body (HTML) |

**Direct CLI:**

```
deepline tools execute lemlist_send_email --payload '{
  "send_user_id": "string",
  "send_user_email": "string",
  "send_user_mailbox_id": "string",
  "contact_id": "string",
  "lead_id": "string",
  "subject": "string",
  "message": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-send-linkedin-message`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_send_linkedin_message:{"send_user_id":"{{send_user_id}}","lead_id":"{{lead_id}}","contact_id":"{{contact_id}}","message":"{{message}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.send_user_id` | `string` | Yes |  | Sender user ID |
| `payload.lead_id` | `string` | Yes |  | Lead ID |
| `payload.contact_id` | `string` | Yes |  | Contact ID |
| `payload.message` | `string` | Yes |  | Message content (plain text) |

**Direct CLI:**

```
deepline tools execute lemlist_send_linkedin_message --payload '{
  "send_user_id": "string",
  "lead_id": "string",
  "contact_id": "string",
  "message": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-send-whatsapp-message`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_send_whatsapp_message:{"send_user_id":"{{send_user_id}}","send_user_whatsapp_account_id":"{{send_user_whatsapp_account_id}}","contact_id":"{{contact_id}}","lead_id":"{{lead_id}}","message":"{{message}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.send_user_id` | `string` | Yes |  |  |
| `payload.send_user_whatsapp_account_id` | `string` | Yes |  |  |
| `payload.contact_id` | `string` | Yes |  |  |
| `payload.lead_id` | `string` | Yes |  |  |
| `payload.message` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_send_whatsapp_message --payload '{
  "send_user_id": "string",
  "send_user_whatsapp_account_id": "string",
  "contact_id": "string",
  "lead_id": "string",
  "message": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-set-export-email-notification`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_set_export_email_notification:{"campaign_id":"{{campaign_id}}","export_id":"{{export_id}}","email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  |  |
| `payload.export_id` | `string` | Yes |  |  |
| `payload.email` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_set_export_email_notification --payload '{
  "campaign_id": "string",
  "export_id": "string",
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-start-campaign`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-start-campaign-export`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-start-lemwarm`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_start_lemwarm:{"user_mailbox_id":"{{user_mailbox_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.user_mailbox_id` | `string` | Yes |  | User mailbox ID |

**Direct CLI:**

```
deepline tools execute lemlist_start_lemwarm --payload '{
  "user_mailbox_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-test-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_test_email_account:{"email_account_id":"{{email_account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_account_id` | `string` | Yes |  | Email account ID to test |

**Direct CLI:**

```
deepline tools execute lemlist_test_email_account --payload '{
  "email_account_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-unsubscribe-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_unsubscribe_contact:{"contact_id":"{{contact_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.contact_id` | `string` | Yes |  | Contact ID to unsubscribe |

**Direct CLI:**

```
deepline tools execute lemlist_unsubscribe_contact --payload '{
  "contact_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-unsubscribe-lead-from-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_unsubscribe_lead_from_campaign:{"campaign_id":"{{campaign_id}}","email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  | Campaign ID |
| `payload.email` | `string` | Yes |  | Lead email to unsubscribe |

**Direct CLI:**

```
deepline tools execute lemlist_unsubscribe_lead_from_campaign --payload '{
  "campaign_id": "string",
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-unsubscribe-variable`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_unsubscribe_variable:{"value":"{{value}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.value` | `string` | Yes |  | Value to unsubscribe (email, domain, phone, LinkedIn URL) |

**Direct CLI:**

```
deepline tools execute lemlist_unsubscribe_variable --payload '{
  "value": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-update-campaign`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-update-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_update_lead:{"campaign_id":"{{campaign_id}}","lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string` | Yes |  |  |
| `payload.lead_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_update_lead --payload '{
  "campaign_id": "string",
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-update-lead-variables`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_update_lead_variables:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute lemlist_update_lead_variables --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-update-lemwarm-settings`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-update-schedule`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-update-sequence-step`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-update-task`

🚧 A parsing error occured. Please contact the owner of this website. They can use the Mintlify CLI to test this website locally and see the errors that occur.


#### `lemlist-upload-audio-for-voice-step`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_upload_audio_for_voice_step:{"lead_id":"{{lead_id}}","step_id":"{{step_id}}","audio_url":"{{audio_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string` | Yes |  | Lead ID |
| `payload.step_id` | `string` | Yes |  | Sequence step ID |
| `payload.audio_url` | `string` | Yes |  | URL of the audio file |

**Direct CLI:**

```
deepline tools execute lemlist_upload_audio_for_voice_step --payload '{
  "lead_id": "string",
  "step_id": "string",
  "audio_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `lemlist-upsert-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_upsert_company:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_upsert_company --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `list-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lemlist_list_campaigns:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lemlist_list_campaigns --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## LimaData

*Provider folder: `limadata` · 13 endpoints*


### LimaData — Agent Guidance

## Playbook

Use Limadata for real-time person enrichment, company enrichment, email/phone finding, identity resolution, web search, AI research, and URL extraction.Pricing is settled from Limadata’s `x-credits-cost` response header whenever present. Static credit notes are used for pre-run estimates and fallback settlement only.The current OpenAPI snapshot contains only synchronous POST endpoints. Watch/webhook APIs are not present in this snapshot and are not exposed by this provider registration.


### LimaData — Endpoints


#### `limadata-enrich-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_enrich_company:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute limadata_enrich_company --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-enrich-person`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_enrich_person:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute limadata_enrich_person --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-find-audience-identifiers`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_find_audience_identifiers:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute limadata_find_audience_identifiers --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-find-company-linkedin`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_find_company_linkedin:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Company domain. Example: microsoft.com or [https://microsoft.com](https://microsoft.com/) |

**Direct CLI:**

```
deepline tools execute limadata_find_company_linkedin --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-find-person-profiles`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_find_person_profiles:{"full_name":"{{full_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.full_name` | `string` | Yes |  | Person’s full name. Example: William Gates |

**Direct CLI:**

```
deepline tools execute limadata_find_person_profiles --payload '{
  "full_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-find-personal-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_find_personal_email:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute limadata_find_personal_email --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `1.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-find-phone`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_find_phone:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute limadata_find_phone --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `2.8` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-find-work-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_find_work_email:{"full_name":"{{full_name}}","company_domain":"{{company_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.full_name` | `string` | Yes |  | Person’s full name. Example: William Gates |
| `payload.company_domain` | `string` | Yes |  | Person’s company domain. Example: microsoft.com |

**Direct CLI:**

```
deepline tools execute limadata_find_work_email --payload '{
  "full_name": "string",
  "company_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-find-work-email-linkedin`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_find_work_email_linkedin:{"linkedin_url":"{{linkedin_url}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.linkedin_url` | `string` | Yes |  | Person’s LinkedIn profile URL. Example: `https://www.linkedin.com/in/williamgates` Or `https://www.linkedin.com/in/ACwAAAAAMGMBPd3AaxX06wlYtpojNXGpAJlGSOc` |

**Direct CLI:**

```
deepline tools execute limadata_find_work_email_linkedin --payload '{
  "linkedin_url": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.84` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-research-extract`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_research_extract:{"urls":"{{urls}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.urls` | `array` | Yes |  | List of URLs to extract content from (1-10 URLs) |

**Direct CLI:**

```
deepline tools execute limadata_research_extract --payload '{
  "urls": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-research-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_research_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | Search query string |

**Direct CLI:**

```
deepline tools execute limadata_research_search --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.09` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-reverse-email-lookup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_reverse_email_lookup:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Person’s email address. Example: [mcuban@gmail.com](mailto:mcuban@gmail.com) |

**Direct CLI:**

```
deepline tools execute limadata_reverse_email_lookup --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `1.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `limadata-search-web`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=limadata_search_web:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | Search query string |

**Direct CLI:**

```
deepline tools execute limadata_search_web --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.03` per pricing unit.
- Billing mode: `post_deduct`.


---

## LinkedIn Ads Audiences

*Provider folder: `linkedin_ads_audiences` · 5 endpoints*


### LinkedIn Ads Audiences — Endpoints


#### `create-audience`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=linkedin_ads_audiences_create_audience:{"account_id":"{{account_id}}","name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `string` | Yes |  | LinkedIn sponsored account numeric ID. Deepline converts it to urn:li:sponsoredAccount:{id}. |
| `payload.name` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute linkedin_ads_audiences_create_audience --payload '{
  "account_id": "string",
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `delete-audience`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=linkedin_ads_audiences_delete_audience:{"account_id":"{{account_id}}","audience_id":"{{audience_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `string` | Yes |  | LinkedIn sponsored account numeric ID. Deepline converts it to urn:li:sponsoredAccount:{id}. |
| `payload.audience_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute linkedin_ads_audiences_delete_audience --payload '{
  "account_id": "string",
  "audience_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `get-audience-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=linkedin_ads_audiences_get_audience_status:{"account_id":"{{account_id}}","audience_id":"{{audience_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `string` | Yes |  | LinkedIn sponsored account numeric ID. Deepline converts it to urn:li:sponsoredAccount:{id}. |
| `payload.audience_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute linkedin_ads_audiences_get_audience_status --payload '{
  "account_id": "string",
  "audience_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `list-audiences`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=linkedin_ads_audiences_list_audiences:{"account_id":"{{account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `string` | Yes |  | LinkedIn sponsored account numeric ID. Deepline converts it to urn:li:sponsoredAccount:{id}. |

**Direct CLI:**

```
deepline tools execute linkedin_ads_audiences_list_audiences --payload '{
  "account_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `sync-audience-members`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=linkedin_ads_audiences_sync_audience_members:{"account_id":"{{account_id}}","audience_id":"{{audience_id}}","rows":"{{rows}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.account_id` | `string` | Yes |  | LinkedIn sponsored account numeric ID. Deepline converts it to urn:li:sponsoredAccount:{id}. |
| `payload.audience_id` | `string` | Yes |  |  |
| `payload.rows` | `array` | Yes |  | Audience member rows to upload. Most platforms accept much smaller per-request batches, so Deepline automatically chunks requests. |

**Direct CLI:**

```
deepline tools execute linkedin_ads_audiences_sync_audience_members --payload '{
  "account_id": "string",
  "audience_id": "string",
  "rows": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


---

## Lusha

*Provider folder: `lusha` · 3 endpoints*


### Lusha — Agent Guidance

## Playbook

# Lusha — Agent Guidance

## When to use

Lusha for B2B email + direct dial enrichment. Strong North American and European coverage with intent signal data. Good for sales prospecting workflows where direct dials matter. Cost-competitive at $0.05/credit.**Key strength**: Direct dials (not just HQ numbers). Lusha often surfaces mobile and desk direct numbers that other providers miss.

## Provider characteristics

- **Input required**: LinkedIn URL (best), email, or first\_name+last\_name+(company\_name or company\_domain)
- **Geographic coverage**: Global, strongest in North America + Europe
- **Credit cost**: ~$0.05 per credit (person enrich, company enrich, or contact from search)
- **LinkedIn URL requirement**: Must contain “linkedin.com/in/”. Sales Navigator URLs not supported.

## Key operations

### lusha\_enrich\_person

Enriches a person by LinkedIn URL (preferred), email, or name+company. Returns emails at `emails[].email` and phones at `phones[].number`.

```
{
  "linkedin_url": "https://www.linkedin.com/in/johndoe"
}
```

```
{
  "first_name": "Jane",
  "last_name": "Smith",
  "company_domain": "acme.com"
}
```

Optional flags:

- `reveal_emails: true` — include email addresses in response (default: true)
- `reveal_phones: true` — include phone numbers (default: true)
- `signals: true` — include intent signal data

### lusha\_enrich\_company

Enriches a company from domain, name, or Lusha company ID. Returns size, revenue, industry, technologies.

```
{
  "domain": "salesforce.com"
}
```

### lusha\_search\_contacts

Prospecting search with rich filters: department, seniority, company size, industry, job title, location, and tech stack.

```
{
  "filters": {
    "seniority": ["director", "vp", "c_suite"],
    "companySize": ["201-500", "501-1000"],
    "department": ["sales"]
  },
  "pageSize": 25
}
```

## Output shape

`lusha_enrich_person` returns a flat profile. Email at `emails[0].email` or `email`. Phone at `phones[0].number` or `phone`.`lusha_enrich_company` returns a flat company object with `name`, `domain`, `size`, `industry`, `technologies[]`.`lusha_search_contacts` returns `{ contacts: [...], pagination: { page, pageSize, total, totalPages } }`.

## Anti-patterns

- Don’t use Sales Navigator or Recruiter LinkedIn URLs — they’ll fail
- Don’t include “http://” or “ [www](http://www/).” in domain values for company enrichment
- Don’t pass `company_name` alone without `first_name` \+ `last_name` for person lookup — name+company is the minimum combo
- Don’t assume `phone` (top-level) is always populated — check `phones[]` array first for the most complete list


### Lusha — Endpoints


#### `enrich-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lusha_enrich_company:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lusha_enrich_company --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.7` per pricing unit.
- Billing mode: `post_deduct`.


#### `enrich-person`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lusha_enrich_person:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute lusha_enrich_person --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.7` per pricing unit.
- Billing mode: `post_deduct`.


#### `search-contacts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=lusha_search_contacts:{"filters":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.filters` | `object` | Yes |  | Current Lusha prospecting search contract uses nested contact/company filters. |

**Direct CLI:**

```
deepline tools execute lusha_search_contacts --payload '{
  "filters": {}
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.7` per pricing unit.
- Billing mode: `post_deduct`.


---

## Meta Audiences

*Provider folder: `meta_audiences` · 5 endpoints*


### Meta Audiences — Endpoints


#### `create-audience`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=meta_audiences_create_audience:{"ad_account_id":"{{ad_account_id}}","name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.ad_account_id` | `string` | Yes |  | Meta ad account ID. Deepline prefixes it with act\_ when needed. |
| `payload.name` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute meta_audiences_create_audience --payload '{
  "ad_account_id": "string",
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `delete-audience`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=meta_audiences_delete_audience:{"ad_account_id":"{{ad_account_id}}","audience_id":"{{audience_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.ad_account_id` | `string` | Yes |  | Meta ad account ID. Deepline prefixes it with act\_ when needed. |
| `payload.audience_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute meta_audiences_delete_audience --payload '{
  "ad_account_id": "string",
  "audience_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `get-audience-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=meta_audiences_get_audience_status:{"ad_account_id":"{{ad_account_id}}","audience_id":"{{audience_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.ad_account_id` | `string` | Yes |  | Meta ad account ID. Deepline prefixes it with act\_ when needed. |
| `payload.audience_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute meta_audiences_get_audience_status --payload '{
  "ad_account_id": "string",
  "audience_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `list-audiences`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=meta_audiences_list_audiences:{"ad_account_id":"{{ad_account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.ad_account_id` | `string` | Yes |  | Meta ad account ID. Deepline prefixes it with act\_ when needed. |

**Direct CLI:**

```
deepline tools execute meta_audiences_list_audiences --payload '{
  "ad_account_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `sync-audience-members`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=meta_audiences_sync_audience_members:{"ad_account_id":"{{ad_account_id}}","audience_id":"{{audience_id}}","rows":"{{rows}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.ad_account_id` | `string` | Yes |  | Meta ad account ID. Deepline prefixes it with act\_ when needed. |
| `payload.audience_id` | `string` | Yes |  |  |
| `payload.rows` | `array` | Yes |  | Audience member rows to upload. Most platforms accept much smaller per-request batches, so Deepline automatically chunks requests. |

**Direct CLI:**

```
deepline tools execute meta_audiences_sync_audience_members --payload '{
  "ad_account_id": "string",
  "audience_id": "string",
  "rows": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


---

## OpenMart

*Provider folder: `openmart` · 11 endpoints*


### OpenMart — Agent Guidance

## Playbook

# Openmart Guidance

Use Openmart for local-business and SMB workflows where store-level location data, brand records, shared company emails, known-person enrichment, employee discovery, or technology detection matter.

- Prefer `openmart_search_brands` when the workflow needs one row per company/brand.
- Prefer `openmart_search_businesses` when the workflow needs physical store records, ratings, categories, addresses, or location filters.
- Use `openmart_enrich_company` when the input is a website or social profile and the goal is to resolve matching Openmart records.
- Async launchers default to synchronous execution: they submit the batch, wait for completion, and return completed task results. Set `wait_for_completion: false` when the workflow only needs a `batch_id`.
- For manually managed async jobs, poll `openmart_get_batch_status`, list completed IDs with `openmart_get_batch_task_ids`, then fetch each result with `openmart_get_task_result`.
- Treat `null` arrays in brand responses as empty arrays.
- Openmart docs require `country` on each `/api/v2/brands/search` location entry.
- The configured provider token should be rotated if it has been pasted into chat, logs, or other plaintext surfaces.

Pricing note: Openmart provider spend is modeled at $0.0298 per Openmart credit. Search and brand search cost 0.3 credits per company record; enrich company costs 0.3 credits per store record; find people and known-person lookup cost 14 credits per phone found, 1 per email found, and 1 per name found; technographics costs 2 credits per company record.


### OpenMart — Endpoints


#### `openmart-enrich-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openmart_enrich_company:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute openmart_enrich_company --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


#### `openmart-find-people`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openmart_find_people:{"tasks":"{{tasks}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tasks` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openmart_find_people --payload '{
  "tasks": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.42` per pricing unit.
- Billing mode: `post_deduct`.


#### `openmart-find-tech`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openmart_find_tech:{"tasks":"{{tasks}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tasks` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openmart_find_tech --payload '{
  "tasks": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.84` per pricing unit.
- Billing mode: `post_deduct`.


#### `openmart-get-batch-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openmart_get_batch_status:{"batch_id":"{{batch_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.batch_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openmart_get_batch_status --payload '{
  "batch_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `openmart-get-batch-task-ids`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openmart_get_batch_task_ids:{"batch_id":"{{batch_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.batch_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openmart_get_batch_task_ids --payload '{
  "batch_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `openmart-get-task-result`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openmart_get_task_result:{"task_id":"{{task_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.task_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openmart_get_task_result --payload '{
  "task_id": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.42` per pricing unit.
- Billing mode: `post_deduct`.


#### `openmart-lookup-business-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openmart_lookup_business_email:{"tasks":"{{tasks}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tasks` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openmart_lookup_business_email --payload '{
  "tasks": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.42` per pricing unit.
- Billing mode: `post_deduct`.


#### `openmart-lookup-people`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openmart_lookup_people:{"tasks":"{{tasks}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.tasks` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openmart_lookup_people --payload '{
  "tasks": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.42` per pricing unit.
- Billing mode: `post_deduct`.


#### `openmart-search-brands`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openmart_search_brands:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute openmart_search_brands --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


#### `openmart-search-business-ids`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openmart_search_business_ids:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute openmart_search_business_ids --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


#### `openmart-search-businesses`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openmart_search_businesses:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute openmart_search_businesses --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.13` per pricing unit.
- Billing mode: `post_deduct`.


---

## OpenSOSData

*Provider folder: `opensosdata` · 1 endpoints*


### OpenSOSData — Agent Guidance

## Playbook

# OpenSOSData Integration Guide

US Secretary of State business entity lookup across currently active OpenSOSData jurisdictions.

## When to use

Use `opensosdata_business_lookup` to find the registered officer name for a business entity.
This is the bridge between “I have a restaurant name” and “I have an owner name to skip-trace.”Typical flow:

1. `opensosdata_business_lookup` → get officer name from SOS
2. `enformion_contact_enrich` → get mobile phone from officer name + city/state

## State-by-state officer data coverage

| Coverage | States | Notes |
| --- | --- | --- |
| **Full** (name + address + title) | FL, TX (franchise tax), CO, PA, MN, NY, WI, KY, SC, RI | FL is best — full officer list with addresses |
| **Name only** | IL, IN, TN, CT, MA, GA, NV, ND, AL, AR, IA, MO |  |
| **Entity found, no officers** | OH, CA, MI, WA, NJ | SOS shields member names — use other methods |

## Critical gotchas

### 1\. FL officer name format — ALWAYS reverse

FL SOS returns `"LASTNAME, FIRSTNAME MIDDLE"`. Use `reverseOfficerName()` from `opensosdata-shared.ts`:

- `"SMITH, JOHN EDWARD"` → `"John Smith"`
- `"DE LA CRUZ, MARIA"` → `"Maria De La Cruz"`

Other states that use this format: LA, SC, sometimes GA.

### 2\. Async states (CA, MA, NV, OR, WA)

These states return HTTP 202 with a `jobId`. The action **automatically polls** every 3 seconds
until complete (up to 90s). No special handling needed by the caller.

### 3\. Skip registered agent services

Filter officer names containing: “Corporation Service”, “CT Corporation”, “Incorp”,
“Northwest Registered”, “Statutory Agent”. These are professional RA services, not people.
Use `isRegisteredAgentService()` from `opensosdata-shared.ts`.

### 4\. Balance monitoring

Each lookup costs Deepline credits. Check remaining balance before large batch runs.
Check balance: `GET /v1/account/balance` → `lookupsRemaining`.
Topup URL: [https://app.opensosdata.com#billing](https://app.opensosdata.com/#billing)

## Response structure

Synchronous (most states):

```
{
  "success": true,
  "data": {
    "entityName": "NOBLE BEAST BREWING LLC",
    "entityType": "LLC",
    "entityId": "2441200",
    "status": "Active",
    "formationDate": "10/28/2015",
    "registeredAgentName": "",
    "officers": [],
    "sosUrl": "https://businesssearch.ohiosos.gov/...",
    "scrapedAt": "2026-05-31T..."
  }
}
```

FL with full officers:

```
{
  "success": true,
  "data": {
    "entityName": "CASTAWAYS RIVER TIKI BAR LLC",
    "officers": [\
      { "name": "SWANSON, KIRK ALAN", "title": "MGR", "address": "..." }\
    ]
  }
}
```

→ Reverse: `reverseOfficerName("SWANSON, KIRK ALAN")` = `"Kirk Swanson"`Async (CA/MA/NV/OR/WA) — handled automatically, caller receives final result.Not found (no charge):

```
{ "success": false, "error": "Entity not found", "cost": 0 }
```

## Credentials

Single API key passed as `x-api-key` header.
Set via Deepline dashboard → Integrations → OpenSOSData, or `OPENSOSDATA_API_KEY` env var.API endpoint: `https://api.opensosdata.com/v1/lookup`


### OpenSOSData — Endpoints


#### `opensosdata-business-lookup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=opensosdata_business_lookup:{"entity_name":"{{entity_name}}","state":"AL"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.entity_name` | `string` | Yes |  | Business or restaurant name to look up (e.g. ‘Noble Beast Brewing’). Fuzzy matching is applied by OpenSOSData. |
| `payload.state` | `"AL" | "AK" | "AZ" | "AR" | "CA" | "CO" | "CT" | "DE" | "FL" | "GA" | "HI" | "ID" | "IL" | "IN" | "IA" | "KS" | "KY" | "LA" | "ME" | "MD" | "MA" | "MI" | "MN" | "MS" | "MO" | "MT" | "NE" | "NV" | "NH" | "NJ" | "NM" | "NY" | "NC" | "ND" | "OH" | "OK" | "OR" | "PA" | "RI" | "SC" | "SD" | "TN" | "TX" | "UT" | "VT" | "VA" | "WA" | "WV" | "WI" | "WY" | "DC" | "PR"` | Yes |  | Two-letter US state code (e.g. ‘OH’, ‘FL’). All currently active OpenSOSData jurisdictions are supported. Note: OH, CA, MI, WA do NOT expose officer names in their SOS records. The entity is found but officers\[\] will be empty for these states. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.state` | `AL`, `AK`, `AZ`, `AR`, `CA`, `CO`, `CT`, `DE`, `FL`, `GA`, `HI`, `ID`, `IL`, `IN`, `IA`, `KS`, `KY`, `LA`, `ME`, `MD`, `MA`, `MI`, `MN`, `MS`, `MO`, `MT`, `NE`, `NV`, `NH`, `NJ`, `NM`, `NY`, `NC`, `ND`, `OH`, `OK`, `OR`, `PA`, `RI`, `SC`, `SD`, `TN`, `TX`, `UT`, `VT`, `VA`, `WA`, `WV`, `WI`, `WY`, `DC`, `PR` |

**Direct CLI:**

```
deepline tools execute opensosdata_business_lookup --payload '{
  "entity_name": "string",
  "state": "AL"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.44` per pricing unit.
- Billing mode: `post_deduct`.


---

## OpenWebNinja

*Provider folder: `openwebninja` · 24 endpoints*


### OpenWebNinja — Agent Guidance

## Playbook

Use `openwebninja_jsearch_*` for Google for Jobs data, `openwebninja_glassdoor_*` for employer/review/salary data, and `openwebninja_localbusiness_*` for Google Maps business data. Prefer the narrowest endpoint that matches the task, and pass official ids from search endpoints into detail endpoints when possible.


### OpenWebNinja — Endpoints


#### `openwebninja-glassdoor-company-interviews`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_glassdoor_company_interviews:{"company_id":"{{company_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_glassdoor_company_interviews --payload '{
  "company_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `pre_reserve`.


#### `openwebninja-glassdoor-company-jobs`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_glassdoor_company_jobs:{"company_id":"{{company_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_glassdoor_company_jobs --payload '{
  "company_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `pre_reserve`.


#### `openwebninja-glassdoor-company-overview`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_glassdoor_company_overview:{"company_id":"{{company_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_glassdoor_company_overview --payload '{
  "company_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `pre_reserve`.


#### `openwebninja-glassdoor-company-reviews`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_glassdoor_company_reviews:{"company_id":"{{company_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_glassdoor_company_reviews --payload '{
  "company_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `pre_reserve`.


#### `openwebninja-glassdoor-company-salaries`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_glassdoor_company_salaries:{"company_id":"{{company_id}}","job_title":"{{job_title}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id` | `string` | Yes |  |  |
| `payload.job_title` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_glassdoor_company_salaries --payload '{
  "company_id": "string",
  "job_title": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `pre_reserve`.


#### `openwebninja-glassdoor-company-salaries-v2`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_glassdoor_company_salaries_v2:{"company_id":"{{company_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_glassdoor_company_salaries_v2 --payload '{
  "company_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `pre_reserve`.


#### `openwebninja-glassdoor-company-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_glassdoor_company_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_glassdoor_company_search --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `pre_reserve`.


#### `openwebninja-glassdoor-job-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_glassdoor_job_search:{"query":"{{query}}","location":"{{location}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  |  |
| `payload.location` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_glassdoor_job_search --payload '{
  "query": "string",
  "location": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `pre_reserve`.


#### `openwebninja-glassdoor-salary-estimation`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_glassdoor_salary_estimation:{"job_title":"{{job_title}}","location":"{{location}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.job_title` | `string` | Yes |  |  |
| `payload.location` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_glassdoor_salary_estimation --payload '{
  "job_title": "string",
  "location": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `pre_reserve`.


#### `openwebninja-jsearch-company-job-salary`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_jsearch_company_job_salary:{"company":"{{company}}","job_title":"{{job_title}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company` | `string` | Yes |  |  |
| `payload.job_title` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_jsearch_company_job_salary --payload '{
  "company": "string",
  "job_title": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-jsearch-estimated-salary`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_jsearch_estimated_salary:{"job_title":"{{job_title}}","location":"{{location}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.job_title` | `string` | Yes |  |  |
| `payload.location` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_jsearch_estimated_salary --payload '{
  "job_title": "string",
  "location": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-jsearch-job-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_jsearch_job_details:{"job_id":"{{job_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.job_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_jsearch_job_details --payload '{
  "job_id": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-jsearch-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_jsearch_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_jsearch_search --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.07` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-localbusiness-autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_localbusiness_autocomplete:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_localbusiness_autocomplete --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-localbusiness-business-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_localbusiness_business_details:{"business_id":"{{business_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.business_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_localbusiness_business_details --payload '{
  "business_id": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-localbusiness-business-photos`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_localbusiness_business_photos:{"business_id":"{{business_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.business_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_localbusiness_business_photos --payload '{
  "business_id": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-localbusiness-business-posts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_localbusiness_business_posts:{"business_id":"{{business_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.business_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_localbusiness_business_posts --payload '{
  "business_id": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-localbusiness-business-reviews`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_localbusiness_business_reviews:{"business_id":"{{business_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.business_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_localbusiness_business_reviews --payload '{
  "business_id": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-localbusiness-photo-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_localbusiness_photo_details:{"business_id":"{{business_id}}","photo_id":"{{photo_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.business_id` | `string` | Yes |  |  |
| `payload.photo_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_localbusiness_photo_details --payload '{
  "business_id": "string",
  "photo_id": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-localbusiness-reverse-geocoding`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_localbusiness_reverse_geocoding:{"lat":"{{lat}}","lng":"{{lng}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lat` | `string` | Yes |  |  |
| `payload.lng` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_localbusiness_reverse_geocoding --payload '{
  "lat": "string",
  "lng": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-localbusiness-review-details`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_localbusiness_review_details:{"business_id":"{{business_id}}","review_author_id":"{{review_author_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.business_id` | `string` | Yes |  |  |
| `payload.review_author_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_localbusiness_review_details --payload '{
  "business_id": "string",
  "review_author_id": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-localbusiness-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_localbusiness_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_localbusiness_search --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-localbusiness-search-in-area`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_localbusiness_search_in_area:{"query":"{{query}}","lat":"{{lat}}","lng":"{{lng}}","zoom":"{{zoom}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  |  |
| `payload.lat` | `string` | Yes |  |  |
| `payload.lng` | `string` | Yes |  |  |
| `payload.zoom` | `integer` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_localbusiness_search_in_area --payload '{
  "query": "string",
  "lat": "string",
  "lng": "string",
  "zoom": "integer"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


#### `openwebninja-localbusiness-search-nearby`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=openwebninja_localbusiness_search_nearby:{"query":"{{query}}","lat":"{{lat}}","lng":"{{lng}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  |  |
| `payload.lat` | `string` | Yes |  |  |
| `payload.lng` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute openwebninja_localbusiness_search_nearby --payload '{
  "query": "string",
  "lat": "string",
  "lng": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.06` per pricing unit.
- Billing mode: `post_deduct`.


---

## Parallel

*Provider folder: `parallel` · 9 endpoints*


### Parallel — Agent Guidance

## Playbook

Use Parallel for managed research/extraction runs without custom orchestration.

- Prefer `parallel_run_task`, `parallel_search`, and `parallel_extract` for agent-friendly workflows.
- Prefer `parallel_search_mcp` and `parallel_fetch_mcp` when you want the free hosted Search MCP instead of the paid REST APIs.
- Prefer `parallel_search` first for attendee/discovery workflows, then `parallel_extract` for targeted pages.
- `parallel_search_mcp` is best for lightweight current-events or broad-source web lookups where zero provider spend matters more than advanced REST-side controls.
- `parallel_fetch_mcp` is best after `parallel_search_mcp` narrows candidates, or when you already have a small list of URLs to read.
- Use `parallel_run_task` when you need synthesized, schema-shaped outputs from multiple sources.
- Call `parallel_run_task` first. If it finishes quickly, use that result.
- If `parallel_run_task` returns pending or times out, keep the `run_id` and use `parallel_get_task_run_result` later to fetch the final output.
- Ignore `parallel_get_task_run` unless you specifically need run metadata like status timestamps or processor info.
- Keep monitor/stream endpoints out of default flows unless a user explicitly needs them.
- Pilot on a small objective first, then widen `max_results` and scope.
- For the free MCP actions, pass a stable `session_id` across related calls when possible to reduce anonymous-tier throttling.

```
deepline tools execute parallel_search --payload '{"mode":"agentic","objective":"Find recent hiring and launch signals for OpenAI","max_results":5,"excerpts":{"max_chars_per_result":1200,"max_chars_total":10000}}'
```

```
deepline tools execute parallel_search_mcp --payload '{"objective":"Find recent OpenAI product announcements","search_queries":["OpenAI recent announcements","site:openai.com/news OpenAI product"],"session_id":"demo-session"}'
```

```
deepline tools execute parallel_extract --payload '{"urls":["https://openai.com/research/index/release/"],"objective":"Extract key product launch signal, release summary, and source evidence","full_content":true}'
```

```
deepline tools execute parallel_fetch_mcp --payload '{"urls":["https://openai.com/news"],"objective":"Extract the latest product announcement headlines","search_queries":["OpenAI latest product announcements"],"session_id":"demo-session"}'
```

```
deepline tools execute parallel_run_task --payload '{"processor":"lite-fast","input":"Summarize key GTM signals for OpenAI from recent public web sources in 3 bullets."}'
```

```
deepline tools execute parallel_get_task_run_result --payload '{"run_id":"trun_123"}'
```

```
deepline tools execute parallel_search --payload '{"objective":"Find AI companies that raised Series A funding in 2024 with source links","max_results":10}'
deepline tools execute parallel_extract --payload '{"urls":["https://techcrunch.com/2024/12/20/heres-the-full-list-of-49-us-ai-startups-that-have-raised-100m-or-more-in-2024/"],"objective":"Extract company name, funding round, amount, date, and source evidence","excerpts":true}'
```


### Parallel — Endpoints


#### `fetch-mcp`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=parallel_fetch_mcp:{"urls":"{{urls}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.urls` | `array` | Yes |  | Required. URLs to fetch through Parallel Search MCP. |

**Direct CLI:**

```
deepline tools execute parallel_fetch_mcp --payload '{
  "urls": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-task-run-result`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=parallel_get_task_run_result:{"run_id":"{{run_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.run_id` | `string` | Yes |  | Required. Task run id to fetch. |

**Direct CLI:**

```
deepline tools execute parallel_get_task_run_result --payload '{
  "run_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `parallel-extract`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=parallel_extract:{"urls":"{{urls}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.urls` | `array` | Yes |  | Required. List of URLs to extract (max 10). |

**Direct CLI:**

```
deepline tools execute parallel_extract --payload '{
  "urls": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `parallel-findall-run-result`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=parallel_findall_run_result:{"findall_id":"{{findall_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.findall_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute parallel_findall_run_result --payload '{
  "findall_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `parallel-retrieve-findall-run-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=parallel_retrieve_findall_run_status:{"findall_id":"{{findall_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.findall_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute parallel_retrieve_findall_run_status --payload '{
  "findall_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `parallel-run-findall`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=parallel_run_findall:{"objective":"Find recent fintech leads with strong fit signals.","entity_type":"companies","match_conditions":[{"name":"industry","description":"fintech software"}]}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.objective` | `string` | Yes |  | Natural language target to discover. Keep specific enough for reliable matching. |
| `payload.entity_type` | `string` | Yes |  | Entity class to return (for example people, companies, accounts). |
| `payload.match_conditions` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute parallel_run_findall --payload '{
  "objective": "Find recent fintech leads with strong fit signals.",
  "entity_type": "companies",
  "match_conditions": [\
    {\
      "name": "industry",\
      "description": "fintech software"\
    }\
  ]
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `parallel-run-task`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=parallel_run_task:{"input":"{{input}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.input` | `string | record` | Yes |  | Required. Prompt or structured input for the task. |

**Direct CLI:**

```
deepline tools execute parallel_run_task --payload '{
  "input": "string"
}' --json
```

**Cost:**

- Pricing model: `provider_usage` (provider usage).
- Estimated Deepline credits: `0` per pricing unit.
- Billing mode: `post_deduct`.


#### `parallel-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=parallel_search:{"objective":"Find recent fintech announcements and analyst reports."}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute parallel_search --payload '{
  "objective": "Find recent fintech announcements and analyst reports."
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `search-mcp`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=parallel_search_mcp:{"objective":"{{objective}}","search_queries":"{{search_queries}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.objective` | `string` | Yes |  | Required. Search objective for Parallel Search MCP. |
| `payload.search_queries` | `array` | Yes |  | Required. One to three search queries for Parallel Search MCP. |

**Direct CLI:**

```
deepline tools execute parallel_search_mcp --payload '{
  "objective": "string",
  "search_queries": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## People Data Labs

*Provider folder: `peopledatalabs` · 11 endpoints*


### People Data Labs — Agent Guidance

## Playbook

Use People Data Labs when you need explicit, auditable structured filters.

- Normalize noisy input first with clean helpers before running expensive search/enrich operations.
- Use autocomplete and narrow incrementally to avoid over-constraining initial queries.
- Prefer small pilot `size` runs and inspect returned fields before batch execution.
- For `peopledatalabs_person_search` / `peopledatalabs_company_search` SQL: use `SELECT *` only and DO NOT include a `LIMIT` clause — PDL rejects any SQL with `LIMIT` as HTTP 400. Pass the `size` input parameter (1–100) to control how many records come back.
- In changed-company email recovery, treat PDL as the fallback after LeadMagic and Crust.
- If earlier, cheaper steps already returned a usable email, skip PDL for that row.

```
deepline tools execute peopledatalabs_company_clean --payload '{"name":"Open AI Inc"}'
```

```
deepline tools execute peopledatalabs_person_search --payload '{"query":{"bool":{"must":[{"term":{"location_country":"united states"}},{"term":{"job_title_role":"marketing"}}]}},"size":5}'
```

```
deepline tools execute peopledatalabs_autocomplete --payload '{"field":"title","text":"growth"}'
```


### People Data Labs — Endpoints


#### `autocomplete`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=peopledatalabs_autocomplete:{"field":"company","text":"open"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.field` | `"all_location" | "class" | "company" | "country" | "industry" | "location" | "location_name" | "major" | "region" | "role" | "school" | "sub_role" | "skill" | "title" | "website"` | Yes |  | PDL field to autocomplete (e.g., company, location, school). |
| `payload.text` | `string` | Yes |  | User text prefix to autocomplete. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.field` | `all_location`, `class`, `company`, `country`, `industry`, `location`, `location_name`, `major`, `region`, `role`, `school`, `sub_role`, `skill`, `title`, `website` |

**Direct CLI:**

```
deepline tools execute peopledatalabs_autocomplete --payload '{
  "field": "company",
  "text": "open"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `bulk-organization-enrichment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=peopledatalabs_bulk_organization_enrichment:{"domains":["example.com"]}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domains` | `array` | Yes |  | List of company domains to enrich (1-100). |

**Direct CLI:**

```
deepline tools execute peopledatalabs_bulk_organization_enrichment --payload '{
  "domains": [\
    "example.com"\
  ]
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `bulk-people-enrichment`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=peopledatalabs_bulk_people_enrichment:{"details":[{"first_name":"Jane","last_name":"Doe","email":"jane.doe@example.com","domain":"example.com"}]}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.details` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute peopledatalabs_bulk_people_enrichment --payload '{
  "details": [\
    {\
      "first_name": "Jane",\
      "last_name": "Doe",\
      "email": "jane.doe@example.com",\
      "domain": "example.com"\
    }\
  ]
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `3.92` per pricing unit.
- Billing mode: `post_deduct`.


#### `company-clean`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=peopledatalabs_company_clean:{"name":"Example Corp"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute peopledatalabs_company_clean --payload '{
  "name": "Example Corp"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `company-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=peopledatalabs_company_search:{"sql":"SELECT * FROM company WHERE location_country=\"united states\" AND website IS NOT NULL"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute peopledatalabs_company_search --payload '{
  "sql": "SELECT * FROM company WHERE location_country=\"united states\" AND website IS NOT NULL"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `enrich-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=peopledatalabs_enrich_company:{"domain":"example.com"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute peopledatalabs_enrich_company --payload '{
  "domain": "example.com"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.4` per pricing unit.
- Billing mode: `post_deduct`.


#### `enrich-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=peopledatalabs_enrich_contact:{"email":"jane.doe@example.com"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute peopledatalabs_enrich_contact --payload '{
  "email": "jane.doe@example.com"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `3.92` per pricing unit.
- Billing mode: `post_deduct`.


#### `location-clean`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=peopledatalabs_location_clean:{"location":"{{location}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.location` | `string` | Yes |  | Free-form location string to clean. |

**Direct CLI:**

```
deepline tools execute peopledatalabs_location_clean --payload '{
  "location": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `person-identify`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=peopledatalabs_person_identify:{"first_name":"Jane","last_name":"Doe","company":"example"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute peopledatalabs_person_identify --payload '{
  "first_name": "Jane",
  "last_name": "Doe",
  "company": "example"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `7.7` per pricing unit.
- Billing mode: `post_deduct`.


#### `person-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=peopledatalabs_person_search:{"sql":"SELECT * FROM person WHERE location_country=\"united states\" AND job_title_role=\"engineering\""}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute peopledatalabs_person_search --payload '{
  "sql": "SELECT * FROM person WHERE location_country=\"united states\" AND job_title_role=\"engineering\""
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `3.92` per pricing unit.
- Billing mode: `post_deduct`.


#### `school-clean`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=peopledatalabs_school_clean:{"name":"Example University"}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute peopledatalabs_school_clean --payload '{
  "name": "Example University"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## PredictLeads

*Provider folder: `predictleads` · 24 endpoints*


### PredictLeads — Agent Guidance

## Playbook

# PredictLeads Guidance

Use PredictLeads for company-level signals: hiring, technology detections, news events, financing events, connections, products, GitHub repositories, and similar companies.Prefer direct company endpoints when you already have a domain. They cost one API credit per request and can return up to 1,000 records on list endpoints. Use discovery endpoints only when you need broad search, because they bill per returned result.Avoid follow/unfollow workflows for now. PredictLeads followed-company APIs are designed for webhook delivery and recurring monthly billing, not one-shot agent execution.


### PredictLeads — Endpoints


#### `predictleads-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_company:{"id_or_domain":"{{id_or_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id_or_domain` | `string` | Yes |  | Company’s ID or domain. |

**Direct CLI:**

```
deepline tools execute predictleads_company --payload '{
  "id_or_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-company-connections`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_company_connections:{"company_id_or_domain":"{{company_id_or_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id_or_domain` | `string` | Yes |  | Company’s ID or domain. |

**Direct CLI:**

```
deepline tools execute predictleads_company_connections --payload '{
  "company_id_or_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-company-financing-events`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_company_financing_events:{"company_id_or_domain":"{{company_id_or_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id_or_domain` | `string` | Yes |  | Company’s ID or domain. |

**Direct CLI:**

```
deepline tools execute predictleads_company_financing_events --payload '{
  "company_id_or_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-company-github-repositories`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_company_github_repositories:{"company_id_or_domain":"{{company_id_or_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id_or_domain` | `string` | Yes |  | Company’s ID or domain. |

**Direct CLI:**

```
deepline tools execute predictleads_company_github_repositories --payload '{
  "company_id_or_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-company-job-openings`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_company_job_openings:{"company_id_or_domain":"{{company_id_or_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id_or_domain` | `string` | Yes |  | Company’s ID or domain. |

**Direct CLI:**

```
deepline tools execute predictleads_company_job_openings --payload '{
  "company_id_or_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-company-news-events`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_company_news_events:{"company_id_or_domain":"{{company_id_or_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id_or_domain` | `string` | Yes |  | Company’s ID or domain. |

**Direct CLI:**

```
deepline tools execute predictleads_company_news_events --payload '{
  "company_id_or_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-company-products`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_company_products:{"company_id_or_domain":"{{company_id_or_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id_or_domain` | `string` | Yes |  | Company’s ID or domain. |

**Direct CLI:**

```
deepline tools execute predictleads_company_products --payload '{
  "company_id_or_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-company-similar-companies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_company_similar_companies:{"company_id_or_domain":"{{company_id_or_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id_or_domain` | `string` | Yes |  | Company’s ID or domain. |

**Direct CLI:**

```
deepline tools execute predictleads_company_similar_companies --payload '{
  "company_id_or_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-company-technology-detections`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_company_technology_detections:{"company_id_or_domain":"{{company_id_or_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id_or_domain` | `string` | Yes |  | Company’s ID or domain. |

**Direct CLI:**

```
deepline tools execute predictleads_company_technology_detections --payload '{
  "company_id_or_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-company-website-evolution`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_company_website_evolution:{"company_id_or_domain":"{{company_id_or_domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company_id_or_domain` | `string` | Yes |  | Company’s ID or domain. |

**Direct CLI:**

```
deepline tools execute predictleads_company_website_evolution --payload '{
  "company_id_or_domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-discover-companies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_discover_companies:{"location":"{{location}}","sizes":"{{sizes}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.location` | `string` | Yes |  | The response will include only companies located in the given country name or state name/abbreviation. |
| `payload.sizes` | `array` | Yes |  | An array of one or more valid company sizes. |

**Direct CLI:**

```
deepline tools execute predictleads_discover_companies --payload '{
  "location": "string",
  "sizes": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-discover-financing-events`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_discover_financing_events:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute predictleads_discover_financing_events --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-discover-job-openings`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_discover_job_openings:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute predictleads_discover_job_openings --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-discover-news-events`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_discover_news_events:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute predictleads_discover_news_events --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-discover-portfolio-company-connections`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_discover_portfolio_company_connections:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute predictleads_discover_portfolio_company_connections --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-discover-products`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_discover_products:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute predictleads_discover_products --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-discover-technology-detections`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_discover_technology_detections:{"technology_id_or_fuzzy_name":"{{technology_id_or_fuzzy_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.technology_id_or_fuzzy_name` | `string` | Yes |  | Technology’s ID or fuzzy name. |

**Direct CLI:**

```
deepline tools execute predictleads_discover_technology_detections --payload '{
  "technology_id_or_fuzzy_name": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-extended-technology-detection`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_extended_technology_detection:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | ExtendedTechnologyDetection’s ID. |

**Direct CLI:**

```
deepline tools execute predictleads_extended_technology_detection --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-job-opening`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_job_opening:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | JobOpening’s ID. |

**Direct CLI:**

```
deepline tools execute predictleads_job_opening --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-news-event`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_news_event:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | NewsEvent’s ID. |

**Direct CLI:**

```
deepline tools execute predictleads_news_event --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-product`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_product:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  | Product’s ID. |

**Direct CLI:**

```
deepline tools execute predictleads_product --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-startup-platform-posts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_startup_platform_posts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute predictleads_startup_platform_posts --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-technologies`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_technologies:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute predictleads_technologies --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


#### `predictleads-technology`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=predictleads_technology:{"id_or_fuzzy_name":"{{id_or_fuzzy_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id_or_fuzzy_name` | `string` | Yes |  | Technology’s ID or fuzzy name. |

**Direct CLI:**

```
deepline tools execute predictleads_technology --payload '{
  "id_or_fuzzy_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.56` per pricing unit.
- Billing mode: `post_deduct`.


---

## Prospeo

*Provider folder: `prospeo` · 6 endpoints*


### Prospeo — Agent Guidance

## Playbook

# Prospeo Workflow Guidance

- Use `prospeo_search_person` or `prospeo_search_company` to build list-level candidates, then refine with `prospeo_enrich_person` and `prospeo_enrich_company` for details.
- Use `prospeo_search_person` for prospecting — it supports stable filters (job title with boolean operators, department, seniority, industry, headcount, technology, location). Each page of 25 results costs 1 credit.
- Do not use Prospeo for job-change detection or job-change filtered searches. The live Prospeo job-change filter has schema drift; use FullEnrich for job-change workflows.
- Use `prospeo_search_company` to build account lists by firmographic criteria before drilling into individual contacts.
- Use `prospeo_enrich_person` for full profile enrichment when you need more than just an email (title, company, location). **Mobile phone reveal (`enrich_mobile: true`) costs 10 credits total instead of 1** — only enable it when phone outreach is explicitly requested.
- Use `prospeo_enrich_company` for firmographic enrichment (industry, headcount, technologies, description) from a website, company name, or LinkedIn company URL.

Recommended workflow: `prospeo_search_person` or `prospeo_search_company` to build lists, then `prospeo_enrich_person` for individual contacts.


### Prospeo — Endpoints


#### `prospeo-bulk-enrich-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=prospeo_bulk_enrich_company:{"data":"{{data}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.data` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute prospeo_bulk_enrich_company --payload '{
  "data": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.55` per pricing unit.
- Billing mode: `post_deduct`.


#### `prospeo-enrich-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=prospeo_enrich_company:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute prospeo_enrich_company --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.55` per pricing unit.
- Billing mode: `post_deduct`.


#### `prospeo-enrich-person`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=prospeo_enrich_person:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute prospeo_enrich_person --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.55` per pricing unit.
- Billing mode: `post_deduct`.


#### `prospeo-search-company`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=prospeo_search_company:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute prospeo_search_company --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.55` per pricing unit.
- Billing mode: `post_deduct`.


#### `prospeo-search-person`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=prospeo_search_person:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute prospeo_search_person --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.55` per pricing unit.
- Billing mode: `post_deduct`.


#### `prospeo-search-suggestions`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=prospeo_search_suggestions:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute prospeo_search_suggestions --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Salesforce

*Provider folder: `salesforce` · 19 endpoints*


### Salesforce — Agent Guidance

## Playbook

Use `salesforce_fetch_fields` before writing custom objects or unknown standard objects so you can confirm exact field API names and validation rules.Use `salesforce_list_contacts`, `salesforce_list_leads`, and `salesforce_list_accounts` for incremental CRM reads. They accept `modified_after` for recent changes and `next_records_url` for pagination handoff.Use the object-specific create, update, and delete tools for Accounts, Contacts, Leads, and Opportunities instead of building raw Salesforce payloads yourself. The integration already maps Deepline-friendly field names to Salesforce API names.For custom field write-back on Accounts, Contacts, Leads, and Opportunities, pass `fields` with official Salesforce API names, for example `{ "id": "001...", "fields": { "Deepline_Score__c": 87, "Deepline_Email_Gate__c": true } }`. Values may be strings, numbers, booleans, or `null` to clear a field. If a friendly field and `fields` both target the same Salesforce API name, the explicit `fields` value wins.After a write, use `salesforce_get_record` with the object API name, record ID, and exact scalar field API names to verify custom field values, for example `{ "object": "Account", "id": "001...", "fields": ["Name", "Deepline_Score__c"] }`.


### Salesforce — Endpoints


#### `accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_list_accounts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute salesforce_list_accounts --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `contacts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_list_contacts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute salesforce_list_contacts --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `create-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_create_account:{"name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_create_account --payload '{
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `create-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_create_contact:{"last_name":"{{last_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.last_name` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_create_contact --payload '{
  "last_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `create-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_create_lead:{"last_name":"{{last_name}}","company_name":"{{company_name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.last_name` | `string` | Yes |  |  |
| `payload.company_name` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_create_lead --payload '{
  "last_name": "string",
  "company_name": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `create-opportunity`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_create_opportunity:{"opportunity_name":"{{opportunity_name}}","close_date":"{{close_date}}","stage":"{{stage}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.opportunity_name` | `string` | Yes |  |  |
| `payload.close_date` | `string` | Yes |  |  |
| `payload.stage` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_create_opportunity --payload '{
  "opportunity_name": "string",
  "close_date": "string",
  "stage": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `delete-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_delete_account:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_delete_account --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `delete-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_delete_contact:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_delete_contact --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `delete-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_delete_lead:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_delete_lead --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `delete-opportunity`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_delete_opportunity:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_delete_opportunity --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `fetch-fields`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_fetch_fields:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute salesforce_fetch_fields --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-record`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_get_record:{"object":"{{object}}","id":"{{id}}","fields":"{{fields}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.object` | `string` | Yes |  | Salesforce object API name, for example Account, Contact, Lead, Opportunity, or CustomObject\_\_c. |
| `payload.id` | `string` | Yes |  | Salesforce record ID. |
| `payload.fields` | `array` | Yes |  | Salesforce field API names to read, including custom fields like Deepline\_Score\_\_c. |

**Direct CLI:**

```
deepline tools execute salesforce_get_record --payload '{
  "object": "string",
  "id": "string",
  "fields": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_list_leads:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute salesforce_list_leads --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `opportunities`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_list_opportunities:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute salesforce_list_opportunities --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `tickets`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_list_tickets:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute salesforce_list_tickets --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_update_account:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_update_account --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_update_contact:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_update_contact --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_update_lead:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_update_lead --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-opportunity`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=salesforce_update_opportunity:{"id":"{{id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute salesforce_update_opportunity --payload '{
  "id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Serper

*Provider folder: `serper` · 2 endpoints*


### Serper — Agent Guidance

## Playbook

# Serper Agent Guidance

Use Serper when you need live Google results fast and broad recall matters more than source-specific extraction.Prefer `serper_google_search` for:

- broad web research
- newsy or changing facts
- finding a company, person, or topic before deeper enrichment
- collecting candidate URLs to hand off to extraction tools

Prefer `serper_google_maps_search` for:

- local business discovery
- location-aware company lookups
- phone, address, rating, website, or CID retrieval

Practical guidance:

- Start with Serper before heavier browser or extraction workflows when you do not yet know the right destination URL.
- Treat Serper as a discovery and recall layer, then pass strong hits into structured tools like Firecrawl, Apify, or provider-specific enrichments.
- Use Maps search when the user cares about storefronts, service areas, offices, or other local entities.
- Expect live-search variability. If a result is important, validate it with the returned URL or a follow-up fetch.


### Serper — Endpoints


#### `maps`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=serper_google_maps_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | Maps search query string. |

**Direct CLI:**

```
deepline tools execute serper_google_maps_search --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.04` per pricing unit.
- Billing mode: `post_deduct`.


#### `search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=serper_google_search:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | Search query string. |

**Direct CLI:**

```
deepline tools execute serper_google_search --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


---

## Slack

*Provider folder: `slack` · 6 endpoints*


### Slack — Agent Guidance

## Playbook

# slack\_message\_with\_hitl — Agent Guidance

## What it does

Posts a message to Slack with buttons you define, pauses the workflow, and resumes when a human clicks a button. Use it whenever a workflow needs a human decision before proceeding.

## Mental model

1. You define **what the human sees** (text, optional blocks, + buttons) and **what data each button carries** (emit).
2. The system posts the message, pauses the workflow, and waits.
3. A human clicks a button.
4. The workflow resumes. The next step receives `selected_option` (the button id) and `event_payload` (the button’s emit data merged with Slack metadata).

That’s it. One input, one pause, one output.

## blocks (Block Kit)

You can provide custom Slack Block Kit blocks to control the message layout. When `blocks` is provided, it replaces the default section block generated from `text`. The system always appends:

- A **status context block** (hourglass / checkmark)
- An **actions block** with your buttons (injected with routing metadata)

**Do NOT include `actions` blocks** in your blocks array — interactive buttons are managed via the `buttons` field.When `blocks` is provided, `text` is still required — Slack uses it as the notification preview and accessibility fallback.

### Example with blocks

```
{
  "channel": "C0123456789",
  "text": "New lead: Sarah Chen, VP Marketing at Stripe",
  "blocks": [\
    {\
      "type": "header",\
      "text": { "type": "plain_text", "text": "New Lead for Review" }\
    },\
    {\
      "type": "section",\
      "fields": [\
        { "type": "mrkdwn", "text": "*Name:*\nSarah Chen" },\
        { "type": "mrkdwn", "text": "*Title:*\nVP Marketing" },\
        { "type": "mrkdwn", "text": "*Company:*\nStripe" },\
        { "type": "mrkdwn", "text": "*Email:*\nsarah@stripe.com" }\
      ]\
    },\
    { "type": "divider" }\
  ],
  "buttons": [\
    {\
      "id": "approve",\
      "label": "Approve",\
      "style": "primary",\
      "emit": { "decision": "approved" }\
    },\
    {\
      "id": "reject",\
      "label": "Reject",\
      "style": "danger",\
      "emit": { "decision": "rejected" }\
    }\
  ],
  "allow_edit": true
}
```

### Supported block types

All Slack Block Kit block types are supported: `section`, `header`, `divider`, `context`, `image`, `rich_text`, `actions` (non-HITL only), `input`, `video`, `table`, `markdown`, `file`, `context_actions`, `task_card`, `plan`.See the [Block Kit reference](https://docs.slack.dev/reference/block-kit/blocks) for full specifications.

## Button schema

```
{
  "buttons": [\
    {\
      "id": "hubspot",\
      "label": "Push to HubSpot",\
      "style": "primary",\
      "emit": { "crm": "hubspot", "action": "create_contact" }\
    },\
    {\
      "id": "skip",\
      "label": "Skip",\
      "style": "danger"\
    }\
  ]
}
```

Each button has:

| Field | Required | Description |
| --- | --- | --- |
| `id` | yes | Unique identifier. Returned as `selected_option` when clicked. Cannot be `"edit"` (reserved). |
| `label` | yes | Text shown on the button in Slack. |
| `style` | no | `"primary"` (green) or `"danger"` (red). Omit for neutral gray. |
| `emit` | no | Arbitrary key-value data merged into `event_payload` when this button is clicked. |

Rules:

- 1–5 buttons required.
- Button ids must be unique.
- `"edit"` is a reserved id — use `allow_edit` instead.
- Unknown fields on buttons are rejected (`additionalProperties: false`).

## What the next step receives

When a human clicks a button, the workflow resumes with this output:

```
{
  "resumed": true,
  "timed_out": false,
  "selected_option": "hubspot",
  "event_payload": {
    "crm": "hubspot",
    "action": "create_contact",
    "selected_option": "hubspot",
    "slack_user_id": "U0123456789",
    "slack_channel_id": "C0123456789"
  },
  "final_text": "Lead: Sarah Chen, VP Marketing at Stripe",
  "edited": false,
  "signal": "workflow:run_1:block_2:slack:hitl",
  "actor": {
    "slack_user_id": "U0123456789",
    "slack_channel_id": "C0123456789"
  },
  "message_ref": {
    "channel": "C0123456789",
    "ts": "1712501111.000100"
  }
}
```

- `selected_option` — the `id` of the button that was clicked.
- `event_payload` — the button’s `emit` data merged with Slack signal metadata (`selected_option`, `final_text`, `edited`, `slack_user_id`, `slack_channel_id`, etc). If the button has no `emit`, event\_payload only contains the signal metadata. **Merge precedence:**`emit` keys overwrite signal metadata keys on collision — avoid using `selected_option`, `final_text`, `edited`, `slack_user_id`, or `slack_channel_id` as emit keys.
- `final_text` — the message text at the time of the click (may differ from the original if the human used Edit).
- `edited` — whether the human modified the draft text before clicking.

Branch on `selected_option` in the next workflow step to decide what to do.

## allow\_edit

Set `"allow_edit": true` to add an Edit button that opens a Slack modal where the human can modify the `text` field. Editing does NOT resume the workflow — the human must still click one of your defined buttons after editing.Defaults to `false`. Use it when the `text` field contains a draft the human should be able to revise (e.g., a draft email, a message template).

## post\_interaction

Controls what happens to the Slack message after any button is clicked:

```
{
  "post_interaction": {
    "disable_buttons": true,
    "replace_message": "Decision recorded."
  }
}
```

- `disable_buttons` — remove all buttons from the message after a click (default: true).
- `replace_message` — replace the entire message text with this string.
- `replace_blocks` — replace the entire message with custom Slack blocks (advanced).

This applies to all buttons. There is no per-button post\_interaction.

## Timeout

```
{
  "timeout": "30m",
  "on_timeout": "return_null",
  "timeout_emit": { "reason": "timed_out" }
}
```

- `timeout` — how long to wait for a click (default: 24h). Examples: `"30m"`, `"2h"`, `"24h"`.
- `on_timeout` — `"return_null"` (resume with `timed_out: true`) or `"error"` (fail the workflow step).
- `timeout_emit` — data returned as `event_payload` on timeout.

## Common patterns

### Simple confirmation gate (1 button)

```
{
  "channel": "C0123456789",
  "text": "About to enrich 500 leads. This will cost ~$25.",
  "buttons": [{ "id": "confirm", "label": "Go ahead", "style": "primary" }]
}
```

### Approve / reject with editable draft

```
{
  "channel": "C0123456789",
  "text": "Hi John, we'd love to set up a call to discuss...",
  "buttons": [\
    {\
      "id": "approve",\
      "label": "Send",\
      "style": "primary",\
      "emit": { "decision": "send" }\
    },\
    {\
      "id": "reject",\
      "label": "Drop",\
      "style": "danger",\
      "emit": { "decision": "drop" }\
    }\
  ],
  "allow_edit": true
}
```

### Multi-option CRM routing (5 buttons)

```
{
  "channel": "C0123456789",
  "text": "New lead: Sarah Chen, VP Marketing at Stripe. Which CRM?",
  "buttons": [\
    {\
      "id": "hubspot",\
      "label": "HubSpot",\
      "style": "primary",\
      "emit": { "crm": "hubspot" }\
    },\
    {\
      "id": "salesforce",\
      "label": "Salesforce",\
      "emit": { "crm": "salesforce" }\
    },\
    { "id": "pipedrive", "label": "Pipedrive", "emit": { "crm": "pipedrive" } },\
    { "id": "attio", "label": "Attio", "emit": { "crm": "attio" } },\
    { "id": "skip", "label": "Skip Lead", "style": "danger" }\
  ]
}
```

Next step branches: `if selected_option === "skip"` → end; otherwise `event_payload.crm` tells you which API to call.

## Using HITL in a workflow

A Deepline workflow is a `commands` array. Each command has an `alias`, a `tool`, and a `payload`.**Important — `row` access differs by step type:**

| Step type | Access pattern | Example |
| --- | --- | --- |
| HITL (`slack_message_with_hitl`) | `row.<alias>.selected_option` | `row.qualify.selected_option` |
| JavaScript (`run_javascript`) | `row.<alias>.result.your_field` | `row.draft_email.result.email_body` |

HITL output is stored **flat** — no `.result` wrapper. JS output is wrapped in `{ result, status, meta }`.**Template interpolation** in tool payloads uses `{{...}}` mustache syntax, NOT `${...}` JS template literals:

```
"text": "{{row.draft_email.result.email_body}}"     ← correct
"text": "${row.draft_email.result.email_body}"       ← WRONG, won't interpolate
```

Here’s a 3-step workflow: qualify a lead via HITL, branch on the decision, then post the draft email for approval.

```
{
  "name": "lead_qualify_and_outbound",
  "publish": true,
  "config": {
    "version": 1,
    "commands": [\
      {\
        "alias": "qualify",\
        "tool": "slack_message_with_hitl",\
        "payload": {\
          "channel": "C07V9TZ4MSQ",\
          "text": "New lead: Sarah Chen, VP Marketing at Stripe.\nEmail: sarah@stripe.com\n\nHow should we classify this lead?",\
          "buttons": [\
            {\
              "id": "hot",\
              "label": "Hot Lead",\
              "style": "primary",\
              "emit": { "qualification": "hot" }\
            },\
            {\
              "id": "warm",\
              "label": "Warm Lead",\
              "emit": { "qualification": "warm" }\
            },\
            { "id": "disqualify", "label": "Disqualify", "style": "danger" }\
          ],\
          "timeout": "2h",\
          "on_timeout": "return_null"\
        }\
      },\
      {\
        "alias": "draft_email",\
        "tool": "run_javascript",\
        "payload": {\
          "code": "const q = row.qualify; if (q.timed_out || q.selected_option === 'disqualify') return { skip: true }; const isHot = q.selected_option === 'hot'; const body = isHot ? 'Hi Sarah, I\\'d love to set up a quick demo call this week...' : 'Hi Sarah, we published a playbook you might find useful...'; return { skip: false, email_body: body, qualification: q.selected_option };"\
        }\
      },\
      {\
        "alias": "approve_email",\
        "tool": "slack_message_with_hitl",\
        "payload": {\
          "channel": "C07V9TZ4MSQ",\
          "text": "{{row.draft_email.result.email_body}}",\
          "buttons": [\
            {\
              "id": "send",\
              "label": "Approve & Send",\
              "style": "primary",\
              "emit": { "decision": "send" }\
            },\
            {\
              "id": "drop",\
              "label": "Drop",\
              "style": "danger",\
              "emit": { "decision": "drop" }\
            }\
          ],\
          "allow_edit": true,\
          "timeout": "4h",\
          "on_timeout": "return_null"\
        }\
      }\
    ]
  }
}
```

### How data flows between steps

```
Step 1 (qualify)                    Step 2 (draft_email)                Step 3 (approve_email)
┌──────────────────┐               ┌──────────────────┐               ┌──────────────────┐
│ HITL: 3 buttons  │               │ JS: reads Step 1 │               │ HITL: approve or │
│                  │──output──────▶│ via row.qualify   │──output──────▶│ drop the email   │
│ Waits for human  │               │ (flat, no .result)│               │ allow_edit: true  │
└──────────────────┘               └──────────────────┘               └──────────────────┘
```

- **Step 1** posts to Slack with 3 buttons, pauses, resumes when human clicks.
- **Step 2** is a `run_javascript` step. It reads Step 1’s HITL output at `row.qualify.selected_option` (flat — no `.result`). If the human disqualified the lead or it timed out, it returns `{ skip: true }`. Otherwise it constructs an email draft.
- **Step 3** is another HITL step. The `text` field uses `{{row.draft_email.result.email_body}}` (mustache template, `.result` because it’s reading a JS step). `allow_edit: true` lets the human revise it. After approval, `row.approve_email.final_text` contains the final email body.

### Key patterns

**Reading prior HITL output:**`row.<alias>.selected_option` gives you the button ID. `row.<alias>.event_payload` gives you the button’s emit data. `row.<alias>.final_text` gives you the (possibly edited) text. No `.result` wrapper.**Reading prior JS output:**`row.<alias>.result.your_field` — JS steps wrap output in `{ result, status, meta }`.**Template interpolation in payloads:** Use `{{row.<alias>.field}}` mustache syntax. For JS outputs: `{{row.<alias>.result.field}}`. For HITL outputs: `{{row.<alias>.selected_option}}`.**Branching:** Use a `run_javascript` step between HITL steps to inspect the prior result and decide what to do next. Return a flag like `{ skip: true }` that downstream steps can check.**Passing data forward:** You don’t need to stuff lead data into every button’s `emit`. The workflow engine carries all step outputs in `row`. Any step can read any prior step’s output via the patterns above.


### Slack — Endpoints


#### `slack-get-channel-history`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=slack_get_channel_history:{"channel":"{{channel}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.channel` | `string` | Yes |  | Slack channel ID. |

**Direct CLI:**

```
deepline tools execute slack_get_channel_history --payload '{
  "channel": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `slack-get-user`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=slack_get_user:{"user":"{{user}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.user` | `string` | Yes |  | Slack user ID (for example U0123456789). |

**Direct CLI:**

```
deepline tools execute slack_get_user --payload '{
  "user": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `slack-list-channels`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=slack_list_channels:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute slack_list_channels --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `slack-message-with-hitl`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=slack_message_with_hitl:{"channel":"{{channel}}","text":"{{text}}","buttons":"{{buttons}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.channel` | `string` | Yes |  | Slack channel ID. |
| `payload.text` | `string` | Yes |  | Message text. When blocks are provided, used as notification/accessibility fallback and editable draft in modal. |
| `payload.buttons` | `array` | Yes |  | Buttons to display in the Slack message (1–5). |

**Direct CLI:**

```
deepline tools execute slack_message_with_hitl --payload '{
  "channel": "string",
  "text": "string",
  "buttons": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `slack-post-message`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=slack_post_message:{"channel":"{{channel}}","text":"{{text}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.channel` | `string` | Yes |  | Slack channel ID (for example C0123456789). |
| `payload.text` | `string` | Yes |  | Message text. When blocks are provided, used as notification/accessibility fallback. |

**Direct CLI:**

```
deepline tools execute slack_post_message --payload '{
  "channel": "string",
  "text": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `slack-reply-in-thread`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=slack_reply_in_thread:{"channel":"{{channel}}","thread_ts":"{{thread_ts}}","text":"{{text}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.channel` | `string` | Yes |  | Slack channel ID. |
| `payload.thread_ts` | `string` | Yes |  | Timestamp of the parent message to reply to. |
| `payload.text` | `string` | Yes |  | Thread reply text. When blocks are provided, used as notification/accessibility fallback. |

**Direct CLI:**

```
deepline tools execute slack_reply_in_thread --payload '{
  "channel": "string",
  "thread_ts": "string",
  "text": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Smartlead

*Provider folder: `smartlead` · 113 endpoints*


### Smartlead — Endpoints


#### `add-campaign-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_add_campaign_email_account:{"campaign_id":"{{campaign_id}}","email_account_ids":"{{email_account_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.email_account_ids` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute smartlead_add_campaign_email_account --payload '{
  "campaign_id": "string",
  "email_account_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `add-domain-block-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_add_domain_block_list:{"domain_block_list":"{{domain_block_list}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain_block_list` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute smartlead_add_domain_block_list --payload '{
  "domain_block_list": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `api-request`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_api_request:{"method":"GET","endpoint":"{{endpoint}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.method` | `"GET" | "POST" | "PUT" | "PATCH" | "DELETE"` | Yes |  | HTTP method to call for Smartlead API requests. |
| `payload.endpoint` | `string` | Yes |  | Smartlead API endpoint (for example “/v1/campaigns”). |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.method` | `GET`, `POST`, `PUT`, `PATCH`, `DELETE` |

**Direct CLI:**

```
deepline tools execute smartlead_api_request --payload '{
  "method": "GET",
  "endpoint": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `campaign-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_campaign_stats:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_get_campaign_stats --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `clone-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_clone_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_clone_campaign --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `create-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_create_campaign:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_create_campaign --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `create-client`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_create_client:{"name":"{{name}}","email":"{{email}}","permission":"{{permission}}","password":"{{password}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  |  |
| `payload.email` | `string` | Yes |  |  |
| `payload.permission` | `array` | Yes |  |  |
| `payload.password` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute smartlead_create_client --payload '{
  "name": "string",
  "email": "string",
  "permission": "array",
  "password": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `delete-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_delete_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_delete_campaign --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `delete-lead-by-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_delete_lead_by_campaign:{"campaign_id":"{{campaign_id}}","lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.lead_id` | `string | integer` | Yes |  | Lead identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_delete_lead_by_campaign --payload '{
  "campaign_id": "string",
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `export-campaign-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_export_campaign_leads:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_export_campaign_leads --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `fetch-campaign-sequences`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_fetch_campaign_sequences:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_fetch_campaign_sequences --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `fetch-campaigns-by-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_fetch_campaigns_by_lead:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string | integer` | Yes |  | Lead identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_fetch_campaigns_by_lead --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `fetch-clients`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_fetch_clients:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_fetch_clients --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `fetch-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_fetch_email_account:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_fetch_email_account --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `fetch-email-account-warmup-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_fetch_email_account_warmup_stats:{"email_account_id":"{{email_account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_account_id` | `string | integer` | Yes |  | Email account identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_fetch_email_account_warmup_stats --payload '{
  "email_account_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `fetch-lead-by-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_fetch_lead_by_email:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute smartlead_fetch_lead_by_email --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `fetch-lead-message-history`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_fetch_lead_message_history:{"campaign_id":"{{campaign_id}}","lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.lead_id` | `string | integer` | Yes |  | Lead identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_fetch_lead_message_history --payload '{
  "campaign_id": "string",
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-block-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_block_list:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_block_list --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_get_campaign --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-campaign-analytics`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_campaign_analytics:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_get_campaign_analytics --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-campaign-analytics-by-date`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_campaign_analytics_by_date:{"campaign_id":"{{campaign_id}}","start_date":"{{start_date}}","end_date":"{{end_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.start_date` | `string` | Yes |  | Start date in YYYY-MM-DD format. |
| `payload.end_date` | `string` | Yes |  | End date in YYYY-MM-DD format. |

**Direct CLI:**

```
deepline tools execute smartlead_get_campaign_analytics_by_date --payload '{
  "campaign_id": "string",
  "start_date": "string",
  "end_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-lead-statistics`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_lead_statistics:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_get_lead_statistics --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `get-webhook-publish-summary`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_webhook_publish_summary:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_get_webhook_publish_summary --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `list-campaign-email-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_list_campaign_email_accounts:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_list_campaign_email_accounts --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `list-campaign-leads`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_list_campaign_leads:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_list_campaign_leads --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `list-campaigns`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_list_campaigns:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_list_campaigns --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `list-email-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_list_email_accounts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_list_email_accounts --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `list-lead-categories`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_list_lead_categories:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_list_lead_categories --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `pause-lead-by-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_pause_lead_by_campaign:{"campaign_id":"{{campaign_id}}","lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.lead_id` | `string | integer` | Yes |  | Lead identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_pause_lead_by_campaign --payload '{
  "campaign_id": "string",
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `push-to-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_push_to_campaign:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_push_to_campaign --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `reconnect-failed-email-accounts`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_reconnect_failed_email_accounts:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_reconnect_failed_email_accounts --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `remove-campaign-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_remove_campaign_email_account:{"campaign_id":"{{campaign_id}}","email_account_ids":"{{email_account_ids}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.email_account_ids` | `array` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute smartlead_remove_campaign_email_account --payload '{
  "campaign_id": "string",
  "email_account_ids": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `reply-to-lead-thread`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_reply_to_lead_thread:{"campaign_id":"{{campaign_id}}","email_stats_id":"{{email_stats_id}}","email_body":"{{email_body}}","reply_message_id":"{{reply_message_id}}","reply_email_time":"{{reply_email_time}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.email_stats_id` | `string` | Yes |  |  |
| `payload.email_body` | `string` | Yes |  |  |
| `payload.reply_message_id` | `string` | Yes |  |  |
| `payload.reply_email_time` | `string` | Yes |  | ISO datetime (e.g. 2026-01-15T09:30:00.000Z). |

**Direct CLI:**

```
deepline tools execute smartlead_reply_to_lead_thread --payload '{
  "campaign_id": "string",
  "email_stats_id": "string",
  "email_body": "string",
  "reply_message_id": "string",
  "reply_email_time": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `resume-lead-by-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_resume_lead_by_campaign:{"campaign_id":"{{campaign_id}}","lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.lead_id` | `string | integer` | Yes |  | Lead identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_resume_lead_by_campaign --payload '{
  "campaign_id": "string",
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `save-campaign-sequences`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_save_campaign_sequences:{"campaign_id":"{{campaign_id}}","sequences":"{{sequences}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.sequences` | `array` | Yes |  | List of campaign sequences payload blocks. |

**Direct CLI:**

```
deepline tools execute smartlead_save_campaign_sequences --payload '{
  "campaign_id": "string",
  "sequences": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-add-domain`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_add_domain:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Domain to add (e.g. example.com). |

**Direct CLI:**

```
deepline tools execute smartlead_add_domain --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-add-tag-to-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_add_tag_to_lead:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_add_tag_to_lead --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-assign-email-account-tag`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_assign_email_account_tag:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_assign_email_account_tag --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-assign-tags-to-lead-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_assign_tags_to_lead_list:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_assign_tags_to_lead_list --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-auto-generate-mailboxes`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_auto_generate_mailboxes:{"vendor_id":"{{vendor_id}}","domains":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.vendor_id` | `string | integer` | Yes |  | Smart Senders vendor identifier. |
| `payload.domains` | `record` | Yes |  | Mapping of domain names to mailbox generation requests. |

**Direct CLI:**

```
deepline tools execute smartlead_auto_generate_mailboxes --payload '{
  "vendor_id": "string",
  "domains": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-create-delivery-test`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_create_delivery_test:{"name":"{{name}}","email_accounts":"{{email_accounts}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | Test name. |
| `payload.email_accounts` | `array` | Yes |  | Email account IDs to test. |

**Direct CLI:**

```
deepline tools execute smartlead_create_delivery_test --payload '{
  "name": "string",
  "email_accounts": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-create-email-account-tag`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_create_email_account_tag:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_create_email_account_tag --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-create-inbox-note`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_create_inbox_note:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_create_inbox_note --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-create-inbox-task`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_create_inbox_task:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_create_inbox_task --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-create-lead-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_create_lead_list:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_create_lead_list --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-create-lead-tag`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_create_lead_tag:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_create_lead_tag --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-delete-client`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_delete_client:{"client_id":"{{client_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.client_id` | `string | integer` | Yes |  | Client identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_delete_client --payload '{
  "client_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-delete-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_delete_email_account:{"email_account_id":"{{email_account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_account_id` | `string | integer` | Yes |  | Email account identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_delete_email_account --payload '{
  "email_account_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-forward-inbox-thread`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_forward_inbox_thread:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_forward_inbox_thread --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-all-lead-lists`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_all_lead_lists:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_all_lead_lists --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-all-lead-tags`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_all_lead_tags:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_all_lead_tags --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-campaign-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_campaign_list:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_campaign_list --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-campaign-performance`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_campaign_performance:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_campaign_performance --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-campaign-response-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_campaign_response_stats:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_campaign_response_stats --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-campaign-status-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_campaign_status_stats:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_campaign_status_stats --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-client-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_client_list:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_client_list --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-day-wise-positive-reply`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_day_wise_positive_reply:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_day_wise_positive_reply --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-day-wise-positive-sent-time`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_day_wise_positive_sent_time:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_day_wise_positive_sent_time --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-day-wise-sent-time`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_day_wise_sent_time:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_day_wise_sent_time --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-day-wise-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_day_wise_stats:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_day_wise_stats --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-email-wise-health`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_email_wise_health:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_email_wise_health --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-followup-reply-rate`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_followup_reply_rate:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_followup_reply_rate --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-lead-category-response`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_lead_category_response:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_lead_category_response --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-lead-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_lead_stats:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_lead_stats --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-lead-to-reply-time`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_lead_to_reply_time:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_lead_to_reply_time --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-leads-for-first-reply`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_leads_for_first_reply:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_leads_for_first_reply --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-mailbox-health`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_mailbox_health:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_mailbox_health --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-month-wise-client-count`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_month_wise_client_count:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_month_wise_client_count --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-overview`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_overview:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_overview --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-analytics-provider-performance`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_analytics_provider_performance:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_analytics_provider_performance --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-client-overall-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_client_overall_stats:{"start_date":"{{start_date}}","end_date":"{{end_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.start_date` | `string` | Yes |  | Required start date in YYYY-MM-DD format. |
| `payload.end_date` | `string` | Yes |  | Required end date in YYYY-MM-DD format. |

**Direct CLI:**

```
deepline tools execute smartlead_get_client_overall_stats --payload '{
  "start_date": "string",
  "end_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-delivery-reply-headers`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_delivery_reply_headers:{"test_id":"{{test_id}}","reply_id":"{{reply_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.test_id` | `string | integer` | Yes |  | Spam test identifier. |
| `payload.reply_id` | `string | integer` | Yes |  | Reply identifier within the spam test report. |

**Direct CLI:**

```
deepline tools execute smartlead_get_delivery_reply_headers --payload '{
  "test_id": "string",
  "reply_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-delivery-score`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_delivery_score:{"test_id":"{{test_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.test_id` | `string | integer` | Yes |  | Delivery test identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_get_delivery_score --payload '{
  "test_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-delivery-test`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_delivery_test:{"test_id":"{{test_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.test_id` | `string | integer` | Yes |  | Delivery test identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_get_delivery_test --payload '{
  "test_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-domain-health`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_domain_health:{"start_date":"{{start_date}}","end_date":"{{end_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.start_date` | `string` | Yes |  | Required start date in YYYY-MM-DD format. |
| `payload.end_date` | `string` | Yes |  | Required end date in YYYY-MM-DD format. |

**Direct CLI:**

```
deepline tools execute smartlead_get_domain_health --payload '{
  "start_date": "string",
  "end_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-inbox-important`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_inbox_important:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_inbox_important --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-inbox-messages`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_inbox_messages:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_inbox_messages --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-inbox-reminders`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_inbox_reminders:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_inbox_reminders --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-inbox-reply-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_inbox_reply_status:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_inbox_reply_status --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-inbox-scheduled`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_inbox_scheduled:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_inbox_scheduled --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-inbox-sent`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_inbox_sent:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_inbox_sent --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-inbox-snoozed`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_inbox_snoozed:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_inbox_snoozed --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-provider-ids`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_provider_ids:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_provider_ids --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-team-board-stats`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_team_board_stats:{"start_date":"{{start_date}}","end_date":"{{end_date}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.start_date` | `string` | Yes |  | Required start date in YYYY-MM-DD format. |
| `payload.end_date` | `string` | Yes |  | Required end date in YYYY-MM-DD format. |

**Direct CLI:**

```
deepline tools execute smartlead_get_team_board_stats --payload '{
  "start_date": "string",
  "end_date": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-get-vendors`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_get_vendors:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_get_vendors --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-import-leads-into-lead-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_import_leads_into_lead_list:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_import_leads_into_lead_list --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-list-delivery-tests`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_list_delivery_tests:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_list_delivery_tests --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-list-domains`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_list_domains:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_list_domains --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-manage-client-api-keys`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_manage_client_api_keys:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_manage_client_api_keys --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-mark-inbox-thread-read`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_mark_inbox_thread_read:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_mark_inbox_thread_read --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-push-inbox-to-subsequence`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_push_inbox_to_subsequence:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_push_inbox_to_subsequence --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-push-leads-from-list-to-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_push_leads_from_list_to_campaign:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_push_leads_from_list_to_campaign --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-remove-tag-from-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_remove_tag_from_lead:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_remove_tag_from_lead --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-reply-inbox-thread`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_reply_inbox_thread:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_reply_inbox_thread --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-search-domain`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_search_domain:{"vendor_id":"{{vendor_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.vendor_id` | `string | integer` | Yes |  | Smart Senders vendor identifier. Live requests without vendor\_id return “vendor\_id must be a number”. |

**Direct CLI:**

```
deepline tools execute smartlead_search_domain --payload '{
  "vendor_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-set-inbox-reminder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_set_inbox_reminder:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_set_inbox_reminder --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-suspend-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_suspend_email_account:{"email_account_id":"{{email_account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_account_id` | `string | integer` | Yes |  | Email account identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_suspend_email_account --payload '{
  "email_account_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-unsuspend-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_unsuspend_email_account:{"email_account_id":"{{email_account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_account_id` | `string | integer` | Yes |  | Email account identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_unsuspend_email_account --payload '{
  "email_account_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-update-client`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_update_client:{"client_id":"{{client_id}}","name":"{{name}}","email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.client_id` | `string | integer` | Yes |  | Client identifier. |
| `payload.name` | `string` | Yes |  | Updated client name. |
| `payload.email` | `string` | Yes |  | Updated client email. |

**Direct CLI:**

```
deepline tools execute smartlead_update_client --payload '{
  "client_id": "string",
  "name": "string",
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-update-inbox-category`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_update_inbox_category:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_update_inbox_category --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-update-inbox-revenue`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_update_inbox_revenue:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_update_inbox_revenue --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-update-lead-list`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_update_lead_list:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute smartlead_update_lead_list --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `smartlead-verify-domain`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_verify_domain:{"domain_id":"{{domain_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain_id` | `string | integer` | Yes |  | Domain identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_verify_domain --payload '{
  "domain_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `unsubscribe-lead`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_unsubscribe_lead:{"lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.lead_id` | `string | integer` | Yes |  | Lead identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_unsubscribe_lead --payload '{
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `unsubscribe-lead-by-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_unsubscribe_lead_by_campaign:{"campaign_id":"{{campaign_id}}","lead_id":"{{lead_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.lead_id` | `string | integer` | Yes |  | Lead identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_unsubscribe_lead_by_campaign --payload '{
  "campaign_id": "string",
  "lead_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-campaign-schedule`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_update_campaign_schedule:{"campaign_id":"{{campaign_id}}","timezone":"{{timezone}}","days_of_the_week":"{{days_of_the_week}}","start_hour":"{{start_hour}}","end_hour":"{{end_hour}}","max_new_leads_per_day":"{{max_new_leads_per_day}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.timezone` | `string` | Yes |  | IANA time zone used for the campaign schedule. |
| `payload.days_of_the_week` | `array` | Yes |  | Day numbers for campaign send schedule (0=Sun, 6=Sat). |
| `payload.start_hour` | `string` | Yes |  | Start hour in HH:MM format. |
| `payload.end_hour` | `string` | Yes |  | End hour in HH:MM format. |
| `payload.max_new_leads_per_day` | `integer` | Yes |  | Maximum new leads accepted per day. Required by the live Smartlead API. |

**Direct CLI:**

```
deepline tools execute smartlead_update_campaign_schedule --payload '{
  "campaign_id": "string",
  "timezone": "string",
  "days_of_the_week": "array",
  "start_hour": "string",
  "end_hour": "string",
  "max_new_leads_per_day": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-campaign-settings`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_update_campaign_settings:{"campaign_id":"{{campaign_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |

**Direct CLI:**

```
deepline tools execute smartlead_update_campaign_settings --payload '{
  "campaign_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-campaign-status`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_update_campaign_status:{"campaign_id":"{{campaign_id}}","status":"PAUSED"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.status` | `"PAUSED" | "STOPPED" | "START"` | Yes |  | Campaign status to apply. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.status` | `PAUSED`, `STOPPED`, `START` |

**Direct CLI:**

```
deepline tools execute smartlead_update_campaign_status --payload '{
  "campaign_id": "string",
  "status": "PAUSED"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_update_email_account:{"email_account_id":"{{email_account_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_account_id` | `string | integer` | Yes |  | Email account identifier. |

**Direct CLI:**

```
deepline tools execute smartlead_update_email_account --payload '{
  "email_account_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-email-account-warmup`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_update_email_account_warmup:{"email_account_id":"{{email_account_id}}","warmup_enabled":"{{warmup_enabled}}","total_warmup_per_day":"{{total_warmup_per_day}}","daily_rampup":"{{daily_rampup}}","reply_rate_percentage":"{{reply_rate_percentage}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email_account_id` | `string | integer` | Yes |  | Email account identifier. |
| `payload.warmup_enabled` | `boolean` | Yes |  |  |
| `payload.total_warmup_per_day` | `integer` | Yes |  |  |
| `payload.daily_rampup` | `integer` | Yes |  |  |
| `payload.reply_rate_percentage` | `integer` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute smartlead_update_email_account_warmup --payload '{
  "email_account_id": "string",
  "warmup_enabled": false,
  "total_warmup_per_day": "integer",
  "daily_rampup": "integer",
  "reply_rate_percentage": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-lead-by-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_update_lead_by_campaign:{"campaign_id":"{{campaign_id}}","lead_id":"{{lead_id}}","email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.lead_id` | `string | integer` | Yes |  |  |
| `payload.email` | `string` | Yes |  | Lead email address. |

**Direct CLI:**

```
deepline tools execute smartlead_update_lead_by_campaign --payload '{
  "campaign_id": "string",
  "lead_id": "string",
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-lead-category-by-campaign`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_update_lead_category_by_campaign:{"campaign_id":"{{campaign_id}}","lead_id":"{{lead_id}}","category_id":"{{category_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.campaign_id` | `string | integer` | Yes |  | Smartlead campaign identifier (number or positive numeric string). |
| `payload.lead_id` | `string | integer` | Yes |  |  |
| `payload.category_id` | `integer` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute smartlead_update_lead_category_by_campaign --payload '{
  "campaign_id": "string",
  "lead_id": "string",
  "category_id": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `upsert-email-account`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=smartlead_upsert_email_account:{"id":"{{id}}","from_name":"{{from_name}}","from_email":"{{from_email}}","user_name":"{{user_name}}","password":"{{password}}","smtp_host":"{{smtp_host}}","smtp_port":"{{smtp_port}}","imap_host":"{{imap_host}}","imap_port":"{{imap_port}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.id` | `integer` | Yes |  |  |
| `payload.from_name` | `string` | Yes |  | Display name for outgoing emails. |
| `payload.from_email` | `string` | Yes |  | From email address. |
| `payload.user_name` | `string` | Yes |  | User name for SMTP/IMAP login. |
| `payload.password` | `string` | Yes |  | Password for SMTP/IMAP login. |
| `payload.smtp_host` | `string` | Yes |  | SMTP server hostname. |
| `payload.smtp_port` | `integer` | Yes |  | SMTP server port. |
| `payload.imap_host` | `string` | Yes |  | IMAP server hostname. |
| `payload.imap_port` | `integer` | Yes |  | IMAP server port. |

**Direct CLI:**

```
deepline tools execute smartlead_upsert_email_account --payload '{
  "id": "integer",
  "from_name": "string",
  "from_email": "string",
  "user_name": "string",
  "password": "string",
  "smtp_host": "string",
  "smtp_port": "integer",
  "imap_host": "string",
  "imap_port": "integer"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Snowflake

*Provider folder: `snowflake` · 4 endpoints*


### Snowflake — Agent Guidance

## Playbook

Use Snowflake when the task requires querying an organization’s warehouse data.Prefer `snowflake_run_semantic_query` when a saved semantic layer exists or the user asks for business metrics, dimensions, filters, funnels, or model-defined entities. The semantic query tool renders the stored Snowflake semantic layer into SQL and returns both rows and the rendered SQL for inspection.Use `snowflake_run_query` only when the user provides raw SQL, asks for direct SQL, or the semantic layer does not cover the requested analysis. Leave `write` unset for read-only `SELECT` or `WITH` SQL. Set `write: true` only when the user intentionally wants a single statement that may modify or overwrite customer data. Never expose Snowflake warehouse spend as Deepline spend.


### Snowflake — Endpoints


#### `get-semantic-layer`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=snowflake_get_semantic_layer:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute snowflake_get_semantic_layer --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `run-query`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=snowflake_run_query:{"query":"{{query}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.query` | `string` | Yes |  | SQL query text to execute. Defaults to read-only SELECT or WITH statements; set write: true for a single mutating or DDL statement. |

**Direct CLI:**

```
deepline tools execute snowflake_run_query --payload '{
  "query": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `run-semantic-query`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=snowflake_run_semantic_query:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute snowflake_run_semantic_query --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `update-semantic-layer`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=snowflake_update_semantic_layer:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute snowflake_update_semantic_layer --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## Tamradar

*Provider folder: `tamradar` · 0 endpoints*


### Tamradar — Agent Guidance

## Playbook

# TAMRadar Agent Guidance

Use Deepline monitor tools for customer-facing TAMRadar workflows. TAMRadar provider actions in the integration catalog are internal adapter operations used by monitor deployment, status checks, update polling, webhook diagnostics, and cleanup.Do not call the generated TAMRadar lifecycle operations directly from public play or agent flows. Start from monitor capabilities such as `tamradar.company_radar`, `tamradar.contact_radar`, or `tamradar.industry_radar`, then let Deepline manage upstream radar ids, polling, billing presentation, and event emission.


---

## TheirStack

*Provider folder: `theirstack` · 4 endpoints*


### TheirStack — Agent Guidance

## Playbook

# TheirStack Agent Guidance

## Decision Framework

**Use TheirStack when:**

- Crustdata is unavailable or rate-limited
- You need tech-stack-driven company discovery (TheirStack’s core strength)
- You need job posting data as a hiring intent signal
- You need to enrich a known company’s tech stack

**Do not use TheirStack when:**

- You need to filter by MX/email provider such as Microsoft 365, Office 365, Google Workspace, or Gmail. `theirstack_company_search` has no `company_email_provider` filter. Use `prospeo_search_company` with `company_email_provider` when the MX provider value is supported, or use `company_technology` and post-filter returned `email_tech.mx_provider` evidence.

**Prefer Crustdata when:**

- You need person/contact enrichment (TheirStack has no people data)
- You need PersonDB search

## Operation Sequence

1. **Validate keyword slugs first (free):** Use `theirstack_catalog_keywords` to look up the correct keyword slug (e.g., `react`, `salesforce`, `hubspot`) before running a paid company or job search.
2. **Company discovery:** Use `theirstack_company_search` with `company_keyword_slug_or` or `company_keyword_slug_and`. For precision, use `_and`. For broad reach, use `_or`.
3. **Hiring signals:** Use `theirstack_job_search` to find companies actively hiring for specific roles or technologies. Always provide `posted_at_max_age_days` or a company filter — the API requires at least one.
4. **Tech stack enrichment:** Use `theirstack_technographics` for a single known company — provide `company_domain` when possible (most reliable identifier).
5. **Check credits:** Use `theirstack_credit_balance` before large batch runs.

## Key Filters

- **Keyword slugs** use kebab-case (e.g., `react`, `node-js`, `salesforce`). Use `theirstack_catalog_keywords` to find the exact slug first.
- **Field names differ by endpoint:**`theirstack_company_search` and company-level filters on `theirstack_job_search` use `company_keyword_slug_*`; job text filters on `theirstack_job_search` use `job_keyword_slug_*`; `theirstack_technographics` uses `keyword_slug_or`.
- **No email-provider filter:** do not invent `company_email_provider`, `email_provider`, `mx_provider`, or similar fields for TheirStack company search.
- **Country codes** are ISO 2-letter (e.g., `US`, `GB`, `DE`).
- **Funding stages:**`seed`, `series_a`, `series_b`, `series_c`, `growth`, `ipo`.
- **Job seniority:**`senior`, `junior`, `manager`, `director`, `vp`, `c_level`.

## Cost Awareness

- Company search: 3 credits per company returned. Use `limit: 10` for exploration.
- Job search: 1 credit per job. Safe to use with `limit: 25`.
- Technographics: 3 credits per company lookup (regardless of result count).
- Catalog keywords and credit balance: free.

## Common Mistakes

- Forgetting to provide a time filter OR company filter on job search → API returns error
- Using display names for keywords instead of slugs → no results
- Reusing `keyword_slug_or` from technographics in `theirstack_company_search` → use `company_keyword_slug_or` instead
- Using Prospeo-style MX filters such as `company_email_provider` in `theirstack_company_search` → TheirStack does not support email-provider filtering
- Setting `limit` too high on company search → expensive


### TheirStack — Endpoints


#### `theirstack-catalog-keywords`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=theirstack_catalog_keywords:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute theirstack_catalog_keywords --payload '{}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `theirstack-company-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=theirstack_company_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute theirstack_company_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.05` per pricing unit.
- Billing mode: `post_deduct`.


#### `theirstack-job-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=theirstack_job_search:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute theirstack_job_search --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


#### `theirstack-technographics`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=theirstack_technographics:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute theirstack_technographics --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.05` per pricing unit.
- Billing mode: `post_deduct`.


---

## Trestle

*Provider folder: `trestle` · 2 endpoints*


### Trestle — Agent Guidance

## Playbook

# Trestle Workflow Guidance

## Key patterns

- **Use `trestle_phone_validate` as the final gate after phone enrichment.** It verifies the phone is a real active line AND confirms the name matches the phone owner — catching the two biggest quality problems in enriched mobile data.
- **Always pass `name` alongside `phone`**. Without `name`, `phone.name_match` will not be scored and contact grades degrade.
- **Interpret `contact_grade` as the headline quality signal:** A = high confidence, B = good, C = acceptable, D/F = drop before outbound.
- **`activity_score >= 70`** indicates an active line. Scores `<=30` suggest the line is stale or disconnected.
- **`linetype = "Mobile"`** is the outbound-friendly type. Landlines and VOIP variants are lower value for cold calling.

## When to use

- Post-waterfall validation after `contact_to_phone_waterfall` so you know which phones are safe to call.
- Identity validation where you have a name + phone and want to confirm they belong together.
- Optionally pass `email` and `address` in the same request to get multi-field identity validation in one call.

## When NOT to use

- Don’t use as a phone _discovery_ tool — Trestle validates known numbers, it doesn’t find new ones.
- Don’t use for fraud scoring alone — IPQS is better for fraud/DNC/carrier details.
- Don’t call without a `name` parameter unless you only need the line type.

## Pricing

- Consumption is per-request and post-deduct — you only pay when the call succeeds.
- Trestle’s own per-call pricing is low single cents; Deepline charges 1 credit per successful validation.


### Trestle — Endpoints


#### `trestle-phone-validation`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=trestle_phone_validation:{"phone":"{{phone}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.phone` | `string` | Yes |  | Phone number to validate (E.164 or digits). |

**Direct CLI:**

```
deepline tools execute trestle_phone_validation --payload '{
  "phone": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.21` per pricing unit.
- Billing mode: `post_deduct`.


#### `trestle-real-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=trestle_real_contact:{"phone":"{{phone}}","name":"{{name}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.phone` | `string` | Yes |  | Phone number to validate (E.164 or digits). |
| `payload.name` | `string` | Yes |  | Full name of the person the phone should belong to. Required by the upstream API to score name\_match and contact\_grade. |

**Direct CLI:**

```
deepline tools execute trestle_real_contact --payload '{
  "phone": "string",
  "name": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.42` per pricing unit.
- Billing mode: `post_deduct`.


---

## Upcell

*Provider folder: `upcell` · 2 endpoints*


### Upcell — Agent Guidance

## Playbook

# Upcell

Use Upcell when the workflow needs mobile phone availability checks or matched
mobile phone reveal for a known person.Prefer `upcell_contact_existence` first when the user only needs to know whether
a mobile exists, because it is free. Use `upcell_enrich_contact` when the mobile
number itself is needed.For both actions, provide at least one strong matcher:

- `linkedinUrl`
- `email`
- `personalEmail`
- `firstName`, `lastName`, `title`, and `companyName`
- `firstName`, `lastName`, and either `companyDomain` or `companySocialUrl`

Only mobile workflows are currently enabled. Email, social URL, and usage stats
endpoints are intentionally not exposed until pricing and availability are
confirmed.


### Upcell — Endpoints


#### `upcell-contact-existence`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=upcell_contact_existence:{"contact":{},"existenceFields":"{{existenceFields}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.contact` | `object` | Yes |  | Contact data to match. Supports linkedinUrl, firstName, lastName, title, companyName, companyDomain, companySocialUrl, email, personalEmail. |
| `payload.existenceFields` | `array` | Yes |  | List of fields to check for existence (e.g., mobile). |

**Direct CLI:**

```
deepline tools execute upcell_contact_existence --payload '{
  "contact": {},
  "existenceFields": "array"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `upcell-enrich-contact`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=upcell_enrich_contact:{"fields":"{{fields}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.fields` | `array` | Yes |  | List of fields to enrich (e.g., mobile). |

**Direct CLI:**

```
deepline tools execute upcell_enrich_contact --payload '{
  "fields": "array"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `1.4` per pricing unit.
- Billing mode: `post_deduct`.


---

## Wiza

*Provider folder: `wiza` · 2 endpoints*


### Wiza — Agent Guidance

## Playbook

# Wiza — Agent Guidance

## When to use

Wiza for LinkedIn → email/phone enrichment. Key advantage over ContactOut: **accepts Sales Navigator and LinkedIn Recruiter URLs** in addition to standard LinkedIn profile URLs. Strong for outbound teams with Sales Nav lists.

## Provider characteristics

- **Input required**: LinkedIn URL (including Sales Nav), email, or name+company
- **Geographic coverage**: Global
- **Credit cost**: 1 credit for profile-only, 2 credits for email, 5 credits for phone
- **Enrichment levels**: partial (email, 2 credits), phone (5 credits), full (email+phone, 7 credits), none (profile only, 1 credit)
- **Async**: reveals are queued and processed — the handler polls until finished

## Key operations

### wiza\_reveal\_person

Starts an async enrichment job and polls until finished. Accepts any LinkedIn URL type.

```
{
  "linkedin_url": "https://www.linkedin.com/in/johndoe",
  "enrichment_level": "partial"
}
```

For phones + emails:

```
{
  "linkedin_url": "https://www.linkedin.com/in/johndoe",
  "enrichment_level": "full"
}
```

Name + company fallback:

```
{
  "first_name": "John",
  "last_name": "Doe",
  "company_domain": "acme.com",
  "enrichment_level": "partial"
}
```

### wiza\_search\_prospects

Discover prospects by job title, level, company, industry, location. **Free** — returns masked profiles without contact info. Returns up to 30 results per search.

```
{
  "filters": {
    "job_title": "VP of Sales",
    "job_level": "vp",
    "company_industry": "SaaS",
    "person_location": "United States"
  }
}
```

Typical flow: search → get LinkedIn URLs → feed into `wiza_reveal_person` to enrich.

## Output shape

`wiza_reveal_person` returns a flat object. Email at `email`, phones at `phone_number1`, `mobile_phone1`. Status at `status` (“finished” \| “failed”).`wiza_search_prospects` returns `{ prospects: [...], total: N }`.

## Enrichment levels

| Level | Returns |
| --- | --- |
| `none` | Profile data only (1 credit) |
| `partial` | Emails only (2 credits) |
| `phone` | Phone numbers only (5 credits) |
| `full` | Emails + phones (7 credits) |

## Anti-patterns

- Don’t use `enrichment_level: "full"` on large lists without budgeting phone credits separately
- Don’t skip polling — reveals are async, status starts as “queued”
- Don’t expect more than 30 results from search per call


### Wiza — Endpoints


#### `reveal-person`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=wiza_reveal_person:{}' --json
```

**Required fields:**

No required fields documented.

**Direct CLI:**

```
deepline tools execute wiza_reveal_person --payload '{}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.35` per pricing unit.
- Billing mode: `post_deduct`.


#### `search-prospects`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=wiza_search_prospects:{"filters":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.filters` | `object` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute wiza_search_prospects --payload '{
  "filters": {}
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


---

## WizLeads

*Provider folder: `wizleads` · 6 endpoints*


### WizLeads — Agent Guidance

## Playbook

Use `wizleads_find_email`, `wizleads_verify_email`, and `wizleads_get_company_linkedin_id` for synchronous enrichment.WizLeads allows 10 requests per second across the provider account. Treat that as queue guidance when planning multi-step runs, especially SalesNav scrape + polling workflows.Use `wizleads_scrape_salesnav` for Sales Navigator scraping. By default Deepline waits briefly for the task to finish and returns task detail if ready. If it returns `status: "running"`, keep the returned `task_id` and poll `wizleads_get_task` until status is terminal. The scrape call opens async billing and reconciliation uses task detail counts, so do not charge polling reads separately.Use the public Deepline pricing summary returned by tools metadata when explaining cost. Relevant task flags are `inputs.useAccountless` and, for `salesnav-profile` only, `inputs.enrichEmails`. The public UI mentions Company Followers and Group Members, but the current OpenAPI snapshot does not expose those as API operations.The batch CSV endpoints are registered but disabled until shared multipart upload support exists.


### WizLeads — Endpoints


#### `wizleads-abort-task`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=wizleads_abort_task:{"task_id":"{{task_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.task_id` | `string` | Yes |  | Task ID to abort. |

**Direct CLI:**

```
deepline tools execute wizleads_abort_task --payload '{
  "task_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `wizleads-find-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=wizleads_find_email:{"first_name":"{{first_name}}","website":"{{website}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.first_name` | `string` | Yes |  | First name or full name. |
| `payload.website` | `string` | Yes |  | Company website. |

**Direct CLI:**

```
deepline tools execute wizleads_find_email --payload '{
  "first_name": "string",
  "website": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.04` per pricing unit.
- Billing mode: `post_deduct`.


#### `wizleads-get-company-linkedin-id`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=wizleads_get_company_linkedin_id:{"company":"{{company}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.company` | `string` | Yes |  | Company page URL or username on LinkedIn. |

**Direct CLI:**

```
deepline tools execute wizleads_get_company_linkedin_id --payload '{
  "company": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.04` per pricing unit.
- Billing mode: `post_deduct`.


#### `wizleads-get-task`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=wizleads_get_task:{"task_id":"{{task_id}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.task_id` | `string` | Yes |  |  |

**Direct CLI:**

```
deepline tools execute wizleads_get_task --payload '{
  "task_id": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0` per pricing unit.
- Provider-native pricing may still exist outside Deepline credit billing.
- Billing mode: `no_bill`.


#### `wizleads-scrape-salesnav`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=wizleads_scrape_salesnav:{"name":"{{name}}","type":"salesnav-company","inputs":{}}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.name` | `string` | Yes |  | A name for the task. |
| `payload.type` | `"salesnav-company" | "salesnav-profile"` | Yes |  | Type of scraping task. |
| `payload.inputs` | `object` | Yes |  | Input parameters for the task. |

### Allowed values

| Field | Allowed values |
| --- | --- |
| `payload.type` | `salesnav-company`, `salesnav-profile` |

**Direct CLI:**

```
deepline tools execute wizleads_scrape_salesnav --payload '{
  "name": "string",
  "type": "salesnav-company",
  "inputs": {}
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.04` per pricing unit.
- Billing mode: `post_deduct`.


#### `wizleads-verify-email`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=wizleads_verify_email:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address to verify. |

**Direct CLI:**

```
deepline tools execute wizleads_verify_email --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `fixed` (per call).
- Estimated Deepline credits: `0.02` per pricing unit.
- Billing mode: `post_deduct`.


---

## ZeroBounce

*Provider folder: `zerobounce` · 4 endpoints*


### ZeroBounce — Agent Guidance

## Playbook

# ZeroBounce Workflow Guidance

- Use `zerobounce_validate` as the final email validation gate before any outbound send. Treat `invalid`, `catch-all`, `spamtrap`, `abuse`, and `do_not_mail` statuses as non-send by default.
- Always inspect `sub_status` for granular failure reasons; `status` alone is not sufficient. For example, `do_not_mail` \+ `role_based` may still be acceptable for account-based campaigns whereas `do_not_mail` \+ `disposable` never is.
- Do not use `zerobounce_batch_validate`. ZeroBounce’s batch endpoint currently returns 403 Access denied from Deepline egress, so Deepline disables the batch action and `deepline enrich` runs `zerobounce_validate` as single-email calls instead.
- Use `zerobounce_email_finder` only for person-level lookups. `domain` is required, and at least one of `first_name` or `last_name` must be present.
- Use `zerobounce_domain_search` for domain-only pattern discovery. It preserves the legacy Deepline public contract while mapping to the shared v2 `guessformat` endpoint.
- Use `zerobounce_activity_data` to check recent engagement before re-engaging cold contacts. An `active_in_days` over 365 suggests the address may be abandoned.
- When the `did_you_mean` field is non-empty, consider prompting the user or auto-correcting before sending.


### ZeroBounce — Endpoints


#### `zerobounce-activity-data`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=zerobounce_activity_data:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address to check for activity data. |

**Direct CLI:**

```
deepline tools execute zerobounce_activity_data --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.


#### `zerobounce-domain-search`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=zerobounce_domain_search:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Company domain to inspect for known email patterns. |

**Direct CLI:**

```
deepline tools execute zerobounce_domain_search --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `5.46` per pricing unit.
- Billing mode: `post_deduct`.


#### `zerobounce-email-finder`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=zerobounce_email_finder:{"domain":"{{domain}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.domain` | `string` | Yes |  | Company domain to search. |

**Direct CLI:**

```
deepline tools execute zerobounce_email_finder --payload '{
  "domain": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `5.46` per pricing unit.
- Billing mode: `post_deduct`.


#### `zerobounce-validate`

**Spreadsheet step:**

```
deepline enrich --input leads.csv --output leads.enriched.csv --with 'result=zerobounce_validate:{"email":"{{email}}"}' --json
```

**Required fields:**

| Name | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `payload.email` | `string` | Yes |  | Email address to validate. |

**Direct CLI:**

```
deepline tools execute zerobounce_validate --payload '{
  "email": "string"
}' --json
```

**Cost:**

- Pricing model: `per_result` (per result).
- Estimated Deepline credits: `0.28` per pricing unit.
- Billing mode: `post_deduct`.

---

# PART 5 — Plays & Tasks (multi-step workflows)

*Every play/task: its execution code, the provider waterfall order, parameters, and cost. A task finds one data point; a play chains multiple steps.*


---

## Play: `plays-overview`

# Plays

Deepline plays are multi-step GTM workflows that chain enrichment, AI research, and outreach into complete go-to-market motions. [Tasks](https://deepline.com/docs/plays/tasks-overview) find a single data point. Plays chain multiple enrichment steps with AI reasoning and structured output into full workflows. Each play can be triggered with a single natural language prompt through Claude Code or Codex.

## What is a GTM workflow?

A GTM (go-to-market) workflow is an automated sequence of data enrichment, AI research, and outreach steps that replaces manual sales and marketing work. In Deepline, a “play” is a GTM workflow you run by describing what you want in plain language. No template configuration required.For example, “build a prospect list of VP Engineering contacts at Series B fintech companies with verified emails” chains company search, decision maker lookup, email waterfall, and validation into one run. According to industry benchmarks, waterfall enrichment across multiple providers can improve email coverage by 20-40% compared to any single provider ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)). Deepline’s waterfall across 95+ integrations keeps credits low by stopping at the first valid result.

## How does the 2-pass AI research pattern work?

Many Deepline plays use a 2-pass AI research approach: gather data first, synthesize second. Separating the two passes means each step can be validated independently, and you can inspect raw search results before synthesis runs.

> The GTM engineering principle: “describe the goal and constraints, not the exact provider sequence.” Deepline selects the optimal provider path automatically.

1. **Pass 1 — Search:** Web search (Claude Code’s native web agent, Exa, or Parallel AI depending on the use case) gathers raw data from the web
2. **Pass 2 — Synthesize:**`call_ai` processes the raw data into structured output

## Which plays are available in Deepline?

Deepline currently offers 9 plays, each automating a complete GTM workflow. Each play is triggered with a natural-language prompt that handles step sequencing, provider selection, and data flow automatically — no UI configuration or template setup required.

| Play | What it does |
| --- | --- |
| [Company Research Brief](https://deepline.com/docs/plays/play-company-research-brief) | AI-powered company research with structured output |
| [Competitive Landscape](https://deepline.com/docs/plays/play-competitive-landscape) | Map competitors and analyze positioning |
| [Qualify & Score Leads](https://deepline.com/docs/plays/play-qualify-lead-fit) | Score leads against your ICP with AI classification |
| [Personalize Outreach](https://deepline.com/docs/plays/play-personalize-outreach) | Research contacts and generate personalized emails |
| [Classify Company Signals](https://deepline.com/docs/plays/play-classify-company-signals) | Detect expansion, acquisition, hiring, and regulatory signals |
| [Build Prospect List](https://deepline.com/docs/plays/play-build-prospect-list) | End-to-end: ICP to companies to contacts to verified emails |
| [Account Mapping](https://deepline.com/docs/plays/play-account-mapping) | Map stakeholders at target accounts by department and seniority |
| [Job Change Alerts](https://deepline.com/docs/plays/play-job-change-alerts) | Detect job changes and flag re-engagement opportunities |
| [Ad Intelligence Research](https://deepline.com/docs/plays/play-ad-intelligence) | Analyze competitor ad spend and creative strategy |

## What is the difference between a Play and a Task?

B2B buyers are 57% through the purchase decision before engaging a sales rep (CEB/Gartner), so plays help you engage earlier with richer context. Here’s how tasks and plays differ:

- **Tasks** are atomic: one input, one output, one waterfall. Use them when you need a specific data point (e.g., a work email, a phone number).
- **Plays** are workflows: multiple steps, AI reasoning, structured output. Use them when you need a complete GTM motion (e.g., build a prospect list, generate personalized outreach).

You can always break a play into its component tasks if you want more control over individual steps.

Every play can be piloted on a small subset first. Just add “start with the
first 3 rows” to your prompt.

* * *

**Related pages:** [Tasks Overview](https://deepline.com/docs/plays/tasks-overview) \| [Build Prospect List](https://deepline.com/docs/plays/play-build-prospect-list) \| [Personalize Outreach](https://deepline.com/docs/plays/play-personalize-outreach)

## Frequently Asked Questions

### How do I run a Deepline play?

In Claude Code, type `/deepline-gtm` followed by what you want in plain language. For example: `/deepline-gtm Build a prospect list of VP Engineering contacts at Series B fintech companies with verified emails.` Deepline chains the necessary tasks — company search, decision maker lookup, email waterfall — into a single automated workflow. You can also use the Deepline CLI for programmatic access.

### What is the difference between a Task and a Play in Deepline?

A task finds a single data point (e.g., a work email) using a waterfall of data providers. A play chains multiple tasks with AI reasoning into a complete workflow. The “Build Prospect List” play, for example, chains company enrichment, decision maker search, email waterfall, and validation into one run. Tasks are building blocks; plays are complete workflows.

### Can I customize which steps a play runs?

Yes. You can break any play into its component tasks and run them individually with custom parameters. You can also modify play behavior by describing constraints in your prompt — for example, “build a prospect list but skip phone number lookup” or “only search for contacts in the US.”

### Why run GTM workflows inside the IDE?

Deepline plays run inside Claude Code or Codex — the same environment where you write code, manage data, and build automations. Describe the workflow in plain language and Deepline handles step sequencing, provider selection, and data flow. No context-switching to a separate UI, no template configuration, no column wiring. Your ICP criteria, scoring logic, and output format all live in the prompt, so they version-control naturally alongside your code.

### How much does a Deepline play cost?

Play costs depend on the component tasks and the number of rows processed. Each task within a play follows waterfall pricing (cheapest provider first, stop at first valid result). A typical “Build Prospect List” play might cost 2-5 credits per company (company enrichment + decision maker search + email waterfall for each contact). Always pilot with 3-5 rows to estimate total cost before running a full list.

### How do Deepline plays compare to Clay tables?

Clay tables are spreadsheet-based workflows where you manually add columns, pick providers, and wire up enrichment steps. Each step is a separate template. Deepline plays chain multiple providers and AI passes into a single natural-language prompt — describe the outcome you want and the play runs the full pipeline. Deepline plays use a 2-pass approach for research — search first, synthesize second — so the AI only works with verified data. Plays run inside your IDE or terminal, so the results feed directly into your code, scripts, or CI/CD pipelines without switching between a browser UI and your codebase.


---

## Play: `tasks-overview`

# Tasks

A Deepline task finds, verifies, or enriches one data point per call. Each task is a waterfall lookup across 95+ integrations that tries the cheapest provider first and stops at the first valid result — a work email, a phone number, a LinkedIn URL. You describe what you want in plain language from your IDE or terminal, and Deepline picks the right providers in the right order. No UI configuration, no template selection — just natural language prompts that chain multiple providers automatically.

## What is waterfall enrichment?

Waterfall enrichment tries multiple data providers in sequence, cheapest first, and stops at the first valid result. You only pay for providers that actually ran. According to industry benchmarks, this approach can improve email coverage by 20-40% compared to any single provider ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)). In Deepline, every task uses waterfall enrichment automatically: the system selects which providers to try, in what order, based on your input data and the provider’s coverage for that region or data type.

> “The best enrichment workflow tries free patterns first, then falls through to paid providers only when needed.” — Deepline documentation

B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), so nearly a quarter of your contacts switch jobs every year. Waterfall enrichment compensates by cross-referencing multiple providers — stale data from one provider gets caught by fresh data from another.

## How does Deepline handle provider routing automatically?

Your prompt should describe the goal and constraints, not the exact provider sequence. Deepline handles provider selection, cost optimization, and fallback logic automatically across 95+ integrations. It looks at what data you have and routes accordingly. Most lookups resolve on the first provider for just 0.3 credits, and the cost-aware waterfall can resolve for as little as 0.1 credits when a free email pattern validates.Since Deepline runs inside your IDE or terminal, an AI agent can chain enrichment steps as part of a larger automated pipeline without switching to a separate UI.

## Which tasks are available in Deepline?

Deepline currently offers 7 tasks across 95+ integrations:

| Task | What it does | Providers |
| --- | --- | --- |
| [Find Work Email](https://deepline.com/docs/plays/task-find-work-email) | Verified work emails via cost-aware waterfall (free patterns first, then paid finders) | LeadMagic patterns, Dropleads, Hunter, LeadMagic, Deepline Native, PeopleDataLabs |
| [Find Personal Email](https://deepline.com/docs/plays/task-find-personal-email) | Personal emails for ads and direct outreach | Dropleads, LeadMagic, PeopleDataLabs |
| [Find Mobile Phone](https://deepline.com/docs/plays/task-find-mobile-phone) | Mobile numbers with automatic region routing | enrich\_phone\_finder, Dropleads, LeadMagic |
| [Find LinkedIn Profile](https://deepline.com/docs/plays/task-find-linkedin-profile) | Resolve LinkedIn URLs from names, emails, or companies | Google, Exa, Crustdata, Apollo, PeopleDataLabs, Apify |
| [Find Decision Makers](https://deepline.com/docs/plays/task-find-decision-makers) | Find contacts at companies by role and seniority | Dropleads, Apollo, Icypeas, Prospeo, Crustdata |
| [Enrich Company](https://deepline.com/docs/plays/task-enrich-company) | Full company profile from a domain or name | Apollo, Crustdata, PeopleDataLabs |
| [Email Waterfall with Validation](https://deepline.com/docs/plays/task-email-waterfall-with-validation) | Find + validate emails in one pass, free patterns first | Free patterns + paid fallback + ZeroBounce |

## How do Deepline tasks work step by step?

Every Deepline task follows the same pattern: analyze input, try providers in cost order, return the first valid hit. Most lookups resolve on the first or second provider at 0.3 credits.

1

Analyze your input data

Deepline inspects what columns or fields you provide (name, email, LinkedIn
URL, domain) and selects the right waterfall variant.

2

Try providers in cost order

The cheapest provider runs first. If it returns a valid result, you’re done.
If not, the next provider fires.

3

Return the first valid hit

You only pay for providers that actually ran. Most lookups resolve on the
first or second provider.

Always pilot with 3-5 rows before running a full file. Just say: “Run this on
the first 3 rows of leads.csv.”

* * *

**Related pages:** [Plays Overview](https://deepline.com/docs/plays/plays-overview) \| [Find Work Email](https://deepline.com/docs/plays/task-find-work-email) \| [Email Waterfall with Validation](https://deepline.com/docs/plays/task-email-waterfall-with-validation)

## Frequently Asked Questions

### What is the difference between a Task and a Play in Deepline?

A task is a single-step waterfall lookup that finds one data point (e.g., a work email or phone number). A play is a multi-step workflow that chains multiple tasks together with AI reasoning — finding decision makers at a company, enriching each with emails, and generating personalized outreach. Research on 1.8M sales opportunities shows single-threaded deals close at roughly 5%, while engaging 5+ stakeholders reaches roughly 30% ( [Gong](https://www.gong.io/blog/data-shows-top-reps-dont-just-sell-they-orchestrate-with-ai)). Plays help you build those multi-threaded pipelines. Use tasks when you need a specific data point; use plays when you need a complete GTM motion.

### How much does a typical Deepline task cost?

Most tasks resolve on the first provider for 0.3-0.4 credits ($0.024-$0.032). The cost-aware email waterfall can resolve for as little as 0.1 credits when a free email pattern validates. Worst case across a full provider chain is 7.2 credits, but this is rare.

### Can I run Deepline tasks in bulk from a CSV?

Yes. type `/deepline-gtm find work emails for leads.csv` and Deepline reads your column headers, picks the right waterfall variant, and enriches every row. You can also use the `deepline enrich` CLI command with `--input` and `--output` flags for programmatic bulk runs. Always pilot with 3-5 rows first.

### Why use Deepline for data enrichment?

Deepline runs inside your IDE or terminal, accepts natural language prompts, and chains multiple providers automatically — no UI configuration or template selection. Waterfall routing analyzes your input data, selects the right providers, and tries them in cost order for what industry benchmarks suggest is 20-40% higher hit rates than single-provider lookups ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)). Since everything runs in your coding environment, enrichment plugs directly into scripts, CI/CD pipelines, and agent workflows.

### Do I need to know which data providers to use?

No. Deepline selects providers automatically based on your input data. If you have a name and company domain, it picks the name-and-company waterfall. If you have a LinkedIn URL, it picks the LinkedIn-optimized chain. You describe the goal; Deepline handles provider selection.

### How is Deepline different from Clay for enrichment?

Clay is a spreadsheet UI where you pick templates, configure columns, and wire providers manually. Each enrichment type (work email, phone, LinkedIn) needs a separate template. Deepline works inside your IDE — describe what you need in plain English and it picks the right providers, chains them in a waterfall, and handles validation automatically. Results feed directly into your code, scripts, and CI/CD pipelines without copy-pasting between a browser UI and your terminal.


---

## Play: `decision-tree`

# I Have X, I Want Y

Pick your starting point. Type `/deepline-gtm` in Claude Code followed by what you need.

## Email Finding

| I have… | I want… | Type in Claude Code… | Learn more |
| --- | --- | --- | --- |
| First name + last name + company + domain | Work email | /deepline-gtm Run waterfall email enrichment on leads.csv | [Email Waterfall](https://deepline.com/docs/plays/email-waterfall) |
| LinkedIn URL | Work email | /deepline-gtm Find work emails from LinkedIn URLs in leads.csv | [LinkedIn to Email](https://deepline.com/docs/plays/person-linkedin-to-email-waterfall) |
| Name + domain | Work email | /deepline-gtm Find emails using names and domains in leads.csv | [Name + Domain to Email](https://deepline.com/docs/plays/email-waterfall) |
| First name + last name + domain | Work email | /deepline-gtm Find emails using names and domains in leads.csv | [Name + Domain to Email](https://deepline.com/docs/plays/name-and-company-to-email-waterfall) |
| Work email | Person + company context | /deepline-gtm Enrich these emails with person and company data | [Crustdata Enrich Contact](https://deepline.com/docs/providers/crustdata/guide) |
| Personal email | Work email | /deepline-gtm Find work emails for these personal email addresses | [Email Waterfall](https://deepline.com/docs/plays/email-waterfall) |

## People Finding

| I have… | I want… | Type in Claude Code… | Learn more |
| --- | --- | --- | --- |
| Company name + target role | Matching contacts | /deepline-gtm Find the VP Sales at each company in companies.csv | [Find Decision Makers](https://deepline.com/docs/plays/task-find-decision-makers) |
| Company name only | Company domain | /deepline-gtm Find the website domain for each company in companies.csv | [Enrich Company](https://deepline.com/docs/plays/task-enrich-company) |
| Name + company | LinkedIn URL | /deepline-gtm Find LinkedIn profiles for these contacts | [Find LinkedIn Profile](https://deepline.com/docs/plays/task-find-linkedin-profile) |
| Target account | Full org chart by department | /deepline-gtm Map the org chart at stripe.com — find contacts in Engineering, Sales, and Marketing leadership | [Account Mapping](https://deepline.com/docs/plays/play-account-mapping) |
| ICP criteria (industry, size, stage) | Prospect list with verified emails | /deepline-gtm Build me a prospect list: B2B SaaS companies, 100-500 employees, Series A-C. Find VP Sales with verified emails. | [Build Prospect List](https://deepline.com/docs/plays/play-build-prospect-list) |
| Business type + location (SMB) | Local businesses with contact info | /deepline-gtm Find dentists in Austin TX with phone numbers and websites | [SMB Prospecting](https://deepline.com/docs/plays/play-smb-prospecting) |
| Company name + phone needs | Mobile phone numbers | /deepline-gtm Find mobile phone numbers for the decision makers at these companies | [Find Mobile Phone](https://deepline.com/docs/plays/task-find-mobile-phone) |

## Validation & Scoring

| I have… | I want… | Type in Claude Code… | Learn more |
| --- | --- | --- | --- |
| Email address | Validation (valid/catch-all/invalid) | /deepline-gtm Validate these emails and flag catch-alls | [Email Waterfall](https://deepline.com/docs/plays/task-email-waterfall-with-validation) |
| Enriched lead data | AI qualification score | /deepline-gtm Score these leads against our B2B SaaS ICP, tier them A/B/C | [Qualify & Score Leads](https://deepline.com/docs/plays/play-qualify-lead-fit) |
| Company list | Account prioritization scores | /deepline-gtm Score these accounts by signal density — hiring, fundraising, tech stack fit | [Account Scoring](https://deepline.com/docs/plays/account-scoring) |

## Company Research & Competitive Intel

| I have… | I want… | Type in Claude Code… | Learn more |
| --- | --- | --- | --- |
| Company domain | Full research brief (revenue, headcount, funding, tech stack) | /deepline-gtm Write a research brief for stripe.com | [Company Research Brief](https://deepline.com/docs/plays/play-company-research-brief) |
| Company domain | Competitor landscape matrix | /deepline-gtm Build a competitive landscape for stripe.com — include pricing, features, market position | [Competitive Landscape](https://deepline.com/docs/plays/play-competitive-landscape) |
| Company domain | Ad spend and creative strategy | /deepline-gtm Analyze the Google and Meta ad strategy for hubspot.com | [Ad Intelligence](https://deepline.com/docs/plays/play-ad-intelligence) |
| Company list | Signal classification (expansion, acquisition, hiring, regulatory) | /deepline-gtm Classify these companies by buying signals — flag anyone expanding, hiring, or raising | [Classify Company Signals](https://deepline.com/docs/plays/play-classify-company-signals) |

## Social Listening & Competitive Monitoring

| I have… | I want… | Type in Claude Code… | Learn more |
| --- | --- | --- | --- |
| Competitor LinkedIn page | People engaging with competitor posts + their emails | /deepline-gtm Scrape the last 10 LinkedIn posts from \[competitor\]. Extract everyone who liked or commented. Enrich with emails, filter to VP/Director titles. | [Competitive Mentions Campaign](https://deepline.com/docs/plays/competitive-mentions-campaign) |
| Competitor name | Displacement opportunities (layoffs, outages, pricing changes) | /deepline-gtm Monitor \[competitor\] for displacement signals — layoffs, outages, pricing changes, negative reviews | [Competitive Intel Signals](https://deepline.com/docs/plays/signal-competitive-intel) |
| CRM deal data | Win/loss analysis by competitor | /deepline-gtm Analyze my closed deals in HubSpot from the last 6 months. Break down win rate by competitor and loss stage. | [Win-Loss Signals](https://deepline.com/docs/plays/win-loss-signals) |
| Lost deal list | Re-engagement opportunities | /deepline-gtm Check my closed-lost deals from 6+ months ago. Flag any where the champion changed jobs or the company raised funding. | [Closed-Lost Recovery](https://deepline.com/docs/plays/closed-lost-recovery) |

## Outbound Campaigns

| I have… | I want… | Type in Claude Code… | Learn more |
| --- | --- | --- | --- |
| Contact list with emails | Personalized cold emails | /deepline-gtm Research these contacts and write a personalized cold email for each, referencing something specific about their company | [Personalize Outreach](https://deepline.com/docs/plays/play-personalize-outreach) |
| Contact list + multiple signals | Multi-signal outreach sequence | /deepline-gtm Build a multi-signal outreach sequence: combine job changes, hiring signals, and funding events for each contact | [Multi-Signal Outreach](https://deepline.com/docs/plays/multi-signal-outreach) |
| Qualified leads CSV | Push to email sequencer | /deepline-gtm Add these leads to my Instantly campaign ‘Q2 Outbound’ with personalized first lines | [Email Sequencer Integration](https://deepline.com/docs/features/email-sequencer-integration) |

## CRM Hygiene & Job Changes

| I have… | I want… | Type in Claude Code… | Learn more |
| --- | --- | --- | --- |
| HubSpot/Attio/Salesforce CRM | Stale contact detection + updates | /deepline-gtm Pull my HubSpot contacts and check which ones changed companies. Update their records. | [CRM Cleaning & Job Changes](https://deepline.com/docs/plays/crm-cleaning-job-changes) |
| Contact list | Job change detection | /deepline-gtm Check which of these contacts changed jobs in the last 90 days | [Job Change Detector](https://deepline.com/docs/plays/crm-job-change-detector) |
| Former champions list | Re-engagement when champions move | /deepline-gtm Track these former buyers. When any of them move to a new company, find their new email and flag it. | [Champion Tracking](https://deepline.com/docs/plays/signal-champion-tracking) |
| Target companies | Hiring signals for specific roles | /deepline-gtm Find which target companies are hiring GTM Engineers or RevOps roles | [Hiring Signals](https://deepline.com/docs/plays/signal-hiring) |

## Signals & Triggers

| I have… | I want… | Type in Claude Code… | Learn more |
| --- | --- | --- | --- |
| Target account list | Fundraising alerts | /deepline-gtm Monitor these companies for new funding rounds. When one raises, find the VP Sales and get their email. | [Fundraising Signals](https://deepline.com/docs/plays/signal-fundraising) |
| Target account list | Company news monitoring (M&A, exec hires, regulatory) | /deepline-gtm Check for recent news on these companies — M&A, new executives, regulatory changes | [Company News & Social](https://deepline.com/docs/plays/signal-company-news) |
| Pipeline deals | Deal risk signals | /deepline-gtm Flag deals in my pipeline where the champion went quiet, a competitor entered, or the buying committee changed | [Deal Risk & Combos](https://deepline.com/docs/plays/signal-deal-risk) |

**Pilot mode:** Add “just test on the first 2 rows” to any prompt to run a
quick test before processing the full file.

CLI commands (advanced)

**Email Finding:**

```
# Name + domain → email (waterfall, with cost-aware pattern guessing)
deepline enrich --input leads.csv --output out.csv \
  --with '{"alias":"email","tool":"name_and_domain_to_email_waterfall","payload":{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"{{domain}}"}}'

# LinkedIn URL → email
deepline enrich --input leads.csv --output out.csv \
  --with '{"alias":"email","tool":"person-linkedin-to-email","payload":{"linkedin_url":"{{linkedin_url}}"}}'

# Email → person + company context (single-provider reverse lookup)
deepline enrich --input leads.csv --output out.csv \
  --with '{"alias":"context","tool":"crustdata_enrich_contact","payload":{"email":"{{email}}"}}'
```

**People Finding:**

```
# Company → contacts by role
deepline enrich --input companies.csv --output out.csv \
  --with '{"alias":"contacts","tool":"company_to_contact_by_role_waterfall","payload":{"company_name":"{{company_name}}","domain":"{{domain}}","role":"VP Sales"}}'

# Name + company → LinkedIn URL
deepline enrich --input contacts.csv --output out.csv \
  --with '{"alias":"linkedin","tool":"crustdata_people_search","payload":{"companyDomain":"{{domain}}","titleKeywords":["{{title}}"],"limit":1}}'
```

**Validation:**

```
# Email validation
deepline enrich --input leads.csv --output out.csv \
  --with '{"alias":"valid","tool":"leadmagic_email_validation","payload":{"email":"{{email}}"}}'
```

Add `--rows 0:2` to any command to test on 2 rows first.

## Still not sure?

In Claude Code, type:

```
/deepline-gtm I have a CSV with [columns]. I want [outcome].
```

It will pick the right workflow automatically. It reads the [Deepline skill](https://github.com/getaero-io/gtm-eng-skills) and builds the right command.

## Stats that matter

- **Waterfall enrichment** — industry benchmarks suggest multi-provider waterfalls improve coverage by 20-40% over single-provider lookups ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/))
- **Single-threaded deals** win at ~5%; engaging 5+ stakeholders reaches ~30% — a 6x improvement ( [Gong](https://www.gong.io/blog/data-shows-top-reps-dont-just-sell-they-orchestrate-with-ai), 1.8M opportunities)
- **30%** of B2B contacts switch jobs annually ( [Cognism](https://www.cognism.com/blog/data-decay)) — refresh your CRM quarterly
- **Signal-based outreach** gets 18% response rates vs 3.4% for generic cold email ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide))
- **First seller** to reach out after a trigger event is 5x more likely to win ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide))


---

## Play: `account-scoring`

# Account Scoring & Propensity Modeling

Static ICP checklists break the moment your market shifts. A propensity model built from your actual wins and losses adapts with your data.

## The Play

1

Export wins and losses from your CRM

Pull closed-won and closed-lost opportunities from HubSpot or Salesforce via
MCP. Include company attributes: employee count, industry, revenue, funding
stage, deal size, sales cycle length.

2

Enrich the historical dataset

For each company, pull firmographics via Crustdata or Apollo: headcount,
tech stack, hiring velocity, funding rounds, revenue estimates. The more
signal dimensions, the better the model.

3

Build a baseline scoring model

Ask Claude to analyze the enriched win/loss dataset and identify which
attributes correlate with winning. Weight each dimension by its predictive
power. Start simple — logistic regression or a weighted scorecard.

4

Backtest against held-out deals

Hold out 20-30% of your historical deals. Score them with the model. Check:
does the model rank wins higher than losses? What’s the AUC? If it’s below
0.65, the model needs more signal dimensions or the data is too noisy.

5

Iterate until consistent but not overfit

Tune weights, add or drop dimensions, re-run. Watch for overfitting: if
training accuracy is 95% but holdout accuracy is 55%, you’re memorizing
noise. Target holdout accuracy within 10% of training accuracy.

6

Score new target accounts

Apply the model to your pipeline and target account list. Tier them by
propensity score. Focus outbound on the top tier.

7

Find contacts at top accounts

For high-propensity accounts, use the company-to-contact waterfall to find
decision-makers. Enrich with verified emails.

## Invoke in Claude Code

```
/deepline-gtm Pull all closed-won and closed-lost deals from HubSpot for the last 12 months. Enrich each company with Crustdata firmographics. Build a propensity model that predicts win vs loss based on company attributes. Backtest it on 25% held-out deals. Keep iterating until holdout accuracy is within 10% of training accuracy. Then score the 300 companies in target-accounts.csv and tier them. Find the VP of Sales at every top-tier company and write everything to scored-accounts.csv.
```

## How the Model Works

**Input:** Your CRM’s closed-won and closed-lost deals, enriched with firmographic data.**What the model learns:**

| Signal | Why it matters |
| --- | --- |
| Employee count range | You may win more often at 100-500 than at 5,000+ |
| Industry vertical | SaaS companies may convert 3x vs manufacturing |
| Funding stage | Series B might be your sweet spot |
| Hiring velocity | Companies adding GTM roles signal active buying |
| Tech stack overlap | Using Salesforce + Outreach might correlate with wins |
| Revenue range | $10M-$50M ARR might be where you win on value |
| Geography | Regional patterns in close rates |
| Sales cycle length | Short cycles at similar companies predict fit |

**Output:** A weighted score per account, calibrated against your actual outcomes.

## Avoiding Overfitting

The goal is a model that generalizes, not one that memorizes your training data.

- **Split your data** — 75% train, 25% test. Never tune on the test set.
- **Start with fewer dimensions** — 5-8 signals. Add more only if holdout accuracy improves.
- **Watch the gap** — training accuracy 80%, holdout 72% = fine. Training 95%, holdout 55% = overfit.
- **Re-run quarterly** — your market changes. The model should too. Pull fresh wins/losses and retrain.

## Cost Estimate

- Company enrichment via Crustdata: ~0.4 credits per account
- Contact finding (top tier): ~0.2-0.6 credits per company (via company\_to\_contact waterfall)
- Email waterfall for found contacts: ~0.3 credits per contact
- Model building and backtesting: no enrichment cost (runs on already-enriched data)

After your first outbound campaign, compare response rates by tier. If
mid-tier converts as well as top-tier, retrain — the model’s weights are off.

* * *

[Niche Signal Discovery →](https://deepline.com/docs/plays/win-loss-signals) \| [Closed-Lost Recovery →](https://deepline.com/docs/plays/closed-lost-recovery) \| [I Have X, I Want Y →](https://deepline.com/docs/plays/decision-tree)


---

## Play: `closed-lost-recovery`

# Closed-Lost Recovery

Most teams forget about closed-lost deals. Conditions change though. New budget appears, leadership turns over, a competitor drops the ball. The accounts that said no six months ago might say yes today.

## The Play

1

Export closed-lost deals from CRM

Pull closed-lost opportunities from HubSpot or Salesforce for the last 6-12
months. Include close reason, competitor mentioned, deal size, and cycle
length.

2

Enrich lost accounts with current data

For each company, pull current firmographics via Crustdata or Apollo: recent
funding, hiring signals, leadership changes.

3

Detect re-engagement triggers

Flag accounts where something changed since you lost the deal: new budget
from a funding round, new champion from a leadership change, hiring surge,
or competitor dissatisfaction showing up on review sites.

4

Score and prioritize

Rank targets by signal strength and original deal size. A $100K lost deal
at a company that just raised Series B outranks a $10K loss with no
changes.

5

Route to AEs with context

Push prioritized targets back to the original AE with context: what changed,
why now, and a suggested approach.

## Invoke in Claude Code

```
/deepline-gtm Pull my closed-lost deals from HubSpot for the last 6 months. Enrich each company with Crustdata to check for funding, hiring, or leadership changes. Flag any account where something material changed since we lost the deal. Write results to winback-targets.csv.
```

## Signal Detection Matrix

| Signal | Source | Priority |
| --- | --- | --- |
| New funding round | Crustdata company enrichment | High — new budget |
| Leadership change in buying team | Job change detection | High — new decision-maker |
| Hiring for roles your product covers | Crustdata hiring signals | Medium — growing pain |
| Competitor negative reviews | G2/Capterra via web scraping | Medium — dissatisfaction |
| Company expansion (new offices, M&A) | Crustdata company data | Medium — new scaling needs |
| Time elapsed > 6 months | CRM close date | Low — cooling period passed |

## Cost Estimate

- CRM data pull: Free (via MCP)
- Company enrichment: ~0.4 credits per account (Crustdata)
- Job change detection: only billed on confirmed “Moved” results (2 credits per confirmed changer)
- New email resolution for changers: ~0.3 credits per changer

The best re-engagement window is 2-4 weeks after a trigger event. Enough time
for the new situation to settle, but before they’ve committed to a competitor
again.

* * *

[Account Scoring →](https://deepline.com/docs/plays/account-scoring) \| [Signal Playbooks →](https://deepline.com/docs/plays/signal-playbooks) \| [I Have X, I Want Y →](https://deepline.com/docs/plays/decision-tree)


---

## Play: `company-domain-to-individual`

```
deepline tools execute deepline_native:company_domain_to_individual \
  --payload '{"domain":"<domain>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `domain` | `string` | Yes | Company domain. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**1.27-1.4 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Deepline Native | 0.6 |
| Icypeas | 0.2 |
| Contactout | 1.4 |
| Prospeo | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `company-domain-to-individual-email`

```
deepline tools execute deepline_native:company_domain_to_individual_email \
  --payload '{"domain":"<domain>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `domain` | `string` | Yes | Company domain. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**1.69-2.65 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Deepline Native: Native Candidate Search | 0.6 |
| Icypeas: Icypeas Candidate Search | 0.2 |
| Contactout | 1.4 |
| Prospeo: Prospeo Candidate Search | 0.6 |
| Crustdata V2 | 0.4 |
| Leadmagic: Candidate Pattern First At | 0.1 |
| Leadmagic: Candidate Pattern First Dot Last | 0.1 |
| Leadmagic: Candidate Pattern Firstlast | 0.1 |
| Prospeo: Prospeo Candidate Linkedin Email | 0.6 |
| Deepline Native: Native Candidate Linkedin Email | 1 |
| Wiza: Wiza Candidate Linkedin Email | 0.4 |
| Dropleads: Dropleads Candidate Email | 0.3 |
| Hunter | 0.3 |
| Leadmagic: Leadmagic Candidate Email | 0.3 |
| Prospeo: Prospeo Candidate Domain Email | 0.6 |
| Icypeas: Icypeas Candidate Email | 0.2 |


---

## Play: `company-domain-to-individual-first-last`

```
deepline tools execute deepline_native:company_domain_to_individual_first_last \
  --payload '{"domain":"<domain>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping._\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `domain` | `string` | Yes | Company domain. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**1.27-1.4 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Deepline Native | 0.6 |
| Icypeas | 0.2 |
| Contactout | 1.4 |
| Prospeo | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `company-domain-to-individual-linkedin-url`

```
deepline tools execute deepline_native:company_domain_to_individual_linkedin_url \
  --payload '{"domain":"<domain>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping._\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `domain` | `string` | Yes | Company domain. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**1.27-1.4 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Deepline Native | 0.6 |
| Icypeas | 0.2 |
| Contactout | 1.4 |
| Prospeo | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `company-domain-to-linkedin-employees`

```
deepline tools execute deepline_native:company_domain_to_linkedin_employees \
  --payload '{"domain":"<domain>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Apify: Linkedin Company Url From Domain

Apify: Linkedin Company Employees \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |  |
| --- | --- | --- | --- | --- | --- |
| `domain` | `string` | Yes | Company domain to resolve into a LinkedIn company page before employee scraping. |  |  |
| `max_items` | `integer` | No | Maximum employee rows to request from the LinkedIn company employees actor. Increase and rerun to continue farther into the company employee list. |  |  |
| `profile_depth` | \`string | string | string\` | No | Apify actor profile depth. Use short for cheap discovery, full for richer profile rows, and full\_with\_email only when contact fields are worth the extra cost. |

## Cost

**0.7-1.06 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Apify: Linkedin Company Url From Domain | 0.5 |
| Apify: Linkedin Company Employees | 0.5 |


---

## Play: `company-linkedin-url-to-individual`

```
deepline tools execute deepline_native:company_linkedin_url_to_individual \
  --payload '{"linkedin_company_url":"<linkedin_company_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping._\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `linkedin_company_url` | `string` | Yes | LinkedIn company URL. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**2.33-2.53 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Deepline Native: Native Candidate Search | 0.6 |
| Prospeo: Company Linkedin Url To Individual Resolved Domain Prospeo | 0.6 |
| Deepline Native: Native Candidate Search | 0.6 |
| Icypeas | 0.2 |
| Contactout | 1.4 |
| Prospeo: Prospeo Candidate Search | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `company-linkedin-url-to-individual-email`

```
deepline tools execute deepline_native:company_linkedin_url_to_individual_email \
  --payload '{"linkedin_company_url":"<linkedin_company_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `linkedin_company_url` | `string` | Yes | LinkedIn company URL. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**2.75-3.78 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Deepline Native: Native Candidate Search | 0.6 |
| Prospeo: Company Linkedin Url To Individual Email Resolved Domain Prospeo | 0.6 |
| Deepline Native: Native Candidate Search | 0.6 |
| Icypeas: Icypeas Candidate Search | 0.2 |
| Contactout | 1.4 |
| Prospeo: Prospeo Candidate Search | 0.6 |
| Crustdata V2 | 0.4 |
| Leadmagic: Candidate Pattern First At | 0.1 |
| Leadmagic: Candidate Pattern First Dot Last | 0.1 |
| Leadmagic: Candidate Pattern Firstlast | 0.1 |
| Prospeo: Prospeo Candidate Linkedin Email | 0.6 |
| Deepline Native: Native Candidate Linkedin Email | 1 |
| Wiza: Wiza Candidate Linkedin Email | 0.4 |
| Dropleads: Dropleads Candidate Email | 0.3 |
| Hunter | 0.3 |
| Leadmagic: Leadmagic Candidate Email | 0.3 |
| Prospeo: Prospeo Candidate Domain Email | 0.6 |
| Icypeas: Icypeas Candidate Email | 0.2 |


---

## Play: `company-linkedin-url-to-individual-first-last`

```
deepline tools execute deepline_native:company_linkedin_url_to_individual_first_last \
  --payload '{"linkedin_company_url":"<linkedin_company_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping._\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `linkedin_company_url` | `string` | Yes | LinkedIn company URL. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**2.33-2.53 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Deepline Native: Native Candidate Search | 0.6 |
| Prospeo: Company Linkedin Url To Individual First Last Resolved Domain Prospeo | 0.6 |
| Deepline Native: Native Candidate Search | 0.6 |
| Icypeas | 0.2 |
| Contactout | 1.4 |
| Prospeo: Prospeo Candidate Search | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `company-linkedin-url-to-individual-linkedin-url`

```
deepline tools execute deepline_native:company_linkedin_url_to_individual_linkedin_url \
  --payload '{"linkedin_company_url":"<linkedin_company_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `linkedin_company_url` | `string` | Yes | LinkedIn company URL. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**2.33-2.53 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Deepline Native: Native Candidate Search | 0.6 |
| Prospeo: Company Linkedin Url To Individual Linkedin Url Resolved Domain Prospeo | 0.6 |
| Deepline Native: Native Candidate Search | 0.6 |
| Icypeas | 0.2 |
| Contactout | 1.4 |
| Prospeo: Prospeo Candidate Search | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `company-name-to-individual`

```
deepline tools execute deepline_native:company_name_to_individual \
  --payload '{"company_name":"<company_name>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `company_name` | `string` | Yes | Company name. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**1.57-1.96 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Exa | 0.1 |
| Serper | 0 |
| Prospeo: Company Name To Individual Resolved Domain Prospeo | 0.6 |
| Deepline Native | 0.6 |
| Icypeas | 0.2 |
| Contactout | 1.4 |
| Prospeo: Prospeo Candidate Search | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `company-name-to-individual-email`

```
deepline tools execute deepline_native:company_name_to_individual_email \
  --payload '{"company_name":"<company_name>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `company_name` | `string` | Yes | Company name. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**1.99-3.21 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Exa | 0.1 |
| Serper | 0 |
| Prospeo: Company Name To Individual Email Resolved Domain Prospeo | 0.6 |
| Deepline Native: Native Candidate Search | 0.6 |
| Icypeas: Icypeas Candidate Search | 0.2 |
| Contactout | 1.4 |
| Prospeo: Prospeo Candidate Search | 0.6 |
| Crustdata V2 | 0.4 |
| Leadmagic: Candidate Pattern First At | 0.1 |
| Leadmagic: Candidate Pattern First Dot Last | 0.1 |
| Leadmagic: Candidate Pattern Firstlast | 0.1 |
| Prospeo: Prospeo Candidate Linkedin Email | 0.6 |
| Deepline Native: Native Candidate Linkedin Email | 1 |
| Wiza: Wiza Candidate Linkedin Email | 0.4 |
| Dropleads: Dropleads Candidate Email | 0.3 |
| Hunter | 0.3 |
| Leadmagic: Leadmagic Candidate Email | 0.3 |
| Prospeo: Prospeo Candidate Domain Email | 0.6 |
| Icypeas: Icypeas Candidate Email | 0.2 |


---

## Play: `company-name-to-individual-first-last`

```
deepline tools execute deepline_native:company_name_to_individual_first_last \
  --payload '{"company_name":"<company_name>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping.

Exa

Serper

Prospeo: Company Name To Individual First Last Resolved Domain Prospeo

Dropleads \*

Deepline Native \*

Wiza \*

Icypeas \*

Contactout \*

Prospeo: Prospeo Candidate Search \*

Crustdata V2 \*

Local \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `company_name` | `string` | Yes | Company name. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**1.57-1.96 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Exa | 0.1 |
| Serper | 0 |
| Prospeo: Company Name To Individual First Last Resolved Domain Prospeo | 0.6 |
| Deepline Native | 0.6 |
| Icypeas | 0.2 |
| Contactout | 1.4 |
| Prospeo: Prospeo Candidate Search | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `company-name-to-individual-linkedin-url`

```
deepline tools execute deepline_native:company_name_to_individual_linkedin_url \
  --payload '{"company_name":"<company_name>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 4 results are required before stopping.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `company_name` | `string` | Yes | Company name. |  |
| `roles` | \`array | string\` | No |  |
| `seniority` | \`array | string\` | No |  |
| `limit` | `integer` | No |  |  |

## Cost

**1.57-1.96 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Exa | 0.1 |
| Serper | 0 |
| Prospeo: Company Name To Individual Linkedin Url Resolved Domain Prospeo | 0.6 |
| Deepline Native | 0.6 |
| Icypeas | 0.2 |
| Contactout | 1.4 |
| Prospeo: Prospeo Candidate Search | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `company-to-contact-by-role-waterfall`

```
deepline tools execute deepline_native:company_to_contact_by_role_waterfall \
  --payload '{"roles":"<roles>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped. At least 2 results are required before stopping._\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |  |
| --- | --- | --- | --- | --- |
| `company_name` | `string` | No | Company name for human context and provider fallbacks. |  |
| `domain` | `string` | No | Company domain for contact lookup (for example acme.com). |  |
| `linkedin_company_url` | `string` | No | Optional LinkedIn company URL in /company/ format. Use this when you have the LinkedIn company page but not the domain. |  |
| `roles` | \`array | string\` | Yes | Primary persona intent for contact lookup. Accepts one or more role strings as an array or comma-separated string. Prefer exact, real-world title tokens when you know them. |
| `seniority` | \`array | string\` | No | Broad seniority intent for cross-provider matching. Use portable values like C-Level, Founder, VP, Head, Director, Manager, Senior, Entry, or Intern. |
| `limit` | `integer` | No | Optional result cap per waterfall step (1-100). Use `1` for the single best candidate, or `3` when you want a few exact-title options to review. |  |

## Cost

**1.35-0.57 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Deepline Native: Deepline Native Search Contact By Domain | 0.6 |
| Deepline Native: Deepline Native Search Contact By Linkedin Company | 0.6 |
| Icypeas | 0.2 |
| Prospeo | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `competitive-mentions-campaign`

# Competitive Mentions Campaign

Someone who comments on a competitor’s LinkedIn post is telling you they care about the problem you solve. Find those people, get their work emails, filter to your ICP, and reach out while the topic is fresh.

## Watch It in Action

Getting Started with Social Listening Strategies 📊

[Getting Started with Social Listening Strategies 📊](https://www.loom.com/share/b8cb53a288b9486286e45e611bf95555 "Getting Started with Social Listening Strategies 📊")

55 views

Copy link

[Open video in Loom](https://www.loom.com/share/b8cb53a288b9486286e45e611bf95555)

0

1.2×

5 min⚡️6 min 29 sec5 min 11 sec4 min 19 sec3 min 27 sec3 min 3 sec2 min 35 sec2 min 4 sec

[Getting Started with Social Listening Strategies 📊](https://www.loom.com/share/b8cb53a288b9486286e45e611bf95555 "Getting Started with Social Listening Strategies 📊")

55 views

Copy link

[Open video in Loom](https://www.loom.com/share/b8cb53a288b9486286e45e611bf95555)

0

1.2×

5 min⚡️6 min 29 sec5 min 11 sec4 min 19 sec3 min 27 sec3 min 3 sec2 min 35 sec2 min 4 sec

- - 😂

      joy

      1

  - 😍

    love

    2

  - 😮

    wow

    3

  - 🙌

    yay

    4

  - 👍

    up

    5

  - 👎

    down

    6

More reactions

7

✋

## Frequently Used

  - 💯

  - 🎉

  - ✅

  - ❌

  - 👀

  - ✨

  - 🚀

  - ➕

  - 🙏

  - 🔥

  - 😆

  - 🤔

  - 😱

  - 👋

  - 🌈

  - ❤️

  - 👏

  - 🐞

## Smileys & Emotion

## People & Body

## Animals & Nature

## Food & Drink

## Activities

## Travel & Places

## Objects

## Symbols

## Flags

Make frequently used emojis my defaultOn activating the toggle button, the emojis will update in the background

Comment

Comment

C

## The Play

1

Identify competitor content

Find LinkedIn posts from competitor company pages, executives, and industry influencers that discuss problems your product solves. Focus on posts with high engagement (50+ reactions).

2

Extract engagers

Pull everyone who liked, commented on, or shared those posts.

3

Enrich contacts

Run the engager list through Deepline’s waterfall enrichment to get verified work emails, current job titles, company info, and LinkedIn profiles.

4

Score and filter

Filter to your ICP: target titles (VP Sales, Head of RevOps, GTM Engineer), company size, industry. Score by engagement depth — commenters > likers.

5

Route to sequences

Push qualified leads into your outreach tool with personalized context: the specific post they engaged with and what they said.

## Invoke in Claude Code

```
/deepline-gtm Scrape the last 10 LinkedIn posts from [competitor company page]. Extract everyone who liked or commented. Enrich them with waterfall email enrichment and filter to VP/Director titles at companies with 50-500 employees. Write results to competitor-leads.csv.
```

## How It Works Under the Hood

1. **Apify LinkedIn scraper** extracts post engagers (likes, comments, shares)
2. **Crustdata or LinkedIn enrichment** resolves full profiles from engagement data
3. **Email waterfall** (Dropleads → Hunter → LeadMagic → others) finds verified work emails
4. **AI scoring** filters to ICP and ranks by engagement signal strength

## Scoring Framework

| Signal | Weight | Rationale |
| --- | --- | --- |
| Commented on competitor post | High | Active evaluation, wrote something specific |
| Shared competitor content | High | Amplifying — deep interest in the topic |
| Liked competitor post | Medium | Passive interest, but still in-category |
| Engaged with multiple posts | Very High | Repeated behavior = active buying cycle |
| Title matches ICP | Required | Filter gate — skip non-decision-makers |

## What to Expect

Results vary by competitor post engagement volume and your ICP filters. A high-engagement post (100+ reactions) yields more raw engagers; tight ICP filters reduce the final list but improve quality.

## Cost Estimate

- LinkedIn scraping: ~$1-10 per 1,000 profiles via Apify (varies by actor and data depth)
- Email waterfall: ~0.3 credits per contact (waterfall stops at first match)
- Total depends on post engagement volume and enrichment hit rate

Reference the specific post in your outreach. “Saw your comment on \[topic\]”
beats a generic pitch because it’s true.

## Variations

- **Influencer audience mining** — Target posts from industry thought leaders, not just competitors
- **Event attendee enrichment** — Same pattern but with conference/webinar attendee lists
- **Community member targeting** — Extract active members from Slack communities or Discord servers

* * *

[Email Waterfall →](https://deepline.com/docs/features/waterfall-enrichment) \| [Multi-Signal Outreach →](https://deepline.com/docs/plays/multi-signal-outreach) \| [I Have X, I Want Y →](https://deepline.com/docs/plays/decision-tree)


---

## Play: `crm-cleaning-job-changes`

# CRM Cleaning & Job Change Detection

30% of B2B contacts switch jobs every year ( [Cognism](https://www.cognism.com/blog/data-decay)). If you haven’t cleaned your CRM in a year, a third of it is wrong.Job change detection is only billed when a confirmed “Moved” result is returned. 2 credits per confirmed changer, plus ~0.3 credits for new email resolution. No change detected = no charge.

## One Prompt

```
/deepline-gtm Connect to my HubSpot, pull all contacts updated more than 90 days ago, check which ones changed jobs, flag invalid emails, and update the CRM with current data. Tag job changers for re-engagement.
```

That prompt runs this pipeline:

1

Pull stale contacts from CRM

Connects to HubSpot via MCP. Filters contacts not updated in 90+ days, the
ones most likely to have stale data.

2

Detect job changes

Cross-references each contact’s current employer against their CRM record
using LinkedIn-backed enrichment. Flags mismatches as job changers.

3

Validate existing emails

Runs email verification on all contacts. Marks bounced/invalid emails for
cleanup. Catch-all domains get flagged for manual review.

4

Enrich job changers

For confirmed job changers: resolves new company, new title, new work email
via waterfall enrichment.

5

Update CRM

Pushes updates back to HubSpot via MCP: new company/title/email for
changers, “email\_invalid” tag for bounces, “needs\_review” for catch-alls.

6

Tag for re-engagement

Job changers who moved to ICP-fit companies get tagged for re-engagement
sequences. Former champions get routed to their original AE.

## What You Get Back

| Category | What happens | Cost |
| --- | --- | --- |
| Job changers | New company, title, email enriched | 2 credits (detection) + ~0.3 credits (new email) |
| Invalid emails | Tagged in CRM for removal/replacement | ~0.1 credits per validation |
| Catch-all domains | Flagged for manual review | ~0.1 credits per check |
| No change detected | No charge | Free |

Job change detection only bills when a confirmed changer is returned. No change = no charge.

## Variations

Quarterly CRM hygiene (recommended):

```
/deepline-gtm Run a full CRM health check — pull all HubSpot contacts, validate emails, detect job changes, and give me a summary of how many contacts are stale, bounced, or changed jobs.
```

Pre-campaign cleaning:

```
/deepline-gtm I'm about to run an outbound campaign to these 500 contacts in campaign-list.csv. Validate all emails first, check for job changes, and remove anyone who's no longer at the company.
```

Champion tracking:

```
/deepline-gtm Pull all closed-won contacts from the last 2 years in HubSpot. Check which ones changed companies. For anyone who moved to an ICP-fit company, find their new email and flag them for the re-engagement sequence.
```

## Scheduling

Run this quarterly at minimum. Set up a recurring prompt:

```
/deepline-gtm Every quarter, check my HubSpot for job changes and email validity. Summarize what changed and update the CRM.
```

Uses the **[deepline-gtm](https://github.com/getaero-io/gtm-eng-skills/tree/main/skills/deepline-gtm)** skill.

10,000 contacts, uncleaned for a year = ~3,000 stale records. Job change
detection charges 2 credits per confirmed “Moved” result, plus ~0.3 per
changer for new email resolution. No change = no charge. BYOK users pay
providers directly at $0 on Deepline.

* * *

[Signal Playbooks →](https://deepline.com/docs/plays/signal-playbooks) \| [Niche Signal Discovery →](https://deepline.com/docs/plays/win-loss-signals) \| [Closed-Lost Recovery →](https://deepline.com/docs/plays/closed-lost-recovery) \| [I Have X, I Want Y →](https://deepline.com/docs/plays/decision-tree)


---

## Play: `crm-job-change-detector`

# Find CRM Contacts Who Switched Jobs

Detect which contacts in your CRM changed companies, find their new work emails, and add them to re-engagement campaigns. Past champions win at **37% vs 19%** for cold outbound — nearly 2x the win rate ( [Champify](https://www.champify.io/resources/the-impact-of-tracking-job-changes-value-report), 230K champions analyzed).

## When to use

- Quarterly CRM hygiene — catch contacts who left their companies
- Win-back campaigns — re-engage churned contacts at their new companies
- Pipeline reactivation — find champions who moved to new accounts

## How to run it

Uses the **[deepline-gtm](https://github.com/getaero-io/gtm-eng-skills/tree/main/skills/deepline-gtm)** skill. Connect your CRM via MCP ( [HubSpot](https://developers.hubspot.com/mcp), [Salesforce](https://developer.salesforce.com/docs/platform/hosted-mcp-servers/guide/hosted-mcp-servers-overview.html)), then type in Claude Code:

```
/deepline-gtm Pull my HubSpot contacts, check which ones changed companies, find their new work emails, update HubSpot with the changes, and add job changers to my Instantly re-engagement campaign
```

CLI commands (advanced)

```
# 1. Enrich current company info (pilot on 2 rows)
deepline enrich --input crm_contacts.csv --output job_changes.csv --rows 0:2 \
  --with '{"alias":"current_info","tool":"crustdata_enrich_contact","payload":{"email":"{{email}}"}}'

# 2. Flag changers (compare enriched vs stored company)
deepline enrich --csv job_changes.csv --in-place \
  --with '{"alias":"job_changed","tool":"run_javascript","payload":{"code":"const current = (row.current_info_company || \"\").toLowerCase(); const stored = (row.company || \"\").toLowerCase(); return current && stored && current !== stored ? \"YES\" : \"NO\""}}'

# 3. Find new emails for job changers
deepline enrich --csv job_changes.csv --in-place \
  --with '{"alias":"new_email","tool":"name_and_domain_to_email_waterfall","payload":{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"{{current_info_domain}}"}}'
```

## Key rules

- **Pilot first.** Always test on 2 rows before running 1000+ contacts.
- **LinkedIn URLs improve accuracy.** If your CRM has them, use `crustdata_person_enrichment` with LinkedIn URL for best results.
- **Data freshness.** PDL/Crustdata data can be 1-3 months stale — cross-reference with LinkedIn for high-value contacts.
- **Batch size.** For large lists (1000+), process in batches of 200-500.

## Why this matters

- **30%** of B2B contacts switch jobs every year ( [Cognism](https://www.cognism.com/blog/data-decay))
- At **2.1% monthly** data decay, a 10K-contact CRM loses ~210 valid emails per month ( [SMARTe](https://www.landbase.com/blog/data-decay-rate-statistics))
- Past champions convert at **3x** the rate of cold prospects ( [UserGems](https://www.usergems.com/blog/signal-based-marketing))
- The first seller to contact after a trigger event is **5x** more likely to win ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide))

## Common pitfalls

| Pitfall | Fix |
| --- | --- |
| Company name mismatch due to casing (“Ramp” vs “RAMP”) | Normalize to lowercase before comparing |
| False positives from acquisitions/rebrands | Check if domain also changed, not just company name |
| Missing LinkedIn URLs reduces accuracy | Enrich LinkedIn URLs first if not in CRM |

View source on [GitHub](https://github.com/getaero-io/gtm-eng-skills/tree/main/skills/deepline-gtm)


---

## Play: `email-waterfall`

# Email Waterfall

Try multiple email providers in cost order, stop at the first valid hit. You only pay for the one that works. Industry benchmarks suggest this gets **20-40% more emails** than using any single provider alone ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)).

## Pick Your Starting Point

Deepline automatically selects the right waterfall based on what data you have:

| You have | Waterfall used | Typical cost |
| --- | --- | --- |
| Name + domain | `name_and_domain_to_email_waterfall` | 0.1-0.4 credits |
| LinkedIn URL + name | `person-linkedin-to-email` | 0.3-0.4 credits |
| LinkedIn URL or name + domain (personal) | `personal_email_waterfall` | 0.3-0.5 credits |
| Company + role (find contacts) | `company_to_contact_by_role_waterfall` | 0.2-0.6 credits |

## How It Works

Each waterfall follows the same pattern:

1

Try cheapest provider first

Deepline sends the request to the lowest-cost provider (e.g., Dropleads at
~0.3 credits).

2

Validate the result

If the provider returns an email, Deepline checks it’s a real address — not
a catch-all or bounce.

3

Fall through on miss

If the provider fails or returns invalid, Deepline moves to the next
cheapest provider automatically.

4

Stop on first valid hit

The moment a valid email is found, the waterfall stops. You’re only billed
for providers that ran.

## Single Lookup

In Claude Code, type:

```
/deepline-gtm Find the work email for the VP of Engineering at Ramp (ramp.com)
```

CLI command

```
deepline tools execute name_and_domain_to_email_waterfall \
  --payload '{"first_name":"Jane","last_name":"Smith","domain":"ramp.com"}'
```

## Bulk Enrichment (CSV)

```
/deepline-gtm Run waterfall email enrichment on leads.csv
```

Deepline reads the column headers and picks the right waterfall variant. If your CSV has `first_name`, `last_name`, and `domain` — it uses the `name_and_domain_to_email_waterfall` (which tries free patterns first). If it has `linkedin_url` — it uses the LinkedIn waterfall.

CLI command

```
deepline enrich --input leads.csv --output enriched.csv \
  --with '{"alias":"email","tool":"name_and_domain_to_email_waterfall","payload":{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"{{domain}}"}}'
```

## Provider Order

Providers are tried in sequence. The exact chain depends on the waterfall variant, but a typical order:

1. **Dropleads** (0.3 cr)
2. **Hunter** (0.3 cr)
3. **LeadMagic** (0.4 cr)
4. **Deepline Native** (1.0 cr)
5. **Crustdata** (1.2 cr)
6. **PeopleDataLabs** (4.0 cr)

**BYOK mode:** If you bring your own API keys, waterfall lookups are free —
you pay providers directly at their rates. In managed mode, a typical lookup
costs 0.3-0.4 credits (first provider hit). Worst-case if all providers fire:
up to 7.2 credits.

## Pilot Mode

Always test before running a full file:

```
/deepline-gtm Run waterfall email enrichment on leads.csv — just the first 3 rows
```

Or with the CLI: `--rows 0:2`

## When to Use Each Variant

- **Name + company + domain** — Most common. Use when you have a lead list with company info.
- **LinkedIn URL** — Use when scraping from LinkedIn or when LinkedIn is your primary identifier.
- **Name + domain only** — Use when you don’t have the company name, just the website.
- **Email → context** — Reverse enrichment. You have emails, you want person/company data back.
- **Company + role** — Prospecting. Find contacts at target companies by job title.

* * *

See the auto-generated pages below for exact provider chains, parameters, and per-variant cost breakdowns.


---

## Play: `engagers-to-icp-qualification`

```
deepline tools execute deepline_native:engagers_to_icp_qualification \
  --payload '{"first_name":"<first_name>","last_name":"<last_name>","position":"<position>","icp_description":"<icp_description>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Deeplineagent

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `first_name` | `string` | Yes | Contact first name |
| `last_name` | `string` | Yes | Contact last name |
| `position` | `string` | Yes | Current position/headline |
| `company` | `string` | No | Company name if known |
| `icp_description` | `string` | Yes | ICP criteria for tier1/tier2/tier3 classification |

## Cost

**1-1.5 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Deeplineagent | 1.5 |


---

## Play: `linkedin-post-to-engagers`

```
deepline tools execute deepline_native:linkedin_post_to_engagers \
  --payload '{"post_url":"<post_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `post_url` | `string` | Yes | LinkedIn post URL |
| `max_items` | `number` | No | Max engagers to return |

## Cost

**0.35-0.53 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Apify | 0.5 |


---

## Play: `multi-signal-outreach`

# Multi-Signal Outreach

A job change alone is noise. A job change + the company hiring for your category + their team commenting on competitor posts = someone building a new stack. That’s worth an email.Stacking signals like this gets **18% response rates** vs 3.4% for cold email ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide)).

## The Play

1

Define your signal sources

Choose 3-5 signals to monitor: job changes, hiring posts, funding rounds, competitor engagement, technology adoption, or intent data.

2

Collect signals per account

Run enrichment across all signal sources for your target account list. Each signal adds a column to your dataset.

3

Stack and score

Count signals per account. Weight by strength: job change of a champion (10 pts) > generic hiring signal (3 pts). Rank by composite score.

4

Generate personalized sequences

For top-scored accounts, generate 3-step outreach sequences that reference their specific signals. “Congrats on the Series B — noticed you’re hiring RevOps, which usually means \[pain point\].”

5

Route to sequences

Push to your outreach tool (Instantly, Outreach, Apollo sequences) with signal context in custom fields.

## Invoke in Claude Code

```
/deepline-gtm For each company in target-accounts.csv: check for recent funding, hiring signals, and job changes among my HubSpot contacts. Stack the signals, score each account, and generate a personalized 3-step email sequence for the top 20. Write to outreach-ready.csv.
```

## Signal Stacking Framework

| Signals Detected | Priority | Action |
| --- | --- | --- |
| 3+ signals | P0 — Immediate | Personal email from AE within 48h |
| 2 signals | P1 — This week | Automated sequence with signal-specific personalization |
| 1 signal | P2 — Queue | Add to nurture sequence, monitor for additional signals |
| 0 signals | Skip | Don’t waste outreach credits on cold accounts |

## Signal Combinations That Convert

| Combo | Why It Works |
| --- | --- |
| Job change + funding round | New leader with fresh budget — classic buying window |
| Competitor engagement + hiring | Evaluating alternatives while scaling the team |
| Tech stack change + job posting | Rebuilding infrastructure, need new tools |
| Funding + multiple job posts | Rapid growth = urgent tool needs |

## Cost Estimate

- TAM building & web scraping: Included in Claude Code (AI-powered via Claude)
- Email enrichment: ~0.3 credits per contact (waterfall, first provider hit)
- Company enrichment: ~0.4 credits per account (via Crustdata)
- Signal-specific enrichment costs vary by source and provider

Speed matters more than perfection. A good-enough personalized email today
beats a perfect one next week.

* * *

[Competitive Mentions →](https://deepline.com/docs/plays/competitive-mentions-campaign) \| [Signal Playbooks →](https://deepline.com/docs/plays/signal-playbooks) \| [I Have X, I Want Y →](https://deepline.com/docs/plays/decision-tree)


---

## Play: `name-and-domain-to-email-waterfall`

```
deepline tools execute deepline_native:name_and_domain_to_email_waterfall \
  --payload '{"first_name":"<first_name>","last_name":"<last_name>","domain":"<domain>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Leadmagic: Pattern First At \*

Leadmagic: Pattern Flast \*

Leadmagic: Pattern First Dot Last \*

Leadmagic: Pattern Firstl \*

Leadmagic: Pattern Firstlast \*

Hunter \*

Leadmagic: Leadmagic Email \*

Datagma \*

Findymail \*

Icypeas \*

Prospeo \*

Deepline Native \*

Lusha \*

Contactout \*

Dropleads \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `first_name` | `string` | Yes | Contact first name. |
| `last_name` | `string` | Yes | Contact last name. |
| `domain` | `string` | Yes | Company domain (for example acme.com). |
| `company_name` | `string` | No | Optional company name hint accepted for compatibility with older email waterfall payloads. |
| `linkedin_url` | `string` | No | Optional standard LinkedIn profile URL. Enables LinkedIn-native fallback enrichment when available. |

## Cost

**0.85-1.85 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Leadmagic: Pattern First At | 0.1 |
| Leadmagic: Pattern Flast | 0.1 |
| Leadmagic: Pattern First Dot Last | 0.1 |
| Leadmagic: Pattern Firstl | 0.1 |
| Leadmagic: Pattern Firstlast | 0.1 |
| Hunter | 0.3 |
| Leadmagic: Leadmagic Email | 0.3 |
| Datagma | 0.2 |
| Findymail | 0.3 |
| Icypeas | 0.2 |
| Prospeo | 0.6 |
| Deepline Native | 1 |
| Lusha | 0.7 |
| Contactout | 1.4 |
| Dropleads | 0.3 |


---

## Play: `person-linkedin-sales-nav-url-to-email`

```
deepline tools execute deepline_native:person_linkedin_sales_nav_url_to_email \
  --payload '{"linkedin_sales_nav_url":"<linkedin_sales_nav_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_sales_nav_url` | `string` | Yes | LinkedIn Sales Navigator person URL. |

## Cost

**0.7-1.4 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Prospeo | 0.6 |
| Wiza: Wiza Linkedin Email | 0.4 |
| Contactout | 1.4 |
| Wiza: Wiza Personal | 0.4 |
| Leadmagic | 0.7 |
| Datagma | 0.3 |


---

## Play: `person-linkedin-sales-nav-url-to-job-history`

```
deepline tools execute deepline_native:person_linkedin_sales_nav_url_to_job_history \
  --payload '{"linkedin_sales_nav_url":"<linkedin_sales_nav_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped._\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_sales_nav_url` | `string` | Yes | LinkedIn Sales Navigator person URL. |

## Cost

**0.33-0.57 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Linkedin Scraper | 0 |
| Datagma | 0.3 |
| Prospeo | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `person-linkedin-sales-nav-url-to-personal-email`

```
deepline tools execute deepline_native:person_linkedin_sales_nav_url_to_personal_email \
  --payload '{"linkedin_sales_nav_url":"<linkedin_sales_nav_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_sales_nav_url` | `string` | Yes | LinkedIn Sales Navigator person URL. |

## Cost

**1.89-1.4 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Contactout | 1.4 |
| Wiza | 0.4 |
| Leadmagic | 0.7 |
| Datagma | 0.3 |


---

## Play: `person-linkedin-sales-nav-url-to-phone`

```
deepline tools execute deepline_native:person_linkedin_sales_nav_url_to_phone \
  --payload '{"linkedin_sales_nav_url":"<linkedin_sales_nav_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped._\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_sales_nav_url` | `string` | Yes | LinkedIn Sales Navigator person URL. |

## Cost

**2.74-4.91 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Leadmagic | 1.7 |
| Wiza | 0.4 |
| Deepline Native | 4.9 |


---

## Play: `person-linkedin-sales-nav-url-to-work-email`

```
deepline tools execute deepline_native:person_linkedin_sales_nav_url_to_work_email \
  --payload '{"linkedin_sales_nav_url":"<linkedin_sales_nav_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Local

Prospeo \*

Wiza \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_sales_nav_url` | `string` | Yes | LinkedIn Sales Navigator person URL. |

## Cost

**0.59-0.57 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Prospeo | 0.6 |
| Wiza | 0.4 |


---

## Play: `person-linkedin-to-email-waterfall`

```
deepline tools execute deepline_native:person_linkedin_to_email_waterfall \
  --payload '{"linkedin_url":"<linkedin_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Prospeo \*

Deepline Native \*

Lusha \*

Contactout \*

Forager \*

Leadmagic \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_url` | `string` | Yes | Standard LinkedIn profile URL in /in/ format. |

## Cost

**0.8-1.68 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Prospeo | 0.6 |
| Deepline Native | 1 |
| Lusha | 0.7 |
| Contactout | 1.4 |
| Forager | 1.6 |
| Leadmagic | 1.7 |


---

## Play: `person-linkedin-url-context-to-email`

```
deepline tools execute deepline_native:person_linkedin_url_context_to_email \
  --payload '{"linkedin_url":"<linkedin_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Leadmagic: Pattern First At \*

Leadmagic: Pattern Flast \*

Leadmagic: Pattern First Dot Last \*

Leadmagic: Pattern Firstl \*

Leadmagic: Pattern Firstlast \*

Dropleads \*

Hunter \*

Leadmagic: Leadmagic Email \*

Icypeas \*

Prospeo: Prospeo Linkedin Email \*

Wiza: Wiza Linkedin Email \*

Wiza: Wiza Identity Email

Prospeo: Prospeo Identity Email

Contactout \*

Wiza: Wiza Personal \*

Leadmagic: Leadmagic Personal \*

Datagma \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_url` | `string` | Yes | LinkedIn person profile URL. |
| `first_name` | `string` | No |  |
| `last_name` | `string` | No |  |
| `domain` | `string` | No |  |
| `company_name` | `string` | No |  |
| `email` | `string` | No |  |

## Cost

**0.91-1.85 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Leadmagic: Pattern First At | 0.1 |
| Leadmagic: Pattern Flast | 0.1 |
| Leadmagic: Pattern First Dot Last | 0.1 |
| Leadmagic: Pattern Firstl | 0.1 |
| Leadmagic: Pattern Firstlast | 0.1 |
| Dropleads | 0.3 |
| Hunter | 0.3 |
| Leadmagic: Leadmagic Email | 0.3 |
| Icypeas | 0.2 |
| Prospeo: Prospeo Linkedin Email | 0.6 |
| Wiza: Wiza Linkedin Email | 0.4 |
| Wiza: Wiza Identity Email | 0.4 |
| Prospeo: Prospeo Identity Email | 0.6 |
| Contactout | 1.4 |
| Wiza: Wiza Personal | 0.4 |
| Leadmagic: Leadmagic Personal | 0.7 |
| Datagma | 0.3 |


---

## Play: `person-linkedin-url-context-to-job-history`

```
deepline tools execute deepline_native:person_linkedin_url_context_to_job_history \
  --payload '{"linkedin_url":"<linkedin_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Linkedin Scraper \*

Datagma \*

Prospeo \*

Crustdata V2: Crustdata V2 Linkedin Job History \*

Crustdata V2: Crustdata V2 Email Job History \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_url` | `string` | Yes | LinkedIn person profile URL. |
| `first_name` | `string` | No |  |
| `last_name` | `string` | No |  |
| `domain` | `string` | No |  |
| `company_name` | `string` | No |  |
| `email` | `string` | No |  |

## Cost

**0.33-0.57 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Linkedin Scraper | 0 |
| Datagma | 0.3 |
| Prospeo | 0.6 |
| Crustdata V2: Crustdata V2 Linkedin Job History | 0.4 |
| Crustdata V2: Crustdata V2 Email Job History | 0.4 |


---

## Play: `person-linkedin-url-context-to-personal-email`

```
deepline tools execute deepline_native:person_linkedin_url_context_to_personal_email \
  --payload '{"linkedin_url":"<linkedin_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Contactout \*

Wiza \*

Leadmagic \*

Datagma \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_url` | `string` | Yes | LinkedIn person profile URL. |
| `first_name` | `string` | No |  |
| `last_name` | `string` | No |  |
| `domain` | `string` | No |  |
| `company_name` | `string` | No |  |
| `email` | `string` | No |  |

## Cost

**1.89-1.4 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Contactout | 1.4 |
| Wiza | 0.4 |
| Leadmagic | 0.7 |
| Datagma | 0.3 |


---

## Play: `person-linkedin-url-context-to-phone`

```
deepline tools execute deepline_native:person_linkedin_url_context_to_phone \
  --payload '{"linkedin_url":"<linkedin_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped._\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_url` | `string` | Yes | LinkedIn person profile URL. |
| `first_name` | `string` | No |  |
| `last_name` | `string` | No |  |
| `domain` | `string` | No |  |
| `company_name` | `string` | No |  |
| `email` | `string` | No |  |

## Cost

**2.74-4.91 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Leadmagic | 1.7 |
| Wiza | 0.4 |
| Deepline Native | 4.9 |


---

## Play: `person-linkedin-url-context-to-work-email`

```
deepline tools execute deepline_native:person_linkedin_url_context_to_work_email \
  --payload '{"linkedin_url":"<linkedin_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Leadmagic: Pattern First At \*

Leadmagic: Pattern Flast \*

Leadmagic: Pattern First Dot Last \*

Leadmagic: Pattern Firstl \*

Leadmagic: Pattern Firstlast \*

Dropleads \*

Hunter \*

Leadmagic: Leadmagic Email \*

Icypeas \*

Prospeo: Prospeo Linkedin Email \*

Wiza: Wiza Linkedin Email \*

Wiza: Wiza Identity Email

Prospeo: Prospeo Identity Email

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_url` | `string` | Yes | LinkedIn person profile URL. |
| `first_name` | `string` | No |  |
| `last_name` | `string` | No |  |
| `domain` | `string` | No |  |
| `company_name` | `string` | No |  |
| `email` | `string` | No |  |

## Cost

**0.91-1.01 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Leadmagic: Pattern First At | 0.1 |
| Leadmagic: Pattern Flast | 0.1 |
| Leadmagic: Pattern First Dot Last | 0.1 |
| Leadmagic: Pattern Firstl | 0.1 |
| Leadmagic: Pattern Firstlast | 0.1 |
| Dropleads | 0.3 |
| Hunter | 0.3 |
| Leadmagic: Leadmagic Email | 0.3 |
| Icypeas | 0.2 |
| Prospeo: Prospeo Linkedin Email | 0.6 |
| Wiza: Wiza Linkedin Email | 0.4 |
| Wiza: Wiza Identity Email | 0.4 |
| Prospeo: Prospeo Identity Email | 0.6 |


---

## Play: `person-linkedin-url-to-job-history`

```
deepline tools execute deepline_native:person_linkedin_url_to_job_history \
  --payload '{"linkedin_url":"<linkedin_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Linkedin Scraper \*

Datagma \*

Prospeo \*

Crustdata V2 \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_url` | `string` | Yes | LinkedIn person profile URL. |

## Cost

**0.33-0.57 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Linkedin Scraper | 0 |
| Datagma | 0.3 |
| Prospeo | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `person-linkedin-url-to-personal-email`

```
deepline tools execute deepline_native:person_linkedin_url_to_personal_email \
  --payload '{"linkedin_url":"<linkedin_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped._\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_url` | `string` | Yes | LinkedIn person profile URL. |

## Cost

**1.89-1.4 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Contactout | 1.4 |
| Wiza | 0.4 |
| Leadmagic | 0.7 |
| Datagma | 0.3 |


---

## Play: `person-linkedin-url-to-phone`

```
deepline tools execute deepline_native:person_linkedin_url_to_phone \
  --payload '{"linkedin_url":"<linkedin_url>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Leadmagic \*

Wiza \*

Deepline Native \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `linkedin_url` | `string` | Yes | LinkedIn person profile URL. |

## Cost

**2.74-4.91 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Leadmagic | 1.7 |
| Wiza | 0.4 |
| Deepline Native | 4.9 |


---

## Play: `person-name-company-to-email`

```
deepline tools execute deepline_native:person_name_company_to_email \
  --payload '{"first_name":"<first_name>","last_name":"<last_name>","company_name":"<company_name>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Exa

Serper

Prospeo: Person Name Company To Email Resolved Domain Prospeo

Leadmagic: Pattern First At \*

Leadmagic: Pattern Flast \*

Leadmagic: Pattern First Dot Last \*

Leadmagic: Pattern Firstl \*

Leadmagic: Pattern Firstlast \*

Dropleads \*

Hunter \*

Leadmagic: Leadmagic Email \*

Icypeas \*

Prospeo: Prospeo Linkedin Email \*

Wiza: Wiza Linkedin Email \*

Wiza: Wiza Identity Email

Prospeo: Prospeo Identity Email

Contactout \*

Wiza: Wiza Personal \*

Leadmagic: Leadmagic Personal \*

Datagma \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `first_name` | `string` | Yes | Contact first name. |
| `last_name` | `string` | Yes | Contact last name. |
| `company_name` | `string` | Yes | Company name. |
| `linkedin_url` | `string` | No | Optional LinkedIn profile URL context. |

## Cost

**1.21-2.41 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Exa | 0.1 |
| Serper | 0 |
| Prospeo: Person Name Company To Email Resolved Domain Prospeo | 0.6 |
| Leadmagic: Pattern First At | 0.1 |
| Leadmagic: Pattern Flast | 0.1 |
| Leadmagic: Pattern First Dot Last | 0.1 |
| Leadmagic: Pattern Firstl | 0.1 |
| Leadmagic: Pattern Firstlast | 0.1 |
| Dropleads | 0.3 |
| Hunter | 0.3 |
| Leadmagic: Leadmagic Email | 0.3 |
| Icypeas | 0.2 |
| Prospeo: Prospeo Linkedin Email | 0.6 |
| Wiza: Wiza Linkedin Email | 0.4 |
| Wiza: Wiza Identity Email | 0.4 |
| Prospeo: Prospeo Identity Email | 0.6 |
| Contactout | 1.4 |
| Wiza: Wiza Personal | 0.4 |
| Leadmagic: Leadmagic Personal | 0.7 |
| Datagma | 0.3 |


---

## Play: `person-name-company-to-job-history`

```
deepline tools execute deepline_native:person_name_company_to_job_history \
  --payload '{"first_name":"<first_name>","last_name":"<last_name>","company_name":"<company_name>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped._\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `first_name` | `string` | Yes | Contact first name. |
| `last_name` | `string` | Yes | Contact last name. |
| `company_name` | `string` | Yes | Company name. |
| `linkedin_url` | `string` | No | Optional LinkedIn profile URL context. |

## Cost

**0.79-1.15 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Serper: Serper Linkedin Search | 0 |
| Prospeo: Prospeo Enrich Person Linkedin | 0.6 |
| Serper: Serper Linkedin Identity Validate | 0 |
| Linkedin Scraper | 0 |
| Datagma | 0.3 |
| Prospeo: Prospeo Job History | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `person-name-company-to-linkedin-url`

```
deepline tools execute deepline_native:person_name_company_to_linkedin_url \
  --payload '{"first_name":"<first_name>","last_name":"<last_name>","company_name":"<company_name>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `first_name` | `string` | Yes | Contact first name. |
| `last_name` | `string` | Yes | Contact last name. |
| `company_name` | `string` | Yes | Company name. |
| `linkedin_url` | `string` | No | Optional LinkedIn profile URL context. |

## Cost

**0.46-0.59 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Serper: Serper Linkedin Search | 0 |
| Prospeo | 0.6 |
| Serper: Serper Linkedin Identity Validate | 0 |


---

## Play: `person-name-company-to-personal-email`

```
deepline tools execute deepline_native:person_name_company_to_personal_email \
  --payload '{"first_name":"<first_name>","last_name":"<last_name>","company_name":"<company_name>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `first_name` | `string` | Yes | Contact first name. |
| `last_name` | `string` | Yes | Contact last name. |
| `company_name` | `string` | Yes | Company name. |
| `linkedin_url` | `string` | No | Optional LinkedIn profile URL context. |

## Cost

**2.35-1.98 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Serper: Serper Linkedin Search | 0 |
| Prospeo | 0.6 |
| Serper: Serper Linkedin Identity Validate | 0 |
| Contactout | 1.4 |
| Wiza | 0.4 |
| Leadmagic | 0.7 |
| Datagma | 0.3 |


---

## Play: `person-name-company-to-work-email`

```
deepline tools execute deepline_native:person_name_company_to_work_email \
  --payload '{"first_name":"<first_name>","last_name":"<last_name>","company_name":"<company_name>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `first_name` | `string` | Yes | Contact first name. |
| `last_name` | `string` | Yes | Contact last name. |
| `company_name` | `string` | Yes | Company name. |
| `linkedin_url` | `string` | No | Optional LinkedIn profile URL context. |

## Cost

**1.21-1.57 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Exa | 0.1 |
| Serper | 0 |
| Prospeo: Person Name Company To Work Email Resolved Domain Prospeo | 0.6 |
| Leadmagic: Pattern First At | 0.1 |
| Leadmagic: Pattern Flast | 0.1 |
| Leadmagic: Pattern First Dot Last | 0.1 |
| Leadmagic: Pattern Firstl | 0.1 |
| Leadmagic: Pattern Firstlast | 0.1 |
| Dropleads | 0.3 |
| Hunter | 0.3 |
| Leadmagic: Leadmagic Email | 0.3 |
| Icypeas | 0.2 |
| Prospeo: Prospeo Linkedin Email | 0.6 |
| Wiza: Wiza Linkedin Email | 0.4 |
| Wiza: Wiza Identity Email | 0.4 |
| Prospeo: Prospeo Identity Email | 0.6 |


---

## Play: `person-name-domain-to-job-history`

```
deepline tools execute deepline_native:person_name_domain_to_job_history \
  --payload '{"first_name":"<first_name>","last_name":"<last_name>","domain":"<domain>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `first_name` | `string` | Yes | Contact first name. |
| `last_name` | `string` | Yes | Contact last name. |
| `domain` | `string` | Yes | Company domain. |
| `company_name` | `string` | No | Optional company name context. |
| `email` | `string` | No | Optional known email context. |
| `linkedin_url` | `string` | No | Optional LinkedIn profile URL context. |

## Cost

**0.79-1.78 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Serper: Serper Linkedin Search Domain | 0 |
| Prospeo: Prospeo Enrich Person Linkedin | 0.6 |
| Crustdata | 1.2 |
| Serper: Serper Linkedin Identity Validate | 0 |
| Linkedin Scraper | 0 |
| Datagma | 0.3 |
| Prospeo: Prospeo Job History | 0.6 |
| Crustdata V2 | 0.4 |


---

## Play: `person-name-domain-to-linkedin-url`

```
deepline tools execute deepline_native:person_name_domain_to_linkedin_url \
  --payload '{"first_name":"<first_name>","last_name":"<last_name>","domain":"<domain>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Dropleads

Serper: Serper Linkedin Search Domain

Prospeo

Crustdata \*

Serper: Serper Linkedin Identity Validate

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `first_name` | `string` | Yes | Contact first name. |
| `last_name` | `string` | Yes | Contact last name. |
| `domain` | `string` | Yes | Company domain. |
| `company_name` | `string` | No | Optional company name context. |
| `email` | `string` | No | Optional known email context. |
| `linkedin_url` | `string` | No | Optional LinkedIn profile URL context. |

## Cost

**0.46-1.22 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Serper: Serper Linkedin Search Domain | 0 |
| Prospeo | 0.6 |
| Crustdata | 1.2 |
| Serper: Serper Linkedin Identity Validate | 0 |


---

## Play: `person-name-domain-to-personal-email`

```
deepline tools execute deepline_native:person_name_domain_to_personal_email \
  --payload '{"first_name":"<first_name>","last_name":"<last_name>","domain":"<domain>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `first_name` | `string` | Yes | Contact first name. |
| `last_name` | `string` | Yes | Contact last name. |
| `domain` | `string` | Yes | Company domain. |
| `company_name` | `string` | No | Optional company name context. |
| `email` | `string` | No | Optional known email context. |
| `linkedin_url` | `string` | No | Optional LinkedIn profile URL context. |

## Cost

**2.35-2.62 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Serper: Serper Linkedin Search Domain | 0 |
| Prospeo | 0.6 |
| Crustdata | 1.2 |
| Serper: Serper Linkedin Identity Validate | 0 |
| Contactout | 1.4 |
| Wiza | 0.4 |
| Leadmagic | 0.7 |
| Datagma | 0.3 |


---

## Play: `person-name-domain-to-phone`

```
deepline tools execute deepline_native:person_name_domain_to_phone \
  --payload '{"first_name":"<first_name>","last_name":"<last_name>","domain":"<domain>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

Dropleads

Serper: Serper Linkedin Search Domain

Prospeo

Crustdata \*

Serper: Serper Linkedin Identity Validate

Leadmagic \*

Wiza \*

Deepline Native \*

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `first_name` | `string` | Yes | Contact first name. |
| `last_name` | `string` | Yes | Contact last name. |
| `domain` | `string` | Yes | Company domain. |
| `company_name` | `string` | No | Optional company name context. |
| `email` | `string` | No | Optional known email context. |
| `linkedin_url` | `string` | No | Optional LinkedIn profile URL context. |

## Cost

**3.2-6.13 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Serper: Serper Linkedin Search Domain | 0 |
| Prospeo | 0.6 |
| Crustdata | 1.2 |
| Serper: Serper Linkedin Identity Validate | 0 |
| Leadmagic | 1.7 |
| Wiza | 0.4 |
| Deepline Native | 4.9 |


---

## Play: `personal-email-to-linkedin-waterfall`

```
deepline tools execute deepline_native:personal_email_to_linkedin_waterfall \
  --payload '{"personal_email":"<personal_email>"}'
```

## How It Works

Providers are tried left to right. The first one to return a result wins — the rest are skipped.

_\\* Only runs when optional fields are provided._

## Parameters

| Name | Type | Required | Description |
| --- | --- | --- | --- |
| `personal_email` | `string` | Yes | Personal email (Gmail/GitHub signup address) to reverse-resolve into a LinkedIn profile. |

## Cost

**2.58-3.92 credits** — waterfalls stop at the first match, so you only pay for the provider that returns a result.

| Step | Max Credits |
| --- | --- |
| Deepline Native | 1 |
| Forager | 1.6 |
| Findymail | 0.6 |
| Peopledatalabs | 3.9 |


---

## Play: `play-account-mapping`

# Account Mapping

Deepline’s role-based contact discovery maps stakeholders at target accounts: `company_to_contact_by_role_waterfall` finds contacts by department and seniority across 5 providers (Dropleads, Apollo, Icypeas, Prospeo, Crustdata), then enriches each with LinkedIn URLs and verified work emails. You get a structured org chart organized by department.

> Single-threaded deals close at ~5%; engaging 5+ stakeholders reaches ~30% — a 6x improvement ( [Gong](https://www.gong.io/blog/data-shows-top-reps-dont-just-sell-they-orchestrate-with-ai), 1.8M opportunities). Account mapping gives your reps the contacts they need across departments.

A complete account map typically covers 2-4 departments with 2-4 roles each, giving your team the 5+ contact points that correlate with 6x higher close rates.

## How do I map stakeholders at a target account with Claude Code?

In Claude Code, type:

```
/deepline-gtm Map the org chart at stripe.com -- find contacts in Engineering, Sales, and Marketing leadership
```

```
/deepline-gtm For each company in targets.csv, find the CTO, VP Engineering, and Head of Security with emails
```

**With Codex:**

```
codex "Map stakeholders at stripe.com: find CTO, VP Engineering, CISO, and Head of Security with verified emails"
```

## What does the account mapping workflow do step by step?

Deepline runs a role-based search for each department and seniority level you specify, then enriches each found contact with LinkedIn URLs and verified work emails. According to industry benchmarks, waterfall enrichment can improve contact coverage by 20-40% compared to any single provider ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)).

1

Parse target accounts and departments

Claude Code reads your company list and the departments/roles you want
mapped.

2

Search for contacts by role

For each company and role combination,
`company_to_contact_by_role_waterfall` finds matching contacts across
multiple providers.

3

Enrich each contact

Found contacts are enriched with LinkedIn URLs and work emails via the
appropriate waterfalls.

4

Write account map

Results are organized by company and department, with contact details for
each stakeholder.

## Which providers power account mapping?

Waterfall enrichment across multiple providers handles both contact discovery and email resolution — higher hit rates, lower credit spend.

1. **company\_to\_contact\_by\_role\_waterfall** — Finds contacts by role at each target company (Dropleads, Apollo, Icypeas, Prospeo, Crustdata)
2. **name\_and\_domain\_to\_email\_waterfall** — Gets verified emails for each found contact
3. **LinkedIn resolution** — Resolves LinkedIn URLs for contacts where missing

The waterfall runs separately for each role, accumulating contacts across departments. B2B data decays at ~22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), so run account mapping quarterly to keep org charts current.

Example prompt for Claude Code

```
Map stakeholders at each company in targets.csv:
- Engineering: CTO, VP Engineering, Head of Platform
- Security: CISO, Head of Security, Security Director
- IT: CIO, VP IT, Head of Infrastructure

For each contact, get: name, title, LinkedIn URL, verified work email
Write to account-map.csv grouped by company
```

Account mapping works best when you specify 2-4 roles per department. Too many
roles per company will increase credit usage significantly. Focus on the
personas your sales team actually engages with.

* * *

**Related:** [Find Decision Makers](https://deepline.com/docs/plays/task-find-decision-makers) \| [Build Prospect List](https://deepline.com/docs/plays/play-build-prospect-list) \| [Find Work Email](https://deepline.com/docs/plays/task-find-work-email)

## Frequently Asked Questions

### How do I map an org chart at a target company?

Use Deepline’s account mapping play. In Claude Code, type: `/deepline-gtm Map the org chart at stripe.com -- find contacts in Engineering, Sales, and Marketing leadership.` Deepline uses `company_to_contact_by_role_waterfall` to search across 5 providers (Dropleads, Apollo, Icypeas, Prospeo, Crustdata) for each role, then enriches contacts with LinkedIn URLs and verified work emails.

### Why does multi-threading matter for account mapping?

Single-threaded deals close at ~5%; engaging 5+ stakeholders reaches ~30% — a 6x improvement ( [Gong](https://www.gong.io/blog/data-shows-top-reps-dont-just-sell-they-orchestrate-with-ai), 1.8M opportunities). Account mapping gives your reps the contacts to multi-thread across departments: champions, economic buyers, technical evaluators.

### Can I map accounts in bulk from a CSV?

Yes. Pass a CSV of target companies and specify the departments and roles you want mapped. Deepline processes each company through the role-based search and enrichment pipeline. A run of 20 companies with 6 roles each produces a single account-map.csv organized by company and department.

### How much does account mapping cost?

Each role search uses 1 `company_to_contact_by_role_waterfall` credit, plus 1 `name_and_domain_to_email_waterfall` credit per found contact. Mapping 10 companies with 6 roles each (approximately 60 searches) plus email enrichment for found contacts typically uses 100-200 credits.

### What data do I need to run account mapping?

You need company names or domains, plus the specific roles you want to find. Deepline handles the contact discovery, LinkedIn resolution, and email enrichment. The more specific your role titles (e.g., “VP Engineering” vs. “engineering leader”), the better the match quality.

### Account mapping with Deepline vs manual research — what’s different?

Manual account mapping on LinkedIn Sales Navigator takes 15-30 minutes per company. Deepline automates contact discovery, LinkedIn resolution, and email enrichment across multiple providers simultaneously. A 20-company account map that would take 5-10 hours manually finishes in minutes.


---

## Play: `play-ad-intelligence`

# Ad Intelligence Research

Deepline queries Adyntel for active ads across Facebook, Google, LinkedIn, and TikTok, then uses `call_ai` to synthesize spend distribution, creative themes, messaging patterns, and platform prioritization into a structured competitive analysis.

> B2B buyers are 57% through the purchase decision before talking to a sales rep (CEB/Gartner). Knowing what ads your competitors run reveals their positioning and messaging — your sales team can use this to differentiate in conversations.

The play pulls a competitor’s active ads across paid channels — Facebook, Google, LinkedIn, TikTok — to analyze spend distribution, creative strategy, messaging themes, and platform mix.

## How do I research competitor ads with Claude Code?

In Claude Code, type:

```
/deepline-gtm What ads is stripe.com running on LinkedIn and Google right now?
```

```
/deepline-gtm Analyze the ad strategy for each competitor in competitors.csv -- show spend, creative themes, and platform mix
```

**With Codex:**

```
codex "Analyze the ad strategy for stripe.com across Facebook, Google, LinkedIn, and TikTok"
```

## What does the ad intelligence workflow do step by step?

Deepline queries 4 ad platform APIs via Adyntel, aggregates the data, and synthesizes a strategic analysis with `call_ai`. Each platform returns different data — creative copy, spend estimates, targeting signals — and Deepline normalizes it into a consistent format. This replaces manual checks across Facebook Ad Library, Google Ads Transparency Center, and LinkedIn Ad Library.

1

Query ad platforms

Deepline queries Adyntel for active ads across Facebook, Google, LinkedIn,
and TikTok for each target domain.

2

Aggregate ad data

Ad creative, copy, spend estimates, and targeting signals are collected
across platforms.

3

Analyze strategy

`call_ai` synthesizes the ad data into a competitive analysis: spend
distribution, creative themes, messaging patterns, platform prioritization.

4

Write intelligence report

Results include per-platform breakdown, creative examples, and strategic
insights.

## Which providers power the ad intelligence?

Adyntel provides access to 4 ad platform APIs. Combined with `call_ai` for synthesis, a single prompt produces a full view of competitor ad strategy.

- **adyntel-facebook** — Facebook/Instagram ad creative and targeting
- **adyntel\_google** — Google Search and Display ad data
- **adyntel\_linkedin** — LinkedIn Sponsored Content data
- **adyntel-tiktok-search** — TikTok ad creative and performance

Each platform query uses 1 credit, so a full 4-platform analysis costs 4 credits per competitor plus 1 `call_ai` credit for synthesis.

CLI commands

```
# LinkedIn ads for a domain
deepline tools execute adyntel_linkedin \
  --payload '{"domain":"stripe.com"}'

# Google ads for a domain
deepline tools execute adyntel_google \
  --payload '{"domain":"stripe.com"}'
```

Most useful for competitive analysis in paid marketing. Combine with the
[Competitive Landscape](https://deepline.com/docs/plays/play-competitive-landscape) play to get both
organic positioning and paid strategy in one analysis.

* * *

**Related:** [Competitive Landscape](https://deepline.com/docs/plays/play-competitive-landscape) \| [Company Research Brief](https://deepline.com/docs/plays/play-company-research-brief) \| [Classify Company Signals](https://deepline.com/docs/plays/play-classify-company-signals)

## Frequently Asked Questions

### How do I see what ads my competitors are running?

Use Deepline’s ad intelligence play with the Adyntel integration. In Claude Code, type: `/deepline-gtm What ads is stripe.com running on LinkedIn and Google right now?` Deepline queries Adyntel for active ads across Facebook, Google, LinkedIn, and TikTok, returning creative copy, spend estimates, and targeting signals.

### How do I scrape competitor ad copy?

Deepline’s Adyntel integration pulls competitor ad creative and copy from 4 platforms: Facebook/Instagram (`adyntel_facebook`), Google Search and Display (`adyntel_google`), LinkedIn Sponsored Content (`adyntel_linkedin`), and TikTok (`adyntel_tiktok_search`). No scraping needed — Adyntel provides structured API access to active ad data including copy text, creative formats, and targeting parameters. Each platform query costs 1 credit.

### How do I analyze competitor ad spend programmatically?

Use Deepline’s ad intelligence play. In Claude Code, type: `/deepline-gtm Analyze the ad strategy for stripe.com across all platforms.` Deepline queries Adyntel for active ads on Facebook, Google, LinkedIn, and TikTok, then uses `call_ai` to synthesize spend distribution, creative themes, and platform prioritization into a structured report. Run monthly to track spend shifts over time.

### How does Deepline turn raw ad data into strategic analysis?

Deepline queries Adyntel for raw ad data (creative copy, spend estimates, targeting signals) across all 4 platforms, then uses `call_ai` to synthesize it into a strategic analysis: spend distribution, creative themes, messaging patterns, and platform prioritization. You get both the raw data and the strategic layer from a single prompt.

### Can I analyze competitor ads in bulk from a CSV?

Yes. Pass a CSV of competitor domains to Claude Code: “Analyze the ad strategy for each competitor in competitors.csv.” Deepline queries Adyntel for each domain across all 4 platforms and synthesizes a per-competitor analysis with spend distribution, creative themes, and platform mix.

### How much does ad intelligence research cost?

Each domain query uses 1 credit per platform (4 credits for all platforms: Facebook, Google, LinkedIn, TikTok) plus 1 `call_ai` credit for synthesis. A competitive analysis across 10 competitors would use approximately 50 credits.

### What’s the best way to monitor competitor ad strategy over time?

Run the ad intelligence play weekly or monthly against the same competitors. Compare output across runs to spot changes in spend distribution, new creative themes, platform shifts, or messaging pivots. Pair with the [Competitive Landscape](https://deepline.com/docs/plays/play-competitive-landscape) play for organic positioning alongside paid strategy.


---

## Play: `play-build-prospect-list`

# Build Prospect List

Deepline’s prospect list pipeline chains 4-5 tasks into one workflow: find companies matching your ICP, enrich with firmographic data, identify decision makers by role, get verified work emails via waterfall enrichment, and validate deliverability. Output is a ready-to-use CSV.

> According to industry benchmarks, waterfall enrichment across multiple providers can improve email coverage by 20-40% compared to any single provider ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)). Deepline’s waterfall across 95+ integrations tries the cheapest provider first and stops at the first valid result, maximizing coverage while minimizing credits.

The pipeline takes an ICP definition and produces a deliverable list of verified contacts — companies matched to your criteria, decision makers identified by role, work emails validated for deliverability.

## How do I build a prospect list with Claude Code?

In Claude Code, type:

```
/deepline-gtm Build me a prospect list: B2B SaaS companies in the US with 100-500 employees. Find the VP Sales and VP Marketing at each. Get verified emails.
```

```
/deepline-gtm I want 50 qualified leads in the cybersecurity space. Find companies, get decision makers, enrich with emails and phone numbers.
```

**With Codex:**

```
codex "Build a prospect list: 20 B2B SaaS companies, 100-500 employees, Series A-C. Find VP Sales and VP Marketing with verified emails."
```

## What does the prospect list building workflow do step by step?

Deepline chains 4-5 tasks into a single pipeline. Each step builds on the previous step’s output, and Claude Code handles the data flow between steps. Single-threaded deals close at ~5%; engaging 5+ stakeholders reaches ~30% ( [Gong](https://www.gong.io/blog/data-shows-top-reps-dont-just-sell-they-orchestrate-with-ai), 1.8M opportunities) — so building lists with multiple contacts per account matters.

1

Find matching companies

Claude Code uses web search or Apollo company-search to find companies
matching your ICP criteria.

2

Enrich company profiles

Each company is enriched with `enrich_company_finder` for revenue, employee
count, industry, and funding.

3

Find decision makers

`company_to_contact_by_role_waterfall` finds contacts matching your target
roles at each company.

4

Get verified emails

`name_and_domain_to_email_waterfall` finds work emails for each decision
maker via waterfall enrichment.

5

Validate emails

ZeroBounce verification checks deliverability on every found email.

6

Output prospect list

The enriched, validated prospect list is written to prospects.csv.

## Which providers power the prospect list pipeline?

Deepline chains 4-5 specialized tasks, each using waterfall enrichment across multiple providers for higher hit rates. B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), which is why the email validation step matters.

1. **Company search** — Web search (Claude Code’s native web agent, Exa, or Parallel AI) or Apollo company-search
2. **enrich\_company\_finder** — Company enrichment waterfall
3. **company\_to\_contact\_by\_role\_waterfall** — Decision maker search across Dropleads, Apollo, Icypeas, Prospeo, and Crustdata
4. **name\_and\_domain\_to\_email\_waterfall** — Email waterfall across multiple providers (free patterns first, then paid finders)
5. **ZeroBounce** — Email validation

Each step builds on the previous step’s output. Claude Code wires the data between steps.

Example prompt for Claude Code

```
Build a prospect list:
- Find 20 B2B SaaS companies in the US, 100-500 employees, Series A-C
- At each company, find the VP Sales and VP Marketing
- Get verified work emails for each contact
- Write to prospects.csv with: company, domain, revenue, employee_count, contact_name, title, email, email_status
- Start with 3 companies as a pilot, then run the rest if it looks good
```

This is the most credit-intensive play since it chains multiple paid lookups.
Always pilot with 3-5 companies first. A full run of 50 companies with 2
contacts each could use 200-400 credits.

* * *

**Related:** [Find Work Email](https://deepline.com/docs/plays/task-find-work-email) \| [Find Decision Makers](https://deepline.com/docs/plays/task-find-decision-makers) \| [Qualify & Score Leads](https://deepline.com/docs/plays/play-qualify-lead-fit)

## Frequently Asked Questions

### How do I build a prospect list programmatically?

Use Deepline’s build prospect list play. Type your ICP criteria and target roles in Claude Code: `/deepline-gtm Build a prospect list: B2B SaaS, 100-500 employees, find VP Sales and VP Marketing with verified emails.` Deepline chains company search, firmographic enrichment, decision maker discovery, email waterfall, and ZeroBounce validation into one automated pipeline.

### Can I build prospect lists in bulk from a CSV?

Yes — two ways. You can start with ICP criteria (Deepline finds companies for you), or pass an existing CSV of company domains and Deepline handles everything from decision maker discovery onward. Either way, output is a single CSV with company data, contact details, and validated emails.

### How much does building a prospect list cost?

This is the most credit-intensive play. Each company uses approximately 1 credit for company enrichment, 1-2 credits per role for contact finding, and 1 credit per email for waterfall + validation. A run of 50 companies with 2 contacts each typically uses 200-400 credits. Always pilot with 3-5 companies first.

### What’s the best way to build a B2B prospect list?

Start with a narrow ICP definition — industry, size range, funding stage, geography. Find matching companies, then use role-based contact discovery for the specific decision makers your sales team talks to. Deepline’s waterfall across 5+ providers (Dropleads, Apollo, Icypeas, Prospeo, Crustdata) produces higher hit rates than any single provider.

### Why build prospect lists inside the IDE?

The entire pipeline — from ICP definition to validated CSV — runs in Claude Code or Codex, the same environment where you write code. Describe the pipeline in plain language and Deepline chains company search, contact discovery, email waterfall, and validation automatically. No column configuration, no manual step wiring, no separate UI. Your pipeline definition lives in the prompt, so it version-controls naturally.


---

## Play: `play-classify-company-signals`

# Classify Company Signals

To detect growth signals across target accounts programmatically, use Deepline’s 2-pass play: web search (Claude Code’s native web agent, Exa, or Parallel AI) queries the web for specific signal types (expansion, acquisition, hiring surges, regulatory changes), then `call_ai` classifies each company against your criteria with evidence and confidence scores. Define all your signal types in one natural-language prompt — adding a new signal type is one line, not a new workflow.

> B2B buyers are 57% through the purchase decision before engaging a sales rep, according to CEB/Gartner research. Signal-based selling detects buying intent early, letting your team engage before competitors do.

The play monitors target accounts for specific business events — facility expansions, acquisitions, hiring surges, regulatory exposure, leadership changes — that indicate buying intent or trigger a sales motion. Static firmographic filters miss these changes. B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), which is why real-time signal detection matters.

## How do I classify company signals with Claude Code?

In Claude Code, type:

```
/deepline-gtm Classify these companies by growth signals: expansion, acquisition, new funding, regulatory pressure
```

```
/deepline-gtm Which of these companies opened new manufacturing facilities in the last 12 months?
```

```
/deepline-gtm Flag companies in targets.csv that are affected by new cybersecurity regulations
```

**With Codex:**

```
codex "Classify companies in targets.csv by signals: expansion, acquisition, hiring surge, regulatory pressure"
```

## What does the signal classification workflow do step by step?

Two passes: targeted web search for each signal type, then AI classification with evidence extraction. Every detected signal includes a confidence score, evidence snippet, and source URL.

1

Search for signals (Pass 1)

For each company, web search queries for the specific signal types you
requested: expansion news, acquisitions, regulatory filings, hiring surges.

2

Classify and tag (Pass 2)

`call_ai` reviews the search results and classifies each company against
your signal criteria, tagging matches with evidence.

3

Write tagged output

Results include signal type, confidence level, evidence snippet, and source
URL for each detected signal.

## Which providers power the signal classification?

Two tools run in sequence. Signal types are defined in your prompt, not hardcoded — classify on any signal without changing templates. 95+ integrations let you cross-reference signals against firmographic data for higher confidence.

1. **Web search** — Claude Code’s native web agent, Exa, or Parallel AI runs targeted searches for specific signal types per company
2. **call\_ai** — Classification and evidence extraction from raw search results

You can classify on any signal: facility expansion, M&A activity, leadership changes, regulatory exposure, hiring in specific departments, product launches, or custom signals relevant to your industry.

Example prompt for Claude Code

```
For each company in targets.csv, classify by these signals:
- EXPANSION: new office, facility, or market entry
- ACQUISITION: acquired or was acquired
- HIRING_SURGE: 20%+ headcount growth in last 6 months
- REGULATORY: affected by new compliance requirements

Write to signals.csv with columns: company, signal_type, confidence, evidence, source_url
Only include companies that match at least one signal.
```

Signal classification works best when you’re specific about what you’re
looking for. “Growth signals” is too broad. “Opened a new manufacturing
facility in North America in the last 12 months” is actionable.

* * *

**Related:** [Company Research Brief](https://deepline.com/docs/plays/play-company-research-brief) \| [Competitive Landscape](https://deepline.com/docs/plays/play-competitive-landscape) \| [Qualify & Score Leads](https://deepline.com/docs/plays/play-qualify-lead-fit)

## Frequently Asked Questions

### How do I detect buying signals across target accounts?

In Claude Code, type: `/deepline-gtm Classify companies in targets.csv by signals: expansion, acquisition, hiring surge, regulatory pressure.` Deepline uses web search to find evidence for each signal type, then `call_ai` classifies matches with confidence scores and source URLs. Results land in a CSV with one row per detected signal.

### How do I add or change signal types?

Signal types are defined in your prompt, not hardcoded into a configuration. Add a new signal type by adding one line to your prompt. Change classification criteria by editing the prompt and rerunning. This makes it easy to iterate on what signals matter as your GTM strategy evolves.

### Can I classify signals in bulk from a CSV?

Yes. Pass a CSV of target companies to Claude Code. Each company goes through the search-then-classify pipeline, checked for every signal type you specified. Output includes only companies that matched at least one signal, with evidence and source URLs for each match.

### What kinds of signals can Deepline detect?

Any signal with a public web footprint: facility expansions, M&A activity, leadership changes, IPO filings, product launches, regulatory exposure, hiring surges in specific departments, office openings, partnership announcements, and custom signals relevant to your industry. If it shows up in news, press releases, or SEC filings, Deepline can detect it.

### How much does signal classification cost?

Each company uses 1 web search credit per signal type searched and 1 `call_ai` credit for classification. Searching 50 companies across 4 signal types costs approximately 200-250 credits.


---

## Play: `play-company-research-brief`

# Company Research Brief

To generate a structured company research brief programmatically, use Deepline’s 2-pass play: web search (Claude Code’s native web agent, Exa, or Parallel AI depending on the use case) gathers raw web data (news, funding, hiring), then `call_ai` synthesizes it into a consistent brief. Separating search from synthesis means each step can be validated independently — the AI only summarizes verified search results.

> B2B buyers are 57% through the purchase decision before engaging a sales rep, according to CEB/Gartner research. Pre-call research briefs ensure your team walks in with context that matches where the buyer already is.

The play gathers and structures publicly available information about a target account — recent news, funding status, hiring trends, product launches, pain points — so your sales team walks into every call prepared.

## How do I research a company with Claude Code?

In Claude Code, type:

```
/deepline-gtm Research these target accounts and create a one-page brief for each
```

```
/deepline-gtm I need pre-call research on stripe.com -- recent news, funding, hiring trends, product launches
```

```
/deepline-gtm Build research briefs for each company in targets.csv -- focus on pain points relevant to cybersecurity
```

**With Codex:**

```
codex "Research these target accounts and create a one-page brief for each company"
```

## What does the research workflow do step by step?

Two passes: first, search the web for raw information; second, synthesize it into a structured brief with consistent sections. Each pass uses a different tool.

1

Read your target list

Claude Code reads targets.csv and identifies company domains or names.

2

Search for each company (Pass 1)

For each company, web search gathers recent news, funding announcements,
hiring activity, and product launches. 95+ integrations feed the search for
broad coverage.

3

Synthesize into structured brief (Pass 2)

`call_ai` processes the raw search results into a structured brief with
consistent sections.

4

Write output

Results are written to research-briefs.csv with columns: company, summary,
recent\_news, funding\_status, hiring\_signals, pain\_points.

## Which providers and AI models power the research?

Two tools run in sequence. Separating search from synthesis is what makes it more reliable than single-pass tools. B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), so the live web search in Pass 1 keeps your briefs current rather than stale.**Pass 1 — Web search:** Queries the web for recent, relevant information about each company using Claude Code’s native web agent, Exa, or Parallel AI depending on the use case. Multiple searches may run per company (news, funding, hiring) to get comprehensive coverage.**Pass 2 — call\_ai:** Takes the raw search results and synthesizes them into a structured brief. The AI follows your prompt to focus on what matters (e.g., “pain points relevant to cybersecurity”).

CLI command

```
deepline tools execute exa_search \
  --payload '{"query":"stripe.com recent news funding hiring 2026"}'
```

Customize the research focus by being specific in your prompt. “Focus on pain
points relevant to cybersecurity” produces very different briefs than “focus
on growth and expansion signals.”

* * *

**Related:** [Enrich Company](https://deepline.com/docs/plays/task-enrich-company) \| [Competitive Landscape](https://deepline.com/docs/plays/play-competitive-landscape) \| [Qualify & Score Leads](https://deepline.com/docs/plays/play-qualify-lead-fit)

## Frequently Asked Questions

### How do I research a company before a sales call?

In Claude Code, type: `/deepline-gtm Research stripe.com -- recent news, funding, hiring trends, product launches.` Deepline runs a 2-pass search (web search for raw data, `call_ai` for synthesis) and returns a structured brief with summary, news, funding status, hiring signals, and pain points.

### Can I research companies in bulk from a CSV?

Yes. Point Claude Code at a CSV with company domains or names: “Build research briefs for each company in targets.csv.” Each company goes through the 2-pass workflow and results land in a single output CSV. No batch-size limit — runs of 200+ companies work without configuration changes.

### How accurate is AI-powered company research?

It depends on how much public information exists. Well-funded companies with active press coverage produce the richest briefs. For stealth-mode startups or very small companies, web search may return limited results — the brief will have thinner sections rather than hallucinated content, because search and synthesis are separated.

### Why run company research inside the IDE?

Research briefs are generated in the same environment where you manage your data and build workflows. Describe the research focus in plain language — no template configuration, no UI columns. The 2-pass approach (search, then synthesize) lets you inspect raw search results before synthesis, and you can customize the output format by adjusting the prompt.

### How much does a company research brief cost?

Each brief uses 1 web search credit and 1 `call_ai` credit. For a batch of 50 companies, expect roughly 50-100 credits depending on whether Claude Code runs multiple searches per company for deeper coverage.


---

## Play: `play-competitive-landscape`

# Competitive Landscape Analysis

To analyze your competitive landscape programmatically, use Deepline’s 3-step play: search the web for competitors using Claude Code’s native web agent, Exa, or Parallel AI, enrich each with firmographic data via `enrich_company_finder`, then synthesize a competitive matrix with `call_ai`. One natural-language prompt produces a structured comparison with revenue, headcount, funding, and positioning data.

> B2B buyers are 57% through the purchase decision before engaging a sales rep, according to CEB/Gartner research. A structured competitive matrix lets your reps address comparison questions before the buyer raises them.

The play identifies a company’s competitors, enriches each with structured firmographic data, and produces a side-by-side comparison of pricing, features, and market position.

## How do I run a competitive analysis with Claude Code?

In Claude Code, type:

```
/deepline-gtm Find the top 5 competitors for each company in targets.csv and compare them
```

```
/deepline-gtm Build a competitive landscape for stripe.com -- include pricing, features, market position
```

**With Codex:**

```
codex "Build a competitive landscape for stripe.com including pricing, features, and market position"
```

## What does the competitive analysis workflow do step by step?

Three steps: discover competitors, enrich each with firmographic data, then synthesize a competitive matrix.

1

Identify competitors

For each target company, Deepline uses web search to find competitors
through market research, comparison articles, and industry reports.

2

Enrich competitor profiles

Each identified competitor is enriched with `enrich_company_finder` for
revenue, employee count, funding, and tech stack. According to industry
benchmarks, waterfall enrichment across multiple providers can improve data
coverage by 20-40% compared to any single provider
( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)).

3

Analyze positioning

`call_ai` synthesizes the data into a competitive comparison: pricing tiers,
feature gaps, market position, strengths and weaknesses.

4

Write structured output

Results are written as a competitive matrix with side-by-side comparisons.

## Which providers power the competitive analysis?

Three tools, each handling a different part of the pipeline. Structured data (revenue, headcount) comes from verified provider APIs, not AI generation.

1. **Web search** — Claude Code’s native web agent, Exa, or Parallel AI finds competitors and gathers positioning data from the web
2. **enrich\_company\_finder** — Gets structured data (revenue, headcount, funding) for each competitor via a multi-provider waterfall
3. **call\_ai** — Synthesizes everything into a structured competitive analysis

CLI command

```
# Find competitors for a domain
deepline tools execute exa_search \
  --payload '{"query":"stripe.com competitors payment processing comparison"}'
```

Quality depends on how much public information is available. Well-funded
companies in competitive markets produce the richest analyses. Stealth-mode
startups will have thinner profiles.

* * *

**Related:** [Company Research Brief](https://deepline.com/docs/plays/play-company-research-brief) \| [Classify Company Signals](https://deepline.com/docs/plays/play-classify-company-signals) \| [Ad Intelligence Research](https://deepline.com/docs/plays/play-ad-intelligence)

## Frequently Asked Questions

### How do I find a company’s competitors programmatically?

In Claude Code, type: `/deepline-gtm Find the top 5 competitors for stripe.com and compare them.` Deepline uses web search to discover competitors from comparison articles and industry reports, then enriches each with `enrich_company_finder` for revenue, headcount, and funding data.

### How does Deepline chain the competitive analysis automatically?

Describe the analysis in one prompt and Deepline chains web search, `enrich_company_finder`, and `call_ai` without any manual step wiring. Firmographic data comes from provider APIs (not AI-generated estimates), and you can adjust the analysis dimensions by changing the prompt.

### Can I run competitive analysis in bulk from a CSV?

Yes. Pass a CSV of target companies to Claude Code: “Find the top 5 competitors for each company in targets.csv and compare them.” Each company goes through the full discovery-enrich-synthesize pipeline, and results land in a single competitive matrix.

### How much does a competitive landscape analysis cost?

Each target company uses approximately 1 web search credit for competitor discovery, plus 1 `enrich_company_finder` credit per competitor found (typically 3-5 competitors). A batch of 10 target companies with 5 competitors each would use approximately 60 credits total.

### How do I automate competitive monitoring?

Run the competitive landscape play on a recurring basis (weekly or monthly) against the same set of target companies. Compare the output across runs to detect changes in competitor positioning, funding, headcount, or product launches. Combine with the [Classify Company Signals](https://deepline.com/docs/plays/play-classify-company-signals) play for event-driven alerts.


---

## Play: `play-job-change-alerts`

# Job Change Alerts

Deepline’s job change pipeline detects which contacts in your CRM changed companies. `job_change` (powered by LeadMagic) checks LinkedIn profiles for role or company changes, `enrich_company_finder` pulls data on the new employer, and `name_and_domain_to_email_waterfall` finds their new work email. Former champions at new companies convert better than cold outbound — this play finds them.

> 30% of B2B contacts switch jobs annually, and CRM data decays at ~22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)). Catching changes within 30-90 days of a move reaches new leaders while they’re still setting up their tech stack.

The play monitors your existing contacts for company or role changes, then enriches the new context (employer, title, email) so your sales team can re-engage warm relationships at new accounts.

## How do I detect job changes with Claude Code?

In Claude Code, type:

```
/deepline-gtm Check which of my HubSpot contacts changed jobs in the last 90 days
```

```
/deepline-gtm Pull contacts from my CRM and flag job changes -- enrich with their new company info
```

**With Codex:**

```
codex "Check contacts.csv for job changes in the last 90 days and enrich with new company info and emails"
```

## What does the job change detection workflow do step by step?

The pipeline detects changes, enriches the new company, finds the new email, and tags the type of change. Every flagged contact includes old and new context side by side.

1

Load your contact list

Claude Code reads from your CSV or pulls contacts from your CRM integration.

2

Detect job changes

Deepline’s `job_change` detection (powered by LeadMagic) checks each
contact’s current LinkedIn profile against your stored data to detect role
or company changes.

3

Enrich new company

For contacts who changed companies, `enrich_company_finder` pulls data on
their new employer.

4

Find new work email

`name_and_domain_to_email_waterfall` finds their email at the new company.
According to industry benchmarks, waterfall enrichment across multiple
providers can improve email coverage by 20-40% compared to any single
provider ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)).

5

Flag opportunities

Results are tagged with change type (new company, new role, promotion) and
enriched with new company context.

## Which providers power job change detection?

Three tools run in sequence. Job change detection is powered by LeadMagic, and the enrichment steps use waterfall patterns across multiple providers.

1. **Deepline job\_change (powered by LeadMagic)** — Checks each contact’s current LinkedIn profile against your stored data to detect role or company changes
2. **enrich\_company\_finder** — Enriches the new company with revenue, size, industry
3. **name\_and\_domain\_to\_email\_waterfall** — Finds verified email at the new company

Example prompt for Claude Code

```
Check contacts.csv for job changes in the last 90 days.
For anyone who changed companies:
- Get their new company name, domain, and employee count
- Find their new work email
- Write to job-changes.csv with: name, old_company, new_company, new_title, new_email, change_date
```

Job change detection requires LinkedIn URLs for your contacts. If you don’t
have LinkedIn URLs, run the [Find LinkedIn\\
Profile](https://deepline.com/docs/plays/task-find-linkedin-profile) task first.

* * *

**Related:** [Find LinkedIn Profile](https://deepline.com/docs/plays/task-find-linkedin-profile) \| [Build Prospect List](https://deepline.com/docs/plays/play-build-prospect-list) \| [Personalize Outreach](https://deepline.com/docs/plays/play-personalize-outreach)

## Frequently Asked Questions

### How do I detect which contacts changed jobs?

Use Deepline’s job change alerts play. In Claude Code, type: `/deepline-gtm Check contacts.csv for job changes in the last 90 days.` Deepline’s `job_change` detection (powered by LeadMagic) checks each contact’s LinkedIn profile, then enriches changed contacts with new employer data and new work emails. Output includes old company, new company, new title, new email, and change date.

### Why are job changes valuable for sales?

Former champions at new companies already know your product, have a new budget, and are setting up their tech stack. B2B data decays at ~22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)). The 30-90 day window after a move is when new leaders make purchasing decisions — catch them there.

### Can I monitor job changes in bulk from a CSV?

Yes. Pass a CSV with contact names and LinkedIn URLs. Deepline checks each contact for changes and outputs only the contacts who moved, enriched with new company data and new work emails. Runs of 500+ contacts work without configuration changes.

### What data do I need to run job change detection?

You need LinkedIn URLs for your contacts. If you only have names and companies, run the [Find LinkedIn Profile](https://deepline.com/docs/plays/task-find-linkedin-profile) task first to resolve LinkedIn URLs, then run job change detection on the enriched list.

### How do I automate job change monitoring?

Run the job change detection play on a recurring basis (weekly or monthly) against your CRM contact list. Each run checks for changes since the last check. Combine with the [Personalize Outreach](https://deepline.com/docs/plays/play-personalize-outreach) play to automatically generate re-engagement emails for contacts who moved to new companies.


---

## Play: `play-personalize-outreach`

# Personalize Outreach

To generate personalized cold emails for a list of contacts, use Deepline’s 3-pass play: research each contact with web search (Claude Code’s native web agent, Exa, or Parallel AI), extract the strongest personalization signals with `call_ai`, then generate tailored copy in a separate `call_ai` pass. One natural-language prompt produces emails that reference verified details about each prospect.

> Personalized emails outperform generic templates in open and reply rates. Deepline’s 3-pass separation grounds personalization in real research rather than hallucinated details.

The play researches each prospect’s recent activity and company news, then writes a cold email that references something specific to them instead of sounding templated.

## How do I write personalized cold emails with Claude Code?

In Claude Code, type:

```
/deepline-gtm Research these contacts and write a personalized cold email for each, referencing something specific about their company or role
```

```
/deepline-gtm Generate personalized one-liners for contacts.csv based on their LinkedIn activity and company news
```

**With Codex:**

```
codex "Research contacts.csv and write a personalized cold email opening for each contact"
```

## What does the personalization workflow do step by step?

Research, signal extraction, and copy generation run as three separate passes. Single-pass tools that try to research and write simultaneously tend to produce generic or hallucinated personalization.

1

Research each contact (Pass 1)

For each contact, Deepline gathers context: recent LinkedIn activity,
company news, role changes, and public content they’ve authored.

2

Extract personalization signals (Pass 2)

`call_ai` identifies the strongest personalization hooks: a recent talk, a
company milestone, a shared interest, a relevant pain point.

3

Generate personalized copy (Pass 3)

`call_ai` writes the email or one-liner using the extracted signals,
following your tone and structure guidelines.

4

Write output

Results include the personalized email/one-liner, the signal used, and the
source for each contact.

## Which providers power the personalization?

Three tools run across the three passes. The AI writes copy based on verified research, not hallucinated details. 95+ GTM integrations feed richer context than single-provider tools.

1. **Web search** — Claude Code’s native web agent, Exa, or Parallel AI gathers recent public information about the contact and their company
2. **call\_ai (signal extraction)** — Identifies the best personalization hooks from raw research
3. **call\_ai (copy generation)** — Writes personalized output using the extracted signals

B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), so the live web search in Pass 1 ensures your personalization references current events rather than stale data.

Example prompt for Claude Code

```
For each contact in contacts.csv:
1. Research their company's recent news and the contact's LinkedIn activity
2. Write a personalized cold email opening (2-3 sentences) that references something specific
3. Keep the tone professional but conversational
4. Write to outreach.csv with columns: name, email, personalized_opening, signal_used
```

The quality of personalization depends on how much public information exists
for each contact. C-level executives at well-known companies produce the best
results. For less visible contacts, the play falls back to company-level
personalization.

* * *

**Related:** [Build Prospect List](https://deepline.com/docs/plays/play-build-prospect-list) \| [Find Work Email](https://deepline.com/docs/plays/task-find-work-email) \| [Company Research Brief](https://deepline.com/docs/plays/play-company-research-brief)

## Frequently Asked Questions

### How do I personalize cold emails for a large list?

Pass a CSV of contacts to Claude Code: “Research these contacts and write a personalized cold email for each.” Deepline runs a 3-pass workflow — web search for research, `call_ai` for signal extraction, `call_ai` for copy generation — producing emails that reference real, verifiable details about each prospect.

### Why does the 3-pass approach produce better personalization?

Single-pass approaches that try to research and write simultaneously tend to produce generic or hallucinated output. Deepline’s 3-pass separation means the copy-generation step only works with verified signals from the research step. You can inspect the extracted signals before copy generation runs, and adjust the tone or angle by changing the prompt.

### Can I personalize outreach in bulk from a CSV?

Yes. Point Claude Code at a CSV with contact names, emails, or LinkedIn URLs. Deepline processes each contact through the 3-pass research-extract-generate pipeline. Batches of 100+ contacts work without configuration changes, and output includes the personalized copy, the signal used, and the source URL for verification.

### What data do I need to run personalized outreach?

At minimum, contact names and company domains. LinkedIn URLs improve results because Deepline can pull the contact’s recent activity and posts. Adding role, company, and LinkedIn URL together yields the most specific personalization.

### How do I automate personalized outreach?

Combine the personalize outreach play with the [Build Prospect List](https://deepline.com/docs/plays/play-build-prospect-list) play. First build a list of qualified prospects with verified emails, then run the personalization play against that list. The output CSV can be imported directly into your outreach tool (Outreach, Apollo, Salesloft).


---

## Play: `play-qualify-lead-fit`

# Qualify & Score Leads

To score leads against your ICP programmatically, use Deepline’s 2-step play: enrich each lead with firmographic data via `enrich_company_finder`, then classify with `call_ai` against your ICP criteria. Your scoring criteria live in the prompt, not in a template — so when your ICP evolves, you change the prompt instead of reconfiguring a template.

> “Describe the goal and constraints, not the exact provider sequence.” Deepline selects the optimal enrichment path and applies your ICP criteria as a flexible AI classification — no template migration required.

B2B buyers are 57% through their purchase decision before engaging a sales rep (CEB/Gartner), so early, accurate qualification matters. This play scores prospects on company size, industry, funding stage, tech stack, and geography, then tiers them (A/B/C) so sales prioritizes the highest-fit accounts.

## How do I score leads with Claude Code?

In Claude Code, type:

```
/deepline-gtm Score these leads against our ICP: B2B SaaS, 50-500 employees, Series A-C, US/EU. Tier them A/B/C.
```

```
/deepline-gtm Qualify leads.csv -- our ideal customer uses AWS, has a security team of 5+, and revenue >\$10M
```

**With Codex:**

```
codex "Score leads.csv against our ICP: B2B SaaS, 50-500 employees, Series A-C, US/EU. Tier A/B/C."
```

## What does the lead scoring workflow do step by step?

Two steps: enrich each lead with real firmographic data from provider APIs, then apply AI classification against your ICP criteria. Every scored lead includes a tier, score breakdown, and written rationale.

1

Enrich company data

Each lead’s company is enriched with `enrich_company_finder` to get revenue,
employee count, industry, funding stage, and tech stack. According to
industry benchmarks, waterfall enrichment across multiple providers can
improve data coverage by 20-40% compared to any single provider
( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)).

2

Apply ICP criteria

`call_ai` evaluates each enriched lead against your stated ICP criteria,
scoring on each dimension.

3

Tier and rank

Leads are classified into tiers (A/B/C or your custom scheme) with a
rationale for each score.

4

Write scored output

Results are written with tier, score breakdown, and reasoning for each lead.

## Which providers power the lead scoring?

Two tools run in sequence. Enrichment pulls real provider data (not AI-generated estimates), and classification uses AI to apply your custom criteria.

1. **enrich\_company\_finder** — Gets structured company data (revenue, headcount, funding, tech stack) for each lead via a multi-provider waterfall
2. **call\_ai** — Applies your ICP criteria as a classification prompt, scoring each lead across every dimension you specify

You can score on any criteria: employee count ranges, specific technologies in the stack, geographic regions, funding stages, industry verticals, or custom signals like “recently opened a new office.”

Example prompt for Claude Code

```
Score leads.csv against this ICP:
- B2B SaaS company
- 50-500 employees
- Series A through Series C
- US or EU headquarters
- Uses cloud infrastructure (AWS, GCP, Azure)

Tier A = matches 5/5 criteria
Tier B = matches 3-4
Tier C = matches 1-2

Write to scored-leads.csv with columns: company, tier, score, reasoning
```

Your ICP criteria live in the prompt, not in a template configuration. When
your ICP changes, just change the prompt. No template migration needed.

* * *

**Related:** [Enrich Company](https://deepline.com/docs/plays/task-enrich-company) \| [Build Prospect List](https://deepline.com/docs/plays/play-build-prospect-list) \| [Classify Company Signals](https://deepline.com/docs/plays/play-classify-company-signals)

## Frequently Asked Questions

### How do I qualify leads against my ICP programmatically?

Type your ICP criteria in Claude Code: `/deepline-gtm Score leads.csv against our ICP: B2B SaaS, 50-500 employees, Series A-C.` Deepline enriches each lead with `enrich_company_finder` for real firmographic data, then uses `call_ai` to classify and tier each lead with a written rationale.

### How do I update my scoring criteria?

Your ICP criteria live in the natural-language prompt, not in a fixed template. When your ICP evolves, change the prompt and rerun — no migration, no column remapping. This makes it easy to experiment with different scoring dimensions or adjust thresholds as your market understanding sharpens.

### Can I score leads in bulk from a CSV?

Yes. Point Claude Code at any CSV with company names or domains. Each row goes through the enrich-then-classify pipeline. Output includes tier, breakdown, and reasoning per lead. Batches of 500+ work without configuration changes.

### What data do I need to run lead scoring?

At minimum, company names or domains for each lead. `enrich_company_finder` handles the rest — pulling revenue, employee count, industry, funding stage, and tech stack from provider APIs. More specific ICP criteria yield more useful scoring output.

### How much does lead scoring cost?

Each lead uses 1 `enrich_company_finder` credit and 1 `call_ai` credit. A batch of 100 leads costs approximately 100-200 credits depending on enrichment depth.


---

## Play: `research-workflow`

Use this for discovery-heavy tasks where you need context and summary output.

## Workflow

1. Seed search queries.
2. Run broad discovery.
3. Deepen only the top matches.
4. Synthesize into briefs.

Keep discovery broad, then spend deeper research credits only on promising
rows.

## Claude Code Prompt

```
Use research-queries.csv and build a two-pass Deepline research workflow:
1) broad discovery for each query
2) deeper research only for the top matches
3) synthesize a short brief per row

Run rows 0-3 first and write output to research-queries-output.csv.
Return:
- the action chain you used
- evidence links used for each brief
- a cost-conscious scaling plan for rows 4+
```

## Why This Layout Is Efficient

- Async depth is isolated to high-priority hits.
- You avoid overpaying for unnecessary deep research.
- You can route the output directly into review workflows.

* * *

View source on [GitHub](https://github.com/getaero-io/gtm-eng-skills) \| [I Have X, I Want Y →](https://deepline.com/docs/plays/decision-tree)


---

## Play: `signal-champion-tracking`

# Champion Tracking — Job Change Signals

Past champions who move to new companies win deals at **37% vs 19%** for cold outbound, based on 230K champions and $256M in pipeline ( [Champify](https://www.champify.io/resources/the-impact-of-tracking-job-changes-value-report)). They convert at **3x** the rate of cold prospects ( [UserGems](https://www.usergems.com/blog/signal-based-marketing)). 30% of B2B contacts switch jobs annually ( [Cognism](https://www.cognism.com/blog/data-decay)), so automated champion tracking pays for itself fast.

## How do I detect job changes with Claude Code?

The core prompt pulls your CRM, detects who moved, finds new emails, and updates your records in a single pass.In Claude Code, type:

```
/deepline-gtm Find contacts in my HubSpot who changed jobs, get their new work emails, update HubSpot, and add job changers to my re-engagement campaign
```

**With Codex:**

```
codex "Pull my closed-won contacts from HubSpot, check which ones changed companies, find their new work emails, and flag them for re-engagement"
```

Uses the **[deepline-gtm](https://github.com/getaero-io/gtm-eng-skills/tree/main/skills/deepline-gtm)** skill.

## What does the champion tracking workflow do step by step?

**Priority:** P0-P1 \| 90-day decay \| 48h SLA \| Route to original AE

1

Pull CRM contacts

Extract contacts from HubSpot/Salesforce via MCP. Focus on closed-won
contacts, active champions, and power users from the last 24 months.

2

Enrich current company

Check each contact’s current employer against their CRM record using
LinkedIn profile data or enrichment providers.

3

Flag job changers

Identify contacts whose company no longer matches. Tag with previous
company, previous deal value, and relationship strength.

4

Find new emails

Run email waterfall for new company domain. Use the multi-provider waterfall
to maximize hit rate.

5

Push to CRM + sequence

Update CRM records with new company info, create a new contact record at the
new company, and trigger re-engagement campaign via MCP.

## Which sub-plays should I run for different champion scenarios?

Route each champion through the right sub-play based on their new company’s relationship with you.

- **1A: Champion moves to a new logo** — Not yet a customer, but the company fits your ICP. Send a personal congratulations within 48 hours. A warm intro from someone who already believes in your product is the best new-logo entry point.
- **1B: Champion moves to an existing customer** — Alert the CSM and AE immediately. The champion can push expansion from inside.
- **1C: Champion moves to an active deal** — Tell that AE today. The champion validates your product internally and can shorten the evaluation cycle.
- **1D: Key contact leaves an active deal** — A risk signal, not an opportunity. Map the remaining buying committee immediately. Single-threaded deals win at roughly 5% ( [Gong](https://www.gong.io/blog/data-shows-top-reps-dont-just-sell-they-orchestrate-with-ai)).

## What email template works for champion re-engagement?

Email template (Day 3 — Champion at new company)

> Subject: The move to \[New Company\]\[First Name\],Saw you joined \[New Company\] as \[Title\]. Good move.I keep thinking about what your team built at \[Previous Company\] — the \[specific result\]. I’d imagine the first 90 days at \[New Company\] are about \[rebuilding/scaling/standing up\]. If \[product\] makes sense there, I’m around.Either way, congrats.

**Timing matters:** Day 1-2 feels like a bot. Day 3-5 feels human. After day 7, you are one of many.

## When should I upgrade a champion signal to P0?

Stack champion tracking with other signals to prioritize your outreach queue.**Upgrade triggers:**

- Job change + new company raised funding = **P0** (see [Fundraising Signals](https://deepline.com/docs/plays/signal-fundraising))
- Job change + new company hiring relevant roles = **P0** (see [Hiring Signals](https://deepline.com/docs/plays/signal-hiring))
- Job change + previous deal was Lost = P2 (see [Closed-Lost Recovery](https://deepline.com/docs/plays/closed-lost-recovery))

## Frequently Asked Questions

### How often should I scan for job changes?

Weekly. Daily scans burn credits without meaningful lift since most job changes are detectable within a few days. Monthly is too slow. The 48-hour SLA matters because the first seller to reach out after a job change is **5x more likely to win** ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide)).

### What if the champion moved to a company that is too small or outside my ICP?

Still send the congratulations. Champions talk to other champions. Even if their new company is not a fit, they may refer you to peers at companies that are. Track the relationship in your CRM as “non-ICP champion” for future reference.

### Should I reassign the champion to a new AE or keep the original relationship?

Route to the original AE. The existing relationship is the whole point. If territory rules conflict, make an exception. Relationship continuity drives conversion.

### How do I handle a champion who left on bad terms?

If the champion churned or had a negative experience, flag for manager review before outreach. A bad re-engagement attempt burns the bridge permanently. Consider whether the product issues that caused the negative experience have been resolved.

### What CRM fields should I track for champion monitoring?

At minimum: original deal value, relationship strength (champion/user/evaluator), last interaction date, and original AE. These fields let you prioritize which job changes to act on first.

* * *

**Related:** [Hiring Signals](https://deepline.com/docs/plays/signal-hiring) \| [Fundraising Signals](https://deepline.com/docs/plays/signal-fundraising) \| [Deal Risk Signals](https://deepline.com/docs/plays/signal-deal-risk) \| [CRM Cleaning & Job Changes](https://deepline.com/docs/plays/crm-cleaning-job-changes)


---

## Play: `signal-company-news`

# Company News & Social Engagement Signals

When something happens at a target company (M&A, a new C-level hire, a regulatory event, social activity), the first seller to connect the news to a specific problem wins. Signal-based outreach gets **18% response rates** vs 3.4% for generic cold email ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide)). Both news and social engagement require monitoring external activity and acting within a tight window.

## How do I detect company news signals with Claude Code?

The workflow scans your target accounts for recent events, classifies the trigger type, and routes to the right contact with personalized context.In Claude Code, type:

```
/deepline-gtm Pull my target accounts from HubSpot, find recent M&A, executive hires, or regulatory news for each, classify the trigger type, find the right contact, and get their email
```

**With Codex:**

```
codex "Search my target accounts for M&A activity, new VP/C-level hires, and regulatory events in the last 30 days. Find the right decision maker for each trigger and get their work email."
```

## What does the company news workflow do step by step?

**Priority:** P2-P3 \| 45-day decay \| 48h for M&A/exec hires, 1-2 weeks for rest

1

Pull target accounts

Load accounts from CRM via MCP. News signals can surface at any account in
your ICP, so include the full list.

2

Search company news

Scrape recent news for M&A, executive hires, regulatory events, product
launches, and market expansion. Use Exa, Firecrawl, or Apollo news search.

3

Classify trigger type

AI categorizes each event: M&A, new exec, regulatory, product launch,
expansion, restructuring. Each type maps to a different decision-maker role
and message angle.

4

Find right contact

Match trigger type to the decision-maker role. M&A triggers go to CFO/COO.
New exec hires go to the new executive directly. Regulatory events go to
compliance or legal leadership.

5

Email waterfall

Resolve verified work email via multi-provider waterfall.

6

Push to sequence

Route with trigger context and personalized messaging via MCP. Include the
specific event in personalization fields.

## Which company events are fast triggers vs slow triggers?

| Trigger | SLA | Why it matters | Who to contact |
| --- | --- | --- | --- |
| **M&A announcement** | 48h | Two customer bases, two systems, and two compliance frameworks need unifying | CFO, COO, CTO |
| **New C-level/VP hire** | 48h | New leaders re-evaluate vendor stacks in their first 90 days | The new executive directly |
| **Regulatory event or fine** | 48h | Urgent remediation creates immediate budget and urgency | Head of Compliance, General Counsel |
| **Product launch** | 1 week | Scaling infrastructure, new operational needs | VP Product, VP Engineering |
| **Geographic expansion** | 2 weeks | New market = new compliance, new tools, new processes | VP Operations, Head of International |
| **Restructuring/layoffs** | 1 week | Vendor consolidation, cost optimization | CFO, VP Operations |

## What email template works for M&A triggers?

M&A email template

> Subject: Post-merger \[function\] — what I’ve seen work\[First Name\],Saw the \[Company\] + \[Acquired Company\] news.The hardest part of integrating two companies’ \[function\] stacks isn’t the tech. It’s the \[specific challenge\]. Two systems, two \[processes\], two sets of \[requirements\], one board that wants them unified by next year.We’ve walked several companies through this. The approach: centralize on one \[function\] layer first, migrate sequentially.If this is on the integration roadmap, happy to share the playbook.

## How do I use social engagement as a buying signal?

When someone at an ICP account engages with your content or posts about problems you solve, they are signaling interest. Respond within 24-48 hours while the topic is fresh.**Priority:** P3-P4 \| 45-day decay \| 24h for direct engagementIn Claude Code, type:

```
/deepline-gtm Find people on LinkedIn who posted about data enrichment or Clay alternatives this week, enrich their profiles, find their work emails, and add high-intent ones to my Instantly campaign
```

**With Codex:**

```
codex "Search LinkedIn for posts mentioning data enrichment challenges or sales intelligence tools from people at companies with 50-500 employees. Get their emails and push to outbound."
```

1

Define signal keywords

Set keywords: competitor names, problem terms, category phrases. Be
specific. “Data enrichment” beats “sales tools.”

2

Search LinkedIn posts

Find recent posts and comments matching your keywords. Look for original
posts (higher intent) over simple likes (lower intent).

3

Enrich engaged contacts

Pull full profiles and company data for engagers. Verify they are at ICP-fit
companies before spending enrichment credits.

4

Email waterfall

Resolve verified work emails via multi-provider waterfall.

5

Push to sequence

Route to outbound with topic-specific personalization via MCP. Reference the
topic, never the behavior.

## What are the rules for social engagement outreach?

**The golden rule: Reference the topic, never the behavior.**

| Approach | Example | Verdict |
| --- | --- | --- |
| **Good** | ”Your take on \[topic\] was interesting.” | References the idea |
| **Bad** | ”I noticed you liked our post at 2:47pm on Tuesday.” | Creepy surveillance |
| **Good** | ”I’ve been thinking about the same \[problem\] you wrote about.” | Peer-level engagement |
| **Bad** | ”I saw you commented on 3 posts about our competitor.” | Stalker energy |

Social signals are lower intent than job changes or funding, but they reveal what problems people are actively thinking about. Use that context to personalize, not to demonstrate that you are tracking their activity.

## Frequently Asked Questions

### How do I find company news across many accounts?

Combine multiple sources: Apollo news search for structured data, Exa for semantic search across news sites, Firecrawl for scraping industry publications. Set up weekly scans across your target account list.

### Should I reach out to the new executive or their predecessor?

The new executive. They are in evaluation mode during their first 90 days and looking for tools that help them make an impact quickly. The predecessor likely doesn’t have decision-making authority anymore.

### How do I monitor social engagement without being creepy?

Focus on public posts and comments, not private activity. Reference the topic or idea, never the specific engagement action. Wait at least 24 hours so it doesn’t feel like real-time surveillance. Have something genuinely useful to say about the topic.

### What if I find multiple news events for the same company?

Combine them into one outreach message. Never send separate touches for each signal. A company going through M&A while hiring a new CTO is stronger than either signal alone. See [Deal Risk & Signal Combos](https://deepline.com/docs/plays/signal-deal-risk) for combination plays.

### How do I prioritize between news types?

M&A and new executive hires are highest priority because they create immediate re-evaluation windows. Regulatory events are high priority if your product solves compliance problems. Product launches and expansions are medium priority; the need is real but less urgent.

* * *

**Related:** [Champion Tracking](https://deepline.com/docs/plays/signal-champion-tracking) \| [Hiring Signals](https://deepline.com/docs/plays/signal-hiring) \| [Fundraising Signals](https://deepline.com/docs/plays/signal-fundraising) \| [Competitive Intelligence](https://deepline.com/docs/plays/signal-competitive-intel)


---

## Play: `signal-competitive-intel`

# Competitive Intelligence Signals

Competitive intelligence is a monthly operating rhythm, not a one-time report. It tells you who you are winning and losing against, and gives you displacement plays to run when competitors stumble. Signal-based outreach gets **18% response rates** vs 3.4% for generic cold email ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide)). Displacement plays convert even higher because the prospect already has a defined problem and budget.

## How do I build a competitive intelligence workflow with Claude Code?

The workflow analyzes your CRM deal data to identify competitive patterns and monitors external signals for displacement opportunities.In Claude Code, type:

```
/deepline-gtm Analyze my closed deals in HubSpot from the last 6 months. Show me competitor frequency, win rate by competitor, which stage I tend to lose at, and flag any new competitors appearing for the first time.
```

**With Codex:**

```
codex "Pull all closed-won and closed-lost deals from HubSpot with competitor fields populated. Break down win rate by competitor, loss stage, and rep. Flag any competitor appearing for the first time."
```

## What should I analyze in my monthly competitive review?

**Priority:** P1-P2 \| Quarterly review cycle \| Monthly data pullRun this analysis monthly. The trends matter more than any single data point.

| Analysis | What to look for | Action |
| --- | --- | --- |
| **Competitor frequency** | Who is showing up more or less on deals? | Rising competitors need new battlecards. Declining ones may indicate market shifts. |
| **Win rate by competitor** | Getting better or worse against each one? | Declining win rates need immediate attention. Talk to reps who won recently. |
| **Loss stage** | Where do you lose? Discovery = positioning problem. Proposal = pricing problem. | Adjust sales process at the failing stage. |
| **Rep performance** | Who is strong/weak against which competitor? | Pair weak reps with strong ones for coaching. Route competitive deals to your best closer. |
| **New competitors** | Anyone appearing for the first time? | Research immediately. Get a demo. Build a preliminary battlecard within 2 weeks. |

## How do I detect displacement opportunities?

When a competitor stumbles, their customers are briefly open to alternatives. Act within the window or lose it.In Claude Code, type:

```
/deepline-gtm Search for news about [competitor] layoffs, outages, data breaches, or pricing changes in the last 30 days. Cross-reference against my target accounts that use [competitor]. Find the right contact and get their email.
```

**With Codex:**

```
codex "Find companies using [competitor] that are showing dissatisfaction signals — search for social posts mentioning [competitor] problems, check for recent [competitor] outages, and find decision maker emails at affected companies."
```

## What are the key displacement triggers?

| Trigger | Window | Play |
| --- | --- | --- |
| **Competitor layoffs** | 2-4 weeks | Layoffs mean reduced support, slower roadmap, and worried customers. Any customer showing a signal is a displacement target. |
| **Competitor outage or breach** | 48 hours | Act fast. Reference reliability and security without being predatory. “We built \[specific architecture\] to prevent this” works better than “Did you see their outage?” |
| **Competitor pricing increase** | 2-6 weeks | Customers talking about price hikes on social media or in industry forums are actively signaling. Lead with total cost of ownership, not just lower price. |
| **Competitor acquisition** | 4-8 weeks | Acquisitions create product uncertainty and roadmap changes. Customers worry about being deprioritized. |
| **Customer hiring while using a competitor** | 2-4 weeks | They are either switching or building in-house. Either way, they are evaluating alternatives. |

## What does a displacement outreach sequence look like?

1

Monitor competitor signals

Set up keyword monitoring for competitor name + negative terms (outage,
breach, pricing, layoffs, acquisition). Use Exa search, social monitoring,
and news scraping.

2

Cross-reference with target accounts

Match affected companies against your target account list and any accounts
known to use the competitor. CRM competitor fields and technographic data
help here.

3

Find the right contact

For competitor-related signals, target the person most affected: the primary
user (for outages), the budget owner (for pricing), or the executive sponsor
(for strategic concerns).

4

Email waterfall

Resolve verified work email via multi-provider waterfall.

5

Deploy battlecard-informed outreach

Use competitor-specific battlecard messaging. Lead with your specific
advantage that addresses the competitor’s weakness, not generic displacement
language.

Displacement email template (competitor outage)

> Subject: \[Function\] reliability — how we built differently\[First Name\],I know this week has been rough for teams running on \[Competitor\].We built \[Product\] with \[specific architecture decision\] specifically to avoid single points of failure in \[function\]. Our uptime over the last 12 months is \[stat\]. Not because we are lucky — because of \[specific technical choice\].If reliability is becoming a priority for your team, I can walk you through the architecture in 15 minutes.

Displacement email template (competitor pricing increase)

> Subject: \[Function\] costs going up?\[First Name\],Been hearing from a few \[Competitor\] customers about the pricing changes.We price \[differently — usage-based / flat-rate / per-seat with no overages\]. For a team your size, the math usually works out to \[X\]% less than what teams were paying on \[Competitor\]‘s new pricing.Happy to run the numbers if it would be useful.

## Frequently Asked Questions

### How do I know which accounts use a specific competitor?

Three sources: (1) CRM competitor fields from past deals, (2) technographic data from providers like Crustdata or BuiltWith, (3) social signals where employees mention the competitor’s product. Combine all three for better coverage.

### How aggressive should displacement outreach be?

Don’t be predatory. Reference your strengths, not their failures. “We built X to prevent Y” works. “Did you see their outage?” does not. Prospects who feel you are exploiting their vendor’s problems will distrust you.

### How often should I run the competitive review?

Monthly data pull, quarterly deep review. The monthly pull catches trends early. The quarterly review is where you adjust battlecards, re-train reps, and update competitive positioning.

### What if a new competitor appears that I have never seen before?

Get a demo within 2 weeks. Build a preliminary battlecard with positioning, pricing (if public), and differentiators. Talk to any reps who encountered them. By the time a competitor shows up on 3+ deals, they are established, so treat first appearances seriously.

### Should I track competitor job postings too?

Yes. Competitor hiring patterns reveal strategic direction. If they are hiring heavily in a market you serve, expect more competition. If they are cutting in a segment, that is a displacement opportunity. Cross-reference with [Hiring Signals](https://deepline.com/docs/plays/signal-hiring) for the full workflow.

* * *

**Related:** [Champion Tracking](https://deepline.com/docs/plays/signal-champion-tracking) \| [Company News & Social](https://deepline.com/docs/plays/signal-company-news) \| [Deal Risk & Signal Combos](https://deepline.com/docs/plays/signal-deal-risk) \| [Win-Loss Signals](https://deepline.com/docs/plays/win-loss-signals)


---

## Play: `signal-deal-risk`

# Deal Risk Signals & Signal Combination Plays

Six CRM signals catch pipeline rot before it shows up in forecast calls. Single-threaded deals win at roughly **5%**; deals with 5+ stakeholders reach **30%** ( [Gong](https://www.gong.io/blog/data-shows-top-reps-dont-just-sell-they-orchestrate-with-ai), 1.8M opportunities). Combining multiple buying signals into one message also converts well. Signal-based outreach gets **18% response rates** vs 3.4% for generic cold email ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide)).

## How do I detect deal risk signals with Claude Code?

The workflow scans your active pipeline for risk patterns and flags deals that need immediate attention.In Claude Code, type:

```
/deepline-gtm Analyze my active deals in HubSpot. Flag deals with no meeting in 14+ days, deals with only one contact on calls, deals that stalled in the same stage for 2x the average, and any deal with a close date pushed more than once.
```

**With Codex:**

```
codex "Pull all open deals from HubSpot pipeline. Score each deal for risk: check meeting cadence, contact count, stage duration vs average, close date history, and competitor presence. Output a ranked risk report."
```

## What are the six deal risk signals?

**Priority:** P0-P1 \| 30-day decay \| 24h SLA

| Signal | Trigger | Action |
| --- | --- | --- |
| **Gone cold** | No meeting in 14+ days after active cadence | Re-engage with a value touchpoint. Never “just checking in.” Share a case study, industry insight, or ROI calculation. |
| **Single-threaded** | Only one contact on calls | Multi-thread immediately. Single-threaded deals win at ~5%; 5+ stakeholders reaches ~30% ( [Gong](https://www.gong.io/blog/data-shows-top-reps-dont-just-sell-they-orchestrate-with-ai), 1.8M opportunities). Find other stakeholders via enrichment. |
| **Competitor entered** | New competitor mentioned on deal | Deploy battlecard. Lead with your specific advantage, not generic positioning. See [Competitive Intelligence](https://deepline.com/docs/plays/signal-competitive-intel). |
| **Stage stall** | Deal in same stage 2x longer than average | Re-qualify. Ask directly: “What needs to happen to move forward?” If they cannot answer, the deal may not be real. |
| **Close date pushed again** | Second or more push | Repeated pushes signal a qualification problem, not a timing problem. Re-qualify or close it out. Zombie deals corrupt your forecast. |
| **Big deal, no activity** | $200K+ ACV, no stage change in 30+ days | Biggest forecast risk. Weekly check-in required. Escalate to VP Sales if no movement after direct outreach. |

## How should I escalate based on risk stacking?

The number of simultaneous risk signals on a deal determines the escalation level.

| Risk signals | Escalation | Response |
| --- | --- | --- |
| **1 signal** | AE handles | Standard risk mitigation. AE addresses the specific signal within 24h. |
| **2 signals** | AE + manager weekly check | Manager reviews the deal weekly. AE presents a specific plan to address both risk signals. |
| **3+ signals** | VP Sales gets involved today | This deal is at serious risk. VP Sales reviews the account, may join the next call, and decides whether to invest more or cut losses. |

## How do I combine multiple buying signals into one outreach?

Don’t send separate touches for each signal. Combine them into one contextual message.

| Play | Signal combo | Approach |
| --- | --- | --- |
| **The Re-Entry** | Champion move + Funding | Former buyer at company that just raised. Personal note referencing both the move and the round. |
| **The Build** | Funding + Hiring | Raised B+ and hiring relevant roles. Deploy product before hires start — the “sequencing” angle. |
| **The Rescue** | Deal Risk + Contact Departure | Primary contact left an active deal. Map remaining committee immediately. See [Champion Tracking](https://deepline.com/docs/plays/signal-champion-tracking). |
| **The Displacement** | Competitor News + Any Signal | Prospect uses competitor that just had bad news. Any additional signal makes this a top-priority target. |
| **The Regulatory Push** | Any Signal + Deadline | Any ICP account showing a signal near a regulatory deadline. Urgency is built in. |

In Claude Code, type:

```
/deepline-gtm Check my target accounts for signal combinations: companies where a former champion moved AND they recently raised, or companies where they are hiring AND had recent M&A news. Rank by signal strength and find decision maker emails.
```

**With Codex:**

```
codex "Cross-reference my champion job change list with recent funding data. Find accounts that have BOTH signals. Get the champion's new email and push to my P0 outbound sequence."
```

## What are the rules for signal-based account management?

These rules prevent over-contacting accounts.

1. **Max 1 signal outreach per account per 14 days.** Multiple signals on the same account go into one message, not multiple touches.
2. **Combine signals into ONE message.** Never send separate emails for each signal. The combined context makes the outreach land.
3. **30-day cool-off after a reply or meeting booked.** Once you get engagement, switch to the sales process. Stop signal-triggered outreach.
4. **3+ signals per month on the same account = manual AE review.** High signal density either means the account is genuinely hot or your signal definitions are too broad. AE reviews before any outreach.

## What does the deal risk workflow do step by step?

1

Pull active pipeline

Load all open deals from CRM via MCP. Include deal value, stage, close date,
contact associations, and activity history.

2

Calculate stage benchmarks

Compute average time-in-stage for each pipeline stage across all won deals.
This becomes the baseline for stall detection.

3

Score each deal for risk

Check each deal against the six risk signals. Count the number of active
signals and assign a risk score.

4

Enrich single-threaded deals

For deals with only one contact, use enrichment to find additional
stakeholders at the company. Look for the economic buyer, technical
evaluator, and end user.

5

Generate risk report

Output a ranked list of at-risk deals with specific signals, recommended
actions, and escalation level.

6

Push alerts to CRM

Update deal records with risk scores and create tasks for AEs and managers
via MCP.

## Frequently Asked Questions

### How often should I run deal risk analysis?

Weekly. Pipeline health changes fast. Set up a Monday morning scan so AEs start the week knowing which deals need attention.

### What is the best way to re-engage a cold deal without “just checking in”?

Share something specific: a case study, a new feature that addresses their stated concern, an industry benchmark, or a warm intro to a peer who solved the same problem. Give them a reason to respond, not just a reminder that you exist.

### How do I multi-thread a single-threaded deal?

Use enrichment to find other stakeholders at the company. Look for the economic buyer (usually a VP or C-level), a technical evaluator (usually a manager or director), and the end user. Reach out to each with role-specific messaging. Ask your existing contact for introductions — “Who else on your team should be involved in this evaluation?”

### When should I kill a deal instead of trying to save it?

When you see 3+ risk signals, the close date has been pushed twice, and the primary contact is unresponsive. Zombie deals corrupt your forecast and waste AE time. Close it out, note the reason, and set a re-engagement reminder for 90 days.

### How do I prevent over-contacting high-signal accounts?

Follow the 14-day rule: max one signal-triggered outreach per account per 14 days. If multiple signals fire in the same window, combine them into one message. After any positive response (reply or meeting), switch to the regular sales cadence and stop signal-triggered outreach for 30 days.

* * *

**Related:** [Champion Tracking](https://deepline.com/docs/plays/signal-champion-tracking) \| [Hiring Signals](https://deepline.com/docs/plays/signal-hiring) \| [Fundraising Signals](https://deepline.com/docs/plays/signal-fundraising) \| [Competitive Intelligence](https://deepline.com/docs/plays/signal-competitive-intel) \| [Win-Loss Signals](https://deepline.com/docs/plays/win-loss-signals)


---

## Play: `signal-fundraising`

# Fundraising Signals

Funding means budget. The first seller to reach out after a trigger event is **5x more likely to win** ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide)). Timing matters more with fundraising than other signals: week 1 is noise, weeks 2-8 is the window, after week 8 decisions may already be made. Signal-based outreach gets **18% response rates** vs 3.4% for generic cold email ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide)).

## How do I detect fundraising signals with Claude Code?

The workflow pulls your target accounts, checks for recent funding rounds, extracts round details, and routes funded companies to outbound.In Claude Code, type:

```
/deepline-gtm Pull my target accounts from HubSpot, find which ones raised funding recently, extract the round details, find the CRO or VP Sales contact with email, and add Series B+ companies to my outbound sequence
```

**With Codex:**

```
codex "Search my target account list for companies that raised Series A or later in the last 60 days, find the VP of Sales or CRO, get their email, and push to my Instantly campaign"
```

## What does the fundraising signal workflow do step by step?

**Priority:** P1-P2 \| 6-month decay \| 48h SLA for B+, 1 week for Seed/A

1

Pull target companies

Load accounts from CRM via MCP. You can also run this against a broader ICP
list to discover new accounts that just raised.

2

Search funding news

Check each company for recent funding rounds via Crustdata, Apollo, or news
scraping. Filter by round type and recency.

3

Extract round details

AI extracts round type, amount, lead investor, and date. Mentioning the lead
investor in your outreach shows you did your homework.

4

Find decision makers

Identify CRO, VP Sales, or Head of \[function\] at funded companies.
Post-funding, these roles are making tool decisions and have fresh budget.

5

Email waterfall

Resolve verified work emails via multi-provider waterfall.

6

Push to sequence

Route to outbound sequence with funding context via MCP. Include round type,
amount, and investor in personalization fields.

## What is the best timing window for post-funding outreach?

Here is the week-by-week breakdown for post-funding outreach timing.

| Window | What is happening | Your move |
| --- | --- | --- |
| **Week 1** | Everyone congratulating them. Inbox flooded. | Do not reach out. You will be noise. |
| **Week 2-3** | Budget conversations starting. Leadership planning spend. | **This is the window.** Reach out now. |
| **Week 4-8** | Implementation planning. Vendor shortlists forming. | Still good. Lead with ROI and speed-to-value. |
| **After week 8** | Decisions may already be made. Contracts in review. | Lower priority. Only reach out if you have a strong differentiator. |

The sweet spot is **days 10-21 after announcement**. Before that, you are one of hundreds. After that, you are late.

## How should I prioritize by round type?

| Round | Priority | Why |
| --- | --- | --- |
| **Series C+** | P1 | Large budget, scaling fast, buying enterprise tools |
| **Series B** | P1 | Product-market fit confirmed, building GTM infrastructure |
| **Series A** | P2 | Budget exists but may be tighter, longer sales cycles |
| **Seed** | P3 | Budget is limited, but you are establishing the relationship early |
| **Growth/PE** | P1 | Often triggers vendor consolidation and new tool purchases |

## What email template works for post-funding outreach?

Email template (post-Series B)

> Subject: Post-Series B \[function\] stack — what \[peer\] got wrong\[First Name\],Congrats on the round. \[Investor\] knows the space.One thing I keep seeing with post-B companies: the \[function\] stack that worked at 10K users breaks at 100K. Manual \[process\] spikes, \[problem\] shows up, the team ends up firefighting.We handle \[function\] for \[impressive customer list\]. The math at your stage: less \[problem A\], less \[problem B\], less manual work.If the \[function\] stack is on the post-funding roadmap, worth 15 minutes.

## Frequently Asked Questions

### Where do I find funding data?

Crustdata and Apollo both provide funding round data. For broader coverage, combine with news scraping via Firecrawl or Exa search. Crunchbase has the widest coverage but requires a separate subscription. Deepline’s enrichment providers cover most Series A+ rounds within 48 hours of announcement.

### Should I mention the funding amount in my email?

Only if it is public. Mentioning the lead investor works better. It shows awareness without being presumptuous. “Congrats on the round. \[Investor\] knows the space” lands better than “Congrats on the $50M raise.”

### How do I handle companies that raised but are not in my target list?

Use funding signals as a discovery mechanism. Run a weekly scan for companies in your ICP vertical that raised in the last 14 days, regardless of whether they are in your CRM. This is how you find net-new accounts with confirmed budget.

### What if a company raised but is already a customer?

Route to the CSM, not outbound. Post-funding is a natural expansion moment. The CSM should propose upsell or additional seats before the customer starts evaluating alternatives with their new budget.

### How do I combine fundraising with other signals?

Funding + champion move is a high-converting combination. A former buyer at a company that just raised is a P0 target: they have budget, they know your product, and they have political capital as a new hire. See [Champion Tracking](https://deepline.com/docs/plays/signal-champion-tracking) for the full workflow.

* * *

**Related:** [Champion Tracking](https://deepline.com/docs/plays/signal-champion-tracking) \| [Hiring Signals](https://deepline.com/docs/plays/signal-hiring) \| [Company News & Social](https://deepline.com/docs/plays/signal-company-news) \| [Deal Risk & Signal Combos](https://deepline.com/docs/plays/signal-deal-risk)


---

## Play: `signal-hiring`

# Hiring Signals — New Roles

When a company posts jobs for roles that use or evaluate your product category, the budget is approved and the problem is acknowledged. New hires convert at **2.5x higher rates** in their first 3 months ( [UserGems](https://www.usergems.com/blog/signal-based-marketing)). Hiring signals decay slower than most triggers, so you get a wider outreach window. The first seller to reach out is still **5x more likely to win** ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide)).

## How do I detect hiring signals with Claude Code?

The workflow pulls your target accounts, searches for relevant job postings, scores signal strength, and routes high-scorers to outbound.In Claude Code, type:

```
/deepline-gtm Pull my target accounts from HubSpot, find which ones are hiring GTM Engineers or RevOps roles, score the signal strength, find the decision maker's email, and add high-scorers to my Instantly campaign
```

**With Codex:**

```
codex "Check my target account list for companies hiring data engineering or RevOps roles, find the hiring manager's email, and push top matches to my outbound sequence"
```

Uses the **[deepline-gtm](https://github.com/getaero-io/gtm-eng-skills)** skills.

## What does the hiring signal workflow do step by step?

**Priority:** P2-P3 \| 90-day decay \| 1-week SLA

1

Pull target companies

Load target account list from CRM via MCP. Filter to ICP-fit accounts to
avoid wasting enrichment credits on non-targets.

2

Search job postings

Find open roles at each company matching your category. Use job board APIs
or web scraping to pull current postings.

3

AI scores signal strength

Score relevance on a 3-tier scale: exact role match (e.g., “RevOps Manager”
for a RevOps tool) is highest priority. Adjacent role (e.g., “Sales
Operations”) is medium. Generic hiring surge is lowest but still worth
tracking.

4

Find decision maker

Identify the hiring manager or department head. The person who wrote the job
description is often the budget holder.

5

Email waterfall

Resolve verified work email via multi-provider waterfall. Cross-reference
with LinkedIn profile data for accuracy.

6

Push to sequence

Route high-scorers to outbound sequence via MCP. Include the job posting URL
and specific role in the personalization fields.

## How should I score hiring signal strength?

Score postings to prioritize your outreach queue.

| Score | Signal | Example | SLA |
| --- | --- | --- | --- |
| **High** | Exact role match | ”Data Enrichment Engineer” for an enrichment tool | 48h |
| **Medium** | Adjacent role | ”RevOps Manager” for a sales intelligence tool | 1 week |
| **Low** | Hiring surge | 10+ roles posted in a month across the org | 2 weeks |
| **Bonus** | Role mentions your category | Job description says “experience with Clay/Apollo/ZoomInfo” | 24h |

The bonus tier is the most actionable. If the job description names your category or competitors, the company has already defined the problem and is evaluating solutions.

## What email template works for hiring signals?

Email template

> Subject: Scaling \[function\] ops — what tripped up \[Peer Company\]\[First Name\],Noticed \[Company\] is building out the \[function\] team.I’ve watched a dozen companies go through the same buildout. The pattern: hire the team, then buy the tools. The team spends 3-6 months doing manual \[task\] while evaluating vendors.\[Peer Company\] did it differently. They deployed \[product\] before the new hires started. New hires focused on \[high-value work\] from day one.Might be worth a 15-minute conversation about sequencing.

## Frequently Asked Questions

### How do I find job postings across many accounts?

Use Deepline’s integration with job board APIs (Forager, Apollo job postings, or Crustdata job listings) to search across your target account list. For companies not covered by structured APIs, Firecrawl or Apify can scrape careers pages directly.

### Should I reach out to the new hire or the hiring manager?

The hiring manager or department head. They control budget and tool decisions. The new hire won’t have context or authority in their first weeks. After 60-90 days, the new hire becomes a secondary contact.

### What if the job posting is old?

Job postings older than 60 days are weaker signals. The role may already be filled. Focus on postings from the last 30 days. If a posting was recently refreshed or reposted, treat it as fresh; that signals urgency.

### How do I differentiate from competitors who also monitor job postings?

Speed and specificity. Reference the exact role title and a specific challenge that role will face. Generic “I see you’re hiring” emails are transparent and ignored. Lead with an insight about what tripped up similar companies during the same buildout phase.

### Can I combine hiring signals with other triggers?

Yes. Hiring + funding is a strong combination. A company that just raised and is now hiring for your category has confirmed budget and confirmed the problem. See [Signal Combination Plays](https://deepline.com/docs/plays/signal-deal-risk) for the full playbook.

* * *

**Related:** [Champion Tracking](https://deepline.com/docs/plays/signal-champion-tracking) \| [Fundraising Signals](https://deepline.com/docs/plays/signal-fundraising) \| [Company News & Social](https://deepline.com/docs/plays/signal-company-news) \| [Competitive Intelligence](https://deepline.com/docs/plays/signal-competitive-intel)


---

## Play: `signal-playbooks`

# Signal Playbooks

Signal-based outreach gets 18% response rates vs 3.4% for generic cold email ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide)). The first seller to reach out after a trigger event is 5x more likely to win ( [Autobound](https://www.autobound.ai/blog/signal-based-selling-complete-guide)).Each recipe below detects a buying signal, finds the right contact, and gives you an opener.

## Signal Recipes

| Signal | What it detects | Priority | Recipe |
| --- | --- | --- | --- |
| **Champion Tracking** | Former buyer/champion moves to a new company | P0-P1 | [Champion Tracking](https://deepline.com/docs/plays/signal-champion-tracking) |
| **Hiring Signals** | Company posts jobs for roles that use your product category | P2-P3 | [Hiring Signals](https://deepline.com/docs/plays/signal-hiring) |
| **Fundraising** | Company raises a funding round | P1-P2 | [Fundraising Signals](https://deepline.com/docs/plays/signal-fundraising) |
| **Company News & Social** | M&A, executive hires, regulatory events, social engagement | P2-P3 | [News & Social](https://deepline.com/docs/plays/signal-company-news) |
| **Competitive Intel** | Competitor stumbles, displacement opportunities | P1-P2 | [Competitive Intelligence](https://deepline.com/docs/plays/signal-competitive-intel) |
| **Deal Risk & Combos** | Pipeline risk signals and multi-signal combination plays | P0-P1 | [Deal Risk & Combos](https://deepline.com/docs/plays/signal-deal-risk) |

## Related Recipes

| Recipe | What it does |
| --- | --- |
| [CRM Cleaning & Job Changes](https://deepline.com/docs/plays/crm-cleaning-job-changes) | Bulk CRM hygiene — detect stale contacts, update job changes, fix data decay |
| [Win-Loss Signals](https://deepline.com/docs/plays/win-loss-signals) | Post-deal analysis to improve competitive positioning and sales process |
| [Closed-Lost Recovery](https://deepline.com/docs/plays/closed-lost-recovery) | Re-engage lost deals when new signals indicate timing has changed |
| [Account Scoring](https://deepline.com/docs/plays/account-scoring) | Multi-signal account scoring to prioritize your target list |

## Sources

All stats across the signal recipes come from these sources:

- [Champify 2024 Impact Report](https://www.champify.io/resources/the-impact-of-tracking-job-changes-value-report) — 230K champions, 37% vs 19% win rate
- [UserGems: Signal-Based Marketing](https://www.usergems.com/blog/signal-based-marketing) — 3x champion conversion, 2.5x new hire conversion
- [Autobound: Signal-Based Selling](https://www.autobound.ai/blog/signal-based-selling-complete-guide) — 18% response rate, 5x first-mover advantage
- [Gong](https://www.gong.io/blog/data-shows-top-reps-dont-just-sell-they-orchestrate-with-ai) — 1.8M opportunities, 5% vs 30% win rate by threading
- [Cognism: Data Decay](https://www.cognism.com/blog/data-decay) — 30% annual job switching

View all skills on [GitHub](https://github.com/getaero-io/gtm-eng-skills)


---

## Play: `task-email-waterfall-with-validation`

# Email Waterfall with Validation

Deepline’s cost-aware email waterfall tries deterministic email patterns with free validation first, falls back to 5 paid providers (Dropleads, Hunter, LeadMagic, Deepline Native, PeopleDataLabs) if no pattern matches, and optionally verifies deliverability with ZeroBounce. Pattern matches resolve for 0.1 credits.According to industry benchmarks, waterfall enrichment across multiple providers can improve email coverage by 20-40% compared to any single provider ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)). You can chain a ZeroBounce validation step after the waterfall to verify deliverability. B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), so the verification step catches stale emails that would otherwise bounce.

## How do I find and validate emails with Claude Code?

In Claude Code, type:

```
/deepline-gtm Find and validate emails for leads.csv -- use the cost-aware approach
```

```
/deepline-gtm I want verified emails, not just found emails. Run the full waterfall with validation.
```

**With Codex:**

```
codex "Find and validate work emails for leads.csv using cost-aware waterfall"
```

## What does the email waterfall with validation do step by step?

The cost-aware waterfall runs in three phases: free pattern validation, paid provider fallback, and deliverability verification. Most lookups resolve in Phase 1 at 0.1 credits. The paid fallback only fires when no pattern validates.

1

Try free email patterns

Deepline generates 3 deterministic patterns (first.last@domain,
firstlast@domain, first\_last@domain) and validates each with LeadMagic’s
free validation.

2

Fall through to paid providers if needed

If no pattern validates, the standard waterfall fires: Dropleads, Hunter,
LeadMagic, Deepline Native, PeopleDataLabs.

3

Optionally validate deliverability

Chain `zerobounce_validate` after the waterfall for bounce-risk scoring —
checking deliverability, not just syntax.

4

Return verified email with status

The email is returned with a deliverability status: valid, catch-all, or
invalid.

## Which providers does the waterfall try and in what order?

The `name_and_domain_to_email_waterfall` runs in three phases, ordered cheapest-first:**Phase 1 — Free patterns:**

1. Generate `first.last@domain`, `firstlast@domain`, `first_last@domain`
2. Validate each with LeadMagic (free validation tier)
3. If one validates, skip paid providers entirely

**Phase 2 — Paid fallback (if no pattern matched):**
Dropleads, Hunter, LeadMagic, Deepline Native, PeopleDataLabs**Phase 3 — Optional deliverability check:**
Chain `zerobounce_validate` on the found email for bounce-risk scoring

| Scenario | Typical cost |
| --- | --- |
| Pattern matches (most common) | **0.1 credits** |
| First paid provider hits | 0.3-0.4 credits |
| Full waterfall + validation | Up to 7.2 credits |

CLI command

```
deepline enrich --input leads.csv --output verified.csv \
  --with '{"alias":"email","tool":"name_and_domain_to_email_waterfall","payload":{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"{{domain}}"}}'
```

The cost-aware waterfall saves the most on well-known domains (Google, Stripe,
etc.) where email patterns are predictable. For smaller companies with
non-standard patterns, it falls through to paid providers.

* * *

**Related tasks:** [Find Work Email](https://deepline.com/docs/plays/task-find-work-email) \| [Find Personal Email](https://deepline.com/docs/plays/task-find-personal-email) \| [Build Prospect List](https://deepline.com/docs/plays/play-build-prospect-list)

## Frequently Asked Questions

### What is the difference between Find Work Email and Email Waterfall with Validation?

Find Work Email finds an email using a 6-provider waterfall. Email Waterfall with Validation adds two things: free email pattern matching first (0.1 credits when patterns match), and optional ZeroBounce deliverability verification. Use Find Work Email for speed. Use Email Waterfall with Validation when you need deliverability guarantees.

### How much does the email waterfall with validation cost?

Most lookups resolve via free pattern matching at 0.1 credits ($0.008). If patterns fail, the first paid provider typically resolves at 0.3-0.4 credits. Worst case across all providers plus ZeroBounce verification: 7.2 credits. The cost-aware approach saves the most money on well-known domains with predictable email patterns.

### Can I find and validate emails in bulk from a CSV?

Yes. type `/deepline-gtm find and validate emails for leads.csv` and Deepline reads your column headers (first\_name, last\_name, domain), selects the cost-aware waterfall, and enriches every row with a verified email and deliverability status. Always pilot with 3-5 rows first.

### What does the deliverability verification check?

ZeroBounce checks whether the email address is deliverable, not just syntactically valid. It returns: valid (safe to send), catch-all (domain accepts all addresses, delivery uncertain), or invalid (will bounce). This matters for sender reputation in outbound campaigns.

### What data do I need for the email waterfall with validation?

First name, last name, and domain (e.g., “Sarah”, “Chen”, “stripe.com”). The waterfall uses these to generate email patterns before trying paid providers. Company name doesn’t change behavior here — this waterfall is built for the name + domain input combination.


---

## Play: `task-enrich-company`

# Enrich Company

Deepline’s company enrichment queries Apollo, Crustdata, and PeopleDataLabs, then coalesces their responses into a single profile with 30+ fields: revenue, employee count, industry, funding, tech stack, social profiles, and more. One natural language prompt returns everything — no manual provider configuration required.
If Apollo has revenue but not tech stack, and Crustdata has tech stack, both fields appear in the output.B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)). Querying 3 providers means stale data from one gets supplemented by fresher data from another.

## How do I enrich companies with Claude Code?

In Claude Code, type:

```
/deepline-gtm Enrich these companies with revenue, employee count, industry, and funding data
```

```
/deepline-gtm I have a list of domains -- get me full company profiles
```

```
/deepline-gtm What's the revenue and employee count for stripe.com?
```

**With Codex:**

```
codex "Enrich companies in companies.csv with revenue, employee count, and industry"
```

## What does company enrichment return step by step?

Deepline resolves your domain or company name to a canonical record, queries multiple providers, and coalesces responses into one profile. According to industry benchmarks, waterfall enrichment can improve coverage by 20-40% compared to any single provider ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)).

1

Resolve company identity

Deepline matches your domain or company name to a canonical company record.

2

Enrich across providers

Multiple providers are queried and their responses are coalesced into a
unified profile.

3

Return unified fields

A single enriched record is returned with all available fields filled from
the best source.

## Which providers and fields does company enrichment cover?

Deepline queries 3 providers and merges their data: Apollo enrich-company, Crustdata company-enrichment, and PeopleDataLabs company-enrich. If Apollo has revenue but not tech stack, and Crustdata has tech stack, both fields appear in the output.Returned fields include: revenue, employee\_count, industry, founded\_date, funding\_total, funding\_stage, latest\_funding\_round, valuation, tech\_stack, social\_profiles (Twitter, Facebook, LinkedIn, GitHub, Instagram), SIC/NAICS codes, headquarters address, and more.Deepline returns all available fields in one call — no need to configure separate columns or templates for each data point.

CLI command

```
# Single lookup
deepline tools execute enrich_company_finder \
  --payload '{"domain":"stripe.com"}'

# Bulk CSV
deepline enrich --input companies.csv --output companies-enriched.csv \
  --with '{"alias":"company","tool":"enrich_company_finder","payload":{"domain":"{{domain}}"}}'
```

A single call can return 30+ fields. When using Claude Code, describe which
fields you care about and it extracts the relevant ones.

* * *

**Related tasks:** [Find Decision Makers](https://deepline.com/docs/plays/task-find-decision-makers) \| [Find Work Email](https://deepline.com/docs/plays/task-find-work-email) \| [Build Prospect List](https://deepline.com/docs/plays/play-build-prospect-list)

## Frequently Asked Questions

### How much does it cost to enrich a company with Deepline?

Company enrichment queries multiple providers per lookup. You pay for each provider that runs, but the coalesced result gives you 30+ fields in one call.

### Can I enrich companies in bulk from a CSV?

Yes. type `/deepline-gtm enrich companies in companies.csv` and Deepline reads your domain column, runs company enrichment for each row, and writes back a unified profile. You can also use the `deepline enrich` CLI command with `--input` and `--output` flags. Always pilot with 3-5 rows first.

### What data do I need to enrich a company?

A domain (e.g., “stripe.com”) is the most reliable identifier. Company names work too, but domain-based lookups have higher match rates because domains are unique. If you have both, provide both.

### What fields does company enrichment return?

A single call returns 30+ fields: revenue, employee\_count, industry, founded\_date, funding\_total, funding\_stage, latest\_funding\_round, valuation, tech\_stack, social profiles (Twitter, Facebook, LinkedIn, GitHub, Instagram), SIC/NAICS codes, and headquarters address. Not every field is available for every company — Deepline returns whatever the providers have.

### Why use Deepline for company enrichment?

Ask for company data in plain English from your IDE or terminal. Deepline queries Apollo, Crustdata, and PeopleDataLabs in parallel and merges results into a single record — no UI configuration or manual provider setup. Pulling from 3 providers also keeps data fresher, since B2B contacts decay at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)).


---

## Play: `task-find-decision-makers`

# Find Decision Makers

Deepline’s contact-by-role waterfall searches Dropleads, Apollo, Icypeas, Prospeo, and Crustdata, accumulating results until a minimum threshold is met (default: 3 contacts). Research on 1.8M sales opportunities shows single-threaded deals close at roughly 5%, while engaging 5+ stakeholders reaches roughly 30% ( [Gong](https://www.gong.io/blog/data-shows-top-reps-dont-just-sell-they-orchestrate-with-ai)).You specify the role you want (e.g., “VP Engineering”) and Deepline expands it to VP, Director, Head of, SVP, and other seniority variations. Just describe the roles you’re targeting in natural language — no template selection or UI configuration required.

## How do I find decision makers with Claude Code?

In Claude Code, type:

```
/deepline-gtm Find the VP of Sales and Head of Marketing at each company in targets.csv
```

```
/deepline-gtm Who are the decision makers for IT security at these companies?
```

```
/deepline-gtm Find 3 engineering leaders at stripe.com
```

**With Codex:**

```
codex "Find VP of Sales and Head of Marketing at each company in targets.csv"
```

## What does the decision maker search do step by step?

Deepline reads your company list and target roles, searches providers until the minimum result threshold is met, filters by role and seniority, and returns matching contacts. B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), so the 5-provider approach helps you find current stakeholders instead of stale records.

1

Parse target companies and roles

Deepline reads your company list and the roles you’re looking for.

2

Search across providers

The company\_to\_contact\_by\_role waterfall fires, trying each provider until
min\_results (default 3) are found.

3

Match by role and seniority

Results are filtered to match your requested titles, including expanded
variations.

4

Return contact list

Matching contacts are returned with name, title, LinkedIn URL, and company
info.

## Which providers does the decision maker waterfall use?

The decision maker waterfall uses 5 providers in sequence: Dropleads, Apollo, Icypeas, Prospeo, and Crustdata. Unlike email waterfalls that stop at the first valid hit, this waterfall accumulates results across providers until `min_results` is met (default: 3 contacts).“VP Sales” expands to VP, Director, Head of, SVP, and other seniority variations. You don’t need to specify every title variant.

CLI command

```
deepline tools execute company_to_contact_by_role_waterfall \
  --payload '{"domain":"stripe.com","role":"VP Engineering","min_results":3}'
```

Combine this with the email waterfall for a complete prospecting flow: find
decision makers first, then enrich each contact with verified work emails.

* * *

**Related tasks:** [Find Work Email](https://deepline.com/docs/plays/task-find-work-email) \| [Enrich Company](https://deepline.com/docs/plays/task-enrich-company) \| [Build Prospect List](https://deepline.com/docs/plays/play-build-prospect-list)

## Frequently Asked Questions

### How do I find decision makers at a company using an API?

Use Deepline’s `company_to_contact_by_role_waterfall` tool with the company domain and target role. Deepline searches Dropleads, Apollo, Icypeas, Prospeo, and Crustdata, expanding the role to include seniority variations. Call it via the CLI (`deepline tools execute`) or through Claude Code with a natural language prompt like “find 3 engineering leaders at stripe.com.”

### Can I find decision makers in bulk from a CSV?

Yes. type `/deepline-gtm find the VP of Sales at each company in targets.csv` and Deepline reads your company list, searches for matching contacts at each company, and writes results back. Each company lookup searches across up to 5 providers. Always pilot with 3-5 companies first.

### How does Deepline handle role title variations?

Deepline expands role queries to include seniority variations. Searching for “VP Sales” also matches Director of Sales, Head of Sales, SVP Sales, and similar titles across all 5 providers. You specify the general role and seniority level; Deepline handles title permutations. Note: the decision maker waterfall accumulates results rather than stopping at the first match.

### What data do I get back for each decision maker?

Each returned contact includes: full name, current job title, LinkedIn profile URL, company name, and company domain. You can then enrich each contact further with verified work emails, phone numbers, or company data using other Deepline tasks.

### Why use Deepline for finding decision makers?

Ask for decision makers in plain English from your IDE or terminal and the 5-provider waterfall runs automatically. Deepline handles role expansion, provider selection, and result accumulation without any UI configuration — and since it runs in your coding environment, you can chain decision maker discovery directly into downstream enrichment scripts.


---

## Play: `task-find-linkedin-profile`

# Find LinkedIn Profile

Deepline resolves `linkedin.com/in/` URLs from work emails, personal emails, names, or company information. The lookup combines web search (Google, Exa), provider databases (Crustdata, Apollo, PeopleDataLabs), and Apify LinkedIn scraping for identity verification. Just describe who you’re looking for in natural language — no UI configuration or template selection required.
In our internal testing, getting the LinkedIn URL first improved downstream email hit rates by 15-25%. B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), so resolving LinkedIn early means you’re enriching the right person at their current company, not a stale record.

## How do I find LinkedIn profiles with Claude Code?

In Claude Code, type:

```
/deepline-gtm Find LinkedIn profiles for everyone in contacts.csv
```

```
/deepline-gtm I have work emails -- find their LinkedIn URLs
```

```
/deepline-gtm Find the LinkedIn profile for the CTO at stripe.com
```

**With Codex:**

```
codex "Find LinkedIn profile URLs for contacts in contacts.csv"
```

## What does the LinkedIn lookup do step by step?

The lookup runs a multi-stage pipeline: web search for candidates, nickname expansion for fuzzy matching, provider enrichment, and Apify verification for ambiguous matches. Industry benchmarks suggest multi-provider waterfalls improve coverage by 20-40% over single-provider lookups ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)).

1

Search for LinkedIn URL candidates

Deepline searches Google (`google_search_google_search`) and Exa
(`exa_search`) with the person’s name, company, and title to find candidate
LinkedIn profile URLs. This catches profiles that provider databases might
miss.

2

Expand nicknames

If the name could be a nickname (e.g., “Mike” for “Michael”, “Bill” for
“William”), Deepline expands it using a 2,600+ entry nickname group database
and searches for all variants. This prevents missed matches when someone’s
LinkedIn uses their full name but your CRM has a short form.

3

Query enrichment providers

For email-based lookups, Crustdata, Apollo, and PeopleDataLabs do reverse
email search. For name-based lookups, Crustdata people-search and Apollo
search-people-with-match do fuzzy matching against their databases.

4

Verify identity with Apify

For ambiguous matches (common names, multiple candidates), Deepline runs the
Apify LinkedIn profile scraper to pull the candidate’s actual job history,
current company, and title. It then validates the scraped profile against
your input data to confirm the match.

5

Return validated LinkedIn URL

Only profiles that pass identity validation are returned. False positives
(wrong “Sarah Chen” at the wrong company) get filtered out.

## Which providers and methods does the LinkedIn lookup use?

The lookup chain depends on your input data. Email-based lookups use reverse search (higher precision). Name-based lookups combine web search with provider databases and nickname expansion:

| You have | Method | What runs |
| --- | --- | --- |
| Work email | Reverse lookup | Crustdata enrich-contact, Apollo people-match, PeopleDataLabs |
| Name + company | Web search + provider search | Google/Exa search for LinkedIn URLs, Crustdata people-search, Apollo search-people-with-match, nickname expansion |
| Name + company (ambiguous) | Web search + Apify verification | Same as above, plus Apify LinkedIn profile scraper to verify job history matches |
| Personal email | Reverse lookup variant | Same as work email chain |

CLI commands

```
# Search for LinkedIn profile via Google
deepline tools execute google_search_google_search \
  --payload '{"query":"Sarah Chen CTO Stripe site:linkedin.com/in","num":5}'

# Enrich from work email (returns LinkedIn URL)
deepline tools execute crustdata_enrich_contact \
  --payload '{"email":"sarah@stripe.com"}'

# People search from name + company
deepline tools execute crustdata_people_search \
  --payload '{"companyDomain":"stripe.com","titleKeywords":["CTO"],"limit":1}'

# Verify a candidate profile with Apify
deepline tools execute apify_run_actor_sync \
  --payload '{"actorId":"apimaestro/linkedin-profile-scraper","input":{"url":"https://linkedin.com/in/sarahchen"},"timeoutMs":60000}'
```

## How does nickname expansion work?

Deepline maintains a database of 2,600+ nickname groups (e.g., “Mike” maps to “Michael”, “Miguel”; “Bill” maps to “William”, “Billy”, “Will”). When searching for a person by name, Deepline checks if the first name has known variants and searches for all of them.This matters because your CRM might say “Mike Johnson” but his LinkedIn says “Michael Johnson.” Without nickname expansion, the search misses. With it, Deepline queries both “Mike Johnson” and “Michael Johnson” and deduplicates the results.Common examples: Bob/Robert, Dick/Richard, Liz/Elizabeth, Jim/James, Chuck/Charles, Peggy/Margaret, Bill/William, Ted/Theodore, Jack/John.

Once you have a LinkedIn URL, email and phone lookups become significantly
more accurate. If you have no LinkedIn URLs yet, find them first.

* * *

**Related tasks:** [Find Work Email](https://deepline.com/docs/plays/task-find-work-email) \| [Find Decision Makers](https://deepline.com/docs/plays/task-find-decision-makers) \| [Enrich Company](https://deepline.com/docs/plays/task-enrich-company)

## Frequently Asked Questions

### What data do I need to find a LinkedIn profile?

At minimum, a work email or a first name + company name. Work email gives the highest match rate via reverse lookup. Name + company triggers a broader search: Google/Exa web search for LinkedIn URLs, provider people-search, and nickname expansion. Having both email and name improves validation.

### Can I find LinkedIn profiles in bulk from a CSV?

Yes. type `/deepline-gtm find LinkedIn profiles for contacts.csv` and Deepline reads your column headers, selects the right lookup method per row (reverse email, name-based web search, or both), and enriches every row. Always pilot with `--rows 0:2` first to verify match quality before running the full batch.

### How does Deepline handle common names like “John Smith”?

For common names, Deepline runs multiple searches (Google, Exa, provider databases) to find candidate LinkedIn profiles, then uses the Apify LinkedIn profile scraper to pull actual job history from each candidate. It validates current company and title against your input data. If no candidate matches confidently, it flags the row for manual review rather than returning a wrong profile.

### How accurate is LinkedIn profile resolution?

Email-based reverse lookups have the highest precision because email-to-LinkedIn is a 1:1 mapping in most provider databases. Name + company lookups combine web search, provider databases, nickname expansion, and Apify verification to prevent false positives. Unique names at well-known companies resolve most reliably. Common names at small companies are the hardest case — that’s where Apify job history verification matters most.

### Why should I find LinkedIn profiles before other enrichment?

A LinkedIn URL is the strongest person-level identifier. With it, email and phone waterfalls use the LinkedIn-optimized provider chain (`person-linkedin-to-email`) that has higher hit rates than name-based lookups.

### Why use Deepline for LinkedIn profile lookups?

Describe who you’re looking for in plain English from your IDE or terminal. Deepline combines Google/Exa web search, nickname expansion, provider enrichment, and Apify verification in one step — no template selection, no manual provider configuration, no switching between a browser UI and your code.


---

## Play: `task-find-mobile-phone`

# Find Mobile Phone

Deepline’s phone waterfall detects the contact’s region and routes to the right providers automatically. No need to choose between regional templates — just describe who you need phone numbers for and Deepline handles routing. In our internal testing, coverage reaches 60-80% in US/Canada and Western Europe.
The waterfall tries multiple providers in sequence, validates each result for mobile type (filtering out landlines), and returns the first valid mobile number with country code. You only pay for providers that ran. B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), so multi-provider coverage helps you reach contacts even when one provider’s data is stale.

## How do I find mobile phone numbers with Claude Code?

In Claude Code, type:

```
/deepline-gtm Find mobile numbers for these US sales contacts
```

```
/deepline-gtm I need phone numbers for EMEA prospects in leads.csv
```

```
/deepline-gtm Find the mobile number for John Davis, CTO at Ramp (ramp.com)
```

**With Codex:**

```
codex "Find mobile phone numbers for contacts in leads.csv"
```

## What does the phone number waterfall do step by step?

The phone waterfall detects the contact’s region, routes to the right providers, validates each number for mobile type, and returns the first valid mobile number with country code. Cheapest provider runs first; the waterfall stops on the first valid hit.

1

Detect contact region

Deepline infers region from company domain, address, or country fields in
your data.

2

Route to region-optimized providers

The right providers fire based on region — no need to pick a regional
template.

3

Validate phone number

Returned numbers are validated for format and mobile type (filtering out
landlines).

4

Return mobile number

The first valid mobile number is returned with country code.

## Which providers does the phone waterfall use?

The phone waterfall uses 3 providers: enrich\_phone\_finder, Dropleads mobile-finder, and LeadMagic mobile-finder. Deepline analyzes domain TLD, address fields, and country codes to pick the right provider configuration.

CLI command

```
deepline tools execute enrich_phone_finder \
  --payload '{"first_name":"John","last_name":"Davis","company_name":"Ramp","domain":"ramp.com"}'
```

Phone number find rates vary significantly by region. In our internal testing,
US/Canada and Western Europe show the highest coverage (60-80%). APAC and
LATAM coverage is lower but still beats single-provider lookups.

* * *

**Related tasks:** [Find Work Email](https://deepline.com/docs/plays/task-find-work-email) \| [Find Decision Makers](https://deepline.com/docs/plays/task-find-decision-makers) \| [Find LinkedIn Profile](https://deepline.com/docs/plays/task-find-linkedin-profile)

## Frequently Asked Questions

### How much does it cost to find a mobile phone number with Deepline?

Phone lookups cost more than email lookups because phone data is more expensive across all providers. You only pay for providers that ran — most lookups resolve on the first or second provider. Expect 1-5 credits per phone lookup depending on the provider chain for the detected region, compared to 0.3-0.4 credits for a typical email waterfall.

### Can I find phone numbers in bulk from a CSV?

Yes. type `/deepline-gtm find mobile numbers for leads.csv` and Deepline reads your column headers, detects regions for each contact, and enriches every row with the appropriate provider chain. Always pilot with 3-5 rows first to verify coverage for your target regions.

### What data do I need to find a mobile phone number?

For the best results, provide first name, last name, company name, and domain. Deepline uses these to both identify the contact and detect their region. If you have a LinkedIn URL, that can also help resolve the contact’s identity before the phone lookup.

### How accurate is mobile phone number lookup?

Deepline filters out landlines and validates phone number format before returning results. Hit rates vary by region: in our testing, US/Canada and Western Europe reach 60-80% coverage, while APAC and LATAM are lower. Industry benchmarks suggest multi-provider waterfalls improve coverage by 20-40% over single-provider lookups ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)).

### Why use Deepline for finding mobile phone numbers?

Deepline detects region automatically from your data — no need to segment lists by geography or pick regional templates. Ask for phone numbers from your IDE or terminal and Deepline routes to the right providers. Since it runs in your coding environment, phone enrichment chains directly into larger automation pipelines.


---

## Play: `task-find-personal-email`

# Find Personal Email

Deepline’s personal email waterfall tries 3 providers — Dropleads, LeadMagic, and PeopleDataLabs — to resolve consumer email addresses from professional identifiers like LinkedIn URLs and names. Just describe what you need in natural language and Deepline handles provider selection automatically.
Personal emails survive job changes, domain switches, and corporate reorgs. B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), so personal emails are a more stable channel for ABM advertising and direct outreach.

## How do I find personal emails with Claude Code?

In Claude Code, type:

```
/deepline-gtm Find personal emails for these contacts for our LinkedIn Matched Audiences campaign
```

```
/deepline-gtm I need personal emails for ad targeting -- find them from LinkedIn profiles in contacts.csv
```

**With Codex:**

```
codex "Find personal emails for contacts in contacts.csv for ad targeting"
```

## What does the personal email waterfall do step by step?

The waterfall reads your contact list, tries 3 providers in cost order, and returns personal (non-work) email addresses. Each provider resolves consumer emails from professional identifiers. The multi-provider approach increases your odds of getting fresh data, since B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)).

1

Read your contact list

Deepline parses your CSV or payload for LinkedIn URLs, names, and any
existing identifiers.

2

Run personal email waterfall

Providers are tried in cost order, focusing on personal (non-work) email
addresses.

3

Return personal emails

Results are written back with the personal email address and source
provider.

## Which providers does the personal email waterfall use?

The personal email waterfall uses 3 providers: Dropleads, LeadMagic, and PeopleDataLabs. They run in cost order, and the lookup stops at the first valid personal email.

CLI command

```
deepline enrich --input contacts.csv --output contacts-personal.csv \
  --with '{"alias":"personal_email","tool":"personal_email_waterfall","payload":{"linkedin_url":"{{linkedin_url}}","first_name":"{{first_name}}","last_name":"{{last_name}}"}}'
```

> Deepline routes to the appropriate provider chain based on your input data.

**Compliance note:** Personal email regulations vary by use case. Ad targeting
(LinkedIn Matched Audiences, Facebook Custom Audiences) has broader
permissibility than cold outreach. Check your jurisdiction’s rules before
using personal emails for outbound campaigns.

* * *

**Related tasks:** [Find Work Email](https://deepline.com/docs/plays/task-find-work-email) \| [Find LinkedIn Profile](https://deepline.com/docs/plays/task-find-linkedin-profile) \| [Email Waterfall with Validation](https://deepline.com/docs/plays/task-email-waterfall-with-validation)

## Frequently Asked Questions

### What is the difference between finding a work email and a personal email?

Work emails are corporate addresses (e.g., [sarah@stripe.com](mailto:sarah@stripe.com)) used for B2B outreach. Personal emails are consumer addresses (e.g., [sarah.chen@gmail.com](mailto:sarah.chen@gmail.com)) used primarily for ad targeting on platforms like LinkedIn Matched Audiences and Facebook Custom Audiences. Deepline has separate waterfall tasks for each, using different provider chains optimized for the email type.

### Can I find personal emails in bulk from a CSV?

Yes. type `/deepline-gtm find personal emails for contacts.csv` and Deepline reads your column headers, selects the personal email waterfall, and enriches every row. The personal email waterfall works best when you provide LinkedIn URLs alongside names. Always pilot with 3-5 rows first.

### How accurate is personal email lookup?

Hit rates are lower than work email lookups because consumer data is less systematically indexed. A LinkedIn URL significantly improves match rates. Industry benchmarks suggest multi-provider waterfalls improve coverage by 20-40% over single-provider lookups ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)).

### Is it legal to use personal emails for outreach?

Ad targeting (LinkedIn Matched Audiences, Facebook Custom Audiences) generally has broader permissibility than cold email outreach. Regulations vary by jurisdiction — CAN-SPAM (US), GDPR (EU), and CASL (Canada) each have different requirements for personal email marketing. Always check your jurisdiction’s rules before using personal emails for direct outbound campaigns.

### What data do I need to find a personal email?

For the best results, provide a LinkedIn URL plus first and last name. Deepline can also work with just a LinkedIn URL or just a name and company, but match rates are highest with LinkedIn URLs.


---

## Play: `task-find-work-email`

# Find Work Email

Deepline’s email waterfall tries up to 6 providers in cost order — Dropleads (0.3 credits), Hunter (0.3 credits), LeadMagic, Deepline Native, Crustdata, and PeopleDataLabs — and stops at the first valid hit. Typical cost: 0.3-0.4 credits per lookup. Just describe what you need in natural language — Deepline picks the right waterfall variant automatically.
According to industry benchmarks, waterfall enrichment can improve email coverage by 20-40% compared to any single provider ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)). B2B data decays at roughly 22.5% per year ( [HubSpot via Cognism](https://www.cognism.com/blog/data-decay)), so a single-provider lookup goes stale fast. The 6-provider waterfall cross-references multiple sources in real time to compensate.

## How do I find work emails with Claude Code?

In Claude Code, type:

```
/deepline-gtm Find work emails for everyone in leads.csv
```

```
/deepline-gtm I have LinkedIn URLs and names -- find their work emails
```

```
/deepline-gtm Find the work email for Sarah Chen, VP Engineering at Stripe
```

**With Codex:**

```
codex "Find work emails for everyone in leads.csv using waterfall enrichment"
```

## What does the email waterfall do step by step?

The waterfall reads your input columns, picks a provider chain, and tries each provider cheapest-first until it finds a verified email. You only pay for providers that ran. Most lookups resolve on the first or second provider (0.3-0.4 credits).

1

Analyze your input columns

Deepline inspects your CSV headers or payload fields to determine which
waterfall variant to use.

2

Route to the right waterfall

Based on available data, Deepline selects the optimal provider chain (see
provider details below).

3

Try providers in cost order

The cheapest provider runs first. Each result is validated before accepting.

4

Return verified email

The first valid, non-catch-all email is returned. You only pay for providers
that ran.

## Which providers does the waterfall try and in what order?

There are 4 waterfall variants. Deepline picks one based on what data you provide:

| You have | Waterfall | Providers |
| --- | --- | --- |
| Name + domain | `name_and_domain_to_email_waterfall` | Free LeadMagic pattern validation first, then Dropleads, Hunter, LeadMagic, Deepline Native, PeopleDataLabs |
| LinkedIn URL + name | `person-linkedin-to-email` | Dropleads, Hunter, LeadMagic, Deepline Native, Crustdata, PeopleDataLabs (all receive the LinkedIn URL) |

Typical cost: **0.3-0.4 credits** (first provider hit).

CLI commands

```
# Single lookup
deepline tools execute name_and_domain_to_email_waterfall \
  --payload '{"first_name":"Sarah","last_name":"Chen","domain":"stripe.com"}'

# Bulk CSV
deepline enrich --input leads.csv --output enriched.csv \
  --with '{"alias":"email","tool":"name_and_domain_to_email_waterfall","payload":{"first_name":"{{first_name}}","last_name":"{{last_name}}","domain":"{{domain}}"}}'
```

If you have name + domain but no company name, Deepline uses the cost-aware
waterfall that tries free email patterns first. This can save 70% on lookups
where the pattern matches.

* * *

**Related tasks:** [Email Waterfall with Validation](https://deepline.com/docs/plays/task-email-waterfall-with-validation) \| [Find LinkedIn Profile](https://deepline.com/docs/plays/task-find-linkedin-profile) \| [Find Decision Makers](https://deepline.com/docs/plays/task-find-decision-makers)

## Frequently Asked Questions

### How much does it cost to find a work email with Deepline?

Most lookups resolve on the first provider at 0.3 credits ($0.024). The cost-aware variant tries free email patterns first and can resolve for as little as 0.1 credits. Worst case across all 6 providers: 7.2 credits, but that rarely happens.

### Can I find work emails in bulk from a CSV?

Yes. type `/deepline-gtm find work emails for leads.csv` and Deepline reads your column headers, picks the right waterfall variant, and enriches every row. You can also use the `deepline enrich` CLI command directly. Always pilot with 3-5 rows first to verify the waterfall is routing correctly.

### What data do I need to find a work email?

At minimum, you need a name and domain (e.g., “Sarah Chen” + “stripe.com”). For the best hit rates, provide name + company name + domain. If you have a LinkedIn URL, Deepline uses a LinkedIn-optimized chain. The more identifiers you provide, the higher the match rate.

### Why use Deepline for finding work emails?

Ask for emails in plain English from your IDE or terminal and the waterfall runs automatically. Deepline selects the right provider chain based on your input, tries providers cheapest-first, and chains multiple sources without any UI configuration. You stay in your coding workflow instead of switching to a separate enrichment tool.

### How accurate is the work email waterfall?

Deepline validates every email before returning it, filtering out catch-all addresses and invalid syntax. Multiple providers cross-check each other, which industry benchmarks suggest improves coverage by 20-40% over single-provider lookups ( [Instantly](https://instantly.ai/blog/waterfall-enrichment/)). For deliverability verification, use the [Email Waterfall with Validation](https://deepline.com/docs/plays/task-email-waterfall-with-validation) task to chain a ZeroBounce validation step for bounce-risk scoring.


---

## Play: `win-loss-signals`

# Niche Signal Discovery

Every team knows the obvious ICP signals — company size, industry, funding stage. The signals that actually separate your wins from your losses are usually more specific: a particular tech stack combination, a hiring pattern, a revenue growth rate, a specific competitor on the account.This play finds those signals by mining your actual deal history.

## The Play

1

Export wins and losses from CRM

Pull closed-won and closed-lost deals from HubSpot or Salesforce for the
last 12 months. Include deal size, sales cycle, competitor mentioned, close
reason, and any custom fields your team tracks.

2

Enrich with deep firmographic data

For each company, pull detailed firmographics: employee count, department
breakdown, tech stack, hiring velocity by role, funding history, revenue
estimates, geographic presence, recent news.

3

Run comparative analysis

Ask Claude to compare enriched wins vs losses across every dimension. Look
for attributes where the distribution is meaningfully different between wins
and losses.

4

Identify niche signals

The goal is non-obvious patterns. Examples from real analyses: - “We win 4x
more often at companies with 3-8 open engineering roles” - “Deals close 2x
faster when the company uses both Salesforce and Outreach” - “We lose 80% of
deals where the primary competitor is \[X\]” - “Companies that raised Series B
in the last 6 months close at 3x the rate of Series A”

5

Validate with holdout data

Test the discovered signals against held-out deals. If a signal predicts
wins/losses on data the model hasn’t seen, it’s real. If it only works on
training data, it’s noise.

6

Feed into account scoring

Add validated niche signals to your [account scoring\\
model](https://deepline.com/docs/plays/account-scoring). These signals become the weighted dimensions
that separate generic ICP from your specific propensity model.

## How to Run It

The `/niche-signal-discovery` skill automates this entire play. Give it your won and lost domain lists and it handles enrichment, analysis, and reporting.

```
/niche-signal-discovery
```

Claude Code will ask for your won and lost domain lists (minimum 20 won + 10 lost), then run the full pipeline:

1. **Discovers your vertical** \- researches what you sell, who you sell to, and your competitive landscape
2. **Generates vertical-specific configs** \- keywords, tech stack tools, and job roles tailored to your buyer persona
3. **Enriches every company** \- pulls multi-page website content (~5 pages per domain) and job listings via Deepline CLI
4. **Runs differential analysis** \- computes Laplace-smoothed lift scores across every dimension to find what separates wins from losses
5. **Generates a full report** with signal strength dashboards, scoring models, Apollo search recipes, and buyer persona quick-reference cards

### What You Get

- **Signal strength dashboard** with lift scores and visual bars (e.g., “Hiring 3+ SDRs simultaneously” at 4.2x lift)
- **Anti-fit signals** that flag accounts to skip (e.g., “Consumer checkout language” at 0.2x lift)
- **Lead scoring model** \- 0-100 point system with three tiers (Core Fit, Buying Intent, Infrastructure Readiness)
- **Pre-built Apollo URLs** for people and company searches based on discovered signals
- **Source evidence** \- exact quotes with page URLs and job listings backing every signal
- **Scoring cheatsheet** with “How to Check” column for each signal

### Cost

The skill uses Serper for page discovery and Firecrawl for content extraction - the cheapest enrichment path available.

| Step | Tool | Credits per company | Total (60 companies) |
| --- | --- | --- | --- |
| Discover relevant pages | Serper Google Search | 0.02 | 1.20 |
| Scrape ~5 pages per domain | Firecrawl Scrape | 0.05 | 3.00 |
| Job listings | Crustdata Job Listings | 0.40 | 24.00 |
| **Total** |  | **~0.47** | **~28** |

That’s about $0.05 per company on managed credits, or $3 for 60 companies. BYOK users pay $0 on Deepline. The skill asks for credit approval before running any paid enrichment.

If you have an Apollo API key connected, job postings are BYOK (free on
Deepline) - bringing the total down to ~0.07 credits per company.

If your Closed Won list is small (under 15), the skill can supplement with
lookalike companies. It flags this in the report so you know the limitation.

## What Niche Signals Look Like

Generic ICP attributes (company size, industry) get you to the right ballpark. Niche signals get you to the right accounts.

| Generic signal | Niche signal |
| --- | --- |
| ”SaaS companies" | "SaaS companies using Salesforce + Outreach with 200-500 employees" |
| "Series A-C funding" | "Series B in the last 6 months with 20%+ headcount growth" |
| "Hiring GTM roles" | "3+ SDR/BDR roles open simultaneously" |
| "US-based" | "US-based with at least one office outside the Bay Area" |
| "Enterprise" | "$20M-$80M ARR with engineering team > 30% of headcount” |

Run this quarterly. Your niche signals shift as your product evolves and your
market changes. Signals that predicted wins 6 months ago may not predict wins
today.

* * *

[Account Scoring →](https://deepline.com/docs/plays/account-scoring) \| [Closed-Lost Recovery →](https://deepline.com/docs/plays/closed-lost-recovery) \| [I Have X, I Want Y →](https://deepline.com/docs/plays/decision-tree)

---

# PART 6 — Master Pricing Schedule (all providers, all endpoints)

*The complete Deepline-credit rate card. BYOK users pay $0 on Deepline for everything below. Managed-credit users pay the listed credits. Check `deepline tools get <tool_id> --json` for live pricing before large runs.*


## Bring Your Own Keys

Connect your own provider accounts. **Deepline cost: $0.** You pay providers directly.

## Managed Credits

**$0.10 per credit**, pay as you go. Volume discount at 1,000+ credits ($0.09 each).

All prices below are in **Deepline credits** (managed keys). BYOK is always free. [Clay comparison](https://deepline.com/docs/_generated/pricing-schedule#clay-comparison) \| 93+ integrations.

Use the CLI for the latest live pricing before a run: `deepline tools get <tool_id> --json`. The response includes `deepline_credits_per_pricing_unit`, `deepline_usd_per_pricing_unit`, dynamic pricing notes, and your current billing source. Use `deepline tools search "<provider or task>" --json` to find the tool ID.

* * *

## Pricing by capability

- Email Finding

- Email Verification

- Person Enrichment

- Company Enrichment

- Mobile / Phone

- Prospecting

- Web & Research

- Ad Intelligence

- Sequencers

- Waterfall

Find professional email addresses from name + company.

| Provider | Endpoint | Credits | Notes |
| --- | --- | --- | --- |
| **wizleads** | Find Email | **0.04** |  |
| **[AI Ark](https://aiark.ai/)** | Find Emails | 0.14 |  |
| **datagma** | Find Email | 0.14 |  |
| **[Icypeas](https://www.icypeas.com/)** | Email Search | 0.14 | No charge if not found |
| **[Dropleads](https://dropleads.io/)** | Email Finder | 0.26 | Charged on hit |
| **findymail** | Find From Business Profile | 0.28 |  |
| **findymail** | Find From Name | 0.28 |  |
| **[Hunter](https://hunter.io/)** | Email Finder | 0.3 |  |
| **[LeadMagic](https://leadmagic.io/)** | Email Finder | 0.34 |  |
| **openmart** | Lookup Business Email | 0.42 |  |
| **[LeadMagic](https://leadmagic.io/)** | Personal Email Finder | 0.68 |  |
| **[Forager](https://forager.ai/)** | Person Contacts Lookup Personal Emails | 1.56 |  |
| **[Forager](https://forager.ai/)** | Person Contacts Lookup Work Emails | 1.56 |  |
| **[LeadMagic](https://leadmagic.io/)** | B2B Social Email | 1.68 |  |
| **enformion** | Person Search | 3.5 | Charged on match |
| **[ZeroBounce](https://www.zerobounce.net/)** | Email Finder | 5.46 |  |
| **[AI Ark](https://aiark.ai/)** | Email Finder Results | Free |  |
| **[AI Ark](https://aiark.ai/)** | Email Finder Statistics | Free |  |

Validate whether an email is deliverable.

| Provider | Endpoint | Credits | Notes |
| --- | --- | --- | --- |
| **[Icypeas](https://www.icypeas.com/)** | Email Verification | **0.02** |  |
| **[Dropleads](https://dropleads.io/)** | Email Verifier | 0.03 | Charged on hit |
| **[IPQS](https://www.ipqualityscore.com/)** | Batch Email Verify | 0.07 |  |
| **[IPQS](https://www.ipqualityscore.com/)** | Email Verify | 0.07 |  |
| **[LeadMagic](https://leadmagic.io/)** | Email Validation | 0.09 |  |
| **[Hunter](https://hunter.io/)** | Email Verifier | 0.15 |  |

Enrich a person with job title, company, social links, and more.

| Provider | Endpoint | Credits | Notes |
| --- | --- | --- | --- |
| **linkedin\_scraper** | LinkedIn Extract People Comment Activity | **0.04** |  |
| **linkedin\_scraper** | LinkedIn Extract People Experiences | 0.04 |  |
| **linkedin\_scraper** | LinkedIn Extract People Post Activity | 0.04 |  |
| **linkedin\_scraper** | LinkedIn Extract People Reaction Activity | 0.04 |  |
| **linkedin\_scraper** | LinkedIn Extract People Skills | 0.04 |  |
| **[AI Ark](https://aiark.ai/)** | Reverse Lookup | 0.07 |  |
| **aviato** | Person Enrich | 0.14 | Preview free, full enrich charged |
| **[Apollo](https://www.apollo.io/)** | Bulk People Enrichment | BYOK |  |
| **[Apollo](https://www.apollo.io/)** | Enrich Person | BYOK |  |
| **[Apollo](https://www.apollo.io/)** | People Match | BYOK |  |
| **[Apollo](https://www.apollo.io/)** | Reveal Person | BYOK |  |
| **[Apollo](https://www.apollo.io/)** | Search People With Match | BYOK |  |
| **[Icypeas](https://www.icypeas.com/)** | Scrape Profile | 0.21 |  |
| **[Dropleads](https://dropleads.io/)** | Enrich Single Lead | 0.26 |  |
| **[Dropleads](https://dropleads.io/)** | Single Person Enrichment | 0.26 |  |
| **datagma** | Enrich Person | 0.27 |  |
| **findymail** | Find Employees | 0.28 |  |
| **limadata** | Enrich Person | 0.28 |  |
| **[Hunter](https://hunter.io/)** | Combined Find | 0.3 | Charged on match |
| **[Hunter](https://hunter.io/)** | People Find | 0.3 |  |
| **[Forager](https://forager.ai/)** | Person Detail Lookup | 0.32 |  |
| **wiza** | Reveal Person | 0.35 |  |
| **crustdata-v2** | Crustdata V2 Enrich Person | 0.4 |  |
| **openmart** | Get Task Result | 0.42 |  |
| **openmart** | Lookup People | 0.42 |  |
| **opensosdata** | Business Lookup | 0.44 |  |
| **[Prospeo](https://prospeo.io/)** | Enrich Person | 0.55 |  |
| **[AI Ark](https://aiark.ai/)** | Personality Analysis | 0.56 |  |
| **findymail** | Reverse Email Lookup | 0.56 |  |
| **[BetterContact](https://bettercontact.rocks/)** | Bulk Enrich | 0.69 | Phone-enabled: 7.55 credits with `enrich_phone_number=true`. |
| **[BetterContact](https://bettercontact.rocks/)** | Enrich | 0.69 | Phone-enabled: 7.55 credits with `enrich_phone_number=true`. |
| **[AI Ark](https://aiark.ai/)** | Mobile Phone Finder | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Extract People | 0.7 |  |
| **lusha** | Enrich Person | 0.7 |  |
| **[Dropleads](https://dropleads.io/)** | Mobile Finder | 0.76 |  |
| **[FullEnrich](https://fullenrich.com/)** | Bulk Enrich | 0.84 | Phone-enabled: 8.38 credits when `contact.phones` is requested. |
| **[FullEnrich](https://fullenrich.com/)** | Reverse Email | 0.84 |  |
| **[Crustdata](https://crustdata.com/)** | Enrich Contact | 1.2 |  |
| **[Crustdata](https://crustdata.com/)** | People Enrich | 1.2 |  |
| **[Crustdata](https://crustdata.com/)** | Person Enrichment | 1.2 | Preview free, full enrich charged |
| **contactout** | Enrich Person | 1.4 |  |
| **upcell** | Enrich Contact | 1.4 | Charged on match |
| **[Forager](https://forager.ai/)** | Person Detail Reverse Lookup By Email | 1.56 |  |
| **[LeadMagic](https://leadmagic.io/)** | Mobile Finder | 1.68 |  |
| **rocketreach** | Lookup Person | 1.68 |  |
| **[LeadMagic](https://leadmagic.io/)** | Email To Profile | 3.36 |  |
| **datagma** | Search Phone Numbers | 3.91 |  |
| **[People Data Labs](https://www.peopledatalabs.com/)** | Bulk People Enrichment | 3.92 |  |
| **[People Data Labs](https://www.peopledatalabs.com/)** | Enrich Contact | 3.92 |  |
| **[Forager](https://forager.ai/)** | Person Contacts Lookup Phone Numbers | 4.67 |  |
| **[Forager](https://forager.ai/)** | Person Detail Reverse Lookup By Phone Number | 4.67 |  |
| **[People Data Labs](https://www.peopledatalabs.com/)** | Person Identify | 7.7 | Charged on match |
| **datagma** | Full Enrichment | Runtime usage |  |
| **datagma** | Job Change Detection | Runtime usage |  |
| **[BetterContact](https://bettercontact.rocks/)** | Get Result | Free |  |
| **contactout** | Check Email Status | Free |  |
| **contactout** | Check Personal Email | Free |  |
| **contactout** | Check Phone | Free |  |
| **contactout** | Check Work Email | Free |  |
| **crustdata-v2** | Crustdata V2 Identify Company | Free |  |
| **[FullEnrich](https://fullenrich.com/)** | Get Result | Free |  |
| **[FullEnrich](https://fullenrich.com/)** | Get Reverse Result | Free |  |
| **upcell** | Contact Existence | Free |  |

Enrich a company with firmographics, tech stack, headcount, and more.

| Provider | Endpoint | Credits | Notes |
| --- | --- | --- | --- |
| **aviato** | Company Search | **0.04** |  |
| **wizleads** | Get Company LinkedIn ID | 0.04 |  |
| **theirstack** | Technographics | 0.05 |  |
| **[Icypeas](https://www.icypeas.com/)** | Scrape Company | 0.07 |  |
| **rocketreach** | Lookup Company | 0.07 |  |
| **openmart** | Enrich Company | 0.13 |  |
| **aviato** | Company Bulk Enrich | 0.14 |  |
| **aviato** | Company Enrich | 0.14 | Preview free, full enrich charged |
| **aviato** | Get Company Founders | 0.14 |  |
| **aviato** | Get Company Fund Investments | 0.14 |  |
| **aviato** | Get Company Funding Rounds | 0.14 |  |
| **aviato** | Get Company Funds | 0.14 |  |
| **aviato** | Get Company Outbound Investments | 0.14 |  |
| **aviato** | Person Get Company Investments | 0.14 |  |
| **[Icypeas](https://www.icypeas.com/)** | Find Company URL | 0.14 |  |
| **[Apollo](https://www.apollo.io/)** | Bulk Organization Enrichment | BYOK |  |
| **[Apollo](https://www.apollo.io/)** | Enrich Company | BYOK |  |
| **[Apollo](https://www.apollo.io/)** | Organization Enrich | BYOK |  |
| **datagma** | Enrich Company | 0.27 |  |
| **findymail** | Get Company Information | 0.28 |  |
| **findymail** | Lookup Technologies By Domain | 0.28 |  |
| **limadata** | Enrich Company | 0.28 |  |
| **limadata** | Find Company LinkedIn | 0.28 |  |
| **[Hunter](https://hunter.io/)** | Companies Find | 0.3 |  |
| **[Forager](https://forager.ai/)** | Website Detail Lookup | 0.32 |  |
| **[Crustdata](https://crustdata.com/)** | Company Enrichment | 0.4 | Charged on match |
| **[Crustdata](https://crustdata.com/)** | Enrich Company | 0.4 |  |
| **crustdata-v2** | Crustdata V2 Enrich Company | 0.4 |  |
| **bloomberry** | Get Company Tech Stack | 0.43 |  |
| **[Prospeo](https://prospeo.io/)** | Bulk Enrich Company | 0.55 | Charged on match |
| **[Prospeo](https://prospeo.io/)** | Enrich Company | 0.55 |  |
| **predictleads** | Company | 0.56 |  |
| **predictleads** | Company Technology Detections | 0.56 |  |
| **predictleads** | Company Website Evolution | 0.56 |  |
| **predictleads** | Discover Technology Detections | 0.56 |  |
| **predictleads** | Extended Technology Detection | 0.56 |  |
| **predictleads** | Technologies | 0.56 |  |
| **predictleads** | Technology | 0.56 |  |
| **aviato** | Get Company Posts | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Extract Company | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Extract Company Affiliates | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Extract Company Employees Insights | 0.7 |  |
| **linkedin\_scraper** | Salesnavigator Extract Company | 0.7 |  |
| **lusha** | Enrich Company | 0.7 |  |
| **openmart** | Find Tech | 0.84 |  |
| **contactout** | Enrich Domain | 1.4 |  |
| **[People Data Labs](https://www.peopledatalabs.com/)** | Bulk Organization Enrichment | 1.4 |  |
| **[People Data Labs](https://www.peopledatalabs.com/)** | Enrich Company | 1.4 |  |
| **findymail** | Search Lookalike Companies | Runtime usage |  |
| **[Apollo](https://www.apollo.io/)** | Get Complete Organization Info | BYOK |  |
| **findymail** | Search Technologies | Free |  |
| **[People Data Labs](https://www.peopledatalabs.com/)** | Company Clean | Free |  |

Find direct-dial or mobile numbers.

| Provider | Endpoint | Credits | Notes |
| --- | --- | --- | --- |
| **[IPQS](https://www.ipqualityscore.com/)** | Batch Phone Validate | **0.07** |  |
| **[IPQS](https://www.ipqualityscore.com/)** | Phone Validate | 0.07 |  |
| **[Trestle](https://trestleiq.com/)** | Phone Validation | 0.21 |  |
| **[AI Ark](https://aiark.ai/)** | Mobile Phone Finder | 0.7 |  |
| **[Dropleads](https://dropleads.io/)** | Mobile Finder | 0.76 |  |
| **upcell** | Enrich Contact | 1.4 | Charged on match |
| **enformion** | Contact Enrich | 1.68 | Charged on match |
| **[LeadMagic](https://leadmagic.io/)** | Mobile Finder | 1.68 |  |
| **findymail** | Find Phone | 2.78 |  |
| **limadata** | Find Phone | 2.8 |  |
| **datagma** | Search Phone Numbers | 3.91 |  |
| **[Forager](https://forager.ai/)** | Person Contacts Lookup Phone Numbers | 4.67 |  |
| **[Forager](https://forager.ai/)** | Person Detail Reverse Lookup By Phone Number | 4.67 |  |
| **enformion** | Workplace Search | 7 | Charged on match |
| **contactout** | Check Phone | Free |  |

Search for people or companies by criteria.

| Provider | Endpoint | Credits | Notes |
| --- | --- | --- | --- |
| **[AI Ark](https://aiark.ai/)** | Company Search | **0.02** | Charged on match |
| **[Crustdata](https://crustdata.com/)** | People Search | 0.02 |  |
| **[Crustdata](https://crustdata.com/)** | Persondb Search | 0.02 | Preview free, full enrich charged |
| **[LeadMagic](https://leadmagic.io/)** | Employee Finder | 0.02 |  |
| **[Serper](https://serper.dev/)** | Google Maps Search | 0.04 |  |
| **theirstack** | Company Search | 0.05 | Charged on match |
| **[OpenWebNinja](https://openwebninja.com/)** | Localbusiness Search | 0.06 |  |
| **[OpenWebNinja](https://openwebninja.com/)** | Localbusiness Search In Area | 0.06 |  |
| **[OpenWebNinja](https://openwebninja.com/)** | Localbusiness Search Nearby | 0.06 |  |
| **[AI Ark](https://aiark.ai/)** | People Search | 0.07 | Charged on match |
| **[Exa](https://exa.ai/)** | Company Search | 0.07 |  |
| **[Exa](https://exa.ai/)** | People Search | 0.07 |  |
| **[Exa](https://exa.ai/)** | Search Companies | 0.07 |  |
| **[Exa](https://exa.ai/)** | Search People | 0.07 |  |
| **[Icypeas](https://www.icypeas.com/)** | Find Companies | 0.07 | Charged on match |
| **[OpenWebNinja](https://openwebninja.com/)** | Glassdoor Company Search | 0.07 |  |
| **[OpenWebNinja](https://openwebninja.com/)** | Jsearch Company Job Salary | 0.07 |  |
| **[Adyntel](https://adyntel.com/)** | Facebook | 0.13 |  |
| **[Adyntel](https://adyntel.com/)** | Facebook Ad Search | 0.13 |  |
| **[Adyntel](https://adyntel.com/)** | Google | 0.13 |  |
| **[Adyntel](https://adyntel.com/)** | LinkedIn | 0.13 |  |
| **[Adyntel](https://adyntel.com/)** | Tiktok Search | 0.13 |  |
| **openmart** | Search Brands | 0.13 |  |
| **openmart** | Search Business Ids | 0.13 |  |
| **openmart** | Search Businesses | 0.13 |  |
| **[AI Ark](https://aiark.ai/)** | Export People | 0.14 |  |
| **[BuiltWith](https://builtwith.com/)** | Company To URL | 0.14 |  |
| **[BuiltWith](https://builtwith.com/)** | Product Search | 0.14 |  |
| **[BuiltWith](https://builtwith.com/)** | Tag Lookup | 0.14 |  |
| **[Icypeas](https://www.icypeas.com/)** | Find People | 0.14 | Charged on match |
| **rocketreach** | Search People | 0.14 |  |
| **[Apollo](https://www.apollo.io/)** | Company Search | BYOK |  |
| **[Apollo](https://www.apollo.io/)** | People Search | BYOK |  |
| **[Apollo](https://www.apollo.io/)** | People Search Paid | BYOK |  |
| **[FullEnrich](https://fullenrich.com/)** | Company Search | 0.21 |  |
| **[FullEnrich](https://fullenrich.com/)** | People Search | 0.21 |  |
| **findymail** | Find From Domain | 0.28 |  |
| **findymail** | Search Leads | 0.28 |  |
| **[Hunter](https://hunter.io/)** | Domain Search | 0.3 |  |
| **[Forager](https://forager.ai/)** | Organization Search | 0.32 |  |
| **[Forager](https://forager.ai/)** | Person Role Search | 0.32 |  |
| **[LeadMagic](https://leadmagic.io/)** | Company Search | 0.34 |  |
| **[LeadMagic](https://leadmagic.io/)** | Profile Search | 0.34 |  |
| **[Crustdata](https://crustdata.com/)** | Companydb Search | 0.4 |  |
| **crustdata-v2** | Crustdata V2 Company Search Realtime | 0.4 | Charged on match |
| **crustdata-v2** | Crustdata V2 Companydb Search | 0.4 |  |
| **crustdata-v2** | Crustdata V2 People Search Realtime | 0.4 |  |
| **crustdata-v2** | Crustdata V2 Persondb Search | 0.4 | Preview free, full enrich charged |
| **openmart** | Find People | 0.42 |  |
| **[Prospeo](https://prospeo.io/)** | Search Company | 0.55 |  |
| **[Prospeo](https://prospeo.io/)** | Search Person | 0.55 |  |
| **predictleads** | Company Similar Companies | 0.56 |  |
| **predictleads** | Discover Companies | 0.56 |  |
| **[LeadMagic](https://leadmagic.io/)** | Role Finder | 0.68 |  |
| **linkedin\_scraper** | LinkedIn Search Companies | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Search Company Employees | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Search Content | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Search Events | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Search Groups | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Search Jobs | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Search People | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Search Schools | 0.7 |  |
| **linkedin\_scraper** | Recruiterlite Search People | 0.7 |  |
| **linkedin\_scraper** | Salesnavigator Search Companies | 0.7 |  |
| **linkedin\_scraper** | Salesnavigator Search Company Employees | 0.7 |  |
| **linkedin\_scraper** | Salesnavigator Search Metrics | 0.7 |  |
| **linkedin\_scraper** | Salesnavigator Search People | 0.7 |  |
| **lusha** | Search Contacts | 0.7 |  |
| **datagma** | Find People | 1.31 |  |
| **contactout** | Search People | 1.4 |  |
| **[People Data Labs](https://www.peopledatalabs.com/)** | Company Search | 1.4 |  |
| **bloomberry** | Get Current Customers | 2.11 |  |
| **[People Data Labs](https://www.peopledatalabs.com/)** | Person Search | 3.92 |  |
| **[AI Ark](https://aiark.ai/)** | Export Results | Free |  |
| **[Apollo](https://www.apollo.io/)** | Search People | BYOK |  |
| **bloomberry** | List Vendors | Free |  |
| **[BuiltWith](https://builtwith.com/)** | Lists | Free |  |
| **[Crustdata](https://crustdata.com/)** | Companydb Autocomplete | Free |  |
| **[Crustdata](https://crustdata.com/)** | Persondb Autocomplete | Free |  |
| **[Dropleads](https://dropleads.io/)** | Get Lead Count | Free |  |
| **[Dropleads](https://dropleads.io/)** | Search People | Free |  |
| **findymail** | Get Export Status | Free |  |
| **findymail** | Get Results | Free |  |
| **findymail** | Get Saved Contacts | Free |  |
| **[Forager](https://forager.ai/)** | Organization Search Totals | Free |  |
| **[Forager](https://forager.ai/)** | Person Role Search Totals | Free |  |
| **[Hunter](https://hunter.io/)** | Discover | Free |  |
| **[People Data Labs](https://www.peopledatalabs.com/)** | Autocomplete | Free |  |
| **[Salesforce](https://www.salesforce.com/)** | Create Account | Free |  |
| **[Salesforce](https://www.salesforce.com/)** | Create Contact | Free |  |
| **[Salesforce](https://www.salesforce.com/)** | Create Lead | Free |  |
| **[Salesforce](https://www.salesforce.com/)** | List Accounts | Free |  |
| **[Salesforce](https://www.salesforce.com/)** | List Contacts | Free |  |
| **[Salesforce](https://www.salesforce.com/)** | List Leads | Free |  |
| **[Salesforce](https://www.salesforce.com/)** | Update Account | Free |  |
| **[Salesforce](https://www.salesforce.com/)** | Update Contact | Free |  |
| **[Salesforce](https://www.salesforce.com/)** | Update Lead | Free |  |
| **theirstack** | Catalog Keywords | Free |  |
| **wiza** | Search Prospects | Free |  |

Search, scrape, and extract structured data from the web.

| Provider | Endpoint | Credits | Notes |
| --- | --- | --- | --- |
| **[DataForSEO](https://dataforseo.com/)** | Content Analysis Category Trends Live | **0.01** |  |
| **[DataForSEO](https://dataforseo.com/)** | Content Analysis Phrase Trends Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Content Analysis Rating Distribution Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Content Analysis Search Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Content Analysis Sentiment Analysis Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Content Analysis Summary Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Keywords Data Clickstream Data Bulk Search Volume Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Keywords Data Clickstream Data Dataforseo Search Volume Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Keywords Data Clickstream Data Global Search Volume Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Keywords Data Dataforseo Trends Demography Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Keywords Data Dataforseo Trends Explore Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Keywords Data Dataforseo Trends Merged Data Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Keywords Data Dataforseo Trends Subregion Interests Live | 0.01 |  |
| **aviato** | Geocoder Search | 0.02 |  |
| **[Exa](https://exa.ai/)** | Contents | 0.02 |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Batch Scrape | 0.02 |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Crawl | 0.02 |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Map | 0.02 |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Scrape | 0.02 |  |
| **[Parallel](https://parallel.ai/)** | Extract | 0.02 |  |
| **[Parallel](https://parallel.ai/)** | Search | 0.02 |  |
| **scrapecreators** | Instagram Reels Search | 0.02 |  |
| **scrapecreators** | Reddit Post Comments | 0.02 |  |
| **scrapecreators** | Reddit Search | 0.02 |  |
| **scrapecreators** | Tiktok Keyword Search | 0.02 |  |
| **scrapecreators** | YouTube Search | 0.02 |  |
| **scrapecreators** | YouTube Transcript | 0.02 |  |
| **[Serper](https://serper.dev/)** | Google Search | 0.02 |  |
| **theirstack** | Job Search | 0.02 | Preview free, full enrich charged |
| **[DataForSEO](https://dataforseo.com/)** | SERP Bing Organic Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Bing Organic Live HTML | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Bing Organic Live Regular | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Ads Advertisers Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Ads Search Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google AI Mode Live HTML | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Autocomplete Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Dataset Info Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Dataset Search Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Events Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Finance Explore Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Finance Explore Live HTML | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Finance Markets Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Finance Markets Live HTML | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Finance Quote Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Finance Quote Live HTML | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Finance Ticker Search Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Images Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Images Live HTML | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Local Finder Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Local Finder Live HTML | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Maps Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google News Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google News Live HTML | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Organic Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Organic Live HTML | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google Organic Live Regular | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Yahoo Organic Live Advanced | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Yahoo Organic Live HTML | 0.03 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Yahoo Organic Live Regular | 0.03 |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Search | 0.03 |  |
| **limadata** | Search Web | 0.03 |  |
| **linkedin\_scraper** | LinkedIn Extract Post Likers | 0.03 |  |
| **aviato** | Person Search | 0.04 |  |
| **linkedin\_scraper** | LinkedIn Extract Licenses Certifications | 0.04 |  |
| **wizleads** | Scrape Salesnav | 0.04 |  |
| **[DataForSEO](https://dataforseo.com/)** | AI Optimization ChatGPT LLM Scraper Live Advanced | 0.06 |  |
| **[DataForSEO](https://dataforseo.com/)** | AI Optimization ChatGPT LLM Scraper Live HTML | 0.06 |  |
| **[DataForSEO](https://dataforseo.com/)** | AI Optimization Gemini LLM Scraper Live Advanced | 0.06 |  |
| **[DataForSEO](https://dataforseo.com/)** | AI Optimization Gemini LLM Scraper Live HTML | 0.06 |  |
| **[DataForSEO](https://dataforseo.com/)** | SERP Google AI Mode Live Advanced | 0.06 |  |
| **[Exa](https://exa.ai/)** | Answer | 0.07 |  |
| **[Exa](https://exa.ai/)** | Search | 0.07 |  |
| **linkedin\_scraper** | LinkedIn Extract Event Attendees | 0.07 |  |
| **linkedin\_scraper** | LinkedIn Extract Group Members | 0.07 |  |
| **linkedin\_scraper** | LinkedIn Extract Post Commenters | 0.07 |  |
| **linkedin\_scraper** | LinkedIn Extract Post Reposters | 0.07 |  |
| **linkedin\_scraper** | LinkedIn Extract School Alumnis | 0.07 |  |
| **[OpenWebNinja](https://openwebninja.com/)** | Glassdoor Job Search | 0.07 |  |
| **[OpenWebNinja](https://openwebninja.com/)** | Jsearch Search | 0.07 |  |
| **limadata** | Research Search | 0.09 |  |
| **[BuiltWith](https://builtwith.com/)** | Recommendations | 0.14 |  |
| **[BuiltWith](https://builtwith.com/)** | Vector Search | 0.14 |  |
| **[Icypeas](https://www.icypeas.com/)** | Bulk Search | 0.14 |  |
| **[Icypeas](https://www.icypeas.com/)** | Domain Search | 0.14 |  |
| **limadata** | Research Extract | 0.28 |  |
| **crustdata-v2** | Crustdata V2 Job Search | 0.4 |  |
| **crustdata-v2** | Crustdata V2 LinkedIn Posts Keyword Search | 0.4 | Charged on match |
| **crustdata-v2** | Crustdata V2 Live Job Search | 0.4 |  |
| **[Forager](https://forager.ai/)** | Job Search | 0.63 |  |
| **linkedin\_scraper** | LinkedIn Extract Event | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Extract Job | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Extract Post | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Extract Similar Companies | 0.7 |  |
| **linkedin\_scraper** | LinkedIn Visit Company | 0.7 |  |
| **linkedin\_scraper** | Salesnavigator Extract Accounts List | 0.7 |  |
| **linkedin\_scraper** | Salesnavigator Extract Employees Count | 0.7 |  |
| **linkedin\_scraper** | Salesnavigator Extract Employees Distribution | 0.7 |  |
| **linkedin\_scraper** | Salesnavigator Extract Leads List | 0.7 |  |
| **linkedin\_scraper** | Salesnavigator Visit Company | 0.7 |  |
| **enformion** | Business Search | 4.9 | Charged on match |
| **[ZeroBounce](https://www.zerobounce.net/)** | Domain Search | 5.46 |  |
| **[Deepline Agent](https://code.deepline.com/)** | Deeplineagent | Runtime usage |  |
| **[Exa](https://exa.ai/)** | Agent | Runtime usage |  |
| **[Exa](https://exa.ai/)** | Research | Runtime usage |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Agent | Runtime usage |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Extract | Runtime usage |  |
| **[Parallel](https://parallel.ai/)** | Run Task | Runtime usage |  |
| **[Apollo](https://www.apollo.io/)** | News Articles Search | BYOK |  |
| **[Exa](https://exa.ai/)** | Get Agent Run | Free |  |
| **[Exa](https://exa.ai/)** | Get Research Result | Free |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Cancel Agent | Free |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Cancel Batch Scrape | Free |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Cancel Crawl | Free |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Crawl Params Preview | Free |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Get Agent Status | Free |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Get Batch Scrape Errors | Free |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Get Batch Scrape Status | Free |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Get Crawl Errors | Free |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Get Crawl Status | Free |  |
| **[Firecrawl](https://www.firecrawl.dev/)** | Get Extract Status | Free |  |
| **[Parallel](https://parallel.ai/)** | Search Mcp | Free |  |
| **[Salesforce](https://www.salesforce.com/)** | List Tickets | Free |  |

Monitor competitor ad spend across platforms.

| Provider | Endpoint | Credits | Notes |
| --- | --- | --- | --- |
| **google\_ads\_audiences** | Create Audience | **0** |  |
| **google\_ads\_audiences** | Delete Audience | 0 |  |
| **google\_ads\_audiences** | Get Audience Status | 0 |  |
| **google\_ads\_audiences** | List Audiences | 0 |  |
| **google\_ads\_audiences** | Sync Audience Members | 0 |  |
| **linkedin\_ads\_audiences** | Create Audience | 0 |  |
| **linkedin\_ads\_audiences** | Delete Audience | 0 |  |
| **linkedin\_ads\_audiences** | Get Audience Status | 0 |  |
| **linkedin\_ads\_audiences** | List Audiences | 0 |  |
| **linkedin\_ads\_audiences** | Sync Audience Members | 0 |  |
| **meta\_audiences** | Create Audience | 0 |  |
| **meta\_audiences** | Delete Audience | 0 |  |
| **meta\_audiences** | Get Audience Status | 0 |  |
| **meta\_audiences** | List Audiences | 0 |  |
| **meta\_audiences** | Sync Audience Members | 0 |  |
| **[DataForSEO](https://dataforseo.com/)** | AI Optimization AI Keyword Data Keywords Search Volume Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Amazon Product Competitors Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Amazon Product Keyword Intersections Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Amazon Ranked Keywords Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Amazon Related Keywords Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Apple App Competitors Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Apple Keywords For App Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google App Competitors Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google Keywords For App Live | 0.01 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google Bulk Keyword Difficulty Live | 0.02 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google Categories For Keywords Live | 0.02 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google Historical Keyword Data Live | 0.02 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google Keyword Ideas Live | 0.02 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google Keyword Overview Live | 0.02 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google Keyword Suggestions Live | 0.02 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google Keywords For Categories Live | 0.02 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google Keywords For Site Live | 0.02 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google Ranked Keywords Live | 0.02 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google Related Keywords Live | 0.02 |  |
| **[DataForSEO](https://dataforseo.com/)** | Dataforseo Labs Google SERP Competitors Live | 0.02 |  |
| **[LeadMagic](https://leadmagic.io/)** | B2B Ads Search | 0.07 |  |
| **[LeadMagic](https://leadmagic.io/)** | Google Ads Search | 0.07 |  |
| **[LeadMagic](https://leadmagic.io/)** | Meta Ads Search | 0.07 |  |
| **[Adyntel](https://adyntel.com/)** | Domain Keywords | 0.13 |  |
| **[Adyntel](https://adyntel.com/)** | Google Shopping Sync | 0.13 |  |
| **[Adyntel](https://adyntel.com/)** | Tiktok Ad Details | 0.13 |  |
| **[BuiltWith](https://builtwith.com/)** | Keywords | 0.14 |  |
| **[LeadMagic](https://leadmagic.io/)** | B2B Ads Details | 0.68 |  |
| **[Adyntel](https://adyntel.com/)** | Google Shopping Status | Free |  |
| **[Forager](https://forager.ai/)** | Organization Keyword Autocomplete | Free |  |

Push enriched contacts to email and LinkedIn sequencers.

| Platform | What You Can Do | Credits |
| --- | --- | --- |
| **emailbison** | Campaign, lead, sender account, inbox, warmup, webhook, and workspace management for EmailBison outbound workflows. | **Free** |
| **[HeyReach](https://www.heyreach.io/)** | LinkedIn outreach campaign management and contact sequencing | **Free** |
| **[Instantly](https://instantly.ai/)** | Campaign management, lead import, activation controls, performance stats | **Free** |
| **[Lemlist](https://www.lemlist.com/)** | Multi-channel campaign sequencing with email and LinkedIn touches | **Free** |
| **[SmartLead](https://smartlead.ai/)** | Smartlead campaign management and lead import for outbound execution | **Free** |

Deepline-native multi-provider cascades.

| Operation | Credits | Notes |
| --- | --- | --- |
| **Search Contact** | 0.56 |  |
| **Enrich Company** | 0.98 | Charged on match |
| **Enrich Contact** | 0.98 |  |
| **Prospector** | 1.4 |  |
| **Job Change** | 1.96 | Charged on confirmed move |
| **Enrich Phone** | 4.9 | Charged on match |
| **Company Domain To Individual** | See providers |  |
| **Company Domain To Individual Email** | See providers |  |
| **Company Domain To Individual First Last** | See providers |  |
| **Company Domain To Individual LinkedIn URL** | See providers |  |
| **Company Domain To LinkedIn Employees** | See providers |  |
| **Company LinkedIn URL To Individual** | See providers |  |
| **Company LinkedIn URL To Individual Email** | See providers |  |
| **Company LinkedIn URL To Individual First Last** | See providers |  |
| **Company LinkedIn URL To Individual LinkedIn URL** | See providers |  |
| **Company Name To Individual** | See providers |  |
| **Company Name To Individual Email** | See providers |  |
| **Company Name To Individual First Last** | See providers |  |
| **Company Name To Individual LinkedIn URL** | See providers |  |
| **Company To Contact By Role Waterfall** | See providers |  |
| **Contact To Phone Waterfall** | See providers |  |
| **Engagers To Icp Qualification** | See providers |  |
| **Js Test Extract** | See providers |  |
| **LinkedIn Post To Engagers** | See providers |  |
| **Name And Domain To Email Waterfall** | See providers |  |
| **Name To LinkedIn URL Waterfall** | See providers |  |
| **Person LinkedIn Sales Nav URL To Email** | See providers |  |
| **Person LinkedIn Sales Nav URL To Job History** | See providers |  |
| **Person LinkedIn Sales Nav URL To Personal Email** | See providers |  |
| **Person LinkedIn Sales Nav URL To Phone** | See providers |  |
| **Person LinkedIn Sales Nav URL To Work Email** | See providers |  |
| **Person LinkedIn To Email Waterfall** | See providers |  |
| **Person LinkedIn URL Context To Email** | See providers |  |
| **Person LinkedIn URL Context To Job History** | See providers |  |
| **Person LinkedIn URL Context To Personal Email** | See providers |  |
| **Person LinkedIn URL Context To Phone** | See providers |  |
| **Person LinkedIn URL Context To Work Email** | See providers |  |
| **Person LinkedIn URL To Email** | See providers |  |
| **Person LinkedIn URL To Job History** | See providers |  |
| **Person LinkedIn URL To Personal Email** | See providers |  |
| **Person LinkedIn URL To Phone** | See providers |  |
| **Person LinkedIn URL To Work Email** | See providers |  |
| **Person Name Company To Email** | See providers |  |
| **Person Name Company To Job History** | See providers |  |
| **Person Name Company To LinkedIn URL** | See providers |  |
| **Person Name Company To Personal Email** | See providers |  |
| **Person Name Company To Work Email** | See providers |  |
| **Person Name Domain To Email** | See providers |  |
| **Person Name Domain To Job History** | See providers |  |
| **Person Name Domain To LinkedIn URL** | See providers |  |
| **Person Name Domain To Personal Email** | See providers |  |
| **Person Name Domain To Phone** | See providers |  |
| **Person Name Domain To Work Email** | See providers |  |
| **Personal Email To LinkedIn Waterfall** | See providers |  |
| **Personal Email Waterfall** | See providers |  |
| **Test Play** | See providers |  |

* * *

## Full provider rate cards

Every endpoint per provider, in Deepline credits. BYOK users pay $0 for all of these.

Adyntel — 9 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Domain Keywords | 0.13 | Per request |  |
| Facebook | 0.13 | Per request |  |
| Facebook Ad Search | 0.13 | Per request |  |
| Google | 0.13 | Per request |  |
| Google Shopping Sync | 0.13 | Per request |  |
| LinkedIn | 0.13 | Per request |  |
| Tiktok Ad Details | 0.13 | Per request |  |
| Tiktok Search | 0.13 | Per request |  |
| Google Shopping Status | Free | Free |  |

AI Ark — 11 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Company Search | 0.02 | Per result | Charged on match |
| People Search | 0.07 | Per result | Charged on match |
| Reverse Lookup | 0.07 | Per request |  |
| Export People | 0.14 | Per result |  |
| Find Emails | 0.14 | Per result |  |
| Personality Analysis | 0.56 | Per request |  |
| Mobile Phone Finder | 0.7 | Per request |  |
| Email Finder Results | Free | Free |  |
| Email Finder Statistics | Free | Free |  |
| Export Results | Free | Free |  |
| Export Statistics | Free | Free |  |

allegrow — 6 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Validate | 0.12 | Per result |  |
| Validate Async | 0.12 | Per request |  |
| Create CSV Validation Job | Free | Free |  |
| Download CSV Job Results | Free | Free |  |
| Get CSV Job Status | Free | Free |  |
| Validate Status | Free | Free |  |

Apify — 10 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Run Actor | Runtime usage | Usage-based |  |
| Run Actor Sync | Runtime usage | Usage-based |  |
| Get Actor | Free | Free |  |
| Get Actor Input Schema | Free | Free |  |
| Get Actor Run | Free | Free |  |
| Get Dataset Items | Free | Free |  |
| Get Kv Record | Free | Free |  |
| Get Log | Free | Free |  |
| List Actor Runs | Free | Free |  |
| List Store Actors | Free | Free |  |

Apollo — 15 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Bulk Organization Enrichment | BYOK | Per result |  |
| Bulk People Enrichment | BYOK | Per result |  |
| Company Search | BYOK | Per request |  |
| Enrich Company | BYOK | Per request |  |
| Enrich Person | BYOK | Per result |  |
| Organization Enrich | BYOK | Per request |  |
| Organization Job Postings | BYOK | Per result |  |
| People Match | BYOK | Per result |  |
| People Search | BYOK | Per request |  |
| People Search Paid | BYOK | Per request |  |
| Reveal Person | BYOK | Per result |  |
| Search People With Match | BYOK | Per result |  |
| Get Complete Organization Info | BYOK | Per request |  |
| News Articles Search | BYOK | Per request |  |
| Search People | BYOK | Per request |  |

aviato — 27 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Geocoder Search | 0.02 | Per request |  |
| Company Search | 0.04 | Per request |  |
| Person Search | 0.04 | Per request |  |
| Get Employees | 0.07 | Per request |  |
| Get Founded Companies | 0.07 | Per request |  |
| Company Bulk Enrich | 0.14 | Per result |  |
| Company Enrich | 0.14 | Per result | Preview free, full enrich charged |
| Get Company Founders | 0.14 | Per request |  |
| Get Company Fund Investments | 0.14 | Per request |  |
| Get Company Funding Rounds | 0.14 | Per request |  |
| Get Company Funds | 0.14 | Per request |  |
| Get Company Outbound Investments | 0.14 | Per request |  |
| Get Fund Investments | 0.14 | Per request |  |
| Person Bulk Contact Info | 0.14 | Per result |  |
| Person Bulk Enrich | 0.14 | Per result |  |
| Person Enrich | 0.14 | Per result | Preview free, full enrich charged |
| Person Get Company Investments | 0.14 | Per request |  |
| Get Acquisitions | 0.28 | Per request |  |
| Get Post Comments | 0.28 | Per request |  |
| Get Post Reactions | 0.28 | Per request |  |
| Get Post Reshares | 0.28 | Per request |  |
| Get Company Posts | 0.7 | Per request |  |
| Get Person Posts | 0.7 | Per request |  |
| Pull Contact Info | 1.12 | Per request |  |
| Pull Email Hash | 1.12 | Per request |  |
| Get Vestings | 1.4 | Per request |  |
| Generate Map | 2.1 | Per request |  |

BetterContact — 3 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Bulk Enrich | 0.69 | Per result | Phone-enabled: 7.55 credits with `enrich_phone_number=true`. |
| Enrich | 0.69 | Per result | Phone-enabled: 7.55 credits with `enrich_phone_number=true`. |
| Get Result | Free | Free |  |

bloomberry — 5 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Search Job Postings | 0.09 | Per result |  |
| Get Company Tech Stack | 0.43 | Per request |  |
| Get Current Customers | 2.11 | Per result |  |
| Get Tech Stack Changes | 4.21 | Per result |  |
| List Vendors | Free | Free |  |

BuiltWith — 16 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Bulk Domain Lookup | 0.14 | Usage-based |  |
| Company To URL | 0.14 | Usage-based |  |
| Domain Lookup | 0.14 | Usage-based |  |
| Keywords | 0.14 | Usage-based |  |
| Product Search | 0.14 | Usage-based |  |
| Recommendations | 0.14 | Usage-based |  |
| Redirects | 0.14 | Usage-based |  |
| Relationships | 0.14 | Usage-based |  |
| Tag Lookup | 0.14 | Usage-based |  |
| Trust Lookup | 0.14 | Usage-based |  |
| Vector Search | 0.14 | Usage-based |  |
| Bulk Domain Job Result | Free | Free |  |
| Bulk Domain Job Status | Free | Free |  |
| Free Lookup | Free | Free |  |
| Lists | Free | Free |  |
| Trends | Free | Free |  |

contactout — 7 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Enrich Domain | 1.4 | Per result |  |
| Enrich Person | 1.4 | Per result |  |
| Search People | 1.4 | Per result |  |
| Check Email Status | Free | Free |  |
| Check Personal Email | Free | Free |  |
| Check Phone | Free | Free |  |
| Check Work Email | Free | Free |  |

Crustdata — 11 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| People Search | 0.02 | Per result |  |
| Persondb Search | 0.02 | Per result | Preview free, full enrich charged |
| Company Enrichment | 0.4 | Per result | Charged on match |
| Companydb Search | 0.4 | Per result |  |
| Enrich Company | 0.4 | Per result |  |
| LinkedIn Posts | 0.4 | Per result | Charged on match |
| Enrich Contact | 1.2 | Per result |  |
| People Enrich | 1.2 | Per result |  |
| Person Enrichment | 1.2 | Per result | Preview free, full enrich charged |
| Companydb Autocomplete | Free | Free |  |
| Persondb Autocomplete | Free | Free |  |

crustdata-v2 — 14 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Crustdata V2 Company Search Realtime | 0.4 | Per result | Charged on match |
| Crustdata V2 Companydb Search | 0.4 | Per result |  |
| Crustdata V2 Enrich Company | 0.4 | Per result |  |
| Crustdata V2 Enrich Person | 0.4 | Per result |  |
| Crustdata V2 Job Search | 0.4 | Per result |  |
| Crustdata V2 LinkedIn Posts | 0.4 | Per result | Charged on match |
| Crustdata V2 LinkedIn Posts Keyword Search | 0.4 | Per result | Charged on match |
| Crustdata V2 Live Job Search | 0.4 | Per result |  |
| Crustdata V2 People Search Realtime | 0.4 | Per result |  |
| Crustdata V2 Persondb Search | 0.4 | Per result | Preview free, full enrich charged |
| Crustdata V2 Companydb Autocomplete | Free | Free |  |
| Crustdata V2 Filters Autocomplete | Free | Free |  |
| Crustdata V2 Identify Company | Free | Free |  |
| Crustdata V2 Persondb Autocomplete | Free | Free |  |

customer\_db — 4 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Get Customer Db File | Free | Free |  |
| Materialize Free Customer Companies | Free | Free |  |
| Query Customer Db | Free | Free |  |
| Upsert Customer Db File | Free | Free |  |

DataForSEO — 99 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| AI Optimization AI Keyword Data Keywords Search Volume Live | 0.01 | Usage-based |  |
| Business Data Business Listings Categories Aggregation Live | 0.01 | Usage-based |  |
| Business Data Business Listings Search Live | 0.01 | Usage-based |  |
| Content Analysis Category Trends Live | 0.01 | Usage-based |  |
| Content Analysis Phrase Trends Live | 0.01 | Usage-based |  |
| Content Analysis Rating Distribution Live | 0.01 | Usage-based |  |
| Content Analysis Search Live | 0.01 | Usage-based |  |
| Content Analysis Sentiment Analysis Live | 0.01 | Usage-based |  |
| Content Analysis Summary Live | 0.01 | Usage-based |  |
| Dataforseo Labs Amazon Bulk Search Volume Live | 0.01 | Usage-based |  |
| Dataforseo Labs Amazon Product Competitors Live | 0.01 | Usage-based |  |
| Dataforseo Labs Amazon Product Keyword Intersections Live | 0.01 | Usage-based |  |
| Dataforseo Labs Amazon Product Rank Overview Live | 0.01 | Usage-based |  |
| Dataforseo Labs Amazon Ranked Keywords Live | 0.01 | Usage-based |  |
| Dataforseo Labs Amazon Related Keywords Live | 0.01 | Usage-based |  |
| Dataforseo Labs Apple App Competitors Live | 0.01 | Usage-based |  |
| Dataforseo Labs Apple App Intersection Live | 0.01 | Usage-based |  |
| Dataforseo Labs Apple Bulk App Metrics Live | 0.01 | Usage-based |  |
| Dataforseo Labs Apple Keywords For App Live | 0.01 | Usage-based |  |
| Dataforseo Labs Google App Competitors Live | 0.01 | Usage-based |  |
| Dataforseo Labs Google App Intersection Live | 0.01 | Usage-based |  |
| Dataforseo Labs Google Bulk App Metrics Live | 0.01 | Usage-based |  |
| Dataforseo Labs Google Keywords For App Live | 0.01 | Usage-based |  |
| Keywords Data Clickstream Data Bulk Search Volume Live | 0.01 | Usage-based |  |
| Keywords Data Clickstream Data Dataforseo Search Volume Live | 0.01 | Usage-based |  |
| Keywords Data Clickstream Data Global Search Volume Live | 0.01 | Usage-based |  |
| Keywords Data Dataforseo Trends Demography Live | 0.01 | Usage-based |  |
| Keywords Data Dataforseo Trends Explore Live | 0.01 | Usage-based |  |
| Keywords Data Dataforseo Trends Merged Data Live | 0.01 | Usage-based |  |
| Keywords Data Dataforseo Trends Subregion Interests Live | 0.01 | Usage-based |  |
| App Data Apple App Listings Search Live | 0.02 | Usage-based |  |
| App Data Google App Listings Search Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Bulk Keyword Difficulty Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Bulk Traffic Estimation Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Categories For Domain Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Categories For Keywords Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Domain Intersection Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Domain Metrics By Categories Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Domain Rank Overview Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Historical Bulk Traffic Estimation Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Historical Keyword Data Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Historical Rank Overview Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Historical Serps Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Keyword Ideas Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Keyword Overview Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Keyword Suggestions Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Keywords For Categories Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Keywords For Site Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Page Intersection Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Ranked Keywords Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Related Keywords Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Relevant Pages Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Search Intent Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google SERP Competitors Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Subdomains Live | 0.02 | Usage-based |  |
| Dataforseo Labs Google Top Searches Live | 0.02 | Usage-based |  |
| Domain Analytics Technologies Aggregation Technologies Live | 0.02 | Usage-based |  |
| Domain Analytics Technologies Domain Technologies Live | 0.02 | Usage-based |  |
| Domain Analytics Technologies Domains By HTML Terms Live | 0.02 | Usage-based |  |
| Domain Analytics Technologies Domains By Technology Live | 0.02 | Usage-based |  |
| Domain Analytics Technologies Technologies Summary Live | 0.02 | Usage-based |  |
| Domain Analytics Technologies Technology Stats Live | 0.02 | Usage-based |  |
| Domain Analytics Whois Overview Live | 0.02 | Usage-based |  |
| SERP Bing Organic Live Advanced | 0.03 | Usage-based |  |
| SERP Bing Organic Live HTML | 0.03 | Usage-based |  |
| SERP Bing Organic Live Regular | 0.03 | Usage-based |  |
| SERP Google Ads Advertisers Live Advanced | 0.03 | Usage-based |  |
| SERP Google Ads Search Live Advanced | 0.03 | Usage-based |  |
| SERP Google AI Mode Live HTML | 0.03 | Usage-based |  |
| SERP Google Autocomplete Live Advanced | 0.03 | Usage-based |  |
| SERP Google Dataset Info Live Advanced | 0.03 | Usage-based |  |
| SERP Google Dataset Search Live Advanced | 0.03 | Usage-based |  |
| SERP Google Events Live Advanced | 0.03 | Usage-based |  |
| SERP Google Finance Explore Live Advanced | 0.03 | Usage-based |  |
| SERP Google Finance Explore Live HTML | 0.03 | Usage-based |  |
| SERP Google Finance Markets Live Advanced | 0.03 | Usage-based |  |
| SERP Google Finance Markets Live HTML | 0.03 | Usage-based |  |
| SERP Google Finance Quote Live Advanced | 0.03 | Usage-based |  |
| SERP Google Finance Quote Live HTML | 0.03 | Usage-based |  |
| SERP Google Finance Ticker Search Live Advanced | 0.03 | Usage-based |  |
| SERP Google Images Live Advanced | 0.03 | Usage-based |  |
| SERP Google Images Live HTML | 0.03 | Usage-based |  |
| SERP Google Local Finder Live Advanced | 0.03 | Usage-based |  |
| SERP Google Local Finder Live HTML | 0.03 | Usage-based |  |
| SERP Google Maps Live Advanced | 0.03 | Usage-based |  |
| SERP Google News Live Advanced | 0.03 | Usage-based |  |
| SERP Google News Live HTML | 0.03 | Usage-based |  |
| SERP Google Organic Live Advanced | 0.03 | Usage-based |  |
| SERP Google Organic Live HTML | 0.03 | Usage-based |  |
| SERP Google Organic Live Regular | 0.03 | Usage-based |  |
| SERP Yahoo Organic Live Advanced | 0.03 | Usage-based |  |
| SERP Yahoo Organic Live HTML | 0.03 | Usage-based |  |
| SERP Yahoo Organic Live Regular | 0.03 | Usage-based |  |
| AI Optimization ChatGPT LLM Scraper Live Advanced | 0.06 | Per page |  |
| AI Optimization ChatGPT LLM Scraper Live HTML | 0.06 | Per page |  |
| AI Optimization Gemini LLM Scraper Live Advanced | 0.06 | Per page |  |
| AI Optimization Gemini LLM Scraper Live HTML | 0.06 | Per page |  |
| On Page Lighthouse Live JSON | 0.06 | Usage-based |  |
| SERP Google AI Mode Live Advanced | 0.06 | Usage-based |  |

datagma — 7 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Find Email | 0.14 | Per result |  |
| Enrich Company | 0.27 | Per result |  |
| Enrich Person | 0.27 | Per result |  |
| Find People | 1.31 | Per result |  |
| Search Phone Numbers | 3.91 | Usage-based |  |
| Full Enrichment | Runtime usage | Usage-based |  |
| Job Change Detection | Runtime usage | Usage-based |  |

Deepline Agent — 2 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| AI Inference | Runtime usage | Usage-based |  |
| Deeplineagent | Runtime usage | Usage-based |  |

Deepline Waterfall — 62 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Search Contact | 0.56 | Per result |  |
| Enrich Company | 0.98 | Per request | Charged on match |
| Enrich Contact | 0.98 | Per result |  |
| Prospector | 1.4 | Per result |  |
| Job Change | 1.96 | Per result | Charged on confirmed move |
| Enrich Phone | 4.9 | Per result | Charged on match |
| Company Domain To Individual | See providers | Composite |  |
| Company Domain To Individual Email | See providers | Composite |  |
| Company Domain To Individual First Last | See providers | Composite |  |
| Company Domain To Individual LinkedIn URL | See providers | Composite |  |
| Company Domain To LinkedIn Employees | See providers | Composite |  |
| Company LinkedIn URL To Individual | See providers | Composite |  |
| Company LinkedIn URL To Individual Email | See providers | Composite |  |
| Company LinkedIn URL To Individual First Last | See providers | Composite |  |
| Company LinkedIn URL To Individual LinkedIn URL | See providers | Composite |  |
| Company Name To Individual | See providers | Composite |  |
| Company Name To Individual Email | See providers | Composite |  |
| Company Name To Individual First Last | See providers | Composite |  |
| Company Name To Individual LinkedIn URL | See providers | Composite |  |
| Company To Contact By Role Waterfall | See providers | Composite |  |
| Contact To Phone Waterfall | See providers | Composite |  |
| Engagers To Icp Qualification | See providers | Composite |  |
| Js Test Extract | See providers | Composite |  |
| LinkedIn Post To Engagers | See providers | Composite |  |
| Name And Domain To Email Waterfall | See providers | Composite |  |
| Name To LinkedIn URL Waterfall | See providers | Composite |  |
| Person LinkedIn Sales Nav URL To Email | See providers | Composite |  |
| Person LinkedIn Sales Nav URL To Job History | See providers | Composite |  |
| Person LinkedIn Sales Nav URL To Personal Email | See providers | Composite |  |
| Person LinkedIn Sales Nav URL To Phone | See providers | Composite |  |
| Person LinkedIn Sales Nav URL To Work Email | See providers | Composite |  |
| Person LinkedIn To Email Waterfall | See providers | Composite |  |
| Person LinkedIn URL Context To Email | See providers | Composite |  |
| Person LinkedIn URL Context To Job History | See providers | Composite |  |
| Person LinkedIn URL Context To Personal Email | See providers | Composite |  |
| Person LinkedIn URL Context To Phone | See providers | Composite |  |
| Person LinkedIn URL Context To Work Email | See providers | Composite |  |
| Person LinkedIn URL To Email | See providers | Composite |  |
| Person LinkedIn URL To Job History | See providers | Composite |  |
| Person LinkedIn URL To Personal Email | See providers | Composite |  |
| Person LinkedIn URL To Phone | See providers | Composite |  |
| Person LinkedIn URL To Work Email | See providers | Composite |  |
| Person Name Company To Email | See providers | Composite |  |
| Person Name Company To Job History | See providers | Composite |  |
| Person Name Company To LinkedIn URL | See providers | Composite |  |
| Person Name Company To Personal Email | See providers | Composite |  |
| Person Name Company To Work Email | See providers | Composite |  |
| Person Name Domain To Email | See providers | Composite |  |
| Person Name Domain To Job History | See providers | Composite |  |
| Person Name Domain To LinkedIn URL | See providers | Composite |  |
| Person Name Domain To Personal Email | See providers | Composite |  |
| Person Name Domain To Phone | See providers | Composite |  |
| Person Name Domain To Work Email | See providers | Composite |  |
| Personal Email To LinkedIn Waterfall | See providers | Composite |  |
| Personal Email Waterfall | See providers | Composite |  |
| Test Play | See providers | Composite |  |
| Company Titles | Free | Free |  |
| Enrich Company Finder | Free | Free |  |
| Enrich Contact Finder | Free | Free |  |
| Enrich Phone Finder | Free | Free |  |
| Free Simple Company Search | Free | Free |  |
| Prospector Finder | Free | Free |  |

Dropleads — 7 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Email Verifier | 0.03 | Per result | Charged on hit |
| Email Finder | 0.26 | Per result | Charged on hit |
| Enrich Single Lead | 0.26 | Per result |  |
| Single Person Enrichment | 0.26 | Per result |  |
| Mobile Finder | 0.76 | Per result |  |
| Get Lead Count | Free | Free |  |
| Search People | Free | Free |  |

emailbison — 162 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Accept Workspace Invitation | Free | Free |  |
| Accept Workspace Invitation API Workspaces V1 1 Accept Team Invitation ID | Free | Free |  |
| Account Details | Free | Free |  |
| Activate Or Deactivate Asequence Step Variant | Free | Free |  |
| Add Lead To Blacklist | Free | Free |  |
| Archive Campaign | Free | Free |  |
| Attach Scheduled Email To Reply | Free | Free |  |
| Attach Tags To Campaigns | Free | Free |  |
| Attach Tags To Email Accounts | Free | Free |  |
| Attach Tags To Leads | Free | Free |  |
| Breakdown Of Events By Date | Free | Free |  |
| Bulk Add Sender Emails | Free | Free |  |
| Bulk Check Missing Mxrecords | Free | Free |  |
| Bulk Create Leads | Free | Free |  |
| Bulk Create Leads Using CSV | Free | Free |  |
| Bulk Delete Campaigns By ID | Free | Free |  |
| Bulk Delete Leads By ID | Free | Free |  |
| Bulk Update Email Daily Limits | Free | Free |  |
| Bulk Update Email Signatures | Free | Free |  |
| Bulk Update Lead Status | Free | Free |  |
| Campaign Details | Free | Free |  |
| Check Mxrecords | Free | Free |  |
| Compose New Email | Free | Free |  |
| Create Acampaign | Free | Free |  |
| Create Anew Custom Variable | Free | Free |  |
| Create Anew Webhook | Free | Free |  |
| Create Apitoken For Workspace | Free | Free |  |
| Create Areply Template | Free | Free |  |
| Create Blacklisted Domain | Free | Free |  |
| Create Blacklisted Email | Free | Free |  |
| Create Campaign Schedule | Free | Free |  |
| Create Campaign Schedule From Template | Free | Free |  |
| Create Custom Tracking Domain | Free | Free |  |
| Create Ignore Phrase | Free | Free |  |
| Create Imapsmtpemail Account | Free | Free |  |
| Create Lead | Free | Free |  |
| Create New Reply | Free | Free |  |
| Create Sequence Stepsdeprecated | Free | Free |  |
| Create Sequence Stepsv11 | Free | Free |  |
| Create Tag | Free | Free |  |
| Create Userand Add To Workspace | Free | Free |  |
| Create Workspace | Free | Free |  |
| Create Workspace API Workspaces V1 1 | Free | Free |  |
| Delete Acampaign | Free | Free |  |
| Delete Alead | Free | Free |  |
| Delete Awebhook | Free | Free |  |
| Delete Email Account | Free | Free |  |
| Delete Reply | Free | Free |  |
| Delete Reply Template | Free | Free |  |
| Delete Sequence Step | Free | Free |  |
| Delete Workspace | Free | Free |  |
| Delete Workspace Member | Free | Free |  |
| Delete Workspace Member API Workspaces V1 1 Members User ID | Free | Free |  |
| Disable Warmup For Email Accounts | Free | Free |  |
| Duplicate Campaign | Free | Free |  |
| Enable Warmup For Email Accounts | Free | Free |  |
| Forward Reply | Free | Free |  |
| Generate Headless Uitokenbeta | Free | Free |  |
| Get Acustom Tracking Domains | Free | Free |  |
| Get All Blacklisted Domains | Free | Free |  |
| Get All Blacklisted Emails | Free | Free |  |
| Get All Campaign Sender Emails | Free | Free |  |
| Get All Custom Tracking Domains | Free | Free |  |
| Get All Custom Variables | Free | Free |  |
| Get All Ignore Phrases | Free | Free |  |
| Get All Leads | Free | Free |  |
| Get All Leads For Campaign | Free | Free |  |
| Get All Replies | Free | Free |  |
| Get All Replies For Lead | Free | Free |  |
| Get All Reply Templates | Free | Free |  |
| Get All Scheduled Emails | Free | Free |  |
| Get All Scheduled Emails API Scheduled Emails | Free | Free |  |
| Get All Scheduled Emails For Campaign | Free | Free |  |
| Get All Sent Emails For Alead | Free | Free |  |
| Get All Tags For Workspace | Free | Free |  |
| Get All Webhook Event Types | Free | Free |  |
| Get All Webhooks | Free | Free |  |
| Get Asingle Webhook | Free | Free |  |
| Get Blacklisted Domain | Free | Free |  |
| Get Blacklisted Email | Free | Free |  |
| Get Campaign Replies | Free | Free |  |
| Get Campaign Statssummary | Free | Free |  |
| Get Email Account Oauth Access Token | Free | Free |  |
| Get Email Account Replies | Free | Free |  |
| Get Full Normalized Stats By Date | Free | Free |  |
| Get Full Normalized Stats By Date API Workspaces V1 1 Line Area Chart Stats | Free | Free |  |
| Get Master Inbox Settings | Free | Free |  |
| Get Reply | Free | Free |  |
| Get Reply Conversation Thread | Free | Free |  |
| Get Sample Webhook Event Payload | Free | Free |  |
| Get Scheduled Email | Free | Free |  |
| Get Single Ignore Phrase | Free | Free |  |
| Get Single Lead | Free | Free |  |
| Get Workspace Statssummary | Free | Free |  |
| Import Leads By Ids | Free | Free |  |
| Import Leads From Existing List | Free | Free |  |
| Import Sender Emails By ID | Free | Free |  |
| Invite Team Member | Free | Free |  |
| Invite Team Member API Workspaces V1 1 Invite Members | Free | Free |  |
| List Campaigns | Free | Free |  |
| List Email Accounts | Free | Free |  |
| List Email Accounts With Warmup Stats | Free | Free |  |
| List Workspaces | Free | Free |  |
| List Workspaces API Workspaces V1 1 | Free | Free |  |
| Mark As Automated Or Not Automated | Free | Free |  |
| Mark As Interested | Free | Free |  |
| Mark As Not Interested | Free | Free |  |
| Mark As Read Or Unread | Free | Free |  |
| Move Leads To Another Campaign | Free | Free |  |
| Pause Campaign | Free | Free |  |
| Push Replyand Lead Toreply Followup Campaign | Free | Free |  |
| Remove Blacklisted Domain | Free | Free |  |
| Remove Blacklisted Email | Free | Free |  |
| Remove Custom Tracking Domain | Free | Free |  |
| Remove Ignore Phrase | Free | Free |  |
| Remove Leads From Acampaign | Free | Free |  |
| Remove Sender Emails By ID | Free | Free |  |
| Remove Tag | Free | Free |  |
| Remove Tags From Campaigns | Free | Free |  |
| Remove Tags From Email Accounts | Free | Free |  |
| Remove Tags From Leads | Free | Free |  |
| Reply Template Details | Free | Free |  |
| Resume Campaign | Free | Free |  |
| Send Atest Webhook Event | Free | Free |  |
| Send Sequence Step Test Email | Free | Free |  |
| Show Email Account Campaigns | Free | Free |  |
| Show Email Account Details | Free | Free |  |
| Show Sending Schedule For Campaign | Free | Free |  |
| Show Sending Schedules For Campaigns | Free | Free |  |
| Show Single Email Account With Warmup Details | Free | Free |  |
| Stop Future Emails For Leads | Free | Free |  |
| Switch Workspace | Free | Free |  |
| Switch Workspace API Workspaces V1 1 Switch Workspace | Free | Free |  |
| Unsubscribe Contact That Replied | Free | Free |  |
| Unsubscribe Lead | Free | Free |  |
| Update Areply Template | Free | Free |  |
| Update Awebhook | Free | Free |  |
| Update Campaign Schedule | Free | Free |  |
| Update Campaign Settings | Free | Free |  |
| Update Daily Warmup Limits For Email Accounts | Free | Free |  |
| Update Lead | Free | Free |  |
| Update Lead API Leads Lead ID | Free | Free |  |
| Update Lead Status | Free | Free |  |
| Update Master Inbox Settings | Free | Free |  |
| Update Or Create Lead | Free | Free |  |
| Update Or Create Multiple Leads | Free | Free |  |
| Update Password | Free | Free |  |
| Update Profile Picture | Free | Free |  |
| Update Sender Email | Free | Free |  |
| Update Sequence Stepsdeprecated | Free | Free |  |
| Update Sequence Stepsv11 | Free | Free |  |
| Update Workspace | Free | Free |  |
| Update Workspace API Workspaces V1 1 Team ID | Free | Free |  |
| Update Workspace Member | Free | Free |  |
| Update Workspace Member Role | Free | Free |  |
| View All Available Schedule Timezones | Free | Free |  |
| View All Schedule Templates | Free | Free |  |
| View Campaign Schedule | Free | Free |  |
| View Campaign Sequence Stepsdeprecated | Free | Free |  |
| View Campaign Sequence Stepsv11 | Free | Free |  |
| Workspace Details | Free | Free |  |
| Workspace Details API Workspaces V1 1 Team ID | Free | Free |  |

enformion — 4 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Contact Enrich | 1.68 | Per result | Charged on match |
| Person Search | 3.5 | Per result | Charged on match |
| Business Search | 4.9 | Per result | Charged on match |
| Workplace Search | 7 | Per result | Charged on match |

Exa — 11 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Contents | 0.02 | Per result |  |
| Answer | 0.07 | Per request |  |
| Company Search | 0.07 | Per result |  |
| People Search | 0.07 | Per result |  |
| Search | 0.07 | Per result |  |
| Search Companies | 0.07 | Per result |  |
| Search People | 0.07 | Per result |  |
| Agent | Runtime usage | Usage-based |  |
| Research | Runtime usage | Usage-based |  |
| Get Agent Run | Free | Free |  |
| Get Research Result | Free | Free |  |

findymail — 29 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Find Employees | 0.28 | Per result |  |
| Find From Business Profile | 0.28 | Per result |  |
| Find From Domain | 0.28 | Per result |  |
| Find From Name | 0.28 | Per result |  |
| Get Company Information | 0.28 | Per result |  |
| Lookup Technologies By Domain | 0.28 | Per result |  |
| Search Leads | 0.28 | Per result |  |
| Verify Email | 0.28 | Per request |  |
| Reverse Email Lookup | 0.56 | Per result |  |
| Find Phone | 2.78 | Per result |  |
| Search Lookalike Companies | Runtime usage | Usage-based |  |
| Add Excluded Domains | Free | Free |  |
| Create Exclusion List | Free | Free |  |
| Create List | Free | Free |  |
| Delete Exclusion List | Free | Free |  |
| Delete List | Free | Free |  |
| Get Excluded Domains | Free | Free |  |
| Get Exclusion List Details | Free | Free |  |
| Get Export Status | Free | Free |  |
| Get Results | Free | Free |  |
| Get Saved Contacts | Free | Free |  |
| Get Signal | Free | Free |  |
| List Contact Lists | Free | Free |  |
| List Exclusion Lists | Free | Free |  |
| List Signals | Free | Free |  |
| Remove Excluded Domains | Free | Free |  |
| Search Technologies | Free | Free |  |
| Update Contact List | Free | Free |  |
| Update Exclusion List | Free | Free |  |

Firecrawl — 17 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Batch Scrape | 0.02 | Per page |  |
| Crawl | 0.02 | Per page |  |
| Map | 0.02 | Per result |  |
| Scrape | 0.02 | Per request |  |
| Search | 0.03 | Per result |  |
| Agent | Runtime usage | Usage-based |  |
| Extract | Runtime usage | Usage-based |  |
| Cancel Agent | Free | Free |  |
| Cancel Batch Scrape | Free | Free |  |
| Cancel Crawl | Free | Free |  |
| Crawl Params Preview | Free | Free |  |
| Get Agent Status | Free | Free |  |
| Get Batch Scrape Errors | Free | Free |  |
| Get Batch Scrape Status | Free | Free |  |
| Get Crawl Errors | Free | Free |  |
| Get Crawl Status | Free | Free |  |
| Get Extract Status | Free | Free |  |

Forager — 19 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Organization Search | 0.32 | Per page |  |
| Person Detail Lookup | 0.32 | Per result |  |
| Person Role Search | 0.32 | Per page |  |
| Website Detail Lookup | 0.32 | Per result |  |
| Job Search | 0.63 | Per page |  |
| Person Contacts Lookup Personal Emails | 1.56 | Per result |  |
| Person Contacts Lookup Work Emails | 1.56 | Per result |  |
| Person Detail Reverse Lookup By Email | 1.56 | Per result |  |
| Person Contacts Lookup Phone Numbers | 4.67 | Per result |  |
| Person Detail Reverse Lookup By Phone Number | 4.67 | Per result |  |
| Industry Autocomplete | Free | Free |  |
| Job Search Totals | Free | Free |  |
| Location Autocomplete | Free | Free |  |
| Organization Autocomplete | Free | Free |  |
| Organization Keyword Autocomplete | Free | Free |  |
| Organization Search Totals | Free | Free |  |
| Person Role Search Totals | Free | Free |  |
| Person Skill Autocomplete | Free | Free |  |
| Web Technology Autocomplete | Free | Free |  |

FullEnrich — 7 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Company Search | 0.21 | Usage-based |  |
| People Search | 0.21 | Usage-based |  |
| Bulk Enrich | 0.84 | Per result | Phone-enabled: 8.38 credits when `contact.phones` is requested. |
| Reverse Email | 0.84 | Per result |  |
| Get Result | Free | Free |  |
| Get Reverse Result | Free | Free |  |
| Verify Key | Free | Free |  |

generic\_http — 1 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Request | Free | Free |  |

google\_ads\_audiences — 5 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Create Audience | 0 | Per request |  |
| Delete Audience | 0 | Per request |  |
| Get Audience Status | 0 | Per request |  |
| List Audiences | 0 | Per request |  |
| Sync Audience Members | 0 | Per request |  |

Hunter — 8 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Email Verifier | 0.15 | Per result |  |
| Combined Find | 0.3 | Per result | Charged on match |
| Companies Find | 0.3 | Per result |  |
| Domain Search | 0.3 | Per result |  |
| Email Finder | 0.3 | Per result |  |
| People Find | 0.3 | Per result |  |
| Discover | Free | Free |  |
| Email Count | Free | Free |  |

Icypeas — 13 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Email Verification | 0.02 | Per request |  |
| Find Companies | 0.07 | Per result | Charged on match |
| Scrape Company | 0.07 | Per result |  |
| Bulk Search | 0.14 | Per result |  |
| Domain Search | 0.14 | Per result |  |
| Email Search | 0.14 | Per result | No charge if not found |
| Find Company URL | 0.14 | Per result |  |
| Find People | 0.14 | Per result | Charged on match |
| Scrape Profile | 0.21 | Per result |  |
| Count Companies | Free | Free |  |
| Count People | Free | Free |  |
| Read Bulk Files | Free | Free |  |
| Read Results | Free | Free |  |

IPQS — 4 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Batch Email Verify | 0.07 | Per result |  |
| Batch Phone Validate | 0.07 | Per result |  |
| Email Verify | 0.07 | Per result |  |
| Phone Validate | 0.07 | Per result |  |

LeadMagic — 16 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Employee Finder | 0.02 | Per result |  |
| B2B Ads Search | 0.07 | Per result |  |
| Google Ads Search | 0.07 | Per result |  |
| Meta Ads Search | 0.07 | Per result |  |
| Email Validation | 0.09 | Per result |  |
| Company Search | 0.34 | Per result |  |
| Email Finder | 0.34 | Per result |  |
| Jobs Finder | 0.34 | Per result |  |
| Profile Search | 0.34 | Per result |  |
| B2B Ads Details | 0.68 | Per result |  |
| Personal Email Finder | 0.68 | Per result |  |
| Role Finder | 0.68 | Per result |  |
| Company Funding | 1.35 | Per result |  |
| B2B Social Email | 1.68 | Per result |  |
| Mobile Finder | 1.68 | Per result |  |
| Email To Profile | 3.36 | Per result |  |

limadata — 13 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Search Web | 0.03 | Per request |  |
| Research Search | 0.09 | Per request |  |
| Enrich Company | 0.28 | Per request |  |
| Enrich Person | 0.28 | Per result |  |
| Find Audience Identifiers | 0.28 | Per request |  |
| Find Company LinkedIn | 0.28 | Per request |  |
| Find Work Email | 0.28 | Per request |  |
| Research Extract | 0.28 | Per result |  |
| Find Person Profiles | 0.56 | Per request |  |
| Find Work Email LinkedIn | 0.84 | Per request |  |
| Find Personal Email | 1.4 | Per request |  |
| Reverse Email Lookup | 1.4 | Per request |  |
| Find Phone | 2.8 | Per request |  |

linkedin\_ads\_audiences — 5 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Create Audience | 0 | Per request |  |
| Delete Audience | 0 | Per request |  |
| Get Audience Status | 0 | Per request |  |
| List Audiences | 0 | Per request |  |
| Sync Audience Members | 0 | Per request |  |

linkedin\_scraper — 40 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| LinkedIn Extract Post Likers | 0.03 | Per result |  |
| LinkedIn Extract Licenses Certifications | 0.04 | Per result |  |
| LinkedIn Extract People Comment Activity | 0.04 | Per result |  |
| LinkedIn Extract People Experiences | 0.04 | Per result |  |
| LinkedIn Extract People Post Activity | 0.04 | Per result |  |
| LinkedIn Extract People Reaction Activity | 0.04 | Per result |  |
| LinkedIn Extract People Skills | 0.04 | Per result |  |
| LinkedIn Extract Event Attendees | 0.07 | Per result |  |
| LinkedIn Extract Group Members | 0.07 | Per result |  |
| LinkedIn Extract Post Commenters | 0.07 | Per result |  |
| LinkedIn Extract Post Reposters | 0.07 | Per result |  |
| LinkedIn Extract School Alumnis | 0.07 | Per result |  |
| LinkedIn Extract Company | 0.7 | Per result |  |
| LinkedIn Extract Company Affiliates | 0.7 | Per result |  |
| LinkedIn Extract Company Employees Insights | 0.7 | Per result |  |
| LinkedIn Extract Event | 0.7 | Per result |  |
| LinkedIn Extract Job | 0.7 | Per result |  |
| LinkedIn Extract People | 0.7 | Per result |  |
| LinkedIn Extract Post | 0.7 | Per result |  |
| LinkedIn Extract Similar Companies | 0.7 | Per result |  |
| LinkedIn Search Companies | 0.7 | Per result |  |
| LinkedIn Search Company Employees | 0.7 | Per result |  |
| LinkedIn Search Content | 0.7 | Per result |  |
| LinkedIn Search Events | 0.7 | Per result |  |
| LinkedIn Search Groups | 0.7 | Per result |  |
| LinkedIn Search Jobs | 0.7 | Per result |  |
| LinkedIn Search People | 0.7 | Per result |  |
| LinkedIn Search Schools | 0.7 | Per result |  |
| LinkedIn Visit Company | 0.7 | Per result |  |
| Recruiterlite Search People | 0.7 | Per result |  |
| Salesnavigator Extract Accounts List | 0.7 | Per result |  |
| Salesnavigator Extract Company | 0.7 | Per result |  |
| Salesnavigator Extract Employees Count | 0.7 | Per result |  |
| Salesnavigator Extract Employees Distribution | 0.7 | Per result |  |
| Salesnavigator Extract Leads List | 0.7 | Per result |  |
| Salesnavigator Search Companies | 0.7 | Per result |  |
| Salesnavigator Search Company Employees | 0.7 | Per result |  |
| Salesnavigator Search Metrics | 0.7 | Per result |  |
| Salesnavigator Search People | 0.7 | Per result |  |
| Salesnavigator Visit Company | 0.7 | Per result |  |

lusha — 3 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Enrich Company | 0.7 | Per result |  |
| Enrich Person | 0.7 | Per result |  |
| Search Contacts | 0.7 | Per result |  |

meta\_audiences — 5 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Create Audience | 0 | Per request |  |
| Delete Audience | 0 | Per request |  |
| Get Audience Status | 0 | Per request |  |
| List Audiences | 0 | Per request |  |
| Sync Audience Members | 0 | Per request |  |

openmart — 11 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Enrich Company | 0.13 | Per result |  |
| Search Brands | 0.13 | Per result |  |
| Search Business Ids | 0.13 | Per result |  |
| Search Businesses | 0.13 | Per result |  |
| Find People | 0.42 | Per result |  |
| Get Task Result | 0.42 | Per result |  |
| Lookup Business Email | 0.42 | Per result |  |
| Lookup People | 0.42 | Per result |  |
| Find Tech | 0.84 | Per result |  |
| Get Batch Status | Free | Free |  |
| Get Batch Task Ids | Free | Free |  |

opensosdata — 1 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Business Lookup | 0.44 | Per result |  |

OpenWebNinja — 24 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Localbusiness Autocomplete | 0.06 | Per result |  |
| Localbusiness Business Details | 0.06 | Usage-based |  |
| Localbusiness Business Photos | 0.06 | Per result |  |
| Localbusiness Business Posts | 0.06 | Per result |  |
| Localbusiness Business Reviews | 0.06 | Per result |  |
| Localbusiness Photo Details | 0.06 | Usage-based |  |
| Localbusiness Reverse Geocoding | 0.06 | Per result |  |
| Localbusiness Review Details | 0.06 | Usage-based |  |
| Localbusiness Search | 0.06 | Per result |  |
| Localbusiness Search In Area | 0.06 | Per result |  |
| Localbusiness Search Nearby | 0.06 | Per result |  |
| Glassdoor Company Interviews | 0.07 | Per request |  |
| Glassdoor Company Jobs | 0.07 | Per request |  |
| Glassdoor Company Overview | 0.07 | Per request |  |
| Glassdoor Company Reviews | 0.07 | Per request |  |
| Glassdoor Company Salaries | 0.07 | Per request |  |
| Glassdoor Company Salaries V2 | 0.07 | Per request |  |
| Glassdoor Company Search | 0.07 | Per request |  |
| Glassdoor Job Search | 0.07 | Per request |  |
| Glassdoor Salary Estimation | 0.07 | Per request |  |
| Jsearch Company Job Salary | 0.07 | Per result |  |
| Jsearch Estimated Salary | 0.07 | Per result |  |
| Jsearch Job Details | 0.07 | Usage-based |  |
| Jsearch Search | 0.07 | Usage-based |  |

Parallel — 9 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Extract | 0.02 | Per result |  |
| Search | 0.02 | Per result |  |
| Run Findall | Runtime usage | Usage-based |  |
| Run Task | Runtime usage | Usage-based |  |
| Fetch Mcp | Free | Free |  |
| Findall Run Result | Free | Free |  |
| Get Task Run Result | Free | Free |  |
| Retrieve Findall Run Status | Free | Free |  |
| Search Mcp | Free | Free |  |

People Data Labs — 11 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Bulk Organization Enrichment | 1.4 | Per result |  |
| Company Search | 1.4 | Per result |  |
| Enrich Company | 1.4 | Per result |  |
| Bulk People Enrichment | 3.92 | Per result |  |
| Enrich Contact | 3.92 | Per result |  |
| Person Search | 3.92 | Per result |  |
| Person Identify | 7.7 | Per result | Charged on match |
| Autocomplete | Free | Free |  |
| Company Clean | Free | Free |  |
| Location Clean | Free | Free |  |
| School Clean | Free | Free |  |

predictleads — 24 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Company | 0.56 | Per request |  |
| Company Connections | 0.56 | Per request |  |
| Company Financing Events | 0.56 | Per request |  |
| Company Github Repositories | 0.56 | Per request |  |
| Company Job Openings | 0.56 | Per request |  |
| Company News Events | 0.56 | Per request |  |
| Company Products | 0.56 | Per request |  |
| Company Similar Companies | 0.56 | Per result |  |
| Company Technology Detections | 0.56 | Per request |  |
| Company Website Evolution | 0.56 | Per request |  |
| Discover Companies | 0.56 | Per result |  |
| Discover Financing Events | 0.56 | Per result |  |
| Discover Job Openings | 0.56 | Per result |  |
| Discover News Events | 0.56 | Per result |  |
| Discover Portfolio Company Connections | 0.56 | Per result |  |
| Discover Products | 0.56 | Per result |  |
| Discover Technology Detections | 0.56 | Per result |  |
| Extended Technology Detection | 0.56 | Per request |  |
| Job Opening | 0.56 | Per request |  |
| News Event | 0.56 | Per request |  |
| Product | 0.56 | Per request |  |
| Startup Platform Posts | 0.56 | Per result |  |
| Technologies | 0.56 | Per request |  |
| Technology | 0.56 | Per request |  |

Prospeo — 6 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Bulk Enrich Company | 0.55 | Per result | Charged on match |
| Enrich Company | 0.55 | Per result |  |
| Enrich Person | 0.55 | Per result |  |
| Search Company | 0.55 | Per result |  |
| Search Person | 0.55 | Per result |  |
| Search Suggestions | Free | Free |  |

rocketreach — 3 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Lookup Company | 0.07 | Per request |  |
| Search People | 0.14 | Per result |  |
| Lookup Person | 1.68 | Per result |  |

Salesforce — 19 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Create Account | Free | Free |  |
| Create Contact | Free | Free |  |
| Create Lead | Free | Free |  |
| Create Opportunity | Free | Free |  |
| Delete Account | Free | Free |  |
| Delete Contact | Free | Free |  |
| Delete Lead | Free | Free |  |
| Delete Opportunity | Free | Free |  |
| Fetch Fields | Free | Free |  |
| Get Record | Free | Free |  |
| List Accounts | Free | Free |  |
| List Contacts | Free | Free |  |
| List Leads | Free | Free |  |
| List Opportunities | Free | Free |  |
| List Tickets | Free | Free |  |
| Update Account | Free | Free |  |
| Update Contact | Free | Free |  |
| Update Lead | Free | Free |  |
| Update Opportunity | Free | Free |  |

scrapecreators — 6 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Instagram Reels Search | 0.02 | Per request |  |
| Reddit Post Comments | 0.02 | Per request |  |
| Reddit Search | 0.02 | Per request |  |
| Tiktok Keyword Search | 0.02 | Per request |  |
| YouTube Search | 0.02 | Per request |  |
| YouTube Transcript | 0.02 | Per request |  |

Serper — 2 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Google Search | 0.02 | Per result |  |
| Google Maps Search | 0.04 | Per result |  |

slack — 6 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Get Channel History | Free | Free |  |
| Get User | Free | Free |  |
| List Channels | Free | Free |  |
| Message With Hitl | Free | Free |  |
| Post Message | Free | Free |  |
| Reply In Thread | Free | Free |  |

theirstack — 4 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Job Search | 0.02 | Per result | Preview free, full enrich charged |
| Company Search | 0.05 | Per result | Charged on match |
| Technographics | 0.05 | Per result |  |
| Catalog Keywords | Free | Free |  |

Trestle — 2 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Phone Validation | 0.21 | Per result |  |
| Real Contact | 0.42 | Per result |  |

upcell — 2 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Enrich Contact | 1.4 | Per result | Charged on match |
| Contact Existence | Free | Free |  |

wiza — 2 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Reveal Person | 0.35 | Per result |  |
| Search Prospects | Free | Free |  |

wizleads — 6 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Verify Email | 0.02 | Per request |  |
| Find Email | 0.04 | Per result |  |
| Get Company LinkedIn ID | 0.04 | Per result |  |
| Scrape Salesnav | 0.04 | Per result |  |
| Abort Task | Free | Free |  |
| Get Task | Free | Free |  |

ZeroBounce — 4 endpoints

| Endpoint | Credits | Model | Notes |
| --- | --- | --- | --- |
| Activity Data | 0.28 | Per result |  |
| Validate | 0.28 | Per result |  |
| Domain Search | 5.46 | Per result |  |
| Email Finder | 5.46 | Per result |  |

Sequencers — All Free

| Platform | Operations | Credits |
| --- | --- | --- |
| **[HeyReach](https://www.heyreach.io/)** | LinkedIn outreach campaign management and contact sequencing | **Free** |
| **[Instantly](https://instantly.ai/)** | Campaign management, lead import, activation controls, performance stats | **Free** |
| **[Lemlist](https://www.lemlist.com/)** | Multi-channel campaign sequencing with email and LinkedIn touches | **Free** |
| **[SmartLead](https://smartlead.ai/)** | Smartlead campaign management and lead import for outbound execution | **Free** |

* * *

## Clay Comparison

We show both Clay eras below so you can compare regardless of which plan you’re on.

## Email Finding

**Deepline: 0.1 - 0.6 cr** (0.008−0.008-0.008−0.048)Legacy Clay: 2 cr (0.07−0.07-0.07−0.15)New Clay: 2 data cr ($0.10) + actions**~55-80% cheaper**

## Email Verification

**Deepline: 0.1 - 0.2 cr** (0.008−0.008-0.008−0.016)Legacy Clay: 1 cr (0.035−0.035-0.035−0.075)New Clay: 1 data cr ($0.05) + actions**~68-80% cheaper**

## Person Enrichment

**Deepline: 0.1 - 0.6 cr** (0.008−0.008-0.008−0.048)Legacy Clay: 2-5 cr (0.07−0.07-0.07−0.375)New Clay: 2-5 data cr (0.10−0.10-0.10−0.25) + actions**~52-80% cheaper**

## Company Enrichment

**Deepline: 0.1 - 0.6 cr** (0.008−0.008-0.008−0.048)Legacy Clay: 1-2 cr (0.035−0.035-0.035−0.15)New Clay: 1-2 data cr (0.05−0.05-0.05−0.10) + actions**~52-77% cheaper**

## Phone Finding

**Deepline: 0.4 - 1.7 cr** (0.032−0.032-0.032−0.136)Legacy Clay: 3-10 cr (0.105−0.105-0.105−0.75)New Clay: 3-10 data cr (0.15−0.15-0.15−0.50) + actions**~56-82% cheaper**

## Additional Providers

**93+ integrations** including Crustdata, Exa, Firecrawl, Parallel, AI Ark, Dropleads, CRMs, sequencers, and more.

* * *

Credit values reflect the underlying provider rate with a small platform fee. BYOK users always pay their direct provider rate — $0 on Deepline. Prices may vary for dynamic operations (batch sizes, reveal add-ons, realtime enrichment), so check the CLI for the current per-tool values before large runs.
