# Forager API credentials
FORAGER_API_KEY=your_forager_api_key_here
FORAGER_ACCOUNT_ID=your_forager_account_id_here

# HubSpot Private App token
HUBSPOT_TOKEN=your_hubspot_private_app_token_here

# Anthropic / Claude API — powers ICP fit scoring + logo recognizability.
# If unset, company enrichment still runs; the two score sets are simply skipped.
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Optional: scoring model override (defaults to claude-opus-4-8).
ANTHROPIC_MODEL=claude-opus-4-8
