flask==3.0.3
requests==2.31.0
python-dotenv==1.0.1
gunicorn==22.0.0
# Phone number -> country + calling code (Deepline phone metadata fields).
phonenumbers>=8.13.0,<10.0.0
# ISO country code -> country name (so Phone Country reads "India", not "IN").
pycountry>=22.0.0
# Anthropic / Claude SDK (company scoring). Capped at the next major to stop a
# redeploy silently pulling a breaking release. Pin to the EXACT version from your
# deployed env (pip freeze) for full reproducibility.
anthropic>=0.40.0,<2.0.0
